---
milestone: v2.1
name: Import/Sync Rebuild
audited: 2026-06-30
reaudited: 2026-07-21
status: tech_debt
scores:
  requirements: 19/19 satisfied (code-complete + verified)
  phases: 6/6 (5 passed, 1 human_needed=code-complete+operational — accepted as documented operational follow-up, not a blocker)
  integration: 6/6 cross-phase connections wired end-to-end (re-confirmed live on HEAD dcad22db == origin/main, 2026-07-21)
  flows: 1/1 E2E import flow complete, 0 broken
gaps: []   # no critical blockers — every requirement resolves to a live cross-phase connection
tech_debt:
  - phase: 28-server-side-sync-all
    items:
      - "ACCEPTED FOLLOW-UP (operational, not a code defect): resume-heartbeat pg_cron is active + prod-ref-free on PROD (jobid 8) but no-ops until the one-time GUC app.supabase_url is set via the Supabase dashboard SQL editor (superuser; pooler user gets permission-denied). Self-chain (primary resume) + Phase 27 reaper work without it — degraded-but-safe, not broken. Same one-time step the existing fathom-reconcile cron needs. Requires Andrew's Supabase dashboard access; not actionable by an agent."
      - "ACCEPTED FOLLOW-UP (operational, not a code defect): live provider end-to-end proof deferred — SYNC-01 resume + SYNC-03 idempotency proven on the real TEST DB provider-agnostically, but a live provider-backed multi-slice backfill (e.g. sync 200 real Fathom calls) was not exercised because the TEST project has zero provider credentials in any env file. Will be exercised organically the first time a real account clicks 'Sync all' in production — watch the first real sync-all in the Phase 27 observable-jobs surface. Requires Andrew to supply TEST provider credentials if pre-production proof is wanted; not actionable by an agent."
---

# CallVault v2.1 — Import/Sync Rebuild — Milestone Audit

**Status:** tech_debt (no blockers; 2 accepted operational follow-ups tracked in `V2.1-COMPLETION-FOLLOWUPS.md`)

## Re-audit note (2026-07-21, milestone close)

Since the 2026-06-30 audit, the frontend has been pushed to `origin/main` and is live in production (confirmed: `HEAD == origin/main == dcad22db`, 0 commits ahead/behind). This closes tech-debt item #1 from the original audit ("frontend not pushed"). A re-run integration check (gsd-integration-checker, 2026-07-21) confirmed all 6/6 cross-phase connections still wired on current HEAD, 17/17 ImportSurface tests passing, and found that 2 of the 3 post-audit commits (`0b4ac358`, `d7e0de41`) actually fixed real gaps the original audit had flagged as deferred (sync-all credential resolution for api_key providers; a production sync-all-resume stall). No new integration gaps introduced. The remaining 2 tech-debt items (both under Phase 28) are accepted as documented operational follow-ups — not code defects, not blockers — per the phase's own VERIFICATION.md, which states "no code-level gaps." Both require Andrew-only access (provider credentials, Supabase dashboard superuser SQL) and are not actionable by an agent.

## Definition of Done — met

Reframe call import from a transient, fire-and-forget action across two forked UI codepaths into one durable, observable, provider-agnostic resource — so selection, progress, and partial-failure survive navigation, "sync all" actually syncs all, and browsing already-synced calls is cleanly separated from finding/importing new ones. **Achieved at the code level across all 6 phases. Frontend is live in production as of this close.**

## Requirements coverage — 19/19

| Category | Reqs | Status |
|---|---|---|
| IMP (sync-status foundation + idempotency) | IMP-01..04 | ✓ satisfied, verified real-DB (Phase 24, live on prod) |
| SEL (durable selection) | SEL-01,02 | ✓ satisfied (Phase 25, 11 tests) |
| TBL (unified surface) + BROWSE | TBL-01..04, BROWSE-01 | ✓ satisfied (Phase 26, live-verified via Interceptor with real Fathom data; dead-code cleanup completed post-audit) |
| JOB (observable jobs) | JOB-01..05 | ✓ satisfied, reaper live on prod+TEST (Phase 27) |
| SYNC (server-side sync-all) | SYNC-01..03 | ✓ code-complete + real-DB contract/idempotency proven; pager live on prod (inert until frontend ships); live provider proof deferred (Phase 28) |
| FAIL (partial-success + retry) | FAIL-01,02 | ✓ satisfied (Phase 29, 35 tests) |

## Phase verifications

| Phase | Status | Note |
|---|---|---|
| 24 Sync-Status Foundation | passed | 4/4; migrations + RLS hardening live on prod; real-DB integration 5/5 |
| 25 Durable Selection | passed | 6/6; tests re-run 11/11 |
| 26 Unified Import Surface | passed | 5/5 code + visual (Interceptor live check, real data) |
| 27 Observable Jobs | passed | 5/5 adversarial; reaper live, proven 5/5 on real TEST DB |
| 28 Server-Side Sync-All | human_needed | 12/12 code truths; 2 operational items (GUC, live proof) — see tech_debt |
| 29 Partial-Success & Retry | passed | 5/5; 35 tests green |

## Integration — no blockers

`ImportSurface` is a real integration hub: Phase 24 reader → Phase 25 selection → Phase 26 two-section dense surface → Phase 27 observable jobs (banner + chip) → Phase 28 sync-all → Phase 29 breakdown + retry, all reading one `useSyncJobs` stream. E2E flow (connect → search → grey-synced → durable-select → import → clear-on-job → observe → partial-failure breakdown → retry-failures) traced to real code, 0 broken flows.

## Notable integrity wins during build
- Phase 24 code-review caught 2 live criticals (orphan-table RLS, sync_jobs WITH CHECK) → fixed + re-verified.
- Phase 28 executor caught + killed a fake-pass (tests soft-skipping assertions while reporting green) → rewrote to assert real DB behavior; verifier independently re-confirmed.
- Corrected a factual error (Plaud IS sync-all-capable) → included in scope.
- TBL-03 residual dead code (3 orphaned files, 614 lines) deleted post-audit; build green.

## Verdict
No critical blockers. All 19 requirements code-complete and verified. Frontend and backend both live on prod. The 2 remaining tracked items are operational, not defects — the cron GUC is a one-time Supabase dashboard step and the live-provider proof happens organically on first real customer use. Milestone closed 2026-07-21; both items carried forward as accepted follow-ups requiring Andrew's input (not agent-actionable).
