---
phase: 26-unified-import-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/transcript-library/TranscriptTable.tsx
  - src/components/import/__tests__/ImportSurface.test.tsx
  - src/components/import/__tests__/ImportSurface.syncStatus.test.tsx
  - src/components/import/__tests__/ImportSurface.virtualization.test.tsx
autonomous: false
requirements: [TBL-04]
user_setup: []

must_haves:
  truths:
    - "A dense table page of 150+ rows scrolls smoothly without rendering every row's full subtree at once"
    - "The 'Load 10 at a time' manual cursor loop is replaced by a larger-page, virtualized/offscreen-skipping table"
    - "Wave 0 test scaffolds exist so downstream plans have a RED contract to build against"
  artifacts:
    - path: "src/components/transcript-library/TranscriptTable.tsx"
      provides: "Dense table with offscreen-row render skipping (content-visibility) or library virtualization for TBL-04"
      contains: "content-visibility"
    - path: "src/components/import/__tests__/ImportSurface.test.tsx"
      provides: "TBL-01 / BROWSE-01 structure RED scaffold"
    - path: "src/components/import/__tests__/ImportSurface.syncStatus.test.tsx"
      provides: "TBL-02 + carry-forward (per-provider grouping, merge-not-clobber, org threading) RED scaffold"
    - path: "src/components/import/__tests__/ImportSurface.virtualization.test.tsx"
      provides: "TBL-04 virtualization/perf RED scaffold or documented content-visibility decision"
  key_links:
    - from: "src/components/transcript-library/TranscriptTable.tsx"
      to: "TableRow rendering"
      via: "content-visibility:auto per row (or @tanstack/react-virtual windowing)"
      pattern: "content-visibility"
---

<objective>
Establish the TBL-04 fast-table foundation and the Wave 0 test scaffolds that downstream plans (02 build, 03 rewire, 04 delete) build against. This is the measure-first virtualization step: prove whether the zero-dependency `content-visibility: auto` path makes the dense `TranscriptTable` fast at large page sizes (50–200 rows), and ONLY add a virtualization library if measurement proves it insufficient.

Purpose: Andrew's hard requirement is "should not take forever to load… I should not have had to watch John click 'more' a million times 10 calls at a time." `TranscriptTable` does NOT virtualize today — it does `sortedCalls.map()` over the whole array with per-row `WorkspaceEntriesBatchProvider` lookups. TBL-04 is the actual perf work.

Output:
- `TranscriptTable.tsx` modified so offscreen rows skip render/layout cost (default: `content-visibility: auto` per `<TableRow>`, zero new dependency).
- Three failing test scaffolds under `src/components/import/__tests__/` that define the contracts Plans 02/03 implement.
- A blocking checkpoint ONLY if measurement forces a library install (default path adds nothing).
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-unified-import-surface/26-RESEARCH.md
@src/CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase this session — no exploration required. -->

TranscriptTable props (src/components/transcript-library/TranscriptTable.tsx:83-115):
- calls: Meeting[]; selectedCalls: (number | string)[] — matched by String(call.recording_id)
- onSelectCall(id: number|string), onSelectAll(), onCallClick(call), onPageChange?, onPageSizeChange?
- isUnsyncedView?: boolean (checkbox column for find-style selection)
- totalCount?, page?, pageSize? — drives PaginationControls
- Renders <Table><TableHeader><TableBody> shadcn primitives; iterates sortedCalls.map() (NO virtualization today, verified line 332); sort via useTableSort (line 149); per-row TranscriptTableRow inside WorkspaceEntriesBatchProvider

Pitfall 4 (RESEARCH.md): virtualizing a semantic <Table>/<TableBody> with absolute positioning breaks column alignment + sticky header. content-visibility:auto on each <TableRow> is the least-invasive approach — keeps the <table> markup intact.

Token rules (src/CLAUDE.md): shadcn semantic tokens only (text-foreground, bg-card, border-border, text-vibe-orange). Remix Icons only. No framer-motion (use motion/react). npm only.

RTK note (verified Phases 24/25): RTK blanks vitest/tsc/grep output. Run ALL verification via `rtk proxy <cmd>` or `/usr/bin/grep` directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Wave 0 test scaffolds (RED contracts for Plans 02/03)</name>
  <read_first>
    - src/components/transcripts/UnsyncedMeetingsSection.tsx (the proven TranscriptTable reuse template — Meeting→row map, selection keys)
    - src/components/transcripts/SyncedTranscriptsSection.tsx (browse-synced section shape + props)
    - src/services/sync-status.service.ts (getSyncStatusForExternalIds signature: (sourceApp, externalIds, {organizationId}) => Map<string, {recordingUuid, hasWorkspaceEntries}>)
    - src/hooks/useImportSelection.ts (the durable hook API Plan 02 will consume)
    - src/hooks/__tests__/useImportSelection.test.ts (existing vitest + @testing-library/react pattern, vi.spyOn on getSyncStatusForExternalIds)
  </read_first>
  <files>src/components/import/__tests__/ImportSurface.test.tsx, src/components/import/__tests__/ImportSurface.syncStatus.test.tsx, src/components/import/__tests__/ImportSurface.virtualization.test.tsx</files>
  <action>
    Create three vitest + @testing-library/react test files that import the not-yet-existing `<ImportSurface>` from `@/components/import/ImportSurface` (these MUST fail at module-resolution / behavior level — they are the RED contract Plan 02 turns GREEN).

    ImportSurface.test.tsx (TBL-01 / BROWSE-01): assert the surface renders TWO stacked sections — a find-new section (heading text like "Available to import" / "New to import") ABOVE a browse-synced section (heading "Already in your vault" / "Synced Transcripts"), each rendering a TranscriptTable. Assert the find-new section renders a checkbox/selectable table (isUnsyncedView) and the browse section is the de-emphasized archive. Mock provider adapter + hooks as needed.

    ImportSurface.syncStatus.test.tsx (TBL-02 + carry-forward triple): assert that after a search returns rows for a NON-Fathom provider (e.g. sourceApp="zoom"), the surface calls getSyncStatusForExternalIds with the REAL per-row source_app (NOT literal "fathom") AND threads organizationId through opts. Assert merge-not-clobber semantics: when two providers' rows are present, marking one provider's row synced does NOT reset the other provider's rows to unsynced. Use vi.spyOn on getSyncStatusForExternalIds. These three assertions encode CR-02 (real source_app), WR-01 (merge not clobber), WR-02 (org threading) from .planning/phases/24-sync-status-foundation/deferred-items.md.

    ImportSurface.virtualization.test.tsx (TBL-04): assert the dense table does NOT eagerly mount all rows' heavy subtrees for a large (150+ row) page — OR, if the content-visibility path is chosen, assert each rendered TableRow carries the content-visibility style and document the perf decision in a top-of-file comment. Keep the assertion approach resilient to jsdom (jsdom does not lay out content-visibility, so prefer asserting the style attribute / class presence rather than actual offscreen culling).

    Use shadcn semantic tokens / Remix Icons if any markup is asserted. Do not inline production implementation — these are tests only.
  </action>
  <verify>
    <automated>rtk proxy npx vitest run src/components/import/__tests__/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under src/components/import/__tests__/ (verify: `/usr/bin/ls src/components/import/__tests__/`).
    - Running `rtk proxy npx vitest run src/components/import/__tests__/` shows the suites FAILING (RED) — module `@/components/import/ImportSurface` not found OR behavior assertions unmet. RED is the expected, correct state for this task.
    - ImportSurface.syncStatus.test.tsx references getSyncStatusForExternalIds and asserts a non-"fathom" sourceApp argument: `/usr/bin/grep -c 'getSyncStatusForExternalIds' src/components/import/__tests__/ImportSurface.syncStatus.test.tsx` >= 1 and `/usr/bin/grep -c 'organizationId' src/components/import/__tests__/ImportSurface.syncStatus.test.tsx` >= 1.
    - No `parseInt`/`Number(` coercion of external ids in any test file: `/usr/bin/grep -cE 'parseInt|Number\(' src/components/import/__tests__/*.tsx` reads 0.
  </acceptance_criteria>
  <done>Three RED test scaffolds committed; they encode TBL-01, TBL-02, BROWSE-01, TBL-04, and the carry-forward triple as failing contracts for Plan 02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add offscreen-row render skipping to TranscriptTable (TBL-04, zero-dep default)</name>
  <read_first>
    - src/components/transcript-library/TranscriptTable.tsx (FULL file — the map-over-sortedCalls render loop, TableBody, TableRow usage, PaginationControls, useTableSort)
    - src/components/transcript-library/TranscriptTableRow.tsx (the per-row component whose render cost we are skipping offscreen)
  </read_first>
  <files>src/components/transcript-library/TranscriptTable.tsx</files>
  <behavior>
    - A large page (e.g. pageSize 100–200) renders without paying full layout cost for offscreen rows: each rendered TableRow carries `content-visibility: auto` plus a `contain-intrinsic-size` height hint (matching the dense row height h-10/h-12) so scroll height stays stable.
    - Existing behavior preserved: sort, column visibility, selection (selectedCalls matched by String(recording_id)), pagination controls, sticky header alignment all unchanged.
    - isUnsyncedView checkbox column still renders correctly.
  </behavior>
  <action>
    Apply the least-invasive TBL-04 approach (Pitfall 4): add `content-visibility: auto` and a `contain-intrinsic-size` block-size hint to each rendered row in the TranscriptTable body (style or a utility class on `<TableRow>` / the row wrapper). Set the intrinsic-size to the dense row height so the scrollbar does not jump as rows cull/uncull. Do NOT switch to absolute positioning or a CSS-grid table — keep the semantic `<Table>` markup so column alignment and the sticky header survive.

    Keep the existing `sortedCalls.map()` + `useTableSort` + `PaginationControls` intact. Raise the default/allowed page size options where pagination size is offered so a user can load 50/100/200 instead of 10 (kill the "10 at a time" complaint) — but do not change the existing onPageChange/onPageSizeChange contract. Use shadcn semantic tokens; no new icon library; no framer-motion.

    Do NOT add any npm dependency in this task. If, after this change, the virtualization test scaffold or a manual measure proves content-visibility is insufficient for the target page sizes, STOP and surface that finding — Task 3 (the checkpoint) is the only path to add a library.
  </action>
  <verify>
    <automated>rtk proxy npx tsc -p tsconfig.app.json 2>&1 | /usr/bin/grep -c "TranscriptTable.tsx" ; rtk proxy npx vitest run src/components/import/__tests__/ImportSurface.virtualization.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'content-visibility' src/components/transcript-library/TranscriptTable.tsx` >= 1 (offscreen skip present).
    - `/usr/bin/grep -c 'contain-intrinsic-size' src/components/transcript-library/TranscriptTable.tsx` >= 1 (stable scroll height hint present).
    - No new import of `@tanstack/react-virtual` / `react-window` / `react-virtuoso` in this task: `/usr/bin/grep -cE 'react-virtual|react-window|react-virtuoso' src/components/transcript-library/TranscriptTable.tsx` reads 0.
    - tsc shows zero NEW errors in TranscriptTable.tsx (run `rtk proxy npx tsc -p tsconfig.app.json` and confirm TranscriptTable.tsx is not newly listed; pre-existing baseline errors in unrelated files are out of scope per the Scope Boundary rule).
    - No `framer-motion` / Lucide import introduced: `/usr/bin/grep -cE "framer-motion|lucide-react" src/components/transcript-library/TranscriptTable.tsx` reads 0.
  </acceptance_criteria>
  <done>TranscriptTable skips offscreen row layout via content-visibility with a stable intrinsic-size hint; larger page sizes are selectable; zero new dependencies; existing sort/selection/pagination/sticky-header behavior intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [CHECKPOINT] Approve a virtualization-library install ONLY if content-visibility is insufficient</name>
  <action>PAUSE for human approval before installing any npm package. Default outcome: ZERO new dependencies (content-visibility from Task 2 stands). Do NOT run npm install without an explicit approval response. This checkpoint is never auto-approved (Andrew's npm/dependency policy).</action>
  <what-built>
    Task 2 applied the zero-dependency `content-visibility: auto` TBL-04 approach to TranscriptTable (no new npm package). This checkpoint exists ONLY to gate the EXCEPTION: if the content-visibility path is measured insufficient and a virtualization library (`@tanstack/react-virtual`) must be added instead. Per Andrew's npm/dependency policy, adding ANY new npm dependency requires explicit human approval — it is never done silently.
  </what-built>
  <how-to-verify>
    DEFAULT PATH (no library needed): If the content-visibility approach from Task 2 is acceptable (smooth scroll at 100–200 rows, no new package), respond "approved — no library" and this phase adds ZERO dependencies.

    LIBRARY PATH (only if content-visibility measured insufficient):
    1. Run the legitimacy gate before any install: `npm view @tanstack/react-virtual version` and confirm the package on https://www.npmjs.com/package/@tanstack/react-virtual (same TanStack org as the already-installed TanStack Query).
    2. Confirm you want `@tanstack/react-virtual` added to package.json (it is the headless option that keeps the existing <Table> markup — Pitfall 4).
    3. Respond "approved — install @tanstack/react-virtual" to authorize `npm install @tanstack/react-virtual`, after which the executor retrofits the table per the @tanstack/react-virtual table recipe (measure rows, translate tbody, single column-width source).
  </how-to-verify>
  <resume-signal>Reply "approved — no library" (default) OR "approved — install @tanstack/react-virtual" OR describe a different choice.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | A new package install crosses an untrusted supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-SC | Tampering | Optional `@tanstack/react-virtual` install | mitigate | Default path adds NO dependency. Library path gated behind blocking-human checkpoint + `npm view` legitimacy check + npmjs.com verification (never auto-approved). |
| T-26-01-DoS | Denial of Service | Rendering 200+ heavy rows at once | mitigate | content-visibility:auto skips offscreen layout; contain-intrinsic-size keeps scroll stable so a large page does not freeze the tab. |
</threat_model>

<verification>
- `rtk proxy npx vitest run src/components/import/__tests__/` — scaffolds present (RED for ImportSurface-dependent suites is expected pre-Plan-02; virtualization suite reflects Task 2's chosen approach).
- `rtk proxy npx tsc -p tsconfig.app.json` — no NEW errors in TranscriptTable.tsx.
- No new npm dependency unless the checkpoint approved one.
</verification>

<success_criteria>
- TranscriptTable skips offscreen row layout (content-visibility) with a stable intrinsic-size hint; larger page sizes selectable; "10 at a time" loop is gone at the table level.
- Three Wave 0 test scaffolds exist as RED contracts for TBL-01/02/04 + BROWSE-01 + the carry-forward triple.
- Zero new dependencies on the default path; any library addition went through the blocking-human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/26-unified-import-surface/26-01-SUMMARY.md` when done.
</output>
