---
phase: 26-unified-import-surface
plan: 03
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - src/pages/ImportPage.tsx
  - src/pages/TranscriptsNew.tsx
autonomous: true
requirements: [TBL-01]
user_setup: []

must_haves:
  truths:
    - "The Import tab renders <ImportSurface> in its connector branch (replacing <ConnectorImportWizard>)"
    - "The Sync tab renders <ImportSurface> (replacing the <SyncTab> body)"
    - "Both surfaces are the SAME component — one paging model, one selection store"
    - "The app boots without a white screen — OAUTH_CALLBACK_ROUTES stays non-empty against a committed-tree build"
  artifacts:
    - path: "src/pages/ImportPage.tsx"
      provides: "Connector branch wired to <ImportSurface> instead of <ConnectorImportWizard>"
      contains: "ImportSurface"
    - path: "src/pages/TranscriptsNew.tsx"
      provides: "Sync tab wired to <ImportSurface> instead of <SyncTab>"
      contains: "ImportSurface"
  key_links:
    - from: "src/pages/ImportPage.tsx"
      to: "src/components/import/ImportSurface.tsx"
      via: "connector branch render with sourceApp + activeOrgId"
      pattern: "ImportSurface"
    - from: "src/pages/TranscriptsNew.tsx"
      to: "src/components/import/ImportSurface.tsx"
      via: "TabsContent value=sync render"
      pattern: "ImportSurface"
---

<objective>
Rewire BOTH consumers to `<ImportSurface>`: the Import tab (`ImportPage`'s connector branch, replacing `<ConnectorImportWizard>`) and the Sync tab (`TranscriptsNew`'s `TabsContent value="sync"`, replacing `<SyncTab>`). This MUST happen BEFORE any deletion (Plan 04) — the wizard, SyncTab, and useSyncTab* hooks stay on disk this plan so the build never breaks (RESEARCH "Safe deletion order" Wave A steps 3-5).

Purpose: TBL-01 — the same component renders in both tabs (one paging model, one selection store, one progress UI). This is the cutover; deletion is the separate late wave.

Output:
- `ImportPage.tsx` connector branch renders `<ImportSurface>` (passing sourceApp + sourceId + activeOrgId).
- `TranscriptsNew.tsx` sync tab renders `<ImportSurface>` for the user's active provider context.
- `npm run build` green so the boot-crash gate (OAUTH_CALLBACK_ROUTES) passes — no white screen.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-unified-import-surface/26-RESEARCH.md
@src/CLAUDE.md

<interfaces>
<!-- Extracted this session — use directly. -->

ImportPage connector branch (src/pages/ImportPage.tsx:152-171): the `isConnectorWizardImportSource(selectedSource)` branch currently renders <ConnectorImportWizard sourceApp={selectedSource} onImportComplete={...}> inside a scrollable Pane-3 div. Org context available via const { activeOrgId } = useOrgContext() (already imported at line 68). onImportComplete invalidates queryKeys.calls.all / imports.counts() / imports.failed().

TranscriptsNew sync tab (src/pages/TranscriptsNew.tsx): line 10 imports SyncTab; line 383-385 renders <SyncTab /> inside <TabsContent value="sync" className="mt-0 h-full overflow-auto absolute inset-0 p-4 md:p-10">.

<ImportSurface> props (from Plan 02): { sourceApp, sourceId, workspaceId, organizationId } — owns its own date-range state + DateRangePicker; consumes useImportSelection + the overlay; renders the two-section dense surface. (Confirm the exact prop names in the Plan 02 SUMMARY / the ImportSurface.tsx file before wiring.)

Boot-crash gate (RESEARCH Runtime State Inventory + STATE.md): source-registry.ts oauthCallbackFunctionName entries drive OAUTH_CALLBACK_ROUTES at module init; an empty array white-screens prod (the FALLBACK_ROUTE IIFE throws, oauth-callback-routing.ts:50-60). Test: src/lib/__tests__/oauth-callback-routing.test.ts asserts OAUTH_CALLBACK_ROUTES.length non-zero. Build MUST be run against the COMMITTED tree before claiming boot-safe.

RTK note: RTK blanks build/vitest/tsc/grep — run via `rtk proxy` or `/usr/bin/grep`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire ImportPage connector branch to <ImportSurface></name>
  <read_first>
    - src/pages/ImportPage.tsx (FULL — the connector branch at 152-171, activeOrgId at 68, OAuth-return effect at 78-134, isConnectorWizardImportSource gating)
    - src/components/import/ImportSurface.tsx (the Plan 02 component + its exact prop shape)
    - .planning/phases/26-unified-import-surface/26-02-SUMMARY.md (confirm ImportSurface prop names + how sourceId/workspaceId are obtained)
  </read_first>
  <files>src/pages/ImportPage.tsx</files>
  <action>
    In the `isConnectorWizardImportSource(selectedSource)` branch, replace `<ConnectorImportWizard sourceApp={selectedSource} onImportComplete={...}>` with `<ImportSurface sourceApp={selectedSource} organizationId={activeOrgId} ...>`, passing whatever sourceId/workspaceId context ImportSurface expects (ImportSurface owns connection-status + workspace picking internally per Plan 02 — pass only what its prop contract requires). Preserve the existing scrollable Pane-3 wrapper div and the cache-invalidation behavior on import completion (wire ImportSurface's import-complete affordance to the same queryClient.invalidateQueries calls if ImportSurface exposes a callback; otherwise rely on ImportSurface's own invalidation and keep the page-level behavior minimal).

    Add the `import { ImportSurface } from "@/components/import/ImportSurface"` import. Do NOT remove the `ConnectorImportWizard` import or file yet — leave the wizard on disk (Plan 04 deletes it after both consumers are rewired). Keep the OAuth-return effect and isConnectorWizardImportSource gating unchanged. Use shadcn tokens; no new icon lib.
  </action>
  <verify>
    <automated>rtk proxy npx tsc -p tsconfig.app.json 2>&1 | /usr/bin/grep -c "ImportPage.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - ImportPage renders ImportSurface in the connector branch: `/usr/bin/grep -c 'ImportSurface' src/pages/ImportPage.tsx` >= 2 (import + usage).
    - organizationId/activeOrgId threaded to the surface: `/usr/bin/grep -c 'activeOrgId' src/pages/ImportPage.tsx` >= 1.
    - tsc shows zero errors in ImportPage.tsx (grep on tsc output reads 0).
    - Wizard not deleted this plan (still importable): `/usr/bin/test -f src/components/connectors/ConnectorImportWizard.tsx` succeeds.
  </acceptance_criteria>
  <done>The Import tab connector branch renders <ImportSurface> with org context threaded; the wizard remains on disk; ImportPage type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire TranscriptsNew sync tab to <ImportSurface> + build-green boot gate</name>
  <read_first>
    - src/pages/TranscriptsNew.tsx (FULL — the sync TabsContent at 383-385, SyncTab import at line 10, how the active provider/org context is available on this page)
    - src/components/transcripts/SyncTab.tsx (what SyncTab provided: activeOrganizationId via useOrganizationContext, the two-section shape ImportSurface now owns)
    - src/components/import/ImportSurface.tsx (prop contract)
    - src/lib/__tests__/oauth-callback-routing.test.ts (the OAUTH_CALLBACK_ROUTES.length boot assertion)
  </read_first>
  <files>src/pages/TranscriptsNew.tsx</files>
  <action>
    Replace `<SyncTab />` inside `<TabsContent value="sync" ...>` with `<ImportSurface ...>`, passing the active provider sourceApp + organizationId the page has in context (the Sync tab historically defaulted to the user's connected providers — pass the appropriate sourceApp; if the Sync tab must remain multi-provider, render the surface for the active/selected provider and confirm the chosen approach against the ImportSurface prop contract). Add the ImportSurface import. Preserve the TabsContent wrapper classes (mt-0 h-full overflow-auto absolute inset-0 p-4 md:p-10) so AppShell pane behavior is unchanged.

    Do NOT delete SyncTab.tsx or the useSyncTab* hooks this plan — they stay on disk until Plan 04. After wiring, run the full build against the committed tree to prove the boot-crash gate passes.

    Use shadcn tokens; Remix Icons only; no framer-motion.
  </action>
  <verify>
    <automated>rtk proxy npx tsc -p tsconfig.app.json 2>&1 | /usr/bin/grep -c "TranscriptsNew.tsx" ; rtk proxy npx vitest run src/lib/__tests__/oauth-callback-routing.test.ts 2>&1 | tail -8 ; rtk proxy npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - TranscriptsNew renders ImportSurface in the sync tab: `/usr/bin/grep -c 'ImportSurface' src/pages/TranscriptsNew.tsx` >= 2 (import + usage).
    - `rtk proxy npm run build` exits zero (boot-crash gate green; OAUTH_CALLBACK_ROUTES non-empty against the committed tree).
    - `rtk proxy npx vitest run src/lib/__tests__/oauth-callback-routing.test.ts` passes (OAUTH_CALLBACK_ROUTES.length assertion holds).
    - tsc shows zero errors in TranscriptsNew.tsx (grep on tsc output reads 0).
    - SyncTab + hooks still on disk: `/usr/bin/test -f src/components/transcripts/SyncTab.tsx && /usr/bin/test -f src/hooks/useSyncTabState.ts` succeeds.
  </acceptance_criteria>
  <done>Both consumers now render <ImportSurface>; the build is green against the committed tree (boot gate passes); the wizard, SyncTab, and useSyncTab* hooks remain on disk for Plan 04 to delete. TBL-01 cutover complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module init → React mount | source-registry.ts / OAUTH_CALLBACK_ROUTES is load-bearing at boot; an empty array crashes the app |
| page context → surface | organizationId must be threaded so the surface scopes synced-status to the caller's org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-DoS | Denial of Service | OAUTH_CALLBACK_ROUTES boot init | mitigate | `npm run build` against the committed tree + oauth-callback-routing.test.ts run as the boot gate before claiming boot-safe (prod white-screen guard). |
| T-26-03-ID | Information Disclosure | org threading at call sites | mitigate | ImportPage passes activeOrgId; TranscriptsNew passes the page's organizationId — surface cannot read cross-org synced status. |
| T-26-03-SC | Tampering | npm/connector churn | accept | No package installs in this plan; build gate covers boot integrity. |
</threat_model>

<verification>
- `rtk proxy npm run build` — zero exit (boot gate).
- `rtk proxy npx vitest run src/lib/__tests__/oauth-callback-routing.test.ts` — passes.
- `rtk proxy npx tsc -p tsconfig.app.json` — zero NEW errors in the two rewired pages.
- Manual smoke (executor, dev-browser per CLAUDE.md): both tabs render the surface; selection persists across a nav round-trip.
</verification>

<success_criteria>
- Import tab and Sync tab both render <ImportSurface> (TBL-01 cutover done).
- Build green against committed tree; OAUTH_CALLBACK_ROUTES non-empty (no white screen).
- Wizard, SyncTab, and useSyncTab* hooks still on disk — deletion is Plan 04.
</success_criteria>

<output>
Create `.planning/phases/26-unified-import-surface/26-03-SUMMARY.md` when done.
</output>
