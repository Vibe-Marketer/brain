---
phase: 28-server-side-sync-all
plan: 02
type: execute
wave: 3
depends_on: ["28-01", "28-03"]
files_modified:
  - supabase/functions/connector-sync-all/index.ts
autonomous: true
requirements: [SYNC-01, SYNC-03]
must_haves:
  truths:
    - "The pager processes exactly ONE provider page per invocation, persists provider_cursor + progress to sync_jobs, heartbeats, then self-chains — never a whole-batch waitUntil loop"
    - "A user-started invocation authenticates via JWT; a cron/service-role resume operates on the job row's stored organization_id/user_id and never on a caller-supplied org"
    - "A 23505 unique-violation from runPipeline is reclassified as skipped (counted in skipped_count), never recorded in failed_ids"
    - "organization_id is written onto the job and passed into runPipeline so the org-scoped dedup constraint is hit deterministically"
    - "listPage is resolved from the POPULATED connector-list-page-registry.ts (built in Plan 03), not from the contract-only connector-list-page.ts"
  artifacts:
    - path: "supabase/functions/connector-sync-all/index.ts"
      provides: "provider-agnostic one-page-per-invocation checkpoint/resume pager; owns the conservative SLICE_ITEM_BUDGET const"
      contains: "connector-sync-all"
  key_links:
    - from: "supabase/functions/connector-sync-all/index.ts"
      to: "supabase/functions/_shared/connector-pipeline.ts runPipeline"
      via: "per-item upsert (dedup primitive reused verbatim)"
      pattern: "runPipeline"
    - from: "supabase/functions/connector-sync-all/index.ts"
      to: "supabase/functions/_shared/connector-list-page-registry.ts populated resolver (Plan 03)"
      via: "resolve listPage by sync_jobs.source_app, round-trip opaque cursor"
      pattern: "connector-list-page-registry"
    - from: "supabase/functions/connector-sync-all/index.ts"
      to: "sync_jobs.provider_cursor"
      via: "checkpoint write each slice + self-chain via functions.invoke"
      pattern: "provider_cursor"
---

<objective>
Build the provider-agnostic checkpoint/resume pager edge function `connector-sync-all` (SYNC-01) that processes exactly one provider page per invocation, reuses runPipeline for idempotency (SYNC-03), and self-chains the next slice — eliminating the silent mid-run death of the sync-meetings whole-batch waitUntil loop. This plan runs in Wave 3, AFTER the populated listPage registry (Plan 03, Wave 2) exists, because the pager imports the POPULATED resolver, not the contract-only file.

Purpose: This is the load-bearing correctness change for the whole milestone — "sync all" must actually sync all, decoupled from UI scroll, resumable after a kill, with no duplicates under concurrency. The pager owns the cursor; the DB row IS the resume point.
Output: One new edge function with a dual auth path (JWT start + service-role/cron resume), one-page-per-slice processing, 23505→skipped reclassification, a conservative SLICE_ITEM_BUDGET const (owned here; tuned in place by Plan 04), and failure recording for Phase 29.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-server-side-sync-all/28-RESEARCH.md
@.planning/phases/28-server-side-sync-all/28-CONTEXT.md
@supabase/CLAUDE.md
@.planning/phases/28-server-side-sync-all/28-01-PLAN.md
@.planning/phases/28-server-side-sync-all/28-03-PLAN.md

<read_first>
- supabase/functions/_shared/connector-list-page.ts (Plan 01 contract: ListPageResult/ListPageParams/ListPageFn TYPES — import these types for typing).
- supabase/functions/_shared/connector-list-page-registry.ts (Plan 03 POPULATED resolver: the source_app→listPage map the pager resolves against at runtime. THIS is the file the pager imports for resolution — NOT the contract-only connector-list-page.ts. It exists only after Plan 03, which is why this plan depends_on 28-03 and runs in a later wave).
- supabase/functions/_shared/connector-pipeline.ts — runPipeline(supabase, userId, record): returns {success, skipped?, error?}; checkDuplicate is owner_user_id-scoped (fast-path) while the DB constraint is org-scoped → concurrent slices can both pass the check, the 2nd insert throws 23505 (RESEARCH Pitfall 1). insertRecording throws on the constraint. ConnectorRecord requires non-empty external_id (RESEARCH Pitfall 2 — assert before calling).
- supabase/functions/sync-meetings/index.ts — REUSE its provider-fetch + connector-pipeline upsert + heartbeat write shape, but INVERT it: do NOT copy the maxPages=100 loop inside one EdgeRuntime.waitUntil (the anti-pattern). Read its sync_jobs INSERT/UPDATE column writes and validateRequestedWorkspaceId IDOR gate.
- supabase/functions/_shared/auth.ts — authenticateRequest(req, supabase, corsHeaders) shared JWT helper for the user-start path (do NOT inline JWT parsing).
- supabase/functions/_shared/connector-function-utils.ts — resolveOAuthAccessToken (per-provider token; must be the JOB OWNER's token, not a caller's) + getConnectorDateWindow.
- supabase/migrations/20260620120000_sync_jobs_durable_resource.sql — exact sync_jobs columns: provider_cursor, mode, date_start, date_end, organization_id, workspace_id, source_app, source_id, last_heartbeat_at, status, synced_ids, failed_ids, skipped_count.
- supabase/migrations/20251128100000_embedding_queue_system.sql lines 261-271 — the cron net.http_post shape that will call this fn with NO Authorization header (drives the service-role resume branch requirement).
- supabase/migrations/20260623120000_sync_jobs_reaper.sql — 5-min stale-heartbeat fail threshold the pager must stay under (heartbeat every slice).
</read_first>

<interfaces>
sync_jobs row (the cursor + progress source of truth):
  id, status ('pending'|'processing'|'completed'|'completed_with_errors'|'failed'),
  mode ('all' for sync-all), source_app, source_id, organization_id, workspace_id,
  date_start, date_end, provider_cursor (opaque string | null), last_heartbeat_at,
  synced_ids (string[]), failed_ids (string[]), skipped_count (int), error (text), completed_at

listPage resolution (Plan 03):
  import the POPULATED resolver from connector-list-page-registry.ts → resolveListPage(source_app): ListPageFn | undefined
  (the contract-only connector-list-page.ts has NO populated entries; importing the resolver from there would resolve nothing)

runPipeline(supabase, userId, record: ConnectorRecord): Promise<{success, recordingId?, skipped?, error?}>
  - skipped === true  → duplicate (count in skipped_count, NOT failure)
  - success === false + error matching 23505/unique-violation → reclassify as skipped (Pitfall 1)
  - record.organization_id MUST be set from the job row so the org-scoped constraint is hit

SLICE_ITEM_BUDGET:
  A single named const OWNED by this file. This plan sets a CONSERVATIVE default (20) with a comment that Plan 04's measure-first task adjusts this exact const in place using real TEST latency. This file is the sole owner of the const; Plan 04 edits the value here (28-02's index.ts is in Plan 04's files_modified to make that cross-plan touch valid and clean).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dual-auth entry + one-page slice loop + cursor checkpoint</name>
  <files>supabase/functions/connector-sync-all/index.ts</files>
  <read_first>
  - supabase/functions/_shared/auth.ts (authenticateRequest)
  - supabase/functions/sync-meetings/index.ts (provider-fetch + sync_jobs write shape; the anti-pattern loop to avoid)
  - supabase/functions/_shared/connector-list-page-registry.ts (Plan 03 POPULATED resolver — import for runtime listPage resolution)
  - supabase/functions/_shared/connector-list-page.ts (Plan 01 contract TYPES only)
  - supabase/migrations/20260620120000 (sync_jobs columns)
  </read_first>
  <action>
  Create supabase/functions/connector-sync-all/index.ts (kebab-case, sync* prefix) as a Deno.serve edge function with TWO authorization paths per RESEARCH Open Question 2 + the locked cron-auth decision:

  - USER-START path: body carries a JWT (Authorization header). Validate via authenticateRequest. Zod-validate the start payload (sourceId, source_app, dateStart, dateEnd). Resolve the user's organization_id and WRITE it onto the sync_jobs row at creation (locked decision — org_id at creation so dedup hits the org-scoped constraint deterministically). Create the sync_jobs row with mode='all', status='processing', provider_cursor=NULL, source_app, source_id, organization_id, workspace_id, date_start/end. Then process the FIRST slice (below) and return jobId immediately.
  - CRON/SERVICE-ROLE RESUME path: body carries { jobId } and NO user JWT (pg_net from cron). Detect the absence of a caller JWT and authorize via service role by LOADING the job row and operating ONLY on its stored organization_id/user_id — never a caller-supplied org. Reject if the job row's mode != 'all' or status != 'processing'. This is the IDOR boundary: the resume path must not let any caller advance another org's job into their own data.

  Core slice logic (shared by both paths), per RESEARCH Pattern 1 — process exactly ONE provider page per invocation, NEVER a for-loop over all pages inside waitUntil:
  1. Load the job row (provider_cursor, dates, source_app, organization_id, user_id).
  2. Resolve the provider's listPage via the POPULATED resolver imported from connector-list-page-registry.ts (Plan 03), keyed by source_app. (Do NOT import a resolver from the contract-only connector-list-page.ts — it has no populated entries.) Resolve the JOB OWNER's OAuth/access token via resolveOAuthAccessToken (never a caller's token).
  3. Call listPage({ accessToken, cursor: provider_cursor, dateStart, dateEnd }) → { items, nextCursor }.
  4. For each item: assert non-empty external_id (Pitfall 2 — never pass empty to runPipeline); map to ConnectorRecord WITH organization_id from the job row; call runPipeline(supabase, jobOwnerUserId, record).
  5. Cap detail-fetch+upsert work at a conservative per-slice item budget via a single named const SLICE_ITEM_BUDGET (set it to a conservative 20 here; Read.ai naturally ~10/page) — if a single list page exceeds the budget, process a sub-batch and encode a within-page offset in provider_cursor so the next slice continues (RESEARCH Finding 3). Comment the const: "Conservative default. Plan 04's measure-first task tunes THIS const in place from real TEST per-slice latency; this file is the sole owner of SLICE_ITEM_BUDGET." Do NOT scatter the magic number — one const, used once.
  6. Write sync_jobs: append to synced_ids, append failures to failed_ids, increment skipped_count, set provider_cursor = nextCursor, set last_heartbeat_at = now() (heartbeat EVERY slice — reaper net). If nextCursor is null, set status = (failed_ids.length ? 'completed_with_errors' : 'completed') and completed_at = now().
  7. If nextCursor is not null: SELF-CHAIN — fire-and-forget supabase.functions.invoke('connector-sync-all', { body: { jobId } }) (do NOT await) and return HTTP immediately. The cron resume-heartbeat (Plan 04) re-kicks if the self-chain link drops.

  Reuse validateRequestedWorkspaceId from sync-meetings on the start path (IDOR gate, per phase-spanning knowledge). Do NOT log access tokens or headers.
  </action>
  <verify>
    <automated>rtk grep -c "authenticateRequest\|provider_cursor\|functions.invoke\|runPipeline\|SLICE_ITEM_BUDGET\|connector-list-page-registry" supabase/functions/connector-sync-all/index.ts; rtk grep -c "maxPages\|for .*pages" supabase/functions/connector-sync-all/index.ts</automated>
  </verify>
  <acceptance_criteria>
  - connector-sync-all/index.ts exists; processes exactly one page per invocation (grep shows NO maxPages / page-loop; the anti-pattern is absent).
  - The pager imports the POPULATED resolver from connector-list-page-registry.ts (grep shows connector-list-page-registry); it does NOT resolve listPage from the contract-only connector-list-page.ts.
  - Both auth paths present: authenticateRequest used on JWT start; a service-role branch that loads the job row and uses its stored organization_id/user_id when no caller JWT is present.
  - org_id written onto the job at creation and passed into the ConnectorRecord; provider_cursor checkpoint + last_heartbeat_at written each slice; self-chain via functions.invoke when nextCursor != null.
  - SLICE_ITEM_BUDGET is a single named const owned by this file with the conservative default and the "Plan 04 tunes in place" comment.
  - No access token / header logged.
  </acceptance_criteria>
  <done>The pager runs one slice, resolves listPage from the populated registry, checkpoints provider_cursor, heartbeats, and self-chains; dual auth enforced; the whole-batch loop is provably absent.</done>
</task>

<task type="auto">
  <name>Task 2: 23505 reclassification + skipped-not-failed + failure recording</name>
  <files>supabase/functions/connector-sync-all/index.ts</files>
  <read_first>
  - supabase/functions/_shared/connector-pipeline.ts lines 91-104 (owner_user_id-scoped fast check) + 259-282 (insert throws on the org-scoped constraint) — the exact race in RESEARCH Pitfall 1.
  - supabase/migrations/20260303000004_add_source_call_id.sql (recordings_source_dedup constraint name).
  </read_first>
  <action>
  Harden the per-item classification in the slice loop per SYNC-03 + RESEARCH Pitfall 1:
  - runPipeline returning { skipped: true } → increment skipped_count (already success-equivalent; NEVER count as failure).
  - runPipeline returning { success: false } whose error indicates a Postgres unique-violation (code 23505 / "duplicate key" / the recordings_source_dedup constraint) → RECLASSIFY as skipped (increment skipped_count), do NOT push to failed_ids. This is the concurrent-slice loser path: two slices (or selective-import + sync-all) both passed the owner_user_id-scoped checkDuplicate, the 2nd insert hit the org-scoped constraint. The winner imported the call; the loser must report skipped, not failed — otherwise Phase 29 surfaces a phantom failure.
  - Only genuine errors (token expiry, network, mapping) push the item's external_id to failed_ids with no coercion (TEXT ids).
  - Ensure failed_ids and skipped_count survive every slice write so Phase 29 (FAIL) can read partial-success truth.
  Detect 23505 robustly (Supabase error shape may surface code on error.code or the message string from the thrown insert error) — match defensively, not on one exact substring.
  </action>
  <verify>
    <automated>rtk grep -c "23505\|skipped_count\|failed_ids" supabase/functions/connector-sync-all/index.ts</automated>
  </verify>
  <acceptance_criteria>
  - A 23505/unique-violation from runPipeline increments skipped_count and is absent from failed_ids (objective: concurrent selective-import + sync-all of the same call yields exactly 1 recordings row and the loser is reclassified skipped, not failed — proven by Plan 01's idempotency integration test going GREEN, executed in Plan 05).
  - skipped (duplicate) is never counted as a failure.
  - failed_ids holds only genuine errors, ids uncoerced.
  </acceptance_criteria>
  <done>Per-item classification distinguishes synced / skipped (incl. 23505) / failed; failure record persists each slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/user → connector-sync-all (start) | Untrusted JWT-bearing caller; org + workspace must be membership-validated |
| pg_cron/pg_net → connector-sync-all (resume) | No JWT; service-role context — must bind to the job row's stored org/user only |
| connector-sync-all → provider API | Outbound with the job owner's decrypted OAuth token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Elevation / Info disclosure | cross-org sync-all (resume path syncs into wrong org) | mitigate | service-role resume operates ONLY on the job row's stored organization_id/user_id; org_id written at creation; runPipeline org resolution; sync_jobs RLS |
| T-28-04 | Elevation (IDOR) | resume someone else's jobId | mitigate | resume path loads job row, derives owner from the row (never caller input); start path scoped by JWT + validateRequestedWorkspaceId |
| T-28-05 | Spoofing | caller-supplied org/workspace on start | mitigate | validateWorkspaceMembership / validateRequestedWorkspaceId IDOR gate reused from sync-meetings; Zod-validate payload |
| T-28-06 | Info disclosure | OAuth token / headers in logs | mitigate | never log access_token or Authorization header (matches existing fns) |
| T-28-07 | Tampering | replay/duplicate inserts on slice retry | mitigate | org-scoped unique constraint (23505) + reclassify violation as skipped |
| T-28-SC | Tampering | npm/deno installs | accept | zero new packages (RESEARCH audit N/A) |
</threat_model>

<verification>
- One page per invocation; no maxPages/whole-batch loop (grep gate).
- listPage resolved from the POPULATED connector-list-page-registry.ts (Plan 03), not the contract-only file.
- Dual auth: JWT start (authenticateRequest) + service-role resume bound to job-row org/user.
- 23505 → skipped; failed_ids holds only genuine errors; ids uncoerced.
- provider_cursor + last_heartbeat_at written each slice; self-chain on nextCursor.
- SLICE_ITEM_BUDGET single named const, conservative default, owned here.
- No token/header logging.
</verification>

<success_criteria>
A resumable, provider-agnostic pager exists that processes one page per slice (resolving listPage from the populated registry), checkpoints the cursor, reclassifies 23505 as skipped, records failures for Phase 29, and authorizes both the JWT start and the service-role resume safely. (Real-DB proof executed in Plan 05.)
</success_criteria>

<output>
Create `.planning/phases/28-server-side-sync-all/28-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
