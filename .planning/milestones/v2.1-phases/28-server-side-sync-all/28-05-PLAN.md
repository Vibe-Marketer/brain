---
phase: 28-server-side-sync-all
plan: 05
type: execute
wave: 5
depends_on: ["28-04"]
files_modified: []
autonomous: false
requirements: [SYNC-01, SYNC-02, SYNC-03]
user_setup: []
must_haves:
  truths:
    - "The real-DB concurrency/idempotency + resume integration tests (Plan 01 scaffolds) run GREEN against the TEST project with the pager + listPage deployed — not a guard skip"
    - "The listPage unit suite proves all 6 providers paginate to exhaustion (the safety net for any provider lacking live TEST credentials)"
    - "The live multi-slice proof NAMES which providers have TEST credentials and were live-proven; every un-exercised provider is recorded as a known gap (unit-only) with rationale — the advertised-vs-live delta is stated, not silent"
    - "The resume-heartbeat migration is pushed TEST-then-PROD, prod-ref-guarded, additive-verified (0 destructive DDL), and the TEST-applied cron targets the TEST function host (not PROD)"
    - "connector-sync-all is deployed to PROD via --use-api (Docker-less)"
    - "A live end-to-end sync-all on TEST completes across multiple self-chaining slices, resumes after a killed slice, and produces no duplicates"
  artifacts: []
  key_links:
    - from: "sync-all-resume-heartbeat cron (PROD)"
      to: "connector-sync-all (PROD)"
      via: "net.http_post verified registered + active in cron.job, targeting the same-environment host"
      pattern: "sync-all-resume-heartbeat"
---

<objective>
[BLOCKING] Prove Phase 28 on a real database, then ship the backend to prod. Run the Plan 01 integration scaffolds GREEN (truthful, not guard-skipped) against the TEST project with the pager + listPage + resume cron in place; explicitly state the advertised-vs-live coverage delta across all 6 providers; then push the resume-heartbeat migration TEST-then-PROD (prod-ref-guarded, additive-verified, TEST cron verified to target TEST) and deploy connector-sync-all to PROD via --use-api.

Purpose: This is the highest-risk phase's go/no-go gate. SYNC-03's correctness (no duplicates under concurrency; 23505 to skipped) and SYNC-01's resumability are claims that MUST be proven on a real DB, not mocked — per BUG-01 the exact mocked-passes/real-fails bug class lives here. 6 syncAll buttons ship; the live proof can only exercise providers that have TEST credentials, so the un-exercised providers must be honestly recorded as unit-only (backed by the all-6 listPage unit suite), never silently implied as live-proven. The backend pager + cron must reach prod to function; the frontend stays un-pushed (milestone-end).
Output: A truthful GREEN integration run, an all-6 unit proof, an explicit per-provider advertised-vs-live coverage statement, a prod-deployed pager + resume cron verified active (TEST cron targeting TEST, PROD cron targeting PROD), and a live multi-slice sync-all proof. No frontend origin push.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-server-side-sync-all/28-RESEARCH.md
@supabase/CLAUDE.md
@.planning/phases/28-server-side-sync-all/28-01-PLAN.md
@.planning/phases/28-server-side-sync-all/28-02-PLAN.md
@.planning/phases/28-server-side-sync-all/28-04-PLAN.md

<read_first>
- supabase/CLAUDE.md "Running integration tests safely" (triple guard; npm run test:integration; TEST-ref-only; cleanup contract) + "Deploying Edge Functions" (--use-api, Docker-less).
- .planning/STATE.md last-session 27-04 entry — the EXACT proven prod-push procedure to mirror: link TEST (ref swjzxiddcrtaqixsfaac, TEST DB password from .env.local) then db push; then prod-ref guard (assert DATABASE_URL contains vltmrnjsubfzrgrtdqey, booleans only) then link PROD then db push; then functions deploy --use-api.
- supabase/functions/connector-sync-all/__tests__/resume.integration.test.ts + idempotency.integration.test.ts (Plan 01 — the proofs to turn truthfully GREEN).
- supabase/functions/_shared/__tests__/listPage.test.ts (Plan 01 — the all-6-provider unit safety net; the basis for unit-only coverage claims).
- CLAUDE.md env-file map: .env = PROD ref vltmrnjsubfzrgrtdqey; .env.local/.env.test = TEST. Migrations/DDL read .env; verify prod ref before connecting.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to TEST + run the real-DB integration proofs GREEN (truthful, not a guard skip)</name>
  <files></files>
  <read_first>
  - supabase/CLAUDE.md integration-test safety + cleanup contract.
  - The two Plan 01 integration test files + listPage.test.ts (all-6 unit suite).
  - STATE 27-04 entry — the "seed under a real auth.users id so the donor-guard doesn't short-circuit" technique used to prove the reaper LIVE.
  </read_first>
  <action>
  This task is the fully-automatable portion (deploy-to-TEST + automated test runs). The manual PROD push + live kill-a-slice proof is the separate checkpoint task below.

  First, push the Plan 04 resume-heartbeat migration to the TEST project so the schema/cron exist there (link TEST ref swjzxiddcrtaqixsfaac, TEST DB password from .env.local, supabase db push --linked). Deploy connector-sync-all to TEST via --use-api so the pager is invokable. Confirm the 6 listPage providers + the populated registry are present (they are code, deployed with the fn). Verify the TEST-applied 'sync-all-resume-heartbeat' cron resolves its target to the TEST function host (NOT the prod ref) — query cron.job on TEST and confirm the URL/host is the TEST project's, proving the per-environment host derivation from Plan 04 works and a TEST cron will not drive PROD.

  Then run the automated suites (TEST env):
  - listPage.test.ts unit suite: confirm ALL 6 providers (Fathom, Grain, Zoom, Read.ai, Fireflies, Plaud) paginate to exhaustion GREEN — this is the safety net for any provider that lacks live TEST credentials below.
  - npm run test:integration (VITEST_INTEGRATION_OK=true, TEST env):
    - idempotency.integration.test.ts (SYNC-03): concurrent selective-import + sync-all of the SAME source_call_id → exactly ONE recordings row; the loser is reclassified skipped (in skipped_count), NOT in failed_ids; a slice retry after simulated crash produces no duplicate.
    - resume.integration.test.ts (SYNC-01): one slice advances provider_cursor + freshens last_heartbeat_at; a killed-mid-run job resumes from provider_cursor to a terminal status; failed_ids/skipped_count written.
  If the TEST project has no seed rows and the donor-guard would short-circuit, SEED under a real auth.users id (mirror the 27-04 technique) so the assertions actually execute — a truthful GREEN, never a guard skip claimed as a pass. Clean up every seeded row in afterAll (capture-before-mutate, try/catch, cleanup_test_fixture_users). Zero mocks.

  Record in the SUMMARY: the all-6 unit result, the integration result, and the TEST-cron-targets-TEST verification.
  </action>
  <verify>
    <automated>rtk proxy npx vitest run supabase/functions/_shared/__tests__/listPage.test.ts 2>&1 | rtk grep -ci "pass\|fail"; rtk proxy npm run test:integration 2>&1 | rtk grep -ci "resume\|idempotency\|pass\|fail"</automated>
  </verify>
  <acceptance_criteria>
  - listPage.test.ts is GREEN for ALL 6 providers (unit safety net established).
  - resume.integration.test.ts + idempotency.integration.test.ts pass GREEN against TEST with assertions actually executed (seeded if needed), not a describe.skipIf short-circuit.
  - The TEST-applied resume cron's target host is the TEST function host (verified via cron.job on TEST) — not the prod ref; the per-environment derivation holds.
  - All seeded TEST rows cleaned up (suite re-runnable, leaves DB unchanged).
  </acceptance_criteria>
  <done>connector-sync-all deployed to TEST; all-6 unit suite + both integration proofs GREEN (truthful, assertions executed); TEST cron verified targeting TEST; cleanup verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Live multi-slice/resume proof + advertised-vs-live coverage statement + [BLOCKING] PROD push & deploy</name>
  <what-built>
  The all-6 listPage unit suite + the resume/idempotency integration proofs are GREEN on TEST (Task 1), and the TEST resume cron targets TEST. This checkpoint covers the manual operator steps: (a) the LIVE multi-slice / kill-a-slice end-to-end proof on TEST, (b) the explicit advertised-vs-live coverage statement across all 6 providers, and (c) the [BLOCKING] PROD push of the migration + PROD deploy of connector-sync-all. PROD writes are destructive-adjacent; operator pre-approval per the 27-04 precedent.
  </what-built>
  <action>
  Autonomously execute everything that is automatable BEFORE pausing; the live kill-a-slice proof and the PROD push are the human-gated steps.

  A. LIVE multi-slice proof on TEST (needs real provider creds):
  1. Identify which providers have TEST credentials available and NAME them explicitly. Start a sync-all over a range large enough to require 2+ slices for at least one credentialed provider.
  2. Observe self-chaining advance provider_cursor across >=2 slices to a terminal status (completed | completed_with_errors).
  3. Kill a slice mid-run; confirm the resume path (self-chain OR the 2-min cron) re-advances it to completion with no duplicates.
  4. Confirm concurrent selective-import + sync-all of the same call yields exactly ONE recordings row; the 23505 loser is in skipped_count, absent from failed_ids.

  B. Advertised-vs-live coverage statement (MANDATORY, explicit in SUMMARY): for EACH of the 6 providers (Fathom, Grain, Zoom, Read.ai, Fireflies, Plaud), record whether it was live-proven (had TEST creds + exercised end-to-end here) OR unit-only (covered by listPage.test.ts but not live-exercised, reason = no TEST creds). State the delta plainly: "N of 6 live-proven, M of 6 unit-only" — the advertised-vs-live gap is STATED, not silent. This is the BLOCKER-2 acceptance criterion.

  C. PROD push (mirror the proven 27-04 prod-push exactly): verify the Plan 04 migration is STRICTLY ADDITIVE (grep ALTER/DROP on non-comment lines = 0; grep literal prod ref in the migration body = 0, host is per-environment-derived); re-confirm the TEST 'sync-all-resume-heartbeat' cron is active with the TEST host; assert DATABASE_URL from .env contains vltmrnjsubfzrgrtdqey (booleans only) BEFORE connecting; link PROD ref vltmrnjsubfzrgrtdqey and `supabase db push`; confirm the cron active on PROD via cron.job with the PROD function host; deploy `supabase functions deploy connector-sync-all --use-api` to PROD; run `npm run build` exit 0 on the committed tree. Then PAUSE for human verification (production schema change + edge deploy).
  </action>
  <acceptance_criteria>
  - Live TEST sync-all completes across >=2 self-chaining slices; a killed slice resumes from provider_cursor; concurrent import + sync-all yields exactly 1 recordings row per call; 23505 loser in skipped_count, absent from failed_ids.
  - SUMMARY states, per the 6 providers, whether each was unit-only or live-proven; the advertised-vs-live delta is explicit (N live-proven / M unit-only with reasons), not silent.
  - Migration additive (ALTER/DROP = 0) and no hardcoded prod ref (literal prod-ref grep = 0); pushed TEST-then-PROD prod-ref-guarded; resume cron active on PROD (PROD host) and TEST (TEST host).
  - connector-sync-all deployed to PROD via --use-api and live; npm run build exit 0 on committed tree; zero new test failures; NO origin push.
  </acceptance_criteria>
  <how-to-verify>
  A. LIVE multi-slice proof on TEST (manual, needs real provider creds):
  1. Identify which providers have TEST credentials available. NAME them explicitly. Start a sync-all over a range large enough to require 2+ slices for at least one credentialed provider.
  2. Observe self-chaining advance provider_cursor across >=2 slices to a terminal status (completed | completed_with_errors).
  3. Kill a slice mid-run; confirm the resume path (self-chain OR the 2-min cron) re-advances it to completion with no duplicates.
  4. Confirm concurrent selective-import + sync-all of the same call yields exactly ONE recordings row; the 23505 loser is in skipped_count, absent from failed_ids.

  B. Advertised-vs-live coverage statement (MANDATORY, explicit in SUMMARY):
  5. For EACH of the 6 providers (Fathom, Grain, Zoom, Read.ai, Fireflies, Plaud), record whether it was: live-proven (had TEST creds + exercised end-to-end here) OR unit-only (covered by listPage.test.ts but not live-exercised, with the reason = no TEST creds). State the delta plainly: "N of 6 live-proven, M of 6 unit-only" — the advertised-vs-live gap is STATED, not silent. This is the BLOCKER-2 acceptance criterion.

  C. PROD push (mirror the proven 27-04 prod-push exactly):
  6. Verify the Plan 04 migration is STRICTLY ADDITIVE: grep for ALTER/DROP on non-comment lines — must be 0; and grep for the literal prod ref in the migration body — must be 0 (host is per-environment-derived).
  7. TEST first (already pushed in Task 1): re-confirm 'sync-all-resume-heartbeat' is active=true in cron.job on TEST and its host is the TEST function host.
  8. PROD-REF GUARD: assert DATABASE_URL from .env contains vltmrnjsubfzrgrtdqey (booleans only, no secret echo) BEFORE connecting.
  9. PROD push: link PROD ref vltmrnjsubfzrgrtdqey, `supabase db push`; confirm 'sync-all-resume-heartbeat' cron active on PROD via direct pg query against cron.job, and that ITS resolved host is the PROD function host.
  10. Deploy `supabase functions deploy connector-sync-all --use-api` to PROD (Docker-less). Confirm the fn is live.
  11. Phase gate: `npm run build` exit 0 on the committed tree (boot-crash guard); full unit suite shows ZERO new failures vs the documented baseline.
  Expected: live multi-slice + resume + no-dup proven on TEST; per-provider advertised-vs-live delta recorded; resume cron active on PROD (PROD host) + TEST (TEST host); connector-sync-all deployed to PROD; build green; no new test failures; NO origin push (frontend batched to milestone-end).
  </how-to-verify>
  <resume-signal>Type "approved" after the live proof is recorded, the per-provider advertised-vs-live delta is in the SUMMARY, and the prod push + deploy are verified (cron active on PROD with PROD host, fn live) — or describe issues.</resume-signal>
  <done>SYNC-01/02/03 proven on a real DB; per-provider advertised-vs-live delta recorded; backend pager + per-environment resume cron live on PROD (verified active); build green; no origin push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → PROD Supabase | Migration push + edge deploy against the prod ref — catastrophic if confused with TEST |
| test harness → TEST Supabase | Service-role bypasses RLS; TEST ref only |
| TEST cron → connector-sync-all | Must resolve to the TEST function host, never PROD (verified this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-15 | Tampering | migration pushed to wrong project (prod/test confusion) | mitigate | prod-ref guard asserts DATABASE_URL contains vltmrnjsubfzrgrtdqey before connect (booleans only); TEST uses .env.local creds; 27-04 proven procedure |
| T-28-16 | Tampering | integration test mutating prod | mitigate | triple-guarded TEST-only env; cleanup_test_fixture_users afterAll; mocks rejected |
| T-28-17 | Availability | bad edge deploy or zeroed registry breaks prod | mitigate | build-green committed-tree gate + OAUTH_CALLBACK_ROUTES assertion before deploy; --use-api verified live |
| T-28-18 | Elevation / cross-env | TEST cron driving PROD function | mitigate | verify TEST cron.job host = TEST function host before/after push; per-environment host derivation (Plan 04); prod-ref literal absent in migration |
| T-28-SC | Tampering | npm/deno installs at deploy | accept | zero new packages (RESEARCH audit N/A) |
</threat_model>

<verification>
- All-6 listPage unit suite GREEN; resume + idempotency integration proofs GREEN on TEST (executed, not skipped) + live multi-slice/resume/no-dup proof.
- Per-provider advertised-vs-live coverage delta stated explicitly in the SUMMARY (N live-proven / M unit-only, with reasons).
- Migration additive (0 destructive DDL, 0 hardcoded prod ref); pushed TEST-then-PROD prod-ref-guarded; resume cron active on both refs with the correct per-environment host (TEST cron → TEST, PROD cron → PROD).
- connector-sync-all deployed to PROD via --use-api; build green; zero new test failures.
- No origin push.
</verification>

<success_criteria>
SYNC-01/02/03 proven on a real DB (all-6 unit + live multi-slice/resume/no-dup), the per-provider advertised-vs-live coverage delta stated truthfully, and the backend (pager + per-environment resume cron) is live on PROD, verified active — the highest-risk phase shipped safely, frontend staged for milestone-end.
</success_criteria>

<output>
Create `.planning/phases/28-server-side-sync-all/28-05-SUMMARY.md` when done.
</output>
</content>
</invoke>
