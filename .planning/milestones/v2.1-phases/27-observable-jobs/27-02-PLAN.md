---
phase: 27-observable-jobs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260623120000_sync_jobs_reaper.sql
  - supabase/functions/sync-meetings/index.ts
  - src/test/migrations/phase27-sync-jobs-reaper.integration.test.ts
autonomous: true
requirements: [JOB-02]
tags: [supabase, pg_cron, reaper, heartbeat, sql, integration-test, edge-function]

must_haves:
  truths:
    - "A job whose worker dies (stale heartbeat) is flipped processing→failed by a pg_cron reaper, not left a zombie"
    - "A processing job with a FRESH heartbeat is NOT reaped"
    - "A processing job that never wrote a heartbeat is still reaped via an absolute created_at fallback"
    - "sync-meetings writes last_heartbeat_at at job INSERT and on every per-item progress UPDATE (zero new write sites)"
    - "The reaper is verified against a REAL TEST database (zero mocks), TEST-ref guarded"
  artifacts:
    - path: "supabase/migrations/20260623120000_sync_jobs_reaper.sql"
      provides: "reap_stale_sync_jobs() SECURITY DEFINER fn + sync-jobs-reaper pg_cron schedule (additive)"
      contains: "reap_stale_sync_jobs"
    - path: "src/test/migrations/phase27-sync-jobs-reaper.integration.test.ts"
      provides: "Real-DB reaper test: stale→failed, fresh→untouched, NULL-heartbeat absolute fallback"
    - path: "supabase/functions/sync-meetings/index.ts"
      provides: "last_heartbeat_at written at INSERT + each progress UPDATE"
      contains: "last_heartbeat_at"
  key_links:
    - from: "supabase/migrations/20260623120000_sync_jobs_reaper.sql"
      to: "cron.schedule('sync-jobs-reaper', ...)"
      via: "pg_cron registration (graceful-degradation guarded)"
      pattern: "cron\\.schedule\\('sync-jobs-reaper'"
    - from: "supabase/functions/sync-meetings/index.ts"
      to: "sync_jobs.last_heartbeat_at"
      via: "piggybacked on existing INSERT + per-item UPDATE objects"
      pattern: "last_heartbeat_at"
---

<objective>
Make a silently-dead import job become a VISIBLE failed job (JOB-02): add the producer-side heartbeat (`sync-meetings` writes `last_heartbeat_at` at INSERT and on every existing progress UPDATE — zero new write sites) and an additive pure-SQL `pg_cron` reaper that flips stale `processing` jobs to `failed`, modeled on the in-repo `embedding_queue` stale-lock precedent. Prove it against a REAL test database.

Purpose: A dead Edge worker can't mark itself failed; only an out-of-band scheduler can. This closes John's "nothing actually imported, no status" zombie.
Output: one additive migration, the heartbeat writes in `sync-meetings`, and a TEST-DB-guarded reaper integration test. This plan does NOT push to prod or deploy the edge function — that is the [BLOCKING] Plan 27-04.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-observable-jobs/27-CONTEXT.md
@.planning/phases/27-observable-jobs/27-RESEARCH.md

# The reaper precedent (stale-lock predicate + pg_cron registration shape):
@supabase/migrations/20251128100000_embedding_queue_system.sql
# pg_cron graceful-degradation + cron registration precedent:
@supabase/migrations/20260512000002_fathom_daily_reconcile_cron.sql
# The producer to add heartbeat writes into:
@supabase/functions/sync-meetings/index.ts
# Integration-test safety contract + the cron-test precedent to mirror:
@supabase/CLAUDE.md
@src/test/migrations/phase39-fathom-reconcile-cron.integration.test.ts

<interfaces>
<!-- sync_jobs columns ALREADY EXIST (additive 20260620120000 migration, live on PROD+TEST): -->
<!--   status TEXT, last_heartbeat_at TIMESTAMPTZ, created_at TIMESTAMPTZ, error TEXT, completed_at TIMESTAMPTZ -->
<!-- sync-meetings WRITE SITES (verified): -->
<!--   INSERT: index.ts ~568-579 (status:'processing', progress_total, type:'fathom') -->
<!--   per-item progress UPDATE objects: ~728-737, ~765-773, ~782-790 -->
<!--   final status UPDATE: ~802-809 -->
<!-- Integration test guards: integration-setup.ts -> integrationDbReachable, makeIntegrationClient (service-role TEST client). -->
<!-- TEST ref = swjzxiddcrtaqixsfaac; PROD ref = vltmrnjsubfzrgrtdqey (NEVER target prod in tests). -->

Reaper threshold decision (locked, grounded in Edge ~400s wall-clock + 30-60s cadence):
  - Reap status='processing' AND last_heartbeat_at IS NOT NULL AND last_heartbeat_at < NOW() - INTERVAL '5 minutes'
  - ALSO reap status='processing' AND last_heartbeat_at IS NULL AND created_at < NOW() - INTERVAL '15 minutes' (absolute fallback for pre-heartbeat/legacy zombies)
  - Cron cadence '* * * * *' (1 min — pg_cron minimum)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive reaper migration + heartbeat writes in sync-meetings</name>
  <read_first>
    - supabase/migrations/20251128100000_embedding_queue_system.sql (lines 100-140 stale-lock SECURITY DEFINER fn; 244-278 cron.schedule guarded shape)
    - supabase/migrations/20260512000002_fathom_daily_reconcile_cron.sql (graceful-degradation EXCEPTION blocks)
    - supabase/migrations/20260620120000_sync_jobs_durable_resource.sql (confirms last_heartbeat_at column already exists — DO NOT re-add)
    - supabase/functions/sync-meetings/index.ts (the INSERT at ~568-579 and the three per-item UPDATE objects at ~728-737, ~765-773, ~782-790)
    - supabase/CLAUDE.md (migration naming/structure; additive-only; --use-api deploy note)
  </read_first>
  <files>supabase/migrations/20260623120000_sync_jobs_reaper.sql, supabase/functions/sync-meetings/index.ts</files>
  <action>
    Migration `supabase/migrations/20260623120000_sync_jobs_reaper.sql` (STRICTLY ADDITIVE — no ALTER/retype; last_heartbeat_at already exists):
    - Create `public.reap_stale_sync_jobs()` RETURNS INT, `LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`, mirroring the embedding_queue SECURITY DEFINER shape. In a single UPDATE … RETURNING wrapped in a CTE, set status='failed', error=COALESCE(error,'worker died (no heartbeat)'), completed_at=NOW() WHERE status='processing' AND ((last_heartbeat_at IS NOT NULL AND last_heartbeat_at < NOW() - INTERVAL '5 minutes') OR (last_heartbeat_at IS NULL AND created_at < NOW() - INTERVAL '15 minutes')). Return COUNT(*) of reaped rows. No dynamic SQL, no client input. Add COMMENT ON FUNCTION.
    - Register the cron with the EXACT guarded shape from embedding-worker-backup / fathom-daily-reconcile: a DO block that PERFORM cron.unschedule('sync-jobs-reaper') wrapped in EXCEPTION WHEN undefined_function THEN RAISE NOTICE / WHEN others THEN NULL; then a DO block PERFORM cron.schedule('sync-jobs-reaper', '* * * * *', $body$ SELECT public.reap_stale_sync_jobs(); $body$) wrapped in the same EXCEPTION degradation (free-tier-safe). Header comment per supabase/CLAUDE.md migration template, noting strictly-additive + prod-ref guard applies at push time (Plan 27-04).

    `sync-meetings/index.ts` heartbeat piggyback (NO new write sites — add one field to writes that already happen):
    - Add `last_heartbeat_at: new Date().toISOString()` to the initial INSERT object (~568-579) so NULL never persists for new jobs.
    - Add the same `last_heartbeat_at: new Date().toISOString()` to EACH of the three per-item progress UPDATE objects (~728-737, ~765-773, ~782-790). Do NOT add a separate heartbeat loop/interval (RESEARCH Pitfall 5 — keep the existing cadence; do not add Realtime write volume).
    - Leave the final status UPDATE (~802-809) as-is (terminal write; heartbeat irrelevant).
  </action>
  <verify>
    <automated>rtk grep -c "reap_stale_sync_jobs\|cron.schedule('sync-jobs-reaper'" supabase/migrations/20260623120000_sync_jobs_reaper.sql && rtk grep -c "last_heartbeat_at" supabase/functions/sync-meetings/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists, is named `20260623120000_sync_jobs_reaper.sql`, defines `reap_stale_sync_jobs()` as SECURITY DEFINER with `SET search_path = public`, and registers `cron.schedule('sync-jobs-reaper', '* * * * *', ...)` inside graceful-degradation EXCEPTION blocks.
    - The reaper UPDATE contains BOTH the stale-heartbeat predicate (5 min) and the NULL-heartbeat absolute fallback (created_at 15 min). No ALTER/DROP/retype of any sync_jobs column.
    - `grep -c last_heartbeat_at supabase/functions/sync-meetings/index.ts` returns ≥ 4 (one INSERT + three progress UPDATEs).
    - No new standalone heartbeat write loop / setInterval added to the edge function.
  </acceptance_criteria>
  <done>The additive reaper migration exists with a guarded pg_cron schedule and a NULL-tolerant SECURITY DEFINER reap function; sync-meetings writes last_heartbeat_at at INSERT and on every existing progress UPDATE.</done>
</task>

<task type="auto">
  <name>Task 2: Real-DB reaper integration test (TEST-ref guarded, zero mocks)</name>
  <read_first>
    - src/test/migrations/phase39-fathom-reconcile-cron.integration.test.ts (the describe.skipIf(!integrationDbReachable) + makeIntegrationClient + afterAll-cleanup-with-try/catch pattern to mirror)
    - src/test/integration-setup.ts (integrationDbReachable, makeIntegrationClient — service-role TEST client; TEST-ref guard throws if it equals prod)
    - supabase/CLAUDE.md "Running integration tests safely" (VITEST_INTEGRATION_OK gate; cleanup contract; donor pattern for user_id)
    - the migration from Task 1 (the reap_stale_sync_jobs() contract)
  </read_first>
  <files>src/test/migrations/phase27-sync-jobs-reaper.integration.test.ts</files>
  <action>
    Create the reaper integration test mirroring the phase39 cron test idioms: `describe.skipIf(!integrationDbReachable)`, `makeIntegrationClient()` (service-role TEST client), donor pattern for user_id (read one existing row's user_id, e.g. from sync_jobs or fathom_raw_calls, to avoid auth admin createUser), and a try/catch-wrapped `afterAll` that deletes every seeded sync_jobs row by id. Seed sync_jobs rows directly via the service-role client (RLS bypassed — fine for the seed/cleanup; the reaper itself runs SECURITY DEFINER).

    Cases (call the reaper via `supabase.rpc('reap_stale_sync_jobs')`):
    1. STALE-HEARTBEAT: insert status='processing', last_heartbeat_at = NOW() - 10 min. Run reaper. Assert the row is now status='failed' AND error ILIKE '%no heartbeat%' AND completed_at IS NOT NULL.
    2. FRESH-HEARTBEAT (spared): insert status='processing', last_heartbeat_at = NOW(). Run reaper. Assert it is STILL status='processing' (untouched).
    3. NULL-HEARTBEAT ABSOLUTE FALLBACK: insert status='processing', last_heartbeat_at = NULL, created_at = NOW() - 20 min. Run reaper. Assert it is now status='failed'.
    4. NULL-HEARTBEAT YOUNG (spared): insert status='processing', last_heartbeat_at = NULL, created_at = NOW() - 2 min. Run reaper. Assert STILL status='processing'.
    5. IDEMPOTENT: re-run the reaper; assert an already-'failed' row is not re-touched (completed_at unchanged / no error mutation).

    Note in a header comment: the migration must be applied to the TEST project (`supabase db push --linked` against the TEST ref) for `rpc('reap_stale_sync_jobs')` to resolve — if the function is absent the test will surface a clear RPC error rather than a false pass; document that the [BLOCKING] Plan 27-04 also pushes to TEST. The test must clean-SKIP (not fail) when integration env is unset.
  </action>
  <verify>
    <automated>rtk proxy npm run test:integration -- phase27-sync-jobs-reaper 2>&1 | rtk grep -iE "pass|skip|fail|reaper"</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists under src/test/migrations/, uses `describe.skipIf(!integrationDbReachable)` and `makeIntegrationClient`, and contains all 5 cases above.
    - When integration env is unset → clean SKIP (no failure). When env is set against the TEST ref with the migration applied → all cases PASS (stale + NULL-old reaped to failed; fresh + NULL-young spared; idempotent).
    - `afterAll` deletes every seeded row by id inside try/catch (cleanup contract). No prod ref touched (TEST-ref guard in integration-setup enforces).
  </acceptance_criteria>
  <done>A real-DB, TEST-guarded reaper test proves stale and pre-heartbeat zombies flip to failed while healthy jobs are spared, and the reaper is idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron (scheduler) → sync_jobs | An automated, unattended prod-write path runs on a schedule with no user in the loop. |
| Edge worker (sync-meetings) → sync_jobs | Writes heartbeat + progress under service-role. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-E | Elevation of Privilege | `reap_stale_sync_jobs()` SECURITY DEFINER | mitigate | Function ONLY UPDATEs sync_jobs.status/error/completed_at on rows matching the deterministic stale predicate; `SET search_path = public` (matches claim_embedding_tasks); no dynamic SQL; takes no client input. |
| T-27-02-T | Tampering | unguarded scheduled prod-write path | mitigate | Pure-SQL deterministic predicate (stale heartbeat OR absolute age); idempotent (re-running is a no-op on already-failed rows — proven by test case 5); pushed via prod-ref-guarded `supabase db push` (Plan 27-04). |
| T-27-02-D | Denial of Service (mass-reap healthy jobs) | reaper threshold too tight | mitigate | 5 min heartbeat threshold sits well above Edge ~400s wall-clock + 30-60s cadence; NULL fallback gated at 15 min; test cases 2 & 4 assert fresh/young jobs are spared. |
| T-27-02-SC | Tampering | package installs | n/a | Zero new packages this phase (pg_cron/pg_net already enabled; no npm install). No legitimacy gate required. |
</threat_model>

<verification>
- Grep gates: reap fn + cron schedule present in the migration; ≥4 `last_heartbeat_at` writes in sync-meetings.
- `rtk proxy npm run test:integration -- phase27-sync-jobs-reaper` — PASS against TEST (post Plan 27-04 push) or clean SKIP when env unset.
- Migration is strictly additive (no ALTER/DROP/retype of sync_jobs).
</verification>

<success_criteria>
A pure-SQL pg_cron reaper exists and is proven against a real TEST database to flip stale-heartbeat and pre-heartbeat zombie `processing` jobs to `failed` while sparing healthy jobs; `sync-meetings` writes `last_heartbeat_at` at INSERT and on every progress update with no added write volume.
</success_criteria>

<output>
Create `.planning/phases/27-observable-jobs/27-02-SUMMARY.md` when done.
</output>
