---
phase: 27-observable-jobs
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/components/import/SyncJobBanner.tsx
  - src/components/import/PerProviderSyncChip.tsx
  - src/components/import/__tests__/SyncJobBanner.test.tsx
  - src/components/import/__tests__/PerProviderSyncChip.test.tsx
  - src/components/import/ImportSurface.tsx
autonomous: true
requirements: [JOB-03, JOB-05]
tags: [react, import-surface, banner, chip, ui, sync-status]

must_haves:
  truths:
    - "A durable job-status banner renders directly off the sync_jobs row in <ImportSurface> (both tabs) — no setTimeout dismissal"
    - "Failed / completed_with_errors banners persist until the user explicitly dismisses them; clean completed may auto-fade"
    - "A persistent per-provider chip reads 'Last synced X · N new available · M failed'"
    - "'N new available' = find-section rows not yet imported (results.length − importedIds.size), no new query"
    - "The chip is per-provider (this surface's source_app) and org-scoped — never a hardcoded 'fathom'"
  artifacts:
    - path: "src/components/import/SyncJobBanner.tsx"
      provides: "Durable progress/result banner driven by a SyncJob row; sticky failures; local dismiss"
      exports: ["SyncJobBanner"]
      min_lines: 50
    - path: "src/components/import/PerProviderSyncChip.tsx"
      provides: "Persistent 'Last synced X · N new · M failed' chip per provider"
      exports: ["PerProviderSyncChip"]
      min_lines: 40
    - path: "src/components/import/ImportSurface.tsx"
      provides: "Banner mounted at the :474 seam + chip in the toolbar; consumes useSyncJobs"
      contains: "useSyncJobs"
  key_links:
    - from: "src/components/import/ImportSurface.tsx"
      to: "useSyncJobs"
      via: "hook call with { sourceApp, organizationId }"
      pattern: "useSyncJobs\\("
    - from: "src/components/import/ImportSurface.tsx"
      to: "SyncJobBanner / PerProviderSyncChip"
      via: "render at the Phase-27 seam (:474) + toolbar"
      pattern: "SyncJobBanner|PerProviderSyncChip"
---

<objective>
Surface the observable job state in the UI: evolve the PRESERVED `SyncStatusIndicator`/`ActiveSyncJobsCard` into a durable `SyncJobBanner` (driven directly by the `sync_jobs` row — failures sticky, no 8s timer; JOB-03) and a persistent per-provider `PerProviderSyncChip` ("Last synced X · N new available · M failed"; JOB-05), and mount both at the clean Phase-26 seams in `<ImportSurface>`, fed by the Plan 27-01 `useSyncJobs` hook.

Purpose: Andrew's ask — "more visibility into the status of imports… make the customer feel like all their content is being synced without worry." The always-visible chip + sticky failure banner is the direct answer.
Output: two new presentational components + tests + the ImportSurface mount. No DB/edge/realtime logic lives in the components (that is the hook's job, Plan 27-01).
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-observable-jobs/27-CONTEXT.md
@.planning/phases/27-observable-jobs/27-RESEARCH.md

# The hook this plan consumes (created in Plan 27-01):
@src/hooks/useSyncJobs.ts

# The PRESERVED presentational components to evolve (keep presentational; drive from the hook):
@src/components/transcripts/ActiveSyncJobsCard.tsx
@src/components/transcripts/SyncStatusIndicator.tsx

# The mount target — the two clean Phase-27 seams:
@src/components/import/ImportSurface.tsx

# Frontend binding rules (Remix icons only; motion/react not framer-motion; tokens; date-fns):
@src/CLAUDE.md

<interfaces>
<!-- From useSyncJobs (Plan 27-01): { activeJobs, terminalJobs } : SyncJob[] (string ids). -->
<!-- SyncJob fields used here: id, status, progress_current, progress_total, -->
<!--   synced_ids (string[]|null), failed_ids (string[]|null), error, completed_at, source_app. -->

<!-- ImportSurface already computes the chip inputs (verified): -->
<!--   results: AvailableCall[]          (find-section rows; results.length) -->
<!--   importedIds: ReadonlySet<string>  (already-imported externalIds; importedIds.size) -->
<!--   => "N new available" = results.length - importedIds.size  (LOCKED — no new query) -->
<!--   sourceApp: ConnectorSourceApp prop; organizationId: string|null prop -->
<!--   Phase-27 banner seam: ImportSurface.tsx ~line 474 (commented "Phase 27 SEAM") -->
<!--   toolbar: the flex container ~388-471 (chip mounts in/near the toolbar header) -->

<!-- "Last synced X" relative time: import { formatDistanceToNow } from 'date-fns' (project lib). -->
<!-- Icons: @remixicon/react only (RiCheckLine, RiAlertLine, RiLoader2Line, RiCloseLine, etc.). -->
<!-- Optional clean-completed fade: motion/react ONLY (never framer-motion). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED — banner + chip unit tests</name>
  <read_first>
    - src/components/transcripts/ActiveSyncJobsCard.tsx (the "Auto-dismissing..." text at line 66 + the 5-min "Appears Stuck" client heuristic at 77-99 — both to be DROPPED)
    - src/components/transcripts/SyncStatusIndicator.tsx (the success/partial/failed pill variants to evolve)
    - src/components/import/ImportSurface.tsx (the seam + the results/importedIds inputs)
    - an existing component test under src/components/import/__tests__/ for the project's render-test idioms (e.g. ImportSurface.test.tsx)
  </read_first>
  <files>src/components/import/__tests__/SyncJobBanner.test.tsx, src/components/import/__tests__/PerProviderSyncChip.test.tsx</files>
  <behavior>
    SyncJobBanner:
    - Test: a `failed` job renders a failure banner AND remains in the DOM after fake-advancing timers 9000ms (vi.useFakeTimers) — proves NO 8s/auto dismissal of failures.
    - Test: a `completed_with_errors` job renders "X synced, Y failed" and is sticky (still present after 9000ms).
    - Test: clicking the dismiss control invokes the `onDismiss(jobId)` callback (dismissal is local UI, never a timer/DB delete).
    - Test: a `processing` job renders progress `progress_current/progress_total`.
    - Test (regression guard): the rendered source does NOT contain the literal text "Auto-dismissing".
    PerProviderSyncChip:
    - Test: given lastSyncedAt, newCount=3, failedCount=2, renders "Last synced", "3 new", and "2 failed" (exact copy can vary; assert the three numbers/labels are present).
    - Test: failedCount=0 hides the failed segment; newCount=0 shows an up-to-date/"0 new" state (discretion, assert no crash).
    - Test (regression guard): the rendered source contains NO literal "fathom" (provider comes from props).
  </behavior>
  <action>Write RED render tests for the not-yet-created `SyncJobBanner` and `PerProviderSyncChip` (imported from `@/components/import/SyncJobBanner` and `@/components/import/PerProviderSyncChip`). Use @testing-library/react render + screen queries; use vi.useFakeTimers for the sticky-failure assertions. Construct SyncJob fixtures inline with string-id arrays. Do not implement the components in this task.</action>
  <verify>
    <automated>rtk proxy npx vitest run src/components/import/__tests__/SyncJobBanner.test.tsx src/components/import/__tests__/PerProviderSyncChip.test.tsx</automated>
  </verify>
  <acceptance_criteria>Both test files exist, import the two components, and FAIL RED (module not found). Coverage includes sticky-failures-after-9000ms, dismiss callback, progress render, and the two regression guards (no "Auto-dismissing", no "fathom").</acceptance_criteria>
  <done>RED tests committed; they fail solely because the components are unimplemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement SyncJobBanner + PerProviderSyncChip; mount in ImportSurface</name>
  <read_first>
    - the RED tests from Task 1 (the exact contract)
    - src/components/import/ImportSurface.tsx (the :474 seam, the toolbar ~388-471, results/importedIds/sourceApp/organizationId already in scope)
    - src/hooks/useSyncJobs.ts (the return shape to consume)
    - src/components/transcripts/ActiveSyncJobsCard.tsx + SyncStatusIndicator.tsx (visual patterns to evolve — strip the timer + "Appears Stuck" guess)
    - src/CLAUDE.md (tokens: bg-card/border-border/text-muted-foreground; Remix icons; date-fns; motion/react)
  </read_first>
  <files>src/components/import/SyncJobBanner.tsx, src/components/import/PerProviderSyncChip.tsx, src/components/import/ImportSurface.tsx</files>
  <action>
    `SyncJobBanner.tsx` — presentational, props `{ job: SyncJob; onDismiss: (jobId: string) => void }`. Render off the row's status: `processing`/`pending` → progress banner (progress_current/progress_total, spinner via RiLoader2Line); `completed` (clean) → success state that MAY auto-fade via `motion/react` (discretion) OR show a dismiss — but NEVER a setTimeout that could also drop failures; `completed_with_errors` → "{synced} synced, {failed} failed" sticky with a dismiss control; `failed` → error banner (job.error) sticky with a dismiss control. Failure/partial banners have NO timer and call `onDismiss(job.id)` on the dismiss control only. Evolve the visual language from ActiveSyncJobsCard but DELETE the "Auto-dismissing..." text and the client-side 5-min "Appears Stuck" heuristic (the reaper now owns stuck→failed). Use semantic tokens + Remix icons. Ids are strings — use `.length` on the string[] arrays, never coerce.

    `PerProviderSyncChip.tsx` — presentational, props `{ sourceApp: ConnectorSourceApp; lastSyncedAt?: string | null; newCount: number; failedCount: number }`. Render a persistent pill: "Last synced {formatDistanceToNow(lastSyncedAt)} · {newCount} new · {failedCount} failed" (omit the failed segment when failedCount===0; show an up-to-date state when newCount===0 — discretion). Provider label derived from sourceApp/adapter metadata, NEVER a literal "fathom". Use date-fns formatDistanceToNow, semantic tokens, Remix icons.

    `ImportSurface.tsx` wiring:
    - Call `const { activeJobs, terminalJobs } = useSyncJobs({ sourceApp, organizationId });`
    - At the Phase-27 seam (~line 474) render `{[...activeJobs, ...visibleTerminalJobs].map(job => <SyncJobBanner key={job.id} job={job} onDismiss={dismiss} />)}` where `dismiss` adds the job id to a local `useState<Set<string>>` of dismissed ids and `visibleTerminalJobs` filters terminalJobs by that set (local "I've seen it" — never a DB delete, never a timer). Dismissal must NOT hide active/processing jobs.
    - In the toolbar header area, render `<PerProviderSyncChip sourceApp={sourceApp} lastSyncedAt={lastCompletedJob?.completed_at} newCount={Math.max(0, results.length - importedIds.size)} failedCount={lastCompletedJob?.failed_ids?.length ?? 0} />` where `lastCompletedJob` is the most recent terminalJob for this source. "N new available" = `results.length - importedIds.size` (LOCKED — no new query).
    - Do not break the existing toolbar/section layout; the chip sits in the toolbar, the banner under it at the existing seam comment.
  </action>
  <verify>
    <automated>rtk proxy npx vitest run src/components/import/__tests__/SyncJobBanner.test.tsx src/components/import/__tests__/PerProviderSyncChip.test.tsx && rtk proxy npx tsc -p tsconfig.app.json --noEmit 2>&1 | rtk grep -v '^#' | rtk grep -cE "SyncJobBanner|PerProviderSyncChip|ImportSurface\.tsx" | rtk grep -q '^0$' && echo TSC_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - Task 1's banner + chip tests go GREEN.
    - `tsc -p tsconfig.app.json --noEmit` produces ZERO errors referencing `SyncJobBanner.tsx`, `PerProviderSyncChip.tsx`, or `ImportSurface.tsx` (pre-existing unrelated errors deferred per 26-04).
    - `grep -c "useSyncJobs(" src/components/import/ImportSurface.tsx` ≥ 1; both `SyncJobBanner` and `PerProviderSyncChip` are rendered in ImportSurface.
    - `grep -c '"fathom"' src/components/import/SyncJobBanner.tsx src/components/import/PerProviderSyncChip.tsx` returns 0; neither component file contains the string "Auto-dismissing".
    - `grep -c 'setTimeout' src/components/import/SyncJobBanner.tsx` — if present it MUST NOT gate failed/completed_with_errors dismissal (reviewer confirms the timer only touches clean `completed`; preferred is 0 setTimeout, use motion/react fade instead).
  </acceptance_criteria>
  <done>The durable banner (sticky failures, no 8s timer) and the persistent per-provider chip are implemented and mounted in <ImportSurface> via useSyncJobs; both tabs get them for free; tests green and tsc clean for the touched files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered job data → screen | Job rows reach the DOM; a chip/banner must only ever show THIS user/org's job for THIS provider. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-I | Information Disclosure | PerProviderSyncChip / SyncJobBanner cross-org or cross-provider leak | mitigate | Components are purely presentational and render ONLY what `useSyncJobs({ sourceApp, organizationId })` already scoped (RLS + client narrowing in Plan 27-01). The chip is per source_app; no component issues its own un-scoped query. |
| T-27-03-T | Tampering | id coercion in count rendering | mitigate | failed/synced counts use `.length` on string[] — no parseInt/Number (grep absence enforced via 27-01 + dual-ID rule). |
| T-27-03-D | Denial of Service (regression: silent-failure return) | banner auto-dismissing failures | mitigate | No setTimeout dismisses failures; sticky-after-9000ms test asserts failures persist (the exact JOB-03 regression class). |
</threat_model>

<verification>
- `rtk proxy npx vitest run` for both component test files — green.
- `rtk proxy npx tsc -p tsconfig.app.json --noEmit` — zero NEW errors in the three touched files.
- Grep gates: `useSyncJobs(` in ImportSurface; both components rendered; no "fathom"; no "Auto-dismissing"; no failure-dismissing timer.
</verification>

<success_criteria>
`<ImportSurface>` (both tabs) shows a persistent per-provider "Last synced X · N new · M failed" chip and a durable job banner whose failures never auto-vanish — driven entirely by the shared `useSyncJobs` hook, with the preserved presentational components retired into the new banner/chip.
</success_criteria>

<output>
Create `.planning/phases/27-observable-jobs/27-03-SUMMARY.md` when done.
</output>
