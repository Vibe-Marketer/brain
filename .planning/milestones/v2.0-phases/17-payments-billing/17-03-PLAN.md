---
phase: 17-payments-billing
plan: 03
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - supabase/functions/polar-cancel/index.ts
  - supabase/functions/track-ai-usage/index.ts
  - supabase/functions/polar-webhook/index.ts
autonomous: true
requirements: [PAY-07]
must_haves:
  truths:
    - "Polar webhooks process subscription.created, subscription.active, subscription.canceled, and subscription.revoked events correctly"
    - "polar-cancel edge function is deployed and callable"
    - "track-ai-usage edge function is deployed and callable"
  artifacts:
    - path: "supabase/functions/polar-webhook/index.ts"
      provides: "Webhook handler for all Polar subscription events"
    - path: "supabase/functions/polar-cancel/index.ts"
      provides: "Cancel subscription endpoint"
    - path: "supabase/functions/track-ai-usage/index.ts"
      provides: "AI usage tracking and enforcement endpoint"
  key_links:
    - from: "Polar dashboard webhook config"
      to: "supabase/functions/polar-webhook"
      via: "HTTPS POST with signature"
      pattern: "validateEvent"
    - from: "polar-webhook"
      to: "user_profiles table"
      via: "service role UPDATE"
      pattern: "subscription_status"
---

<objective>
Deploy all new edge functions and verify the Polar webhook handler processes subscription lifecycle events correctly. This plan deploys polar-cancel and track-ai-usage (created in plans 01 and 02), and verifies polar-webhook handles all event types.

Purpose: PAY-07 requires webhook verification. Deploying the new functions completes the billing backend. The webhook handler already exists and handles 6 event types — this plan verifies it works correctly and deploys the new functions alongside it.
Output: All 3 edge functions deployed; webhook handling verified via code review and deployment.
</objective>

<execution_context>
@/Users/Naegele/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Naegele/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-payments-billing/17-01-SUMMARY.md
@.planning/phases/17-payments-billing/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy new edge functions and verify webhook handler</name>
  <files>supabase/functions/polar-cancel/index.ts, supabase/functions/track-ai-usage/index.ts, supabase/functions/polar-webhook/index.ts</files>
  <read_first>
    - supabase/functions/polar-webhook/index.ts (full webhook handler)
    - supabase/functions/polar-cancel/index.ts (cancel function from plan 01)
    - supabase/functions/track-ai-usage/index.ts (usage tracking from plan 02)
    - supabase/CLAUDE.md (deployment instructions — must use --use-api flag)
  </read_first>
  <action>
    1. Review polar-webhook/index.ts for correctness:
       - Verify all 6 event types handled: subscription.created, subscription.active, subscription.canceled, subscription.revoked, customer.created, customer.state_changed
       - Verify each handler updates the correct user_profiles fields
       - Verify webhook signature validation uses POLAR_WEBHOOK_SECRET
       - Verify findUserByCustomerId fallback works when externalId missing
       - If any issues found, fix them

    2. Review polar-cancel/index.ts for correctness:
       - Verify it authenticates via JWT
       - Verify it reads subscription_id from user_profiles
       - Verify it calls Polar SDK to cancel
       - Verify it updates local status to 'canceled'
       - If any issues found, fix them

    3. Review track-ai-usage/index.ts for correctness:
       - Verify it validates actionType against allowed values
       - Verify it checks current usage via RPC before inserting
       - Verify it returns 429 when limit reached
       - If any issues found, fix them

    4. Deploy all three functions:
       - Run: supabase functions deploy polar-cancel --use-api
       - Run: supabase functions deploy track-ai-usage --use-api
       - Run: supabase functions deploy polar-webhook --use-api
       (Use --use-api because Docker is not running on this machine)

    5. Verify deployments succeeded by checking function list:
       - Run: supabase functions list
       - Confirm polar-cancel, track-ai-usage, and polar-webhook appear
  </action>
  <verify>
    <automated>cd /Users/Naegele/dev/brain && supabase functions list 2>&1 | grep -E "polar-cancel|track-ai-usage|polar-webhook" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "subscription.created" supabase/functions/polar-webhook/index.ts
    - grep -q "subscription.canceled" supabase/functions/polar-webhook/index.ts
    - grep -q "subscription.revoked" supabase/functions/polar-webhook/index.ts
    - grep -q "validateEvent" supabase/functions/polar-webhook/index.ts
    - test -f supabase/functions/polar-cancel/index.ts
    - test -f supabase/functions/track-ai-usage/index.ts
  </acceptance_criteria>
  <done>All three edge functions reviewed for correctness, any issues fixed, and deployed to Supabase. Polar webhook handles subscription lifecycle events (created, active, canceled, revoked) + customer events. polar-cancel and track-ai-usage are live.</done>
</task>

</tasks>

<verification>
- All 3 functions deployed successfully (supabase functions list shows them)
- polar-webhook handles all 6 Polar event types
- polar-cancel authenticates and calls Polar SDK cancel
- track-ai-usage enforces monthly limits with 429 response
</verification>

<success_criteria>
- PAY-07: Polar webhook handler processes subscription.created, subscription.active, subscription.canceled, subscription.revoked, customer.created, and customer.state_changed events — all updating user_profiles correctly
</success_criteria>

<output>
After completion, create `.planning/phases/17-payments-billing/17-03-SUMMARY.md`
</output>
