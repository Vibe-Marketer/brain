---
plan: 36-04
phase: 36
title: BUG-08 — Remove Auto-Folders ("Hall of Fame", "Manager Reviews")
wave: 2
depends_on: []
files_modified:
  - src/hooks/useOrganizationMutations.ts
  - src/hooks/useWorkspaceMutations.ts
  - src/components/dialogs/CreateOrganizationDialog.tsx
  - supabase/functions/onboarding-init/index.ts
  - supabase/functions/_shared/onboarding.ts
  - src/components/dialogs/__tests__/CreateWorkspaceDialog.phase25.test.tsx
requirements: [BUG-08]
autonomous: true
---

# Plan 36-04: BUG-08 — Auto-Folder Creation Removed

## Goal

"Hall of Fame" and "Manager Reviews" folders are NOT auto-created on first run / workspace creation / org creation. Fresh signup produces zero auto-folders. Existing tests in `src/components/dialogs/__tests__/CreateWorkspaceDialog.phase25.test.tsx` already enforce this on the workspace-creation path (Phase 25 work), so the remaining surface is org-creation / onboarding / any Edge Function that seeds folders.

## Tasks

<task id="36-04-T1">
<read_first>
- src/components/dialogs/__tests__/CreateWorkspaceDialog.phase25.test.tsx (existing assertions)
- src/hooks/useOrganizationMutations.ts (org creation flow)
- src/hooks/useWorkspaceMutations.ts (workspace creation — already audited in Phase 25)
- src/components/dialogs/CreateOrganizationDialog.tsx (org-creation UI)
</read_first>
<action>
Run a repo-wide grep for "Hall of Fame" and "Manager Reviews" (case-insensitive) across `src/`, `supabase/functions/`, and `supabase/migrations/`. Identify every code path that creates either folder. Produce an inline audit in this task's verification output listing:
- File path
- Line number
- Context (which function, which trigger)
- Whether it's already removed (test stub) or still active

Then delete every active code path. Test stubs and migration history references are exempt (they're regression sentinels).

Repo-wide grep commands to run:
```
grep -rn "Hall of Fame" src/ supabase/functions/ supabase/migrations/
grep -rn "Manager Reviews" src/ supabase/functions/ supabase/migrations/
grep -rn "hall_of_fame\|manager_reviews" src/ supabase/functions/ supabase/migrations/
```
</action>
<acceptance_criteria>
- After cleanup, the only remaining matches for "Hall of Fame" or "Manager Reviews" in source code are inside `__tests__/` files OR inside `supabase/migrations/` (historical SQL — must not be edited)
- `grep -rn "Hall of Fame" src/ supabase/functions/ | grep -v __tests__ | grep -v "\.snap"` returns zero matches
- `grep -rn "Manager Reviews" src/ supabase/functions/ | grep -v __tests__ | grep -v "\.snap"` returns zero matches
</acceptance_criteria>
</task>

<task id="36-04-T2">
<read_first>
- supabase/functions/onboarding-init/index.ts (if it exists — onboarding seeder)
- supabase/functions/_shared/onboarding.ts (if it exists)
- supabase/functions/ (top-level — look for any seed/init function)
</read_first>
<action>
If any Edge Function seeds default folders on org creation or first signup, audit it. If "Hall of Fame" / "Manager Reviews" are seeded, remove the inserts. Keep any non-magic seed logic (e.g., a default "Inbox" folder if one exists is fine — only the two named magic folders are forbidden).

If no such function exists, this task is a no-op — log "no seeder found" and pass.
</action>
<acceptance_criteria>
- Every Edge Function in `supabase/functions/` is grep-clean for both "Hall of Fame" and "Manager Reviews" (case-insensitive)
- If a seeder file was edited, it still compiles (Deno check or `supabase functions deploy --dry-run`)
- If no seeder existed, the task documents that finding in its output
</acceptance_criteria>
</task>

<task id="36-04-T3">
<read_first>
- src/components/dialogs/__tests__/CreateWorkspaceDialog.phase25.test.tsx (existing pattern)
- src/hooks/useOrganizationMutations.ts (the org-creation mutation)
</read_first>
<action>
Extend `src/components/dialogs/__tests__/CreateWorkspaceDialog.phase25.test.tsx` (or add a sibling test file `src/hooks/__tests__/useOrganizationMutations.noMagicFolders.test.ts`) to assert that `useCreateOrganization` source does NOT contain "Hall of Fame" or "Manager Reviews" strings. Mirror the existing Phase 25 assertion pattern: read the hook source as text, grep for the literals, assert no match.

Also add a real-DB integration test (optional but recommended): create a fresh organization via the mutation, then immediately query `folders` table for that org_id, assert zero rows OR only the rows that are non-magic (e.g., an "Inbox" if one exists by design — but NOT the two named magic folders).
</action>
<acceptance_criteria>
- The test file contains `expect(src).not.toMatch(/Hall of Fame/i)`
- The test file contains `expect(src).not.toMatch(/Manager Reviews/i)`
- `npm run test -- noMagicFolders` (or the existing Phase25 test name) exits 0
</acceptance_criteria>
</task>

<task id="36-04-T4" type="verification">
<read_first>
- (no files — dev-browser verification)
</read_first>
<action>
Use dev-browser on `https://app.callvaultai.com`:
1. Sign up a fresh throwaway account (`qa-bug08-{timestamp}@vibeos.com`)
2. Complete signup, get to home
3. Navigate to Workspaces / Folders sidebar
4. Assert: NO folder named "Hall of Fame" exists
5. Assert: NO folder named "Manager Reviews" exists
6. Delete the throwaway account afterwards via Supabase Auth admin (per Phase 29 QA-23 protocol)

If signup is currently broken (Phase 31 still in progress), use an existing test account that has not yet had auto-folders created. If all test accounts already have them, manually delete the magic folders via the UI first, sign out / sign back in, and verify they don't reappear.
</action>
<acceptance_criteria>
- Fresh signup OR fresh re-login shows NO "Hall of Fame" folder in sidebar
- Fresh signup OR fresh re-login shows NO "Manager Reviews" folder in sidebar
- Cleanup completed: throwaway account deleted from Supabase Auth admin
</acceptance_criteria>
</task>

## Verification

- `npm run typecheck` exits 0
- `npm run test -- phase25` exits 0 (existing tests still pass)
- `npm run test -- noMagicFolders` exits 0 (new test)
- Dev-browser per T4
- Atomic commit: `fix(36-04): remove all auto-folder seeders (Hall of Fame, Manager Reviews) [BUG-08]`

## Must-Haves (Goal-Backward)

1. Zero "Hall of Fame" or "Manager Reviews" references in active code (tests + migrations exempt).
2. Fresh signup produces zero magic folders.
3. Existing accounts that already have these folders are unaffected (deletion is a manual user action; this fix only stops new creation).

## Out of Scope

- Deletion of pre-existing magic folders from existing user accounts (out of scope — user data, not our call)
- "Inbox" or other non-magic default folders (still allowed if they exist by design)
- Rebranding the magic-folder concept (BACKLOG)
