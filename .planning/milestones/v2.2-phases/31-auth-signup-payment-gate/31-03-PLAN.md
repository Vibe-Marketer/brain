---
phase: 31-auth-signup-payment-gate
plan: 03
type: execute
wave: 3
depends_on: [31-01, 31-02]
files_modified:
  - src/pages/SharedCallView.tsx
  - src/pages/Login.tsx
  - .planning/phases/31-auth-signup-payment-gate/31-03-VERIFY.md
autonomous: true
requirements: [AUTH-05]
must_haves:
  truths:
    - "Before any Google OAuth signin happens from /s/:token, sessionStorage.pendingShareToken is written so post-login redirect lands on /s/{token}"
    - "Login.tsx handleGoogleSignIn writes pendingShareToken from the current /s/:token route context (if applicable) before calling supabase.auth.signInWithOAuth"
    - "Dev-browser verification confirms a logged-out user clicking 'Continue with Google' from a share-call URL lands back on the share-call URL after OAuth (not the home page)"
  artifacts:
    - path: "src/pages/SharedCallView.tsx"
      provides: "Updated to write pendingShareToken to sessionStorage before signin/signup redirects"
      contains: "pendingShareToken"
    - path: ".planning/phases/31-auth-signup-payment-gate/31-03-VERIFY.md"
      provides: "Dev-browser screenshots + PASS evidence for the AUTH-05 surface fix"
      contains: "share-call wrong-recipient routing"
  key_links:
    - from: "SharedCallView.tsx (anon user)"
      to: "Login.tsx → Google OAuth → /s/{token}"
      via: "sessionStorage.pendingShareToken bridge across the OAuth round-trip"
      pattern: "pendingShareToken"
---

<objective>
Close AUTH-05's frontend surface scope: ensure a Google-authenticated user who hits a shared-call URL is routed to the share-call view (not home) so the share-call view can render an error message. The actual "wrong recipient" error UX is gated on Phase 32's backend signal restoration (per CONTEXT.md), but Phase 31 must guarantee the share-token survives the OAuth round-trip.

This plan is intentionally narrow — just the routing plumbing — because the underlying error UI lands in Phase 32.

Output: Updated SharedCallView (or Login) that writes `pendingShareToken` to sessionStorage before any auth redirect originating from a share-call URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/31-auth-signup-payment-gate/31-CONTEXT.md
@.planning/phases/31-auth-signup-payment-gate/31-UI-SPEC.md
@.planning/phases/31-auth-signup-payment-gate/31-RESEARCH.md
@.planning/phases/31-auth-signup-payment-gate/31-PATTERNS.md
@src/CLAUDE.md

<interfaces>
Existing plumbing the plan exercises:
- `sessionStorage.pendingShareToken` — already read at:
  - `src/components/ProtectedRoute.tsx:22-26` (redirects authenticated users to `/s/${token}` after login)
  - `src/pages/Login.tsx:19-32` (getPostLoginRedirect)
- The bug per RESEARCH F8 is that this key may not be WRITTEN reliably when an unauthenticated user lands on `/s/:token` and proceeds to sign in/up. Phase 31 must guarantee it.

UI-SPEC contract (AUTH-05):
- Forward-compatible string for Phase 32 wrong-recipient case: heading `'This share is for a different account'`, body `'This call was shared with {recipientEmail}. Sign out and sign back in with that account, or ask the sender to re-share it.'`, button `'Sign out'`. NOT IMPLEMENTED in Phase 31 — locked here for Phase 32 to consume.

Out of scope: backend response shape changes to `supabase/functions/share-call`. That's Phase 32 SHARE-02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit SharedCallView for unauthed-user OAuth flow + write pendingShareToken</name>
  <files>src/pages/SharedCallView.tsx</files>
  <read_first>
    - src/pages/SharedCallView.tsx (full file)
    - src/components/ProtectedRoute.tsx
    - src/pages/Login.tsx (after Plan 02 changes)
  </read_first>
  <action>
    Read `src/pages/SharedCallView.tsx` fully to identify the unauthed-user path.

    Two possible architectures depending on what the file does today:

    **Architecture A — SharedCallView redirects unauthed users to /login:**
    If the file detects no auth and navigates to `/login`, ADD a `sessionStorage.setItem('pendingShareToken', token)` call BEFORE the navigate. Use the `token` extracted from `useParams<{ token: string }>()`.

    **Architecture B — SharedCallView shows an inline "Sign in to view this call" button:**
    If the file renders an inline auth prompt with a button that goes to `/login`, add the `sessionStorage.setItem('pendingShareToken', token)` call inside that button's onClick handler.

    **If neither architecture matches:** identify the actual flow and add the sessionStorage write at the latest point before the auth redirect. The goal is: when the user finishes auth (any method), they land back at `/s/${token}` automatically via the existing `getPostLoginRedirect()` / `ProtectedRoute` logic.

    Additionally, verify that `Login.tsx handleGoogleSignIn` does NOT clobber `pendingShareToken`. If `redirectTo: ${window.location.origin}/` strips query params, that's fine — sessionStorage survives the OAuth round-trip. Add a one-line comment in `handleGoogleSignIn` noting the contract:
    ```ts
    // Note: sessionStorage.pendingShareToken (set by SharedCallView before redirect) survives the OAuth
    // round-trip and is consumed by ProtectedRoute.tsx after the user lands back on '/'.
    ```

    Do NOT add new UI in this task — the forward-compatible wrong-recipient copy (UI-SPEC) is Phase 32 scope.
  </action>
  <verify>
    <automated>grep -c "pendingShareToken" src/pages/SharedCallView.tsx | awk '$1 >= 1 {print "OK"}'</automated>
  </verify>
  <acceptance_criteria>
    - SharedCallView.tsx now writes `sessionStorage.setItem('pendingShareToken', token)` before any auth redirect
    - Login.tsx handleGoogleSignIn has the documenting comment
    - Typecheck passes
  </acceptance_criteria>
  <done>The share-token now survives the OAuth round-trip end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Dev-browser verify share-token survives Google OAuth round-trip</name>
  <files>.planning/phases/31-auth-signup-payment-gate/31-03-VERIFY.md</files>
  <read_first>
    - .planning/phases/31-auth-signup-payment-gate/31-CONTEXT.md (§AUTH-05)
    - .planning/phases/31-auth-signup-payment-gate/31-UI-SPEC.md (§OAuth wrong-recipient)
  </read_first>
  <action>
    Deploy to production. Then use dev-browser MCP against `app.callvaultai.com`:

    1. **Pick a real share-call token from production** — either generate a fresh share from the test account, or use an existing token visible in the database via SQL:
       ```sql
       SELECT token, recording_id, recipient_email
       FROM call_shares
       LIMIT 1;
       ```

    2. **Anon-state test:**
       - Open dev-browser in an incognito-equivalent state (clear cookies/storage)
       - Navigate to `https://app.callvaultai.com/s/{token}` with the real token
       - Take screenshot showing whatever the unauthed share-call view renders
       - If the page shows a sign-in CTA, click it; if it auto-redirects to /login, follow
       - In dev-browser console: `window.sessionStorage.getItem('pendingShareToken')` — verify it equals the token
       - Click "Continue with Google" and complete OAuth with a valid Google account (use the test credentials from .env.local)
       - After OAuth completes, verify the browser ends up at `https://app.callvaultai.com/s/{token}` (NOT at `/`)
       - Screenshot the final landing page

    3. **Document in `31-03-VERIFY.md`:**
       - Token used (truncated for safety)
       - sessionStorage state captured at each step
       - Final URL after OAuth
       - Screenshots: `31-03-anon-share-landing.png`, `31-03-sessionstorage-check.png`, `31-03-post-oauth-landing.png`
       - PASS/FAIL for each step

    4. **Known limitation note:** the share-call view will show "Call Not Found" if the OAuth account doesn't match the share recipient — this is the Phase 32 backend signal destruction issue, NOT a Phase 31 bug. Document it explicitly so reviewers don't flag it as a Phase 31 regression.

    Cleanup: if any test users were created, leave them as-is (no destructive cleanup required for this plan).
  </action>
  <verify>
    <automated>test -f .planning/phases/31-auth-signup-payment-gate/31-03-VERIFY.md && grep -c "PASS\|pendingShareToken" .planning/phases/31-auth-signup-payment-gate/31-03-VERIFY.md | awk '$1 >= 2 {print "OK"}'</automated>
  </verify>
  <acceptance_criteria>
    - 3 screenshots saved to phase screenshots/
    - VERIFY.md documents the token, sessionStorage state, final URL, PASS/FAIL
    - Known Phase-32-dependent limitation noted
  </acceptance_criteria>
  <done>AUTH-05 surface routing is verified live.</done>
</task>

</tasks>

<verification>
- Both tasks have completed `<verify>` blocks
- `npm run typecheck` passes
- Dev-browser confirms share-token survives the Google OAuth round-trip
- Phase 32 limitation explicitly noted
- No regression to authenticated direct-link share access (logged-in user with the right account still hits the share-call view normally)
</verification>
