---
phase: 16
plan: 01
subsystem: admin-center
status: executed
retroactive: true
reconstructed: 2026-06-12
requirements: [ADMC-01, ADMC-02, ADMC-03]
must_haves:
  truths:
    - "Admin reaches a dedicated /admin shell from the MAIN sidebar (admin-gated) with section nav + ⌘K palette"
    - "Dashboard shows live operational truth: deployed SHA vs main, runner state, Needs-You queue, ticket/user counts"
    - "Tickets are fully operable inside the shell; Settings AdminTab reduced to a pointer (no duplicate surfaces)"
  artifacts:
    - path: "src/pages/admin/AdminCenter.tsx"
      provides: "AppShell 4-pane admin shell, AdminGuard-gated"
    - path: "src/pages/admin/DashboardSection.tsx"
      provides: "Live dashboard — counts, health, deploy SHA, runner card, Needs You"
    - path: "src/pages/admin/TicketsSection.tsx"
      provides: "Live TicketTable + TicketDetailDialog + NewTicketDialog mounted in shell"
    - path: "src/components/admin/AdminGuard.tsx"
      provides: "Admin-only route gate"
    - path: "src/components/admin/AdminCommandPalette.tsx"
      provides: "⌘K palette — section jump, ticket search, quick actions"
    - path: "src/components/panes/AdminCategoryPane.tsx"
      provides: "Section nav pane"
    - path: "src/services/admin-dashboard.service.ts"
      provides: "Rebound dashboard queries (live tickets/user_roles), tagNeedsYou"
  key_links:
    - from: "src/components/ui/sidebar-nav.tsx"
      to: "/admin/dashboard"
      via: "isAdmin-gated ADMIN entry"
    - from: "src/components/settings/AdminTab.tsx"
      to: "Admin Center"
      via: "pointer card replacing System Overview + Tickets"
---

> **RETROACTIVELY RECONSTRUCTED from 16-01-SUMMARY.md + commits on 2026-06-12.**
> Phase 16 ran CONTEXT-direct (no formal PLAN.md at execution time) for velocity.
> This plan reverse-engineers the intended scope from what actually shipped so the
> GSD planning record matches the codebase. It is NOT backdated — header dates the reconstruction.

# Phase 16 Plan 01: Admin Center Shell + Dashboard + Tickets

## Goal

Port the proven `/admin` shell from branch `worktree-admin-center` onto the live foundation: a MAIN-sidebar ADMIN entry (admin-only) opening an AppShell 4-pane shell with a ⌘K palette, a live Dashboard, and the full live Tickets queue — replacing the cramped Settings AdminTab System Overview + Tickets surfaces with a pointer card.

## Source

All ports via `git show worktree-admin-center:<path>`, rebound to live schema (support_tickets→tickets, runner_runs→runner_state, status vocab → live 8-status set).

## Tasks

1. **Port cmdk command palette primitive** — add `src/components/ui/command.tsx` (sr-only DialogTitle instead of the phantom `@radix-ui/react-visually-hidden` transitive dep).
2. **Port admin shell primitives** — `AdminGuard.tsx` (+test, rebound to main's useUserRole), `adminDetailStore.ts`, `AdminCategoryPane.tsx` (Wave-1 categories only, Flags entry stripped), `AdminCommandPalette.tsx` (live tickets vocab; users/flags/automation/qa groups stripped), `AdminCenter.tsx` (Wave-1 sections only).
3. **Rebind dashboard to live schema** — `admin-dashboard.service.ts` (+test) and `DashboardSection.tsx`: tagNeedsYou → awaiting_approval/escalated/critical_aging; runner_state untyped read with graceful "not deployed yet"; deploy card = bundle SHA vs GitHub main HEAD best-effort.
4. **Mount /admin shell** — routes + sidebar ADMIN entry (isAdmin gate, useFeatureFlags gate stripped), TicketsSection composition (zero edits to 15-01-owned TicketTable/Dialog), command palette, `/admin`→"ADMIN" TopBar label.
5. **Reduce Settings AdminTab to a pointer** — replace System Overview + Tickets with an "Admin Center moved" card; keep User Management until Wave 2.

## Verification

- vitest full suite green (19 new/ported tests: 11 dashboard service, 3 AdminGuard, 2 sidebar visibility + manifest sync).
- tsc scoped 0 errors in new files; `npm run build` exit 0.
- Live: prod bundle bakes `VITE_VERCEL_GIT_COMMIT_SHA` == main HEAD; `AdminCenter` lazy chunk served from prod.

## Known intentional stubs (deferred, tracked)

- Runner card "not deployed yet" — `runner_state` ships with Phase 13; read wired now.
- Deploy card "main HEAD unavailable" fallback — unauthenticated GitHub API cannot read a private repo from the browser; card always shows the real deployed SHA.
- AdminTab "Advanced user management coming soon" — Wave 2 ports UsersSection + admin-manage-user.

## Commits (as executed)

`fc7b739`, `36ac41a` (port shell primitives), `f15ca66` (dashboard rebind), `459f79c` (mount shell), `9e767f0` (AdminTab pointer).
