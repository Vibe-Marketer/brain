---
phase: 16
plan: 03
subsystem: admin-center
status: executed
retroactive: true
reconstructed: 2026-06-12
requirements: [ADMC-05, ADMC-06]
must_haves:
  truths:
    - "QA section renders crawler runs from qa_runs, findings browsable, run-trigger surface (disabled v1)"
    - "Audit section shows an actor/action/target trail merging ticket_events + admin_audit_log"
    - "The autopilot nightly crawler records one qa_runs summary row per crawl via --record"
  artifacts:
    - path: "supabase/migrations/20260612150000_create_qa_runs.sql"
      provides: "Append-only qa_runs table — admin-read via has_role, service-role-write only"
    - path: "src/services/qa.service.ts"
      provides: "qa_runs read service + findings parse"
    - path: "src/pages/admin/QaSection.tsx"
      provides: "QA run ledger + findings + disabled Run-now (manual: npm run qa:crawl)"
    - path: "src/services/admin-audit.service.ts"
      provides: "Merged admin_audit_log + ticket_events trail, normalized + namespaced ids"
    - path: "src/pages/admin/AuditSection.tsx"
      provides: "Chronological merged audit trail with source badge + action filter + search"
  key_links:
    - from: "src/pages/admin/QaSection.tsx"
      to: "qa_runs"
      via: "useQaRuns → qa.service (admin-read RLS)"
    - from: "~/dev/autopilot/qa/triage.ts (--record)"
      to: "qa_runs"
      via: "service-role INSERT, best-effort, never changes triage exit code"
    - from: "src/pages/admin/AuditSection.tsx"
      to: "admin_audit_log + ticket_events"
      via: "admin-audit.service parallel reads merged into one AuditLog shape"
---

> **RETROACTIVELY RECONSTRUCTED from 16-03-SUMMARY.md + commits on 2026-06-12.**
> Phase 16 ran CONTEXT-direct (no formal PLAN.md at execution time) for velocity.
> This plan reverse-engineers the intended scope from what actually shipped so the
> GSD planning record matches the codebase. It is NOT backdated — header dates the reconstruction.
> Note: the `~/dev/autopilot/qa/*` changes are in the external autopilot repo, not this repo.

# Phase 16 Plan 03: Admin Center QA + Audit Sections

## Goal

Ship the last two Admin Center sections. **QA** (`/admin/qa`): a crawler-run ledger backed by a new append-only `qa_runs` table, fed by the autopilot nightly crawler's new `--record` step, with a latest-run card, run-history table, per-run findings, and a DISABLED Run-now button (v1 — remote trigger lands with Phase 13's dispatcher). **Audit** (`/admin/audit`): one chronological trail merging privileged admin actions (`admin_audit_log`) with ticket lifecycle events (`ticket_events`). Completes ADMC-05 + ADMC-06.

## Source

Branch `worktree-admin-center` QaSection/AuditSection ported and rebound; tests written fresh (branch shipped none for these sections); AuditSection extended from single-source to merged-source per the orchestrator scope.

## Tasks

1. **Create qa_runs** — append-only migration matching the branch `QaRun` TS interface (status/routes_crawled/findings_count/critical_count/report jsonb/triggered_by); admin-only SELECT via `has_role`; no client write policies (service-role INSERT only).
2. **Port QA service + section** — `qa.service.ts` (+test), `useQaRuns.ts`, `QaSection.tsx`; Run-now DISABLED via a span-wrapped tooltip trigger (`manual: npm run qa:crawl`) since a disabled button suppresses pointer events.
3. **Build merged Audit trail** — `admin-audit.service.ts` (+test) reads both tables in parallel, normalizes ticket events to `ticket_<event_type>` with `{old_value,new_value}`, namespaces ids (`aal:`/`te:`), sorts newest-first, caps 200, resolves actor emails; `AuditSection.tsx` with source badge + cross-source action filter + search + `<details>` metadata. Switch `useAdminUsers` audit invalidation to the `['admin','audit']` prefix so filtered queries refresh (Rule 1 — exact-key would miss filtered queries).
4. **Wire into shell** — AdminCategoryPane, AdminCommandPalette, AdminCenter, routes.manifest.
5. **Add autopilot --record step** *(external repo `~/dev/autopilot/`)* — `qa/triage.ts` + `qa/nightly-crawl.sh`: `--record` writes ONE qa_runs row per crawl via service-role key; best-effort (failure warns, never changes triage exit code); `report.findings` carries post-noise-filter findings.

## Verification

- vitest: 19 new tests pass (5 qa.service, 6 admin-audit.service, 5 QaSection, 4 AuditSection − 1 dup). Full suite 1908 passing; the 2 failures are pre-existing/environmental and reference no 16-03 file.
- tsc scoped 0 errors; `npm run build` exit 0 (AdminCenter chunk includes QA + Audit).
- qa_runs live posture: service-role INSERT ok, anon SELECT blocked by RLS; `--record` end-to-end write verified + cleaned up.

## Known stubs

- QaSection Run-now button DISABLED — intentional v1; no browser-reachable crawl trigger until Phase 13's dispatcher. Tooltip points at the real manual command.

## Commits (as executed)

`26bf1b26` (qa_runs table), `2c9c1a2d` (QA section), `8650c9eb` (merged Audit section), `5dc7ae83` (wire QA+Audit into pane/palette/routes). Autopilot `--record` committed in the external autopilot repo (`a2936a5`).
