---
phase: "06.3.2-fathom-provider-id-rename"
plan: "05"
type: execute
wave: 4
depends_on:
  - "06.3.2-03"
  - "06.3.2-04"
files_modified:
  - src/CLAUDE.md
  - supabase/CLAUDE.md
  - docs/integrations/01-connector-pipeline-vet.md
  - docs/architecture/api-naming-conventions.md
  - docs/security/oauth-token-isolation.md
autonomous: true
requirements:
  - "none/TBD"
requirements_addressed:
  - "D-01"
  - "D-02"
  - "D-03"
  - "D-04"
  - "D-05"
  - "D-06"
  - "D-07"
  - "D-08"
  - "D-09"
  - "D-10"
must_haves:
  truths:
    - "D-01 through D-10 are reflected in active docs and final verification gates."
    - "Active code/docs outside historical migrations no longer contain `legacy_recording_id`."
    - "Final verification includes DB dependency scans, exact `rg` gates excluding historical migrations, `npm run type-check`, `npm run build`, and focused tests named in research/validation."
    - "Production DB migration is deferred until final rollout after code is committed/build-ready, and then runs in a maintenance-window/immediate app+Edge deploy sequence."
    - "Production-facing smoke requirements are recorded without claiming deployment or live verification unless actually performed."
  artifacts:
    - path: "src/CLAUDE.md"
      provides: "Current recording ID rules naming `fathom_provider_id` and preserving `isLegacyId()` semantics"
    - path: "supabase/CLAUDE.md"
      provides: "Supabase-side rename guidance if the file contains old recording ID wording"
    - path: "docs/integrations/01-connector-pipeline-vet.md"
      provides: "Connector pipeline docs with `fathom_provider_id`"
  key_links:
    - from: "final exact-string gates"
      to: "historical migrations"
      via: "exclude `supabase/migrations` from active-code old-name failure"
    - from: "final build/type checks"
      to: "renamed DB schema"
      via: "Plan 01 schema push and Plan 02 generated types"
---

<objective>
Update active docs and run the final phase verification gates.

Purpose: This phase must close with proof across DB schema, generated types, active source, focused tests, type-check, and build. Docs should describe the current field name, not a planned rename or a deprecated legacy column.

Output: Active documentation reflects `fathom_provider_id`; final verification output is captured for `$gsd-verify-work`.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@src/CLAUDE.md
@supabase/CLAUDE.md
@docs/CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-CONTEXT.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-RESEARCH.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-VALIDATION.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-03-SUMMARY.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update active docs and comments to current provider-ID terminology</name>
  <files>src/CLAUDE.md, supabase/CLAUDE.md, docs/integrations/01-connector-pipeline-vet.md, docs/architecture/api-naming-conventions.md, docs/security/oauth-token-isolation.md</files>
  <read_first>
    - src/CLAUDE.md
    - supabase/CLAUDE.md
    - docs/CLAUDE.md
    - docs/integrations/01-connector-pipeline-vet.md
    - docs/architecture/api-naming-conventions.md
    - docs/security/oauth-token-isolation.md
  </read_first>
  <action>Update active docs that mention `legacy_recording_id` to use `fathom_provider_id`. In `src/CLAUDE.md`, remove the "planned rename" note and update the two-ID-system table/rules so `recordings.id` is the canonical UUID and `recordings.fathom_provider_id` is the numeric Fathom provider ID used for Fathom-owned BIGINT bridge tables. Explicitly preserve the rule that `isLegacyId()` remains a format helper name for numeric-vs-UUID detection, not a DB-column reference. In `supabase/CLAUDE.md`, update any Supabase schema guidance from `legacy_recording_id` to `fathom_provider_id` while preserving real-DB integration test safety rules. In active docs under `docs/integrations`, `docs/architecture`, and `docs/security`, update current guidance to the renamed column. Do not edit archived historical docs unless they are active guidance linked from current docs; archived/reference material can retain historical terminology. Do not rename `source_call_id`.</action>
  <verify>
    <automated>rg -n "legacy_recording_id|fathom_provider_id|isLegacyId|source_call_id" src/CLAUDE.md supabase/CLAUDE.md docs/integrations/01-connector-pipeline-vet.md docs/architecture/api-naming-conventions.md docs/security/oauth-token-isolation.md</automated>
  </verify>
  <acceptance_criteria>
    - `src/CLAUDE.md` contains `fathom_provider_id` and no "planned rename" language for this column.
    - Active docs do not instruct future agents to use `legacy_recording_id`.
    - Docs explicitly preserve `isLegacyId()` as a numeric-format helper name.
    - `source_call_id` references remain if they describe the separate text source ID.
  </acceptance_criteria>
  <done>Active documentation describes the renamed provider-ID column accurately.</done>
</task>

<task type="auto">
  <name>Task 2: Run DB, source, type, build, and focused test gates</name>
  <files>src/CLAUDE.md, supabase/CLAUDE.md, docs/integrations/01-connector-pipeline-vet.md, docs/architecture/api-naming-conventions.md, docs/security/oauth-token-isolation.md</files>
  <read_first>
    - .planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-VALIDATION.md
    - .planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-RESEARCH.md
    - AGENTS.md
  </read_first>
  <action>Run final pre-production verification in this order. First, run DB assertions against the same dedicated test/local schema used for Plan 01 and Plan 02: `information_schema.columns` must confirm `fathom_provider_id` exists exactly once and `legacy_recording_id` does not exist; `information_schema.views` and `pg_proc` assertions must fail if any active old-name definitions remain; `pg_indexes` assertions must fail if any active old-name indexes remain on `public.recordings`; and `pg_constraint` assertions must fail if any active old-name constraints remain by either `conname` or `pg_get_constraintdef`. This constraint assertion is mandatory because stale names such as `recordings_organization_id_legacy_recording_id_key` can survive while index-only checks pass. Second, run exact source gates excluding immutable historical migrations: no `legacy_recording_id` in `src`, `supabase/functions`, or active `docs` except archived docs if explicitly excluded; no `legacyId` property use for `ResolvedRecording` callers, while `isLegacyId` remains present. Third, run `npm run type-check`, focused tests from research/validation, the named integration tests with an explicit dedicated-test-env preflight, and `npm run build`. Focused tests are `npm test -- src/lib/__tests__/recording-ids.test.ts src/services/__tests__/sync-tab.service.test.ts supabase/functions/fathom-refresh/__tests__/fathom-refresh.test.ts`. For integration tests, first check for the dedicated test DB environment (`SUPABASE_TEST_URL`, `SUPABASE_TEST_ANON_KEY`, and `SUPABASE_TEST_SERVICE_ROLE_KEY`, or the repo's documented equivalent dedicated-test variables). If dedicated test DB variables are absent, record `SKIPPED` with missing variable names and do not run against production. If they are present, run `npm run test:integration -- src/services/__tests__/folders.integration.test.ts src/test/migrations/phase40-fathom-refresh.integration.test.ts supabase/functions/share-call/__tests__/share-call.integration.test.ts supabase/functions/auto-tag-calls/__tests__/auto-tag-calls.integration.test.ts` as a blocking final gate; failures must fail the task. Do not claim integration success if the test env is unavailable. Do not claim deployed/live-verified unless Task 3 production rollout and smoke are actually run.</action>
  <verify>
    <automated>bash -lc 'if [[ -z "${SUPABASE_TEST_DB_URL:-}" ]]; then echo "BLOCKED: SUPABASE_TEST_DB_URL is required for final pre-production DB assertions"; exit 2; fi; supabase db query --db-url "$SUPABASE_TEST_DB_URL" "$(cat <<'"'"'SQL'"'"'
do $$
declare
  old_columns integer;
  new_columns integer;
  stale_views integer;
  stale_functions integer;
  stale_indexes integer;
  stale_constraints integer;
begin
  select count(*) into old_columns
  from information_schema.columns
  where table_schema = '"'"'public'"'"' and table_name = '"'"'recordings'"'"' and column_name = '"'"'legacy_recording_id'"'"';

  select count(*) into new_columns
  from information_schema.columns
  where table_schema = '"'"'public'"'"' and table_name = '"'"'recordings'"'"' and column_name = '"'"'fathom_provider_id'"'"';

  select count(*) into stale_views
  from information_schema.views
  where table_schema = '"'"'public'"'"' and view_definition ilike '"'"'%legacy_recording_id%'"'"';

  select count(*) into stale_functions
  from pg_proc p
  join pg_namespace n on n.oid = p.pronamespace
  where n.nspname = '"'"'public'"'"' and pg_get_functiondef(p.oid) ilike '"'"'%legacy_recording_id%'"'"';

  select count(*) into stale_indexes
  from pg_indexes
  where schemaname = '"'"'public'"'"' and tablename = '"'"'recordings'"'"'
    and (indexname ilike '"'"'%legacy_recording_id%'"'"' or indexdef ilike '"'"'%legacy_recording_id%'"'"');

  select count(*) into stale_constraints
  from pg_constraint c
  where c.conrelid = '"'"'public.recordings'"'"'::regclass
    and (c.conname ilike '"'"'%legacy_recording_id%'"'"' or pg_get_constraintdef(c.oid) ilike '"'"'%legacy_recording_id%'"'"');

  if old_columns <> 0 or new_columns <> 1 or stale_views <> 0 or stale_functions <> 0 or stale_indexes <> 0 or stale_constraints <> 0 then
    raise exception '"'"'stale legacy_recording_id state: old_columns=%, new_columns=%, stale_views=%, stale_functions=%, stale_indexes=%, stale_constraints=%'"'"',
      old_columns, new_columns, stale_views, stale_functions, stale_indexes, stale_constraints;
  end if;
end $$;
SQL
)"' && ! rg -n "legacy_recording_id" src supabase/functions docs --glob '!docs/archive/**' --glob '!src/types/supabase.ts' && ! rg -n "\\.legacyId\\b|legacyId:" src supabase/functions --glob '!**/*.md' && rg -n "isLegacyId" src/lib/recording-ids.ts src/lib/__tests__/recording-ids.test.ts && npm run type-check && npm test -- src/lib/__tests__/recording-ids.test.ts src/services/__tests__/sync-tab.service.test.ts supabase/functions/fathom-refresh/__tests__/fathom-refresh.test.ts && bash -lc 'missing=(); for v in SUPABASE_TEST_URL SUPABASE_TEST_ANON_KEY SUPABASE_TEST_SERVICE_ROLE_KEY; do [[ -n "${!v:-}" ]] || missing+=("$v"); done; if (( ${#missing[@]} )); then echo "SKIPPED integration tests: missing dedicated test DB vars ${missing[*]}"; else npm run test:integration -- src/services/__tests__/folders.integration.test.ts src/test/migrations/phase40-fathom-refresh.integration.test.ts supabase/functions/share-call/__tests__/share-call.integration.test.ts supabase/functions/auto-tag-calls/__tests__/auto-tag-calls.integration.test.ts; fi' && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Dedicated test/local DB assertions mechanically fail unless the renamed column exists and no active old-name DB dependencies remain, including `pg_constraint` checks by both `conname` and `pg_get_constraintdef`.
    - Exact `rg` gate finds no active `legacy_recording_id` in `src`, `supabase/functions`, or active docs.
    - Exact `rg` gate finds no `ResolvedRecording.legacyId` property use; `isLegacyId` still exists.
    - `npm run type-check` exits zero.
    - Focused unit tests exit zero.
    - `npm run build` exits zero.
    - Integration tests are run when a dedicated test DB is configured; unavailable env is reported as `SKIPPED` with missing vars, not passed, and configured-env failures are blocking.
  </acceptance_criteria>
  <done>Final automated verification is complete and ready for `$gsd-verify-work` review.</done>
</task>

<task type="checkpoint:human-action" gate="production-rollout">
  <name>Task 3: [BLOCKING] Production rollout after code is build-ready</name>
  <files>supabase/migrations/20260610*_rename_legacy_recording_id_to_fathom_provider_id.sql</files>
  <read_first>
    - AGENTS.md
    - supabase/CLAUDE.md
    - .planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-01-SUMMARY.md
    - .planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-05-SUMMARY.md
  </read_first>
  <action>[BLOCKING] Run production rollout only after Plans 03 and 04 are complete, Task 2 has passed `npm run type-check`, focused tests, and `npm run build`, and the matching app/Edge code is committed and ready for immediate deployment. Use a maintenance-window sequence: push the DB migration to the production linked project, immediately deploy/push the matching app and Edge Function code per the repo's direct-main workflow, and then run production DB assertions plus representative smoke checks. Do not run this production push in Plan 01. If the executor cannot immediately deploy the matching app/Edge code after the DB migration, stop and schedule a maintenance window instead of creating a production compatibility gap. Do not claim deployed/live-verified unless the production push, app/Edge deploy, DB assertions, and smoke checks actually run.</action>
  <verify>
    <automated>bash -lc 'SUPABASE_ACCESS_TOKEN="$SUPABASE_ACCESS_TOKEN" supabase db push --linked && supabase db query --linked "$(cat <<'"'"'SQL'"'"'
do $$
declare
  old_columns integer;
  new_columns integer;
  stale_views integer;
  stale_functions integer;
  stale_indexes integer;
  stale_constraints integer;
begin
  select count(*) into old_columns
  from information_schema.columns
  where table_schema = '"'"'public'"'"' and table_name = '"'"'recordings'"'"' and column_name = '"'"'legacy_recording_id'"'"';

  select count(*) into new_columns
  from information_schema.columns
  where table_schema = '"'"'public'"'"' and table_name = '"'"'recordings'"'"' and column_name = '"'"'fathom_provider_id'"'"';

  select count(*) into stale_views
  from information_schema.views
  where table_schema = '"'"'public'"'"' and view_definition ilike '"'"'%legacy_recording_id%'"'"';

  select count(*) into stale_functions
  from pg_proc p
  join pg_namespace n on n.oid = p.pronamespace
  where n.nspname = '"'"'public'"'"' and pg_get_functiondef(p.oid) ilike '"'"'%legacy_recording_id%'"'"';

  select count(*) into stale_indexes
  from pg_indexes
  where schemaname = '"'"'public'"'"' and tablename = '"'"'recordings'"'"'
    and (indexname ilike '"'"'%legacy_recording_id%'"'"' or indexdef ilike '"'"'%legacy_recording_id%'"'"');

  select count(*) into stale_constraints
  from pg_constraint c
  where c.conrelid = '"'"'public.recordings'"'"'::regclass
    and (c.conname ilike '"'"'%legacy_recording_id%'"'"' or pg_get_constraintdef(c.oid) ilike '"'"'%legacy_recording_id%'"'"');

  if old_columns <> 0 or new_columns <> 1 or stale_views <> 0 or stale_functions <> 0 or stale_indexes <> 0 or stale_constraints <> 0 then
    raise exception '"'"'stale legacy_recording_id state: old_columns=%, new_columns=%, stale_views=%, stale_functions=%, stale_indexes=%, stale_constraints=%'"'"',
      old_columns, new_columns, stale_views, stale_functions, stale_indexes, stale_constraints;
  end if;
end $$;
SQL
)"'</automated>
  </verify>
  <acceptance_criteria>
    - Production migration is not run until matching app/Edge code is committed/build-ready and can be deployed immediately.
    - Production `supabase db push --linked` exits zero during the approved maintenance-window/immediate-deploy sequence.
    - Production SQL assertions mechanically fail if `legacy_recording_id` remains in live columns, public view definitions, public function definitions, `public.recordings` index names/definitions, or `public.recordings` constraint names/definitions.
    - Matching app and Edge Function deploys are completed immediately after the production DB migration, or the rollout is stopped before the production DB migration runs.
    - Production smoke checks for call list/search, Fathom refresh, share-link creation/listing, and MCP shared/contact call tools are run before claiming live verification.
  </acceptance_criteria>
  <done>Production DB, app, and Edge code are migrated together with no old-name DB dependencies remaining.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> future agents | Active docs can cause future agents to use stale or unsafe DB fields. |
| verification commands -> test DB / production DB | Pre-production assertions and integration tests must use dedicated test DBs; production migration is a separate final rollout gate after build readiness. |
| build/type gates -> deploy readiness | Passing local gates are necessary but not proof of deployment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3.2-15 | Repudiation | final verification claims | mitigate | Record exact command results; separate implemented, locally tested, integration-tested, production-migrated, deployed, and live-verified status. |
| T-06.3.2-16 | Tampering | integration test DB | mitigate | Use dedicated Supabase test guards; do not run integration tests against production fallback. |
| T-06.3.2-17 | Information Disclosure | docs and generated types | mitigate | Docs mention schema names only and do not include secrets; preserve oauth token-isolation guidance. |
| T-06.3.2-18 | Denial of Service | production rollout order | mitigate | Defer production DB migration until final rollout after build-ready code; use a maintenance-window/immediate app+Edge deploy sequence and failing post-migration DB assertions. |
| T-06.3.2-SC | Tampering | package installs | accept | No package installation occurs in this plan. |
</threat_model>

<verification>
Final required gates:
- Dedicated test/local DB dependency assertions for columns, views, functions, constraints, and indexes.
- `pg_constraint` final assertion checks both `conname` and `pg_get_constraintdef` for stale `legacy_recording_id`; it must fail mechanically if stale constraints remain.
- Exact `rg legacy_recording_id` gate excluding historical migrations and archived docs.
- Exact `rg` gate for `.legacyId`/`legacyId:` property use while preserving `isLegacyId`.
- `npm run type-check`.
- `npm test -- src/lib/__tests__/recording-ids.test.ts src/services/__tests__/sync-tab.service.test.ts supabase/functions/fathom-refresh/__tests__/fathom-refresh.test.ts`.
- `bash -lc 'missing=(); for v in SUPABASE_TEST_URL SUPABASE_TEST_ANON_KEY SUPABASE_TEST_SERVICE_ROLE_KEY; do [[ -n "${!v:-}" ]] || missing+=("$v"); done; if (( ${#missing[@]} )); then echo "SKIPPED integration tests: missing dedicated test DB vars ${missing[*]}"; else npm run test:integration -- src/services/__tests__/folders.integration.test.ts src/test/migrations/phase40-fathom-refresh.integration.test.ts supabase/functions/share-call/__tests__/share-call.integration.test.ts supabase/functions/auto-tag-calls/__tests__/auto-tag-calls.integration.test.ts; fi'`.
- `npm run build`.
- Production rollout gate after code is build-ready: maintenance-window production DB push, immediate matching app+Edge deploy, production DB assertions, and production smoke checks. Skip or block this gate rather than running production DB migration without immediate deploy readiness.
</verification>

<success_criteria>
- Active code/docs use `fathom_provider_id`; historical migration SQL is left immutable.
- Generated/manual/app/Edge Function types and source all agree on the renamed field.
- DB dependency assertions, type-check, build, and focused tests are green or any unavailable integration environment is stated plainly.
- Production migration is not performed until final rollout after code is build-ready, with immediate matching app/Edge deploy coverage.
- No deployment or production smoke is claimed unless it is actually performed after these local gates.
</success_criteria>

<output>
Create `.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-05-SUMMARY.md` when done.
</output>
