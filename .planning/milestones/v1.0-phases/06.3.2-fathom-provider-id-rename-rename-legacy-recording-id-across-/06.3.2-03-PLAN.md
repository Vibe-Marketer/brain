---
phase: "06.3.2-fathom-provider-id-rename"
plan: "03"
type: execute
wave: 3
depends_on:
  - "06.3.2-02"
files_modified:
  - src/lib/recording-ids.ts
  - src/lib/__tests__/recording-ids.test.ts
  - src/services/sync-tab.service.ts
  - src/services/api-tokens.service.ts
  - src/services/recordings.service.ts
  - src/services/import-sources.service.ts
  - src/services/meetings-sync.service.ts
  - src/services/transcript-filters.service.ts
  - src/services/folders.service.ts
  - src/services/__tests__/sync-tab.service.test.ts
  - src/services/__tests__/folders.integration.test.ts
  - src/hooks/useGlobalSearch.ts
  - src/hooks/useWorkspaces.ts
  - src/hooks/useContacts.ts
  - src/hooks/useFathomRefresh.ts
  - src/hooks/useWorkspaceAssignment.ts
  - src/hooks/useCallDetailMutations.ts
  - src/hooks/__tests__/useGlobalSearch.p95.integration.test.ts
  - src/components/transcripts/TranscriptsTab.tsx
  - src/components/transcript-library/TranscriptTable.tsx
  - src/components/transcript-library/BulkActionToolbarEnhanced.tsx
  - src/components/transcripts/__tests__/folder-filtering.test.ts
  - src/test/migrations/phase40-fathom-refresh.integration.test.ts
autonomous: true
requirements:
  - "none/TBD"
requirements_addressed:
  - "D-05"
  - "D-06"
  - "D-07"
  - "D-09"
  - "D-10"
must_haves:
  truths:
    - "D-05 and D-10: `isLegacyId()` remains named and keeps numeric-vs-UUID format-detection semantics."
    - "D-06: `ResolvedRecording.legacyId` is renamed to `ResolvedRecording.fathomProviderId`, and callers that read resolved entries use `.fathomProviderId`."
    - "D-07: Active frontend services, hooks, components, and tests use `fathom_provider_id` for the DB column."
    - "D-09: `source_call_id` remains unchanged and Fathom refresh string fallback behavior remains intact."
  artifacts:
    - path: "src/lib/recording-ids.ts"
      provides: "Canonical UUID/BIGINT boundary updated for `fathom_provider_id`"
    - path: "src/services/*.ts"
      provides: "Service-layer queries and row mappings updated to the renamed DB column"
    - path: "src/hooks/*.ts"
      provides: "Hook-level selectors and fallback row mappings updated to the renamed DB column"
    - path: "src/components/**/*.tsx"
      provides: "Component adapters and local helper mappings updated to the renamed DB column"
  key_links:
    - from: "toRecordingUuidBatch().resolved[].fathomProviderId"
      to: "folder/share/delete BIGINT bridge arrays"
      via: "preserved `legacyIds` return array for format-level compatibility"
    - from: "recordings.fathom_provider_id"
      to: "source_call_id"
      via: "Fathom refresh keeps source_call_id fallback when provider ID is null"
---

<objective>
Update active frontend/app TypeScript references from `legacy_recording_id` to `fathom_provider_id`, while preserving numeric/UUID helper semantics and `source_call_id`.

Purpose: After Plan 02 regenerates DB types, TypeScript errors become the checklist for active app source. This plan handles the browser-side services, hooks, components, ID boundary, and app tests.

Output: App source compiles against `fathom_provider_id`; `ResolvedRecording` mirrors the DB field as `fathomProviderId`; `isLegacyId()` remains unchanged as a format helper.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@src/CLAUDE.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-CONTEXT.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-RESEARCH.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-PATTERNS.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-02-SUMMARY.md
@src/lib/recording-ids.ts
@src/lib/__tests__/recording-ids.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update the canonical recording ID boundary</name>
  <files>src/lib/recording-ids.ts, src/lib/__tests__/recording-ids.test.ts</files>
  <read_first>
    - src/lib/recording-ids.ts
    - src/lib/__tests__/recording-ids.test.ts
    - src/CLAUDE.md
  </read_first>
  <behavior>
    - Numeric input still resolves by querying `recordings.fathom_provider_id`.
    - UUID input still resolves by querying `recordings.id`.
    - `isLegacyId()` remains exported with the same name and same numeric-string behavior.
    - `ResolvedRecording` exposes `fathomProviderId`, not `legacyId`.
    - `toRecordingUuidBatch()` still returns `legacyIds` as the numeric Fathom provider ID array for existing callers that need BIGINT bridge values.
  </behavior>
  <action>In `src/lib/recording-ids.ts`, rename `ResolvedRecording.legacyId` to `fathomProviderId`. Update select strings from `id, legacy_recording_id, source_app` to `id, fathom_provider_id, source_app`; update `.eq('legacy_recording_id', legacyId)` and `.in('legacy_recording_id', numericIds)` to use `fathom_provider_id`; update local row types and row mapping from `r.legacy_recording_id` to `r.fathom_provider_id`. Keep the local variable name `legacyId` where it means numeric input format, keep the returned array property `legacyIds` unless TypeScript callers require a separate rename, and keep `isLegacyId()` name and behavior unchanged per D-05/D-10. Update comments to describe numeric Fathom provider IDs rather than a DB column named legacy. In `src/lib/__tests__/recording-ids.test.ts`, update mocked rows, expected select strings, `.eq`/`.in` expectations, and object expectations to `fathom_provider_id` and `fathomProviderId`, while keeping tests that prove `isLegacyId()` returns true for finite numbers and digit-only strings.</action>
  <verify>
    <automated>npm test -- src/lib/__tests__/recording-ids.test.ts && rg -n "isLegacyId|fathom_provider_id|fathomProviderId|legacy_recording_id|legacyId" src/lib/recording-ids.ts src/lib/__tests__/recording-ids.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/recording-ids.ts` exports `ResolvedRecording` with `fathomProviderId: number | null`.
    - `src/lib/recording-ids.ts` still exports `isLegacyId`.
    - `src/lib/recording-ids.ts` contains no `legacy_recording_id` query/select text.
    - Unit tests assert `fathom_provider_id` query strings and `fathomProviderId` resolved objects.
    - `npm test -- src/lib/__tests__/recording-ids.test.ts` exits zero.
  </acceptance_criteria>
  <done>The canonical ID boundary uses `fathom_provider_id` without renaming numeric-format helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Update app services and hooks from compiler errors</name>
  <files>src/services/sync-tab.service.ts, src/services/api-tokens.service.ts, src/services/recordings.service.ts, src/services/import-sources.service.ts, src/services/meetings-sync.service.ts, src/services/transcript-filters.service.ts, src/services/folders.service.ts, src/services/__tests__/sync-tab.service.test.ts, src/services/__tests__/folders.integration.test.ts, src/hooks/useGlobalSearch.ts, src/hooks/useWorkspaces.ts, src/hooks/useContacts.ts, src/hooks/useFathomRefresh.ts, src/hooks/useWorkspaceAssignment.ts, src/hooks/useCallDetailMutations.ts, src/hooks/__tests__/useGlobalSearch.p95.integration.test.ts</files>
  <read_first>
    - src/services/sync-tab.service.ts
    - src/services/api-tokens.service.ts
    - src/services/recordings.service.ts
    - src/hooks/useGlobalSearch.ts
    - src/hooks/useWorkspaces.ts
    - src/hooks/useFathomRefresh.ts
    - src/types/supabase.ts
  </read_first>
  <action>Use the Plan 02 type-generation summary plus exact `rg legacy_recording_id src/services src/hooks` and `rg legacyId src/services src/hooks` to update app service and hook references before the whole-repo type-check gate. Do not require `npm run type-check` to pass in this task because component adapters in Task 3 may still reference stale manual types after services/hooks are changed. Replace Supabase select strings, filter keys, row interfaces, row mapping, and test fixture properties from `legacy_recording_id` to `fathom_provider_id`. In `src/services/api-tokens.service.ts`, remove the `(rec as any).legacy_recording_id` escape hatch by reading `fathom_provider_id` from a typed row or a narrow local type; do not introduce new `any`. In `src/services/recordings.service.ts`, keep function names such as `getRecordingByLegacyId` only if they describe numeric-format input compatibility, but update JSDoc to say it fetches by numeric Fathom provider ID stored in `recordings.fathom_provider_id`. In `src/hooks/useFathomRefresh.ts` and Fathom refresh-related types, rename response field references to `fathom_provider_id` while preserving `source_call_id`. In `src/hooks/useGlobalSearch.ts`, update RPC metadata reads and fallback query select strings to `fathom_provider_id`; preserve search access semantics and do not change `global_search` RPC parameter names unless the regenerated types require it. Do not rename `source_call_id` anywhere.</action>
  <verify>
    <automated>npm test -- src/services/__tests__/sync-tab.service.test.ts src/hooks/__tests__/useGlobalSearch.p95.integration.test.ts && ! rg -n "legacy_recording_id" src/services src/hooks --glob '!**/*.md' && ! rg -n "\\.legacyId\\b|legacyId:" src/services src/hooks --glob '!**/*.md'</automated>
  </verify>
  <acceptance_criteria>
    - `src/services` and `src/hooks` contain no active `legacy_recording_id` references.
    - `src/services` and `src/hooks` still preserve `source_call_id` references where they existed before.
    - No new `as any` is introduced for the renamed provider ID.
    - Whole-repo `npm run type-check` is intentionally deferred until Task 3 after component adapters are updated; any obvious service/hook-only failures found by focused tests or exact scans are fixed in this task.
  </acceptance_criteria>
  <done>App service and hook layers compile against `fathom_provider_id`.</done>
</task>

<task type="auto">
  <name>Task 3: Update component adapters and focused app tests</name>
  <files>src/components/transcripts/TranscriptsTab.tsx, src/components/transcript-library/TranscriptTable.tsx, src/components/transcript-library/BulkActionToolbarEnhanced.tsx, src/components/transcripts/__tests__/folder-filtering.test.ts, src/test/migrations/phase40-fathom-refresh.integration.test.ts</files>
  <read_first>
    - src/components/transcripts/TranscriptsTab.tsx
    - src/components/transcript-library/TranscriptTable.tsx
    - src/components/transcript-library/BulkActionToolbarEnhanced.tsx
    - src/lib/recording-ids.ts
    - src/hooks/useWorkspaces.ts
  </read_first>
  <action>Update component-level adapters and tests from `legacy_recording_id` to `fathom_provider_id`, then run the whole-repo type-check gate after these adapters are updated. In `TranscriptsTab.tsx`, update `get_workspace_recordings` row mappings from `row.legacy_recording_id` to `row.fathom_provider_id`, direct select strings against `recordings`, local helper result type from `{ legacyId }` to `{ fathomProviderId }`, local mappings from `r.legacy_recording_id` to `r.fathom_provider_id`, and delete/folder bridge extraction from `resolved.map((r) => r.legacyId)` to `resolved.map((r) => r.fathomProviderId)`. In `TranscriptTable.tsx` and `BulkActionToolbarEnhanced.tsx`, update call shape checks and folder assignment fallback keys to `call.fathom_provider_id`. In focused app tests, update inserted rows, deletes, select strings, and expectations to use `fathom_provider_id`; keep test names that say "legacy ID" only if they are explicitly about numeric ID-format compatibility, otherwise rename descriptions to "Fathom provider ID". Do not touch unrelated Phase 7 folder correctness logic beyond field-name changes. For integration tests, first check for the dedicated test DB environment (`SUPABASE_TEST_URL`, `SUPABASE_TEST_ANON_KEY`, and `SUPABASE_TEST_SERVICE_ROLE_KEY`, or the repo's documented equivalent dedicated-test variables). If dedicated test DB variables are absent, record `SKIPPED` with the missing variable names and do not run against production. If they are present, run the integration tests as blocking; failures must fail the task.</action>
  <verify>
    <automated>npm run type-check && bash -lc 'missing=(); for v in SUPABASE_TEST_URL SUPABASE_TEST_ANON_KEY SUPABASE_TEST_SERVICE_ROLE_KEY; do [[ -n "${!v:-}" ]] || missing+=("$v"); done; if (( ${#missing[@]} )); then echo "SKIPPED integration tests: missing dedicated test DB vars ${missing[*]}"; else npm run test:integration -- src/services/__tests__/folders.integration.test.ts src/test/migrations/phase40-fathom-refresh.integration.test.ts; fi' && ! rg -n "legacy_recording_id" src/components src/test/migrations --glob '!**/*.md' && ! rg -n "\\.legacyId\\b|legacyId:" src/components src/test/migrations --glob '!**/*.md'</automated>
  </verify>
  <acceptance_criteria>
    - Touched component files contain `fathom_provider_id` where they read or map the DB field.
    - Touched component files do not contain active `legacy_recording_id` references.
    - `TranscriptsTab.tsx` callers of `ResolvedRecording` use `.fathomProviderId`.
    - `npm run type-check` exits zero after service, hook, and component adapters are updated.
    - Focused integration tests are updated to the new column name; if dedicated test DB env is unavailable, the task records `SKIPPED` with missing vars and no production DB fallback is used. If the env is available, integration test failures are blocking.
  </acceptance_criteria>
  <done>Component adapters and focused app tests use the renamed provider-ID field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser app -> Supabase client | Services/hooks/components build authenticated Supabase queries using user JWT and generated types. |
| mixed recording IDs -> ID boundary | Numeric Fathom provider IDs and UUID recording IDs cross through `recording-ids.ts`. |
| integration tests -> Supabase project | Integration tests must use a dedicated test project, not production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3.2-08 | Tampering | `recording-ids.ts` | mitigate | Preserve `toRecordingUuid()`/`toRecordingUuidBatch()` as the only mixed-ID boundary; do not add parseInt/Number call-site conversions. |
| T-06.3.2-09 | Information Disclosure | search/share/folder queries | mitigate | Rename column selectors only; preserve existing auth-scoped Supabase client and query filters. |
| T-06.3.2-10 | Tampering | integration tests | mitigate | Do not let integration tests fall back to production; use existing real-DB test guards and record skip/error output if env is missing. |
| T-06.3.2-SC | Tampering | package installs | accept | No package installation occurs in this plan. |
</threat_model>

<verification>
Run:
- `npm test -- src/lib/__tests__/recording-ids.test.ts`
- `npm test -- src/services/__tests__/sync-tab.service.test.ts src/hooks/__tests__/useGlobalSearch.p95.integration.test.ts`
- `npm run type-check` after Task 3 component adapters are updated
- `bash -lc 'missing=(); for v in SUPABASE_TEST_URL SUPABASE_TEST_ANON_KEY SUPABASE_TEST_SERVICE_ROLE_KEY; do [[ -n "${!v:-}" ]] || missing+=("$v"); done; if (( ${#missing[@]} )); then echo "SKIPPED integration tests: missing dedicated test DB vars ${missing[*]}"; else npm run test:integration -- src/services/__tests__/folders.integration.test.ts src/test/migrations/phase40-fathom-refresh.integration.test.ts; fi'`
- `! rg -n "legacy_recording_id" src/lib src/services src/hooks src/components src/test/migrations --glob '!**/*.md'`
</verification>

<success_criteria>
- Active app TypeScript references the renamed DB column as `fathom_provider_id`.
- `ResolvedRecording` uses `fathomProviderId`; `isLegacyId()` remains unchanged.
- `source_call_id` behavior remains unchanged.
- Focused app tests are updated and runnable without mocked Supabase integration tests.
</success_criteria>

<output>
Create `.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-03-SUMMARY.md` when done.
</output>
