---
phase: "06.3.2-fathom-provider-id-rename"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260610*_rename_legacy_recording_id_to_fathom_provider_id.sql
autonomous: false
requirements:
  - "none/TBD"
requirements_addressed:
  - "D-01"
  - "D-02"
  - "D-08"
must_haves:
  truths:
    - "D-01: `public.recordings.legacy_recording_id` is renamed to `public.recordings.fathom_provider_id` with one atomic `ALTER TABLE ... RENAME COLUMN` and no compatibility column, trigger, backfill, or dual-write overlap."
    - "D-02: Every live public view, RPC, function, and procedure body discovered by fresh `pg_get_functiondef` or `pg_get_viewdef` scans is recreated with `fathom_provider_id` before generated types are refreshed."
    - "D-08: Historical migration SQL remains untouched; only the new migration file is modified."
    - "The pre-code schema push runs only against a dedicated test Supabase project or local/test DB after the migration file is complete and before Plan 02 type regeneration; production is not migrated until final rollout after code is build-ready."
  artifacts:
    - path: "supabase/migrations/20260610*_rename_legacy_recording_id_to_fathom_provider_id.sql"
      provides: "Atomic DB rename plus recreated live DB dependencies"
  key_links:
    - from: "public.recordings.fathom_provider_id"
      to: "fathom_raw_calls.recording_id"
      via: "preserved BIGINT Fathom provider bridge"
    - from: "live DB definitions"
      to: "new migration"
      via: "fresh pg_get_functiondef and pg_get_viewdef output, not stale migration assumptions"
---

<objective>
Create and test-apply the database migration for the `legacy_recording_id` to `fathom_provider_id` rename.

Purpose: This phase is DB-first. Source changes must not start from stale local types or historical migration assumptions; the live schema has to expose `fathom_provider_id` before generated types and compiler-driven code edits begin.

Output: One new Supabase migration, pushed to a dedicated test Supabase project or local/test database for type generation, with post-push dependency assertions proving active DB objects no longer reference `legacy_recording_id`. Production rollout is deferred to Plan 05 after code, tests, and build are ready.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@supabase/CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-CONTEXT.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-RESEARCH.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-PATTERNS.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-VALIDATION.md
@supabase/migrations/20260308100001_delete_recording_rpc.sql
@supabase/migrations/20260309200013_global_search_rpc.sql
@supabase/migrations/20260525160000_sort_recordings_by_recording_start_time.sql
@supabase/migrations/20260309200001_fix_fathom_calls_view.sql
@supabase/migrations/20260209082100_fix_hybrid_search_v3.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture fresh live DB dependency inventory</name>
  <files>supabase/migrations/20260610*_rename_legacy_recording_id_to_fathom_provider_id.sql</files>
  <read_first>
    - AGENTS.md
    - supabase/CLAUDE.md
    - .planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-RESEARCH.md
    - .planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-PATTERNS.md
  </read_first>
  <action>Before writing SQL, run fresh linked DB introspection using `supabase db query --linked`. Capture columns on `public.recordings` for `legacy_recording_id` and `fathom_provider_id`; live views whose `pg_get_viewdef` contains either old or new name; live public functions/procedures whose `pg_get_functiondef` contains `legacy_recording_id`, `fathom_provider_id`, `hybrid_search`, `global_search`, `delete_recording`, `get_workspace_recordings`, `apply_tag_rules`, `backfill_transcript_segments`, `get_unindexed_recording_ids`, `get_calls_shared_with_me_v2`, `migrate_batch_fathom_calls`, or `migrate_fathom_call_to_recording`; indexes on `public.recordings` whose names or definitions contain `legacy_recording_id` or `fathom_provider_id`; and constraints on `public.recordings` whose `conname` or `pg_get_constraintdef(oid)` contains `legacy_recording_id` or `fathom_provider_id`. The constraint scan is mandatory because stale names such as `recordings_organization_id_legacy_recording_id_key` can survive even when `pg_indexes` looks clean. Treat the research list as a starting point only: `apply_tag_rules(bigint, uuid, boolean)`, `apply_tag_rules_to_untagged(uuid, boolean, integer)`, `backfill_transcript_segments(integer)`, `delete_recording(uuid)`, `ensure_skip_tag()`, `get_calls_shared_with_me_v2(boolean)`, `get_migration_progress()`, `get_unindexed_recording_ids(uuid)`, `get_workspace_recordings(...)`, `global_search(...)`, `migrate_batch_fathom_calls(integer)`, and `migrate_fathom_call_to_recording(bigint, uuid)` must be checked against current live output. If `fathom_calls` or any `hybrid_search` function is absent or does not reference the old column, record that in the summary and do not recreate it unnecessarily. Do not edit historical migration files during this task.</action>
  <verify>
    <automated>supabase db query --linked "select 'columns' as kind, column_name as name, data_type as detail from information_schema.columns where table_schema='public' and table_name='recordings' and column_name in ('legacy_recording_id','fathom_provider_id') union all select 'views', table_name, left(view_definition, 240) from information_schema.views where table_schema='public' and (view_definition ilike '%legacy_recording_id%' or view_definition ilike '%fathom_provider_id%') union all select 'functions', p.proname || '(' || pg_get_function_identity_arguments(p.oid) || ')', left(pg_get_functiondef(p.oid), 240) from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public' and (pg_get_functiondef(p.oid) ilike '%legacy_recording_id%' or pg_get_functiondef(p.oid) ilike '%fathom_provider_id%' or p.proname in ('global_search','delete_recording','get_workspace_recordings','apply_tag_rules','apply_tag_rules_to_untagged','backfill_transcript_segments','get_unindexed_recording_ids','get_calls_shared_with_me_v2','migrate_batch_fathom_calls','migrate_fathom_call_to_recording')) union all select 'indexes', indexname, indexdef from pg_indexes where schemaname='public' and tablename='recordings' and (indexname ilike '%legacy_recording_id%' or indexdef ilike '%legacy_recording_id%' or indexname ilike '%fathom_provider_id%' or indexdef ilike '%fathom_provider_id%') union all select 'constraints', c.conname, pg_get_constraintdef(c.oid) from pg_constraint c where c.conrelid='public.recordings'::regclass and (c.conname ilike '%legacy_recording_id%' or pg_get_constraintdef(c.oid) ilike '%legacy_recording_id%' or c.conname ilike '%fathom_provider_id%' or pg_get_constraintdef(c.oid) ilike '%fathom_provider_id%') order by kind, name;"</automated>
  </verify>
  <acceptance_criteria>
    - The execution summary lists the exact live objects to recreate.
    - The inventory confirms `recordings.legacy_recording_id` exists before the migration and whether `recordings.fathom_provider_id` is absent.
    - The inventory includes `pg_constraint` results for `public.recordings`, including `conname` and `pg_get_constraintdef`, and explicitly reports any stale `legacy_recording_id` constraint names or definitions.
    - The inventory explicitly mentions live `fathom_calls` and `hybrid_search` status instead of relying on historical migration assumptions.
    - No historical migration file is modified.
  </acceptance_criteria>
  <done>Fresh live DB dependency inventory is available for writing the migration.</done>
</task>

<task type="auto">
  <name>Task 2: Write the atomic rename migration and dependency recreations</name>
  <files>supabase/migrations/20260610*_rename_legacy_recording_id_to_fathom_provider_id.sql</files>
  <read_first>
    - supabase/migrations/20260609120000_api_token_generator.sql
    - supabase/migrations/20260309120001_rename_legacy_constraints_and_indexes.sql
    - supabase/migrations/20260308100001_delete_recording_rpc.sql
    - supabase/migrations/20260309200013_global_search_rpc.sql
    - supabase/migrations/20260525160000_sort_recordings_by_recording_start_time.sql
    - the live `pg_get_functiondef` and `pg_get_viewdef` output captured in Task 1
  </read_first>
  <action>Create one new migration named with the current timestamp and a clear suffix such as `rename_legacy_recording_id_to_fathom_provider_id.sql`. In that migration, use a single transaction. First run `ALTER TABLE public.recordings RENAME COLUMN legacy_recording_id TO fathom_provider_id;` per D-01. Do not add a second column, backfill, trigger, view alias, compatibility column, or dual-read fallback. Recreate every live object from Task 1 that references the old name by taking the current live body and changing only DB identifiers or returned JSON metadata keys from `legacy_recording_id` to `fathom_provider_id`. For every function whose `RETURNS TABLE` shape or returned column names change, add an explicit `DROP FUNCTION IF EXISTS schema.function_name(<full identity argument signature>);` before recreating it; `CREATE OR REPLACE FUNCTION` alone is not valid for return-type changes and must not be relied on. After each drop/recreate, reapply the live function's grants, comments, `SECURITY DEFINER`, volatility, `SET search_path`, language, and owner-sensitive auth/member checks in the same transaction. For functions whose return shape does not change, still preserve each object's `LANGUAGE`, volatility, `SECURITY DEFINER`, `SET search_path`, argument names, grants, and comments. For `global_search`, preserve `filter_user_id` requirements, workspace/member access checks, escaped ILIKE logic, accessible-recording filters, and rename metadata key `legacy_recording_id` to `fathom_provider_id`. For `delete_recording`, preserve `auth.uid()` owner authorization and delete order while replacing bridge references from `r.legacy_recording_id` to `r.fathom_provider_id`. For `get_workspace_recordings`, drop the existing function with its full live identity signature before recreating it because the returned column changes from `legacy_recording_id` to `fathom_provider_id`; return `fathom_provider_id` and preserve the join from `fathom_raw_calls.recording_id` to the renamed `recordings.fathom_provider_id`. Include guarded metadata renames for stale names discovered in Task 1: rename `recordings_organization_id_legacy_recording_id_key` to `recordings_organization_id_fathom_provider_id_key` if it exists, rename `idx_recordings_legacy_recording_id` to `idx_recordings_fathom_provider_id` if it exists, and apply the same guarded rename to any other `public.recordings` index or constraint name discovered with the old term. Do not rename `source_call_id` per D-09 and do not rename `isLegacyId()` per D-10.</action>
  <verify>
    <automated>test "$(rg -l "rename column legacy_recording_id to fathom_provider_id|RENAME COLUMN legacy_recording_id TO fathom_provider_id" supabase/migrations | wc -l | tr -d ' ')" = "1" && rg -n "CREATE OR REPLACE|SECURITY DEFINER|search_path|legacy_recording_id|fathom_provider_id|source_call_id|isLegacyId" supabase/migrations/20260610*_rename_legacy_recording_id_to_fathom_provider_id.sql</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one new migration contains the column rename from `legacy_recording_id` to `fathom_provider_id`.
    - The migration recreates every Task 1 live object that references `legacy_recording_id`.
    - Any function with changed returned column names has a preceding `DROP FUNCTION IF EXISTS` using the full live identity argument signature, then a recreated definition and grants/security/search_path reapplied in the same transaction.
    - Recreated `SECURITY DEFINER` objects keep their `SECURITY DEFINER`, `SET search_path`, auth/member checks, and grants.
    - The migration does not contain `ADD COLUMN fathom_provider_id`, trigger-based compatibility, or any change to `source_call_id`.
    - Historical migration files remain unchanged.
  </acceptance_criteria>
  <done>The migration file is complete and ready to push.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-schema-push">
  <name>Task 3: [BLOCKING] Push migration to dedicated test/local DB and verify post-push dependencies</name>
  <files>supabase/migrations/20260610*_rename_legacy_recording_id_to_fathom_provider_id.sql</files>
  <read_first>
    - supabase/CLAUDE.md
    - .planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-VALIDATION.md
    - .env
  </read_first>
  <action>[BLOCKING] Run the project-approved Supabase schema push after Task 2 and before Plan 02, but only against a dedicated test Supabase project or local/test DB for type generation. Do not push this migration to the production linked project in Plan 01; doing so would create a live window where deployed app/Edge code still queries `legacy_recording_id`. Preferred remote-test command is `SUPABASE_ACCESS_TOKEN="$SUPABASE_ACCESS_TOKEN" supabase db push --db-url "$SUPABASE_TEST_DB_URL"` with `SUPABASE_TEST_DB_URL` pointing at a dedicated non-production database. Acceptable local-test alternative is the repo-approved local Supabase DB workflow, followed by type generation from that same local/test schema in Plan 02. If only the production linked project is configured, stop at this checkpoint and ask for a dedicated test DB/local workflow instead of using `supabase db push --linked`. After the test/local push succeeds, immediately run post-push assertions for old/new column names, views, functions/procedures, `pg_constraint`, and indexes. The constraint assertion must include `conname` and `pg_get_constraintdef` so stale constraints such as `recordings_organization_id_legacy_recording_id_key` cannot be missed. Record the exact type-generation target for Plan 02 in `06.3.2-01-SUMMARY.md`: for remote test DB, Plan 02 must regenerate from the same dedicated test target, not from the production project ID; for local DB, Plan 02 must use the matching local-generation command. This task is intentionally non-autonomous because it mutates a schema and may require explicit confirmation. Do not run `npm run type-check` or type generation before this test/local push; pre-push build/type checks are not sufficient for this phase. Production migration is covered by Plan 05 final rollout after code is committed/build-ready, using a maintenance-window sequence that pushes the DB migration and immediately deploys matching app and Edge code.</action>
  <verify>
    <automated>bash -lc 'if [[ -z "${SUPABASE_TEST_DB_URL:-}" ]]; then echo "BLOCKED: SUPABASE_TEST_DB_URL is required for Plan 01 test schema push; do not use production --linked here"; exit 2; fi; SUPABASE_ACCESS_TOKEN="$SUPABASE_ACCESS_TOKEN" supabase db push --db-url "$SUPABASE_TEST_DB_URL" && supabase db query --db-url "$SUPABASE_TEST_DB_URL" "$(cat <<'"'"'SQL'"'"'
do $$
declare
  old_columns integer;
  new_columns integer;
  stale_views integer;
  stale_functions integer;
  stale_indexes integer;
  stale_constraints integer;
begin
  select count(*) into old_columns
  from information_schema.columns
  where table_schema = '"'"'public'"'"' and table_name = '"'"'recordings'"'"' and column_name = '"'"'legacy_recording_id'"'"';

  select count(*) into new_columns
  from information_schema.columns
  where table_schema = '"'"'public'"'"' and table_name = '"'"'recordings'"'"' and column_name = '"'"'fathom_provider_id'"'"';

  select count(*) into stale_views
  from information_schema.views
  where table_schema = '"'"'public'"'"' and view_definition ilike '"'"'%legacy_recording_id%'"'"';

  select count(*) into stale_functions
  from pg_proc p
  join pg_namespace n on n.oid = p.pronamespace
  where n.nspname = '"'"'public'"'"' and pg_get_functiondef(p.oid) ilike '"'"'%legacy_recording_id%'"'"';

  select count(*) into stale_indexes
  from pg_indexes
  where schemaname = '"'"'public'"'"' and tablename = '"'"'recordings'"'"'
    and (indexname ilike '"'"'%legacy_recording_id%'"'"' or indexdef ilike '"'"'%legacy_recording_id%'"'"');

  select count(*) into stale_constraints
  from pg_constraint c
  where c.conrelid = '"'"'public.recordings'"'"'::regclass
    and (c.conname ilike '"'"'%legacy_recording_id%'"'"' or pg_get_constraintdef(c.oid) ilike '"'"'%legacy_recording_id%'"'"');

  if old_columns <> 0 or new_columns <> 1 or stale_views <> 0 or stale_functions <> 0 or stale_indexes <> 0 or stale_constraints <> 0 then
    raise exception '"'"'stale legacy_recording_id state: old_columns=%, new_columns=%, stale_views=%, stale_functions=%, stale_indexes=%, stale_constraints=%'"'"',
      old_columns, new_columns, stale_views, stale_functions, stale_indexes, stale_constraints;
  end if;
end $$;
SQL
)"'</automated>
  </verify>
  <acceptance_criteria>
    - The Plan 01 schema push targets only a dedicated test Supabase project or local/test DB; production `supabase db push --linked` is not used here.
    - The test/local schema push exits zero or the executor stops for explicit human confirmation/test DB configuration.
    - `06.3.2-01-SUMMARY.md` records the exact same test/local schema target that Plan 02 must use for generated type regeneration, so Plan 02 does not regenerate from production before production rollout.
    - Post-push SQL assertions mechanically fail unless `recordings.fathom_provider_id` exists exactly once and `recordings.legacy_recording_id` is absent.
    - Post-push SQL assertions mechanically fail if any active public view or public function definition contains `legacy_recording_id`.
    - Post-push SQL assertions mechanically fail if any active `public.recordings` index name/definition or constraint name/definition contains `legacy_recording_id`, unless an unavoidable leftover is documented with exact reason and the assertion is deliberately adjusted with that exact allowlist.
  </acceptance_criteria>
  <done>The dedicated test/local DB schema is migrated and safe for generated type regeneration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor -> test/local Supabase DB | Plan 01 migration mutates schema and recreates SECURITY DEFINER functions only in a dedicated test/local project. |
| SQL function -> authenticated user data | Recreated RPCs enforce member/owner checks before returning or mutating recordings. |
| public RPC grants -> callers | Dropping/recreating functions can accidentally widen or remove execution grants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3.2-01 | Elevation of Privilege | `global_search`, `delete_recording`, tag/share/search RPCs | mitigate | Recreate from fresh live definitions and preserve `SECURITY DEFINER`, `SET search_path`, `auth.uid()` and workspace membership checks, and grants. Block on any high severity drift. |
| T-06.3.2-02 | Tampering | test/local schema push | mitigate | Use a blocking task after migration completion, require a dedicated test/local target, prohibit production `--linked` in Plan 01, and run failing post-push old-name dependency assertions before type generation. |
| T-06.3.2-03 | Information Disclosure | `global_search` metadata and sharing RPCs | mitigate | Rename metadata key only; preserve access filters and escaped query logic. Do not widen wildcard MCP CORS or return session-cookie data. |
| T-06.3.2-04 | Denial of Service | live function recreation | mitigate | Keep signatures/grants compatible; run exact live dependency scans immediately after push. |
| T-06.3.2-SC | Tampering | package installs | accept | No package installation occurs in this plan. |
</threat_model>

<verification>
Plan-level gates:
- `supabase db query --linked` pre-migration dependency inventory completes.
- New migration contains exactly one atomic `ALTER TABLE public.recordings RENAME COLUMN legacy_recording_id TO fathom_provider_id`.
- `[BLOCKING]` test/local schema push runs before generated type regeneration; production rollout is deferred to Plan 05.
- Post-push DB assertions mechanically fail if any active live DB dependency on `legacy_recording_id` remains.
- Pre- and post-push DB scans include `pg_constraint` `conname` and `pg_get_constraintdef` checks for stale `legacy_recording_id` and expected `fathom_provider_id` names.
</verification>

<success_criteria>
- The dedicated test/local database exposes `recordings.fathom_provider_id` and not `recordings.legacy_recording_id`.
- All live views/RPCs/functions/procedures that depended on the old column are recreated with the new column name.
- SECURITY DEFINER/search_path/grant/member-check semantics are preserved.
- Historical migrations are not modified.
</success_criteria>

<output>
Create `.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-01-SUMMARY.md` when done.
</output>
