# Phase 06.3.2-03 Summary

## Status

Complete.

Plan 03 verified the app-side TypeScript references already use `fathom_provider_id` and `ResolvedRecording.fathomProviderId`; no source edits were needed in the app layer during this plan.

## Scope Verified

- `src/lib/recording-ids.ts` exports `ResolvedRecording.fathomProviderId`.
- `isLegacyId()` remains exported and keeps numeric-vs-UUID format detection semantics.
- `toRecordingUuid()` and `toRecordingUuidBatch()` query `recordings.fathom_provider_id`.
- `legacyIds` remains the compatibility return array for numeric Fathom provider IDs.
- Services, hooks, components, and focused app tests contain no active `legacy_recording_id` references.
- No `.legacyId` property reads or `legacyId:` object properties remain in `src/services`, `src/hooks`, `src/components`, or `src/test/migrations`.
- `source_call_id` references remain present where expected.

## Verification

```text
npm test -- src/lib/__tests__/recording-ids.test.ts
✓ src/lib/__tests__/recording-ids.test.ts (18 tests)
```

```text
npm test -- src/services/__tests__/sync-tab.service.test.ts src/hooks/__tests__/useGlobalSearch.p95.integration.test.ts
✓ src/services/__tests__/sync-tab.service.test.ts (5 tests)
```

`src/hooks/__tests__/useGlobalSearch.p95.integration.test.ts` is guarded by `describe.skipIf(!integrationDbReachable)` and was not collected under the focused unit run without dedicated integration DB configuration.

```text
npm run type-check
> callvault@0.0.0 type-check
> tsc --noEmit
```

```text
SKIPPED integration tests: missing dedicated test DB vars SUPABASE_TEST_URL SUPABASE_TEST_ANON_KEY SUPABASE_TEST_SERVICE_ROLE_KEY
```

Exact stale-reference gates:

```text
! rg -n "legacy_recording_id" src/lib src/services src/hooks src/components src/test/migrations --glob '!**/*.md'
! rg -n "\.legacyId\b|legacyId:" src/services src/hooks src/components src/test/migrations --glob '!**/*.md'
```

Both gates exited zero.

## Notes

- Remaining `legacy` tokens in app code are intentionally format-level compatibility names such as `isLegacyId`, `legacyIds`, or test variable names. They do not refer to the renamed DB column.
- No production DB or production integration-test fallback was used.
