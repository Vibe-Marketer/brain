---
phase: "06.3.2-fathom-provider-id-rename"
plan: "02"
type: execute
wave: 2
depends_on:
  - "06.3.2-01"
files_modified:
  - package.json
  - src/integrations/supabase/client.ts
  - src/types/supabase.ts
  - src/types/workspace.ts
  - src/types/meetings.ts
  - src/types/contacts.ts
autonomous: true
requirements:
  - "none/TBD"
requirements_addressed:
  - "D-03"
  - "D-04"
must_haves:
  truths:
    - "D-03: Generated Supabase types are regenerated from the exact migrated test/local schema target recorded by `06.3.2-01-SUMMARY.md`, not from the production project ID before production rollout."
    - "D-04: Manual type files expose `fathom_provider_id` instead of `legacy_recording_id`."
    - "The `package.json` type-generation script and Supabase client import target the active generated type file used by services."
  artifacts:
    - path: "package.json"
      provides: "Type-generation script destination cleanup without forcing production project regeneration during the test/local phase"
    - path: "src/integrations/supabase/client.ts"
      provides: "Supabase client imports `Database` from the authoritative `@/types/supabase` generated file"
    - path: "src/types/supabase.ts"
      provides: "Generated schema types with `fathom_provider_id`"
    - path: "src/types/workspace.ts"
      provides: "Manual workspace recording type with `fathom_provider_id`"
    - path: "src/types/meetings.ts"
      provides: "Manual meeting type with `fathom_provider_id` and updated Fathom provider-ID comment"
    - path: "src/types/contacts.ts"
      provides: "Manual contact call history type with `fathom_provider_id`"
  key_links:
    - from: "type-generation target recorded in `06.3.2-01-SUMMARY.md`"
      to: "src/types/supabase.ts"
      via: "phase-local direct regeneration command; `package.json` destination cleanup must not override the test/local target"
---

<objective>
Resolve generated-type destination drift and refresh schema/manual TypeScript types against the migrated DB.

Purpose: Source edits must be driven by the migrated schema, not by manual generated-type editing or a stale `package.json` script that writes to the wrong location.

Output: `src/types/supabase.ts` regenerated with `fathom_provider_id`, manual row types updated, and initial type-check output available as the checklist for Plans 03 and 04.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@src/CLAUDE.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-CONTEXT.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-RESEARCH.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-PATTERNS.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-01-SUMMARY.md
@package.json
@src/integrations/supabase/client.ts
@src/types/supabase.ts
@src/types/workspace.ts
@src/types/meetings.ts
@src/types/contacts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve generated type destination without forcing production regeneration</name>
  <files>package.json, src/integrations/supabase/client.ts</files>
  <read_first>
    - package.json
    - src/integrations/supabase/client.ts
    - src/types/supabase.ts
    - .planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-RESEARCH.md
  </read_first>
  <action>Update `src/integrations/supabase/client.ts` so its `Database` type import comes from `@/types/supabase`, matching the service-layer imports and the authoritative generated destination. Resolve `package.json` `scripts.gen:types` destination drift without hard-coding or invoking the production project ID during this phase: either change the script to an environment-driven/test-safe command that writes `src/types/supabase.ts`, or leave production-project script cleanup to Plan 05 and document that Plan 02 uses the direct test/local command recorded by Plan 01. Do not add another generated type destination and do not create `src/integrations/supabase/types.ts`; this plan deliberately resolves the client import to `@/types/supabase` and keeps phase-local regeneration tied to the Plan 01 test/local target.</action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const s=p.scripts['gen:types']||''; if(s.includes('src/integrations/supabase/types.ts')) process.exit(2); if(s.includes('vltmrnjsubfzrgrtdqey') && !s.includes('src/types/supabase.ts')) process.exit(3); console.log(s || 'gen:types unchanged; phase-local typegen uses 06.3.2-01-SUMMARY.md target')" && rg -n "import type \\{ Database \\} from ['\\\"]@/types/supabase['\\\"]" src/integrations/supabase/client.ts && ! rg -n "from ['\\\"]\\./types['\\\"]" src/integrations/supabase/client.ts</automated>
  </verify>
  <acceptance_criteria>
    - Phase-local regeneration is not tied to production project ID `vltmrnjsubfzrgrtdqey`.
    - `package.json` `gen:types` no longer writes to `src/integrations/supabase/types.ts`, or production script cleanup is explicitly deferred to Plan 05 while Plan 02 uses the Plan 01 test/local direct command.
    - `src/integrations/supabase/client.ts` imports `Database` from `@/types/supabase`, not `./types`.
    - No duplicate generated type file or re-export shim is introduced.
  </acceptance_criteria>
  <done>The Supabase client import targets the active generated type file, and phase-local type generation is bound to the Plan 01 test/local schema target.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate `src/types/supabase.ts` from the migrated schema</name>
  <files>src/types/supabase.ts</files>
  <read_first>
    - .planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-01-SUMMARY.md
    - src/types/supabase.ts
    - package.json
  </read_first>
  <action>After Plan 01 confirms the dedicated test/local DB has been pushed, read `06.3.2-01-SUMMARY.md` and run the exact type-generation command recorded there for that same migrated test/local target. For a remote test DB this may be a direct `supabase gen types typescript --db-url "$SUPABASE_TEST_DB_URL" > src/types/supabase.ts`; for a local DB use the repo-approved local Supabase type-generation command recorded by Plan 01. Do not run `npm run gen:types` if it points at production project ID `vltmrnjsubfzrgrtdqey`; do not run `supabase gen types ... --project-id vltmrnjsubfzrgrtdqey` in Plan 02. Do not hand-edit generated rows to fake the rename. Confirm `Database['public']['Tables']['recordings']['Row'|'Insert'|'Update']` exposes `fathom_provider_id` and no longer exposes `legacy_recording_id`. Confirm RPC return types such as `get_workspace_recordings` expose `fathom_provider_id` when the migrated test/local SQL returns that column. If generation still emits `legacy_recording_id`, stop and return to Plan 01 because the test/local schema push or dependent SQL recreation did not land.</action>
  <verify>
    <automated>bash -lc 'if [[ ! -f .planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-01-SUMMARY.md ]]; then echo "Missing 06.3.2-01-SUMMARY.md with test/local typegen target"; exit 2; fi; if rg -n "vltmrnjsubfzrgrtdqey|--project-id" .planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-01-SUMMARY.md | rg -v "production rollout|do not use"; then echo "Plan 01 summary must record a test/local typegen target, not production project-id"; exit 3; fi; echo "Run the TYPEGEN_COMMAND recorded in 06.3.2-01-SUMMARY.md, then verify generated output"; rg -n "TYPEGEN_COMMAND|SUPABASE_TEST_DB_URL|local" .planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-01-SUMMARY.md' && rg -n "fathom_provider_id" src/types/supabase.ts && ! rg -n "legacy_recording_id" src/types/supabase.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/supabase.ts` is regenerated in this session from the exact migrated test/local schema target recorded by Plan 01.
    - `src/types/supabase.ts` contains `fathom_provider_id`.
    - `src/types/supabase.ts` contains no `legacy_recording_id`.
    - No Plan 02 command regenerates from production project ID `vltmrnjsubfzrgrtdqey`; production type regeneration/script cleanup happens only after the Plan 05 production rollout sequence.
    - Any type errors generated by this change are treated as the source-change checklist for Plans 03 and 04.
  </acceptance_criteria>
  <done>Generated DB types reflect the migrated schema.</done>
</task>

<task type="auto">
  <name>Task 3: Update manual TypeScript row shapes</name>
  <files>src/types/workspace.ts, src/types/meetings.ts, src/types/contacts.ts</files>
  <read_first>
    - src/types/workspace.ts
    - src/types/meetings.ts
    - src/types/contacts.ts
    - src/CLAUDE.md
  </read_first>
  <action>Rename manual snake_case DB-backed properties from `legacy_recording_id` to `fathom_provider_id` in `src/types/workspace.ts`, `src/types/meetings.ts`, and `src/types/contacts.ts` per D-04. Update comments to say this is the numeric Fathom provider recording ID stored on `recordings.fathom_provider_id`; it is populated for Fathom-originated rows and null for Zoom/manual/MCP/other provider rows. Preserve `recording_id` and `source_call_id` fields exactly; do not rename `source_call_id` per D-09. Keep app row shapes snake_case because they mirror Supabase rows.</action>
  <verify>
    <automated>rg -n "fathom_provider_id|legacy_recording_id|source_call_id" src/types/workspace.ts src/types/meetings.ts src/types/contacts.ts && ! rg -n "legacy_recording_id" src/types/workspace.ts src/types/meetings.ts src/types/contacts.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/workspace.ts` exposes `fathom_provider_id?: number | null`.
    - `src/types/meetings.ts` exposes `fathom_provider_id?: number | null` and its JSDoc references `recordings.fathom_provider_id`.
    - `src/types/contacts.ts` exposes `fathom_provider_id: number | null`.
    - None of the three files contains `legacy_recording_id`.
  </acceptance_criteria>
  <done>Manual types match the generated schema field name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| linked Supabase schema -> generated TypeScript | Generated types expose DB structure used across app and Edge Function code. |
| generated code -> source code | Type errors drive source edits; incorrect generated destination can hide stale code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3.2-05 | Tampering | generated Supabase types | mitigate | Regenerate from the exact migrated test/local schema target recorded by Plan 01; do not hand-edit generated field names and do not regenerate from production before production rollout. |
| T-06.3.2-06 | Information Disclosure | generated type files | accept | Type files expose schema names only, not token values or data contents. |
| T-06.3.2-07 | Denial of Service | stale `gen:types` target | mitigate | Resolve the abandoned destination and keep phase-local regeneration bound to the Plan 01 test/local target; production script cleanup is allowed only when it does not force pre-rollout production regeneration. |
| T-06.3.2-SC | Tampering | package installs | accept | No package installation occurs in this plan. |
</threat_model>

<verification>
Run:
- `node -e "const p=require('./package.json'); const s=p.scripts['gen:types']||''; if(s.includes('src/integrations/supabase/types.ts')) process.exit(2)"`
- `rg -n "import type \\{ Database \\} from ['\\\"]@/types/supabase['\\\"]" src/integrations/supabase/client.ts`
- Run the `TYPEGEN_COMMAND` recorded in `06.3.2-01-SUMMARY.md` for the migrated test/local schema target; do not use production project ID `vltmrnjsubfzrgrtdqey` in Plan 02.
- `rg -n "fathom_provider_id" src/types/supabase.ts src/types/workspace.ts src/types/meetings.ts src/types/contacts.ts`
- `! rg -n "legacy_recording_id" src/types/supabase.ts src/types/workspace.ts src/types/meetings.ts src/types/contacts.ts`
</verification>

<success_criteria>
- Type-generation destination drift is resolved without forcing production project regeneration before production rollout.
- Generated Supabase types come from the migrated test/local schema target and expose only `fathom_provider_id`.
- Manual app types expose `fathom_provider_id` and preserve `source_call_id`.
</success_criteria>

<output>
Create `.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-02-SUMMARY.md` when done.
</output>
