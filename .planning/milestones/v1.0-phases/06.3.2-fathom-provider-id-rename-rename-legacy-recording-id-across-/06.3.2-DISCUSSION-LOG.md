# Phase 06.3.2: fathom_provider_id rename — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-10
**Phase:** 06.3.2-fathom-provider-id-rename
**Areas discussed:** All decisions delegated to Claude judgment

---

## Migration Approach

| Option | Description | Selected |
|--------|-------------|----------|
| ALTER TABLE RENAME COLUMN + view/RPC recreation | Single atomic migration; cleaner, no overlap period needed | ✓ |
| Dual-column overlap (add new, backfill, drop old) | Safer but unnecessary — no external consumers pass column name directly | |

**User's choice:** Delegated to Claude — "use best judgement and most likely and highest standard judgement possible."
**Notes:** Planner must audit global_search, hybrid_search, delete_recording, and fathom_calls view before writing migration — all reference legacy_recording_id by name.

---

## supabase.ts Update Method

| Option | Description | Selected |
|--------|-------------|----------|
| Regenerate via Supabase CLI | Run `supabase gen types typescript` after migration; TypeScript errors drive code changes | ✓ |
| Manual find-and-replace | Update generated types by hand without CLI regeneration | |

**User's choice:** Delegated to Claude.
**Notes:** Regeneration is the authoritative approach for DB-driven renames — compiler errors become the exhaustive checklist.

---

## TypeScript Helper Naming Scope

| Option | Description | Selected |
|--------|-------------|----------|
| isLegacyId() stays, legacyId → fathomProviderId | isLegacyId tests format not column; ResolvedRecording.legacyId tracks column | ✓ |
| Rename all helpers including isLegacyId | Full rename cascade; risks breaking semantic clarity of the format-detection function | |
| Leave all TS helpers as-is | Inconsistent with column rename | |

**User's choice:** Delegated to Claude.
**Notes:** `isLegacyId()` stays — it describes the numeric-vs-UUID format distinction which outlives any column name. `legacyId` on `ResolvedRecording` renames to `fathomProviderId` because it mirrors the DB column.

---

## Historical Migration SQL

| Option | Description | Selected |
|--------|-------------|----------|
| Leave 131 historical references untouched | Immutable executed SQL — not the right surface to edit | ✓ |
| Update historical migrations | Would cause migration checksum failures in Supabase CI | |

**User's choice:** Delegated to Claude.

---

## Claude's Discretion

All decisions in this phase were delegated by the user: "I don't need to answer any of these... just that it should be done so you decide and I trust your judgement." Full decision set documented in CONTEXT.md.

## Deferred Ideas

None.
