---
phase: 02-mcp-monolith-refactor
plan: 08
type: execute
wave: 8
depends_on:
  - "07"
files_modified:
  - supabase/functions/mcp-server/index.ts
  - supabase/functions/mcp-server/tools/registry.ts
  - supabase/functions/mcp-server/__tests__/golden-replay.test.ts
  - supabase/functions/mcp-server/__tests__/contract-surface.test.ts
  - docs/operations/mcp-runbook.md
autonomous: true
requirements:
  - MCP-05
must_haves:
  truths:
    - "D-04/D-05: Final dispatcher has no inline tool switch and preserves MCP contract."
    - "D-08/D-09: Final verification includes dynamic AI dependency proof and measured deployed cold-start comparison."
    - "D-10: Targeted MCP tests, build, and live token smoke are required before the phase can be marked verified."
  artifacts:
    - path: "supabase/functions/mcp-server/index.ts"
      provides: "Lean HTTP/CORS/auth/protocol/gate/dispatch orchestrator under 300 LOC"
    - path: "docs/operations/mcp-runbook.md"
      provides: "Final module map and verification record"
  key_links:
    - "`index.ts` dispatches every tool through `tools/registry.ts`."
    - "Live smoke uses deployed endpoint `https://api.callvaultai.com/mcp`, not just local source tests."
---

<objective>
Remove the last monolith remnants, verify the full 41-tool modular dispatcher, and capture live/cold-start evidence. This closes MCP-05 only if behavior parity and performance proof meet D-04 through D-10.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mcp-monolith-refactor/02-CONTEXT.md
@.planning/phases/02-mcp-monolith-refactor/02-RESEARCH.md
@.planning/phases/02-mcp-monolith-refactor/02-PATTERNS.md
@supabase/functions/mcp-server/index.ts
@supabase/functions/mcp-server/tools/registry.ts
@docs/operations/mcp-runbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim `index.ts` to orchestration only</name>
  <files>supabase/functions/mcp-server/index.ts, supabase/functions/mcp-server/tools/registry.ts, supabase/functions/mcp-server/__tests__/contract-surface.test.ts</files>
  <read_first>
    - supabase/functions/mcp-server/index.ts
    - supabase/functions/mcp-server/tools/registry.ts
    - supabase/functions/mcp-server/protocol.ts
    - supabase/functions/mcp-server/auth.ts
    - supabase/functions/mcp-server/gating.ts
  </read_first>
  <action>Delete the remaining inline switch and any temporary legacy definition scaffolding. `index.ts` should contain only public CORS/non-POST handling, JSON-RPC parse, auth call, protocol methods, `tools/list`, paid-plan gate, category gate, registry lookup, handler invocation, and final unknown-tool error handling. Keep one Edge Function. Do not split deployment units. Enforce `wc -l supabase/functions/mcp-server/index.ts` <= 300.</action>
  <acceptance_criteria>
    - `index.ts` has no `case '<tool>':` blocks.
    - `index.ts` is 300 lines or fewer.
    - All 41 tool definitions come from modules through `registry.ts`.
    - Unknown tool handling still returns the current JSON-RPC error behavior after valid auth/gates.
  </acceptance_criteria>
  <verify>
    <automated>test "$(wc -l < supabase/functions/mcp-server/index.ts)" -le 300 && ! rg -n "case '.*':" supabase/functions/mcp-server/index.ts && npm test -- --run supabase/functions/mcp-server/__tests__/contract-surface.test.ts supabase/functions/mcp-server/__tests__/golden-replay.test.ts</automated>
  </verify>
  <done>`index.ts` is a lean orchestrator and every tool is registry-dispatched.</done>
</task>

<task type="auto">
  <name>Task 2: Run full targeted MCP and build gates against the final tree</name>
  <files>supabase/functions/mcp-server/__tests__/golden-replay.test.ts, supabase/functions/mcp-server/__tests__/contract-surface.test.ts, docs/operations/mcp-runbook.md</files>
  <read_first>
    - supabase/functions/mcp-server/__tests__/golden-replay.test.ts
    - supabase/functions/mcp-server/__tests__/contract-surface.test.ts
    - supabase/functions/mcp-server/__tests__/category-gating.test.ts
    - supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts
    - supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts
    - supabase/functions/mcp-server/__tests__/track-ai-usage-registry.test.ts
  </read_first>
  <action>Run the final local gates: all targeted MCP tests, `npm run build`, and the Deno check decision from Plan 03. If `deno check supabase/functions/mcp-server/index.ts` now passes, record that as the final type gate. If it still fails for pre-existing external type drift, record the exact failure category and confirm the replacement gate is: targeted MCP tests plus `npm run build` plus live deployed smoke. Update the runbook Phase 2 section with final module layout and the exact commands used.</action>
  <acceptance_criteria>
    - Targeted MCP tests pass: `category-gating`, `ai-tools-invariants`, `write-tools-boundary`, `track-ai-usage-registry`, `golden-replay`, `contract-surface`.
    - `npm run build` exits 0 against the final tree.
    - The summary explicitly states the `deno check` result.
    - Runbook final module map references `tools/{read,write,admin,ai}` and `tools/registry.ts`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/category-gating.test.ts supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts supabase/functions/mcp-server/__tests__/track-ai-usage-registry.test.ts supabase/functions/mcp-server/__tests__/golden-replay.test.ts supabase/functions/mcp-server/__tests__/contract-surface.test.ts && npm run build</automated>
  </verify>
  <done>The final committed tree passes all local MCP gates and build verification.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy and capture live parity plus cold-start evidence</name>
  <files>docs/operations/mcp-runbook.md</files>
  <read_first>
    - docs/operations/mcp-runbook.md
    - .env
    - .env.local
  </read_first>
  <action>Deploy `mcp-server` with `supabase functions deploy mcp-server --use-api` only after local gates pass. Use existing local env values to find a valid MCP token if available; do not print or store the secret. Hit `https://api.callvaultai.com/mcp` for invalid bearer 401 with `WWW-Authenticate`, valid-token `initialize`, valid-token `tools/list`, and one representative read tool. Capture cold-start baseline/candidate measurements using the runbook method and record p95/median values plus whether the candidate improved read-path cold start by at least 30%. If no valid token or deployment credential is available, do not mark Phase 2 fully verified; record the missing proof plainly in the summary.</action>
  <acceptance_criteria>
    - Production/deployed candidate invalid bearer returns HTTP 401 with `WWW-Authenticate`.
    - Valid token `initialize` returns CallVault serverInfo.
    - Valid token `tools/list` returns 41 tools.
    - Valid token read tool returns a JSON-RPC result with `content[0].text`.
    - Cold-start result is measured and compared; if <30% or unavailable, Phase 2 verification remains incomplete.
  </acceptance_criteria>
  <verify>
    <automated>supabase functions deploy mcp-server --use-api && curl -i -sS https://api.callvaultai.com/mcp -H "Authorization: Bearer invalid" -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"initialize","id":1}' | rg -n "HTTP/|WWW-Authenticate"</automated>
  </verify>
  <done>Live MCP behavior and cold-start performance are proven or explicitly recorded as not fully verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| registry dispatcher -> all modules | Every external tool call flows through the new handler map |
| local build -> production deployment | Verified local source becomes deployed Edge Function code |
| production endpoint -> live MCP clients | Claude/Cursor/custom clients consume deployed protocol behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-08-01 | Tampering | final dispatcher | mitigate | Remove inline switch and require all modules through registry contract tests. |
| T-02-08-02 | Information Disclosure | `tools/list` live endpoint | mitigate | Live smoke checks 41 tools and category/output contract after deploy. |
| T-02-08-03 | Denial of Service | cold-start path | mitigate | Measure candidate read-path cold starts against baseline and require documented result. |
| T-02-08-04 | Repudiation | deployment verification | mitigate | Record exact commands and results; do not infer deployed behavior from local build. |
| T-02-08-05 | Elevation of Privilege | live auth | mitigate | Invalid bearer 401 plus valid-token smoke must pass on `api.callvaultai.com/mcp`. |
</threat_model>

<verification>
Run:
`npm test -- --run supabase/functions/mcp-server/__tests__/category-gating.test.ts supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts supabase/functions/mcp-server/__tests__/track-ai-usage-registry.test.ts supabase/functions/mcp-server/__tests__/golden-replay.test.ts supabase/functions/mcp-server/__tests__/contract-surface.test.ts && npm run build`

Then deploy and perform the live smoke/cold-start checks from the runbook.
</verification>

<success_criteria>
- `index.ts` is <=300 LOC and contains no inline tool cases.
- All 41 tools live in `tools/{read,write,admin,ai}/` and registry definitions match categories.
- Top-level AI imports are gone from non-AI hot path.
- Local targeted tests and build pass.
- Deployed live smoke and cold-start comparison are captured before Phase 2 is marked complete.
</success_criteria>

<output>
Create `.planning/phases/02-mcp-monolith-refactor/02-08-SUMMARY.md` when done.
</output>
