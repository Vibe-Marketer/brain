---
phase: 02-mcp-monolith-refactor
plan: 07
type: execute
wave: 7
depends_on:
  - "06"
files_modified:
  - supabase/functions/mcp-server/index.ts
  - supabase/functions/mcp-server/tools/ai/extract_action_items.ts
  - supabase/functions/mcp-server/tools/ai/ask_call.ts
  - supabase/functions/mcp-server/tools/ai/get_sentiment.ts
  - supabase/functions/mcp-server/tools/ai/get_coaching_notes.ts
  - supabase/functions/mcp-server/tools/registry.ts
  - supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts
  - supabase/functions/mcp-server/__tests__/track-ai-usage-registry.test.ts
autonomous: true
requirements:
  - MCP-05
must_haves:
  truths:
    - "D-08: AI SDK/OpenRouter/Zod dependencies are dynamically imported only by AI handlers."
    - "D-05/D-06: The 4 AI tools preserve cache/gate/LLM ordering, markdown response shape, and category gating."
  artifacts:
    - path: "supabase/functions/mcp-server/tools/ai/*.ts"
      provides: "All current AI tool modules with dynamic imports"
  key_links:
    - "AI modules keep `enforceMcpAiUsage` before LLM calls and cache checks before usage gates where existing behavior requires it."
    - "`index.ts` no longer top-level imports `@openrouter/ai-sdk-provider`, `ai`, or `zod` after this plan."
---

<objective>
Extract the 4 AI tools and move AI-only dependencies out of the read-path cold start. This plan implements D-08 while preserving all Phase 22 AI invariants.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mcp-monolith-refactor/02-CONTEXT.md
@.planning/phases/02-mcp-monolith-refactor/02-RESEARCH.md
@.planning/phases/02-mcp-monolith-refactor/02-PATTERNS.md
@supabase/functions/mcp-server/index.ts
@supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts
@supabase/functions/mcp-server/__tests__/track-ai-usage-registry.test.ts
@supabase/functions/_shared/track-ai-usage-inline.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract action-items and ask-call AI tools with dynamic imports</name>
  <files>supabase/functions/mcp-server/index.ts, supabase/functions/mcp-server/tools/ai/extract_action_items.ts, supabase/functions/mcp-server/tools/ai/ask_call.ts, supabase/functions/mcp-server/tools/registry.ts, supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts</files>
  <read_first>
    - Current case blocks for `extract_action_items`, `ask_call`
    - supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts
    - supabase/functions/_shared/track-ai-usage-inline.ts
  </read_first>
  <action>Move `extract_action_items` and `ask_call` into AI modules. Inside each handler, dynamically import `createOpenRouter`, `generateObject` or `generateText`, and `z` only where needed. Preserve `extract_action_items` ownership check, source_metadata -> action_items_cache -> usage gate -> `generateObject` order, `mcp_action_items` action type, cache write, model `openai/gpt-5-nano`, and markdown output. Preserve `ask_call` no-cache behavior, 500-character question validation, `mcp_ask_call` action type, usage gate before `generateText`, and `Q:`/`A:` response prefix.</action>
  <acceptance_criteria>
    - `index.ts` no longer top-level imports `@openrouter/ai-sdk-provider`, `ai`, or `zod` for these two tools.
    - `extract_action_items` keeps cache-before-usage-gate ordering.
    - `ask_call` still has no cache and keeps 500-character validation.
    - `ai-tools-invariants.test.ts` anchors module files after extraction.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts supabase/functions/mcp-server/__tests__/track-ai-usage-registry.test.ts supabase/functions/mcp-server/__tests__/contract-surface.test.ts</automated>
  </verify>
  <done>`extract_action_items` and `ask_call` are AI modules with dynamic imports and preserved invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Extract sentiment and coaching AI tools with cache invariants</name>
  <files>supabase/functions/mcp-server/index.ts, supabase/functions/mcp-server/tools/ai/get_sentiment.ts, supabase/functions/mcp-server/tools/ai/get_coaching_notes.ts, supabase/functions/mcp-server/tools/registry.ts, supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts</files>
  <read_first>
    - Current case blocks for `get_sentiment`, `get_coaching_notes`
    - supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts
    - supabase/functions/_shared/track-ai-usage-inline.ts
  </read_first>
  <action>Move `get_sentiment` and `get_coaching_notes` into AI modules. Dynamically import OpenRouter, Vercel AI SDK helpers, and Zod inside the handlers only. Preserve workspace ownership checks before cache/LLM access, `sentiment_cache` and `coaching_cache` checks before `enforceMcpAiUsage`, action types `mcp_sentiment` and `mcp_coaching`, model `openai/gpt-5-nano`, schema fields, cache updates, and markdown formatting.</action>
  <acceptance_criteria>
    - Both modules export category `ai`.
    - Cache hits do not invoke `enforceMcpAiUsage`.
    - LLM paths call `enforceMcpAiUsage` before `generateObject`.
    - All four AI tools are registered and category mapped to `ai`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts supabase/functions/mcp-server/__tests__/track-ai-usage-registry.test.ts supabase/functions/mcp-server/__tests__/category-gating.test.ts supabase/functions/mcp-server/__tests__/contract-surface.test.ts</automated>
  </verify>
  <done>All four AI tools are modularized and AI dependencies are no longer in the non-AI hot path.</done>
</task>

<task type="auto">
  <name>Task 3: Verify non-AI hot path has no AI top-level imports</name>
  <files>supabase/functions/mcp-server/index.ts, supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts, supabase/functions/mcp-server/__tests__/contract-surface.test.ts</files>
  <read_first>
    - supabase/functions/mcp-server/index.ts
    - supabase/functions/mcp-server/tools/ai/extract_action_items.ts
    - supabase/functions/mcp-server/tools/ai/ask_call.ts
    - supabase/functions/mcp-server/tools/ai/get_sentiment.ts
    - supabase/functions/mcp-server/tools/ai/get_coaching_notes.ts
  </read_first>
  <action>Add or update assertions so `index.ts`, `protocol.ts`, `auth.ts`, `gating.ts`, and non-AI modules do not contain top-level imports for `@openrouter/ai-sdk-provider`, `generateText`, `generateObject`, or `zod`. The four AI modules may contain dynamic `await import(...)` for those packages. Run `npm run build` after the AI extraction because this is the first cold-start-affecting dependency move.</action>
  <acceptance_criteria>
    - `rg -n "openrouter|generateText|generateObject|zod" supabase/functions/mcp-server/index.ts supabase/functions/mcp-server/tools/read supabase/functions/mcp-server/tools/write supabase/functions/mcp-server/tools/admin` returns no matches.
    - AI invariant tests still pass.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts supabase/functions/mcp-server/__tests__/contract-surface.test.ts && npm run build</automated>
  </verify>
  <done>Read-only cold starts no longer parse AI SDK/OpenRouter/Zod from `index.ts` or non-AI modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| AI module -> OpenRouter | Customer transcript data leaves CallVault for AI analysis |
| cache -> usage gate | Cache hit ordering determines whether users are charged |
| non-AI request -> module graph | Read-only calls should not load AI dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-07-01 | Information Disclosure | AI modules | mitigate | Preserve ownership checks before cache or LLM access. |
| T-02-07-02 | Repudiation | AI usage tracking | mitigate | Preserve `enforceMcpAiUsage` action types and ordering in invariant tests. |
| T-02-07-03 | Denial of Service | cold-start path | mitigate | Dynamic imports keep AI dependency parse out of non-AI calls. |
| T-02-07-04 | Tampering | cache fields | mitigate | Preserve cache schema fields and cache write behavior from current case blocks. |
</threat_model>

<verification>
Run:
`npm test -- --run supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts supabase/functions/mcp-server/__tests__/track-ai-usage-registry.test.ts supabase/functions/mcp-server/__tests__/category-gating.test.ts supabase/functions/mcp-server/__tests__/contract-surface.test.ts && npm run build`
</verification>

<success_criteria>
- All 4 AI tools live under `tools/ai/`.
- AI-only dependencies are dynamically imported in AI handlers only.
- AI cache, usage, and response invariants pass.
</success_criteria>

<output>
Create `.planning/phases/02-mcp-monolith-refactor/02-07-SUMMARY.md` when done.
</output>
