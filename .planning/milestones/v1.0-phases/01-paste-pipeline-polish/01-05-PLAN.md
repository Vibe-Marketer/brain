---
phase: 01-paste-pipeline-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/panes/ImportSourcePane.tsx
  - src/components/onboarding/OnboardingModal.tsx
  - src/lib/onboarding-connectors.ts
  - src/components/panes/__tests__/ImportSourcePane.registry.test.ts
  - src/components/onboarding/__tests__/OnboardingModal.registry.test.ts
  - src/lib/__tests__/onboarding-connectors.test.ts
autonomous: true
requirements:
  - MAN-06
must_haves:
  truths:
    - "D-07 / D-10: Audio/video upload cues are no longer visible in import source panes or onboarding."
    - "D-11 / D-12: Manual transcript import remains reachable where intended, without expanding Phase 1 into broader navigation redesign."
    - "Hidden-source behavior stays centralized in shared registry/onboarding helpers so upload surfaces do not silently reappear."
  artifacts:
    - path: "src/components/panes/ImportSourcePane.tsx"
      provides: "Import source list without visible `file-upload` row"
    - path: "src/components/onboarding/OnboardingModal.tsx"
      provides: "Onboarding source choices without audio/video upload cues"
    - path: "src/lib/onboarding-connectors.ts"
      provides: "Shared onboarding helper that filters hidden upload sources"
  key_links:
    - "ImportSourcePane should inherit source visibility from shared source metadata rather than a one-off local branch."
    - "Onboarding source choices and helper tests should stay aligned so hidden upload cues cannot drift back in."
    - "This plan intentionally avoids unrelated global nav changes even if D-12 is reviewed while touching import copy."
---

<objective>
Deliver the pane/onboarding MAN-06 cleanup by applying D-07/D-10 to visible source choices while preserving the D-11 transcript-import path and keeping scope tight per D-12.

Purpose: finish the visible upload-surface removal without coupling it to the route-compatibility work in 01-03.
Output: cleaned pane/onboarding source visibility plus tests that lock hidden-source behavior in the shared helpers.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-paste-pipeline-polish/01-CONTEXT.md
@.planning/phases/01-paste-pipeline-polish/01-PATTERNS.md
@.planning/phases/01-paste-pipeline-polish/01-UI-SPEC.md
@src/components/panes/ImportSourcePane.tsx
@src/components/onboarding/OnboardingModal.tsx
@src/lib/onboarding-connectors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove upload cues from import source panes using the shared visibility rules</name>
  <files>src/components/panes/ImportSourcePane.tsx, src/components/panes/__tests__/ImportSourcePane.registry.test.ts</files>
  <read_first>
    - .planning/phases/01-paste-pipeline-polish/01-PATTERNS.md (`src/components/panes/ImportSourcePane.tsx` -> `src/components/panes/__tests__/ImportSourcePane.registry.test.ts`; `src/config/source-registry.ts` as the shared visibility source)
    - src/components/panes/ImportSourcePane.tsx
    - src/components/panes/__tests__/ImportSourcePane.registry.test.ts
    - .planning/phases/01-paste-pipeline-polish/01-UI-SPEC.md
  </read_first>
  <action>Use the registry-driven source-list analog from `01-PATTERNS.md`: keep `ImportSourcePane.tsx` rendering from shared source metadata rather than local hard-coded exceptions, and make the tests prove that hidden sources stay hidden. Remove any visible File Upload or audio/video upload cues from the pane per D-07 and D-10, while keeping the transcript-import source reachable where the current UX expects it.</action>
  <acceptance_criteria>
    - Import source panes no longer show File Upload or audio/video upload copy.
    - Transcript import remains reachable through the intended pane flow.
    - The pane test asserts hidden-source behavior through the shared registry contract identified in `01-PATTERNS.md`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/panes/__tests__/ImportSourcePane.registry.test.ts</automated>
  </verify>
  <done>The import source pane no longer advertises audio/video upload, and the hidden-source rule is test-backed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove onboarding upload cues without expanding into unrelated navigation work</name>
  <files>src/components/onboarding/OnboardingModal.tsx, src/lib/onboarding-connectors.ts, src/components/onboarding/__tests__/OnboardingModal.registry.test.ts, src/lib/__tests__/onboarding-connectors.test.ts</files>
  <read_first>
    - .planning/phases/01-paste-pipeline-polish/01-PATTERNS.md (`src/components/onboarding/OnboardingModal.tsx` -> `src/lib/__tests__/onboarding-connectors.test.ts`)
    - src/components/onboarding/OnboardingModal.tsx
    - src/lib/onboarding-connectors.ts
    - src/components/onboarding/__tests__/OnboardingModal.registry.test.ts
    - src/lib/__tests__/onboarding-connectors.test.ts
    - .planning/phases/01-paste-pipeline-polish/01-UI-SPEC.md
  </read_first>
  <action>Follow the onboarding-helper analog from `01-PATTERNS.md`: keep upload visibility controlled by the shared onboarding helper and connector metadata instead of adding one-off UI conditions. Remove audio/video upload cards and cues from `OnboardingModal.tsx` and `onboarding-connectors.ts` per D-07 and D-10, preserve the transcript-import path where Phase 1 still needs it, and do not broaden into a global nav/sidebar rename or unrelated onboarding redesign while honoring D-12.</action>
  <acceptance_criteria>
    - Onboarding does not show File Upload or audio/video upload cues.
    - Shared helper tests prove the hidden-source behavior instead of relying on modal-only branching.
    - No unrelated navigation redesign or global label rewrite is introduced in this plan.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/onboarding/__tests__/OnboardingModal.registry.test.ts src/lib/__tests__/onboarding-connectors.test.ts</automated>
  </verify>
  <done>Onboarding no longer suggests the deferred upload path, and the helper/test contract prevents the cue from creeping back in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared source visibility -> import pane | Pane source rows are driven by shared metadata and helper logic |
| onboarding helper -> onboarding modal | Helper output controls whether upload cues reappear in the first-run flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-18 | Information Disclosure | Import source pane visibility | mitigate | Keep hidden-source behavior centralized in shared metadata and prove it with registry tests so deferred upload options cannot reappear accidentally. |
| T-01-19 | Spoofing | Onboarding copy and source choices | mitigate | Remove audio/video upload cues and keep transcript-import language aligned with D-11 so users are not misled about Phase 1 capabilities. |
| T-01-20 | Tampering | Onboarding helper drift | mitigate | Lock helper behavior with focused tests so future edits cannot bypass the shared hidden-source filter. |
</threat_model>

<verification>
Run the pane/onboarding test suites and confirm by source inspection that the only remaining manual-import messaging points to transcript import, not audio/video upload.
</verification>

<success_criteria>
- Import panes and onboarding no longer advertise audio/video upload.
- Transcript import remains reachable where Phase 1 intends it.
- Hidden-source behavior is enforced by shared helper tests rather than ad hoc local branches.
</success_criteria>

<output>
Create `.planning/phases/01-paste-pipeline-polish/01-05-SUMMARY.md` when done
</output>
