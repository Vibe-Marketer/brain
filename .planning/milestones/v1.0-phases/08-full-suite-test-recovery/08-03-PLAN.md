---
phase: 08-full-suite-test-recovery
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/__tests__/MCPTab.permissions.test.tsx
autonomous: true
requirements:
  - SC-2
must_haves:
  truths:
    - "MCPTab.permissions.test.tsx mounts without 'useAuth must be used within AuthProvider' error"
    - "Count assertion checks for (16) write tools, not (12)"
    - "All existing toggle, AI-label, and tool-list tests still pass"
  artifacts:
    - path: src/components/settings/__tests__/MCPTab.permissions.test.tsx
      provides: "MCPTab unit tests with useMcpOAuthGrants properly mocked"
      contains: "vi.mock('@/hooks/useMcpOAuthGrants'"
  key_links:
    - from: src/components/settings/__tests__/MCPTab.permissions.test.tsx
      to: src/hooks/useMcpOAuthGrants.ts
      via: vi.mock stub added before MCPTab import
      pattern: "vi.mock.*useMcpOAuthGrants"
---

<objective>
Fix MCPTab.permissions.test.tsx by adding the missing vi.mock for @/hooks/useMcpOAuthGrants and updating the write-tool count assertion from (12) to (16).

Purpose: MCPTab.tsx imports useMcpOAuthGrantsList from @/hooks/useMcpOAuthGrants. That hook calls useAuth() internally. No AuthProvider wraps the test render, so React throws on mount. The test already mocks every other hook MCPTab depends on — useMcpOAuthGrants is the only gap.

Output: MCPTab mounts successfully in tests; write-tool count matches the live 16-tool surface.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-full-suite-test-recovery/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useMcpOAuthGrants mock and fix write-count assertion in MCPTab.permissions.test.tsx</name>
  <files>src/components/settings/__tests__/MCPTab.permissions.test.tsx</files>
  <read_first>
    - src/components/settings/__tests__/MCPTab.permissions.test.tsx (full file — identify the mock block before the MCPTab import, and the write-count assertion around line 190)
    - src/components/settings/__tests__/McpConnectionsTab.test.tsx (canonical reference for useMcpOAuthGrants mock shape — look at lines 50–53)
  </read_first>
  <action>
    Make exactly 2 changes to src/components/settings/__tests__/MCPTab.permissions.test.tsx:

    Change 1 — Insert the useMcpOAuthGrants mock.
    In the mock block, before the `import MCPTab from '../MCPTab';` line (around line 119 in the original), add this vi.mock call. It must appear BEFORE the MCPTab import so Vitest hoisting works correctly. Place it after the last existing vi.mock block and before the component import:

      vi.mock('@/hooks/useMcpOAuthGrants', () => ({
        useMcpOAuthGrantsList: () => ({ grants: [], isLoading: false, error: null }),
        useRevokeMcpOAuthGrant: () => ({ mutate: vi.fn(), isPending: false }),
      }));

    Change 2 — Update the write-count text assertion.
    Around line 193, change:
      expect(text).toMatch(/\(12\)/); // write
    to:
      expect(text).toMatch(/\(16\)/); // write

    Do NOT change any other lines. The (17) read, (8) admin, (4) ai assertions are already correct. The test title "renders correct counts per category section: 17/12/8/4" can stay as-is (cosmetic, not functional).
  </action>
  <verify>
    <automated>cd /Users/admin/dev/brain && npx vitest run src/components/settings/__tests__/MCPTab.permissions.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vi.mock('@/hooks/useMcpOAuthGrants'" src/components/settings/__tests__/MCPTab.permissions.test.tsx` returns 1
    - `grep -c "useMcpOAuthGrantsList" src/components/settings/__tests__/MCPTab.permissions.test.tsx` returns at least 1 (mock provides it)
    - `grep -c "useRevokeMcpOAuthGrant" src/components/settings/__tests__/MCPTab.permissions.test.tsx` returns at least 1
    - `grep -c "\\\\(12\\\\)" src/components/settings/__tests__/MCPTab.permissions.test.tsx` returns 0 (stale write-count assertion removed)
    - `grep -c "\\\\(16\\\\)" src/components/settings/__tests__/MCPTab.permissions.test.tsx` returns 1 (updated write-count assertion present)
    - `npx vitest run src/components/settings/__tests__/MCPTab.permissions.test.tsx` exits 0 with no test failures
    - No "useAuth must be used within AuthProvider" error in test output
  </acceptance_criteria>
  <done>MCPTab.permissions.test.tsx mounts without AuthProvider error and all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → hooks | No auth boundary; test-only change using vi.mock stubs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-SC | Tampering | test mock addition | accept | Test-only change; no production code touched; no npm installs |
</threat_model>

<verification>
Run targeted test file:
  npx vitest run src/components/settings/__tests__/MCPTab.permissions.test.tsx

Confirm mock is present:
  grep -c "vi.mock('@/hooks/useMcpOAuthGrants'" src/components/settings/__tests__/MCPTab.permissions.test.tsx
  (expected: 1)
</verification>

<success_criteria>
- `npx vitest run src/components/settings/__tests__/MCPTab.permissions.test.tsx` exits 0
- No AuthProvider error in output
- Write-count assertion checks for (16), not (12)
</success_criteria>

<output>
Create `.planning/phases/08-full-suite-test-recovery/08-03-SUMMARY.md` when done
</output>
