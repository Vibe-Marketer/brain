---
phase: 11-ticket-foundation-flag-removal
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - src/services/tickets.service.ts
  - src/hooks/useTickets.ts
  - src/services/__tests__/tickets.service.test.ts
  - src/components/settings/TicketTable.tsx
  - src/components/settings/TicketDetailDialog.tsx
  - src/components/settings/AdminTab.tsx
  - src/components/settings/__tests__/TicketTable.test.tsx
  - src/components/ui/status-badge.tsx
autonomous: true
requirements: [TKT-02]
must_haves:
  truths:
    - "An admin opens AdminTab and sees a Tickets section with a table of all tickets"
    - "The list filters by status, severity, and source via three Select controls"
    - "Clicking a row opens a detail view showing captured context, messages, and the full Activity event timeline"
    - "An admin changes a ticket's status from the detail view and the change persists (writing a ticket_events row via the 11-02 trigger)"
    - "Non-admin users never see the Tickets section (existing isAdmin gate)"
  artifacts:
    - path: "src/services/tickets.service.ts"
      provides: "getTickets, getTicketDetail, updateTicketStatus — pure async, no React"
      exports: ["getTickets", "getTicketDetail", "updateTicketStatus"]
    - path: "src/hooks/useTickets.ts"
      provides: "useTickets, useTicketDetail, useUpdateTicketStatus TanStack Query wrappers"
    - path: "src/components/settings/TicketTable.tsx"
      provides: "tickets table per UI-SPEC (columns, badges, sort)"
      min_lines: 60
  key_links:
    - from: "src/components/settings/AdminTab.tsx"
      to: "src/hooks/useTickets.ts"
      via: "Tickets section consumes hook (no inline supabase calls)"
      pattern: "useTickets"
    - from: "src/services/tickets.service.ts"
      to: "public.tickets"
      via: "supabase-js with user JWT (RLS enforced; admin sees all)"
      pattern: "from\\('tickets'\\)"
---

<objective>
Build the admin Tickets view: `tickets.service.ts` + `useTickets` hooks, a `TicketTable` mirroring the UserTable idiom, a Tickets section in AdminTab (in the slot cleared by 11-01) with status/severity/source filters, and a detail dialog rendering context, messages, and the full event timeline with an admin status-change control.

Purpose: TKT-02 — the lost-screen-capture-ticket scenario ends here: an admin can find any ticket and reconstruct its lifecycle from the timeline. Follows the approved 11-UI-SPEC.md contract exactly (copy, badges, spacing, layout locked there).
Output: working Tickets section in AdminTab, service/hook layer, component + service tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-ticket-foundation-flag-removal/11-CONTEXT.md
@.planning/phases/11-ticket-foundation-flag-removal/11-UI-SPEC.md (design contract — copywriting, badge mapping, layout: FOLLOW EXACTLY)
@.planning/phases/11-ticket-foundation-flag-removal/11-PATTERNS.md (analog excerpts: personal-tags service/hook, UserTable, dialog shell, AdminTab section idiom)
@src/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tickets service + hooks with unit tests</name>
  <files>src/services/tickets.service.ts, src/hooks/useTickets.ts, src/services/__tests__/tickets.service.test.ts</files>
  <read_first>src/services/personal-tags.service.ts, src/hooks/usePersonalTags.ts, src/types/supabase.ts, src/services/__tests__/ (existing test style)</read_first>
  <behavior>
    - getTickets({status?, severity?, source?}) builds a supabase query on tickets ordered created_at desc, applying .eq only for non-'all' filters; throws Error('Failed to fetch tickets: ...') on error
    - getTicketDetail(id) returns the ticket plus its ticket_messages (created_at asc) and ticket_events (created_at desc)
    - updateTicketStatus(id, status) calls .update({status}).eq('id', id) and throws on error
  </behavior>
  <action>Create src/services/tickets.service.ts following the personal-tags.service.ts analog (PATTERNS.md): typed Ticket/TicketMessage/TicketEvent interfaces sourced from src/types/supabase.ts row types (or local interfaces matching the 11-02 columns if codegen names are awkward), the three functions in the behavior block, error style 'Failed to X: ' + error.message. No React imports. Create src/hooks/useTickets.ts following the usePersonalTags analog: useTickets(filters) with queryKey ['tickets', filters] enabled on session, useTicketDetail(ticketId) with queryKey ['ticket', ticketId], useUpdateTicketStatus() mutation invalidating ['tickets'] and ['ticket', id] with toast.success('Status updated') / toast.error('Failed to update status') per UI-SPEC copy. Write src/services/__tests__/tickets.service.test.ts unit tests (mocked supabase client per existing service-test style) covering the three behaviors including filter-skipping for 'all'.</action>
  <verify>
    <automated>npx vitest run src/services/__tests__/tickets.service.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - tickets.service.ts exports getTickets, getTicketDetail, updateTicketStatus; contains no React import
    - useTickets.ts wraps all three via TanStack Query with the specified query keys and toasts
    - Service unit tests pass; `npm run build` exits 0
  </acceptance_criteria>
  <done>Data layer complete per the locked service+hook pattern.</done>
</task>

<task type="auto">
  <name>Task 2: TicketTable + Tickets section in AdminTab with filters</name>
  <files>src/components/settings/TicketTable.tsx, src/components/settings/AdminTab.tsx, src/components/settings/__tests__/TicketTable.test.tsx</files>
  <read_first>.planning/phases/11-ticket-foundation-flag-removal/11-UI-SPEC.md, src/components/settings/UserTable.tsx, src/components/settings/AdminTab.tsx, src/components/ui/status-badge.tsx</read_first>
  <action>Create TicketTable.tsx mirroring UserTable.tsx (React.memo, ui/table primitives, useTableSort + SortButton helper, Remix icons only): columns Type (icon + label), Summary (first-message excerpt from the list query or context title, truncated), Severity (StatusBadge), Status (StatusBadge), Source (muted text), Reporter, Created (date-fns relative, text-xs text-muted-foreground tabular-nums); row onClick opens detail. Use the UI-SPEC badge mapping exactly: status new→new, triaged→info, in_progress→active, awaiting_approval→warning, awaiting_user→default, resolved→success, rejected→inactive, escalated→error; severity critical→error, high→warning, medium→info, low→default — extend status-badge.tsx badgeLabelMap/variants ONLY if a needed label is missing, reusing existing color recipes. In AdminTab.tsx insert the Tickets section between System Overview and User Management (the slot cleared by plan 11-01), using the existing section idiom: h2 'Tickets' + subtitle 'Support tickets, bug reports, and tasks across the platform', space-y-4 content, Separator className="my-16" before User Management. Filters row per UI-SPEC: three Select controls (Status/Severity/Source) at sm:w-40 with Labels, 'All Statuses'/'All Severities'/'All Sources' first. Data via useTickets hook ONLY — no inline supabase calls (locked pattern; do not copy AdminTab's legacy loaders). Loading state: centered RiLoader2Line py-12. Empty state per UI-SPEC: dashed border rounded-xl py-12, RiTicketLine h-12 w-12 text-muted-foreground, heading 'No tickets yet', body 'Tickets submitted from the support form or created here will appear in this list.'; filtered-empty 'No tickets match your filters'. Footer 'Showing {filtered} of {total} tickets'. Load-error inline copy 'Could not load tickets. Refresh to try again.' plus toast 'Failed to load tickets'. Add TicketTable.test.tsx covering: renders rows from props, status/severity badges present, row click fires handler, empty state renders both copy variants.</action>
  <verify>
    <automated>npx vitest run src/components/settings/__tests__/TicketTable.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - AdminTab.tsx contains a Tickets section heading 'Tickets' consuming useTickets (zero new inline supabase queries)
    - TicketTable.tsx uses @/components/ui/table primitives and @remixicon/react icons only
    - Filter Selects for status, severity, source present with 'all' defaults
    - Empty-state and footer copy match 11-UI-SPEC.md strings exactly
    - Component tests pass; `npm run build` exits 0
  </acceptance_criteria>
  <done>Admin sees the filterable tickets list inside AdminTab per the UI contract.</done>
</task>

<task type="auto">
  <name>Task 3: Ticket detail dialog with event timeline and status control</name>
  <files>src/components/settings/TicketDetailDialog.tsx, src/components/settings/AdminTab.tsx</files>
  <read_first>.planning/phases/11-ticket-foundation-flag-removal/11-UI-SPEC.md (Layout & Interaction Contract — detail view), src/components/support/SupportTicketDialog.tsx, src/components/ui/dialog.tsx</read_first>
  <action>Create TicketDetailDialog.tsx per the UI-SPEC detail contract: Radix Dialog at sm:max-w-2xl (dialog shell analog: SupportTicketDialog), controlled open/onOpenChange + ticketId prop, data via useTicketDetail. Layout top-to-bottom: header with title '{Type} · #{first 6 chars of id}' plus status and severity StatusBadges (focal point) and a status-change Select wired to useUpdateTicketStatus offering the 8 lifecycle values; context block rendering captured url, appVersion, userAgent, org/workspace ids from context JSONB as a text-xs definition list; messages list (author_type label + body, created_at asc); 'Activity' heading + event timeline — vertically stacked text-xs rows, dot marker + human-readable event description ('Status changed: new → triaged', 'Ticket created') + date-fns relative timestamp, newest first, sourced from ticket_events (TKT-04 surface). Wire row click in AdminTab's Tickets section to open the dialog with the selected ticket id. Status select disabled while mutation pending; toasts handled by the hook.</action>
  <verify>
    <automated>npm test 2>&1 | tail -5 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - TicketDetailDialog.tsx renders messages and an 'Activity' section mapping ticket_events with old_value/new_value in the description
    - Status Select lists exactly the 8 locked lifecycle values and calls useUpdateTicketStatus
    - Dialog opens from TicketTable row click in AdminTab
    - `npm test` and `npm run build` exit 0
  </acceptance_criteria>
  <done>Full ticket lifecycle reconstructable on screen from the audit trail; admin can transition status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → PostgREST | reads/updates run under the admin's JWT; RLS is the enforcement layer |
| ticket context JSONB → DOM | user-captured strings (url, userAgent) render in the detail dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Elevation of Privilege | Tickets section exposure | mitigate | Section renders inside AdminTab's existing useUserRole isAdmin gate; data access independently enforced by 11-02 RLS (defense in depth — UI gate is cosmetic, RLS is the control) |
| T-11-11 | Tampering | status updates from non-admin JWT | mitigate | 11-02 UPDATE policy admin-only; frontend never the enforcement point |
| T-11-12 | Injection (XSS) | context JSONB / message body rendering | mitigate | render as React text nodes only — no dangerouslySetInnerHTML anywhere in the new components |
| T-11-SC | Tampering | package installs | accept | No package installs this plan — slopcheck gate not applicable |
</threat_model>

<verification>
- `npm test` and `npm run build` exit 0
- Component tests prove list, filters, badges, empty states, detail timeline render
- Execution-time visual verification of the Tickets section against 11-UI-SPEC.md via dev-browser (project hard rule)
</verification>

<success_criteria>
Phase success criterion 3 satisfied: admin opens AdminTab tickets view, filters by status/severity/source, and the detail view renders the full event timeline for any ticket (TKT-02).
</success_criteria>

<output>
Create `.planning/phases/11-ticket-foundation-flag-removal/11-03-SUMMARY.md` when done
</output>
