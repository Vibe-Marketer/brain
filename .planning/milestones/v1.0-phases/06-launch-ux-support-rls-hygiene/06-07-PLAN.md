---
phase: 06-launch-ux-support-rls-hygiene
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/rls-regression.test.ts
  - supabase/functions/fetch-meetings/index.ts
  - supabase/functions/_shared/connector-function-utils.ts
autonomous: true
gap_closure: true
requirements:
  - ONB-01
  - HRD-02
must_haves:
  truths:
    - "HRD-02 real-DB safety closes only when `src/test/rls-regression.test.ts` requires `VITE_SUPABASE_TEST_URL` and `VITE_SUPABASE_TEST_ANON_KEY` with no fallback to production-like env vars."
    - "Launch credential hygiene closes only when refreshed Fathom OAuth tokens persist through the existing encrypted connector token path instead of direct plaintext writes."
  artifacts:
    - path: "src/test/rls-regression.test.ts"
      provides: "Phase 06 RLS regression suite with test-project-only anon URL/key contract"
    - path: "supabase/functions/fetch-meetings/index.ts"
      provides: "Fathom refresh flow that reuses encrypted token persistence patterns during token refresh"
    - path: "supabase/functions/_shared/connector-function-utils.ts"
      provides: "shared connector OAuth persistence helper reused by refresh paths when helper extraction is needed"
  key_links:
    - from: "src/test/rls-regression.test.ts"
      to: "src/test/integration-setup.ts"
      via: "matching test-project-only env guard contract"
    - from: "supabase/functions/fetch-meetings/index.ts"
      to: "supabase/functions/_shared/connector-function-utils.ts"
      via: "persistOAuthTokens helper reuse for refreshed OAuth credentials"
---

<objective>
Close the two Phase 06 code-level blockers: remove unsafe non-test env fallback from the RLS regression suite, and route refreshed Fathom OAuth tokens through the existing encrypted persistence pattern.

Purpose: restore safe real-DB verification boundaries and keep connector OAuth token refresh on the same encrypted storage path already used by callback flows.
Output: guarded RLS regression config plus fetch-meetings refresh persistence aligned with shared connector token helpers.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-CONTEXT.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-RESEARCH.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-PATTERNS.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-VERIFICATION.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-REVIEW.md
@src/CLAUDE.md
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce test-project-only env vars in the Phase 06 RLS regression suite</name>
  <files>src/test/rls-regression.test.ts</files>
  <read_first>
    - src/test/rls-regression.test.ts
    - src/test/integration-setup.ts
    - supabase/CLAUDE.md
    - .planning/phases/06-launch-ux-support-rls-hygiene/06-VERIFICATION.md
    - .planning/phases/06-launch-ux-support-rls-hygiene/06-REVIEW.md
  </read_first>
  <action>Replace the Phase 06 suite-local fallback chain with the same hard contract described in `supabase/CLAUDE.md`: `src/test/rls-regression.test.ts` must read only `VITE_SUPABASE_TEST_URL` and `VITE_SUPABASE_TEST_ANON_KEY` for anon-auth sign-in, never `VITE_SUPABASE_URL` or `VITE_SUPABASE_PUBLISHABLE_KEY`. Update the suite-level error text so missing values explicitly name the `*_TEST_*` vars. Keep the current `integrationDbReachable`/service-role guard flow intact per HRD-02; do not broaden into unrelated integration tests or change the real-DB fixture logic.</action>
  <acceptance_criteria>
    - `src/test/rls-regression.test.ts` no longer references `VITE_SUPABASE_URL`.
    - `src/test/rls-regression.test.ts` no longer references `VITE_SUPABASE_PUBLISHABLE_KEY`.
    - Missing anon test env vars fail with messaging that names `VITE_SUPABASE_TEST_URL` and `VITE_SUPABASE_TEST_ANON_KEY`.
    - The suite still uses `integrationDbReachable` and `makeIntegrationClient()` for the service-role path.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "VITE_SUPABASE_TEST_URL|VITE_SUPABASE_TEST_ANON_KEY" src/test/rls-regression.test.ts && ! rg -n "VITE_SUPABASE_URL|VITE_SUPABASE_PUBLISHABLE_KEY" src/test/rls-regression.test.ts && VITEST_INTEGRATION_OK=true npm run test -- --run src/test/rls-regression.test.ts</automated>
  </verify>
  <done>The RLS regression suite cannot silently target a non-test Supabase URL/key pair.</done>
</task>

<task type="auto">
  <name>Task 2: Persist refreshed Fathom OAuth tokens through the shared encrypted helper path</name>
  <files>supabase/functions/fetch-meetings/index.ts, supabase/functions/_shared/connector-function-utils.ts</files>
  <read_first>
    - supabase/functions/fetch-meetings/index.ts
    - supabase/functions/_shared/connector-function-utils.ts
    - supabase/functions/_shared/oauth-callback-handler.ts
    - supabase/functions/fathom-oauth-callback/index.ts
    - supabase/functions/_shared/user-settings-encrypt.ts
    - .planning/phases/06-launch-ux-support-rls-hygiene/06-VERIFICATION.md
    - .planning/phases/06-launch-ux-support-rls-hygiene/06-REVIEW.md
  </read_first>
  <action>Refactor the expired-token branch in `fetch-meetings` so refreshed Fathom tokens stop writing directly to `import_sources.oauth_access_token`, `import_sources.oauth_refresh_token`, `user_settings.oauth_access_token`, and `user_settings.oauth_refresh_token`. Reuse the connector callback encryption pattern already present in this repo: write `import_sources` through `persistOAuthTokens(...)`, and mirror user-settings persistence through the existing encrypted user-settings RPC/fallback path already used by `fathom-oauth-callback`. If the user-settings branch cannot be reused cleanly inline, extract a tiny shared helper into `connector-function-utils.ts` rather than duplicating another plaintext write path. Do not invent a new token table, new RPC, or a new storage destination.</action>
  <acceptance_criteria>
    - `fetch-meetings/index.ts` no longer contains direct `.update({ oauth_access_token ... oauth_refresh_token ... })` writes for refreshed Fathom tokens on `import_sources`.
    - Refreshed `import_sources` tokens flow through `persistOAuthTokens(`.
    - Refreshed user-settings tokens follow the existing encrypted callback pattern instead of a direct plaintext-only write path.
    - The refresh branch still preserves `oauth_token_expires` and keeps the source active after a successful refresh.
    - No new token storage path or table is introduced.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "persistOAuthTokens\\(|store_encrypted_user_settings_tokens|set_encrypted_user_settings_tokens|encrypt.*oauth|oauth.*encrypt" supabase/functions/fetch-meetings/index.ts supabase/functions/_shared/connector-function-utils.ts supabase/functions/_shared/user-settings-encrypt.ts && ! rg -n "\\.update\\(\\{[[:space:]]*(oauth_access_token|oauth_refresh_token)|oauth_access_token: tokens\\.access_token|oauth_refresh_token: tokens\\.refresh_token" supabase/functions/fetch-meetings/index.ts && npm run build</automated>
  </verify>
  <done>Fathom token refresh persists credentials through the same encrypted pattern already used by connector OAuth callbacks.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-07-01 | Tampering | real-DB test env selection | mitigate | Remove non-test env fallback so destructive fixture setup cannot silently bind to prod-like Supabase credentials. |
| T-06-07-02 | Information Disclosure | refreshed OAuth tokens at rest | mitigate | Reuse encrypted connector persistence helpers/RPCs instead of direct plaintext writes in `fetch-meetings`. |
| T-06-07-03 | Repudiation | token refresh state | mitigate | Preserve explicit expiry updates and shared helper usage so refreshed credentials are written through the standard connector path. |
</threat_model>

<verification>
Run `npm run test -- --run src/test/rls-regression.test.ts` with test env vars set, and `npm run build`. If the RLS suite skips because the dedicated test project env is absent, record that no live cross-org proof was obtained in the summary.
</verification>

<success_criteria>
- `src/test/rls-regression.test.ts` requires `VITE_SUPABASE_TEST_URL` and `VITE_SUPABASE_TEST_ANON_KEY` only.
- `fetch-meetings` refresh no longer writes refreshed OAuth tokens through direct plaintext DB updates.
- Phase 06 verifier truths for real-DB safety and connector credential hygiene are unblocked without unrelated hardening work.
</success_criteria>

<output>
Create `.planning/phases/06-launch-ux-support-rls-hygiene/06-07-SUMMARY.md` when done.
</output>
