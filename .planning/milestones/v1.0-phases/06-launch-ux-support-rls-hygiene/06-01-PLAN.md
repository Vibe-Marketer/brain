---
phase: 06-launch-ux-support-rls-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/SetupTrialUpsell.tsx
  - src/pages/ImportPage.tsx
  - src/components/onboarding/OnboardingVideoModal.tsx
  - src/components/connectors/ConnectorImportWizard.tsx
  - src/components/connectors/__tests__/ConnectorImportWizard.test.tsx
  - src/pages/__tests__/SetupTrialUpsell.registry.test.ts
  - src/pages/__tests__/ImportPage.connector-routing.test.ts
autonomous: true
requirements:
  - ONB-01
  - ONB-04
must_haves:
  truths:
    - "D-01: the existing signup/setup/trial/import path remains intact."
    - "D-02: after trial setup the user lands on the first connector import page, not a dashboard."
    - "D-03/D-04: first connector import surfaces available historical provider calls and makes `Sync all` primary without auto-importing history."
    - "D-05/D-06/D-07/D-08: founder onboarding video appears once after trial setup, explains the first-use process, remains reachable later, and does not replace the import surface."
  artifacts:
    - path: "src/pages/SetupTrialUpsell.tsx"
      provides: "trial completion routing into import with first-run video marker"
    - path: "src/pages/ImportPage.tsx"
      provides: "connected-provider selection and first-run video display"
    - path: "src/components/onboarding/OnboardingVideoModal.tsx"
      provides: "reusable founder video dialog"
---

<objective>
Make the post-trial first-run experience land on the connected provider import surface, show the founder onboarding video once, and make historical `Sync all` explicit.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-CONTEXT.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-RESEARCH.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-UI-SPEC.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-PATTERNS.md
@src/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve trial completion context and route into the first connector import surface</name>
  <files>src/pages/SetupTrialUpsell.tsx, src/pages/ImportPage.tsx, src/pages/__tests__/SetupTrialUpsell.registry.test.ts, src/pages/__tests__/ImportPage.connector-routing.test.ts</files>
  <read_first>
    - src/pages/SetupTrialUpsell.tsx
    - src/pages/SetupWizard.tsx
    - src/pages/OAuthCallback.tsx
    - src/pages/ImportPage.tsx
    - src/lib/onboarding-connectors.ts
    - src/pages/__tests__/SetupTrialUpsell.registry.test.ts
    - src/pages/__tests__/ImportPage.connector-routing.test.ts
  </read_first>
  <action>Update trial completion so both `enterApp()` and `UpgradeButton` checkout start preserve the most recently connected onboarding connector and a first-run video marker in the `/import` success path. Use existing connection params where available: `source`, `sourceId`, and `email`. In `ImportPage`, select the connected source from the URL params or stored setup state, then remove only consumed query params from browser history. Do not route to `/`, dashboard, or organization pages for the normal individual-user flow.</action>
  <acceptance_criteria>
    - `SetupTrialUpsell.tsx` still calls `completeOnboarding()` before app entry and checkout.
    - The normal app-entry path navigates to `/import` with enough state to select the connected source or falls back to `/import` without breaking.
    - `ImportPage.tsx` selects an onboarding connector when `source` identifies a selectable connector.
    - Tests assert `/setup/trial` checkout success stays in the import flow.
    - Tests assert OAuth return connection params are consumed without navigating to a dashboard.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run src/pages/__tests__/SetupTrialUpsell.registry.test.ts src/pages/__tests__/ImportPage.connector-routing.test.ts</automated>
  </verify>
  <done>Trial completion consistently opens the relevant import surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add reusable founder onboarding video modal</name>
  <files>src/components/onboarding/OnboardingVideoModal.tsx, src/pages/ImportPage.tsx</files>
  <read_first>
    - src/components/onboarding/HowItWorksModal.tsx
    - src/components/ui/dialog.tsx
    - src/components/ui/button.tsx
    - src/pages/ImportPage.tsx
    - .planning/phases/06-launch-ux-support-rls-hygiene/06-UI-SPEC.md
  </read_first>
  <action>Create `OnboardingVideoModal` using existing Radix Dialog and Button primitives. The modal title must be `Start with your call library`, the primary CTA must be `Start syncing calls`, and the video slot must use a stable `aspect-video` container. Read video URL from a Vite env var such as `VITE_ONBOARDING_VIDEO_URL`; if absent, render the fallback copy and keep the primary CTA usable. In `ImportPage`, show this modal once when the first-run marker is present and persist dismissal in localStorage under a stable `callvault_onboarding_video_seen` key.</action>
  <acceptance_criteria>
    - `OnboardingVideoModal.tsx` imports icons only from `@remixicon/react`.
    - The modal uses existing `Dialog` and `Button` components.
    - Missing video URL does not render a broken iframe or blank modal.
    - `ImportPage.tsx` can open the modal from first-run marker and dismiss it to reveal the import surface.
    - The dismissal key is stable and scoped to onboarding video, not to all support/help modals.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Start with your call library|Start syncing calls|callvault_onboarding_video_seen|VITE_ONBOARDING_VIDEO_URL" src/components/onboarding/OnboardingVideoModal.tsx src/pages/ImportPage.tsx</automated>
  </verify>
  <done>The founder video is a reusable education layer over the import page.</done>
</task>

<task type="auto">
  <name>Task 3: Make historical import explicit with `Sync all` as primary</name>
  <files>src/components/connectors/ConnectorImportWizard.tsx, src/components/connectors/__tests__/ConnectorImportWizard.test.tsx, src/pages/ImportPage.tsx</files>
  <read_first>
    - src/components/connectors/ConnectorImportWizard.tsx
    - src/components/connectors/connectorSearch.ts
    - src/components/connectors/registry/types.ts
    - src/components/connectors/registry/adapters/fathom.ts
    - src/pages/ImportPage.tsx
    - src/components/connectors/__tests__/ConnectorImportWizard.test.tsx
  </read_first>
  <action>Update the connector import wizard so fetched available calls present `Sync all` as the primary action when results exist. Add or reuse selection logic so `Sync all` selects/imports all currently fetched available calls. Do not auto-import historical calls inside `ImportPage` after OAuth connection success; remove or gate the current connector sync invocation so it cannot import historical calls without the user pressing an explicit CTA. Keep future/background sync behavior only if the called function is proven not to import historical calls.</action>
  <acceptance_criteria>
    - `ConnectorImportWizard` renders a visible `Sync all` primary CTA when importable results are present.
    - Pressing `Sync all` imports all fetched available rows or selects all rows before import through existing importSelected flow.
    - `ImportPage.tsx` no longer invokes a historical sync function automatically on OAuth return.
    - Tests assert no user-visible copy says historical calls are imported automatically.
    - Tests assert a sync-all path exists in the connector wizard.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run src/components/connectors/__tests__/ConnectorImportWizard.test.tsx src/pages/__tests__/ImportPage.connector-routing.test.ts && npm run build</automated>
  </verify>
  <done>The user can intentionally sync all available provider calls in one action.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-01-01 | Tampering | onboarding route state | mitigate | Treat route/localStorage state as UI hints only; never grant data access from it. |
| T-06-01-02 | Repudiation | historical import | mitigate | Require explicit `Sync all` click before historical imports. |
| T-06-01-03 | Information Disclosure | video embed | mitigate | Use configured public video URL only; do not embed user-specific data in iframe URL. |
</threat_model>

<verification>
Run `npm run test -- --run src/pages/__tests__/SetupTrialUpsell.registry.test.ts src/pages/__tests__/ImportPage.connector-routing.test.ts src/components/connectors/__tests__/ConnectorImportWizard.test.tsx` and `npm run build`.
</verification>

<success_criteria>
- A newly onboarded user lands on the connected provider import page after trial setup.
- Founder video appears once and does not block access after dismissal.
- Historical provider calls require explicit `Sync all` confirmation.
</success_criteria>

<output>
Create `.planning/phases/06-launch-ux-support-rls-hygiene/06-01-SUMMARY.md` when done.
</output>
