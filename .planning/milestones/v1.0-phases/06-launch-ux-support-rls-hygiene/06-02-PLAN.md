---
phase: 06-launch-ux-support-rls-hygiene
plan: 02
type: execute
wave: 1
depends_on:
  - "06-01"
files_modified:
  - src/components/ui/sidebar-nav.tsx
  - src/components/support/SupportPopover.tsx
  - src/components/support/SupportTicketDialog.tsx
  - src/services/support-ticket.service.ts
  - supabase/functions/send-support-ticket/index.ts
  - src/components/ui/__tests__/sidebar-nav.test.tsx
autonomous: true
requirements:
  - ONB-05
must_haves:
  truths:
    - "D-15/D-16: Support lives near the sidebar bottom above Settings and opens an anchored popout, not a full page."
    - "D-17: Support actions are Watch the Onboarding Video, Take the Tour, How It Works, Support Docs, and Submit a Ticket."
    - "D-18: Support Docs opens `https://docs.callvaultai.com` in a new tab."
    - "D-19/D-20/D-21: tickets send to support@callvaultai.com without default Andrew cc and include only simple available context."
  artifacts:
    - path: "src/components/support/SupportPopover.tsx"
      provides: "single support action popout"
    - path: "supabase/functions/send-support-ticket/index.ts"
      provides: "authenticated ticket email endpoint"
---

<objective>
Consolidate sidebar help into one support popout and add a simple authenticated support-ticket email flow.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-launch-ux-support-rls-hygiene/06-CONTEXT.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-RESEARCH.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-UI-SPEC.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-PATTERNS.md
@src/CLAUDE.md
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace separate sidebar help buttons with one Support popout</name>
  <files>src/components/ui/sidebar-nav.tsx, src/components/support/SupportPopover.tsx, src/components/ui/__tests__/sidebar-nav.test.tsx</files>
  <read_first>
    - src/components/ui/sidebar-nav.tsx
    - src/components/onboarding/HowItWorksModal.tsx
    - src/components/onboarding/OnboardingVideoModal.tsx
    - src/lib/tour.ts
    - src/components/ui/popover.tsx
    - src/components/ui/dropdown-menu.tsx
    - src/components/ui/__tests__/sidebar-nav.test.tsx
  </read_first>
  <action>Create `SupportPopover` under `src/components/support/` and mount it from `SidebarNav` in the bottom section directly above Settings. Remove the separate visible `Take the tour` and `How it works` sidebar buttons. The popout must show exactly these action labels in order: `Watch the Onboarding Video`, `Take the Tour`, `How It Works`, `Support Docs`, `Submit a Ticket`. Wire `Take the Tour` to `startTour()`, `How It Works` to the existing modal, `Watch the Onboarding Video` to `OnboardingVideoModal`, and `Support Docs` to `window.open("https://docs.callvaultai.com", "_blank", "noopener,noreferrer")`.</action>
  <acceptance_criteria>
    - `sidebar-nav.tsx` renders one Support trigger above Settings.
    - The old sidebar bottom labels `Take the tour` and `How it works` no longer render as separate sidebar buttons.
    - `SupportPopover` contains all five required action labels in the required order.
    - Docs action opens `https://docs.callvaultai.com` in a new tab with noopener/noreferrer.
    - No new icon package is imported.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run src/components/ui/__tests__/sidebar-nav.test.tsx && rg -n "Watch the Onboarding Video|Support Docs|docs.callvaultai.com" src/components/support src/components/ui/sidebar-nav.tsx</automated>
  </verify>
  <done>Support entry point is a single anchored popout.</done>
</task>

<task type="auto">
  <name>Task 2: Add support ticket UI and service wrapper</name>
  <files>src/components/support/SupportTicketDialog.tsx, src/components/support/SupportPopover.tsx, src/services/support-ticket.service.ts</files>
  <read_first>
    - src/components/support/SupportPopover.tsx
    - src/components/ui/dialog.tsx
    - src/components/ui/textarea.tsx
    - src/components/ui/button.tsx
    - src/integrations/supabase/client.ts
    - src/hooks/useOrganizationContext.ts
    - src/contexts/AuthContext.tsx
    - src/lib/logger.ts
  </read_first>
  <action>Create `SupportTicketDialog` with a message textarea, optional reply email only when the authenticated email is unavailable, and submit CTA `Send ticket`. Create `submitSupportTicket()` in `src/services/support-ticket.service.ts` that invokes `send-support-ticket` with message, current URL/path, user agent, user ID, email if available, active organization ID, active workspace ID, and app version/commit only if already exposed through Vite env. The UI must not display raw IDs. Show Sonner success text `Ticket sent to support` and error text `Ticket could not be sent`.</action>
  <acceptance_criteria>
    - `SupportTicketDialog` contains `Send ticket`.
    - Service file contains no React imports.
    - Ticket payload includes `url`, `userAgent`, `userId`, `organizationId`, and `workspaceId` keys when available.
    - Raw user/org/workspace IDs are not rendered as visible labels or body copy.
    - Success and error toasts use the exact UI spec copy.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Send ticket|Ticket sent to support|Ticket could not be sent|send-support-ticket|userAgent|organizationId|workspaceId" src/components/support src/services/support-ticket.service.ts</automated>
  </verify>
  <done>Users can submit a simple ticket from the Support popout.</done>
</task>

<task type="auto">
  <name>Task 3: Add authenticated Resend-backed ticket Edge Function</name>
  <files>supabase/functions/send-support-ticket/index.ts</files>
  <read_first>
    - supabase/functions/send-org-invite/index.ts
    - supabase/functions/_shared/auth.ts
    - supabase/functions/_shared/cors.ts
    - supabase/CLAUDE.md
  </read_first>
  <action>Create `send-support-ticket` Edge Function using `authenticateRequest(req, supabase, corsHeaders)`, Zod request validation, HTML escaping copied from the invite function pattern, and Resend API POST. Send to `support@callvaultai.com`. Do not cc Andrew by default. Limit message length to a bounded value such as 5000 characters and context string fields to bounded lengths. Return JSON `{ success: true }` on success and safe JSON errors on validation/auth/send failure.</action>
  <acceptance_criteria>
    - Edge Function imports and calls `authenticateRequest(req, supabase, corsHeaders)`.
    - Edge Function validates request body with Zod.
    - Email recipient is `support@callvaultai.com`.
    - No hardcoded Andrew cc address exists in the function.
    - Error responses do not include `RESEND_API_KEY` or raw provider response bodies.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "authenticateRequest|z\\.object|support@callvaultai.com|RESEND_API_KEY|cc" supabase/functions/send-support-ticket/index.ts && npm run build</automated>
  </verify>
  <done>Support tickets can be sent through an authenticated backend endpoint.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-02-01 | Information Disclosure | ticket context | mitigate | Attach only bounded basic context and never render raw IDs in UI. |
| T-06-02-02 | Spoofing | ticket endpoint | mitigate | Require Supabase auth via shared `authenticateRequest`. |
| T-06-02-03 | Injection | support email HTML | mitigate | Escape user-supplied message/context before HTML email rendering. |
| T-06-02-04 | Denial of Service | ticket endpoint | accept | Launch with auth and length bounds; no public unauthenticated form. |
</threat_model>

<verification>
Run `npm run test -- --run src/components/ui/__tests__/sidebar-nav.test.tsx`, source guards for `send-support-ticket`, and `npm run build`.
</verification>

<success_criteria>
- One Support popout replaces scattered sidebar help buttons.
- All five support actions are available.
- Ticket submission sends a bounded authenticated email to support.
</success_criteria>

<output>
Create `.planning/phases/06-launch-ux-support-rls-hygiene/06-02-SUMMARY.md` when done.
</output>
