---
phase: 15-support-capture-fix
plan: 02
type: execute
wave: 2
depends_on: [15-01]
external_gate: "DO NOT EXECUTE until Phase 11 plans 11-03 AND 11-04 are complete (shared surfaces: SupportTicketDialog, tickets service contract)"
files_modified:
  - src/lib/console-buffer.ts
  - src/lib/__tests__/console-buffer.test.ts
  - src/components/support/SupportTicketDialog.tsx
  - src/services/support-ticket.service.ts
  - src/services/__tests__/support-ticket.service.test.ts
autonomous: true
requirements: [CAP-01]
must_haves:
  truths:
    - "Submitting a support ticket auto-attaches a JSON console-log buffer of the last ~100 entries with error entries retained preferentially (D-03)"
    - "The console buffer is derived from the existing debug-panel infrastructure (useDebugPanel messages) — no second console interceptor exists (D-03)"
    - "A ticket submitted while the console is empty still submits cleanly (empty buffer → no console attachment or an empty-array JSON, never an error)"
  artifacts:
    - path: "src/lib/console-buffer.ts"
      provides: "pure deriveConsoleBuffer(messages) → trimmed, error-prioritized, chronologically ordered ≤100-entry array + serializer to Blob"
      exports: ["deriveConsoleBuffer"]
      min_lines: 40
    - path: "src/lib/__tests__/console-buffer.test.ts"
      provides: "unit coverage of cap, error priority, ordering, trimming"
  key_links:
    - from: "src/components/support/SupportTicketDialog.tsx"
      to: "src/components/debug-panel"
      via: "useDebugPanel().messages consumed at submit time"
      pattern: "useDebugPanel"
    - from: "src/services/support-ticket.service.ts"
      to: "ticket-attachments bucket"
      via: "console JSON uploaded as application/json object alongside the screenshot"
      pattern: "console_log"
---

<objective>
Deliver the console-buffer slice (D-03): a pure, unit-tested ring-buffer derivation over the globally-captured debug-panel messages, serialized to JSON and uploaded/attached on ticket submit through the attachment pipeline 15-01 built.

Purpose: CAP-01 — tickets carry the user's recent console state (errors first) so admins can diagnose without a reproduction session.
Output: console-buffer util + tests, dialog submit wiring, console_log attachment alongside the screenshot.
</objective>

## Phase Goal

**As a** CallVault user reporting a problem, **I want to** have my recent console errors automatically attached to my ticket, **so that** admins can diagnose the failure without asking me to open devtools.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-support-capture-fix/15-CONTEXT.md
@.planning/phases/15-support-capture-fix/15-RESEARCH.md (Pattern 2 — derivation spec; anti-pattern: never add a second console interceptor)
@src/CLAUDE.md
</context>

<sequencing_gate>
**EXECUTION GATE: Wave 2 — requires 15-01 complete (shares SupportTicketDialog.tsx and support-ticket.service.ts). Phase-level gate also applies: 11-03 AND 11-04 summaries must exist before any Phase 15 execution.**
</sequencing_gate>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deriveConsoleBuffer pure util with unit tests (D-03)</name>
  <files>src/lib/console-buffer.ts, src/lib/__tests__/console-buffer.test.ts</files>
  <read_first>src/components/debug-panel/types.ts (DebugMessage shape), src/components/debug-panel/index.ts (exported types), .planning/phases/15-support-capture-fix/15-RESEARCH.md (Pattern 2)</read_first>
  <behavior>
    - deriveConsoleBuffer(messages: DebugMessage[], cap = 100): given 150 mixed entries (30 errors), returns 100 entries containing all 30 errors plus the 70 most recent non-errors, sorted by timestamp ascending
    - Given 120 errors, returns the 100 most recent errors
    - Given fewer than 100 entries, returns all of them
    - Each output entry contains ONLY: timestamp, type, source, message (truncated to 500 chars), stack (truncated to 2000 chars, optional), httpStatus (optional), url (optional) — appStateSnapshot and other heavy fields stripped
    - serializeConsoleBuffer(entries) returns a Blob of type application/json whose parsed content round-trips the entries
  </behavior>
  <action>Create src/lib/console-buffer.ts importing the DebugMessage type from @/components/debug-panel (type-only import — AI-02 unaffected, this is logging not AI). Implement deriveConsoleBuffer per the behavior block: partition by type === 'error', keep errors first (most recent if over cap), fill remaining slots with most recent non-errors, final sort timestamp ascending, map to the trimmed ConsoleBufferEntry shape with the exact truncation limits above. Implement serializeConsoleBuffer producing new Blob([JSON.stringify({capturedAt, entries})], {type: 'application/json'}). Export both functions and the ConsoleBufferEntry type. Pure module: no React, no side effects, no console interception (the debug-panel provider already intercepts globally — RESEARCH anti-pattern). Write src/lib/__tests__/console-buffer.test.ts covering every behavior case.</action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/console-buffer.test.ts</automated>
  </verify>
  <done>Derivation is deterministic, capped, error-prioritized, chronologically ordered, and fully unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire console buffer into submit — upload as console_log attachment (D-03, D-04)</name>
  <files>src/components/support/SupportTicketDialog.tsx, src/services/support-ticket.service.ts, src/services/__tests__/support-ticket.service.test.ts</files>
  <read_first>src/components/support/SupportTicketDialog.tsx (post 15-01 state), src/services/support-ticket.service.ts (post 15-01 state: uploadTicketAttachment, AttachmentDescriptor), src/components/debug-panel/DebugPanelContext.tsx (useDebugPanel hook contract, lines ~250-290)</read_first>
  <action>In SupportTicketDialog.tsx, consume useDebugPanel() (provider is mounted above the dialog in App.tsx — verified in RESEARCH; in tests, mock @/components/debug-panel) and at handleSubmit call deriveConsoleBuffer(messages) then serializeConsoleBuffer; pass the resulting blob as a second optional attachment input to submitSupportTicket. Extend SubmitSupportTicketParams in support-ticket.service.ts with optional `consoleBuffer?: { blob: Blob }`; in submitSupportTicket upload it via the existing uploadTicketAttachment as type 'console_log' with `.json` extension and mime application/json, appending its descriptor to the attachments array (screenshot first, console second; either may be absent; empty-entries buffer is still uploaded — a ticket with zero console history is itself signal). Keep the 15-01 failure policy: a failed console upload never blocks submission. Extend src/services/__tests__/support-ticket.service.test.ts: attachments array carries both descriptors when both inputs present, console-only when screenshot removed, and invoke body omits attachments when neither present. Update the SupportTicketDialog test mock for @/components/debug-panel if the existing mock setup requires it.</action>
  <verify>
    <automated>npx vitest run src/services/__tests__/support-ticket.service.test.ts src/components/support/__tests__/SupportTicketDialog.test.tsx && npm test 2>&1 | tail -5 && npm run build</automated>
  </verify>
  <done>A submitted ticket's first message carries both a screenshot and a console_log attachment reference; full suite + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| console contents → Storage | console messages may embed response bodies (DebugMessage.responseBody exposes DB/RLS error text) and URLs with identifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Denial of Service | unbounded console JSON size | mitigate | hard cap 100 entries + per-field truncation (message 500 / stack 2000 chars) in deriveConsoleBuffer; bucket 5MB limit as backstop |
| T-15-06 | Information Disclosure | sensitive strings inside console entries | mitigate | trimmed field allowlist (responseBody and appStateSnapshot are STRIPPED — only timestamp/type/source/message/stack/httpStatus/url survive); object private with owner+ADMIN SELECT only (15-01 policies) |
| T-15-SC | Tampering | package installs | accept | No package installs this plan — slopcheck gate not applicable |
</threat_model>

<verification>
- Both vitest targets, full `npm test`, and `npm run build` exit 0
- Execution-time dev-browser check: trigger a console.error on a page (e.g. via a known failing action or devtools), submit a ticket, query ticket_messages.attachments — two descriptors present; download the console JSON via a signed URL and confirm the error entry is present and responseBody/appStateSnapshot fields are absent
</verification>

<success_criteria>
Phase success criterion 2 satisfied: the console-log buffer (~100 entries, errors prioritized, derived from the existing debug-panel infrastructure) is auto-attached as JSON to the submitted ticket (CAP-01, D-03).
</success_criteria>

<output>
Create `.planning/phases/15-support-capture-fix/15-02-SUMMARY.md` when done
</output>
