---
phase: 06.1-mcp-subdomain-routing
plan: worker-routing
type: execute
wave: 4
depends_on:
  - 06.1-sec-worker-bypass
  - 06.1-sec-jwt-fix
  - 06.1-sec-dcr-phishing
  - 06.1-sec-slug-tombstone
  - 06.1-sec-revocation-complete
  - 06.1-sec-workspace-param
  - 06.1-sec-worker-headers
  - 06.1-slug-schema
files_modified:
  - cloudflare/mcp-subdomain-worker/worker.ts
  - cloudflare/mcp-subdomain-worker/wrangler.toml
  - supabase/functions/mcp-server/auth.ts
  - supabase/functions/mcp-server/protocol.ts
  - supabase/functions/mcp-oauth-metadata/index.ts
autonomous: false
requirements:
  - SUBDOMAIN-ROUTING-4
  - SUBDOMAIN-ROUTING-5
  - SUBDOMAIN-ROUTING-6
  - SUBDOMAIN-ROUTING-7
user_setup: []

must_haves:
  truths:
    - "orgslug.callvaultai.com/mcp routes a valid org-scoped token to the correct org's vault"
    - "Token for Org A presented at Org B's subdomain returns 403 with body {\"error\":\"token_org_mismatch\"}"
    - "mcp.callvaultai.com remains fully functional (Deprecation: true header added by sec-worker-headers plan)"
    - "Wildcard DNS A record exists for *.callvaultai.com pointing to Cloudflare proxy"
    - "protocol.ts and mcp-oauth-metadata/index.ts accept *.callvaultai.com subdomains as valid hosts"
    - "Inbound x-callvault-* headers from external clients are stripped before Worker re-injects them"
  artifacts:
    - path: "cloudflare/mcp-subdomain-worker/worker.ts"
      provides: "Subdomain routing Worker — slug extraction, header injection, forwarding"
      exports: ["default fetch handler"]
    - path: "cloudflare/mcp-subdomain-worker/wrangler.toml"
      provides: "Worker config with *.callvaultai.com/mcp wildcard route"
      contains: "*.callvaultai.com/mcp"
    - path: "supabase/functions/mcp-server/auth.ts"
      provides: "Slug→org_id resolution + token_org_mismatch enforcement"
      contains: "token_org_mismatch"
    - path: "supabase/functions/mcp-server/protocol.ts"
      provides: "SUBDOMAIN_PATTERN regex for *.callvaultai.com host acceptance"
      contains: "SUBDOMAIN_PATTERN"
    - path: "supabase/functions/mcp-oauth-metadata/index.ts"
      provides: "Duplicate ALLOWED_HOSTS fix — accepts subdomain hosts"
      contains: "SUBDOMAIN_PATTERN"
  key_links:
    - from: "cloudflare/mcp-subdomain-worker/worker.ts"
      to: "supabase/functions/mcp-server (via Supabase URL)"
      via: "x-callvault-internal-secret + x-callvault-org-slug headers"
      pattern: "x-callvault-internal-secret"
    - from: "supabase/functions/mcp-server/auth.ts"
      to: "organizations table"
      via: "serviceRoleClient.from('organizations').select('id').eq('slug', orgSlugHeader)"
      pattern: "token_org_mismatch"
    - from: "supabase/functions/mcp-server/protocol.ts"
      to: "resolveOriginHost()"
      via: "SUBDOMAIN_PATTERN regex replaces static ALLOWED_HOSTS set"
      pattern: "SUBDOMAIN_PATTERN"
---

<objective>
Deploy the new Cloudflare Worker that routes *.callvaultai.com/mcp subdomain
requests to the correct org/workspace in the MCP Edge Functions, extend auth.ts
with slug→org_id resolution and token_org_mismatch enforcement, fix ALLOWED_HOSTS
in both protocol.ts and mcp-oauth-metadata/index.ts, and provision wildcard DNS.

NOTE: The Deprecation header for legacy MCP routes is handled by sec-worker-headers
(06.1-06-PLAN.md) which already modifies cloudflare/api-proxy/worker.ts. This plan
does NOT touch api-proxy/worker.ts.

This plan is gated by ALL 7 Critical/High security gates (Wave 1) plus
slug-schema (Wave 3). DNS provisioning is the last automated step — it is blocked
until a human verifies those gates are green.

Purpose: Enable per-org subdomain MCP URLs so multi-org operators can hold
simultaneous MCP connections to distinct orgs from a single Claude instance.

Output:
- cloudflare/mcp-subdomain-worker/ directory with worker.ts + wrangler.toml
- Slug resolution + mismatch enforcement in auth.ts
- Subdomain host acceptance in protocol.ts and mcp-oauth-metadata/index.ts
- Wildcard DNS A record *.callvaultai.com → Cloudflare proxy (192.0.2.1, proxied=true)
- Worker deployed to Cloudflare via wrangler deploy
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-mcp-subdomain-routing/06.1-CONTEXT.md
@.planning/phases/06.1-mcp-subdomain-routing/06.1-RESEARCH.md
@.planning/phases/06.1-mcp-subdomain-routing/06.1-PATTERNS.md
</context>

<interfaces>
From cloudflare/api-proxy/worker.ts (exact analog for new Worker):

  SUPABASE_BASE = "https://vltmrnjsubfzrgrtdqey.supabase.co"
  STRIP_REQUEST_HEADERS = ["cf-connecting-ip","cf-ipcountry","cf-ray","cf-visitor","cf-worker"]
  STRIP_RESPONSE_HEADERS = ["x-sb-edge-region","x-sb-gateway-version"]

  export default {
    async fetch(request: Request): Promise<Response>   // no env param in api-proxy
  }

  // New Worker must add env param:
  interface Env { CALLVAULT_INTERNAL_SECRET: string }
  export default { async fetch(request: Request, env: Env): Promise<Response> }

From cloudflare/api-proxy/wrangler.toml:

  account_id = "e9319ad5ca87e4768e7a79f1339ec8c8"
  compatibility_date = "2026-05-07"
  // api-proxy uses custom_domain = true; new Worker uses [[routes]] with wildcard pattern

From supabase/functions/mcp-server/auth.ts (current state — 287 lines):

  // readClientIdFromJwt() lives at lines 263-276; base64UrlDecode at 278-286
  // clientId = readClientIdFromJwt(rawToken) at line 103
  // jwtUser available at line 94 from authClient.auth.getUser(rawToken)
  // serviceRoleClient is a parameter — already available in authenticateMcpRequest()
  // mcpToken.org_id is the resolved org after grant selection (line ~170+)
  // forbiddenResponse() is imported from protocol.ts and available in scope
  // NOTE: sec-jwt-fix plan (Wave 1) removes readClientIdFromJwt() — this plan runs AFTER that

From supabase/functions/mcp-server/protocol.ts (current state — 131 lines):

  // Lines 78-87: ALLOWED_HOSTS static set — must be replaced
  const ALLOWED_HOSTS = new Set(['api.callvaultai.com', 'mcp.callvaultai.com']);
  const FALLBACK_HOST = 'api.callvaultai.com';
  export function resolveOriginHost(req: Request): string
  // Replace with SUBDOMAIN_PATTERN regex approach (see Task 2 action)

From supabase/functions/mcp-oauth-metadata/index.ts (current state):

  // Lines 36-48: IDENTICAL duplicate of the ALLOWED_HOSTS pattern above
  const ALLOWED_HOSTS = new Set(['api.callvaultai.com', 'mcp.callvaultai.com']);
  const FALLBACK_HOST = 'api.callvaultai.com';
  function resolveOriginHost(req: Request): string   // local function, not exported
  // Same replacement required as in protocol.ts
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create new Cloudflare Worker (mcp-subdomain-worker)</name>
  <read_first>
    cloudflare/api-proxy/worker.ts
    cloudflare/api-proxy/wrangler.toml
    .planning/phases/06.1-mcp-subdomain-routing/06.1-PATTERNS.md (Pattern 5, STRIP constants)
  </read_first>
  <files>
    cloudflare/mcp-subdomain-worker/worker.ts,
    cloudflare/mcp-subdomain-worker/wrangler.toml
  </files>
  <behavior>
    - SLUG_REGEX = /^[a-z0-9]+$/ rejects any subdomain segment containing non-alphanumeric characters with 404 {"error":"not_found"}
    - orgslug.callvaultai.com parses to orgSlug=orgslug, wsSlug=null
    - orgslug-wsslug.callvaultai.com parses to orgSlug=orgslug, wsSlug=wsslug (split on FIRST hyphen — slugs cannot contain hyphens per D-07)
    - Inbound x-callvault-org-slug, x-callvault-workspace-slug, x-callvault-internal-secret, x-callvault-scope are stripped from the forwarded request before re-injection (ISC-5 anti-injection)
    - /mcp and /mcp/* forward to ${SUPABASE_BASE}/functions/v1/mcp-server
    - /mcp-register forwards to ${SUPABASE_BASE}/functions/v1/mcp-oauth-register
    - /.well-known/oauth-protected-resource forwards to ${SUPABASE_BASE}/functions/v1/mcp-oauth-metadata?doc=protected-resource&resource_path=%2Fmcp
    - /.well-known/oauth-authorization-server forwards to ${SUPABASE_BASE}/functions/v1/mcp-oauth-metadata?doc=authorization-server
    - /.well-known/openid-configuration forwards to ${SUPABASE_BASE}/functions/v1/mcp-oauth-metadata?doc=openid-configuration
    - All other paths return 404 {"error":"not_found"}
    - Upstream fetch failure returns 502 with JSON-RPC error envelope for /mcp paths
    - x-callvault-internal-secret header value is env.CALLVAULT_INTERNAL_SECRET
    - x-callvault-scope header is "workspace" when wsSlug is present, "org" otherwise
    - CF-Connecting-IP → X-Forwarded-For chain (same pattern as api-proxy)
  </behavior>
  <action>
    Create directory cloudflare/mcp-subdomain-worker/ and write two files.

    worker.ts: Declare interface Env with CALLVAULT_INTERNAL_SECRET: string. Declare SUPABASE_BASE as the Supabase project URL ("https://vltmrnjsubfzrgrtdqey.supabase.co"). Declare SLUG_REGEX = /^[a-z0-9]+$/. Declare STRIP_REQUEST_HEADERS array (same CF headers as api-proxy). Declare STRIP_RESPONSE_HEADERS array (same Supabase internal headers as api-proxy). Declare STRIP_INBOUND_CALLVAULT_HEADERS = ["x-callvault-org-slug", "x-callvault-workspace-slug", "x-callvault-internal-secret", "x-callvault-scope"] — these are stripped from the inbound request before the Worker re-injects authoritative values, preventing header injection attacks (ISC-5).

    In the fetch handler: extract hostname from new URL(request.url).hostname. Extract subdomain = hostname.split('.')[0]. Find first hyphen in subdomain with indexOf('-'). If hyphenIdx !== -1: orgSlug = subdomain.slice(0, hyphenIdx), wsSlug = subdomain.slice(hyphenIdx + 1). Else: orgSlug = subdomain, wsSlug = null. If SLUG_REGEX fails on orgSlug or wsSlug, return 404 {"error":"not_found"}.

    Call a resolveRoute(url) function that implements the path-to-target mapping above and returns {target: string, isJsonRpc: boolean} or null for 404.

    Build forwardHeaders from new Headers(request.headers). Apply X-Forwarded-For from CF-Connecting-IP. Delete all STRIP_REQUEST_HEADERS. Delete all STRIP_INBOUND_CALLVAULT_HEADERS (strip inbound before re-injecting). Set: x-forwarded-host = url.hostname, x-forwarded-proto = url.protocol without colon, x-callvault-host = hostname, x-callvault-org-slug = orgSlug, x-callvault-scope = wsSlug ? "workspace" : "org", x-callvault-internal-secret = env.CALLVAULT_INTERNAL_SECRET. If wsSlug is non-null: set x-callvault-workspace-slug = wsSlug. Set host = new URL(route.target).host.

    Forward via fetch(new Request(route.target, {method, headers: forwardHeaders, body: request.body, redirect: "manual"})). Wrap in try/catch — on error, return 502 with JSON-RPC envelope {"jsonrpc":"2.0","error":{"code":-32603,"message":"Upstream proxy error"},"id":null} for isJsonRpc routes, plain {"error":"Upstream proxy error"} otherwise. Strip STRIP_RESPONSE_HEADERS from upstream response headers. Return upstream body with stripped headers.

    wrangler.toml: Set name = "callvault-mcp-subdomain". Set main = "worker.ts". Set compatibility_date = "2026-06-09". Set account_id = "e9319ad5ca87e4768e7a79f1339ec8c8" (same as api-proxy). Add [[routes]] block with pattern = "*.callvaultai.com/mcp" and zone_name = "callvaultai.com". Add [vars] section with comment: CALLVAULT_INTERNAL_SECRET is set via wrangler secret put. Add [observability] block with enabled = true.

    Note: Do NOT add more wildcard route patterns (e.g., *.callvaultai.com/mcp-register or *.callvaultai.com/.well-known) — the single /mcp pattern is the registered route; all three path types (/mcp, /mcp-register, /.well-known) arrive at the Worker because Cloudflare routes match on prefix when no trailing-wildcard restriction is present.

    Run: cd /Users/admin/dev/brain && npm test to verify no regressions.
  </action>
  <verify>
    <automated>cd /Users/admin/dev/brain && ls cloudflare/mcp-subdomain-worker/worker.ts cloudflare/mcp-subdomain-worker/wrangler.toml && grep -c "STRIP_INBOUND_CALLVAULT_HEADERS" cloudflare/mcp-subdomain-worker/worker.ts && grep -c "x-callvault-internal-secret" cloudflare/mcp-subdomain-worker/worker.ts && grep -c "callvault-mcp-subdomain" cloudflare/mcp-subdomain-worker/wrangler.toml && grep -c "callvaultai.com/mcp" cloudflare/mcp-subdomain-worker/wrangler.toml && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>Both files exist; STRIP_INBOUND_CALLVAULT_HEADERS constant present in worker.ts; x-callvault-internal-secret injection present; wrangler.toml has correct name and wildcard route; npm test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend auth.ts (slug resolution) and fix ALLOWED_HOSTS in protocol.ts + mcp-oauth-metadata</name>
  <read_first>
    supabase/functions/mcp-server/auth.ts
    supabase/functions/mcp-server/protocol.ts
    supabase/functions/mcp-oauth-metadata/index.ts
    .planning/phases/06.1-mcp-subdomain-routing/06.1-PATTERNS.md (Pattern 6, Pattern 7, Shared Patterns)
  </read_first>
  <files>
    supabase/functions/mcp-server/auth.ts,
    supabase/functions/mcp-server/protocol.ts,
    supabase/functions/mcp-oauth-metadata/index.ts
  </files>
  <behavior>
    auth.ts — slug resolution:
    - When x-callvault-org-slug header is absent (legacy path), auth continues unchanged with no 404
    - When x-callvault-org-slug header is present and resolves to an org row: if token.org_id !== orgRow.id, return 403 {"error":"token_org_mismatch"} with Content-Type application/json
    - When x-callvault-org-slug header is present and no org row found: return 404 {"error":"org_not_found"}
    - When x-callvault-workspace-slug header is present and workspace lookup fails: return 403 {"error":"token_workspace_mismatch"}
    - Slug resolution uses serviceRoleClient (bypasses RLS) with .from('organizations').select('id').eq('slug', orgSlugHeader).maybeSingle()
    - Slug check is performed AFTER normal token auth and grant selection succeed (not before)

    protocol.ts — ALLOWED_HOSTS extension:
    - SUBDOMAIN_PATTERN = /^[a-z0-9]+(-[a-z0-9]+)?\.callvaultai\.com$/ accepts both org-only and org+workspace subdomain forms
    - ALLOWED_STATIC_HOSTS retains api.callvaultai.com and mcp.callvaultai.com as explicit allowlist entries
    - resolveOriginHost() returns the fwd value when SUBDOMAIN_PATTERN.test(fwd) is true
    - resolveOriginHost() still falls back to FALLBACK_HOST = 'api.callvaultai.com' for any unrecognized host

    mcp-oauth-metadata/index.ts — same ALLOWED_HOSTS extension:
    - Identical fix applied to the local resolveOriginHost() function in that file
    - SUBDOMAIN_PATTERN and ALLOWED_STATIC_HOSTS replace the existing ALLOWED_HOSTS set
  </behavior>
  <action>
    File 1 — supabase/functions/mcp-server/auth.ts:

    Read the current state of auth.ts after Wave 1 plans have run (sec-jwt-fix will have removed readClientIdFromJwt). This plan ONLY adds the slug resolution block.

    Add slug resolution AFTER the final return {ok: true, ...} for both the manual token path and the OAuth path — specifically, insert the slug check block immediately before each "return { ok: true, mcpToken: ... }" statement. The check is: read x-callvault-org-slug from req.headers. If absent, skip (legacy path). If present: query serviceRoleClient.from('organizations').select('id').eq('slug', orgSlugHeader).maybeSingle(). If orgError || !orgRow: return { ok: false, response: new Response(JSON.stringify({ error: 'org_not_found' }), { status: 404, headers: { 'Content-Type': 'application/json' } }) }. Read the resolved mcpToken's org_id (for the manual token path it is tokenRow.org_id cast to mcpToken; for the OAuth path it is grant.org_id). If mcpToken.org_id !== orgRow.id: return { ok: false, response: new Response(JSON.stringify({ error: 'token_org_mismatch' }), { status: 403, headers: { 'Content-Type': 'application/json' } }) }.

    Workspace slug check: read x-callvault-workspace-slug from req.headers. If present: query serviceRoleClient.from('workspaces').select('id').eq('slug', wsSlugHeader).eq('organization_id', orgRow.id).maybeSingle(). If workspaceError || !wsRow: return { ok: false, response: new Response(JSON.stringify({ error: 'token_workspace_mismatch' }), { status: 403, headers: { 'Content-Type': 'application/json' } }) }.

    The slug check applies to BOTH the isManualToken branch (before its return {ok:true}) and the OAuth branch (before its return {ok:true}). The mcpToken variable is in scope in both branches at the point of insertion.

    File 2 — supabase/functions/mcp-server/protocol.ts:

    Replace lines 78-87 (ALLOWED_HOSTS static set + resolveOriginHost function) with: declare SUBDOMAIN_PATTERN = /^[a-z0-9]+(-[a-z0-9]+)?\.callvaultai\.com$/. Rename ALLOWED_HOSTS to ALLOWED_STATIC_HOSTS (keep same two values). Keep FALLBACK_HOST = 'api.callvaultai.com'. Rewrite resolveOriginHost to: extract fwd from x-callvault-host or x-forwarded-host header; if no fwd return FALLBACK_HOST; if ALLOWED_STATIC_HOSTS.has(fwd) return fwd; if SUBDOMAIN_PATTERN.test(fwd) return fwd; return FALLBACK_HOST.

    File 3 — supabase/functions/mcp-oauth-metadata/index.ts:

    Apply the identical SUBDOMAIN_PATTERN replacement to lines 36-48 (local resolveOriginHost function). The replacement is: add SUBDOMAIN_PATTERN = /^[a-z0-9]+(-[a-z0-9]+)?\.callvaultai\.com$/ constant. Rename ALLOWED_HOSTS to ALLOWED_STATIC_HOSTS. Rewrite the local resolveOriginHost with the same three-branch pattern (ALLOWED_STATIC_HOSTS.has check, then SUBDOMAIN_PATTERN.test check, then FALLBACK_HOST).

    Run: cd /Users/admin/dev/brain && npm test to verify no regressions.
  </action>
  <verify>
    <automated>cd /Users/admin/dev/brain && grep -v "^//" supabase/functions/mcp-server/auth.ts | grep -c "token_org_mismatch" && grep -v "^//" supabase/functions/mcp-server/protocol.ts | grep -c "SUBDOMAIN_PATTERN" && grep -v "^//" supabase/functions/mcp-oauth-metadata/index.ts | grep -c "SUBDOMAIN_PATTERN" && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>token_org_mismatch appears in auth.ts (non-comment); SUBDOMAIN_PATTERN appears in protocol.ts and mcp-oauth-metadata/index.ts; npm test passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Security gate checkpoint — verify all 7 Critical/High gates are green before DNS</name>
  <what-built>
    Tasks 1-2 complete. The new Worker files exist, auth.ts has slug resolution, protocol.ts and
    mcp-oauth-metadata have SUBDOMAIN_PATTERN. This checkpoint verifies the PRIOR Wave 1 security
    plans are ALL green before the automated DNS provisioning step. The Deprecation header for
    legacy mcp.callvaultai.com was added by sec-worker-headers (06.1-06-PLAN.md) — verify it here.
  </what-built>
  <how-to-verify>
    Run each of these verification commands and confirm all pass:

    1. sec-worker-bypass gate:
       curl -s -o /dev/null -w "%{http_code}" -X POST https://vltmrnjsubfzrgrtdqey.supabase.co/functions/v1/mcp-server
       Expected: 403

    2. sec-jwt-fix gate:
       grep -c "readClientIdFromJwt" supabase/functions/mcp-server/auth.ts
       Expected: 0

    3. sec-dcr-phishing gate:
       grep -c "token_org_mismatch\|First-time connection\|redirectDomain" supabase/functions/mcp-server/auth.ts src/pages/OAuthConsentPage.tsx
       Expected: at least 1 match per file

    4. sec-slug-tombstone gate:
       supabase db push --use-api 2>&1 | grep -c "No new migrations\|Migrations applied"
       Expected: at least 1 (confirms tombstone migration applied)

    5. sec-revocation-complete gate:
       grep -c "mcp_oauth_client_grants" supabase/migrations/*_revocation_triggers.sql
       Expected: at least 1

    6. sec-workspace-param gate:
       grep -c "selectedOrgId" src/pages/OAuthConsentPage.tsx
       Expected: at least 1

    7. sec-worker-headers gate (XFF + Deprecation header in api-proxy):
       grep -c "x-callvault-internal-secret\|STRIP_REQUEST_HEADERS" cloudflare/api-proxy/worker.ts
       Expected: at least 2

    8. Deprecation header gate (added by sec-worker-headers):
       curl -s -I "https://mcp.callvaultai.com/mcp" 2>/dev/null | grep -i "deprecation"
       Expected: line containing "Deprecation: true"

    Also confirm: CALLVAULT_INTERNAL_SECRET has been generated and set in both environments.
    Run: wrangler secret list --config cloudflare/mcp-subdomain-worker/wrangler.toml
    Expected: CALLVAULT_INTERNAL_SECRET appears in the output.
    If not set, generate and set it now:
      SECRET=$(openssl rand -hex 32)
      echo "$SECRET" | wrangler secret put CALLVAULT_INTERNAL_SECRET --config cloudflare/mcp-subdomain-worker/wrangler.toml
      supabase secrets set CALLVAULT_INTERNAL_SECRET="$SECRET" --project-ref vltmrnjsubfzrgrtdqey
    (Both commands MUST use the SAME value — generate once, pipe to both.)
  </how-to-verify>
  <resume-signal>
    Type "gates green" when all 8 checks pass and CALLVAULT_INTERNAL_SECRET is confirmed set in both
    the Worker secrets and Supabase project secrets. If any gate is red, fix it before resuming.
  </resume-signal>
  <files></files>
  <action>No automated action. Run the verification commands in how-to-verify and confirm all 8 gates are green before Task 4 proceeds.</action>
  <verify>
    <human-check>All 8 security gate commands return expected values as listed in how-to-verify above; CALLVAULT_INTERNAL_SECRET is set in both cloudflare/mcp-subdomain-worker Worker secrets and Supabase project secrets; Deprecation: true header confirmed on mcp.callvaultai.com/mcp.</human-check>
  </verify>
  <done>All 8 Critical/High security gates verified green; CALLVAULT_INTERNAL_SECRET set in both environments; resume signal "gates green" received.</done>
</task>

<task type="auto">
  <name>Task 4: Deploy Worker and provision wildcard DNS</name>
  <read_first>
    cloudflare/mcp-subdomain-worker/wrangler.toml
    cloudflare/mcp-subdomain-worker/worker.ts
  </read_first>
  <files>
    cloudflare/mcp-subdomain-worker/wrangler.toml
  </files>
  <action>
    All 8 security gates are verified green (Task 3 checkpoint passed). Execute in order:

    Step 1 — Deploy the Worker:
    Run: cd /Users/admin/dev/brain && wrangler deploy --config cloudflare/mcp-subdomain-worker/wrangler.toml
    Verify exit code is 0. If it fails with a TypeScript compilation error, fix the type error in worker.ts and retry. If it fails with a secrets error, the CALLVAULT_INTERNAL_SECRET was not set — return to Task 3 checkpoint.

    Step 2 — Create wildcard DNS A record:
    Run the Cloudflare API call using CLOUDFLARE_API_TOKEN and CLOUDFLARE_ZONE_ID from the environment. The exact curl command is:
    curl -s -X POST "https://api.cloudflare.com/client/v4/zones/${CLOUDFLARE_ZONE_ID}/dns_records" -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" -H "Content-Type: application/json" -d '{"type":"A","name":"*","content":"192.0.2.1","proxied":true,"ttl":1}'
    The content IP 192.0.2.1 is a TEST-NET address that Cloudflare never routes — all proxied traffic is intercepted by the Worker before reaching any origin. This is the correct value per D-17.
    Capture the response. If the response contains "\"success\":true", the DNS record was created. If it contains "already exists" in the errors array, the record was already present — this is also acceptable.

    Step 3 — Verify deployment:
    Run: curl -s -o /dev/null -w "%{http_code}" "https://mcp.callvaultai.com/mcp"
    Expected: 200 (legacy path still works).
    Run: curl -s "https://api.cloudflare.com/client/v4/zones/${CLOUDFLARE_ZONE_ID}/dns_records?name=*" -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" | grep -c '"name":"\*"'
    Expected: 1 (confirms wildcard record exists).

    Note: End-to-end testing of orgslug.callvaultai.com/mcp requires an org with a slug in the database (from the slug-schema migration). If the slug-schema plan has been applied, the verify command below can use a real org slug. If not yet available, the DNS and Worker deployment can still be confirmed via the mcp.callvaultai.com legacy path.
  </action>
  <verify>
    <automated>
      cd /Users/admin/dev/brain &&
      wrangler deploy --config cloudflare/mcp-subdomain-worker/wrangler.toml --dry-run 2>&1 | grep -c "Total Upload" &&
      curl -s "https://api.cloudflare.com/client/v4/zones/${CLOUDFLARE_ZONE_ID}/dns_records?name=*&type=A" -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" 2>/dev/null | grep -c '"name":"\*"'
    </automated>
  </verify>
  <done>
    wrangler deploy exits 0; mcp.callvaultai.com/mcp returns 200;
    Cloudflare DNS records API shows wildcard A record with name="*" for the zone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Cloudflare Worker | External MCP clients (Claude Desktop, Cursor, etc.) hit the Worker; all x-callvault-* headers from this side must be stripped before re-injection |
| Cloudflare Worker → Supabase Edge Functions | Internal Cloudflare-to-Supabase channel; x-callvault-internal-secret authenticates this hop |
| Edge Function → PostgreSQL | Service-role client queries organizations.slug; bypasses RLS for slug resolution only |
| Browser → OAuthConsentPage | Legacy OAuth flow; workspace_id param validated against selectedOrgId already in scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-wr-01 | Spoofing | cloudflare/mcp-subdomain-worker/worker.ts | mitigate | Inbound x-callvault-org-slug, x-callvault-workspace-slug, x-callvault-internal-secret, x-callvault-scope stripped from client request via STRIP_INBOUND_CALLVAULT_HEADERS constant before Worker re-injects authoritative values (ISC-5) |
| T-wr-02 | Tampering | supabase/functions/mcp-server/auth.ts | mitigate | Slug→org_id resolution via serviceRoleClient DB lookup on organizations.slug; token.org_id compared to DB-resolved org_id — 403 token_org_mismatch on mismatch (ISC-84) |
| T-wr-03 | Elevation of Privilege | supabase/functions/mcp-server/auth.ts | mitigate | All requests to Supabase functions must carry x-callvault-internal-secret matching CALLVAULT_INTERNAL_SECRET env var (sec-worker-bypass gate, ISC-1-7) — this plan depends on that gate being green |
| T-wr-04 | Spoofing | supabase/functions/mcp-server/protocol.ts | mitigate | SUBDOMAIN_PATTERN = /^[a-z0-9]+(-[a-z0-9]+)?\.callvaultai\.com$/ allows only well-formed CallVault subdomains; arbitrary hosts fall back to FALLBACK_HOST and never generate cross-origin OAuth metadata |
| T-wr-05 | Information Disclosure | cloudflare/mcp-subdomain-worker/worker.ts | mitigate | SLUG_REGEX = /^[a-z0-9]+$/ validated before any DB lookup; malformed subdomain returns 404 {"error":"not_found"} without revealing whether slug exists (ISC-44-47 uniform 404) |
| T-wr-06 | Tampering | Cloudflare DNS wildcard A record | accept | Wildcard record content is 192.0.2.1 (TEST-NET, unreachable); Cloudflare intercepts all proxied traffic before routing — the IP is a placeholder, not a real origin. Low risk. |
| T-wr-07 | Elevation of Privilege | mcp-subdomain-worker route vs api-proxy custom_domain | accept | Cloudflare custom_domain routes (api.callvaultai.com, mcp.callvaultai.com) take priority over wildcard pattern routes per Cloudflare routing rules — existing traffic is not intercepted by new Worker [ASSUMED: Cloudflare routing priority; monitor api.callvaultai.com/mcp after deploy] |
| T-wr-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — all Worker and Edge Function code uses existing runtime APIs. No slopcheck required. |
</threat_model>

<verification>
Full phase verification after this plan completes:

1. Legacy path still works (Deprecation header added by sec-worker-headers):
   curl -s -I https://mcp.callvaultai.com/mcp | grep -i "deprecation"
   Expected: Deprecation: true (from sec-worker-headers plan)

2. Org mismatch returns correct 403:
   Present a valid org-A token against org-B subdomain.
   Expected: HTTP 403, body {"error":"token_org_mismatch"}

3. SUBDOMAIN_PATTERN present in both files:
   grep -c "SUBDOMAIN_PATTERN" supabase/functions/mcp-server/protocol.ts supabase/functions/mcp-oauth-metadata/index.ts
   Expected: at least 1 match in each file

4. Wildcard DNS record exists:
   curl -s "https://api.cloudflare.com/client/v4/zones/${CLOUDFLARE_ZONE_ID}/dns_records?name=*&type=A" \
     -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" | grep -c '"name":"\*"'
   Expected: 1

5. Inbound header stripping present in Worker:
   grep -c "STRIP_INBOUND_CALLVAULT_HEADERS" cloudflare/mcp-subdomain-worker/worker.ts
   Expected: at least 2 (declaration + usage)

6. Worker deployed:
   wrangler deployments list --config cloudflare/mcp-subdomain-worker/wrangler.toml | head -3
   Expected: deployment entry visible
</verification>

<success_criteria>
- wrangler deploy exits 0 for cloudflare/mcp-subdomain-worker/wrangler.toml
- Valid org-A token against orga.callvaultai.com/mcp returns non-403 (routed correctly)
- Same token against orgb.callvaultai.com/mcp returns 403 {"error":"token_org_mismatch"}
- grep -c 'SUBDOMAIN_PATTERN' supabase/functions/mcp-server/protocol.ts returns ≥1
- grep -c 'SUBDOMAIN_PATTERN' supabase/functions/mcp-oauth-metadata/index.ts returns ≥1
- grep -c 'token_org_mismatch' supabase/functions/mcp-server/auth.ts returns ≥1
- Cloudflare DNS API shows wildcard A record with name="*" and proxied=true for the zone
- npm test passes (no regressions from Worker or Edge Function file edits)
- api-proxy/worker.ts is NOT modified by this plan (Deprecation header responsibility belongs to sec-worker-headers)
</success_criteria>

<output>
Create `.planning/phases/06.1-mcp-subdomain-routing/06.1-worker-routing-SUMMARY.md` when done.
</output>
