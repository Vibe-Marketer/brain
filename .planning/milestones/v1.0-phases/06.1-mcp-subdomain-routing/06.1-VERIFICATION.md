---
phase: "06.1"
slug: mcp-subdomain-routing
status: human_needed
verified: 2026-06-11
verifier: Retroactive bookkeeping audit (01-09 archive audit follow-up) — static probes + recorded live-smoke artifacts
---

# Phase 06.1 — Retroactive Verification

> Phase executed 2026-06-08/10 (14/14 numbered plans + 8 security-gate plans, all SUMMARYs present). No formal phase-level verification record existed; this record was created retroactively on 2026-06-11. It combines today's disk probes with the live production smoke recorded at execution time in `06.1-12-SUMMARY.md`. No new deploys or live calls were made for this record.

## Success Criteria (from ROADMAP, 8 criteria)

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | All 7 Critical/High security gates verified green before wildcard DNS (`sec-worker-bypass`, `sec-jwt-fix`, `sec-dcr-phishing`, `sec-slug-tombstone`, `sec-revocation-complete`, `sec-workspace-param`, `sec-worker-headers`) | **passed** (with one recorded deferral) | All 7 gate SUMMARYs on disk (plus bonus `sec-enumeration` with live Cloudflare rule `d5e2a9fb...` enabled). Each records green verification EXCEPT `sec-dcr-phishing` Task 2 (Cloudflare DCR rate limit, ISC-17): **deferred by operator decision**, tracked in post-launch hardening backlog (also recorded in STATE.md decisions). Task 1 of that gate is complete and verified. |
| 2 | `readClientIdFromJwt()` removed from `auth.ts`; `client_id` only from verified user object | **passed** | Probe today: `grep -c readClientIdFromJwt supabase/functions/mcp-server/auth.ts` → **0**. sec-jwt-fix SUMMARY records GREEN at commit `c307a2c` (client_id from verified `jwtUser.app_metadata`). |
| 3 | Revocation DB triggers cover BOTH `mcp_tokens` AND `mcp_oauth_client_grants` for workspace/org member removal; revoked grant rejects next call with 403 | **passed** (migration + test-level) | Probe today: `20260609000001_revocation_triggers.sql` on disk; updates both tables for workspace AND org removal (ISC-32). Edge-Function path via `remove-org-member` + `auth.admin.signOut` recorded (ISC-31, STATE decisions). Live post-revocation 403 round-trip not re-proven — in human-needed list. |
| 4 | `orgslug.callvaultai.com/mcp` routes org token to correct org; wrong org → 403 `token_org_mismatch` | **passed** (recorded live proof) | 06.1-12 prod smoke: Worker `callvault-mcp-subdomain` v`53bca009` deployed to `*.callvaultai.com/*`; temp `aisimple` org token → **200** initialize at `https://aisimple.callvaultai.com/mcp`; same token at `https://personal.callvaultai.com/mcp` → **403 {"error":"token_org_mismatch"}**; smoke tokens cleaned up (0 remaining). |
| 5 | `orgslug-wsslug.callvaultai.com/mcp` routes workspace token correctly; wrong-workspace token → 403 | **human_needed** (code passed) | Worker routing for org-workspace subdomains implemented and org-token narrowing covered by `auth-slug-resolution.test.ts` (15 tests, 06.1-13; green in today's full suite). But the recorded live smoke only exercised **org** subdomains — no live workspace-subdomain 200/403 proof exists. |
| 6 | Multi-org operator holds simultaneous MCP connections to two orgs from one Claude instance via two subdomain URLs | **human_needed** | Never demonstrated. Requires a human with two orgs connected in one Claude instance. |
| 7 | `mcp.callvaultai.com` stays fully functional for existing tokens; backward-compat URLs return 200 + `Deprecation: true` header | **passed** (recorded live proof) | 06.1-12 prod smoke: `https://mcp.callvaultai.com/mcp` returned 401-with-`Deprecation: true` + correct `resource_metadata` for unauthenticated calls; legacy REST passthrough verified (`/v1/workspaces` → 401 MISSING_TOKEN, proving routing reaches the function). Authenticated legacy-token 200 covered by the criterion-4 smoke pattern at the legacy host during 03/06.1 work. |
| 8 | OAuth consent shows `client_id` (UUID), first-time-connection warning badge, redirect domain, advisory — never `client_name` alone | **passed** (test-level) | Probe today: `OAuthConsentPage.tsx` contains ISC-13 truncated client_id display and ISC-14 first-time detection querying `mcp_oauth_client_grants` (fail-safe default). `OAuthConsentPage.test.tsx` green in today's full suite. Visual confirmation not recorded. |

## Human-needed items

1. Live workspace-subdomain smoke: valid workspace token → 200 at `orgslug-wsslug.callvaultai.com/mcp`; wrong-workspace token → 403 (criterion 5).
2. Two-org simultaneous Claude connection demonstration (criterion 6).
3. Live post-revocation round-trip: remove a member, confirm next MCP call with their previously-valid token/grant returns 403 (criterion 3, live half).
4. Visual check of consent page first-time badge + client_id rendering in a real OAuth flow (criterion 8, visual half).
5. ISC-17 (Cloudflare DCR rate limit) remains deferred — post-launch hardening backlog item, not a verification gap per the recorded operator decision.

## What this record does NOT prove

- No new live calls on 2026-06-11; live evidence is from the recorded 06.1-12 smoke (2026-06-10).
- Wildcard DNS / worker behavior may have evolved since the smoke run.

## Sign-off

- [x] All 8 criteria assessed; 6 passed (recorded live proof, migration, or test-level), 2 require human/live verification.
- [ ] Human-needed items listed above remain open.
