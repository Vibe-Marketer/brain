# Phase 05 Verification Matrix

**Automated gate:** `npm test -- --run src/components/connectors/__tests__/ConnectionsPanel.test.tsx src/services/__tests__/sync-tab.service.test.ts && deno test --allow-env --allow-read supabase/functions/_shared/__tests__/connector-function-utils.test.ts && npm run build` exited 0 on 2026-05-31.

**Browser attempt:** local Vite app started at `http://127.0.0.1:5177/`; Playwright navigated to `/settings?tab=integrations` and was redirected to `/login`. Screenshot: `browser-settings-integrations-attempt.png`. Blocker: no authenticated seeded browser session was available in this runtime, so route-level Connections, Manage dialog, and all-source SyncTab visual checks are not claimed as live-browser verified.

**Migration/data movement:** `import_sources.workspace_id` backfill and future-binding changes affect future imports only. Existing calls/workspace entries are not moved by migration, reconnect, or workspace change code paths.

| Provider | Setup workspace required | Connections row rendered | Manage actions | Bound workspace import proof | Expired-token / refresh behavior | Rate-limit / retry behavior | Partial-sync behavior | Disconnect / reconnect proof | SyncTab canonical visibility | Automated evidence command | Manual/live credential evidence | Blocker if unverified |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Fathom | `ConnectorSetupCluster` blocks OAuth/API-key save without workspace; `fathom-oauth-url` persists `workspace_id`. | `ConnectionsPanel.test.tsx` covers compact provider/account/workspace/status/Manage row. | Manage dialog includes Sync now, Reconnect, Change future landing workspace, Disconnect. | `sync-meetings` resolves connector binding before `runPipeline`; legacy webhook resolves active source binding. | Fathom refresh failure marks reconnect-required in source status. | Shared status helper covers retry/rate-limited metadata. | Fathom sync records partial-sync metadata when some items fail. | Existing adapter disconnect path; reconnect keeps source ID and updates future binding. | `sync-tab.service.test.ts` includes Fathom canonical recording row. | Connector UI test, Deno helper test, source guard, build. | Not live-verified. | No Fathom test credential/session available. |
| Zoom | `zoom-oauth-url` creates/updates `import_sources.workspace_id`; setup UI requires workspace. | Connections row test covers provider-independent row contract. | Manage dialog includes Sync now, Reconnect, Change future landing workspace, Disconnect. | `zoom-sync-meetings` resolves `sourceId` binding; `zoom-webhook` resolves active source binding. | Zoom source refresh failure marks reconnect-required when source-backed. | Shared helper covers passive rate-limit metadata. | Zoom sync records partial-sync metadata when some items fail. | Existing adapter disconnect path; OAuth reconnect keeps source identity. | `sync-tab.service.test.ts` includes Zoom canonical recording row. | Connector UI test, Deno helper test, source guard, build. | Not live-verified. | No Zoom OAuth credential/session available. |
| Fireflies | API-key setup requires workspace; `fireflies-save-source` validates and writes `workspace_id`. | Connections row test covers provider-independent row contract. | Manage dialog includes Sync now, reconnect/token action, Change future landing workspace, Disconnect. | `fireflies-sync-meetings` and `fireflies-webhook` resolve bound workspace before canonical pipeline. | Token/API failure surfaces as connector error or reconnect action depending path. | Shared helper covers retry/rate-limited metadata. | Fireflies sync records partial-sync metadata when some items fail. | Existing disconnect source path; save-source updates existing source binding. | `sync-tab.service.test.ts` includes Fireflies canonical recording row. | Connector UI test, Deno helper test, source guard, build. | Not live-verified. | No Fireflies API key/webhook delivery available. |
| Grain | OAuth/API-token setup requires workspace; shared OAuth URL/token setup writes `workspace_id`. | Connections row test covers provider-independent row contract. | Manage dialog includes Sync now, Reconnect, Change future landing workspace, Disconnect. | `grain-sync-recordings` and `grain-webhook` resolve bound workspace before canonical pipeline. | Shared `resolveOAuthAccessToken` marks reconnect-required on refresh failure. | Shared helper covers retry/rate-limited metadata. | `runConnectorSyncJob` records partial-sync metadata for mixed outcomes. | Existing adapter disconnect path; source ID preserved on reconnect. | `sync-tab.service.test.ts` includes Grain canonical recording row. | Connector UI test, Deno helper test, source guard, build. | Not live-verified. | No Grain OAuth/token credential available. |
| Read.ai | OAuth/API-token setup requires workspace; shared OAuth URL/token setup writes `workspace_id`. | Connections row test covers reconnect-required status. | Manage dialog includes Sync now, Reconnect, Change future landing workspace, Disconnect. | `read-ai-sync-meetings` and `read-ai-webhook` resolve bound workspace before canonical pipeline. | Shared `resolveOAuthAccessToken` marks reconnect-required on refresh failure. | Shared helper covers retry/rate-limited metadata. | `runConnectorSyncJob` records partial-sync metadata for mixed outcomes. | Existing adapter disconnect path; source ID preserved on reconnect. | `sync-tab.service.test.ts` includes Read.ai canonical recording row. | Connector UI test, Deno helper test, source guard, build. | Not live-verified. | No Read.ai OAuth/token credential or webhook delivery available. |
| PLAUD | Browser bridge/token setup requires workspace; `plaud-connect-token` validates and writes top-level `workspace_id`. | Connections row test covers `Manage bridge`. | PLAUD row/dialog uses bridge-specific Manage bridge plus Change future landing workspace and Disconnect. | `plaud-sync-recordings` resolves CallVault binding and keeps provider `connection_metadata.workspace_id` separate. | Missing/invalid token returns reconnect/bridge action path. | Shared helper covers retry/rate-limited metadata. | PLAUD sync records partial-sync metadata when some files fail. | Existing source disconnect path; bridge reconnect keeps source identity. | `sync-tab.service.test.ts` includes PLAUD canonical recording row. | Connector UI test, Deno helper test, source guard, build. | Not live-verified. | No PLAUD browser bridge authenticated session available. |
| YouTube | No persistent connector setup; YouTube remains URL/action driven with explicit workspace import path. | Not represented as a connected account unless a persisted source row exists; setup card remains visible. | No connector Manage action required for action-only import. | `youtube-import` keeps explicit `workspace_id` one-off import behavior. | Not applicable: no provider token. | Not applicable: no connector token retry path. | YouTube import uses existing pipeline result behavior. | Not applicable: no persistent provider credentials. | `sync-tab.service.test.ts` includes YouTube canonical recording row. | SyncTab service test, source guard, build. | Not live-verified. | No seeded YouTube import/browser session available. |

## Browser Verification Status

- Connections route-level mounting: Settings -> Integrations now mounts `ConnectionsPanel scope="global"`.
- Playwright screenshot path: `.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/browser-settings-integrations-attempt.png`.
- Browser blocker: unauthenticated local session redirects to `/login`; no seeded authenticated workspace/session was available to inspect compact rows, Manage dialog, workspace-scoped filter, or seeded all-source SyncTab rows.
- Source-level/UI-unit substitute: `ConnectionsPanel.test.tsx` verifies compact rows, workspace/global filtering, PLAUD Manage bridge, passive statuses, reconnect-required state, and Manage dialog actions. `sync-tab.service.test.ts` verifies all-source canonical rows and workspace filtering.
