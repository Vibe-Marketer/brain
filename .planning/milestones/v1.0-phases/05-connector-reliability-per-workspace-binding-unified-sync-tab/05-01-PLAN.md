---
phase: 05-connector-reliability-per-workspace-binding-unified-sync-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/*_add_import_source_workspace_binding.sql
  - src/components/connectors/registry/types.ts
  - src/components/connectors/hooks/useConnector.ts
  - src/services/import-sources.service.ts
  - src/hooks/useImportSources.ts
  - src/components/connectors/__tests__/deriveConnectorStatus.test.ts
autonomous: true
requirements:
  - CON-02
  - CON-04
must_haves:
  truths:
    - "D-01/D-05: every existing and new connector row has a future landing workspace, with existing rows assigned to the default workspace."
    - "D-02/D-10/D-11: binding changes and migration never move existing recordings or workspace_entries."
    - "D-03: unbound legacy events use default workspace fallback instead of dropping data."
    - "D-07/D-08/D-09: multiple provider accounts remain supported; one connector instance has one future landing workspace; no fanout."
    - "SCHEMA_PUSH_REQUIRED: this plan modifies Supabase migrations and must run `supabase db push` after schema edits before verification."
  artifacts:
    - path: "supabase/migrations/*_add_import_source_workspace_binding.sql"
      provides: "workspace binding schema and minimal backfill"
    - path: "src/components/connectors/hooks/useConnector.ts"
      provides: "workspace-aware connector status source of truth"
    - path: "src/services/import-sources.service.ts"
      provides: "workspace binding read/update service APIs"
---

<objective>
Add the data contract for connector workspace binding and expose it through the existing connector status/service layer.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-CONTEXT.md
@.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-RESEARCH.md
@.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-PATTERNS.md
@.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-VALIDATION.md
@src/CLAUDE.md
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace binding schema and default-workspace backfill</name>
  <files>supabase/migrations/*_add_import_source_workspace_binding.sql</files>
  <read_first>
    - supabase/migrations/20260228000002_create_import_sources.sql
    - supabase/migrations/20260523053000_add_import_source_connection_metadata.sql
    - supabase/migrations/*workspace*.sql
    - .planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-CONTEXT.md
  </read_first>
  <action>Create a Supabase migration that adds `public.import_sources.workspace_id UUID REFERENCES public.workspaces(id) ON DELETE SET NULL`, adds indexes for `workspace_id`, `(user_id, workspace_id)`, and connector lookup by `user_id/source_app/workspace_id`, and backfills existing rows to each user's default workspace. Preserve multi-account rows by auditing/removing/replacing any active `(user_id, source_app)` uniqueness that prevents multiple accounts per provider. Add comments distinguishing `import_sources.workspace_id` as the CallVault destination workspace from provider-specific `connection_metadata.workspace_id`.</action>
  <acceptance_criteria>
    - Migration contains `ADD COLUMN IF NOT EXISTS workspace_id UUID`.
    - Migration does not update `recordings` or `workspace_entries`.
    - Migration comments mention "future landing workspace" or "destination workspace".
    - Migration includes an explicit backfill to a default/home workspace per user/org.
    - Migration preserves multiple rows per `user_id` and `source_app` when account identity differs.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "workspace_id|recordings|workspace_entries|UNIQUE|CREATE.*INDEX" supabase/migrations/*_add_import_source_workspace_binding.sql</automated>
  </verify>
  <done>Connector rows have a durable destination workspace without touching existing calls.</done>
</task>

<task type="auto">
  <name>Task 2: Extend connector status types, service reads, and binding mutation APIs</name>
  <files>src/components/connectors/registry/types.ts, src/components/connectors/hooks/useConnector.ts, src/services/import-sources.service.ts, src/hooks/useImportSources.ts, src/components/connectors/__tests__/deriveConnectorStatus.test.ts</files>
  <read_first>
    - src/components/connectors/registry/types.ts
    - src/components/connectors/hooks/useConnector.ts
    - src/services/import-sources.service.ts
    - src/hooks/useImportSources.ts
    - src/lib/query-config.ts
    - src/components/connectors/__tests__/deriveConnectorStatus.test.ts
  </read_first>
  <action>Add `workspace_id`, `workspaceName`/workspace display data where available, and action-needed status fields to `ConnectorRow`/`ConnectorStatus`. Update `fetchConnectorBundle()` and legacy connector fetches to select `workspace_id` from `import_sources`. Add pure service functions to list connector accounts with workspace labels and update an import source's future landing workspace. Add a hook mutation that invalidates connector bundle, import sources, and call-list caches on settled. Extend `deriveConnectorStatus` tests for bound workspace display, multiple rows, expired token with server-side refresh, and reconnect-required error state.</action>
  <acceptance_criteria>
    - `ConnectorRow` includes `workspace_id: string | null`.
    - `fetchConnectorBundle()` select list includes `workspace_id`.
    - A mutation exists for changing future landing workspace and calls `invalidateCallListCaches(queryClient)` in `onSettled`.
    - `deriveConnectorStatus.test.ts` covers multiple rows and workspace binding.
    - No component directly calls Supabase for this data.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/connectors/__tests__/deriveConnectorStatus.test.ts && npm run build</automated>
  </verify>
  <done>The frontend has one workspace-aware connector status model.</done>
</task>

<task type="auto">
  <name>Task 3: Run the blocking schema push before plan verification</name>
  <files>supabase/migrations/*_add_import_source_workspace_binding.sql</files>
  <read_first>
    - supabase/CLAUDE.md
    - supabase/migrations/*_add_import_source_workspace_binding.sql
  </read_first>
  <action>Run the Supabase schema push command after the migration is written and before claiming verification. Use `supabase db push` with `SUPABASE_ACCESS_TOKEN` available if the CLI requires non-interactive auth. If the command cannot run in this environment, record the exact blocker in the plan summary and do not mark schema verification complete.</action>
  <acceptance_criteria>
    - Terminal output shows `supabase db push` exited 0, or the summary records the exact missing credential/non-interactive blocker.
    - The migration file remains committed with the plan's schema changes.
  </acceptance_criteria>
  <verify>
    <automated>supabase db push</automated>
  </verify>
  <done>The live/project database schema matches the migration before downstream work depends on it.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-01 | Tampering | connector migration | mitigate | Backfill only `import_sources.workspace_id`; never update recordings or workspace_entries. |
| T-05-02 | Information Disclosure | connector status queries | mitigate | Preserve user-scoped RLS and service/hook separation. |
| T-05-03 | Elevation of Privilege | workspace binding update | mitigate | Validate workspace membership before updates and invalidate caches on settled. |
</threat_model>

<verification>
Run `npm test -- --run src/components/connectors/__tests__/deriveConnectorStatus.test.ts && npm run build` and run `supabase db push` after the migration is complete.
</verification>

<success_criteria>
- Existing connector rows have default workspace bindings.
- New code can read and update the future landing workspace.
- Existing calls are not moved.
- Multiple provider accounts are not collapsed.
</success_criteria>

<output>
Create `.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-01-SUMMARY.md` when done.
</output>
