---
phase: 04-mcp-ai-write-tools
plan: 05
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - src/assets/source-logos/mcp.svg
  - src/components/shared/icons/McpSourceIcon.tsx
  - src/config/source-registry.ts
  - src/lib/__tests__/source-labels.registry.test.ts
  - src/lib/__tests__/source-display.test.ts
  - docs/operations/mcp-runbook.md
autonomous: true
requirements:
  - MCP-04
must_haves:
  truths:
    - "D-09/D-10/D-12: MCP-created imports render as `Manual MCP Import` with an official MCP icon path and do not masquerade as a native connector."
    - "D-11 and roadmap 3: client/provider and source URL metadata enrich the record without replacing the main manual-import identity."
    - "MCP-04/D-10: the final visible-source slice covers the official MCP icon path, build health, and live smoke evidence or an explicit credential gap."
  artifacts:
    - path: "src/assets/source-logos/mcp.svg"
      provides: "Official MCP logo asset sourced from the MCP project"
    - path: "src/components/shared/icons/McpSourceIcon.tsx"
      provides: "Registry-compatible icon wrapper for the MCP source entry"
    - path: "src/config/source-registry.ts"
      provides: "Visible `Manual MCP Import` source identity"
    - path: "docs/operations/mcp-runbook.md"
      provides: "Phase 04 smoke commands and proof/gap recording guidance"
  key_links:
    - from: "src/config/source-registry.ts"
      to: "src/components/shared/icons/McpSourceIcon.tsx"
      via: "source icon component mapping"
      pattern: "manual-mcp-import|Manual MCP Import|McpSourceIcon"
    - from: "docs/operations/mcp-runbook.md"
      to: "supabase/functions/mcp-server/tools/write/ingest_transcript.ts"
      via: "Phase 04 smoke commands"
      pattern: "ingest_transcript|append_to_transcript|set_speakers"
---

<objective>
Finish the visible Phase 04 identity and verification layer so MCP imports read correctly in the product, the official MCP icon path is integrated cleanly, and the phase ends with explicit build and smoke evidence.

Purpose: Close the gap between backend writes and what users actually see in CallVault, then capture the final verification proof for MCP-04.
Output: Manual MCP Import source-registry entry, MCP icon asset wrapper, registry tests, and Phase 04 runbook verification steps.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mcp-ai-write-tools/04-CONTEXT.md
@.planning/phases/04-mcp-ai-write-tools/04-RESEARCH.md
@.planning/phases/04-mcp-ai-write-tools/04-VALIDATION.md
@docs/operations/mcp-runbook.md
@src/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the visible Manual MCP Import source identity with the official MCP icon path</name>
  <files>src/assets/source-logos/mcp.svg, src/components/shared/icons/McpSourceIcon.tsx, src/config/source-registry.ts, src/lib/__tests__/source-labels.registry.test.ts, src/lib/__tests__/source-display.test.ts</files>
  <read_first>
    - src/config/source-registry.ts
    - src/lib/source-labels.ts
    - src/lib/source-display.ts
    - src/lib/__tests__/source-labels.registry.test.ts
    - src/lib/__tests__/source-display.test.ts
    - .planning/phases/04-mcp-ai-write-tools/04-CONTEXT.md
  </read_first>
  <action>Create `src/assets/source-logos/mcp.svg` from the official MCP project asset path cited in `04-CONTEXT.md`, add a small `McpSourceIcon.tsx` wrapper that preserves the existing `ComponentType<{ className?: string }>` registry contract, and register a new visible `manual-mcp-import` source entry in `src/config/source-registry.ts` labeled exactly `Manual MCP Import` per D-09. Keep provider-specific/client-specific details out of the visible label per D-11, use the registry tests to pin the new label/order/indicator behavior, and keep the asset source official-first rather than a third-party icon pack per D-10.</action>
  <acceptance_criteria>
    - `src/config/source-registry.ts` contains a `manual-mcp-import` entry labeled `Manual MCP Import`.
    - `src/components/shared/icons/McpSourceIcon.tsx` allows the registry to use the official MCP SVG without changing the registry icon contract broadly.
    - The source-label and source-display tests include the new registry entry and continue to derive labels and display order from the registry.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/lib/__tests__/source-labels.registry.test.ts src/lib/__tests__/source-display.test.ts</automated>
  </verify>
  <done>MCP-created recordings have a first-class visible source label and icon path inside the app instead of reading like a generic manual or connector import.</done>
</task>

<task type="auto">
  <name>Task 2: Record final Phase 04 verification commands, then run build and targeted smoke gates</name>
  <files>docs/operations/mcp-runbook.md</files>
  <read_first>
    - docs/operations/mcp-runbook.md
    - .planning/phases/04-mcp-ai-write-tools/04-VALIDATION.md
    - supabase/functions/mcp-server/tools/write/ingest_transcript.ts
    - supabase/functions/mcp-server/tools/write/append_to_transcript.ts
    - supabase/functions/mcp-server/tools/write/update_call_metadata.ts
    - supabase/functions/mcp-server/tools/write/set_speakers.ts
  </read_first>
  <action>Update `docs/operations/mcp-runbook.md` with the final Phase 04 verification commands for `tools/list`, `ingest_transcript`, and one follow-up tool against `https://api.callvaultai.com/mcp/w/{workspace_uuid}`. Then run the targeted MCP tests, `npm run build`, and the live-smoke sequence if the required production env vars exist locally. If workspace/token credentials are unavailable, record the exact missing env vars and keep the runbook/summary explicit about the live-proof gap instead of claiming Phase 04 is fully smoke-tested.</action>
  <acceptance_criteria>
    - The runbook includes Phase 04 smoke commands and the expected markdown-response checks for `content[0].text`.
    - The plan records whether live smoke proved `ingest_transcript` plus one follow-up tool or whether missing credentials blocked that proof.
    - `npm run build` is part of the final gate because Phase 04 touches `src/config/source-registry.ts`.
  </acceptance_criteria>
  <verify>
    <automated>VITEST_INTEGRATION_OK=true npm test -- --run supabase/functions/mcp-server/__tests__/ingest-transcript.integration.test.ts supabase/functions/mcp-server/__tests__/set-speakers.idempotency.test.ts supabase/functions/mcp-server/__tests__/contract-surface.test.ts supabase/functions/mcp-server/__tests__/category-gating.test.ts supabase/functions/mcp-server/__tests__/workspace-scope.integration.test.ts supabase/functions/mcp-server/__tests__/golden-replay.test.ts supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts && npm run build</automated>
  </verify>
  <done>Phase 04 ends with updated operational smoke instructions, a clean build gate, and explicit live-proof evidence or an explicit credential-based limitation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored source identity -> frontend presentation | Backend `source_app` and metadata become user-visible labels and icons |
| Final verification commands -> deploy confidence | Local test/build results become statements about live MCP readiness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Spoofing | visible source identity | mitigate | Add a dedicated `manual-mcp-import` registry entry and official MCP icon wrapper so MCP imports do not look like native connector syncs. |
| T-04-02 | Information Disclosure | final smoke tooling | mitigate | Keep smoke commands scoped to authenticated MCP calls and preserve category-filtered `tools/list` expectations in the runbook. |
| T-04-04 | Repudiation | verification evidence | mitigate | Record live-smoke proof or the exact missing credential gap instead of claiming verification that did not happen. |
</threat_model>

<verification>
Run the final Phase 04 gate:

`VITEST_INTEGRATION_OK=true npm test -- --run supabase/functions/mcp-server/__tests__/ingest-transcript.integration.test.ts supabase/functions/mcp-server/__tests__/set-speakers.idempotency.test.ts supabase/functions/mcp-server/__tests__/contract-surface.test.ts supabase/functions/mcp-server/__tests__/category-gating.test.ts supabase/functions/mcp-server/__tests__/workspace-scope.integration.test.ts supabase/functions/mcp-server/__tests__/golden-replay.test.ts supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts && npm run build`
</verification>

<success_criteria>
- Visible source identity reads `Manual MCP Import` with an official MCP icon path.
- Registry-derived source-label and display-order tests pass.
- Targeted MCP tests and `npm run build` pass, and the runbook records live smoke proof or an explicit credential gap.
</success_criteria>

<output>
Create `.planning/phases/04-mcp-ai-write-tools/04-05-SUMMARY.md` when done.
</output>
