---
phase: 06.3.1-per-call-obsidian-export
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/export-utils.ts
  - src/lib/__tests__/export-utils.obsidian.test.ts
autonomous: true
requirements:
  - Phase-06.3.1-D-04
  - Phase-06.3.1-D-05
  - Phase-06.3.1-D-06
  - Phase-06.3.1-D-07
  - Phase-06.3.1-D-10
  - Phase-06.3.1-D-11
  - Phase-06.3.1-D-12
  - Phase-06.3.1-D-13
  - Phase-06.3.1-D-15
  - Phase-06.3.1-D-16
  - Phase-06.3.1-D-19
must_haves:
  truths:
    - "D-04 D-05 D-06 D-07: Per-call Obsidian export produces one standalone `.md` file while preserving the full-vault Obsidian note conventions and recommended vault path."
    - "D-10 D-11 D-12 D-13: Obsidian front matter prefers canonical UUID identity, preserves stored or structured transcript text, and emits `Transcript not available.` when no transcript exists."
    - "D-15 D-16 D-19: Full-vault ZIP export and per-call download share the same Obsidian markdown builder and are covered by focused utility tests."
  artifacts:
    - path: "src/lib/export-utils.ts"
      provides: "shared Obsidian markdown, filename, vault-path, ZIP, and single-file download helpers"
    - path: "src/lib/__tests__/export-utils.obsidian.test.ts"
      provides: "Obsidian helper, ZIP compatibility, and single-file download coverage"
---

<objective>
Extract the Obsidian note builder from the ZIP export path and add a single-call `.md` download helper without changing full-vault ZIP behavior.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.1-per-call-obsidian-export/06.3.1-CONTEXT.md
@.planning/phases/06.3.1-per-call-obsidian-export/06.3.1-RESEARCH.md
@.planning/phases/06.3.1-per-call-obsidian-export/06.3.1-PATTERNS.md
@.planning/phases/06.3-obsidian-sync-improvements/06.3-CONTEXT.md
@src/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export pure Obsidian note helpers</name>
  <files>src/lib/export-utils.ts</files>
  <read_first>
    - src/lib/export-utils.ts
    - src/lib/__tests__/export-utils.obsidian.test.ts
    - .planning/phases/06.3.1-per-call-obsidian-export/06.3.1-CONTEXT.md
    - .planning/phases/06.3.1-per-call-obsidian-export/06.3.1-PATTERNS.md
  </read_first>
  <action>Refactor the Obsidian Vault Export section of `src/lib/export-utils.ts` to export pure helpers for the shared note contract: `buildObsidianFileName(call)`, `buildObsidianVaultPath(call, orgName, workspaceName, suffix?)`, and `buildObsidianMarkdown(call, { orgName, workspaceName, syncedAt, vaultPath })`. Extend `ExportableCall` with optional `tag_names?: string[]` so per-call export can carry available CallVault tags without changing ZIP callers. Keep the current YAML fields: `callvault_id`, `type: call`, `date`, `date_time`, `duration_min`, `organization`, `workspace`, `vault_path`, `word_count`, `participants`, `tags`, `has_transcript`, optional `share_url`, and `synced_at`. In `tags`, preserve the existing `callvault`, `workspace/{slug}`, and `{year}/{month}` tags, and append sanitized `call-tags/{slug}` entries for `call.tag_names` when present. Keep `getExportTranscriptText(call)` as the transcript source so structured segments win and stored `full_transcript` remains the fallback.</action>
  <acceptance_criteria>
    - `src/lib/export-utils.ts` exports `buildObsidianFileName`.
    - `src/lib/export-utils.ts` exports `buildObsidianVaultPath`.
    - `src/lib/export-utils.ts` exports `buildObsidianMarkdown`.
    - `ExportableCall` includes optional `tag_names?: string[]`.
    - `buildObsidianMarkdown` includes `call-tags/{slug}` YAML tags when `call.tag_names` is present.
    - `buildObsidianMarkdown` output includes `vault_path`, `callvault_id`, `has_transcript`, `# {title}`, and `## Transcript`.
    - Tests assert Obsidian helper output preserves exact transcript text containing brackets, blank lines, and speaker labels without generic Markdown regrouping.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "export function buildObsidian(FileName|VaultPath|Markdown)|vault_path|has_transcript|## Transcript" src/lib/export-utils.ts && npm test -- src/lib/__tests__/export-utils.obsidian.test.ts</automated>
  </verify>
  <done>Obsidian note construction is reusable outside ZIP generation.</done>
</task>

<task type="auto">
  <name>Task 2: Keep full-vault ZIP behavior on the shared helpers</name>
  <files>src/lib/export-utils.ts, src/lib/__tests__/export-utils.obsidian.test.ts</files>
  <read_first>
    - src/lib/export-utils.ts
    - src/lib/__tests__/export-utils.obsidian.test.ts
    - .planning/phases/06.3-obsidian-sync-improvements/06.3-CONTEXT.md
  </read_first>
  <action>Update `exportToObsidian(calls, orgName, excludedWorkspaceNames)` so it computes workspace name, date slug, duplicate suffixes, and ZIP paths as it does today, then calls `buildObsidianMarkdown()` for file content. Preserve existing full-vault behavior: ZIP filename `callvault-obsidian-{date}.zip`, `CallVault/{org}/{workspace}/{date}-{slug}.md` paths, workspace exclusion, duplicate suffixes, and the existing 5,000-call test target.</action>
  <acceptance_criteria>
    - `exportToObsidian` still exists with parameters `(calls, orgName, excludedWorkspaceNames)`.
    - `exportToObsidian` calls `buildObsidianMarkdown`.
    - Existing tests for duplicate suffixes, workspace exclusion, YAML escaping, structured transcript preference, and 5,000 calls still pass.
    - No product-level `500` export cap is introduced.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/lib/__tests__/export-utils.obsidian.test.ts && ! rg -n "slice\\(0,\\s*500\\)|limit\\(500\\)|500-record" src/lib/export-utils.ts src/lib/__tests__/export-utils.obsidian.test.ts</automated>
  </verify>
  <done>Full-vault Obsidian ZIP export keeps its current contract while using the shared builder.</done>
</task>

<task type="auto">
  <name>Task 3: Add single-call Obsidian download coverage</name>
  <files>src/lib/export-utils.ts, src/lib/__tests__/export-utils.obsidian.test.ts</files>
  <read_first>
    - src/lib/export-utils.ts
    - src/lib/__tests__/export-utils.obsidian.test.ts
    - src/test/setup.ts
  </read_first>
  <action>Add `exportSingleCallToObsidian(call, { orgName, workspaceName })` to `src/lib/export-utils.ts`. It must build the same markdown with `buildObsidianMarkdown()`, create a `Blob` with `text/markdown;charset=utf-8`, and call `saveAs(blob, buildObsidianFileName(call))`. It must not import or use `jszip`. Extend `src/lib/__tests__/export-utils.obsidian.test.ts` to mock `file-saver`, call `exportSingleCallToObsidian`, assert the saved filename follows `{date}-{slug}.md`, and assert the saved Blob text contains the same Obsidian front matter/body fields as the ZIP path.</action>
  <acceptance_criteria>
    - `src/lib/export-utils.ts` exports `exportSingleCallToObsidian`.
    - `exportSingleCallToObsidian` calls `buildObsidianMarkdown`.
    - `exportSingleCallToObsidian` does not import or instantiate `JSZip`.
    - A utility test asserts the per-call filename equals a `{date}-{slug}.md` value.
    - A utility test reads the saved Blob text and asserts it contains `type: call`, `vault_path`, canonical `callvault_id`, participant wikilinks, optional `## Summary`, exact transcript text with brackets/blank lines preserved, `Transcript not available.` for a no-transcript fixture, `share_url`, `call-tags/{slug}`, `#`, and `## Transcript`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/lib/__tests__/export-utils.obsidian.test.ts && npm run type-check</automated>
  </verify>
  <done>Single-call Obsidian `.md` download is test-covered at the utility layer.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06.3.1-01 | Data Loss | shared Obsidian builder | mitigate | Reuse one helper for ZIP and single-file output; keep existing ZIP tests green. |
| T-06.3.1-02 | Information Integrity | YAML/front matter | mitigate | Preserve JSON string escaping and add per-call helper assertions for front matter fields. |
| T-06.3.1-03 | Privacy | markdown download | accept | Export is user-initiated and contains the already-visible transcript and metadata. |
</threat_model>

<verification>
Run `npm test -- src/lib/__tests__/export-utils.obsidian.test.ts` after each task and `npm run type-check` before completing the plan.
</verification>

<success_criteria>
- Obsidian markdown construction is exported as pure helpers.
- Full-vault ZIP behavior still passes existing Obsidian tests.
- A single-call `.md` download helper exists and is covered without JSZip.
</success_criteria>

<output>
Create `.planning/phases/06.3.1-per-call-obsidian-export/06.3.1-01-SUMMARY.md` when done.
</output>
