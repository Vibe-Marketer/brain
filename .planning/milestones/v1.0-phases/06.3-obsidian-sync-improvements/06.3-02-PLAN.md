---
phase: 06.3-obsidian-sync-improvements
plan: 02
type: execute
wave: 1
depends_on:
  - 06.3-01
files_modified:
  - src/lib/export-utils.ts
  - src/lib/__tests__/export-utils.obsidian.test.ts
autonomous: true
requirements:
  - Phase-06.3-SC-1
  - Phase-06.3-SC-2
  - Phase-06.3-SC-3
  - Phase-06.3-SC-4
must_haves:
  truths:
    - "Obsidian ZIP generation writes one markdown file per included call with deterministic paths and no silent truncation."
    - "Obsidian markdown front matter and body are testable, deterministic, and preserve stored transcript text."
  artifacts:
    - path: "src/lib/export-utils.ts"
      provides: "Obsidian note and ZIP generation"
    - path: "src/lib/__tests__/export-utils.obsidian.test.ts"
      provides: "format, duplicate path, missing transcript, and 5,000-call scale coverage"
---

<objective>
Harden the Obsidian markdown/ZIP generator and prove the 5,000-call export target at the file-generation layer.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3-obsidian-sync-improvements/06.3-CONTEXT.md
@.planning/phases/06.3-obsidian-sync-improvements/06.3-RESEARCH.md
@.planning/phases/06.3-obsidian-sync-improvements/06.3-UI-SPEC.md
@.planning/phases/06.3-obsidian-sync-improvements/06.3-VALIDATION.md
@.planning/phases/06.3-obsidian-sync-improvements/06.3-PATTERNS.md
@src/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused Obsidian export tests</name>
  <files>src/lib/__tests__/export-utils.obsidian.test.ts</files>
  <read_first>
    - src/lib/export-utils.ts
    - src/lib/__tests__/source-display.test.ts
    - src/lib/__tests__/recording-ids.test.ts
    - vitest.config.ts
  </read_first>
  <action>Create `src/lib/__tests__/export-utils.obsidian.test.ts` for `exportToObsidian()`. Mock `file-saver` and `jszip` so tests can inspect file paths and markdown contents without writing a real browser download. Cover: one file per call, `CallVault/{org}/{workspace}/{date}-{slug}.md` path shape, duplicate filename suffixes, required front matter fields, participants as wikilinks, missing transcript text, and workspace exclusion.</action>
  <acceptance_criteria>
    - Test file exists and imports `exportToObsidian`.
    - Tests assert a generated path contains `CallVault/`.
    - Tests assert duplicate titles produce suffixed markdown filenames.
    - Tests assert missing transcript output contains `Transcript not available.`.
    - Tests assert excluded workspace calls are not written.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/lib/__tests__/export-utils.obsidian.test.ts</automated>
  </verify>
  <done>Obsidian export format is covered by targeted tests.</done>
</task>

<task type="auto">
  <name>Task 2: Make Obsidian note building deterministic and YAML-safe</name>
  <files>src/lib/export-utils.ts, src/lib/__tests__/export-utils.obsidian.test.ts</files>
  <read_first>
    - src/lib/export-utils.ts
    - src/lib/__tests__/export-utils.obsidian.test.ts
    - .planning/phases/06.3-obsidian-sync-improvements/06.3-CONTEXT.md
  </read_first>
  <action>Refactor the Obsidian-specific section of `src/lib/export-utils.ts` so path sanitization, slug creation, front matter construction, and note body construction are deterministic and testable. Keep the public `exportToObsidian(calls, orgName, excludedWorkspaceNames)` API. Escape YAML string values for `title`, `organization`, `workspace`, participants, and `share_url`. Preserve stored `full_transcript` exactly in `## Transcript`; do not call `transcriptToMarkdown()` or synthesize timestamps. Ensure front matter includes `callvault_id`, `type`, `date`, `date_time`, `duration_min`, `organization`, `workspace`, `vault_path`, `word_count`, `participants`, `tags`, `has_transcript`, optional `share_url`, and `synced_at`.</action>
  <acceptance_criteria>
    - `exportToObsidian` still exists with the same exported function name.
    - Tests assert front matter contains `callvault_id`, `workspace`, `tags`, and `has_transcript`.
    - Tests include a title, workspace, or participant with quotes and pass.
    - Tests assert transcript text is preserved under `## Transcript`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/lib/__tests__/export-utils.obsidian.test.ts && npm run type-check</automated>
  </verify>
  <done>Markdown generation is deterministic and safe enough for Obsidian front matter.</done>
</task>

<task type="auto">
  <name>Task 3: Prove 5,000-call ZIP generation behavior</name>
  <files>src/lib/export-utils.ts, src/lib/__tests__/export-utils.obsidian.test.ts</files>
  <read_first>
    - src/lib/export-utils.ts
    - src/lib/__tests__/export-utils.obsidian.test.ts
    - .planning/phases/06.3-obsidian-sync-improvements/06.3-CONTEXT.md
  </read_first>
  <action>Add a 5,000-call deterministic test fixture with realistic transcript-bearing calls. The test must call `exportToObsidian()` and assert that 5,000 markdown files are written or, if implementation chooses a multi-part strategy, that all 5,000 calls are assigned to explicit parts with no omitted calls. If plain JSZip cannot complete the test reliably, implement a user-safe fallback in `exportToObsidian()` such as deterministic ZIP parts with clear filenames. Do not silently reduce the fixture size. Do not add a product-level 500 cap.</action>
  <acceptance_criteria>
    - Test fixture creates at least 5,000 calls with non-empty transcript bodies.
    - Test asserts exactly 5,000 calls are represented in output files or output parts.
    - Implementation contains no `slice(0, 500)`, no `limit(500)`, and no hard-coded 500-record cap.
    - If multi-part export is implemented, output filenames make the part number and total parts explicit.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/lib/__tests__/export-utils.obsidian.test.ts && ! rg -n "slice\\(0,\\s*500\\)|limit\\(500\\)|500-record" src/lib/export-utils.ts src/lib/__tests__/export-utils.obsidian.test.ts</automated>
  </verify>
  <done>Obsidian ZIP generation covers the 5,000-call full-vault target without truncation.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06.3-03 | Tampering | YAML front matter | mitigate | Escape string values and add tests for quotes/special characters. |
| T-06.3-04 | Data Loss | ZIP generation | mitigate | Assert all 5,000 calls are represented; fallback to explicit parts if needed. |
</threat_model>

<verification>
Run `npm test -- src/lib/__tests__/export-utils.obsidian.test.ts`, `npm run type-check`, and the no-500-cap scan.
</verification>

<success_criteria>
- Obsidian markdown is deterministic and test-covered.
- 5,000 transcript-bearing calls are represented in export output.
- No product-level 500 cap exists in export generation.
</success_criteria>

<output>
Create `.planning/phases/06.3-obsidian-sync-improvements/06.3-02-SUMMARY.md` when done.
</output>
