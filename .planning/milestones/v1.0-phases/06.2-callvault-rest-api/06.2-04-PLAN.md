---
phase: 06.2-callvault-rest-api
plan: 04
type: execute
wave: 2
depends_on:
  - 06.2-02
  - 06.2-03
files_modified:
  - cloudflare/api-proxy/worker.ts
  - cloudflare/api-proxy/__tests__/*.test.ts
  - docs/operations/api-runbook.md
  - docs/operations/mcp-runbook.md
autonomous: true
requirements:
  - Phase-06.2-SC-1
  - Phase-06.2-SC-2
  - Phase-06.2-SC-3
  - Phase-06.2-SC-4
  - Phase-06.2-SC-5
  - Phase-06.2-SC-6
  - Phase-06.2-SC-7
must_haves:
  truths:
    - "`https://api.callvaultai.com/v1/*` routes to the REST API Edge Function in production."
    - "Live smoke proves valid API token success, missing token 401, wrong-source token 403, and revoked token rejection."
  artifacts:
    - path: "cloudflare/api-proxy/worker.ts"
      provides: "public `/v1/*` vanity route"
    - path: "docs/operations/api-runbook.md"
      provides: "operator/developer smoke commands and response examples"
---

<objective>
Expose the REST API on the public vanity host, document the smoke contract, deploy, and verify live behavior end to end.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-callvault-rest-api/06.2-CONTEXT.md
@.planning/phases/06.2-callvault-rest-api/06.2-RESEARCH.md
@.planning/phases/06.2-callvault-rest-api/06.2-VALIDATION.md
@cloudflare/api-proxy/worker.ts
@docs/operations/mcp-runbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route `/v1/*` through the Cloudflare API proxy</name>
  <files>cloudflare/api-proxy/worker.ts, cloudflare/api-proxy/__tests__/*.test.ts</files>
  <read_first>
    - cloudflare/api-proxy/worker.ts
    - cloudflare/api-proxy/wrangler.toml
    - supabase/functions/callvault-api/index.ts
    - docs/operations/mcp-runbook.md
  </read_first>
  <action>Add a route branch in `resolveTarget(url)` so `url.pathname === '/v1'` or `url.pathname.startsWith('/v1/')` forwards to `${SUPABASE_BASE}/functions/v1/callvault-api${url.pathname}${url.search}` or an equivalent function-prefixed target that preserves the public `/v1` path for the Edge Function router. Do not alter `/auth/v1/*`, `/mcp`, `/.well-known/*`, `/fireflies-webhook`, or `mcp.callvaultai.com` root routing. Add or update worker tests to assert `/v1/calls` maps to `callvault-api` and `/auth/v1/oauth/token` still maps to Supabase Auth.</action>
  <acceptance_criteria>
    - `worker.ts` contains a `/v1` route branch.
    - `/v1/calls` target contains `/functions/v1/callvault-api`.
    - Existing `/auth/v1/*` route branch remains present.
    - Worker tests cover `/v1/calls` and `/auth/v1/oauth/token`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "callvault-api|/v1|/auth/v1" cloudflare/api-proxy/worker.ts cloudflare/api-proxy && npm run test -- --run cloudflare/api-proxy</automated>
  </verify>
  <done>Cloudflare proxy can route public `/v1/*` requests without regressing auth/MCP paths.</done>
</task>

<task type="auto">
  <name>Task 2: Document API smoke and developer contract</name>
  <files>docs/operations/api-runbook.md, docs/operations/mcp-runbook.md</files>
  <read_first>
    - docs/operations/mcp-runbook.md
    - .planning/phases/06.2-callvault-rest-api/06.2-CONTEXT.md
    - supabase/functions/callvault-api/index.ts
  </read_first>
  <action>Create `docs/operations/api-runbook.md` with redacted curl examples for `GET /v1/workspaces`, `GET /v1/calls`, `GET /v1/contacts`, and `GET /v1/speakers`; include expected `{ data, pagination }` shape, 401 missing/invalid token shape, 403 wrong-source token shape, temporary-token generation/revocation guidance, and deploy commands. Add a short cross-reference in `mcp-runbook.md` clarifying that MCP remains separate from the REST API.</action>
  <acceptance_criteria>
    - `api-runbook.md` includes `https://api.callvaultai.com/v1/workspaces`.
    - `api-runbook.md` includes `{ data, pagination }` or equivalent JSON envelope documentation.
    - `api-runbook.md` documents 401 and 403 cases.
    - `mcp-runbook.md` states REST `/v1` is separate from MCP.
    - No real tokens or secrets are written to docs.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "api.callvaultai.com/v1/workspaces|data.*pagination|401|403|MCP.*REST|REST.*MCP" docs/operations/api-runbook.md docs/operations/mcp-runbook.md && ! rg -n "cv_api_[0-9a-f]{16,}|SUPABASE_SERVICE_ROLE_KEY=.*[A-Za-z0-9]" docs/operations/api-runbook.md</automated>
  </verify>
  <done>Operators have exact REST API smoke commands without leaking secrets or conflating MCP.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy and live-smoke the public REST API</name>
  <files>.planning/phases/06.2-callvault-rest-api/06.2-04-SUMMARY.md</files>
  <read_first>
    - docs/operations/api-runbook.md
    - cloudflare/api-proxy/worker.ts
    - supabase/functions/callvault-api/index.ts
    - .env
  </read_first>
  <action>Run final local verification, deploy the database migration if not already pushed, deploy `callvault-api` via `supabase functions deploy callvault-api --use-api`, deploy the Cloudflare worker, then run live curl smoke against `https://api.callvaultai.com/v1/*`. Use a temporary API token only; do not print token values into docs or summaries. Prove at minimum: valid token `GET /v1/workspaces` returns HTTP 200 JSON with `data`, missing token returns HTTP 401 JSON, a non-API token returns HTTP 403 JSON if available, revoked temporary token stops working. If any credential is unavailable, record the exact missing env var and do not claim live verification.</action>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `supabase functions deploy callvault-api --use-api` exits 0 or the summary records the exact deploy blocker.
    - Cloudflare worker deploy exits 0 or the summary records the exact deploy blocker.
    - Live `https://api.callvaultai.com/v1/workspaces` valid-token request returns HTTP 200 with JSON `data`.
    - Live missing-token request returns HTTP 401 with JSON `error`.
    - Live wrong-source request returns HTTP 403 with JSON `error`, if a wrong-source token is available.
    - Temporary token is revoked after smoke.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && supabase functions deploy callvault-api --use-api && (cd cloudflare/api-proxy && npx wrangler deploy)</automated>
    <manual>curl -fsS -D /tmp/callvault-api.headers https://api.callvaultai.com/v1/workspaces -H "Authorization: Bearer $CALLVAULT_API_TOKEN" and verify HTTP 200 plus JSON `data`; run missing/wrong/revoked-token checks per runbook.</manual>
  </verify>
  <done>Public REST API is deployed and live-verified through the vanity domain.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06.2-09 | Availability | Cloudflare route | mitigate | Add worker tests and deploy proof for `/v1/*` while preserving existing routes. |
| T-06.2-10 | Information Disclosure | docs/live smoke | mitigate | Redact tokens and never write secrets into tracked docs or summaries. |
| T-06.2-11 | Repudiation | completion proof | mitigate | Record exact live smoke outcomes, including blocked credentials if proof cannot run. |
</threat_model>

<verification>
Run worker tests, API tests, `npm run build`, Edge Function deploy, worker deploy, and live curl smoke through `api.callvaultai.com/v1/*`.
</verification>

<success_criteria>
- `/v1/*` routes are public on `api.callvaultai.com`.
- REST and MCP docs clearly separate protocols.
- Live proof covers success, 401, 403, and revocation behavior.
</success_criteria>

<output>
Create `.planning/phases/06.2-callvault-rest-api/06.2-04-SUMMARY.md` when done.
</output>
