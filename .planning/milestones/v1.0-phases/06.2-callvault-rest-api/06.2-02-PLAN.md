---
phase: 06.2-callvault-rest-api
plan: 02
type: execute
wave: 1
depends_on:
  - 06.2-01
files_modified:
  - supabase/functions/callvault-api/index.ts
  - supabase/functions/callvault-api/auth.ts
  - supabase/functions/callvault-api/responses.ts
  - supabase/functions/callvault-api/routes/calls.ts
  - supabase/functions/callvault-api/routes/contacts.ts
  - supabase/functions/callvault-api/routes/workspaces.ts
  - supabase/functions/callvault-api/routes/speakers.ts
  - supabase/functions/callvault-api/__tests__/*.test.ts
autonomous: true
requirements:
  - Phase-06.2-SC-1
  - Phase-06.2-SC-2
  - Phase-06.2-SC-3
  - Phase-06.2-SC-4
  - Phase-06.2-SC-5
  - Phase-06.2-SC-6
  - Phase-06.2-SC-7
must_haves:
  truths:
    - "REST endpoints authenticate only `token_source = 'api'` bearer tokens."
    - "Every REST list endpoint returns JSON `{ data, pagination }`, never MCP markdown."
    - "Obsidian has enough REST coverage for live sync through calls list plus call detail/transcript route if needed."
  artifacts:
    - path: "supabase/functions/callvault-api/index.ts"
      provides: "canonical REST API Edge Function entry point and router"
    - path: "supabase/functions/callvault-api/auth.ts"
      provides: "API-token auth helper with 401/403 source boundary"
    - path: "supabase/functions/callvault-api/routes/*.ts"
      provides: "calls, contacts, workspaces, speakers, and minimal call detail/transcript handlers"
---

<objective>
Implement the canonical CallVault REST API Edge Function with scoped API-token auth and stable JSON response envelopes.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-callvault-rest-api/06.2-CONTEXT.md
@.planning/phases/06.2-callvault-rest-api/06.2-RESEARCH.md
@.planning/phases/06.2-callvault-rest-api/06.2-PATTERNS.md
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create REST API response and auth helpers</name>
  <files>supabase/functions/callvault-api/auth.ts, supabase/functions/callvault-api/responses.ts, supabase/functions/callvault-api/__tests__/auth.test.ts</files>
  <read_first>
    - supabase/functions/obsidian-sync/index.ts
    - supabase/functions/mcp-server/auth.ts
    - supabase/functions/_shared/cors.ts
    - supabase/functions/_shared/response.ts
    - .planning/phases/06.2-callvault-rest-api/06.2-CONTEXT.md
  </read_first>
  <action>Create local REST helpers for `callvault-api`: `jsonOk(data, pagination?)`, `jsonError(status, code, message)`, and `authenticateApiToken(req, supabase)`. The auth helper must parse case-insensitive bearer headers, return 401 for missing/malformed/unknown/revoked tokens, return 403 for rows whose `token_source` is not `api`, update `last_used_at` after success, and return token context with `id`, `user_id`, `org_id`, `workspace_id`, `scope`, and `name`. Add Vitest tests or source-level contract tests that prove 401 missing, 401 unknown, 403 wrong source, and success for API source.</action>
  <acceptance_criteria>
    - `auth.ts` contains `authenticateApiToken`.
    - `auth.ts` queries `mcp_tokens` and checks `token_source`.
    - Wrong-source tokens produce status 403.
    - Missing or unknown tokens produce status 401.
    - `responses.ts` can return list success with top-level `data` and `pagination`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "authenticateApiToken|token_source|403|pagination|jsonError" supabase/functions/callvault-api && npm run test -- --run supabase/functions/callvault-api/__tests__/auth.test.ts</automated>
  </verify>
  <done>REST auth and response primitives enforce the API token boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Implement REST resource routes</name>
  <files>supabase/functions/callvault-api/index.ts, supabase/functions/callvault-api/routes/calls.ts, supabase/functions/callvault-api/routes/contacts.ts, supabase/functions/callvault-api/routes/workspaces.ts, supabase/functions/callvault-api/routes/speakers.ts</files>
  <read_first>
    - supabase/functions/mcp-server/tools/read/list_calls.ts
    - supabase/functions/mcp-server/tools/read/list_contacts.ts
    - supabase/functions/mcp-server/tools/read/list_speakers.ts
    - supabase/functions/obsidian-sync/index.ts
    - src/hooks/useContacts.ts
    - src/lib/recording-ids.ts
    - src/lib/recording-source-url.ts
  </read_first>
  <action>Implement `GET /v1/workspaces`, `GET /v1/calls`, `GET /v1/contacts`, and `GET /v1/speakers` in `callvault-api`. Add `GET /v1/calls/{id}` or `GET /v1/calls/{id}/transcript` if needed to replace Obsidian live sync without `/obsidian-sync`; the minimum acceptable Obsidian route is one that returns a single accessible call with transcript text and participant metadata. Use token scope rules: workspace tokens are locked to their workspace, organization tokens can list allowed workspaces and accept optional `workspace_id` filters. Responses must be product-shaped JSON and must not include MCP `content`, JSON-RPC `jsonrpc`, or markdown-only envelopes.</action>
  <acceptance_criteria>
    - `index.ts` routes `GET` requests for `/v1/workspaces`, `/v1/calls`, `/v1/contacts`, and `/v1/speakers`.
    - List responses include top-level `data` and `pagination`.
    - Route code enforces workspace/org scope before returning recordings, contacts, or speakers.
    - Calls route reads through `workspace_entries` for workspace scoping.
    - No REST response helper emits MCP `content[].text`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "/v1/workspaces|/v1/calls|/v1/contacts|/v1/speakers|workspace_entries|pagination" supabase/functions/callvault-api && ! rg -n "content\\]|jsonrpc|mcpOk|mcpError" supabase/functions/callvault-api && npm run test -- --run supabase/functions/callvault-api/__tests__/*.test.ts</automated>
  </verify>
  <done>REST function serves the Phase 06.2 endpoint contract with scoped JSON handlers.</done>
</task>

<task type="auto">
  <name>Task 3: Add endpoint contract tests and Deno/source checks</name>
  <files>supabase/functions/callvault-api/__tests__/contract.test.ts</files>
  <read_first>
    - supabase/functions/mcp-server/__tests__/contract-surface.test.ts
    - supabase/functions/mcp-server/__tests__/workspace-scope.integration.test.ts
    - supabase/functions/callvault-api/index.ts
    - supabase/functions/callvault-api/auth.ts
  </read_first>
  <action>Add contract tests for all required routes. Tests must assert that route definitions exist, success response helpers produce `{ data, pagination }`, error helpers produce `{ error: { code, message } }`, and source text contains wrong-source 403 handling. Include a Deno check command in the summary/verification if the function imports Deno/esm dependencies that TypeScript build does not cover.</action>
  <acceptance_criteria>
    - `contract.test.ts` asserts all four required route paths.
    - Tests assert no MCP response shape is present in `callvault-api`.
    - Tests assert wrong-source behavior is covered.
    - `npm run test -- --run supabase/functions/callvault-api/__tests__/*.test.ts` exits 0 before closure.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run supabase/functions/callvault-api/__tests__/*.test.ts && deno check supabase/functions/callvault-api/index.ts</automated>
  </verify>
  <done>REST API contract is covered before proxy/deploy work.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06.2-04 | Spoofing | bearer token auth | mitigate | Enforce exact API token source and revocation checks before dispatch. |
| T-06.2-05 | Information Disclosure | REST endpoints | mitigate | Scope all calls/contacts/speakers through token org/workspace context. |
| T-06.2-06 | Tampering | response contract | mitigate | Use local response helpers and tests so REST cannot drift into MCP markdown/JSON-RPC. |
</threat_model>

<verification>
Run targeted callvault-api tests, `deno check supabase/functions/callvault-api/index.ts`, and `npm run build`.
</verification>

<success_criteria>
- The Edge Function implements all required `/v1` read endpoints.
- Auth behavior matches 401/403 success criteria.
- JSON envelopes match Phase 06.2 and are tested.
</success_criteria>

<output>
Create `.planning/phases/06.2-callvault-rest-api/06.2-02-SUMMARY.md` when done.
</output>
