---
phase: 09-lint-brand-and-documentation-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/organizations.service.ts
  - src/test/rls-regression.test.ts
  - .agent/get-shit-done/bin/lib/cjs-sdk-bridge.cjs
  - .agent/get-shit-done/bin/lib/phase-lifecycle.generated.cjs
  - .agent/get-shit-done/bin/lib/runtime-artifact-layout.cjs
  - .agent/get-shit-done/bin/lib/state.cjs
  - .gemini/get-shit-done/bin/lib/cjs-sdk-bridge.cjs
  - .gemini/get-shit-done/bin/lib/phase-lifecycle.generated.cjs
  - .gemini/get-shit-done/bin/lib/runtime-artifact-layout.cjs
  - .gemini/get-shit-done/bin/lib/state.cjs
autonomous: true
requirements:
  - Review follow-up from full-codebase audit 2026-06-09

must_haves:
  truths:
    - "Stale eslint-disable directives are removed from all targeted files"
    - "npm run lint warning count drops by ~20 from the 237 baseline"
    - "npm run type-check still exits 0 with no output"
    - "npm run build still produces built in X.XXs with no errors"
  artifacts:
    - path: "src/services/organizations.service.ts"
      provides: "No stale eslint-disable at line 47"
    - path: "src/test/rls-regression.test.ts"
      provides: "No stale no-console eslint-disable directives at lines 624,633,642,652,661,674,681"
  key_links:
    - from: "npm run lint -- --fix"
      to: "stale eslint-disable directives"
      via: "--fix flag auto-removes them"
      pattern: "eslint-disable"
---

<objective>
Run the auto-fix pass to eliminate all 20 stale `eslint-disable` directives. These are directives that disable rules that no longer trigger warnings at those locations, making the code noisy and misleading. The `--fix` flag removes them automatically.

Purpose: Drop ~20 warnings from the 237 baseline with zero logic risk. This is the safest and fastest lint reduction available.
Output: Cleaned source files with stale disable directives removed.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-lint-brand-and-documentation-hygiene/09-CONTEXT.md
@.planning/phases/09-lint-brand-and-documentation-hygiene/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run auto-fix and verify scope</name>
  <read_first>
    eslint.config.js
    src/services/organizations.service.ts (line 47 — stale no-explicit-any disable)
    src/test/rls-regression.test.ts (lines 620-685 — 7 stale no-console disables)
  </read_first>
  <action>
Run `npm run lint -- --fix` from the repo root. This removes all 20 stale `eslint-disable` directives automatically per D-01.

The stale directives are concentrated in:
- `.agent/get-shit-done/bin/lib/cjs-sdk-bridge.cjs` lines 84, 90 (global-require)
- `.agent/get-shit-done/bin/lib/phase-lifecycle.generated.cjs` line 63 (no-cond-assign)
- `.agent/get-shit-done/bin/lib/runtime-artifact-layout.cjs` line 35 (global-require)
- `.agent/get-shit-done/bin/lib/state.cjs` lines 889, 944 (no-constant-condition)
- `.gemini/get-shit-done/bin/lib/` — same 6 warnings (mirror of .agent/)
- `src/services/organizations.service.ts` line 47 (no-explicit-any stale disable)
- `src/test/rls-regression.test.ts` lines 624, 633, 642, 652, 661, 674, 681 (7x no-console)

IMPORTANT: If `--fix` errors on the `.agent/` or `.gemini/` generated CJS files (eslint may refuse to fix CJS in the ignore list — these are in `.planning/**` scope), run the scoped variant instead: `npm run lint -- --fix src/ cloudflare/` and then manually remove the CJS stale directives by editing each file. The `.agent/` and `.gemini/` files ARE covered by eslint (they are not in the ignores list in eslint.config.js — only `.planning/**` and `.claude/**` are ignored; verify the actual ignore path before touching CJS files).

After the fix run, immediately verify the diff with `git diff --stat` to confirm only directive-line changes, not logic changes.
  </action>
  <verify>
    <automated>git diff --stat | grep -v "^$" && npm run lint 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat` shows only the targeted files changed
    - `npm run lint 2>&1 | tail -1` shows fewer than 220 warnings (down from 237 — 17+ removed)
    - No file has logic changes (only eslint-disable comment lines removed)
  </acceptance_criteria>
  <done>
    - `git diff --stat` shows only the targeted files changed
    - `npm run lint 2>&1 | tail -1` shows fewer than 220 warnings (down from 237 — 17+ removed)
    - No file has logic changes (only eslint-disable comment lines removed)
  </done>
</task>

<task type="auto">
  <name>Task 2: Final gate — type-check and build unchanged</name>
  <read_first>
    package.json (scripts block — confirm type-check and build commands)
  </read_first>
  <action>
Run `npm run type-check` and confirm it exits 0 with no output. Run `npm run build 2>&1 | tail -5` and confirm it ends with "built in X.XXs" with no error lines.

The auto-fix only removes comment-line directives. It cannot break types or build. This task is a regression gate, not a recovery task. If either command fails, the failure is pre-existing (baseline was clean per RESEARCH.md) and must be investigated before proceeding.
  </action>
  <verify>
    <automated>npm run type-check && npm run build 2>&1 | grep -E "built in|error" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` exits 0 with no output
    - `npm run build` output ends with "built in X.XXs" and contains no "error" lines
    - Both gates match the pre-phase baseline documented in RESEARCH.md
  </acceptance_criteria>
  <done>
    - `npm run type-check` exits 0 with no output
    - `npm run build` output ends with "built in X.XXs" and contains no "error" lines
    - Both gates match the pre-phase baseline documented in RESEARCH.md
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eslint --fix → source files | ESLint writes directly to source; scope must be verified to confirm no logic modifications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-SC | Tampering | eslint --fix scope | mitigate | Run git diff --stat immediately after fix; inspect any file with more than comment-line changes before committing |
</threat_model>

<verification>
After task 2 passes:
- `npm run lint 2>&1 | tail -1` shows warning count materially below 237
- `git diff --stat` shows only comment-line removals
- `npm run type-check` exits 0
- `npm run build` exits clean
</verification>

<success_criteria>
Stale eslint-disable directives removed. Warning count drops by ~17-20 from 237 baseline. Build and type-check unchanged.
</success_criteria>

<output>
Create `.planning/phases/09-lint-brand-and-documentation-hygiene/09-01-SUMMARY.md` when done
</output>
