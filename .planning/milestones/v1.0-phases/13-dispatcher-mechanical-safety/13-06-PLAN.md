---
phase: 13-dispatcher-mechanical-safety
plan: 06
type: execute
wave: 3
depends_on: [13-03, 13-04]
files_modified: [~/dev/autopilot/src/claimer.ts, ~/dev/autopilot/launchd/com.callvault.autopilot.plist, ~/dev/autopilot/src/lib/approval.ts, ~/dev/autopilot/src/lib/approval.test.ts]
autonomous: true
requirements: [AUTO-01, AUTO-03, AUTO-04, AUTO-06]
must_haves:
  truths:
    - "launchd fires the claimer on schedule; each cycle: heartbeat → kill-switch check → stale sweep → approval-merge pass → claim next ticket (urgent lane honored) → run, at concurrency 1"
    - "Kill switch (DB or flag file) set → the cycle heartbeats but claims nothing, within one poll cycle"
    - "An explicit admin-authored approval event on an awaiting_approval ticket triggers gate re-run → merge fix branch to main → push → deploy-SHA verification appended to the evidence"
    - "A NULL-actor or non-admin event NEVER triggers a merge"
    - "Quiet hours and max-runs-per-window from config suppress new claims (approval-merges still allowed)"
  artifacts:
    - path: "~/dev/autopilot/src/claimer.ts"
      provides: "Poll-cycle orchestration (entry point)"
    - path: "~/dev/autopilot/src/lib/approval.ts"
      provides: "Approval-event recognition + merge/push + deploy-SHA verify"
    - path: "~/dev/autopilot/launchd/com.callvault.autopilot.plist"
      provides: "Dispatcher schedule"
  key_links:
    - from: "~/dev/autopilot/src/claimer.ts"
      to: "~/dev/autopilot/gate/push-gate.sh"
      via: "exit-code check before any merge/push"
      pattern: "push-gate"
    - from: "~/dev/autopilot/src/lib/approval.ts"
      to: "ticket_events"
      via: "admin actor_id verification against user_roles"
      pattern: "actor_id"
---

<objective>
Wire the daemon together: the claimer poll loop (heartbeat, kill switch, stale sweep, urgent-lane claim, budget guards), the approval-merge path (admin approval event → gate re-run → merge/push → deploy-SHA verify), and the dispatcher launchd job.

Purpose: Completes AUTO-01/04 and the approval/deploy halves of AUTO-03/06. Implements CONTEXT decisions: v1 ships NOTHING autonomously — merge happens only on an explicit admin-authored approval row in ticket_events (ISA ISC-66); urgent lane skips cooldown but never kills in-flight work; subscription budget (quiet hours + max-runs-per-window, concurrency 1); deploy-SHA check after any merge (ISC-112). MVP slice: after this plan the full loop is live — Andrew approves a held fix and watches it merge, push, deploy, and verify with zero manual git work.
Output: claimer.ts + approval.ts + dispatcher plist at ~/dev/autopilot/ (OUTSIDE repo), job loaded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dispatcher-mechanical-safety/13-CONTEXT.md
@.planning/phases/13-dispatcher-mechanical-safety/13-RESEARCH.md
@.planning/phases/13-dispatcher-mechanical-safety/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: claimer.ts poll cycle</name>
  <files>~/dev/autopilot/src/claimer.ts</files>
  <action>
Single-pass entry point (launchd schedules; no internal infinite loop — spike model). Guards at entry per spike L54-77 rebind: whoami == admin, ~/dev/autopilot + clone present, config parseable; mkdir lockdir ~/dev/autopilot/.dispatcher.lock with trap-equivalent cleanup (concurrency 1 across overlapping fires). Cycle order:
1. updateRunnerState heartbeat (last_heartbeat=now, status per stage) — FIRST, so the watchdog sees life even when later steps fail.
2. Kill-switch check (DB kill_switch OR flag file) → if set, updateRunnerState({status:'idle', last_result:'kill switch'}), exit 0 (claims halt within one poll cycle, AUTO-04).
3. sweepStaleClaims() (plan 02 lib).
4. Approval-merge pass (Task 2's approval.ts) — runs BEFORE new claims so approved work ships promptly and is exempt from quiet hours.
5. Budget guards: inside quiet hours OR run-count in window >= maxRunsPerWindow (count from the JSONL log) → exit 0 with last_result noting the suppression.
6. selectNextTicket() → claimTicket() → run the plan-03 runner pipeline in-process. After the run, if needsImmediateNext() reports an urgent head-of-queue, loop once more in the same invocation (urgent lane skips inter-run cooldown — locked; in-flight work was never killed, it just completed).
7. updateRunnerState({status:'idle', last_result: outcome}).
  </action>
  <verify>
    <automated>cd ~/dev/autopilot && bunx tsc --noEmit && bun run src/claimer.ts --dry-run 2>&1 | grep -iE 'heartbeat|kill|idle' | head -1</automated>
  </verify>
  <done>One invocation executes the seven-step cycle; --dry-run flag prints decisions without DB writes beyond heartbeat; kill switch short-circuits after heartbeat; lockdir prevents overlap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: approval.ts — admin approval recognition + merge + deploy-SHA verify</name>
  <files>~/dev/autopilot/src/lib/approval.ts, ~/dev/autopilot/src/lib/approval.test.ts</files>
  <behavior>
    - Test 1: event {event_type:'approval', actor_id: adminUserId} on an awaiting_approval ticket → qualifies.
    - Test 2: actor_id NULL (trigger/service-role rows) → NEVER qualifies (mocked).
    - Test 3: actor_id of a non-admin user → does not qualify (role check consulted).
    - Test 4: 'rejection' event → branch deleted on origin, reason posted to thread, ticket status → rejected.
  </behavior>
  <action>
findApprovals(): query ticket_events for event_type IN ('approval','rejection') on tickets with status='awaiting_approval', newest per ticket; verify actor_id is non-NULL AND resolves to an ADMIN via a service-role query against the same role table public.has_role uses (read the live user_roles/has_role definition first to target the right table — verify, don't assume). Phase 14 builds the UI that writes these events; until then Andrew can author one via SQL as an authenticated admin (which the existing append-only policy set requires going through service-role or an admin-capable path — document the exact SQL in the SUMMARY for the E2E plan; if no authenticated INSERT path exists for admin approval events — research shows ticket_events INSERT is service-role/trigger-only — the documented v1 mechanism is a service-role INSERT executed by Andrew, with actor_id explicitly set to his admin user id; the recognition logic stays identical when Phase 14 adds an Edge-Function path).
executeApproval(ticket): re-run push-gate.sh against the held branch (kill-switch + denylist + commit-advance re-checked immediately pre-push — ISC-108; NO PUSH_GATE_SKIP_DB); on exit 0: fetch, `git merge --ff-only` the fix branch onto main in the clone (ff-only keeps commit-advance semantics; if ff fails because main moved, rebase the single fix commit and re-run the FULL gate + in-worktree verification before retrying once, else escalate), push main, then deploy-SHA verification (ISC-112, RESEARCH Open Question 1): poll the production deploy for the new commit SHA — discovery sub-step: check for an existing version/commit exposure (e.g. build meta, /version endpoint, deploy provider CLI); if none exists, verify via the deploy provider's API/CLI for the production deployment's source SHA; append the result to the ticket evidence via writeAgentMessage and set status → resolved. Gate exit 1/2 → post the gate output to the thread, leave status awaiting_approval, page via user_notifications.
  </action>
  <verify>
    <automated>cd ~/dev/autopilot && bun test src/lib/approval.test.ts</automated>
  </verify>
  <done>Four behaviors green; merge path is ff-only with full gate re-run; deploy-SHA result lands in evidence; no NULL-actor merge path exists.</done>
</task>

<task type="auto">
  <name>Task 3: Dispatcher launchd plist + load</name>
  <files>~/dev/autopilot/launchd/com.callvault.autopilot.plist</files>
  <action>
Per the proven spike plist (13-PATTERNS.md): Label com.callvault.autopilot, ProgramArguments [/Users/admin/.bun/bin/bun, /Users/admin/dev/autopilot/src/claimer.ts], StartInterval = config.pollIntervalSec (300), RunAtLoad true, WorkingDirectory /Users/admin/dev/autopilot, EnvironmentVariables PATH = /Users/admin/.local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin (claude + bun + git resolvable — RESEARCH Pitfall 3), StandardOut/ErrorPath under ~/dev/autopilot/logs/. cp to ~/Library/LaunchAgents/, `launchctl load`. Confirm a launchd-fired cycle ran by watching runner_state.last_heartbeat advance and the launchd out log. Keep the kill switch ON (runner_state.kill_switch=true) at the end of this plan so no real ticket is claimed before the E2E proof plan deliberately starts one (controlled first run).
  </action>
  <verify>
    <automated>launchctl list | grep com.callvault.autopilot && plutil -lint ~/dev/autopilot/launchd/com.callvault.autopilot.plist</automated>
  </verify>
  <done>Job loaded; a launchd-fired cycle heartbeated; kill switch left ON pending E2E.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket_events rows → merge authority | only a verified admin-authored approval may trigger merge |
| launchd schedule → claim volume | budget guards bound subscription spend |
| clone main push → production | auto-deploy from main makes the merge a deploy action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-21 | Spoofing | forged/NULL-actor approval event | mitigate | actor_id non-NULL + ADMIN role verification; tests 1-3 (RESEARCH Pitfall 7) |
| T-13-22 | Elevation of privilege | merge bypassing the gate | mitigate | executeApproval re-runs push-gate.sh pre-push, live DB kill check, no skip hook |
| T-13-23 | Tampering | main moved under the held branch | mitigate | ff-only merge; rebase path re-runs full gate + verification once, else escalate |
| T-13-24 | DoS | subscription exhaustion | mitigate | Quiet hours + maxRunsPerWindow + concurrency 1 (locked Ops decision) |
| T-13-25 | Repudiation | deploy without proof | mitigate | Deploy-SHA verification appended to evidence (ISC-112) |
| T-13-SC | Tampering | installs | accept | No new packages |
</threat_model>

<verification>
- `bun test` green; launchd-fired heartbeat observed
- Kill-switch drill: switch ON → fired cycle claims nothing; switch OFF → (deferred to E2E plan)
</verification>

<success_criteria>
- Full poll cycle runs from launchd at concurrency 1 with budget guards
- Approval-merge path proven safe by unit tests; live exercise deferred to the E2E proof
</success_criteria>

<output>
Create `.planning/phases/13-dispatcher-mechanical-safety/13-06-SUMMARY.md` when done
</output>
