---
phase: 13-dispatcher-mechanical-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [~/dev/autopilot/package.json, ~/dev/autopilot/tsconfig.json, ~/dev/autopilot/autopilot.config.ts, ~/dev/autopilot/.env.example, ~/dev/autopilot/src/lib/db.ts, ~/dev/autopilot/src/lib/claim.ts, ~/dev/autopilot/src/lib/agent.ts, ~/dev/autopilot/src/lib/claim.test.ts, ~/dev/autopilot/src/lib/agent.test.ts]
autonomous: true
requirements: [AUTO-01]
must_haves:
  truths:
    - "~/dev/autopilot/ exists as a bun+TS project where `bun test` runs green"
    - "Claim ordering is urgent DESC, priority DESC, severity rank (critical>high>medium>low), created_at ASC — proven by a unit test"
    - "Two concurrent claim attempts on the same ticket yield exactly one winner (conditional UPDATE status guard)"
    - "Released claims get next_attempt_at = now + 15min × 4^attempts; stale in_progress claims older than TTL are swept back to new"
    - "Only allowlisted binaries with regex-validated flags can ever be spawned as agent commands"
  artifacts:
    - path: "~/dev/autopilot/src/lib/claim.ts"
      provides: "Atomic claim, selection ordering, backoff, stale sweep"
    - path: "~/dev/autopilot/src/lib/agent.ts"
      provides: "Argv-allowlist agent spawn (ported from dead-branch runAgent)"
    - path: "~/dev/autopilot/src/lib/db.ts"
      provides: "Service-role client + typed writers (runner_state, ticket_events, ticket_messages)"
    - path: "~/dev/autopilot/autopilot.config.ts"
      provides: "Poll interval, quiet hours, max-runs-per-window, paths, watchdog budget, max attempts"
  key_links:
    - from: "~/dev/autopilot/src/lib/claim.ts"
      to: "public.tickets"
      via: "conditional UPDATE eq(status,'new')"
      pattern: "eq\\(.status., .new.\\)"
---

<objective>
Scaffold the daemon pack at `~/dev/autopilot/` (OUTSIDE this repo — bun+TS per locked decision "Daemon pack") and build its core libraries: service-role DB client, atomic claim with the locked queue ordering and urgent lane, exponential backoff + stale-claim sweep, and the argv-allowlist agent spawner. Unit-tested with bun test.

Purpose: These are the contracts every other daemon plan builds against (interface-first). Implements CONTEXT decisions: claim = conditional UPDATE + attempts + next_attempt_at backoff (15min × 4^attempts) + stale sweep; urgent lane; argv-allowlist (steal-list #1). MVP slice: after this plan, Andrew can run `bun test` in ~/dev/autopilot and see the claim mechanics proven, and run a one-shot claim against the live queue.
Output: Working `~/dev/autopilot/` scaffold with tested core libs. NOTE FOR EXECUTOR: all daemon files live OUTSIDE the repo at ~/dev/autopilot/; only planning docs live in this repo. Commit daemon files to nothing (no git repo there yet) — `git init` the directory as its own repo in Task 1 so work is versioned, but never push it into brain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dispatcher-mechanical-safety/13-CONTEXT.md
@.planning/phases/13-dispatcher-mechanical-safety/13-RESEARCH.md
@.planning/phases/13-dispatcher-mechanical-safety/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ~/dev/autopilot (bun+TS) with config module</name>
  <files>~/dev/autopilot/package.json, ~/dev/autopilot/tsconfig.json, ~/dev/autopilot/autopilot.config.ts, ~/dev/autopilot/.env.example</files>
  <action>
Create `~/dev/autopilot/` (does not exist — verified in research): `git init`, `bun init`-equivalent package.json (private, type module), deps `@supabase/supabase-js` + `dotenv` only (both pre-approved in the RESEARCH Package Legitimacy Audit — existing repo dependencies, no new package names). tsconfig strict. `.env.example` documenting SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY (real `.env` chmod 600, never committed — add .gitignore). `autopilot.config.ts` exports a typed config object (per Claude's-discretion area "config file shape"): pollIntervalSec (default 300), watchdogBudgetSec (2400 — locked), maxAttempts (4), staleClaimTtlSec (watchdogBudgetSec + 600), quietHours ({start: "01:00", end: "07:00"} default), maxRunsPerWindow ({windowHours: 24, maxRuns: 12} default), concurrency 1 (locked), paths (cloneDir, worktreeBaseDir, logsDir, killSwitchFlagFile ~/dev/autopilot/KILL), agentCommand "claude -p", denylistFile path, repoRemote (brain origin URL). Document every knob with a comment.
  </action>
  <verify>
    <automated>cd ~/dev/autopilot && bun install && bunx tsc --noEmit</automated>
  </verify>
  <done>~/dev/autopilot exists, bun install clean, tsc --noEmit green, .env.example present, config typed and documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Core libs — db client, claim/backoff/sweep with urgent lane</name>
  <files>~/dev/autopilot/src/lib/db.ts, ~/dev/autopilot/src/lib/claim.ts, ~/dev/autopilot/src/lib/claim.test.ts</files>
  <behavior>
    - Test 1 (ordering): given candidate tickets [{urgent:false,priority:5,severity:'critical'},{urgent:true,priority:0,severity:'low'},{urgent:false,priority:5,severity:'high',older created_at}], selection returns the urgent one first; among non-urgent equal-priority, critical beats high; ties break oldest-first. Severity ranked via explicit map {critical:4,high:3,medium:2,low:1} — NEVER enum/string order (RESEARCH Pitfall 1).
    - Test 2 (atomic claim): claim() issues UPDATE with .eq('id',x).eq('status','new') and returns null when zero rows return (lost race) — assert via a mocked supabase client that the status guard is present and the loser path returns null.
    - Test 3 (backoff): nextAttemptAt(0)=+15min, nextAttemptAt(2)=+240min (15 × 4^attempts, dead-branch formula).
    - Test 4 (stale sweep): sweepStaleClaims() updates tickets where status='in_progress' AND updated_at older than staleClaimTtlSec back to status='new' without touching attempts (re-queue, ISC-38).
  </behavior>
  <action>
db.ts: createClient(url, serviceKey, {auth:{autoRefreshToken:false,persistSession:false}}) per dead-branch pattern; export typed helpers writeEvent(ticketId, eventType, oldValue?, newValue?) targeting LIVE ticket_events columns (ticket_id, actor_id NULL, event_type, old_value, new_value — REBIND from dead branch's type/payload shape per 13-PATTERNS.md), writeAgentMessage(ticketId, body) → ticket_messages author_type='agent', and updateRunnerState(partial) → runner_state id=1.
claim.ts: selectNextTicket() fetches status='new' candidates with attempts < maxAttempts and (next_attempt_at null or past), sorts client-side by the locked order (urgent DESC, priority DESC, severity rank DESC, created_at ASC — concurrency 1 makes client-side sort safe; the conditional UPDATE is the atomicity boundary); claimTicket(ticket) does the conditional UPDATE claiming to 'in_progress' with attempts+1; releaseClaim(ticketId, attempts, reason) sets status back per outcome with next_attempt_at backoff and posts the reason via writeEvent; sweepStaleClaims() per Test 4. Urgent-lane semantics (locked): never kill an in-flight run; the lane is expressed purely through ordering plus skipping any inter-run cooldown when the head of queue is urgent — expose needsImmediateNext(ticket) so the claimer loop (plan 06) can skip cooldown.
  </action>
  <verify>
    <automated>cd ~/dev/autopilot && bun test src/lib/claim.test.ts</automated>
  </verify>
  <done>All four behaviors pass under bun test with a mocked client; severity ranking is map-based; live-schema column names used throughout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Argv-allowlist agent spawner (port dead-branch runAgent)</name>
  <files>~/dev/autopilot/src/lib/agent.ts, ~/dev/autopilot/src/lib/agent.test.ts</files>
  <behavior>
    - Test 1: binary not in allowlist {claude, codex} → throws before any spawn.
    - Test 2: flag failing /^[-a-zA-Z0-9_=.@/]+$/ (e.g. `--foo;rm`) → throws.
    - Test 3: valid command tokenizes and produces an argv array ending with the brief as final positional arg — no shell string ever constructed.
    - Test 4: spawn env omits CLAUDECODE (nested-session block) and ANTHROPIC_API_KEY is not injected (ISC-31).
  </behavior>
  <action>
Port runAgent from the dead branch verbatim in logic (13-PATTERNS.md has the excerpt): tokenize config.agentCommand, allowlist Set(["claude","codex"]) (drop gemini — not installed), validate every flag against AGENT_ARG_PATTERN, spawn via Bun.spawn(argv, {cwd, env: {...process.env, CLAUDECODE: undefined}, stdout/stderr to transcript file}) with a manual kill timer: SIGTERM at config.watchdogBudgetSec, SIGKILL 10s later (spike protocol — Bun.spawn timeout semantics treated as unverified per RESEARCH assumption A3, so implement the manual timer). Export runAgent(brief, cwd, transcriptPath) returning {exitCode, killedByWatchdog}. Append `--dangerously-skip-permissions` only when binary is claude.
  </action>
  <verify>
    <automated>cd ~/dev/autopilot && bun test src/lib/agent.test.ts</automated>
  </verify>
  <done>All four behaviors pass; no code path builds a shell string from config/DB-sourced text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autopilot.config.ts / DB-sourced strings → subprocess spawn | configurable agent command could smuggle shell |
| ticket text → agent brief | hostile ticket content is untrusted DATA |
| ~/dev/autopilot/.env | service-role key at rest on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | agent command spawn | mitigate | Argv allowlist + flag regex + Bun.spawn array (Task 3); no shell strings for non-literal input |
| T-13-06 | Elevation of privilege | nested claude session | mitigate | CLAUDECODE stripped from spawn env (Task 3 Test 4) |
| T-13-07 | Information disclosure | service-role key | mitigate | .env chmod 600, .gitignore'd, never in repo (ISC-78); .env.example carries names only |
| T-13-08 | DoS | claim loop hammering DB | mitigate | pollIntervalSec floor + maxAttempts + backoff caps retries |
| T-13-SC | Tampering | bun installs | accept | Only @supabase/supabase-js + dotenv — pre-approved in RESEARCH Package Legitimacy Audit, no new package names |
</threat_model>

<verification>
- `cd ~/dev/autopilot && bun test` green (all lib tests)
- `bunx tsc --noEmit` green
- `rg -n "ANTHROPIC_API_KEY" ~/dev/autopilot/src` returns nothing live (ISC-31)
</verification>

<success_criteria>
- ~/dev/autopilot scaffolded, its own git repo, bun test green
- Claim ordering/atomicity/backoff/sweep and agent allowlist each proven by a named unit test
</success_criteria>

<output>
Create `.planning/phases/13-dispatcher-mechanical-safety/13-02-SUMMARY.md` when done
</output>
