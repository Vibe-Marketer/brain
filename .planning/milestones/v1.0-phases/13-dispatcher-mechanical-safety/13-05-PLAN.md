---
phase: 13-dispatcher-mechanical-safety
plan: 05
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified: [~/dev/autopilot/src/watchdog.ts, ~/dev/autopilot/src/watchdog.test.ts, ~/dev/autopilot/launchd/com.callvault.autopilot-watchdog.plist]
autonomous: true
requirements: [AUTO-05]
must_haves:
  truths:
    - "A separate launchd job (not the dispatcher) reads runner_state.last_heartbeat and pages when it is staler than the threshold"
    - "Paging = user_notifications INSERT (service-role) + local macOS notification via osascript"
    - "The watchdog also runs /Users/admin/dev/autopilot-tools-health.sh and reports failures as tickets"
    - "DB unreachable from the watchdog is itself a paging condition (no silent pass)"
  artifacts:
    - path: "~/dev/autopilot/src/watchdog.ts"
      provides: "Independent heartbeat monitor + pager (ISC-109)"
    - path: "~/dev/autopilot/launchd/com.callvault.autopilot-watchdog.plist"
      provides: "Separate launchd schedule for the watchdog"
  key_links:
    - from: "~/dev/autopilot/src/watchdog.ts"
      to: "runner_state.last_heartbeat"
      via: "service-role SELECT"
      pattern: "last_heartbeat"
---

<objective>
Build the independent watchdog: separate launchd job that pages Andrew when the dispatcher heartbeat goes stale, runs the toolchain health probe, and reports failures as tickets — the dispatcher never monitors itself.

Purpose: AUTO-05 / ISC-109. Implements CONTEXT Ops decisions: watchdog as separate launchd job; pages via user_notifications INSERT + macOS notification; runs /Users/admin/dev/autopilot-tools-health.sh and reports failures as tickets. MVP slice: after this plan, Andrew gets paged within the threshold when the dispatcher dies — observable by killing it.
Output: watchdog.ts + its own plist at ~/dev/autopilot/ (OUTSIDE repo). No analog exists (dead branch self-reported) — built from the ISC-109 contract per 13-PATTERNS.md "No Analog Found".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dispatcher-mechanical-safety/13-CONTEXT.md
@.planning/phases/13-dispatcher-mechanical-safety/13-RESEARCH.md
@.planning/phases/13-dispatcher-mechanical-safety/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: watchdog.ts — staleness check, paging, tools-health</name>
  <files>~/dev/autopilot/src/watchdog.ts, ~/dev/autopilot/src/watchdog.test.ts</files>
  <behavior>
    - Test 1: heartbeat 1s fresher than threshold → no page.
    - Test 2: heartbeat staler than threshold (2 × pollIntervalSec + watchdogBudgetSec margin, configurable) → page() invoked once with a message naming the staleness age.
    - Test 3: DB read throws/unreachable → page() invoked with "watchdog cannot reach DB" (no silent pass).
    - Test 4: paging is rate-limited — a state file (~/dev/autopilot/logs/.watchdog-last-page) suppresses repeat pages within a cooldown window so a dead weekend dispatcher does not fire hundreds of notifications.
  </behavior>
  <action>
Single-pass program (launchd provides the schedule — no internal loop). Steps: read runner_state via lib/db.ts; compute staleness vs config threshold; if stale or unreachable → page. page() does BOTH: (a) INSERT into user_notifications via service-role (inspect the live user_notifications schema first with a service-role select limit 1 to match required columns — read, don't assume) targeting Andrew's admin user id from config; (b) local macOS notification via Bun.spawn argv ["osascript","-e", "display notification \"<msg>\" with title \"Autopilot watchdog\""]. Then run /Users/admin/dev/autopilot-tools-health.sh (exists, one line per check, non-zero exit on any failure — verified in research); on non-zero exit, create a ticket via service-role INSERT into tickets (type 'bug', severity 'high', source 'manual', reporter_id = Andrew's admin user id from config, context noting watchdog origin + failing check lines) — dedupe by checking for an existing open watchdog ticket with the same failing-check fingerprint in context before inserting.
  </action>
  <verify>
    <automated>cd ~/dev/autopilot && bun test src/watchdog.test.ts</automated>
  </verify>
  <done>Four behaviors green; paging hits both channels; tools-health failures become deduped tickets; no self-monitoring (watchdog never writes last_heartbeat).</done>
</task>

<task type="auto">
  <name>Task 2: Watchdog launchd plist + load + live staleness drill</name>
  <files>~/dev/autopilot/launchd/com.callvault.autopilot-watchdog.plist</files>
  <action>
Plist per the proven spike template (13-PATTERNS.md excerpt — explicit PATH including ~/.local/bin and /opt/homebrew/bin, StandardOut/ErrorPath under ~/dev/autopilot/logs/): Label com.callvault.autopilot-watchdog, ProgramArguments [/Users/admin/.bun/bin/bun, /Users/admin/dev/autopilot/src/watchdog.ts], StartInterval 300, RunAtLoad true, WorkingDirectory /Users/admin/dev/autopilot. Separate plist from the dispatcher (one schedule per agent — dead-branch plist note). Install: cp to ~/Library/LaunchAgents/ and `launchctl load`. Live drill: with the dispatcher not yet running (it doesn't exist until plan 06), runner_state.last_heartbeat is NULL/old — confirm the watchdog fires within one interval: user_notifications row appears and the osascript notification posts; then confirm cooldown suppresses an immediate second page.
  </action>
  <verify>
    <automated>launchctl list | grep com.callvault.autopilot-watchdog && plutil -lint ~/dev/autopilot/launchd/com.callvault.autopilot-watchdog.plist</automated>
  </verify>
  <done>Plist lints, job loaded, live drill produced exactly one page (both channels) with cooldown verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| watchdog → user_notifications/tickets | service-role writes from a scheduled job |
| launchd env → watchdog | minimal PATH; secrets via ~/dev/autopilot/.env only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-18 | Repudiation | dispatcher dies silently | mitigate | Independent job + DB-unreachable-pages-too (fail loud) |
| T-13-19 | DoS | notification storm from dead dispatcher | mitigate | Cooldown state file (Test 4) |
| T-13-20 | Spoofing | watchdog-created tickets flooding queue | mitigate | Fingerprint dedupe before INSERT; severity fixed at high, never urgent |
| T-13-SC | Tampering | installs | accept | No new packages |
</threat_model>

<verification>
- `bun test src/watchdog.test.ts` green
- `launchctl list` shows the job; live drill: kill heartbeat → page within one interval, second page suppressed
</verification>

<success_criteria>
- Stale heartbeat or unreachable DB pages Andrew on both channels within threshold, exactly once per cooldown window
- Tools-health failures surface as deduped tickets
</success_criteria>

<output>
Create `.planning/phases/13-dispatcher-mechanical-safety/13-05-SUMMARY.md` when done
</output>
