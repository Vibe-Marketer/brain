---
phase: "08.1"
plan: "04"
type: summary
status: complete
completed_at: "2026-06-10"
production_commit: "5cbf43e0"
requirements:
  - CON-02
  - HRD-01 follow-up
---

# Plan 04 Summary: Structured Reads for Exports, REST, and MCP

## What Changed

- Added shared frontend formatting from stored transcript segments to export-safe transcript text.
- Updated PDF, DOCX, TXT, JSON, ZIP, markdown, Obsidian, folder/tag, LLM context, narrative, and analysis exports to prefer structured transcript text.
- Updated REST call detail to select and return `transcript_segments`, while preserving the `transcript` compatibility text field.
- Updated MCP `get_transcript` and `get_recording_context` to select `transcript_segments` and format them as markdown text.
- Added regression tests for Obsidian structured-first export, REST call detail contract, MCP markdown response shape, and frontend segment formatting.

## Commits

| Commit | Type | Description |
| --- | --- | --- |
| `5cbf43e0` | production/test | Read structured transcripts in exports, REST, and MCP |

## Files Changed

- `src/lib/transcriptUtils.ts`
- `src/lib/export-utils.ts`
- `src/lib/export-utils-advanced.ts`
- `supabase/functions/callvault-api/routes/calls.ts`
- `supabase/functions/mcp-server/tools/read/get_transcript.ts`
- `supabase/functions/mcp-server/tools/read/get_recording_context.ts`
- Related export/API/MCP tests

## Notes

- `src/services/api-tokens.service.ts` already selected and mapped `transcript_segments` in the current branch before the Plan 04 commit; this was verified by the `api-tokens.service.test.ts` run.
- MCP responses still return `content[].text` markdown through `mcpOk(id, string)`. No JSON payload response shape was introduced.

## Verification

```bash
npx vitest run src/lib/__tests__/transcriptUtils.test.ts src/lib/__tests__/export-utils.obsidian.test.ts src/services/__tests__/api-tokens.service.test.ts supabase/functions/mcp-server/__tests__/golden-replay.test.ts supabase/functions/callvault-api/__tests__/contract.test.ts
```

Result: passed, 5 test files, 62 tests.

```bash
npm run type-check
```

Result: passed.

## Self-Check

PASSED

- Exports prefer structured transcript segments when present.
- Obsidian export emits structured transcript bodies and keeps full-transcript fallback.
- REST call detail exposes `transcript_segments` and preserves `transcript`.
- MCP read tools format structured segments as markdown text.
- Existing full-transcript-only recordings still export and read correctly.
