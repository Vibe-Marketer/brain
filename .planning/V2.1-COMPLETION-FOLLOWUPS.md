# v2.1 Import/Sync Rebuild — Milestone-Completion Follow-ups

Operational items surfaced during autonomous build that require Andrew/dashboard action at milestone end (NOT code defects):

## 1. Frontend prod deploy (batched — awaiting Andrew's review)
All Phase 25/26/27/28 FRONTEND changes are committed on local `main` but NOT pushed to `origin/main` (which auto-deploys to app.callvaultai.com). Held for Andrew's review before customers (esp. John/Clickable, who actively use import) see the new ImportSurface. Backend (DB migrations, edge fns, reaper/sync-all crons) already deployed per-phase. To ship: push `main` → Vercel deploys frontend.

## 2. Phase 28 resume cron GUC (prod dashboard, one-time)
The `sync-all-resume-heartbeat` pg_cron is active + prod-ref-free on PROD (jobid 8) but no-ops until the per-env GUC is set (pooler user gets permission-denied). Set via Supabase dashboard SQL editor (superuser):
  ALTER DATABASE postgres SET app.supabase_url = 'https://vltmrnjsubfzrgrtdqey.supabase.co';
Same one-time step the existing fathom-daily-reconcile cron needs. Self-chain resume (primary) + Phase 27 reaper work without it; this only activates the belt-and-suspenders backstop.

## 3. Phase 28 live provider end-to-end proof (post-ship)
SYNC-01 resume + SYNC-03 idempotency are proven on the real TEST DB provider-agnostically, but a live provider-backed multi-slice backfill (e.g. sync 200 real Fathom calls) was not exercised — TEST project has no provider credentials. This gets exercised the first time a real account clicks "Sync all" after the frontend ships. Watch the first real sync-all in the Phase 27 observable-jobs surface.
