# v2.1 Import/Sync Rebuild — Milestone-Completion Follow-ups

Operational items surfaced during autonomous build that require Andrew/dashboard action at milestone end (NOT code defects):

## 1. Frontend prod deploy — RESOLVED (2026-07-21)
All Phase 25/26/27/28/29 frontend changes are pushed to `origin/main` and live in production at app.callvaultai.com (confirmed `HEAD == origin/main == dcad22db` at milestone close, 2026-07-21). Backend (DB migrations, edge fns, reaper/sync-all crons) was already deployed per-phase.

## 2. Phase 28 resume cron GUC (prod dashboard, one-time) — OPEN, accepted follow-up
The `sync-all-resume-heartbeat` pg_cron is active + prod-ref-free on PROD (jobid 8) but no-ops until the per-env GUC is set (pooler user gets permission-denied). Set via Supabase dashboard SQL editor (superuser):
  ALTER DATABASE postgres SET app.supabase_url = 'https://vltmrnjsubfzrgrtdqey.supabase.co';
Same one-time step the existing fathom-daily-reconcile cron needs. Self-chain resume (primary) + Phase 27 reaper work without it; this only activates the belt-and-suspenders backstop. **Requires Andrew's Supabase dashboard access — not agent-actionable.** Accepted as a documented operational follow-up at milestone close (v2.1), not a blocker: Phase 28's VERIFICATION.md confirms no code-level gap.

## 3. Phase 28 live provider end-to-end proof — OPEN, accepted follow-up
SYNC-01 resume + SYNC-03 idempotency are proven on the real TEST DB provider-agnostically, but a live provider-backed multi-slice backfill (e.g. sync 200 real Fathom calls) was not exercised — TEST project has no provider credentials in any env file. This gets exercised organically the first time a real account clicks "Sync all" in production (frontend is now live). Watch the first real sync-all in the Phase 27 observable-jobs surface. **Requires Andrew to supply TEST provider credentials if pre-production proof is wanted — not agent-actionable.** Accepted as a documented operational follow-up at milestone close (v2.1), not a blocker.
