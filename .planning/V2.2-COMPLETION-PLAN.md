# v2.2 Completion and Release Plan

**Milestone:** Event Resolution & Provenance  
**Working branch:** `v2.2-event-resolution`  
**Production frontend branch:** `main`  
**Current position:** Phase 37 complete; Phases 38 and 39 remain

## Safety Promise

- All remaining milestone work stays on `v2.2-event-resolution`.
- Pushing `v2.2-event-resolution` to GitHub is a backup and does not publish the production frontend.
- The production frontend changes only after a deliberate merge into `main` and push of `main`.
- Supabase migrations and Edge Function deploys are a separate production boundary. Each production action must identify the production project, verify the exact pending change set, and record evidence in the phase summary.
- The final merge to `main` requires an explicit release decision after the milestone audit and verification pass.

## Stage 1 — Stabilize the Milestone Branch

- [x] Work from `/Users/admin/dev/brain/main` on `v2.2-event-resolution`.
- [x] Finish and commit the interrupted Phase 37 records.
- [ ] Push the current branch to `origin/v2.2-event-resolution` as a remote backup.
- [ ] Bring the five newer `main` fixes into this branch.
- [ ] Resolve any conflicts without discarding either milestone work or live fixes.
- [ ] Run the combined-code verification suite.
- [ ] Record the synchronization result in `STATE.md` and push the synchronized branch backup.

## Stage 2 — Finish Phase 38

Run the standard GSD lifecycle on the feature branch:

1. Discuss product and access-policy decisions.
2. Research affected schema, RLS, sharing, notifications, and UI paths.
3. Produce the Phase 38 context, UI contract, plans, validation strategy, and threat model.
4. Execute plans in dependency order with real-database integration tests.
5. Review code and security, fix findings, and verify the phase goal.
6. Update `REQUIREMENTS.md`, `ROADMAP.md`, `PROJECT.md`, and `STATE.md` with tested facts.
7. Push the completed Phase 38 branch backup without merging to `main`.

## Stage 3 — Finish Phase 39

Repeat the same lifecycle for verified-email event discovery and participation claiming:

1. Discuss and lock product decisions.
2. Research cross-organization reads, identity proof, invitation, and claim boundaries.
3. Plan, implement, test, review, and verify.
4. Update every planning and traceability document.
5. Push the completed Phase 39 branch backup without merging to `main`.

## Stage 4 — Milestone Audit and Release Preparation

- [ ] Audit all v2.2 requirements against implementation and production evidence.
- [ ] Run the complete build, type baseline check, unit tests, real-database integration tests, RLS regression suite, and applicable browser checks.
- [ ] Confirm every phase has matching plans, summaries, reviews, and verification records.
- [ ] Reconcile the feature branch with the latest `origin/main` one final time and repeat affected verification.
- [ ] Produce the milestone completion summary and release notes.
- [ ] Present a plain English release report: what changes for users, what is already live in Supabase, known limitations, rollback path, and verification evidence.

## Stage 5 — Deliberate Production Release

This stage runs only after an explicit release decision.

- [ ] Merge the proven `v2.2-event-resolution` branch into `main`.
- [ ] Run the required committed-tree build and final checks.
- [ ] Push `main` to trigger the production frontend deployment.
- [ ] Verify the actual production URL and critical user flows.
- [ ] Archive the milestone and update the planning documents with the confirmed release state.

## Planning Document Contract

During the remaining work:

- `STATE.md` always names the real current phase and next action.
- `ROADMAP.md` plan counts match plan and summary files on disk.
- `REQUIREMENTS.md` is marked complete only after behavior is verified.
- Every executed plan receives a matching `SUMMARY.md`.
- Review findings and their fixes are recorded before phase verification.
- Production claims require evidence from the current session or the phase artifact that performed the production action.

## Immediate Next Action

Complete Stage 1, then start the interactive Phase 38 discussion.
