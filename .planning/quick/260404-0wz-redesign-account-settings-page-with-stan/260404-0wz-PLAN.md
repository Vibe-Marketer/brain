---
phase: quick
plan: 260404-0wz
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/AccountTab.tsx
autonomous: true
requirements: [QUICK-REDESIGN-ACCOUNT]
must_haves:
  truths:
    - "Account settings page has 5 clearly separated sections: Profile, Security, Preferences, Integrations, Danger Zone"
    - "All text uses semantic tokens (text-foreground, text-muted-foreground) — zero hardcoded gray values"
    - "Section headings use the design system typography (font-montserrat font-extrabold uppercase tracking-wide text-sm)"
    - "All existing functionality preserved — profile save, timezone, Fathom email, auto-processing toggles, password change all work"
  artifacts:
    - path: "src/components/settings/AccountTab.tsx"
      provides: "Redesigned account settings with 5 standard SaaS sections"
      contains: "Danger Zone"
  key_links:
    - from: "AccountTab.tsx"
      to: "usePreferencesStore"
      via: "import"
      pattern: "usePreferencesStore"
    - from: "AccountTab.tsx"
      to: "supabase"
      via: "direct queries for profile, settings, password"
      pattern: "supabase\\.from"
---

<objective>
Redesign the Account settings page (AccountTab.tsx) with proper standard SaaS sections, semantic tokens, and design system typography while preserving all existing functionality.

Purpose: The current page has hardcoded colors, inconsistent typography, and a flat section structure. Reorganizing into Profile / Security / Preferences / Integrations / Danger Zone matches user expectations from modern SaaS apps.

Output: Rewritten AccountTab.tsx with polished sections and correct design tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/settings/AccountTab.tsx
@src/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AccountTab.tsx with 5 standard sections and design system tokens</name>
  <files>src/components/settings/AccountTab.tsx</files>
  <action>
Rewrite AccountTab.tsx as a complete redesign. Keep ALL existing state management, hooks (useEditableField), save functions (saveProfile, saveTimezone, saveHostEmail, changePassword), useEffect calls, and the timezones array exactly as-is. Only change the JSX return and fix token usage.

**Section reorganization (top to bottom, each separated by Separator with my-16):**

1. **Profile** — User avatar (initials circle in bg-muted, text-foreground, w-16 h-16 rounded-full), display name (editable), email (read-only). Keep the existing inline edit pattern with RiEditLine/RiCheckboxCircleLine/RiCloseCircleLine icons.

2. **Security** — Password change section. Move from bottom. Same expand/collapse pattern with showPasswordForm state. Same form fields and changePassword handler.

3. **Preferences** — Timezone select AND auto-processing toggles (auto-naming, auto-tagging). Merge the old "Preferences" and "Auto-Processing" sections since they are both user preferences. Timezone at top, then a sub-label "Auto-Processing" with the two switches below.

4. **Integrations** — Fathom email field only. Move out of Preferences into its own section. Same inline edit pattern. Description: "Connect external services to enhance your workflow."

5. **Danger Zone** — New section with a red-bordered container (border border-destructive/30 rounded-lg p-6). Contains a "Delete Account" row with description text and a disabled destructive button. Text: "Permanently delete your account and all associated data. This action cannot be undone." Button text: "Delete Account" with variant="destructive" and disabled={true}. This is a placeholder for future implementation.

**Typography fixes (apply to ALL section headings and descriptions):**
- Section headings: `font-montserrat font-extrabold uppercase tracking-wide text-sm text-foreground` (replaces `font-semibold text-gray-900 dark:text-gray-50`)
- Section descriptions: `mt-1 text-sm text-muted-foreground` (replaces `mt-1 text-sm text-gray-500 dark:text-gray-500`)
- All other gray-500/gray-900 references: replace with text-muted-foreground / text-foreground respectively

**Layout:** Keep the existing 3-column grid pattern (grid grid-cols-1 gap-x-10 gap-y-8 lg:grid-cols-3) with label in col-1 and content in lg:col-span-2.

**Imports:** Add RiShieldLine, RiSettings3Line, RiPlugLine, RiAlertLine, RiUserLine from @remixicon/react for section visual identity (optional small icon next to each section heading). Keep all existing imports.

**Do NOT change:** useEditableField hook, loadProfileData, saveProfile, saveTimezone, saveHostEmail, changePassword functions, timezones array, usePreferencesStore usage, any Supabase queries.
  </action>
  <verify>
    <automated>cd /Users/Naegele/dev/brain && npx tsc --noEmit src/components/settings/AccountTab.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
- AccountTab.tsx has 5 sections: Profile, Security, Preferences, Integrations, Danger Zone
- Zero instances of text-gray-900, text-gray-500, dark:text-gray-50, dark:text-gray-500
- All section headings use font-montserrat font-extrabold uppercase tracking-wide text-sm
- All existing save/load functionality preserved (no function signatures changed)
- Danger Zone section present with disabled Delete Account button
- Avatar initials circle in Profile section
  </done>
</task>

</tasks>

<verification>
- `grep -c "gray-900\|gray-500" src/components/settings/AccountTab.tsx` returns 0
- `grep -c "font-montserrat" src/components/settings/AccountTab.tsx` returns 5 (one per section heading)
- `grep -c "Danger Zone\|Delete Account" src/components/settings/AccountTab.tsx` returns at least 2
- TypeScript compiles without errors
</verification>

<success_criteria>
Account settings page reorganized into 5 standard SaaS sections with proper design system tokens, correct typography, and all existing functionality preserved. Danger Zone placeholder added for future delete account feature.
</success_criteria>

<output>
After completion, create `.planning/quick/260404-0wz-redesign-account-settings-page-with-stan/260404-0wz-SUMMARY.md`
</output>
