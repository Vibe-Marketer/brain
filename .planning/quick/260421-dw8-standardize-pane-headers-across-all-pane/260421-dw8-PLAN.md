---
quick_id: 260421-dw8
description: Standardize pane headers across all panes and update Downloads spec doc as template
date: 2026-04-21
plan_count: 1
---

# Plan 1: Standardize Pane Headers + Update Spec Doc

## Task 1: Upgrade Pane 2 compact headers to rich header pattern

**Files:**
- `src/components/panes/SettingsCategoryPane.tsx`
- `src/components/panes/SortingCategoryPane.tsx`
- `src/components/panes/AnalyticsCategoryPane.tsx`
- `src/components/panes/SharedWithMePane.tsx`

**Action:** These 4 files use Pattern B (compact: `flex items-center gap-3 px-4 py-3 border-b border-border bg-card/50`). Convert them to Pattern A structure — the rich navigation header with icon badge + title stack. The org switcher row is OPTIONAL (only include if the pane needs org context). The key change is the header shape: icon badge (w-8 h-8 rounded-lg), title (text-[10px] font-black uppercase tracking-[0.2em]), subtitle (text-[9px] text-muted-foreground/60 uppercase), wrapped in `px-4 py-4 space-y-4 border-b border-border flex-shrink-0`.

For panes that DON'T need an org switcher (Settings, Shared), just use the top row with icon badge + title stack + optional action button. Skip the org switcher card. The header will be shorter but structurally identical.

**Verify:** Each modified file's header tag matches `<header className="px-4 py-4 ... border-b border-border flex-shrink-0">` with the icon badge + title stack pattern inside.

**Done:** All Pane 2 headers use Pattern A structure (rich header with icon badge + title stack, optional org switcher).

## Task 2: Upgrade Pane 4 panel headers to premium sticky pattern

**Files:**
- `src/components/panels/FolderDetailPanel.tsx` (currently: `p-4 border-b border-border` — no sticky, no blur)
- `src/components/panels/TagDetailPanel.tsx` (currently: `p-4 border-b border-border`)
- `src/components/panels/SettingHelpPanel.tsx` (currently: `p-4 border-b border-border`)
- `src/components/panels/UserDetailPanel.tsx` (currently: `p-4 border-b border-border`)
- `src/components/panels/AutomationRulePanel.tsx` (currently: `px-4 py-3 border-b border-border bg-card/50` — has bg but no sticky)
- `src/components/panels/RoutingRulePanel.tsx` (currently: `px-4 py-3 border-b border-border bg-card/50`)
- `src/components/panels/WorkspaceDetailPanel.tsx` (currently: `px-4 py-3 border-b border-border/40 flex-shrink-0` — partial)

**Action:** Upgrade ALL panel headers to the premium sticky standard:
```
flex items-center justify-between px-4 py-3 border-b border-border bg-card/50 backdrop-blur-md sticky top-0 z-10 flex-shrink-0 min-h-[56px]
```

The two panels that already have this pattern (WorkspaceMemberPanel.tsx, OrganizationMemberPanel.tsx) should be left as-is — they're already correct (they use py-4 instead of py-3 but that's fine, the key properties are all there).

For panels that use `p-4`, change to `px-4 py-3` for consistency. The critical additions are: `bg-card/50 backdrop-blur-md sticky top-0 z-10 flex-shrink-0 min-h-[56px]`.

**Verify:** grep all panel header tags — every one should contain `sticky top-0 z-10 flex-shrink-0` and `backdrop-blur-md`.

**Done:** All Pane 4 headers use the premium sticky pattern.

## Task 3: Update Downloads spec doc as reusable template

**Files:**
- `/Users/Naegele/Downloads/multi-pane-layout-spec.md`

**Action:** Rewrite Section 4 (Pane Header Specifications) to present TWO locked-in standards instead of documenting the old inconsistent patterns. Structure as:

1. **Navigation Header (Pane 2)** — template with two variants:
   - Full variant: icon badge + title stack + org switcher row
   - Compact variant: icon badge + title stack only (org switcher hidden)
   - Document the org switcher as an "optional row" that can be toggled
   
2. **Content Header (Pane 3 & 4)** — single template:
   - The premium sticky pattern
   - Slots for: optional back button, icon, title, subtitle, breadcrumbs, actions

Remove the "Current State (Inconsistent)" documentation and the Pattern A/B/C/D breakdown. Replace with the two standards as copy-paste templates with slot annotations showing what's customizable vs fixed.

**Verify:** Read the updated file, confirm Section 4 presents exactly 2 standards with template syntax.

**Done:** Spec doc updated as a reusable template with 2 standardized patterns.
