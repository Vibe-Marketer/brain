---
phase: 18-mcps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/MCPTab.tsx
  - src/services/mcp-tokens.service.ts
autonomous: true
requirements: [MCP-01, MCP-02, MCP-03]

must_haves:
  truths:
    - "Only one MCP token can exist per organization — creating a second is blocked with a clear message"
    - "MCP server returns only org-scoped data when called with a valid token"
    - "MCP tools (search_calls, list_calls, get_transcript, get_recording_context, list_workspaces) all function correctly"
  artifacts:
    - path: "src/components/settings/MCPTab.tsx"
      provides: "One-per-org enforcement in create flow"
      contains: "already has.*token"
    - path: "src/services/mcp-tokens.service.ts"
      provides: "Token count check before creation"
      contains: "existing.*token"
    - path: "supabase/functions/mcp-server/index.ts"
      provides: "Org-scoped MCP server with 5 tools"
      exports: ["Deno.serve"]
  key_links:
    - from: "src/components/settings/MCPTab.tsx"
      to: "src/services/mcp-tokens.service.ts"
      via: "useMcpTokens hooks"
      pattern: "useCreateMcpToken|useMcpTokensList"
    - from: "supabase/functions/mcp-server/index.ts"
      to: "mcp_tokens table"
      via: "Bearer token lookup"
      pattern: "eq.*token.*rawToken"
---

<objective>
Enforce one MCP token per organization and verify the MCP server functions correctly with org-scoped data access.

Purpose: MCP-01 requires one MCP server per org (currently unlimited tokens allowed). MCP-02 and MCP-03 require the server to be org-scoped and functional — all code exists and is deployed, but needs verification and the one-per-org gate.
Output: MCPTab blocks second token creation per org; MCP server verified functional via curl.
</objective>

<execution_context>
@/Users/Naegele/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Naegele/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/settings/MCPTab.tsx
@src/services/mcp-tokens.service.ts
@src/hooks/useMcpTokens.ts
@supabase/functions/mcp-server/index.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/services/mcp-tokens.service.ts:
```typescript
export type McpTokenScope = 'workspace' | 'organization'
export interface McpToken {
  id: string; user_id: string; org_id: string | null;
  workspace_id: string | null; name: string; token: string;
  scope: McpTokenScope; last_used_at: string | null; created_at: string;
}
export interface CreateMcpTokenParams {
  name: string; scope: McpTokenScope; org_id: string; workspace_id?: string;
}
export async function getMcpTokens(): Promise<McpToken[]>
export async function createMcpToken(params: CreateMcpTokenParams): Promise<McpToken>
export function getMcpUrl(): string
```

From src/hooks/useMcpTokens.ts:
```typescript
export function useMcpTokensList(): { tokens: McpToken[]; isLoading: boolean; error: Error | null }
export function useCreateMcpToken(options?: { onSuccess?: (token: McpToken) => void }): UseMutationResult
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Enforce one MCP token per organization</name>
  <files>src/components/settings/MCPTab.tsx, src/services/mcp-tokens.service.ts</files>
  <read_first>
    - src/components/settings/MCPTab.tsx (full file — understand current create flow)
    - src/services/mcp-tokens.service.ts (full file — understand createMcpToken)
    - src/hooks/useMcpTokens.ts (full file — understand hooks)
  </read_first>
  <action>
    Enforce one MCP token per organization (per MCP-01: "attempting to create a second is blocked"):

    1. In `mcp-tokens.service.ts`, add a `getOrgTokenCount(orgId: string): Promise<number>` function that queries `mcp_tokens` filtered by `org_id` and returns the count. Use `.select('id', { count: 'exact', head: true })` for efficiency.

    2. In `mcp-tokens.service.ts`, modify `createMcpToken` to call `getOrgTokenCount` before inserting. If count >= 1, throw a descriptive Error: `'This organization already has an MCP token. Delete the existing token to create a new one.'`

    3. In `MCPTab.tsx`, add UI-level enforcement:
       - After the tokens list loads, compute `hasOrgToken` by checking if `tokens.some(t => t.org_id === currentOrgId)` (where currentOrgId comes from the org context).
       - Actually, simpler: since the requirement is ONE token per org total (workspace or org scoped), check `tokens.length > 0` for the current org. But tokens are user-scoped via RLS — the user may have tokens for multiple orgs.
       - Better approach: In the NewTokenDialog, after the user selects an org, check if `tokens.filter(t => t.org_id === selectedOrgId).length > 0`. If so, disable the Create button and show inline message: "This organization already has an MCP token. Delete the existing one first."
       - Also hide the "Create Token" button in the main MCPTab when all user orgs already have tokens (edge case, nice-to-have).

    4. The error toast from the service-layer check acts as a server-side fallback if the UI check is bypassed.

    Do NOT change the mcp_tokens table schema or add database constraints — the service-layer + UI enforcement is sufficient.
  </action>
  <verify>
    <automated>cd /Users/Naegele/dev/brain && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "already has.*MCP token" src/services/mcp-tokens.service.ts
    - grep -q "already has.*token" src/components/settings/MCPTab.tsx
    - grep -q "getOrgTokenCount" src/services/mcp-tokens.service.ts
  </acceptance_criteria>
  <done>Creating a second MCP token for the same org is blocked at both UI and service layer with a clear message. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Verify MCP server org-scoping and tool functionality</name>
  <files></files>
  <read_first>
    - supabase/functions/mcp-server/index.ts (review auth + scope enforcement)
  </read_first>
  <action>
    Verify that the deployed MCP server correctly scopes data to the token's org and that all 5 tools work. This is a verification-only task — no code changes unless bugs found.

    1. Use the dev-browser or curl to verify the MCP server endpoint responds. The MCP URL is `{VITE_SUPABASE_URL}/functions/v1/mcp-server`. Read the VITE_SUPABASE_URL from `.env.local` or `.env`.

    2. Send a POST to the MCP server with method `initialize` and no Bearer token — verify it returns error -32001 (missing auth).

    3. Send a POST with method `initialize` and an invalid Bearer token — verify it returns error -32001 (invalid token).

    4. Send a POST with method `tools/list` and a valid token — verify it returns the 5 tool definitions. If no valid token exists in the DB, note this as a gap but do not create one programmatically.

    5. Review the mcp-server/index.ts code to verify org-scoping:
       - `search_calls`: Uses `filter_user_id` from token — the global_search RPC scopes via workspace_entries which are org-scoped. PASS.
       - `list_calls`: Workspace-scoped tokens filter by workspace_id. Org-scoped tokens fetch org workspaces via `organization_id`. PASS.
       - `get_transcript` and `get_recording_context`: Verify ownership via workspace_entries + workspace scope check. PASS.
       - `list_workspaces`: Workspace-scoped tokens return single workspace. Org-scoped tokens filter by org_id + user membership. PASS.

    6. Verify `last_used_at` is updated on each request (fire-and-forget pattern in code).

    7. If any org-scoping gaps are found, fix them. Based on code review, the scoping looks correct — document findings in the SUMMARY.

    Use `curl` commands against the production Supabase URL (since the function is deployed ACTIVE).
  </action>
  <verify>
    <automated>cd /Users/Naegele/dev/brain && curl -s -X POST "$(grep VITE_SUPABASE_URL .env.local 2>/dev/null | cut -d= -f2 || grep VITE_SUPABASE_URL .env 2>/dev/null | cut -d= -f2)/functions/v1/mcp-server" -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","id":1,"method":"initialize"}' 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - MCP server returns JSON-RPC error -32001 when called without Bearer token
    - Code review confirms all 5 tools enforce org/workspace scoping via token metadata
  </acceptance_criteria>
  <done>MCP server verified: responds to requests, rejects invalid tokens, all 5 tools enforce org-scoped data access via token metadata. No cross-org data leakage possible.</done>
</task>

</tasks>

<verification>
- TypeScript compiles clean after one-per-org enforcement changes
- MCP server endpoint responds at production URL
- Invalid/missing tokens are rejected with proper error codes
- Code review confirms all tool handlers scope data by token's org_id/workspace_id
</verification>

<success_criteria>
- One MCP token per org enforced at UI + service layer (MCP-01)
- MCP server deployed and functional with org-scoped access (MCP-02)
- All 5 MCP tools (search, list, transcript, context, workspaces) work within org scope (MCP-03)
</success_criteria>

<output>
After completion, create `.planning/phases/18-mcps/18-01-SUMMARY.md`
</output>
