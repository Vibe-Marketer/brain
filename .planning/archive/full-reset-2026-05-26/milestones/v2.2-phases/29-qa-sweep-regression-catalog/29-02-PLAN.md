---
phase: 29-qa-sweep-regression-catalog
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - .planning/phases/29-qa-sweep-regression-catalog/screenshots/
  - .planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md
autonomous: true
requirements: [QA-01]
must_haves:
  truths:
    - "Persona A (owner) has visited every route defined in src/App.tsx on production at least once"
    - "Every flow on the D-11 primary user flow checklist (Persona A items) has been completed in each of the 3 orgs"
    - "Every BUG-NN re-verification flow (BUG-01..09) was executed against production"
    - "All cross-org cache-leak observation points (table data, folder counts, AI-tag suggestions) were inspected during org switching"
    - "Findings are captured in raw form in 29-02-PERSONA-A-SWEEP-NOTES.md — formal QA-NN write-back happens in Plan 29-05"
  artifacts:
    - path: ".planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md"
      provides: "Raw observation log organized by route + flow + org; every finding lists Surface/Route/Persona/Steps/Observed/Expected/Severity/Screenshot path/Log-snippet plus a [NEW] or [MATCHES-EXISTING-{ID}] tag for dedupe routing in Plan 29-05"
      contains: "Persona A"
      min_lines: 100
    - path: ".planning/phases/29-qa-sweep-regression-catalog/screenshots/"
      provides: "PNG screenshots, at least one per primary user flow on the D-11 checklist, named with kebab-case slugs"
  key_links:
    - from: "Persona A login → 3-org cycle"
      to: "Each route in src/App.tsx"
      via: "Navigation via sidebar / direct URL on each org"
      pattern: "Org: (AI SIMPLE|BUSINESS|GOVIBEY)"
    - from: "29-02-PERSONA-A-SWEEP-NOTES.md"
      to: "Plan 29-05 (catalog write-back)"
      via: "Plan 05 reads this file as the source for QA-NN entries + Sweep Status column values for BUG-* / VIS-* / BRAND-* etc."
      pattern: "\\[NEW\\]|\\[MATCHES-EXISTING-"
---

<objective>
Persona A (the owner test account) cycles through all 3 orgs (AI SIMPLE → BUSINESS → GOVIBEY) on production (app.callvaultai.com) and exercises every route in src/App.tsx + every Persona A item on the D-11 primary user flow checklist. Every observation — bugs, visual defects, cache leaks, regressions, and "still works correctly" confirmations — is captured in a structured raw notes file (`29-02-PERSONA-A-SWEEP-NOTES.md`) for Plan 29-05 to convert into formal QA-NN entries and Sweep Status column updates.

Purpose: This is the largest sweep pass and surfaces the bulk of v2.2 regression findings. The owner persona unlocks the broadest surface area (Settings tabs, all sidebar sections, table mutations, share-link creation, billing). The 3-org cycle is the cross-check for the SEC-03C cache-leak class of bugs.

Output: One structured raw-notes file + a screenshot per primary flow + per finding, all stored phase-locally. NO formal write-back to REQUIREMENTS.md / ROADMAP.md happens here — Plan 29-05 owns that to keep observation and cataloging separate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/29-qa-sweep-regression-catalog/29-CONTEXT.md
@.planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md
@CLAUDE.md
@src/CLAUDE.md
@docs/design/brand-guidelines-v4.4.md
@src/App.tsx

<interfaces>
<!-- Routes the executor MUST visit, derived from src/App.tsx (the source of truth per D-11 route coverage). -->
<!-- Each authenticated route is wrapped in <ProtectedRoute><Layout>. Redirects route to / or /rules. -->

**Authenticated routes (visit each in EACH of the 3 orgs):**
- `/` (Home → TranscriptsNew)
- `/transcripts` (same component as /)
- `/settings` (Settings index)
- `/settings/account`, `/settings/billing`, `/settings/organizations`, `/settings/ai-integrations`, `/settings/admin` (5 settings tabs from D-11)
- `/analytics`
- `/analytics/{category}` (visit at least one category)
- `/people`
- `/organization`
- `/rules` (Routing Rules)
- `/import` (Manual Import)
- `/call/:callId` (open at least one call to view CallDetailPage)
- `/setup` (auth-protected SetupWizard — visit once; if it redirects mid-flow document that)
- `/oauth/callback` and `/oauth/callback/zoom` — DO NOT visit directly (these consume OAuth state); document as "skipped, OAuth callback target only"

**Public / redirect routes (visit each at least once but only one time total, not per-org):**
- `/login`, `/auth`, `/forgot-password`, `/reset-password` (4 unauth routes; visit signed-out)
- `/oauth/consent` (public OAuth consent — open with a representative state if possible, otherwise document the empty state)
- `/s/:token` — DO NOT visit here; Plan 29-04 owns this (wrong-account recipient)
- `/join/org/:token`, `/join/workspace/:token` — visit signed-out only if a valid token is accessible from current state; if not, document "deferred — needs a live invite"

**Legacy redirects (just confirm they redirect, one visit total):**
- `/sorting-tagging`, `/sorting-tagging/rules`, `/workspaces`, `/vaults`, `/agents`, `/team`, `/banks`, `/automation-rules`, `/shared-with-me`
- For each: navigate, confirm the URL changes to the redirect target, no extra capture needed beyond a one-line note

**Cmd+K global search** — also a "route-like surface" required by D-11; trigger via keyboard shortcut from `/`.

**D-11 Persona A primary user flow checklist (must all be completed in at least one org — BUG re-verifications must be on Fathom-imported calls where the symptom is known):**
1. Owner login (already done in Plan 01 precheck; note it as ✓ in notes)
2. Sidebar nav across CALLS, IMPORT, RULES, PEOPLE, ORGANIZATION in **each** of the 3 orgs
3. Settings tabs (Account, Billing, Organizations, AI Integrations, Admin)
4. Global search (Cmd+K) and results
5. Open a call → Overview / Transcript / Invitees / Participants tabs
6. Filter the 3rd-pane table by Date / Source / Contacts / Tags
7. Drag a call to a folder (DND) — read-only intent; do NOT permanently mutate state
8. Click "Tag with AI" on a Fathom-imported call (BUG-01 re-verify) — capture console error if present
9. Toggle default-workspace in Workspace Detail panel (BUG-02 re-verify) — capture 406 if present
10. Move / delete / tag a call and verify table refresh (BUG-03 re-verify) — clean up any test mutation at end of plan
11. Sort by Date asc and desc (BUG-04 re-verify)
12. Open Import Source Manager → click "+" and "Import History" (BUG-06/07 re-verify)
13. Create a new workspace and verify no auto-folders (BUG-08 re-verify) — clean up the test workspace at end
14. Create a share link addressed to `naegele412@gmail.com` (Plan 29-04 will consume this)

**Format for each finding in the raw notes file (matches D-03 + adds dedupe tag):**
```
### Finding {SEQ}: {one-line title}
- **Tag:** [NEW]  // or [MATCHES-EXISTING-BUG-02] / [RE-VERIFY-BUG-01] / [NO-REPRO-BUG-04]
- **Surface/Route:** {URL or in-app location}
- **Persona:** A
- **Org observed:** {AI SIMPLE | BUSINESS | GOVIBEY | all-three}
- **Steps to reproduce:**
  1. {step}
  2. {step}
- **Observed:** {what happened}
- **Expected:** {what should happen — reference brand-guidelines-v4.4.md / src/CLAUDE.md / existing requirement spec as needed}
- **Severity:** {P0 | P1 | P2 | P3 — per D-10 rubric}
- **Maps to (proposal):** {Phase NN | BACKLOG | new mini-phase candidate} — Plan 29-05 finalizes
- **Screenshot:** screenshots/persona-a-{seq}-{slug}.png
- **Backend log (conditional, per D-05):** {fenced code block if console error / network ≥400 / RLS error / auth failure observed; omit otherwise}
- **Related existing requirement:** {ID if [MATCHES-EXISTING-*] or partial match per D-06}
```

**Existing requirement dedupe targets (per D-06):**
When something is observed, scan REQUIREMENTS.md for a match before tagging [NEW]. The 73 active requirement IDs are listed in REQUIREMENTS.md traceability table — match on "same surface AND same root cause" per D-06. Partial matches → [NEW] with "Related: {ID}" line.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persona A — route coverage sweep + sidebar/settings/search/legacy-redirect pass</name>
  <files>
    .planning/phases/29-qa-sweep-regression-catalog/screenshots/
    .planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md
  </files>
  <read_first>
    - .planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md (gate: Status must be READY-FOR-SWEEP; if BLOCKED, stop and remediate)
    - .planning/phases/29-qa-sweep-regression-catalog/29-CONTEXT.md (D-01, D-03, D-05, D-06, D-10, D-11)
    - .planning/REQUIREMENTS.md (the 73 active requirement IDs that observations are deduped against)
    - src/App.tsx (route surface — every listed route must be visited)
    - src/CLAUDE.md (design tokens for evaluating BRAND / VIS findings)
    - docs/design/brand-guidelines-v4.4.md (selection-state canonical pattern, sidebar order, typography)
    - CLAUDE.md (HARD RULE — Claude does all verification via dev-browser)
  </read_first>
  <action>
    Confirm `29-01-PRECHECK.md` Status is `READY-FOR-SWEEP` before starting. If BLOCKED, remediate first.

    Start the raw notes file `29-02-PERSONA-A-SWEEP-NOTES.md` with this header:
    ```markdown
    # Phase 29 — Persona A Sweep Raw Notes (Plan 29-02)

    **Driver:** Claude via dev-browser MCP
    **Persona:** A — Owner, cycled across AI SIMPLE / BUSINESS / GOVIBEY
    **Started:** {ISO-8601}
    **Target:** https://app.callvaultai.com

    ## Coverage Matrix

    | Route | AI SIMPLE | BUSINESS | GOVIBEY | Notes |
    |-------|-----------|----------|---------|-------|
    | / | | | | |
    | /transcripts | | | | |
    | /settings (5 tabs each) | | | | |
    | /analytics | | | | |
    | /analytics/{category} | | | | |
    | /people | | | | |
    | /organization | | | | |
    | /rules | | | | |
    | /import | | | | |
    | /call/:callId | | | | |
    | /setup | | | | (visit once; not 3x) |
    | Cmd+K global search | | | | |

    | Unauth route | Visited | Notes |
    |--------------|---------|-------|
    | /login | | |
    | /auth | | |
    | /forgot-password | | |
    | /reset-password | | |
    | /oauth/consent | | |

    | Legacy redirect | Confirms? | Target |
    |-----------------|-----------|--------|
    | /sorting-tagging | | /rules |
    | /workspaces | | / |
    | /vaults | | / |
    | /agents | | / |
    | /team | | / |
    | /banks | | /settings/organizations |
    | /automation-rules | | /rules |
    | /shared-with-me | | / |

    ## Findings

    {finding entries appended sequentially below; format per the template in interfaces}
    ```

    Then walk the routes in this exact order, organized for efficiency (read-only first, mutation flows in Task 2):

    **Phase A1 — Signed-out routes (single visit each, no org context).** Sign out of Persona A. Visit `/login`, `/auth`, `/forgot-password`, `/reset-password`, `/oauth/consent`. For each: take a screenshot `persona-a-{seq}-{route-slug}.png`, mark the row in the unauth route table as Visited, note any visible bugs (e.g., BRAND-08 search rounding on `/login` if that surface uses it) as findings. Sign back in afterwards.

    **Phase A2 — Legacy redirects (single visit each).** While signed in, type each legacy URL into the browser address bar via `mcp__dev-browser__navigate`. Confirm the URL after navigation matches the expected target. Fill in the Legacy redirect table. No screenshot needed per redirect — one screenshot of `/` after a few redirects is fine. If ANY redirect lands somewhere other than the expected target, that is a [NEW] finding.

    **Phase A3 — Per-org route walk.** For each org (AI SIMPLE → BUSINESS → GOVIBEY in order):
    1. Switch to the org via the top-bar org selector. Capture: `persona-a-{seq}-org-switch-{org-slug}.png`. While the switch is happening, watch for SEC-03C cache-leak symptoms — calls/folders/tags from the previous org appearing briefly. If observed, record as [NEW] finding `Cross-org cache leak — {visible artifact}` with severity P0 (per D-10 rubric for cross-org data exposure).
    2. Visit each route in the Coverage Matrix in order: `/`, `/transcripts`, `/settings` and each of its 5 sub-tabs (`/settings/account`, `/settings/billing`, `/settings/organizations`, `/settings/ai-integrations`, `/settings/admin`), `/analytics`, `/people`, `/organization`, `/rules`, `/import`.
    3. For each route: take a screenshot named `persona-a-{seq}-{route-slug}-{org-slug}.png`. Mark the cell ✓ in the Coverage Matrix. While looking at the route, **scan against BRAND-01..10 and VIS-01..05 expectations** (sidebar order, UPPERCASE titles, selection-state pattern, doubled close button, search modal rounding, etc.). Any divergence from `docs/design/brand-guidelines-v4.4.md` is a re-verification finding tagged `[RE-VERIFY-{ID}]`; new visual issues not matching an existing requirement are `[NEW]`.
    4. Trigger **Cmd+K global search** (via `mcp__dev-browser__type` of `Meta+K` or page-level keyboard event). Capture the modal screenshot. Type a query (e.g., "test"), observe results, capture. Mark Cmd+K cell ✓.
    5. Open exactly one call from the table (click a row) — capture `/call/:callId` and the CallDetailPage / modal — and click through Overview, Transcript, Invitees, Participants tabs. Capture each tab. Close the call.

    **Phase A4 — `/setup` route (single visit).** While signed in to one org, navigate directly to `/setup`. If the wizard renders, capture a screenshot of the first screen. Do NOT complete the wizard (it may permanently change account state). Document what was visible.

    Append every observation as a Finding block. Number sequentially starting at Finding 001. Apply [NEW] vs [MATCHES-EXISTING-*] vs [RE-VERIFY-*] vs [NO-REPRO-*] tags per D-06.

    **Read-only stance:** No clicks that mutate data in Phase A1–A4. The only "mutation" allowed is the org switch (which is session-state, not data-state).
  </action>
  <verify>
    <automated>test -f .planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md && grep -cE "^### Finding [0-9]" .planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md > /dev/null && ls .planning/phases/29-qa-sweep-regression-catalog/screenshots/persona-a-*.png 2>/dev/null | wc -l | awk '{exit ($1 < 15) ? 1 : 0}' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `29-02-PERSONA-A-SWEEP-NOTES.md` exists with the Coverage Matrix header and at least one `### Finding NNN:` block (zero findings is also valid if production is truly clean — but the matrix must be filled in)
    - Coverage Matrix has every authenticated route row checked in ALL 3 org columns (✓ for visited, or an explicit `n/a` with reason)
    - Unauth route table has every row marked Visited
    - Legacy redirect table has every row Confirmed
    - At least 15 PNG files named `persona-a-*.png` exist under `screenshots/` (covers the route × 3 orgs + Cmd+K + call-detail screenshots — actual count will be ~30+)
    - Every Finding block has all 9 fields: Tag, Surface/Route, Persona, Org observed, Steps, Observed, Expected, Severity, Screenshot path (Backend log conditional, Related existing requirement conditional)
    - Every Finding's Severity is one of P0/P1/P2/P3 (literal match per D-10)
    - Every Finding's Tag matches one of: `[NEW]`, `[MATCHES-EXISTING-{ID}]`, `[RE-VERIFY-{ID}]`, `[NO-REPRO-{ID}]`
  </acceptance_criteria>
  <done>Every route in src/App.tsx has been visited by Persona A in the appropriate set of orgs, the Coverage Matrix is complete, and all observations are captured as Finding entries with screenshots. Task 2 will handle the mutation flows (BUG re-verifications) next.</done>
</task>

<task type="auto">
  <name>Task 2: Persona A — flow re-verifications (BUG-01..09 + share link create) with cleanup</name>
  <files>
    .planning/phases/29-qa-sweep-regression-catalog/screenshots/
    .planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md
  </files>
  <read_first>
    - .planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md (Task 1 output — continue appending findings)
    - .planning/REQUIREMENTS.md (BUG-01..09 spec details — each row in Active Requirements has the symptom description)
    - .planning/phases/29-qa-sweep-regression-catalog/29-CONTEXT.md (D-05 for backend log capture, D-11 for the flow checklist)
  </read_first>
  <action>
    Continue appending Finding entries to `29-02-PERSONA-A-SWEEP-NOTES.md`. Pick the org best suited to each test (most owner-pinned calls and most existing data is typically in AI SIMPLE — adjust per actual state). Do each flow read-only where possible; the few that mutate (workspace create, share-link create, optional call mutation) must clean up at the end.

    Track which calls/workspaces/shares Persona A creates during this pass in a temporary "cleanup list" at the top of the findings file. Format:
    ```markdown
    ## Cleanup List (must be reverted at end of Plan 29-02)
    - [ ] Workspace "qa-sweep-test-{timestamp}" in {org} — delete after BUG-08 verification
    - [ ] Share link {token} addressed to naegele412@gmail.com — leave intact; Plan 29-04 consumes it (mark "preserved for Plan 04")
    - [ ] Tag "qa-sweep-test" applied to call {id} — remove
    - [ ] Folder assignment change on call {id} — revert
    ```

    **Flow F1 — Open call → 4 tabs.** Open one call. Click Overview, then Transcript, then Invitees, then Participants. Screenshot each tab. Watch for VIS-04 (canonical tab pattern divergence). Record any divergence.

    **Flow F2 — Table filters.** On `/`, apply Date filter (try a specific range), apply Source filter (toggle Fathom vs Zoom), apply Contacts filter (try searching "Phill" — BUG/FILTER-03 specifically expects Phill Tomlinson to return when contacts DB is queried fully), apply Tags filter. Screenshot each filtered state. Watch for FILTER-01 (Folder filter still present?), FILTER-02 (Duration filter still present?), FILTER-03 (Contacts query incomplete?), FILTER-04 (Source filter overflow). For each existing filter requirement, record [RE-VERIFY-FILTER-NN] or [NO-REPRO-FILTER-NN]. Clear filters after.

    **Flow F3 — DND drag-to-folder.** Identify a folder in the 2nd pane. Use `mcp__dev-browser__evaluate` or screenshot inspection to find the drag handle on a call row. Drag the call to the folder. Capture before/during/after screenshots. Watch for DND-01 (handle shifts on selection), DND-02 (drag target size). Note finding tags. **Cleanup:** if the call was actually moved into a folder it wasn't in before, drag it back out (or remove the folder assignment) before plan completion.

    **Flow F4 — Tag with AI (BUG-01 re-verify).** Open a **Fathom-imported** call (look for source indicator). Click "Tag with AI" button (or equivalent action — find via DOM inspection). Capture the result. **Check browser console** via `mcp__dev-browser__console_messages()` for `invalid input syntax for type uuid` text. If found: tag `[RE-VERIFY-BUG-01]` Severity P0, include the exact console log in the Backend log block. If NOT found: tag `[NO-REPRO-BUG-01]` Severity (existing) and note that closure requires Phase 30 to re-verify (per D-08, No-repro does not close).

    **Flow F5 — Toggle default-workspace (BUG-02 re-verify).** Navigate to a Workspace Detail panel (open Pane 4 on a workspace card). Toggle the "default workspace" switch. **Check network panel** for `PATCH /workspaces` → 406 PGRST116. Toast text: capture verbatim. If 406 observed: tag `[RE-VERIFY-BUG-02]` P0 with the network log block. If not observed: `[NO-REPRO-BUG-02]`. **Cleanup:** toggle the default back to its original setting before completing.

    **Flow F6 — Move/delete/tag → table refresh (BUG-03 re-verify).** Apply a benign tag to a call (e.g., create or pick an existing "qa-sweep-test" tag). Observe whether the table reflects the change without page reload. Capture before/after screenshots. Look at the toast vs the table state — do they agree? Tag accordingly. **Cleanup:** remove the tag at end.

    **Flow F7 — Date sort ascending / descending (BUG-04 re-verify).** Click the Date column header to sort ascending. Take a screenshot showing the first ~5 visible rows' dates. Click again to sort descending. Capture again. Visually scan for out-of-order dates. Record findings.

    **Flow F8 — Import Source Manager: "+" and "Import History" (BUG-06/07 re-verify).** Navigate to the Import Source Manager (Pane 2 of `/import` or wherever it lives). Click the "+" button. Capture what happens (modal? nothing?). Click "Import History" button. Capture. Tag findings per actual behavior.

    **Flow F9 — Create new workspace, verify no auto-folders (BUG-08 re-verify).** Create a new workspace named `qa-sweep-test-{timestamp}` in AI SIMPLE org. Immediately after creation, look at the workspace's folder list. Check for "Hall of Fame" or "Manager Reviews" auto-created. Capture. Tag accordingly. **Cleanup:** delete the test workspace at end of plan.

    **Flow F10 — Create share link addressed to naegele412@gmail.com.** Open the Share Call modal for any owner-accessible call. Set recipient email to `naegele412@gmail.com`. Generate share link. Capture the resulting URL. Add to the cleanup list as "preserved for Plan 04" (do NOT delete this share link — Plan 29-04 needs it). While inspecting the modal, observe SHARE-03 symptoms (spurious orange/red field borders, broken red dots icon, overflow against parent dialog). Record findings.

    **Flow F11 — Manual paste/upload UI (BUG-05 re-verify).** Look for a manual transcript paste / upload entry point on the `/import` page. If absent: tag `[RE-VERIFY-BUG-05]` Severity per existing rubric. If present: tag `[NO-REPRO-BUG-05]`. Do not actually paste a transcript (no mutation).

    **Flow F12 — Dialog accessibility (BUG-09 re-verify).** While various modals were open during F1–F10, check `mcp__dev-browser__console_messages()` for the React `DialogContent requires a DialogTitle / DialogDescription` warning. If observed during any modal open: tag `[RE-VERIFY-BUG-09]`. If never observed: `[NO-REPRO-BUG-09]`.

    **Cleanup pass.** Walk the Cleanup List at the top of the findings file and perform each revert action. Update each checkbox to `[x]` after completion. The share link to `naegele412@gmail.com` stays — annotate it `[x] (preserved for Plan 04 — share token: {token})`.

    Per D-13: **do NOT edit REQUIREMENTS.md or ROADMAP.md in this plan.** Plan 29-05 owns the catalog write-back.

    Per security threat T-29-02-02: when capturing screenshots that include real user emails (recipient lists, invitee panels, etc.), do not redact in the screenshot itself but note in the finding text that emails should be masked in the Plan 29-05 write-back (e.g., `re***@example.com` style).
  </action>
  <verify>
    <automated>grep -cE "^### Finding [0-9]" .planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md > /dev/null && grep -E "BUG-0[1-9]" .planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md | wc -l | awk '{exit ($1 < 9) ? 1 : 0}' && grep -E "SHARE-0[1-4]" .planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md | wc -l | awk '{exit ($1 < 1) ? 1 : 0}' && grep -q "naegele412@gmail.com" .planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md && grep -c "^- \[x\]" .planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md | awk '{exit ($1 < 1) ? 1 : 0}' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Every BUG-NN (BUG-01 through BUG-09) is referenced in the file at least once with a tag (`[RE-VERIFY-BUG-NN]` or `[NO-REPRO-BUG-NN]` or `[MATCHES-EXISTING-BUG-NN]`) — grep `BUG-0[1-9]` returns ≥ 9 occurrences
    - At least one SHARE-NN finding entry references the Share Call modal observation (F10)
    - The share link addressed to `naegele412@gmail.com` is recorded in the cleanup list and annotated `[x] (preserved for Plan 04 ...)` with the actual share token included for Plan 04 to consume
    - Cleanup list has every line checked off `[x]` (no `[ ]` items remaining)
    - No additional test workspaces exist on production (qa-sweep-test-* workspace was deleted)
    - For every flow F1–F12, at least one Finding entry exists OR a `## Re-verification Notes` section explicitly records that flow as "ran, no finding worth a separate entry, observed: {brief note}"
    - File contains the literal string `naegele412@gmail.com` (the share recipient address — used to chain Plan 04)
  </acceptance_criteria>
  <done>Every Persona A flow on the D-11 checklist has been exercised on production, every BUG-NN existing requirement has a sweep observation tag, the share link for Plan 04 is recorded, and all test mutations (except the preserved share link) have been reverted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persona A → production data | Owner has full write access across 3 orgs; sweep must be read-only beyond explicitly listed test mutations |
| Real user PII → captured screenshots | Owner sees other org members' emails, call titles, contact lists — these end up in screenshots |
| dev-browser console logs → notes file | Console output captured for D-05 backend-log entries may include user IDs, JWT fragments, or other tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | Tampering | Production data (workspaces, calls, tags, share links) | mitigate | Explicit cleanup list with `[x]` accounting at plan end; every test mutation is reverted except the preserved share link Plan 04 needs |
| T-29-02-02 | Information Disclosure | Real-user PII in screenshots | accept | Owner-facing screenshots inevitably show colleague names/emails — this is the trade-off for visible-bug evidence. Plan 05 enforces masked text in the Markdown body (`re***@example.com`); the screenshot binaries remain as captured |
| T-29-02-03 | Information Disclosure | JWT / session tokens leaked into Backend log blocks | mitigate | When capturing console output for D-05 logs, the executor must redact any `Bearer ...` strings or full JWT triples (`eyJ.eyJ.signature`) to `Bearer <redacted>` / `<jwt-redacted>` before pasting into the notes file |
| T-29-02-04 | Tampering | Auto-AI-tag mutation triggered during BUG-01 re-verify | accept | "Tag with AI" is the symptom-reproduction action — it WILL run the buggy code path. Side effect is an attempted tag write that errors out; no successful mutation occurs precisely because BUG-01 is failing. If it does succeed, document and revert the tag in cleanup |
</threat_model>

<verification>
- Every authenticated route in src/App.tsx has a ✓ in the Coverage Matrix in each org column (or an explicit n/a with reason)
- Every D-11 Persona A flow has at least one Finding entry or a recorded "ran, no finding" note
- All BUG-NN identifiers (BUG-01 through BUG-09) appear as Finding tags in the notes file
- The share-link recipient `naegele412@gmail.com` is present in the file (Plan 04 chain handoff)
- Cleanup list shows zero unchecked items; no test workspaces or test tags remain on production
- Screenshot count under `screenshots/persona-a-*.png` ≥ 20 (rough proxy for sufficient coverage)
- No Bearer-token or full-JWT string appears in the notes file (grep for `eyJ` returns 0 matches outside of redacted markers)
</verification>

<success_criteria>
- D-11 route coverage condition is satisfied for Persona A
- D-11 flow checklist for Persona A is fully exercised
- Plan 29-05 can author every QA-NN entry from the raw notes alone, without re-running the sweep
- Plan 29-04 has a known-good share link token to consume for the wrong-account recipient test
- Production is in the same state it was in before Plan 29-02 (modulo the preserved share link)
</success_criteria>

<output>
After completion, create `.planning/phases/29-qa-sweep-regression-catalog/29-02-SUMMARY.md` summarizing: routes covered (count), orgs cycled (3), findings count by severity (P0/P1/P2/P3), findings count by tag type (NEW/RE-VERIFY/NO-REPRO/MATCHES-EXISTING), share-link token recorded for Plan 04, cleanup status.
</output>
