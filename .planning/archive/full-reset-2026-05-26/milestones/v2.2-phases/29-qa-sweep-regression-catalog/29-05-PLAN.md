---
phase: 29-qa-sweep-regression-catalog
plan: 05
type: execute
wave: 5
depends_on: [29-02, 29-03, 29-04]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/BACKLOG.md
autonomous: true
requirements: [QA-01]
must_haves:
  truths:
    - "Every [NEW] finding from Plans 29-02/03/04 is converted to a QA-NN entry in REQUIREMENTS.md under the new section ### QA Sweep Findings (Phase 29, 2026-05-11)"
    - "QA-NN IDs start at QA-02 and increment sequentially in observation order"
    - "Every existing v2.2 requirement (73 active rows in the traceability table) has a Sweep Status column value (Confirmed / No-repro / Cannot-verify / Not-tested) derived from the three notes files"
    - "Every new QA-NN entry follows the D-03 format exactly (title + steps + URL + observed + expected + severity + maps-to + screenshot path + conditional log)"
    - "Each new QA-NN's Maps-to destination is recorded in REQUIREMENTS.md AND reflected in ROADMAP.md (appended to the destination phase's Requirements list OR a new mini-phase added per D-13 OR appended to BACKLOG.md per D-09)"
    - "If ≥3 new findings cluster around a single subsystem, a new themed mini-phase numbered between 41 and 42 is added to ROADMAP.md (e.g., Phase 41.5) per D-09"
    - "P2/P3 orphan findings that don't fit any v2.2 phase are appended to BACKLOG.md"
    - "Real-user PII in catalog entries is masked (na***@gmail.com style) per T-29-05-01"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated with new section ### QA Sweep Findings (Phase 29, 2026-05-11) and new Sweep Status column in traceability table"
      contains: "### QA Sweep Findings (Phase 29, 2026-05-11)"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 36 (or other) Requirements list extended with new QA-NN IDs; Progress table updated if a new mini-phase added"
      contains: "QA-"
    - path: ".planning/BACKLOG.md"
      provides: "P2/P3 orphan findings appended; existing structure preserved"
  key_links:
    - from: ".planning/phases/29-qa-sweep-regression-catalog/29-0{2,3,4}-*-NOTES.md"
      to: ".planning/REQUIREMENTS.md (new QA Sweep Findings section)"
      via: "[NEW] finding entries from notes are rewritten to D-03 format and appended; tag types become Sweep Status column values"
      pattern: "QA-[0-9]+"
    - from: "Every new QA-NN entry"
      to: "Its assigned destination phase OR BACKLOG"
      via: "Maps-to line states the phase number explicitly; the destination phase's ROADMAP entry is also updated to include the QA-NN ID"
      pattern: "Maps to: Phase [0-9]+"
---

<objective>
Convert raw observation notes from Plans 29-02 / 29-03 / 29-04 into the locked catalog format: QA-NN entries appended to REQUIREMENTS.md, Sweep Status column added to the traceability table, ROADMAP.md updated to route new findings to their destination phases (or a new themed mini-phase if ≥3 cluster), BACKLOG.md updated for P2/P3 orphans. This is the single source of truth for the v2.2 regression catalog.

Purpose: Plans 02–04 are observation; Plan 05 is the synthesis. Without Plan 05, the sweep is just notes — downstream phases can't consume notes files. After Plan 05, every downstream phase has a clear scope delta (new requirements + sweep-status hints).

Output: Three edits — REQUIREMENTS.md, ROADMAP.md, BACKLOG.md. Plus a checklist of which QA-NN ID maps to which phase, embedded in REQUIREMENTS.md per D-03. Zero source code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/BACKLOG.md
@.planning/phases/29-qa-sweep-regression-catalog/29-CONTEXT.md
@.planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md
@.planning/phases/29-qa-sweep-regression-catalog/29-03-PERSONA-B-SWEEP-NOTES.md
@.planning/phases/29-qa-sweep-regression-catalog/29-04-PERSONA-C-SWEEP-NOTES.md
@CLAUDE.md

<interfaces>
<!-- Exact locked formats from CONTEXT.md D-03, D-07, D-12. -->

**D-03 / D-12 — Each new QA-NN entry format (verbatim, append under new section header):**
```
- [ ] **QA-NN** — {one-line title, <80 chars}
  - **Surface/Route:** {URL or in-app location}
  - **Persona:** {A | B | C}
  - **Steps to reproduce:**
    1. {step}
    2. {step}
  - **Observed:** {what happened}
  - **Expected:** {what should happen}
  - **Severity:** {P0 | P1 | P2 | P3}
  - **Maps to:** {Phase NN — or BACKLOG}
  - **Screenshot:** ![](phases/29-qa-sweep-regression-catalog/screenshots/qa-NN-{slug}.png)
  - **Backend log (conditional):** {fenced code block if applicable per D-05}
  - **Related:** {existing requirement ID if partial-match per D-06}
```

Note: the screenshot path is RELATIVE to `.planning/REQUIREMENTS.md` location — so it includes the `phases/29-qa-sweep-regression-catalog/screenshots/` prefix. Since Plans 02-04 stored screenshots under `screenshots/` directly (relative to the phase dir), the executor may need to either (a) rename the source PNG files to `qa-NN-{slug}.png` and update the existing path references in the notes files for traceability, or (b) keep the existing `persona-{a,b,c}-*.png` filenames and use them in QA-NN entries. **D-04 + D-12 phrasing prefers `qa-NN-{slug}.png`** — so the executor SHOULD copy or rename relevant screenshots to the `qa-NN-{slug}.png` pattern. The original persona-* filenames may stay alongside as raw-evidence backing.

**Section placement (D-12):** Insert `### QA Sweep Findings (Phase 29, 2026-05-11)` between the existing `### Tech Debt (carried forward from v2.0 / v2.1)` section and the `## Future Requirements (captured to BACKLOG, not v2.2)` section. Use the literal header text and date.

**D-07 — Sweep Status column added to traceability table:**
Modify the table header from:
```
| Req ID | Description | Phase | Status |
```
to:
```
| Req ID | Description | Phase | Status | Sweep Status |
```

For each existing row, populate Sweep Status with exactly one of: `Confirmed`, `No-repro`, `Cannot-verify`, `Not-tested`. Mapping from notes-file tags:
- `[RE-VERIFY-{ID}]` in notes → `Confirmed` in column (the bug still reproduces)
- `[NO-REPRO-{ID}]` → `No-repro`
- `[MATCHES-EXISTING-{ID}]` → `Confirmed` (the new observation is an instance of this existing requirement)
- `[CANNOT-VERIFY-{ID}]` → `Cannot-verify`
- Requirements NEVER referenced in any of the three notes files → `Not-tested`

**Append new QA-NN rows to the traceability table** in addition to the new section. Each QA-NN row: `| QA-NN | {one-line title} | {Phase NN | BACKLOG} | Active | Confirmed |` (new findings are by definition Confirmed — the sweep just observed them).

**D-08 — `No-repro` does NOT close the requirement.** Status column stays `Active` for No-repro rows. The Sweep Status column is informational only.

**D-09 — Routing rules for new QA-NN:**
- P0/P1 findings that fit an existing phase's scope → append QA-NN to that phase's Requirements line in ROADMAP.md
- P0/P1 findings that don't fit any existing phase → default destination is Phase 36 (Critical Bug Sweep)
- **If ≥3 new findings cluster around a single subsystem** (e.g., 4 transcript-player bugs, 5 MCP-token-UI bugs) → create a new themed mini-phase numbered between 41 and 42 (e.g., Phase 41.5 — QA-Discovered: Transcript Player). Add to ROADMAP.md `## Phase Details` section AND `## Progress` table.
- P2/P3 findings that don't fit any v2.2 phase → append to `.planning/BACKLOG.md` (with sender/severity/screenshot path).

**D-13 — Allowed ROADMAP.md mutations only:**
- Add QA-NN IDs to existing phases' `**Requirements**:` lines
- Add new themed mini-phase between 41 and 42 with Goal/Depends/Requirements/Success Criteria/Plans:TBD
- Add corresponding row to `## Progress` table
- **NOT allowed:** reorder existing phases, remove existing requirements, change existing dependencies

**Clustering decision algorithm:**
1. Group all [NEW] findings by their most natural subsystem keyword (e.g., transcript-player, mcp-tokens, org-admin, billing-display, search-modal, sidebar, table-column, share-modal, signup-flow)
2. Count findings per cluster
3. Clusters ≥ 3: candidate for new mini-phase. Only create the mini-phase if the cluster is NOT already a primary scope of an existing v2.2 phase (e.g., 5 sidebar findings → still goes to Phase 34, not a new mini-phase, because Phase 34 already owns sidebar polish)
4. Clusters < 3: route individually per D-09

**PII masking enforcement (T-29-05-01):**
- Any real-user email referenced in a finding step/observation must be masked: `na***@gmail.com`, `re***@example.com`, `te***@vibeos.com`
- Org names, call titles, contact names that aren't obviously test data: replace with `<call-title-redacted>` / `<contact-name-redacted>` / `<org-name-redacted>` if they identify a real third party. The 3 test orgs (AI SIMPLE, BUSINESS, GOVIBEY) may be named directly — they are test artifacts.
- The test email `naegele412@gmail.com` (from Plan 04) is masked to `na***@gmail.com` in all finding bodies; it may appear unmasked only in the share-token traceability metadata block

**Footer update (matches existing pattern):**
After all edits to REQUIREMENTS.md, update the existing footer `*Last updated: ...*` to reflect the new state: add count of new QA-NN entries and the new active total. Example: `*Last updated: 2026-05-11 — SEC-01..05 split into 17 atomic items ... Plus Phase 29 QA Sweep added N new QA-NN findings. Total: 73 + N active requirements + 1 validated.*`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synthesize notes → REQUIREMENTS.md (new QA-NN section + Sweep Status column)</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/phases/29-qa-sweep-regression-catalog/screenshots/
  </files>
  <read_first>
    - .planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md (all Findings)
    - .planning/phases/29-qa-sweep-regression-catalog/29-03-PERSONA-B-SWEEP-NOTES.md (all Findings)
    - .planning/phases/29-qa-sweep-regression-catalog/29-04-PERSONA-C-SWEEP-NOTES.md (all Findings)
    - .planning/REQUIREMENTS.md (current state — Active Requirements + traceability table)
    - .planning/phases/29-qa-sweep-regression-catalog/29-CONTEXT.md (D-03 catalog format, D-07 sweep status, D-12 section placement)
  </read_first>
  <action>
    **Step 1: Aggregate.** Read all three notes files. Build a working list of:
    - All `[NEW]` finding entries → become new QA-NN
    - All `[RE-VERIFY-{ID}]`, `[NO-REPRO-{ID}]`, `[MATCHES-EXISTING-{ID}]`, `[CANNOT-VERIFY-{ID}]` tags → become Sweep Status column entries on the existing rows

    Assign sequential QA-NN IDs starting at QA-02 (QA-01 is the existing sweep-execution row). Order by Plan number first (02 → 03 → 04), then by Finding sequence within each plan. So QA-02 is the first [NEW] finding from Plan 02 Task 1; QA-XX is the last [NEW] from Plan 04.

    **Step 2: Apply PII masking.** Walk each new QA-NN's body text. Replace real-user emails with `re***@example.com` style; replace identifying third-party names (call titles, contact names) with `<...-redacted>`. Test-org names (AI SIMPLE, BUSINESS, GOVIBEY) and the test email pattern `na***@gmail.com` for the share recipient stay.

    **Step 3: Copy/rename screenshots.** For each new QA-NN, identify its backing screenshot from the notes. Copy or rename the file from `persona-{a,b,c}-*.png` to `qa-NN-{slug}.png` under the same `screenshots/` directory. The kebab slug should be a short summary of the title (max ~30 chars). Keep the original persona-* file alongside as raw evidence. Update the QA-NN entry's Screenshot path to use the new `qa-NN-{slug}.png` filename. If a QA-NN has multiple screenshots, name them `qa-NN-{slug}-1.png`, `qa-NN-{slug}-2.png`, etc.

    **Step 4: Cluster analysis.** Group [NEW] findings by subsystem keyword. Count per cluster. Apply D-09 clustering decision algorithm. For each cluster ≥ 3 NOT already covered by an existing phase: nominate a new mini-phase (Phase 41.5, 41.6, etc.). Record the cluster decision in the file as a comment block at the top of the new section:
    ```markdown
    <!--
    Phase 29 cluster decisions:
    - {N} findings in cluster {X}: routed to Phase {YY} (no mini-phase needed — Phase {YY} already covers {scope})
    - {N} findings in cluster {Z}: → NEW Phase 41.5 QA-Discovered: {ThemeName}
    - {N} findings P2/P3 orphan: → BACKLOG
    -->
    ```

    **Step 5: Edit REQUIREMENTS.md.** Use the Edit tool (not Write — REQUIREMENTS.md is huge and must preserve existing content):

    **Edit A — Insert new section.** Insert immediately before the line `## Future Requirements (captured to BACKLOG, not v2.2)`:
    ```markdown
    ### QA Sweep Findings (Phase 29, 2026-05-11)

    <!-- Cluster decisions block from Step 4 -->

    {QA-02 entry per D-03 format}

    {QA-03 entry per D-03 format}

    ... and so on
    ```

    **Edit B — Update traceability table header.** Find the line `| Req ID | Description | Phase | Status |` and change it to `| Req ID | Description | Phase | Status | Sweep Status |`. Also update the separator row `|--------|-------------|-------|--------|` to `|--------|-------------|-------|--------|--------------|`.

    **Edit C — Populate Sweep Status column for existing rows.** For each existing row (QA-01 through DEBT-03), add the `| {Sweep Status value} |` column at the end, derived from notes-file tag aggregation. Rules:
    - If ANY notes file has `[RE-VERIFY-{ID}]` or `[MATCHES-EXISTING-{ID}]` → `Confirmed`
    - Else if ANY notes file has `[NO-REPRO-{ID}]` → `No-repro`
    - Else if ANY notes file has `[CANNOT-VERIFY-{ID}]` → `Cannot-verify`
    - Else (requirement never referenced in any notes file) → `Not-tested`
    - QA-01 itself → `Confirmed` (this phase is actively running it)
    - SEC-01E (already validated done) → preserve its existing `✓ Validated (2026-05-11)` Status; Sweep Status = `Not-tested` (it's already closed, not in sweep scope)

    **Edit D — Append new QA-NN rows to traceability table.** After the last existing row (DEBT-03), append one row per new QA-NN:
    ```
    | QA-NN | {one-line title, <80 chars} | {destination phase or BACKLOG} | Active | Confirmed |
    ```

    **Edit E — Update footer.** Find `*Last updated: 2026-05-11 — SEC-01..05 split into 17 atomic items...` and extend with the QA sweep summary: `... Plus Phase 29 QA Sweep added N new QA-NN findings (P0: x, P1: y, P2: z, P3: w). Total: 73 + N active requirements + 1 validated → {phase count} phases, 100% coverage.*`

    Save. Per CLAUDE.md "Reality over documentation" — the catalog is now the single source of truth for v2.2 production state.
  </action>
  <verify>
    <automated>grep -q "^### QA Sweep Findings (Phase 29, 2026-05-11)$" .planning/REQUIREMENTS.md && grep -q "| Req ID | Description | Phase | Status | Sweep Status |" .planning/REQUIREMENTS.md && grep -cE "^\| (QA|AUTH|SHARE|BUG|VIS|BRAND|TABLE|FILTER|DND|CARD|SEC|FEAT|DEBT)-[0-9A-Z]+ \|" .planning/REQUIREMENTS.md | awk '{exit ($1 < 73) ? 1 : 0}' && grep -cE "^\| QA-[0-9]+ \|.*\| Confirmed \|$" .planning/REQUIREMENTS.md | awk '{exit ($1 < 1) ? 1 : 0}' && ! grep -E "@(gmail|vibeos|example)\.com" .planning/REQUIREMENTS.md | grep -vE "^\| AUTH-03 \||soren@vibeos\.com.*free-tier|na\*\*\*@gmail\.com" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Section header `### QA Sweep Findings (Phase 29, 2026-05-11)` exists in REQUIREMENTS.md, inserted between Tech Debt and Future Requirements sections
    - Traceability table header includes the new `Sweep Status` column (grep `| Req ID | Description | Phase | Status | Sweep Status |`)
    - Every pre-existing traceability row (QA-01 through DEBT-03 — 73 rows) has a Sweep Status value (one of: Confirmed, No-repro, Cannot-verify, Not-tested). Verify: every row matching `^| [A-Z]+-[0-9A-Z]+` has at least 5 `|` separators after the ID
    - At least one new QA-NN row exists in the table (or — if Plans 02/03/04 surfaced literally zero new findings — the new section contains a single line `_No net-new findings — every observation matched an existing requirement._` and is allowed to be empty of QA-NN entries)
    - Every new QA-NN entry has all required D-03 fields (Surface/Route, Persona, Steps to reproduce, Observed, Expected, Severity, Maps to, Screenshot — Backend log + Related are conditional)
    - Every Severity in new QA-NN entries is exactly P0/P1/P2/P3
    - PII masking is applied — no real emails outside the explicit allow-list (AUTH-03 row's `soren@vibeos.com`, free-tier canary reference; `na***@gmail.com` masked share recipient). Specifically: `naegele412@gmail.com` does NOT appear in REQUIREMENTS.md
    - Footer line updated to reflect new QA-NN count
    - For each new QA-NN, a `qa-NN-{slug}.png` file exists in `screenshots/` (the renamed/copied screenshot from the persona-* original)
  </acceptance_criteria>
  <done>REQUIREMENTS.md now contains every new finding as a properly-formatted QA-NN entry, every existing requirement has a Sweep Status, the traceability table is extended, and screenshots are renamed to the canonical qa-NN-{slug}.png pattern. ROADMAP.md and BACKLOG.md update next.</done>
</task>

<task type="auto">
  <name>Task 2: Mutate ROADMAP.md (append QA-NN IDs to destination phases, add mini-phase if clustered) + append BACKLOG.md</name>
  <files>
    .planning/ROADMAP.md
    .planning/BACKLOG.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (new QA-NN entries from Task 1 — their Maps-to lines drive every ROADMAP edit)
    - .planning/ROADMAP.md (current state)
    - .planning/BACKLOG.md (current state — preserve existing structure)
    - .planning/phases/29-qa-sweep-regression-catalog/29-CONTEXT.md (D-09, D-13 allowed mutations)
  </read_first>
  <action>
    **Step 1: Build the route table.** Parse Task 1's new QA-NN entries. Build a working list:
    | QA-NN | Maps to | Severity |
    Read the `Maps to:` line from each new QA-NN.

    **Step 2: Mutate ROADMAP.md for each destination phase.** For each existing v2.2 phase that has at least one new QA-NN routed to it: edit the `**Requirements**:` line to append the new IDs. Example:
    Before: `**Requirements**: BUG-02, BUG-03, BUG-04, BUG-05, BUG-06, BUG-07, BUG-08, BUG-09`
    After:  `**Requirements**: BUG-02, BUG-03, BUG-04, BUG-05, BUG-06, BUG-07, BUG-08, BUG-09, QA-05, QA-12, QA-18`

    Use the Edit tool — find each phase's `**Requirements**:` line by surrounding `### Phase NN:` context and append IDs in numeric order.

    Allowed phase numbers for destination (per D-13 — cannot create new phases unless mini-phase clustering):
    Phase 30 (BUG-01), Phase 31 (Auth), Phase 32 (Share landing), Phase 33 (Selection state), Phase 34 (Sidebar/brand), Phase 35 (Table/filters/DND), Phase 36 (Critical bug sweep — default catch-all), Phase 37 (Edge security), Phase 38 (Frontend security), Phase 39 (Fathom mirror), Phase 40 (Fathom re-import), Phase 41 (Tech debt closure).

    **Step 3: Add new mini-phase(s) if Task 1 cluster analysis recommended one.** If Task 1's cluster decisions block named one or more new mini-phases (e.g., Phase 41.5 — QA-Discovered: Transcript Player):

    For each new mini-phase, insert AFTER the existing `### Phase 41:` block and BEFORE `---` / `## Progress`. Use this template:
    ```markdown
    ### Phase 41.5: QA-Discovered: {ThemeName}
    **Goal**: {1-sentence outcome derived from the cluster — e.g., "All N QA-NN findings related to {subsystem} are resolved, leaving no known regressions in that surface"}
    **Depends on**: Phase 29 (originated as a QA-sweep cluster)
    **Requirements**: {QA-NN, QA-NN, QA-NN — the clustered IDs}
    **Success Criteria** (what must be TRUE):
      1. Each listed QA-NN's Observed behavior is replaced by its Expected behavior on production (verified via dev-browser per CLAUDE.md)
      2. {1-2 additional cluster-specific criteria derived from the findings}
    **Plans**: TBD
    **UI hint**: yes  // if any clustered finding is visual
    ```

    Also append a corresponding row to the `## Progress` table:
    ```
    | 41.5. QA-Discovered: {ThemeName} | 0/TBD | Not started | - |
    ```

    Number mini-phases sequentially in cluster-discovery order: 41.5, 41.6, 41.7 if multiple.

    **Step 4: Update ROADMAP.md footer.** Find the existing `*Last updated: ...*` at the bottom and extend:
    `*Last updated: 2026-05-11 — Phase 29 QA Sweep completed; added {N} new QA-NN requirements routed to phases {...}; added {M} new themed mini-phase(s) {if any}; Progress table updated.*`

    **Step 5: Append P2/P3 orphans to BACKLOG.md.** For every QA-NN whose Maps-to is `BACKLOG`: append an entry to `.planning/BACKLOG.md` under a new section heading `## QA Sweep Orphans (Phase 29, 2026-05-11)`. Format each entry:
    ```markdown
    ### QA-NN — {one-line title}

    **Priority:** {derived from Severity — P2 → Low-medium, P3 → Low}
    **Discovered:** 2026-05-11 (Phase 29 QA Sweep)
    **Status:** Captured, deferred to v2.3+

    **Description:** {Observed text from REQUIREMENTS.md QA-NN}

    **Expected:** {Expected text from REQUIREMENTS.md QA-NN}

    **Persona:** {A | B | C}
    **Surface:** {Surface/Route}
    **Screenshot:** `.planning/phases/29-qa-sweep-regression-catalog/screenshots/qa-NN-{slug}.png`

    ---
    ```

    Insert the new `## QA Sweep Orphans` section AT THE END of BACKLOG.md, after all existing entries but before any final timestamp comment. If no P2/P3 orphans exist, skip this section entirely (do not write an empty section).

    **Step 6: Update REQUIREMENTS.md ↔ ROADMAP.md consistency.** Every new QA-NN row in REQUIREMENTS.md's traceability table has a Phase column entry. Cross-check that:
    - Every Phase column value in new QA-NN rows points to a phase that now lists the QA-NN ID in its ROADMAP `**Requirements**:` line
    - OR points to `BACKLOG`, in which case the QA-NN appears in BACKLOG.md's new `## QA Sweep Orphans` section

    If any mismatch is found: fix the ROADMAP/REQUIREMENTS pair before completing the task.

    **Per D-13: do NOT** reorder existing phases, remove existing requirements, or change existing dependencies. Only the listed additions are allowed.
  </action>
  <verify>
    <automated>grep -qE "^### Phase [0-9.]+:" .planning/ROADMAP.md && for QA_ID in $(grep -E "^\| QA-[0-9]+ \|" .planning/REQUIREMENTS.md | grep -v "QA-01" | awk -F'|' '{print $2}' | tr -d ' '); do DEST=$(grep -E "^\| $QA_ID \|" .planning/REQUIREMENTS.md | awk -F'|' '{print $4}' | tr -d ' '); if [ "$DEST" = "BACKLOG" ]; then grep -q "$QA_ID" .planning/BACKLOG.md || { echo "MISSING in BACKLOG: $QA_ID"; exit 1; }; elif echo "$DEST" | grep -qE "^Phase"; then PHASE_NUM=$(echo "$DEST" | sed 's/Phase//'); grep -qE "### Phase ${PHASE_NUM}.*" .planning/ROADMAP.md && grep -A 5 -E "### Phase ${PHASE_NUM}[:.]" .planning/ROADMAP.md | grep -q "$QA_ID" || { echo "MISSING in ROADMAP phase $PHASE_NUM: $QA_ID"; exit 1; }; fi; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md still contains every original phase (29 through 41) in its original order — verify with `grep -c "^### Phase " .planning/ROADMAP.md` ≥ 13
    - For every new QA-NN routed to an existing phase: that phase's `**Requirements**:` line includes the QA-NN ID
    - For every new QA-NN routed to BACKLOG: an entry appears in `.planning/BACKLOG.md`'s new `## QA Sweep Orphans (Phase 29, 2026-05-11)` section
    - If Task 1's cluster analysis recommended new mini-phase(s): each appears in ROADMAP.md `### Phase 41.5:` (and so on) with Goal/Depends/Requirements/Success Criteria/Plans:TBD AND appears in the Progress table
    - REQUIREMENTS.md's traceability table Phase column for each new QA-NN matches the destination it actually routes to in ROADMAP.md / BACKLOG.md (no mismatched routing)
    - ROADMAP.md footer updated with the new last-updated note mentioning Phase 29 sweep summary
    - No existing phase was reordered, no existing requirement was removed, no existing dependency was changed (D-13)
    - BACKLOG.md preserves all existing sections in their original order; the QA Sweep Orphans section is appended at the end
  </acceptance_criteria>
  <done>ROADMAP.md and BACKLOG.md reflect every new QA-NN routing. Every QA-NN in REQUIREMENTS.md has a consistent destination across all three files. The catalog is now self-consistent and consumable by every downstream phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sweep notes (PII may be present) → committed REQUIREMENTS.md / ROADMAP.md / BACKLOG.md | Mask before commit |
| Existing roadmap structure → Phase 29 edits | Locked allowed mutations per D-13 — do not violate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05-01 | Information Disclosure | Real user emails / call titles / contact names in committed catalog | mitigate | Apply masking rules in Task 1 Step 2; acceptance criterion enforces `naegele412@gmail.com` does NOT appear in REQUIREMENTS.md; spot-check during commit |
| T-29-05-02 | Tampering | Existing roadmap structure | mitigate | D-13 lists allowed mutations explicitly; Edit-tool diffs are reviewable; acceptance criterion checks phase count unchanged (≥13) |
| T-29-05-03 | Integrity | Routing inconsistency between REQUIREMENTS.md and ROADMAP.md | mitigate | Task 2 Step 6 cross-checks every new QA-NN's destination; automated verify command iterates and fails on any mismatch |
</threat_model>

<verification>
- REQUIREMENTS.md has new `### QA Sweep Findings (Phase 29, 2026-05-11)` section
- Sweep Status column exists in traceability table; every existing row populated
- Every new QA-NN has all D-03 fields with valid Severity (P0/P1/P2/P3)
- ROADMAP.md updated: destination phases list new QA-NN IDs; new mini-phase(s) added if clustered ≥ 3
- BACKLOG.md has `## QA Sweep Orphans` section if any P2/P3 routed there
- No PII / no Bearer tokens / no real emails outside masking rules
- Cross-file routing consistency verified
</verification>

<success_criteria>
- A developer reading REQUIREMENTS.md alone could reproduce every QA-NN finding from its description (ROADMAP success criterion #4)
- Every downstream v2.2 phase has its updated scope reflected in ROADMAP.md
- BACKLOG.md captures P2/P3 orphans for v2.3+ triage
- The three files (REQUIREMENTS.md, ROADMAP.md, BACKLOG.md) are mutually consistent on every new QA-NN's destination
- Plan 29-06 can run automated verification against the finished catalog
</success_criteria>

<output>
After completion, create `.planning/phases/29-qa-sweep-regression-catalog/29-05-SUMMARY.md` summarizing: total new QA-NN count by severity, destination phase distribution, new mini-phase(s) created (if any), BACKLOG orphans count, files mutated.
</output>
