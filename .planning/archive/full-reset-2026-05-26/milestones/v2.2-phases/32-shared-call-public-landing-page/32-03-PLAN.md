---
phase: 32-shared-call-public-landing-page
plan: 03
type: execute
wave: 3
depends_on: [32-01, 32-02]
files_modified:
  - src/components/sharing/ShareCallDialog.tsx
autonomous: true
requirements: [SHARE-03, SHARE-04]
must_haves:
  truths:
    - "ShareCallDialog imports RiLinksLine instead of RiLinkM — all 3 occurrences (CREATE button icon, link-row icon, empty-state icon) replaced"
    - "ShareCallDialog 'newly-created' row no longer uses border-green-500/50 or bg-green-500/5 — replaced with ring-2 ring-foreground/10 transitional ring OR motion.div scale pulse"
    - "ShareCallDialog 'Copied to clipboard' indicator text changes from text-green-600 to text-foreground"
    - "ShareCallDialog DialogContent has max-h-[calc(100vh-8rem)], overflow-hidden, flex flex-col — header and footer stay fixed, only the active-links list scrolls"
    - "ShareCallDialog copy is sentence-case for buttons (Create link, Close) and section labels (Add recipient, Active share links)"
    - "Production build (npm run build) exits 0 after all visual cleanup changes"
  artifacts:
    - path: "src/components/sharing/ShareCallDialog.tsx"
      provides: "Cleaned-up Share Call modal with locked tokens, icons, copy, and overflow behavior"
      contains: "Create link"
  key_links:
    - from: "src/components/sharing/ShareCallDialog.tsx"
      to: ".planning/phases/32-shared-call-public-landing-page/32-UI-SPEC.md cleanup specifics"
      via: "Three locked fixes per UI-SPEC §### ShareCallDialog cleanup specifics (SHARE-03)"
      pattern: "RiLinksLine"
---

<objective>
Apply the three Phase 29 QA-sweep findings to `ShareCallDialog.tsx` (SHARE-03): replace `RiLinkM` with `RiLinksLine`, remove the green-500 non-semantic highlight tokens in favor of `ring-foreground/10` + sentence-case copy, fix the modal overflow with flex sizing. This is a cleanup plan — NO structural refactoring, NO new features. After this plan, the full Phase 32 surface (backend + frontend + modal cleanup) is code-complete and ready for SHARE-04 end-to-end UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/32-shared-call-public-landing-page/32-CONTEXT.md
@.planning/phases/32-shared-call-public-landing-page/32-UI-SPEC.md
@.planning/phases/32-shared-call-public-landing-page/32-RESEARCH.md
@.planning/phases/32-shared-call-public-landing-page/32-PATTERNS.md
@src/CLAUDE.md

<interfaces>
UI-SPEC locked changes (verbatim — do NOT improvise):

**Icon swap (Fix 2):**
- `RiLinkM` → `RiLinksLine` (3 usage sites in the file).

**Token swap (Fix 1):**
- `border-green-500/50 bg-green-500/5` (line ~208) → `ring-2 ring-foreground/10` for the just-created row. The just-created row temporarily renders with a `ring-foreground/10` ring that auto-fades after 3 seconds via the existing `justCreatedId` state timer.
- `text-green-600` (line ~256) → `text-foreground`.

**Overflow fix (Fix 3):**
- `<DialogContent className="sm:max-w-lg">` → `<DialogContent className="sm:max-w-lg max-h-[calc(100vh-8rem)] overflow-hidden flex flex-col">`.
- The body `<div className="space-y-4">` → `<div className="space-y-4 flex-1 min-h-0 flex flex-col overflow-hidden">`.
- The active-links scroll container: remove `max-h-48`, change to `flex-1 min-h-0 overflow-y-auto`.

**Copy changes:**
| Element | Old | New |
|---------|-----|-----|
| Section label | `Create New Share Link` | `Add recipient` |
| Email placeholder | `Recipient email (optional)` | `recipient@example.com` |
| CREATE button | `CREATE` | `Create link` |
| Active links label | `Active Share Links ({count})` | `Active share links ({count})` |
| Empty state | `No share links yet. Create one above to share this call.` | `No share links yet. Add a recipient above to create one.` |
| Footer helper | `Revoked links will no longer work.` | `Revoked links stop working immediately.` |
| CLOSE button | `CLOSE` | `Close` |

Do NOT touch: the dialog title icon (`RiShareLine` — already correct), the dialog title text (`Share Call`), the description, the helper text under the email row (`Anyone with the link can view this call (account required).`), the `title` tooltips on Copy/Revoke icons, or the revoke confirmation behavior.
</interfaces>

<tasks>

<task id="32-03-T1">
<title>Swap RiLinkM imports and usages for RiLinksLine in ShareCallDialog</title>
<read_first>
- src/components/sharing/ShareCallDialog.tsx (full file)
- .planning/phases/32-shared-call-public-landing-page/32-UI-SPEC.md (Fix 2)
</read_first>
<action>
Open `src/components/sharing/ShareCallDialog.tsx`. In the icon import block at the top (lines ~17-26):

- Remove `RiLinkM` from the import list.
- Add `RiLinksLine` to the import list (if not already present).

Then replace all three usages of `<RiLinkM />` with `<RiLinksLine />`:
- Line ~180: inside the CREATE button.
- Line ~213: inside the share-link row.
- Line ~268: inside the empty-state.

Preserve the `className`s and other JSX attributes — only the component name changes.

Save the file.
</action>
<acceptance_criteria>
- `grep -c "RiLinkM" src/components/sharing/ShareCallDialog.tsx` returns 0
- `grep -c "RiLinksLine" src/components/sharing/ShareCallDialog.tsx` returns at least 3
- `npm run type-check` exits 0
</acceptance_criteria>
</task>

<task id="32-03-T2">
<title>Replace green-500 non-semantic highlight tokens with ring-foreground/10 + text-foreground</title>
<read_first>
- src/components/sharing/ShareCallDialog.tsx
- .planning/phases/32-shared-call-public-landing-page/32-UI-SPEC.md (Fix 1)
</read_first>
<action>
Open `src/components/sharing/ShareCallDialog.tsx`. Locate the share-link row's `className` (line ~205-209) — currently:

```tsx
className={cn(
  "p-3 rounded-md border bg-muted/30 transition-all",
  "hover:bg-muted/50",
  justCreatedId === link.id && "border-green-500/50 bg-green-500/5"
)}
```

Replace with:

```tsx
className={cn(
  "p-3 rounded-md border border-border bg-muted/30 transition-all",
  "hover:bg-muted/50",
  justCreatedId === link.id && "ring-2 ring-foreground/10"
)}
```

Locate the "Copied to clipboard" inline indicator (lines ~256-258) — currently:

```tsx
<span className="text-xs text-green-600 flex items-center gap-1">
  <RiCheckLine className="h-3 w-3" />
  Copied to clipboard
</span>
```

Replace with:

```tsx
<span className="text-xs text-foreground flex items-center gap-1">
  <RiCheckLine className="h-3 w-3" />
  Copied to clipboard
</span>
```

Save.
</action>
<acceptance_criteria>
- `grep -c "border-green-500\|bg-green-500\|text-green-600" src/components/sharing/ShareCallDialog.tsx` returns 0
- `grep -c "ring-foreground/10" src/components/sharing/ShareCallDialog.tsx` returns at least 1
- `npm run type-check` exits 0
</acceptance_criteria>
</task>

<task id="32-03-T3">
<title>Fix modal overflow with flex layout</title>
<read_first>
- src/components/sharing/ShareCallDialog.tsx
- .planning/phases/32-shared-call-public-landing-page/32-UI-SPEC.md (Fix 3)
</read_first>
<action>
Open `src/components/sharing/ShareCallDialog.tsx`. Make these three changes:

**A. `<DialogContent>` (line ~140):**

Change:
```tsx
<DialogContent className="sm:max-w-lg">
```

To:
```tsx
<DialogContent className="sm:max-w-lg max-h-[calc(100vh-8rem)] overflow-hidden flex flex-col">
```

**B. Body wrapper `<div className="space-y-4">` (line ~155):**

Change:
```tsx
<div className="space-y-4">
```

To:
```tsx
<div className="space-y-4 flex-1 min-h-0 flex flex-col overflow-hidden">
```

**C. Active-links scroll container (line ~201):**

Change:
```tsx
<div className="space-y-2 max-h-48 overflow-y-auto">
```

To:
```tsx
<div className="space-y-2 flex-1 min-h-0 overflow-y-auto">
```

Save.
</action>
<acceptance_criteria>
- `grep -c "max-h-\[calc(100vh-8rem)\]" src/components/sharing/ShareCallDialog.tsx` returns at least 1
- `grep -c "flex-1 min-h-0" src/components/sharing/ShareCallDialog.tsx` returns at least 2
- `grep -c "max-h-48" src/components/sharing/ShareCallDialog.tsx` returns 0
- `npm run type-check` exits 0
</acceptance_criteria>
</task>

<task id="32-03-T4">
<title>Apply sentence-case copy across the dialog</title>
<read_first>
- src/components/sharing/ShareCallDialog.tsx
- .planning/phases/32-shared-call-public-landing-page/32-UI-SPEC.md (copy table in interfaces)
</read_first>
<action>
Open `src/components/sharing/ShareCallDialog.tsx` and apply these exact string replacements:

| Location | Old | New |
|----------|-----|-----|
| Section label (line ~158) | `Create New Share Link` | `Add recipient` |
| Email placeholder (line ~164) | `Recipient email (optional)` | `recipient@example.com` |
| CREATE button (lines ~177-184) | `CREATE` (uppercase, inside the `<>...</>` block when not creating) | `Create link` |
| Active links label (line ~199) | `Active Share Links ({activeLinks.length})` | `Active share links ({activeLinks.length})` |
| Empty state (line ~269) | `No share links yet. Create one above to share this call.` | `No share links yet. Add a recipient above to create one.` |
| Footer helper (line ~275) | `Revoked links will no longer work.` | `Revoked links stop working immediately.` |
| CLOSE button (line ~280) | `CLOSE` | `Close` |

Do NOT change: `Share Call` title, the description text, the `Anyone with the link can view this call (account required).` helper text, or the `title` tooltips on the Copy/Revoke icons (which read `Copy link` and `Revoke link` — already correct sentence-case).

Save.
</action>
<acceptance_criteria>
- `grep -c "CREATE\b" src/components/sharing/ShareCallDialog.tsx` returns 0 (uppercase button label removed)
- `grep -c "CLOSE\b" src/components/sharing/ShareCallDialog.tsx` returns 0
- `grep -c "Create link" src/components/sharing/ShareCallDialog.tsx` returns at least 1
- `grep -c "Add recipient" src/components/sharing/ShareCallDialog.tsx` returns at least 1
- `grep -c "Active share links" src/components/sharing/ShareCallDialog.tsx` returns at least 1
- `grep -c "Add a recipient above to create one" src/components/sharing/ShareCallDialog.tsx` returns at least 1
- `grep -c "Revoked links stop working immediately" src/components/sharing/ShareCallDialog.tsx` returns at least 1
- `grep -c "Create New Share Link\|Recipient email (optional)\|Active Share Links\|Revoked links will no longer work" src/components/sharing/ShareCallDialog.tsx` returns 0 (old copy fully removed)
- `npm run type-check` exits 0
- `npm run lint -- src/components/sharing/ShareCallDialog.tsx` exits 0 (no new lint errors)
</acceptance_criteria>
</task>

<task id="32-03-T5">
<title>Verify production build still passes and all Phase 32 changes compile together</title>
<read_first>
- package.json (build script)
- src/components/sharing/ShareCallDialog.tsx
- src/components/share/PublicShareLanding.tsx (Plan 02 output)
- src/components/share/WrongAccountState.tsx (Plan 02 output)
- src/pages/SharedCallView.tsx (Plan 02 output)
- src/pages/Login.tsx (Plan 02 output)
- src/hooks/useSharing.ts (Plan 02 output)
- supabase/functions/share-call/index.ts (Plan 01 output)
</read_first>
<action>
Run the full verification sweep:

```bash
cd /Users/Naegele/dev/brain
npm run type-check 2>&1 | tail -20
npm run lint 2>&1 | tail -20
npm test 2>&1 | tail -30
npm run build 2>&1 | tail -10
```

Capture each command's output. Verify all exit 0 (or that lint and test report no NEW failures introduced by Phase 32 — pre-existing failures from Phase 30 BUG-01 fixtures are acceptable, as noted in Phase 31 VERIFICATION).

Document the exact output in the VERIFY doc.

If `npm run build` fails, return to the failing file and fix. If `npm test` reports failures, isolate whether they're new (Phase 32) or pre-existing (Phase 30 BUG-01 fixture). New failures are blockers; pre-existing are noted.
</action>
<acceptance_criteria>
- `npm run type-check` exits 0
- `npm run lint` exits 0 OR reports zero NEW errors compared to pre-Phase-32 baseline
- `npm test` exits 0 OR reports zero NEW failures compared to pre-Phase-32 baseline (Phase 30 BUG-01 fixture failure is acceptable per Phase 31 VERIFICATION)
- `npm run build` exits 0
- The VERIFY doc captures the exact output of each command
</acceptance_criteria>
</task>

<task id="32-03-T6">
<title>End-to-end UAT instructions for SHARE-04 (recorded for human-needed verification)</title>
<read_first>
- .planning/phases/32-shared-call-public-landing-page/32-CONTEXT.md (SHARE-04 6-step matrix)
</read_first>
<action>
Because dev-browser MCP is not available in the executor session, this task captures the UAT script that the operator runs after Vercel auto-deploys main. Write the script into the plan's VERIFY doc OR into `.planning/phases/32-shared-call-public-landing-page/32-UAT.md`.

Steps (per SHARE-04, CONTEXT.md):

1. **Persona A (sender)** signs in to `https://app.callvaultai.com` as the operator account. Open a call. Click "Share Call". Enter `qa-share-{timestamp}@vibeos.com` (any unused email). Click `Create link`. Copy the URL.

2. **Public view (incognito):** Open the share URL in an incognito window. EXPECT: Loom-style landing card with `{Inviter Name} shared a call with you` heading, call title sub-line, `Sign up to view` primary CTA (with vibe-orange arrow), `Open in existing account` secondary CTA, `Free tier — no credit card needed` helper text.

3. **Sign-up path:** Click `Sign up to view`. EXPECT: redirected to `/login?signup=true&share={token}`, email pre-filled with `qa-share-{timestamp}@vibeos.com`, locked (disabled with `RiLockLine` icon), helper text `Locked to the recipient of this share link.`, sub-heading `You're on the Free plan. Set a password to view the call.` Enter a password, click `Create account`. EXPECT (immediate session): `Account created — welcome to CallVault!` toast → land on `/s/{token}` → see full call view. OR EXPECT (email-confirm-pending): full-screen `Check your email` confirmation screen.

4. **Existing-account path:** In a second tab, sign in as `qa-share-{timestamp}@vibeos.com`. Open the share URL. EXPECT: full call view directly.

5. **Wrong-account path:** Sign in as a different account (e.g., the sender's primary email). Open the share URL. EXPECT: wrong-account card with masked email (`qa***@vibeos.com`), `Sign out` primary, `Cancel` secondary, auto-focus on Sign out. Click `Sign out`. EXPECT: lands on `/s/{token}` public landing.

6. **Sender views own link:** As the sender, open the share URL. EXPECT: full call view directly (sender bypass — RESEARCH section 1 decision).

7. **Modal cleanup verification:** Open the Share Call modal on any call. EXPECT:
   - `Add recipient` section label
   - `Create link` button (sentence-case)
   - `Close` button
   - No green-500 highlights on newly-created rows (subtle ring instead)
   - No `RiLinkM` rendering glitch
   - Modal fits within viewport even with 10+ active links (try creating 5+ to test)

For each step, the operator captures a screenshot via dev-browser and pastes it (or its description) into the VERIFY doc.

Save the UAT script in a new file: `.planning/phases/32-shared-call-public-landing-page/32-UAT.md`.
</action>
<acceptance_criteria>
- File `.planning/phases/32-shared-call-public-landing-page/32-UAT.md` exists
- File contains 7 numbered UAT steps matching SHARE-04
- File references dev-browser as the verification tool
</acceptance_criteria>
</task>

</tasks>

<verification>
- All 6 tasks have acceptance criteria passing
- `npm run type-check` exits 0 across the full Phase 32 surface
- `npm run build` exits 0
- `npm test` reports zero new failures from Phase 32
- ShareCallDialog has zero references to `RiLinkM`, `border-green-500`, `bg-green-500`, `text-green-600`, `max-h-48`, `CREATE`, `CLOSE`, or the old copy strings
- 32-UAT.md exists with the 7-step end-to-end script
</verification>

<must_haves_verification>
1. **Icon swap:** `grep -c "RiLinkM" src/components/sharing/ShareCallDialog.tsx` returns 0; `grep -c "RiLinksLine"` returns >= 3.
2. **Token swap:** `grep -E "border-green|bg-green|text-green-600" src/components/sharing/ShareCallDialog.tsx` returns no matches.
3. **Overflow fix:** `grep -c "max-h-\[calc(100vh-8rem)\]"` returns 1; `grep -c "flex-1 min-h-0"` returns >= 2; `grep -c "max-h-48"` returns 0.
4. **Copy:** `grep -c "Create link\b"` returns >= 1; `grep -c "Close\b"` returns >= 1 (in the close button); `grep -c "CREATE\|CLOSE"` returns 0.
5. **Build passes:** the VERIFY doc shows `npm run build` exit code 0 with bundle size noted.
6. **UAT script:** `.planning/phases/32-shared-call-public-landing-page/32-UAT.md` exists with the 6+modal-cleanup steps.
</must_haves_verification>
