---
phase: 27-close-v2-1-audit-gaps
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/types/supabase.ts
  - src/services/mcp-tokens.service.ts
  - src/services/mcp-token-capabilities.service.ts
  - .planning/REQUIREMENTS.md
  - .planning/phases/20-read-crud-tools/20-VERIFICATION.md
  - .planning/phases/21-write-crud-tools/21-VERIFICATION.md
  - .planning/phases/22-ai-tools/22-VERIFICATION.md
  - .planning/phases/23-management-ui/23-VERIFICATION.md
  - .planning/phases/24-fathom-share-link-save/24-VERIFICATION.md
  - .planning/phases/27-close-v2-1-audit-gaps/27-VERIFICATION.md
autonomous: true
requirements:
  - MGMT-02
  - WS-01
  - WS-02
  - WS-03
  - WS-04
  - WS-05
must_haves:
  truths:
    - "src/types/supabase.ts includes mcp_tokens.enabled_categories, recordings.action_items_cache, recordings.coaching_cache, and call_notes table"
    - "`as McpToken` casts removed from mcp-tokens.service.ts and mcp-token-capabilities.service.ts"
    - "`tsc --noEmit` clean for the two service files this plan touches"
    - "REQUIREMENTS.md traceability table contains 5 new rows for WS-01..05 (Phase 25)"
    - "Phases 20, 21, 22, 23, 24 each have a VERIFICATION.md file with valid frontmatter"
    - "27-VERIFICATION.md exists, references 27-WAVE1-VERIFY.md evidence, lists all 10 ROADMAP success criteria with status"
  artifacts:
    - path: "src/types/supabase.ts"
      provides: "Canonical generated types matching production schema"
      contains: "enabled_categories"
    - path: "src/services/mcp-tokens.service.ts"
      provides: "regenerateMcpToken without `as McpToken` cast"
      not_contains: "as McpToken"
    - path: ".planning/REQUIREMENTS.md"
      provides: "WS-01..05 in traceability table"
      contains: "WS-05"
    - path: ".planning/phases/20-read-crud-tools/20-VERIFICATION.md"
      provides: "Backfilled verification for Phase 20"
      contains: "status: backfilled-from-evidence"
    - path: ".planning/phases/27-close-v2-1-audit-gaps/27-VERIFICATION.md"
      provides: "Phase 27 closure verification"
      contains: "27-WAVE1-VERIFY.md"
  key_links:
    - from: "regenerateMcpToken service function"
      to: "Database['public']['Functions']['regenerate_mcp_token'] generated type"
      via: "supabase.rpc<...>('regenerate_mcp_token', ...)"
      pattern: "regenerate_mcp_token"
    - from: "27-VERIFICATION.md requirements coverage table"
      to: "ROADMAP.md Phase 27 success criteria 1-9"
      via: "explicit numbering match"
      pattern: "criterion.*1.*PROV-02"
---

<objective>
Wave 2 closes the remaining 3 D-* decisions: D-03 (regenerate types + remove casts), D-06 (backfill 5 VERIFICATION.md files), D-07 (add WS-01..05 to REQUIREMENTS.md). Then produces the final 27-VERIFICATION.md that consolidates evidence from 27-WAVE1-VERIFY.md (from Wave 1) plus this wave's work.

Purpose: Phase 27 cannot close until types reflect production schema (D-04 migration must land first → that's why this is Wave 2) and audit-flagged orphans are documented. The traceability table + 5 backfilled VERIFICATION docs are what flip the v2.1 milestone from `gaps_found` to `passed`.

Output: Updated supabase.ts with no casts, REQUIREMENTS.md with 5 new rows, 5 backfilled VERIFICATION.md files (phases 20-24), and 27-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v2.1-MILESTONE-AUDIT.md
@.planning/phases/27-close-v2-1-audit-gaps/27-CONTEXT.md
@.planning/phases/27-close-v2-1-audit-gaps/27-01-SUMMARY.md
@.planning/phases/27-close-v2-1-audit-gaps/27-WAVE1-VERIFY.md
@.planning/phases/19-provisioning-foundation/19-VERIFICATION.md
@.planning/phases/25-workspace-type-retirement/25-VERIFICATION.md
@.planning/phases/20-read-crud-tools/20-SUMMARY.md
@.planning/phases/21-write-crud-tools/21-01-SUMMARY.md
@.planning/phases/21-write-crud-tools/21-UAT.md
@.planning/phases/22-ai-tools/22-UAT.md
@.planning/phases/23-management-ui/23-02-SUMMARY.md
@.planning/phases/24-fathom-share-link-save/24-01-SUMMARY.md

<interfaces>
<!-- Service signatures the type-regen will need to compile cleanly. -->

src/services/mcp-tokens.service.ts:130-148 (current — cast-on-return):
```typescript
export async function regenerateMcpToken(id: string): Promise<McpToken> {
  const { data, error } = await supabase
    .rpc('regenerate_mcp_token', { p_token_id: id })
  if (error) throw new Error(`Failed to regenerate MCP token: ${error.message}`)
  const row = Array.isArray(data) ? data[0] : data
  if (!row) throw new Error('Token not found or you do not have permission to regenerate it')
  return row as McpToken  // ← cast to remove
}
```

src/services/mcp-token-capabilities.service.ts:35-51 (current — cast-on-return):
```typescript
export async function setEnabledCategories(
  tokenId: string,
  value: EnabledCategoriesValue,
): Promise<McpToken> {
  const { data, error } = await supabase
    .from('mcp_tokens')
    .update({ enabled_categories: value })
    .eq('id', tokenId)
    .select('id, user_id, org_id, workspace_id, name, token, scope, last_used_at, created_at, enabled_categories')
    .single()
  if (error) throw new Error(`Failed to update token capabilities: ${error.message}`)
  return data as McpToken  // ← cast to remove
}
```

After type-regen, both `data as McpToken` casts should compile without the `as McpToken` because the generated types will contain `enabled_categories` on the row shape returned by `.rpc('regenerate_mcp_token', ...)` (now JSONB after Wave 1 D-04 migration) and on the `mcp_tokens` table row (already added in migration `20260507130000_add_mcp_token_enabled_categories.sql`).

Existing VERIFICATION.md frontmatter pattern (from 19-VERIFICATION.md and 25-VERIFICATION.md):
```yaml
---
phase: NN-slug
status: verified | human_needed | backfilled-from-evidence
verified_at: 2026-MM-DDTHH:MM:00Z
score: "X/Y must-haves verified — explanation"
human_verification:    # optional list, mostly for live-event-required items
  - test: "..."
    expected: "..."
    why_human: "..."   # if result: human_needed
    result: pass | human_needed | fail
    evidence: "..."    # only if result: pass
source_evidence:        # ONLY for backfilled VERIFICATION.md
  - "NN-SUMMARY.md"
  - "NN-UAT.md (if exists)"
---

# Phase NN ... Verification

## Goal
[verbatim from ROADMAP]

## Success Criteria Status
| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
...

## Requirements Coverage
| Req | Status | Evidence |
...
```

REQUIREMENTS.md traceability table (current location: lines 106-129, ends with PASTE-04):
```markdown
| PASTE-04 | Phase 24 | ✅ Shipped (verified prod 2026-05-07) |
```
The new rows go AFTER PASTE-04 with a section heading per D-07.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate src/types/supabase.ts + remove `as McpToken` casts (D-03)</name>
  <files>
    src/types/supabase.ts
    src/services/mcp-tokens.service.ts
    src/services/mcp-token-capabilities.service.ts
  </files>
  <action>
    Per D-03:

    **Step A — Regenerate types:**
    ```bash
    supabase gen types typescript --linked > src/types/supabase.ts
    ```
    The Supabase CLI sometimes appends a banner ("Connecting to project... Generated types..." etc.) AFTER the closing `} as const` — Phase 25 Plan 02 hit this and it breaks the TS parser. After regen:
    1. Open the file, scroll to the end.
    2. Confirm the last meaningful line is `} as const`.
    3. If any lines appear after `} as const`, delete them.
    4. Confirm the file still compiles: `npx tsc --noEmit src/types/supabase.ts` — should produce 0 errors in this file (other files may have unrelated errors; that's expected per Phase 25 precedent).

    **Step B — Verify expected new schema members are present:**
    ```bash
    grep -c 'enabled_categories' src/types/supabase.ts          # ≥1 (mcp_tokens row)
    grep -c 'action_items_cache' src/types/supabase.ts          # ≥1 (recordings row)
    grep -c 'coaching_cache' src/types/supabase.ts              # ≥1 (recordings row)
    grep -c 'call_notes' src/types/supabase.ts                  # ≥1 (call_notes table)
    grep -c 'regenerate_mcp_token' src/types/supabase.ts        # ≥1 (Functions section, with new return shape including enabled_categories)
    ```
    All five greps must return ≥1. If any returns 0, the regen pulled stale schema or the migration from Wave 1 didn't land — investigate before proceeding.

    **Step C — Remove `as McpToken` casts:**

    In `src/services/mcp-tokens.service.ts:147`, change:
    ```typescript
    return row as McpToken
    ```
    to:
    ```typescript
    return row
    ```
    The function signature is `Promise<McpToken>` so TypeScript will check assignability automatically. If TS now complains, the issue is real and indicates a row-shape mismatch worth fixing — investigate the error rather than re-adding the cast.

    In `src/services/mcp-token-capabilities.service.ts:50`, change:
    ```typescript
    return data as McpToken
    ```
    to:
    ```typescript
    return data
    ```
    Same reasoning.

    **Step D — Typecheck:**
    ```bash
    npx tsc --noEmit
    ```
    Expect: 0 errors in `src/services/mcp-tokens.service.ts` and `src/services/mcp-token-capabilities.service.ts`. Other pre-existing errors are OK and explicitly out-of-scope per CONTEXT.md `<deferred>` ("84 pre-existing TS errors revealed by Phase 25 supabase.ts cleanup — not caused by Phase 27, defer to v2.2 cleanup").

    Capture the per-file error count via:
    ```bash
    npx tsc --noEmit 2>&1 | grep -c "src/services/mcp-tokens.service.ts"
    npx tsc --noEmit 2>&1 | grep -c "src/services/mcp-token-capabilities.service.ts"
    ```
    Both must be 0.

    Commit message: `refactor(27-02): regen supabase.ts + remove as McpToken casts (closes audit BLOCKER #2)`
  </action>
  <verify>
    <automated>grep -c 'enabled_categories' src/types/supabase.ts | awk '{ exit ($1 >= 1 ? 0 : 1) }' && grep -c 'action_items_cache' src/types/supabase.ts | awk '{ exit ($1 >= 1 ? 0 : 1) }' && grep -c 'coaching_cache' src/types/supabase.ts | awk '{ exit ($1 >= 1 ? 0 : 1) }' && grep -c 'call_notes' src/types/supabase.ts | awk '{ exit ($1 >= 1 ? 0 : 1) }' && ! grep -q 'as McpToken' src/services/mcp-tokens.service.ts && ! grep -q 'as McpToken' src/services/mcp-token-capabilities.service.ts && (npx tsc --noEmit 2>&1 | grep -c 'src/services/mcp-tokens.service.ts\|src/services/mcp-token-capabilities.service.ts' | awk '{ exit ($1 == 0 ? 0 : 1) }')</automated>
    Expect: 4 schema additions present, both casts removed, both service files clean per `tsc --noEmit`.
  </verify>
  <done>
    Types regenerated, casts removed, both service files compile clean, commit pushed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add WS-01..05 to REQUIREMENTS.md traceability table (D-07)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Per D-07. Edit `.planning/REQUIREMENTS.md` traceability table. The current table ends at line 129 with `| PASTE-04 | Phase 24 | ✅ Shipped (verified prod 2026-05-07) |`.

    **Step A — Add a section header AFTER the existing table** (before the "Coverage" sub-section at line 131). Insert this block:

    ```markdown
    ### Workspace Type Retirement (Phase 25)

    | Requirement | Phase | Status |
    |-------------|-------|--------|
    | WS-01 | Phase 25 | ✅ Shipped (verified 25-VERIFICATION.md, 7/7 PASS) |
    | WS-02 | Phase 25 | ✅ Shipped (verified 25-VERIFICATION.md, 7/7 PASS) |
    | WS-03 | Phase 25 | ✅ Shipped (verified 25-VERIFICATION.md, 7/7 PASS — regression fixed 87494b23) |
    | WS-04 | Phase 25 | ✅ Shipped (verified 25-VERIFICATION.md, 7/7 PASS — trigger updated by Phase 27 D-05) |
    | WS-05 | Phase 25 | ✅ Shipped (verified 25-VERIFICATION.md, 7/7 PASS) |

    **WS-01..05 wording (verbatim from ROADMAP.md Phase 25 success criteria, collapsed per CONTEXT D-07 + `<specifics>`):**
    - **WS-01:** No workspace_type selector in CreateWorkspaceDialog
    - **WS-02:** No auto-creation of "Hall of Fame" / "Manager Reviews" folders
    - **WS-03:** 2nd-pane workspace list reorderable per-user via DnD, persists across reloads + devices
    - **WS-04:** Each org has exactly one is_default=TRUE workspace; cannot be deleted via UI or API
    - **WS-05:** Lock vs team icon derived from member_count, no behavioral branches on workspace_type
    ```

    **Step B — Update the Coverage section** at lines 131-141. Adjust counts:
    - "v2.1 requirements: 21 active" → "26 active (21 + WS-01..05 added 2026-05-07 by Phase 27 D-07)"
    - Coverage line: `Mapped to phases: 21` → `Mapped to phases: 26`
    - Ship status totals must reflect 5 newly-recognized shipped requirements (WS-01..05). Update the "Shipped + verified" count from 15 to 20.

    Add a footnote at the bottom of the Coverage section:
    ```markdown
    **2026-05-07 update (Phase 27 D-07):** WS-01..05 added retroactively after v2.1 milestone audit flagged them as orphaned from the traceability table despite being verified in 25-VERIFICATION.md. Total active reqs: 26.
    ```

    Commit message: `docs(27-02): add WS-01..05 to REQUIREMENTS.md traceability (closes audit ORPHAN finding)`
  </action>
  <verify>
    <automated>grep -c 'WS-0[1-5]' .planning/REQUIREMENTS.md | awk '{ exit ($1 >= 5 ? 0 : 1) }' && grep -c 'Workspace Type Retirement (Phase 25)' .planning/REQUIREMENTS.md | awk '{ exit ($1 == 1 ? 0 : 1) }'</automated>
    Expect: 5+ references to WS-0[1-5] (the 5 table rows + the wording bullets), 1 section header.
  </verify>
  <done>
    REQUIREMENTS.md updated with new section + 5 rows + Coverage counts adjusted, commit pushed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Backfill VERIFICATION.md for Phase 20 (D-06)</name>
  <files>.planning/phases/20-read-crud-tools/20-VERIFICATION.md</files>
  <action>
    Per D-06. Promote embedded evidence from `.planning/phases/20-read-crud-tools/20-SUMMARY.md` into structured frontmatter. Do NOT re-run verification — this is mechanical promotion.

    Read `.planning/phases/20-read-crud-tools/20-SUMMARY.md` first, then write `.planning/phases/20-read-crud-tools/20-VERIFICATION.md`:

    ```markdown
    ---
    phase: 20-read-crud-tools
    status: backfilled-from-evidence
    verified_at: 2026-05-07T<HH:MM>Z
    score: "Backfilled from 20-SUMMARY.md — code-level verified, runtime not formally verified end-to-end via MCP client (per SUMMARY's own caveat)"
    source_evidence:
      - "20-SUMMARY.md"
    requirements_covered:
      - TOOL-01
      - TOOL-02
      - TOOL-03
      - TOOL-04
    ---

    # Phase 20 Verification (Backfilled 2026-05-07)

    > Promoted from embedded evidence in 20-SUMMARY.md per Phase 27 D-06.
    > 20-SUMMARY explicitly notes "Code-level: ✅. Runtime / dev-browser: ⏳ Not formally verified end-to-end via MCP client." This backfill preserves that caveat — the requirements are marked partial-but-shipped, not fully-runtime-verified.

    ## Goal

    [Copy verbatim from ROADMAP.md Phase 20 — search/list/get_by_id/get_by_url across 17 read tools]

    ## Success Criteria Status

    | # | Criterion | Status | Evidence |
    |---|-----------|--------|----------|
    | 1 | TOOL-01 search_calls | code-verified | 20-SUMMARY: tool exists in mcp-server/index.ts with correct schema |
    | 2 | TOOL-02 list tools | code-verified | 20-SUMMARY: list_calls/list_workspaces/list_folders/list_tags |
    | 3 | TOOL-03 get_by_id tools | code-verified | 20-SUMMARY: get_call/get_workspace/get_folder/etc. |
    | 4 | TOOL-04 get_by_url | code-verified | 20-SUMMARY: get_call_by_url with Fathom URL parsing |

    ## Requirements Coverage

    | Req | Status | Evidence |
    |-----|--------|----------|
    | TOOL-01 | ⚠️ partial (code-verified) | 20-SUMMARY |
    | TOOL-02 | ⚠️ partial (code-verified) | 20-SUMMARY |
    | TOOL-03 | ⚠️ partial (code-verified) | 20-SUMMARY |
    | TOOL-04 | ⚠️ partial (code-verified) | 20-SUMMARY |

    ## Backfill Notes

    - This phase shipped without a GSD plan tracking — backfilled SUMMARY only.
    - All 4 requirements were verified at the code level; runtime end-to-end via real MCP client was not performed.
    - The v2.1 milestone audit accepts this as "verified-by-summary" per the closure path: production usage since ship + smoke-tests in Phase 22 (which exercises the same tool layer) provide implicit runtime evidence.
    ```

    Replace the `<HH:MM>` timestamp with current time in 24-hour UTC.

    Commit message: `docs(27-02): backfill 20-VERIFICATION.md from 20-SUMMARY evidence (D-06)`
  </action>
  <verify>
    <automated>test -f .planning/phases/20-read-crud-tools/20-VERIFICATION.md && grep -c 'status: backfilled-from-evidence' .planning/phases/20-read-crud-tools/20-VERIFICATION.md | awk '{ exit ($1 == 1 ? 0 : 1) }' && grep -c 'TOOL-0[1-4]' .planning/phases/20-read-crud-tools/20-VERIFICATION.md | awk '{ exit ($1 >= 8 ? 0 : 1) }'</automated>
    Expect: file exists, status frontmatter set, 8+ TOOL-0[1-4] references (4 in success criteria + 4 in requirements coverage).
  </verify>
  <done>File written with valid frontmatter; all 4 TOOL-0X requirements documented as code-verified-partial with the SUMMARY caveat preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Backfill VERIFICATION.md for Phases 21, 22, 23, 24 (D-06)</name>
  <files>
    .planning/phases/21-write-crud-tools/21-VERIFICATION.md
    .planning/phases/22-ai-tools/22-VERIFICATION.md
    .planning/phases/23-management-ui/23-VERIFICATION.md
    .planning/phases/24-fathom-share-link-save/24-VERIFICATION.md
  </files>
  <action>
    Per D-06. Same template as Task 3, applied to the 4 remaining phases. Read each phase's existing evidence first (the source files are already loaded via `<context>` `@` references), then write each VERIFICATION.md.

    **Phase 21** (`.planning/phases/21-write-crud-tools/21-VERIFICATION.md`):
    - source_evidence: `21-01-SUMMARY.md`, `21-UAT.md`, `21-SHIPPED-INVENTORY.md`
    - requirements_covered: TOOL-05, TOOL-06, TOOL-07
    - status: `verified` (TOOL-05 has 11/11 UAT PASS — call this verified, not backfilled-partial)
    - Note in body that TOOL-06 (tag_call) and TOOL-07 (add_call_to_folder) shipped pre-Phase-21 (commit ad73e50e per audit) and are backfilled to inventory-only — flag them as `partial (inventory-only, no UAT)` in the requirements coverage table.

    **Phase 22** (`.planning/phases/22-ai-tools/22-VERIFICATION.md`):
    - source_evidence: `22-01-SUMMARY.md`, `22-02-SUMMARY.md`, `22-03-SUMMARY.md`, `22-04-SUMMARY.md`, `22-UAT.md`
    - requirements_covered: AITL-02, AITL-03, AITL-04, AITL-05
    - status: `human_needed` (UAT-22 shows 6/8 PASS smoke + 2 manual pending — items 1 "live MCP client connect" and 7 "capability toggle round-trip"). Replicate the human_verification YAML pattern from 19-VERIFICATION.md for those 2 pending items.
    - All 4 AITL-XX have smoke-PASS evidence — mark them satisfied with the manual-pending caveat.

    **Phase 23** (`.planning/phases/23-management-ui/23-VERIFICATION.md`):
    - source_evidence: `23-01-SUMMARY.md`, `23-02-SUMMARY.md`
    - requirements_covered: MGMT-01, MGMT-02, MGMT-03
    - status: `verified` (audit shows live curl tests 1-7 in 23-02-SUMMARY + -32001 verified live)
    - Note in body: "Plan 23-02 frontend pending merge to main → Vercel auto-deploy. After merge, full UI deploy is verified-by-deploy-pipeline."

    **Phase 24** (`.planning/phases/24-fathom-share-link-save/24-VERIFICATION.md`):
    - source_evidence: `24-01-SUMMARY.md`
    - requirements_covered: PASTE-01, PASTE-02, PASTE-03, PASTE-04
    - status: `verified` (audit shows full prod e2e + 2 bug-fixes during verification)
    - Note in body: "All 4 PASTE-XX have embedded e2e prod evidence in 24-01-SUMMARY (dev-browser session + 2 post-fix verifications: 33b3b9da + ce3b9c9e)."

    Each file follows the exact same template as Task 3 (frontmatter + Goal + Success Criteria Status table + Requirements Coverage table + Backfill Notes). Replace the timestamp `<HH:MM>` with current time.

    Commit each file as its own atomic commit (per the user's `feedback_always_commit.md` rule):
    - `docs(27-02): backfill 21-VERIFICATION.md from UAT + SUMMARY evidence (D-06)`
    - `docs(27-02): backfill 22-VERIFICATION.md from UAT + 4 SUMMARYs (D-06)`
    - `docs(27-02): backfill 23-VERIFICATION.md from live curl evidence (D-06)`
    - `docs(27-02): backfill 24-VERIFICATION.md from prod e2e evidence (D-06)`
  </action>
  <verify>
    <automated>for P in 21-write-crud-tools 22-ai-tools 23-management-ui 24-fathom-share-link-save; do test -f ".planning/phases/$P/$(echo $P | cut -d- -f1)-VERIFICATION.md" || { echo "MISSING: $P"; exit 1; }; done && grep -l 'source_evidence:' .planning/phases/2{1,2,3,4}-*/2{1,2,3,4}-VERIFICATION.md | wc -l | awk '{ exit ($1 == 4 ? 0 : 1) }'</automated>
    Expect: 4 VERIFICATION.md files exist, all 4 have `source_evidence:` frontmatter.
  </verify>
  <done>
    4 backfilled VERIFICATION.md files written with status reflecting actual evidence quality (not all "verified" — Phase 22 keeps `human_needed` per UAT, Phase 21 splits TOOL-05 verified vs TOOL-06/07 partial). 4 atomic commits.
  </done>
</task>

<task type="auto">
  <name>Task 5: Write 27-VERIFICATION.md (final phase closure document)</name>
  <files>.planning/phases/27-close-v2-1-audit-gaps/27-VERIFICATION.md</files>
  <action>
    Produce the final `27-VERIFICATION.md` consolidating evidence from `27-WAVE1-VERIFY.md` (from 27-01-PLAN.md Task 8) plus this wave's outputs.

    Read `27-WAVE1-VERIFY.md` and `27-01-SUMMARY.md` first.

    File template (replicate the structure of 25-VERIFICATION.md):

    ```markdown
    ---
    phase: 27-close-v2-1-audit-gaps
    status: verified
    verified_at: 2026-05-07T<HH:MM>Z
    score: "10/10 success criteria — 9 closed by Phase 27, 1 deferred per D-08"
    source_evidence:
      - "27-WAVE1-VERIFY.md"
      - "27-01-SUMMARY.md"
      - "27-02-SUMMARY.md"
    decisions_closed:
      - D-01: PROV-02 plan-gating re-enabled
      - D-02: curl evidence captured for PROV-02
      - D-03: supabase.ts regenerated, casts removed
      - D-04: regenerate_mcp_token RPC includes enabled_categories
      - D-05: auto_create_default_workspace_entry uses is_default
      - D-06: VERIFICATION.md backfilled for phases 20, 21, 22, 23, 24
      - D-07: WS-01..05 added to REQUIREMENTS.md
      - D-11: mcp-oauth-register fail-closed (Critical Security #1)
      - D-12: zoom-webhook OAuth token in Authorization header (Critical Security #2)
      - D-13: share-call audit log de-poisoned (Critical Security #3)
    decisions_deferred:
      - D-08: Nyquist VALIDATION.md backfill (deferred to v2.2 backlog per CONTEXT.md)
    threats_addressed:
      - T-27-01: regenerate_mcp_token IDOR guard preserved
      - T-27-02: auto-workspace-entry search_path lock preserved
      - T-27-03: share-call JWT-derive does not block on getUser failure
      - T-27-04: zoom-webhook header pattern verified (or fallback documented)
      - T-27-05: mcp-oauth-register fail-closed
      - T-27-06: share-call audit log poisoning closed
      - T-27-07: PROV-02 free-tier callers get -32001
    ---

    # Phase 27 Verification

    ## Goal

    [Verbatim from ROADMAP.md Phase 27]

    ## Success Criteria Status (matches ROADMAP 1-10)

    | # | Criterion | Status | Evidence |
    |---|-----------|--------|----------|
    | 1 | PROV-02 re-enabled (mcpError -32001) | ✅ verified | 27-WAVE1-VERIFY.md Gate 1 — curl returned -32001 for free-tier user |
    | 2 | supabase.ts regenerated + casts removed | ✅ verified | 27-02 Task 1 — 4 schema additions present, both casts removed, tsc clean |
    | 3 | regenerate_mcp_token RPC enabled_categories | ✅ verified | 27-WAVE1-VERIFY.md Gate 2 — pg_get_functiondef shows column |
    | 4 | auto_create_default_workspace_entry uses is_default | ✅ verified | 27-WAVE1-VERIFY.md Gate 3 — pg_get_functiondef confirms is_default = TRUE |
    | 5 | VERIFICATION.md backfilled for phases 20, 21, 22, 23, 24 | ✅ verified | 5 files exist with frontmatter |
    | 6 | WS-01..05 in REQUIREMENTS.md | ✅ verified | 27-02 Task 2 — table updated, coverage 26/26 |
    | 7 | mcp-oauth-register fail-closed | ✅ verified | 27-WAVE1-VERIFY.md Gate 4 — 0 service-role refs |
    | 8 | zoom-webhook OAuth header (or no-log fallback) | ✅ verified | 27-WAVE1-VERIFY.md Gate 5 — primary or fallback path landed |
    | 9 | share-call audit log de-poisoned | ✅ verified | 27-WAVE1-VERIFY.md Gate 6 — forge attempt wrote 0 rows |
    | 10 | (Optional) Nyquist VALIDATION.md backfill | ⏳ deferred | D-08 — out of scope, defer to v2.2 |

    ## Requirements Coverage

    | Req | Phase | Status | Evidence |
    |-----|-------|--------|----------|
    | PROV-02 | Phase 19 | ✅ re-satisfied (Phase 27 D-01) | mcpError -32001 returned to free-tier callers |
    | MGMT-02 | Phase 23 | ✅ now type-safe | enabled_categories column generated in supabase.ts |
    | WS-01..05 | Phase 25 | ✅ now traceable | added to REQUIREMENTS.md table |

    ## Threat Model Closure

    [Map T-27-01..T-27-07 to evidence]

    ## Production Impact

    - Free-tier MCP callers now blocked (was: open access since Phase 20 hot-fix 8f0b9a17)
    - 3 production-exploitable security holes closed
    - v2.1 milestone audit gaps fully addressed
    - 6 atomic backend commits + 7 doc commits = 13 total commits in Phase 27

    ## v2.1 Milestone Status

    Per the v2.1-MILESTONE-AUDIT.md closure path:
    1. ✅ BLOCKER #1 (PROV-02) — fixed
    2. ✅ BLOCKER #2 (supabase.ts) — fixed
    3. ✅ WARNING #1 (regenerate_mcp_token) — fixed
    4. ✅ WARNING #2 (is_home/is_default drift) — fixed
    5. ✅ ORPHAN (WS-01..05) — added to REQUIREMENTS.md
    6. ✅ Verification gap (5 phases without VERIFICATION.md) — backfilled

    **Recommendation:** Update `.planning/v2.1-MILESTONE-AUDIT.md` status from `gaps_found` to `passed` (out of scope for this plan — handled by next milestone audit run).
    ```

    Commit message: `docs(27-02): final 27-VERIFICATION.md — closes 9 D-* decisions, defers 1`
  </action>
  <verify>
    <automated>test -f .planning/phases/27-close-v2-1-audit-gaps/27-VERIFICATION.md && grep -cE '^\| [0-9]+ \|' .planning/phases/27-close-v2-1-audit-gaps/27-VERIFICATION.md | awk '{ exit ($1 >= 10 ? 0 : 1) }' && grep -c 'D-1[123]' .planning/phases/27-close-v2-1-audit-gaps/27-VERIFICATION.md | awk '{ exit ($1 >= 3 ? 0 : 1) }'</automated>
    Expect: file exists, ≥10 numbered table rows (success criteria), ≥3 references to D-11/D-12/D-13 (the three Critical decisions).
  </verify>
  <done>
    27-VERIFICATION.md written, all 10 ROADMAP success criteria mapped, all 7 threats T-27-01..07 mapped to evidence, all 9 closed D-* decisions enumerated. Commit pushed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → supabase.ts generated types | Compile-time only; no runtime surface |
| Phase 27 doc artifacts → milestone audit | Doc-only; trust derives from author + git history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08 | T (Tampering) | supabase.ts banner pollution | mitigate | Task 1 Step A explicitly checks for and strips any trailing CLI banner after `} as const` (Phase 25 Plan 02 hit this — recorded in tech_debt). |
| T-27-09 | I (Info Disclosure) | VERIFICATION.md frontmatter overstating verification quality | accept | Phases 20-24 backfills explicitly use `status: backfilled-from-evidence` and preserve the original SUMMARY caveats (e.g., 20-SUMMARY's "Runtime not formally verified" note). Phase 22 retains `status: human_needed` to honor UAT items 1 and 7 that are still pending live triggers. No claim is upgraded beyond what the evidence supports. |
</threat_model>

<verification>
After Task 5, run the full Phase 27 closure check:
```bash
# All artifacts present:
ls -la .planning/phases/2{0,1,2,3,4}-*/2{0,1,2,3,4}-VERIFICATION.md  # 5 files
ls -la .planning/phases/27-close-v2-1-audit-gaps/27-VERIFICATION.md  # 1 file
grep -c 'WS-0[1-5]' .planning/REQUIREMENTS.md                         # ≥5
grep -c 'enabled_categories' src/types/supabase.ts                    # ≥1
! grep -q 'as McpToken' src/services/mcp-tokens.service.ts            # zero matches
! grep -q 'as McpToken' src/services/mcp-token-capabilities.service.ts # zero matches
```

If any check fails, the corresponding D-* decision is NOT closed.
</verification>

<success_criteria>
1. supabase.ts has 4 new schema members (enabled_categories on mcp_tokens, action_items_cache + coaching_cache on recordings, call_notes table).
2. Both `as McpToken` casts removed; `tsc --noEmit` clean for the two service files.
3. REQUIREMENTS.md has 5 new WS-0X rows + updated Coverage section.
4. 5 backfilled VERIFICATION.md files exist (phases 20, 21, 22, 23, 24) with `status: backfilled-from-evidence` (or `verified` for 23, 24, 21; `human_needed` for 22).
5. 27-VERIFICATION.md exists with all 10 ROADMAP success criteria mapped, 9 closed + 1 deferred (D-08).
6. v2.1 milestone status can be flipped from `gaps_found` to `passed` after next milestone audit run (recommended in the doc, not enforced by this plan).
</success_criteria>

<output>
After completion, create `.planning/phases/27-close-v2-1-audit-gaps/27-02-SUMMARY.md` covering:
- 7 commits expected (1 type regen + 1 REQUIREMENTS update + 5 VERIFICATION backfills + 1 final 27-VERIFICATION = 8 actually; one of the backfills can be batched if commits feel excessive but per the always-commit rule, 1 per file is fine)
- Wait — exact commit count: Task 1 (1) + Task 2 (1) + Task 3 (1) + Task 4 (4 atomic) + Task 5 (1) = 8 commits
- Phase 27 closure status (every D-* with link to evidence)
- v2.1 milestone status recommendation (passed)
</output>
