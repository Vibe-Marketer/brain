---
phase: 19-throughput-scaleup-trust-survival-autonomy
plan: 05
type: execute
wave: 4
depends_on: [19-01, 19-04]
files_modified:
  - repo: autopilot
    paths:
      - /Users/admin/dev/autopilot/autopilot.config.ts
      - /Users/admin/dev/autopilot/src/lib/canary.ts
      - /Users/admin/dev/autopilot/src/lib/canary.test.ts
      - /Users/admin/dev/autopilot/src/lib/tier2.ts
      - /Users/admin/dev/autopilot/src/lib/tier2.test.ts
      - /Users/admin/dev/autopilot/src/claimer.ts
      - /Users/admin/dev/autopilot/autopilot.config.ts
      - /Users/admin/dev/autopilot/launchd/com.callvault.autopilot-canary.plist
      - /Users/admin/dev/autopilot/launchd/com.callvault.autopilot-tier2.plist
autonomous: true
requirements: [TRU-02, TRU-03, ACT-02]
must_haves:
  truths:
    - "Canary re-test reopens the originating ticket with run attribution; it never creates an unlinked ticket per D-04."
    - "Default canary window is 24h post-merge and a pre-30-day survival check per D-04."
    - "Tier-2 is governed by the same autonomy ladder and only surfaces solution-shaped digests per D-06."
    - "Tier-2 runs a Claude-family command/model on a cadence distinct from the tier-1 Codex autopilot window per D-06."
    - "Canary and tier-2 jobs follow one-cycle lockdir/cadence patterns and do not raise concurrency."
  artifacts:
    - path: "/Users/admin/dev/autopilot/src/lib/canary.ts"
      provides: "Canary due-run selection and originating-ticket reopen"
      contains: "reopened_event_id"
    - path: "/Users/admin/dev/autopilot/src/lib/tier2.ts"
      provides: "Tier-2 routing/digest guard"
      contains: "solution"
  key_links:
    - from: "runner_runs.canary_next_run_at"
      to: "tickets.status"
      via: "same-ticket reopen with ticket_events attribution"
      pattern: "canary_regression_reopened"
    - from: "tier2 queue"
      to: "trust ladder"
      via: "same auto/manual decision helper"
      pattern: "manual|eligible|auto"
---

<objective>
Add the post-merge survival checks: canary regression reopen and tier-2 solution-shaped escalation.

Purpose: Higher throughput is only livable if regressions are caught against the originating fix and unresolved residue reaches Andrew as proposed solutions, not raw problems.
Output: Canary helper/job, tier-2 helper/job, launchd-style scheduling, and tests for attribution and operator-digest constraints.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-CONTEXT.md
@.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-RESEARCH.md
@.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-PATTERNS.md
@.planning/design/escalation-tier2-solutions-not-problems.md
@docs/architecture/autopilot-brain-ownership.md
@/Users/admin/dev/autopilot/src/lib/evidence.ts
@/Users/admin/dev/autopilot/src/lib/approval.ts
@/Users/admin/dev/autopilot/src/watchdog.ts
@/Users/admin/dev/autopilot/launchd/com.callvault.autopilot.plist
</context>

## Artifacts this phase produces

- Canary due-run selector and safe repro replay/test runner.
- Same-ticket reopen logic with `ticket_events`, `ticket_messages`, `runner_runs.reopened_event_id`, and trust event writes.
- Tier-2 queue/digest helper governed by ladder decisions and configured for a Claude-family agent distinct from the tier-1 Codex agent.
- One-cycle launchd plist entries for canary and tier-2 jobs.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canary due-run selection and safe replay</name>
  <files>/Users/admin/dev/autopilot/src/lib/canary.ts, /Users/admin/dev/autopilot/src/lib/canary.test.ts, /Users/admin/dev/autopilot/autopilot.config.ts</files>
  <read_first>
    - /Users/admin/dev/autopilot/src/lib/evidence.ts
    - /Users/admin/dev/autopilot/src/lib/approval.ts
    - /Users/admin/dev/autopilot/autopilot.config.ts
    - .planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-PATTERNS.md
  </read_first>
  <behavior>
    - Selects only merged runs with fix identity where `canary_status = 'pending'` and `canary_next_run_at <= now()`.
    - Uses safe argv repro replay when available and never shells arbitrary ticket text.
    - Schedules default 24h post-merge canary and pre-30-day survival check.
  </behavior>
  <action>Create `src/lib/canary.ts` with pure helpers for due-run selection, canary schedule calculation, and replay command resolution using the existing `findReproReplay()` safe argv contract. The due-run picker must select only rows where `canary_status = 'pending'` and `canary_next_run_at <= now()`, plus merged-run and fix-identity guards. Add config keys for canary cadence/window if needed, defaulting to 24h post-merge and a pre-30-day survival mark per D-04. Tests must prove unmerged runs, missing `fix_sha`, non-`pending` canary statuses, `deferred:rate-limit`, unsafe replay commands, and not-yet-due rows are skipped. Do not add a new queue engine or long-running worker.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && bun test src/lib/canary.test.ts src/lib/evidence.test.ts</automated>
    <automated>cd /Users/admin/dev/autopilot && grep -R "canary_next_run_at\\|findReproReplay\\|24" src/lib/canary.ts src/lib/canary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Canary candidates are only real merged fixes with due canary windows.
    - Unsafe or absent replay context does not execute arbitrary shell text.
    - Rate-limit defers and unmerged runs never enter canary survival checks.
  </acceptance_criteria>
  <done>Autopilot can identify and safely re-test due canary fixes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reopen originating ticket on canary regression with attribution</name>
  <files>/Users/admin/dev/autopilot/src/lib/canary.ts, /Users/admin/dev/autopilot/src/lib/canary.test.ts</files>
  <read_first>
    - /Users/admin/dev/autopilot/src/lib/canary.ts
    - /Users/admin/dev/autopilot/src/watchdog.ts
    - supabase/migrations/20260611000002_create_ticket_tables.sql
    - supabase/migrations/20260613200000_phase19_autopilot_trust.sql
    - .planning/design/escalation-tier2-solutions-not-problems.md
  </read_first>
  <behavior>
    - Canary failure updates the same originating ticket, never inserts a new ticket.
    - Failure writes `ticket_events`, `ticket_messages`, `runner_runs.reopened_event_id`, `survival_status = reopened`, `canary_status = failed`, and a trust event.
    - Canary success marks canary passed without reopening.
  </behavior>
  <action>Implement `handleCanaryResult()` or equivalent. On pass, update `runner_runs.canary_status = 'passed'`, `canary_last_run_at`, and future survival status when appropriate. On fail, update the originating `tickets.id` from the run back to `new` or the existing triage-ready status, insert a `ticket_events` row such as `canary_regression_reopened`, write a concise solution-shaped `ticket_messages` entry with run/fix attribution, set `runner_runs.reopened_event_id`, `survival_status = 'reopened'`, `reopened_at`, `canary_status = 'failed'`, and insert `autopilot_trust_events`. Do not call Sentry/QA ingestion and do not create a new unlinked ticket. Add tests that assert tables/method calls target the original ticket id and no `tickets.insert` occurs.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && bun test src/lib/canary.test.ts</automated>
    <automated>cd /Users/admin/dev/autopilot && grep -R "canary_regression_reopened\\|reopened_event_id\\|survival_status.*reopened" src/lib/canary.ts src/lib/canary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Regression attribution points to the originating `runner_runs.id` and `tickets.id`.
    - No new ticket is inserted for canary regression.
    - Survival metrics for the fix/category reflect reopened failure.
  </acceptance_criteria>
  <done>Canary failures reopen the originating ticket with durable fix attribution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add tier-2 ladder-governed solution digest and one-cycle schedules</name>
  <files>/Users/admin/dev/autopilot/src/lib/tier2.ts, /Users/admin/dev/autopilot/src/lib/tier2.test.ts, /Users/admin/dev/autopilot/src/claimer.ts, /Users/admin/dev/autopilot/autopilot.config.ts, /Users/admin/dev/autopilot/launchd/com.callvault.autopilot-canary.plist, /Users/admin/dev/autopilot/launchd/com.callvault.autopilot-tier2.plist</files>
  <read_first>
    - .planning/design/escalation-tier2-solutions-not-problems.md
    - /Users/admin/dev/autopilot/src/lib/trust.ts
    - /Users/admin/dev/autopilot/src/runner.ts
    - /Users/admin/dev/autopilot/src/watchdog.ts
    - /Users/admin/dev/autopilot/autopilot.config.ts
    - /Users/admin/dev/autopilot/launchd/com.callvault.autopilot.plist
  </read_first>
  <behavior>
    - Tier-1 residue routes to tier-2 before operator handoff.
    - Tier-1 remains Codex; Tier-2 uses a configured Claude-family command/model such as existing headless `claude` or Hermes on the host, with no new packages or frameworks.
    - Tier-2 launchd schedule/cadence is distinct from the tier-1 run window and one-cycle schedule.
    - Tier-2 may auto-fix only when the ladder permits; otherwise it emits a digest with 1-2 sentence what/why and 2-3 decisions with a recommendation.
    - Digest blocks raw stack traces, raw problem dumps, and positive "AI-powered" language.
    - Canary and tier-2 jobs run one cycle with lockdir-style concurrency 1.
  </behavior>
  <action>Create `src/lib/tier2.ts` with a pure digest validator and routing helper. Add config in `autopilot.config.ts` for the Tier-2 agent command/model, for example `tier2.agentCommand`, `tier2.modelFamily`, and `tier2.model`, pointing at an already-available Claude-family headless command such as `claude` or an existing Hermes invocation on the host. Tier-1 remains the Codex autopilot fix agent; Tier-2 config must be a different model family and command path from Tier-1. The helper should inspect trust ladder decisions from `trust.ts`: `auto` may enqueue/attempt tier-2 autofix, while `manual` and `eligible` must surface only a solution-shaped operator digest. Add tests proving raw stack trace/problem strings are rejected or summarized, only 2-3 decisions are emitted, a recommended option is present, ladder state governs auto-fix vs digest, and Tier-2's configured command/model family differs from Tier-1 Codex. Wire tier-1 escalation points in `claimer.ts` or the smallest appropriate daemon entry so unresolved residue queues to tier-2 instead of direct raw operator handoff. Add launchd plist files following existing one-cycle plist style for canary and tier-2 schedules; the tier-2 plist must use a distinct `StartInterval`/calendar cadence from `com.callvault.autopilot.plist` and avoid overlapping the tier-1 run window. Keep locks/concurrency at 1 and do not add a new daemon framework or package.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && bun test src/lib/tier2.test.ts src/lib/trust.test.ts src/lib/canary.test.ts</automated>
    <automated>cd /Users/admin/dev/autopilot && bun run typecheck</automated>
    <automated>cd /Users/admin/dev/autopilot && grep -R "ProgramArguments\\|StartInterval\\|StartCalendarInterval" launchd/com.callvault.autopilot-canary.plist launchd/com.callvault.autopilot-tier2.plist</automated>
    <automated>cd /Users/admin/dev/autopilot && grep -R "agentCommand\\|modelFamily\\|[Cc]laude\\|Hermes" autopilot.config.ts src/lib/tier2.ts src/lib/tier2.test.ts launchd/com.callvault.autopilot-tier2.plist</automated>
    <automated>cd /Users/admin/dev/autopilot && test -f src/lib/tier2.ts && ! grep -RnE "stack trace|raw problem|AI-powered" src/lib/tier2.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tier-2 respects the same autonomy ladder as tier-1 approval.
    - Tier-2's configured command/model family is verified to differ from Tier-1 Codex and uses an existing Claude-family host command without new packages.
    - Tier-2 launchd cadence is distinct from the tier-1 run window while preserving one-cycle concurrency 1.
    - Operator digest is solution-shaped and contains bounded decisions plus recommendation.
    - New schedules are one-cycle jobs and preserve concurrency 1.
  </acceptance_criteria>
  <done>Canary and tier-2 trust mechanisms run on safe schedules and surface only actionable solutions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB repro metadata -> local command execution | Canary replay metadata can trigger local test commands |
| canary job -> production ticket state | Failed canaries reopen tickets and affect survival metrics |
| tier-2 model/output -> operator surface | Second-opinion output can reach Andrew as a digest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-18 | Tampering | canary replay | mitigate | Reuse safe argv allowlist; reject shell metacharacters and unsafe commands. |
| T-19-19 | Repudiation | regression reopen | mitigate | Write `ticket_events`, `runner_runs.reopened_event_id`, and trust event for every canary failure. |
| T-19-20 | Information Disclosure | tier-2 digest | mitigate | Validator rejects raw stack traces/problem dumps; digest uses bounded solution format. |
| T-19-21 | Elevation of Privilege | tier-2 auto-fix | mitigate | Govern tier-2 auto-fix by same trust ladder; manual/eligible categories cannot auto-fix. |
| T-19-22 | Denial of Service | canary/tier-2 schedules | mitigate | One-cycle launchd jobs, lockdir pattern, no new queue engine, concurrency remains 1. |
| T-19-SC | Tampering | package installs | accept | No new packages. |
</threat_model>

<verification>
- `cd /Users/admin/dev/autopilot && bun test src/lib/canary.test.ts src/lib/tier2.test.ts src/lib/trust.test.ts`
- `cd /Users/admin/dev/autopilot && bun run typecheck`
- Static check that canary failure code does not insert into `tickets`
- Static check that launchd plist files are one-cycle schedules and do not configure parallel workers.
</verification>

<success_criteria>
TRU-03 catches regressions on the originating ticket with attribution, and D-06 tier-2 escalation is integrated with the autonomy ladder without raw operator problem dumps or concurrency increases.
</success_criteria>

<output>
Create `.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-05-SUMMARY.md` when done.
</output>
