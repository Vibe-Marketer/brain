---
phase: 19-throughput-scaleup-trust-survival-autonomy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - repo: brain
    paths:
      - supabase/migrations/20260613200000_phase19_autopilot_trust.sql
      - src/types/supabase.ts
autonomous: true
requirements: [TRU-01, TRU-02, TRU-03]
must_haves:
  truths:
    - "30-day fix survival is durable per fix and durably recorded per category per D-02."
    - "`autopilot_category_trust` stores the category gate state; `autopilot_trust_metrics()` may read it but is not the source of truth."
    - "Category auto promotion can never happen silently; `auto` requires an audited admin event per D-03."
    - "Canary regressions can link back to the originating run and ticket per D-04."
    - "Rate-limit defers are excluded from survival denominators per D-05."
  artifacts:
    - path: "supabase/migrations/20260613200000_phase19_autopilot_trust.sql"
      provides: "Runner trust columns, category ladder/rollup table, trust event audit table, rollup updater, and admin-only trust metrics RPC"
      contains: "autopilot_trust_metrics"
    - path: "src/types/supabase.ts"
      provides: "Generated live Supabase types for Phase 19 trust schema"
      contains: "autopilot_category_trust"
  key_links:
    - from: "runner_runs.id"
      to: "autopilot_trust_events.run_id"
      via: "originating fix/run attribution"
      pattern: "FOREIGN KEY"
    - from: "autopilot_category_trust.rung"
      to: "autopilot_trust_events"
      via: "explicit audited admin promotion/demotion event"
      pattern: "manual|eligible|auto"
---

<objective>
Create the durable trust schema that makes Phase 19 measurable and enforceable.

Purpose: Survival, autonomy promotion, canary attribution, and rate-limit defers all need a shared database contract before Brain UI or Autopilot daemon code can depend on them.
Output: One Supabase migration, live linked schema push, regenerated TypeScript types, and proof that new tables/columns/RPCs and persisted category rollups exist.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-CONTEXT.md
@.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-RESEARCH.md
@.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-PATTERNS.md
@.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-VALIDATION.md
@docs/architecture/autopilot-brain-ownership.md
@supabase/CLAUDE.md
@supabase/migrations/20260611000002_create_ticket_tables.sql
@supabase/migrations/20260612120000_create_admin_audit_log.sql
@supabase/migrations/20260613090000_create_or_extend_runner_runs.sql
@supabase/migrations/20260613180500_source_attribution_backfill_metrics.sql
@src/types/supabase.ts
</context>

## Artifacts this phase produces

- Queryable per-fix survival/canary fields on `runner_runs`.
- `autopilot_category_trust` category ladder state with manual/eligible/auto rungs plus persisted 30-day survival counters.
- Append-only `autopilot_trust_events` audit/event table.
- Rollup updater that persists category counters from matured `runner_runs` and reopen events.
- Admin-only `autopilot_trust_metrics()` convenience RPC over stored category state.
- Regenerated `src/types/supabase.ts` from the linked database.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable trust schema, persisted category rollups, and admin-only metrics RPC</name>
  <files>supabase/migrations/20260613200000_phase19_autopilot_trust.sql</files>
  <read_first>
    - .planning/STATE.md
    - .planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-CONTEXT.md
    - .planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-RESEARCH.md
    - .planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-PATTERNS.md
    - supabase/CLAUDE.md
    - supabase/migrations/20260613090000_create_or_extend_runner_runs.sql
    - supabase/migrations/20260613180500_source_attribution_backfill_metrics.sql
  </read_first>
  <behavior>
    - Matured merged fixes count toward 30-day survival; unmatured, unmerged, and rate-limit-deferred runs do not.
    - Category survival counters are stored durably on `autopilot_category_trust`, not only computed inside `autopilot_trust_metrics()`.
    - Category eligibility is derived from stored rollup fields using defaults of survival rate >= 0.90 and completed fixes >= 5, but `auto` is never computed silently.
    - Canary failure can store the originating run, reopened ticket event, and failed status.
  </behavior>
  <action>Create `supabase/migrations/20260613200000_phase19_autopilot_trust.sql`. Extend `public.runner_runs` with idempotent `ADD COLUMN IF NOT EXISTS` fields for `fix_category`, `merged_at`, `survival_due_at`, `survival_status`, `reopened_at`, `reopened_event_id`, `canary_status`, `canary_last_run_at`, `canary_next_run_at`, and `canary_failure_detail`. Add check constraints for survival statuses `pending`, `held`, `reopened`, `deferred`, `not_applicable` and canary statuses `pending`, `passed`, `failed`, `skipped` using `DO $$`/`pg_constraint` guards. Add `autopilot_category_trust` with `rung` constrained to `manual`, `eligible`, `auto`, default gate columns `min_fixes = 5` and `survival_threshold = 0.90`, durable rollup columns `completed_fixes_30d`, `survived_fixes_30d`, `reopened_fixes_30d`, `survival_rate_30d`, `deferred_runs_30d`, `last_rollup_at`, admin promotion/demotion metadata, and RLS enabled with admin SELECT only. Add append-only `autopilot_trust_events` with run/ticket/category/actor/event metadata, RLS enabled with admin SELECT only and no client INSERT/UPDATE/DELETE policies. Add indexes for due canaries, category survival rollups, trust events by category/created_at, and trust events by run. Add comments documenting D-02, D-03, D-04, and D-05. Create a `SECURITY DEFINER` rollup function such as `public.rollup_autopilot_category_trust()` that upserts/updates `autopilot_category_trust` from matured `runner_runs` within the rolling 30-day window plus linked ticket reopen/trust events: completed fixes are matured merged non-deferred fixes, survived fixes are held/non-reopened matured fixes, reopened fixes are fixes with `survival_status = 'reopened'` or a linked reopen event, deferred runs are `deferred:rate-limit`/deferred survival rows counted separately outside the survival denominator, `survival_rate_30d` is persisted, and `last_rollup_at` is set on every rollup. Create `public.autopilot_trust_metrics()` as `SECURITY DEFINER`, `SET search_path = public, pg_temp`, internally guarded by `public.has_role(auth.uid(), 'ADMIN')`, with PUBLIC/anon revoked and authenticated execute granted. The RPC must return category, rung, completed_fixes, survived_fixes, reopened_fixes, deferred_runs, survival_rate, eligible, canary_due_count, canary_failed_count, threshold, and min_fixes by reading the persisted `autopilot_category_trust` rollup fields, not by recomputing category survival as its source of truth. Do not touch any enum unless append-only semantics are required; this plan should not need enum changes.</action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260613200000_phase19_autopilot_trust.sql | grep -E "autopilot_category_trust|autopilot_trust_events|autopilot_trust_metrics|rollup_autopilot_category_trust|completed_fixes_30d|survived_fixes_30d|reopened_fixes_30d|survival_rate_30d|deferred_runs_30d|last_rollup_at|survival_due_at|canary_next_run_at"</automated>
    <automated>grep -v '^--' supabase/migrations/20260613200000_phase19_autopilot_trust.sql | grep -q "SECURITY DEFINER"</automated>
    <automated>grep -v '^--' supabase/migrations/20260613200000_phase19_autopilot_trust.sql | grep -q "SET search_path = public, pg_temp"</automated>
    <automated>grep -v '^--' supabase/migrations/20260613200000_phase19_autopilot_trust.sql | grep -q "public.has_role(auth.uid(), 'ADMIN')"</automated>
    <automated>grep -v '^--' supabase/migrations/20260613200000_phase19_autopilot_trust.sql | grep -q "GRANT EXECUTE ON FUNCTION public.autopilot_trust_metrics() TO authenticated"</automated>
    <automated>test "$(grep -v '^--' supabase/migrations/20260613200000_phase19_autopilot_trust.sql | grep -Ec \"ALTER TYPE|DROP TYPE\")" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - Migration is idempotent and safe to run on the linked project.
    - RLS allows admin reads and prevents browser writes to trust/audit state.
    - Category rollup values persist in `autopilot_category_trust` and are not only RPC-computed.
    - `autopilot_trust_metrics()` reads persisted category rollups and excludes rate-limit defers and unmerged/unmatured runs from survival denominators.
    - Eligible categories are reported as eligible but not silently promoted to `auto`.
  </acceptance_criteria>
  <done>The Phase 19 trust schema, persisted category rollups, rollup updater, and admin-only metrics RPC exist in migration form with no enum rewrite risk.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push linked schema, regenerate types, and prove live persisted trust tables</name>
  <files>src/types/supabase.ts</files>
  <read_first>
    - supabase/CLAUDE.md
    - .planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-VALIDATION.md
    - supabase/migrations/20260613200000_phase19_autopilot_trust.sql
  </read_first>
  <action>After Task 1 passes static checks, export `SUPABASE_ACCESS_TOKEN` in the shell environment if it is not already available, then run `supabase db push --linked`. Regenerate types with exactly `supabase gen types typescript --linked --schema public > src/types/supabase.ts`. Confirm the linked database contains the new `runner_runs` columns, durable rollup columns on `autopilot_category_trust`, `autopilot_trust_events`, `rollup_autopilot_category_trust()`, and `autopilot_trust_metrics()`. Prove persistence with a transaction-backed linked SQL smoke: insert/update a disposable test category row or use seeded matured run fixtures if available, invoke the rollup updater or update path, query `autopilot_category_trust` directly for `completed_fixes_30d`, `survived_fixes_30d`, `reopened_fixes_30d`, `survival_rate_30d`, `deferred_runs_30d`, and `last_rollup_at`, then roll back or delete the disposable row. This task is blocking: no Brain UI, admin mutation, or Autopilot code may rely on the trust schema until this task is green.</action>
  <verify>
    <automated>supabase db push --linked</automated>
    <automated>supabase gen types typescript --linked --schema public > src/types/supabase.ts</automated>
    <automated>supabase db query --linked "select column_name from information_schema.columns where table_schema = 'public' and table_name = 'runner_runs' and column_name in ('fix_category','survival_due_at','survival_status','canary_next_run_at','reopened_event_id') order by column_name"</automated>
    <automated>supabase db query --linked "select column_name from information_schema.columns where table_schema = 'public' and table_name = 'autopilot_category_trust' and column_name in ('completed_fixes_30d','survived_fixes_30d','reopened_fixes_30d','survival_rate_30d','deferred_runs_30d','last_rollup_at') order by column_name"</automated>
    <automated>supabase db query --linked "select to_regclass('public.autopilot_category_trust') as category_trust, to_regclass('public.autopilot_trust_events') as trust_events, to_regprocedure('public.rollup_autopilot_category_trust()') as rollup_rpc, to_regprocedure('public.autopilot_trust_metrics()') as metrics_rpc"</automated>
    <automated>supabase db query --linked "begin; insert into public.autopilot_category_trust (category, completed_fixes_30d, survived_fixes_30d, reopened_fixes_30d, survival_rate_30d, deferred_runs_30d, last_rollup_at) values ('__phase19_persistence_probe__', 5, 4, 1, 0.8000, 2, now()) on conflict (category) do update set completed_fixes_30d = excluded.completed_fixes_30d, survived_fixes_30d = excluded.survived_fixes_30d, reopened_fixes_30d = excluded.reopened_fixes_30d, survival_rate_30d = excluded.survival_rate_30d, deferred_runs_30d = excluded.deferred_runs_30d, last_rollup_at = excluded.last_rollup_at; select completed_fixes_30d, survived_fixes_30d, reopened_fixes_30d, survival_rate_30d, deferred_runs_30d, last_rollup_at is not null as has_rollup from public.autopilot_category_trust where category = '__phase19_persistence_probe__'; rollback;"</automated>
    <automated>grep -E "autopilot_category_trust|autopilot_trust_events|autopilot_trust_metrics|rollup_autopilot_category_trust|completed_fixes_30d|survival_due_at|canary_next_run_at" src/types/supabase.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push --linked` exits zero in this session.
    - `src/types/supabase.ts` is generated from the linked database, not manually edited.
    - Live schema proof shows the new tables, persisted category rollup columns, and RPCs are present.
    - Direct table query proves category rollup values persist outside `autopilot_trust_metrics()`.
  </acceptance_criteria>
  <done>The linked Supabase project and generated TypeScript contract are ready for Phase 19 consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration author -> production Postgres | Durable trust state and admin RPCs are added to the live database |
| browser admin -> `autopilot_trust_metrics()` | Authenticated browser callers can ask for operational trust metrics |
| autopilot service-role -> trust tables | Daemon can later write trust state that drives approval authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Elevation of Privilege | `autopilot_category_trust.rung` | mitigate | Schema separates computed `eligible` from stored `auto`; later promotion must be explicit admin mutation and audited per D-03. |
| T-19-02 | Information Disclosure | trust tables and metrics RPC | mitigate | RLS admin SELECT only, no browser write policies, RPC internal `has_role` guard, PUBLIC/anon execute revoked. |
| T-19-03 | Tampering | survival denominator | mitigate | RPC requires merged fix identity and matured survival window and excludes `deferred:rate-limit` per D-05. |
| T-19-04 | Repudiation | category promotion/demotion history | mitigate | Append-only `autopilot_trust_events` captures actor, category, old/new values, and metadata. |
| T-19-05 | Tampering | canary attribution | mitigate | `runner_runs.reopened_event_id` references `ticket_events(id)` so canary reopen links to the originating ticket/run per D-04. |
| T-19-SC | Tampering | package installs | accept | No package installs are planned. |
</threat_model>

<verification>
- `supabase db push --linked`
- `supabase gen types typescript --linked --schema public > src/types/supabase.ts`
- Linked schema probes for `runner_runs` trust columns, `autopilot_category_trust`, `autopilot_trust_events`, and `autopilot_trust_metrics()`
- `npm run type-check`
</verification>

<success_criteria>
TRU-01, TRU-02, and TRU-03 have a live, typed database substrate; survival math is admin-only and defer-safe; auto promotion cannot be represented as a silent metric side effect.
</success_criteria>

<output>
Create `.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-01-SUMMARY.md` when done.
</output>
