---
phase: 19-throughput-scaleup-trust-survival-autonomy
plan: 04
type: execute
wave: 3
depends_on: [19-01, 19-02, 19-03]
files_modified:
  - repo: autopilot
    paths:
      - /Users/admin/dev/autopilot/src/lib/trust.ts
      - /Users/admin/dev/autopilot/src/lib/trust.test.ts
      - /Users/admin/dev/autopilot/src/lib/approval.ts
      - /Users/admin/dev/autopilot/src/lib/approval.test.ts
      - /Users/admin/dev/autopilot/src/claimer.ts
autonomous: true
requirements: [TRU-01, TRU-02, ACT-02]
must_haves:
  truths:
    - "Autopilot fails closed to manual approval when trust state is missing or unreadable."
    - "Manual and eligible categories still require explicit admin approval."
    - "Only categories already promoted to `auto` by audited admin event can auto-approve."
    - "Auto-demotion durably updates the trust rung and writes an audit event when rolling survival drops below threshold."
  artifacts:
    - path: "/Users/admin/dev/autopilot/src/lib/trust.ts"
      provides: "Trust ladder reads, durable demotion writes, and eligibility decisions"
      contains: "canAutoApprove"
    - path: "/Users/admin/dev/autopilot/src/lib/approval.ts"
      provides: "Ladder-gated approval candidate discovery"
      contains: "auto"
  key_links:
    - from: "autopilot_category_trust.rung"
      to: "approval merge candidate"
      via: "trust helper fail-closed gate"
      pattern: "manual|eligible|auto"
    - from: "autopilot_trust_metrics"
      to: "auto-demotion"
      via: "survival drop below threshold"
      pattern: "eligible false"
---

<objective>
Enforce the autonomy ladder in the Autopilot approval path.

Purpose: Phase 19 only makes 25-30/day livable if proven categories can auto-approve while risky categories remain manual and silent promotion is impossible.
Output: Trust helper, approval integration, tests for manual/eligible/auto/durable-demotion states, and claimer wiring.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-CONTEXT.md
@.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-RESEARCH.md
@.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-PATTERNS.md
@docs/architecture/autopilot-brain-ownership.md
@/Users/admin/dev/autopilot/src/lib/approval.ts
@/Users/admin/dev/autopilot/src/lib/approval.test.ts
@/Users/admin/dev/autopilot/src/lib/db.ts
@/Users/admin/dev/autopilot/src/claimer.ts
</context>

## Artifacts this phase produces

- `trust.ts` ladder helper with pure state-machine tests.
- Approval path that recognizes admin approvals plus system auto approvals only for `auto` categories.
- Durable auto-demotion logic that updates `autopilot_category_trust.rung` and inserts `autopilot_trust_events` when rolling survival metrics fall below threshold.
- Claimer wiring that processes trust-aware merge candidates before new claims.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add trust ladder helper and fail-closed tests</name>
  <files>/Users/admin/dev/autopilot/src/lib/trust.ts, /Users/admin/dev/autopilot/src/lib/trust.test.ts</files>
  <read_first>
    - /Users/admin/dev/autopilot/src/lib/db.ts
    - /Users/admin/dev/autopilot/src/lib/approval.ts
    - .planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-CONTEXT.md
    - .planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-PATTERNS.md
  </read_first>
  <behavior>
    - `manual` never auto-approves.
    - `eligible` never auto-approves without admin promotion.
    - `auto` auto-approves only while current metrics remain above threshold.
    - A stored `auto` category whose rolling 30-day survival drops below gate is durably demoted to a non-auto rung (`manual` or `eligible` per the implemented trust recomputation policy).
    - Durable demotion writes update `autopilot_category_trust.rung`, `last_demoted_at`, and `updated_at`, and insert an `autopilot_trust_events` row with old/new rung, category, rolling counts, survival rate, and reason.
    - Missing DB rows, RPC errors, unknown categories, and malformed metrics all fail closed to manual.
  </behavior>
  <action>Create `/Users/admin/dev/autopilot/src/lib/trust.ts` with pure functions such as `computeTrustDecision()` and DB functions that read `autopilot_category_trust` and/or `autopilot_trust_metrics()` via the existing `DbLike` shape. Use category from the runner/ticket context, not raw free-form agent text; if no category exists, return manual. Add `applyTrustDemotion()` or equivalent as the durable write path: when metrics show a stored `auto` category is no longer eligible over the rolling 30-day window, update `autopilot_category_trust` to the demoted non-auto rung (`manual` or `eligible` per the implemented trust recomputation policy) and insert an `autopilot_trust_events` event such as `auto_demoted` with old/new rung and metric metadata. Add tests in `trust.test.ts` for manual, eligible, auto, below-threshold durable demotion, unknown category, DB error, and rate-limit-defer exclusion expectations. The demotion test must assert both the trust-table update and the audit insert, not only that a merge was refused.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && bun test src/lib/trust.test.ts</automated>
    <automated>cd /Users/admin/dev/autopilot && grep -R "manual\\|eligible\\|auto_demoted\\|autopilot_category_trust\\|autopilot_trust_events" src/lib/trust.ts src/lib/trust.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Default/failure decision is manual.
    - Survival gate and explicit rung are both required for auto approval.
    - Demotion from `auto` is a durable state change: `autopilot_category_trust` is updated and `autopilot_trust_events` receives an audit row when rolling survival drops below threshold.
  </acceptance_criteria>
  <done>Trust ladder decisions are deterministic, tested, and fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate approval candidates through trust ladder</name>
  <files>/Users/admin/dev/autopilot/src/lib/approval.ts, /Users/admin/dev/autopilot/src/lib/approval.test.ts, /Users/admin/dev/autopilot/src/lib/trust.ts</files>
  <read_first>
    - /Users/admin/dev/autopilot/src/lib/approval.ts
    - /Users/admin/dev/autopilot/src/lib/approval.test.ts
    - /Users/admin/dev/autopilot/src/lib/trust.ts
    - .planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-RESEARCH.md
  </read_first>
  <behavior>
    - Existing admin approval/rejection behavior remains intact.
    - Auto categories produce a merge candidate only when tests/gate are clean and trust helper says auto is allowed.
    - Auto approval writes a distinct system/trust event and does not forge admin approval rows.
  </behavior>
  <action>Extend `approval.ts` so the candidate source returns explicit admin approvals plus trust-approved system candidates. Keep the existing rebase -> repro replay -> gate -> ff-only -> push/deploy verification order unchanged; `auto` means auto-approve only and never bypasses the deterministic push-gate or ff-only merge. Add a `source` or `approvalSource` field if needed to distinguish `admin` from `autopilot_auto`. For auto candidates, write an `autopilot_trust_events` event such as `auto_approval_used` with run/ticket/category metadata; do not insert a fake admin `approval` event. Preserve rejection semantics and role lookup for manual admin events. Add tests proving manual and eligible categories stay awaiting approval, auto categories merge through the same mechanics, and stale/below-threshold auto categories call the Task 1 demotion write path and do not merge.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && bun test src/lib/approval.test.ts src/lib/trust.test.ts</automated>
    <automated>cd /Users/admin/dev/autopilot && grep -R "auto_approval\\|approvalSource\\|autopilot_trust_events" src/lib/approval.ts src/lib/approval.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Admin-authored approvals and rejections behave as before.
    - No category reaches auto approval unless DB rung is `auto` and current survival gate passes.
    - Auto approvals are separately auditable and do not spoof an admin actor.
  </acceptance_criteria>
  <done>Approval execution is ladder-gated and preserves the deterministic push-gate authority boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire trust-aware approvals into the one-cycle claimer</name>
  <files>/Users/admin/dev/autopilot/src/claimer.ts, /Users/admin/dev/autopilot/src/claimer.test.ts</files>
  <read_first>
    - /Users/admin/dev/autopilot/src/claimer.ts
    - /Users/admin/dev/autopilot/src/lib/approval.ts
    - /Users/admin/dev/autopilot/src/lib/trust.ts
    - /Users/admin/dev/autopilot/src/claimer.test.ts
  </read_first>
  <behavior>
    - Claimer processes trust-aware merge candidates before claiming new work.
    - Claimer still exits when lockdir is held, preserving concurrency 1.
    - Dry-run logs would-merge decisions without mutating DB.
  </behavior>
  <action>Replace or wrap `findApprovals()` in `claimer.ts` with the trust-aware candidate source from Task 2. Keep approval processing before new claims, keep the one-cycle launchd shape, and keep lockdir concurrency behavior unchanged. Add tests or extend `claimer.test.ts` to show manual/eligible categories do not increase merge count, auto category does, dry-run does not execute merge, and budget/quiet-hours suppression still runs after approval processing.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && bun test src/claimer.test.ts src/lib/approval.test.ts src/lib/trust.test.ts</automated>
    <automated>cd /Users/admin/dev/autopilot && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Trust-aware auto approvals are handled in the existing approval-before-claim phase.
    - Concurrency remains 1 and dry-run remains non-mutating.
    - The run cap only controls new claims, not completion of already-approved safe merges.
  </acceptance_criteria>
  <done>The one-cycle daemon can use the autonomy ladder without changing scheduler/concurrency invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase trust state -> merge authority | DB ladder rows influence whether Autopilot can merge without per-fix admin approval |
| Autopilot daemon -> Git merge/push | Service process executes merge mechanics on behalf of approved candidates |
| metrics freshness -> auto-demotion | Stale or failed metric reads could over-grant authority if not fail-closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-14 | Elevation of Privilege | `trust.ts` / `approval.ts` | mitigate | Require stored rung `auto` plus current survival gate; missing/error state returns manual. |
| T-19-15 | Spoofing | auto approval events | mitigate | Use distinct system trust events; never forge admin `approval` events or actor IDs. |
| T-19-16 | Tampering | merge path | mitigate | Preserve rebase, repro replay, deterministic gate, ff-only merge, and deploy verification order. |
| T-19-17 | Repudiation | auto-demotion | mitigate | Write trust event metadata when survival drop causes demotion. |
| T-19-SC | Tampering | package installs | accept | No new packages. |
</threat_model>

<verification>
- `cd /Users/admin/dev/autopilot && bun test src/lib/trust.test.ts src/lib/approval.test.ts src/claimer.test.ts`
- `cd /Users/admin/dev/autopilot && bun run typecheck`
- Manual DB seed smoke, if linked test DB is available, with one manual, one eligible, and one auto category.
</verification>

<success_criteria>
TRU-02 is enforced in the daemon: category survival can make work eligible, explicit admin promotion can make it auto, and drops demote automatically without raising concurrency or bypassing push-gate.
</success_criteria>

<output>
Create `.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-04-SUMMARY.md` when done.
</output>
