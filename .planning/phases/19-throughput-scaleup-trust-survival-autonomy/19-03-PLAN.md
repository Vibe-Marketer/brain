---
phase: 19-throughput-scaleup-trust-survival-autonomy
plan: 03
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - repo: autopilot
    paths:
      - /Users/admin/dev/autopilot/autopilot.config.ts
      - /Users/admin/dev/autopilot/src/claimer.ts
      - /Users/admin/dev/autopilot/src/claimer.test.ts
      - /Users/admin/dev/autopilot/src/lib/claim.ts
      - /Users/admin/dev/autopilot/src/lib/claim.test.ts
      - /Users/admin/dev/autopilot/src/runner.ts
      - /Users/admin/dev/autopilot/src/runner.test.ts
autonomous: true
requirements: [ACT-02, TRU-01]
must_haves:
  truths:
    - "Throughput tuning is via run cap and cadence only; `concurrency` remains literal 1 per D-01."
    - "Phase 19 ships the mechanism and conservative tunable cap, not a live volume raise."
    - "Rate-limit hits release the claim, jitter/back off, clean worktree, and ledger as defer, not failed fix, per D-05."
    - "Rate-limit defers do not poison survival metrics or max-attempt accounting."
  artifacts:
    - path: "/Users/admin/dev/autopilot/autopilot.config.ts"
      provides: "Conservative throughput and quiet-hours config knobs"
      contains: "concurrency: 1"
    - path: "/Users/admin/dev/autopilot/src/lib/claim.ts"
      provides: "Rate-limit defer release helper"
      contains: "rate_limit_defer"
  key_links:
    - from: "runner.ts detectRateLimit"
      to: "claim.ts rate-limit defer helper"
      via: "distinct terminal branch"
      pattern: "deferred:rate-limit"
    - from: "autopilot.config.ts"
      to: "claimer.ts"
      via: "maxRunsPerWindow + quietHours"
      pattern: "concurrency: 1"
---

<objective>
Ship the throughput control and rate-limit survival guard in Autopilot without raising concurrency or live volume.

Purpose: ACT-02 only becomes safe when the daemon can tune cap/cadence conservatively and treat subscription rate limits as retryable defers rather than failed fixes.
Output: Config/test coverage for conservative cap/cadence and a distinct rate-limit defer path in claim/runner.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-CONTEXT.md
@.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-RESEARCH.md
@.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-PATTERNS.md
@docs/architecture/autopilot-brain-ownership.md
@/Users/admin/dev/autopilot/autopilot.config.ts
@/Users/admin/dev/autopilot/src/claimer.ts
@/Users/admin/dev/autopilot/src/lib/claim.ts
@/Users/admin/dev/autopilot/src/lib/evidence.ts
@/Users/admin/dev/autopilot/src/runner.ts
</context>

## Artifacts this phase produces

- Conservative throughput config knobs for future post-activation tuning.
- Test proving concurrency stays 1 and quiet-hours/headroom still suppress claims.
- Rate-limit defer helper and runner branch.
- Tests proving rate-limit defers are not failed fixes.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add conservative throughput cap/cadence tests without live volume raise</name>
  <files>/Users/admin/dev/autopilot/autopilot.config.ts, /Users/admin/dev/autopilot/src/claimer.ts, /Users/admin/dev/autopilot/src/claimer.test.ts</files>
  <read_first>
    - .planning/STATE.md
    - .planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-CONTEXT.md
    - /Users/admin/dev/autopilot/autopilot.config.ts
    - /Users/admin/dev/autopilot/src/claimer.ts
  </read_first>
  <behavior>
    - `concurrency` remains the literal value `1`.
    - Claims are suppressed during quiet hours and when max runs per window is reached.
    - Config includes a conservative tunable cap/cadence mechanism, but does not flip live volume to 25-30/day in Phase 19.
  </behavior>
  <action>In `/Users/admin/dev/autopilot`, add or extend config comments/types for conservative Phase 19 throughput tuning: run cap, cadence, and quiet-hours reserve headroom for Andrew's interactive Claude per D-01. Keep the current live `maxRunsPerWindow.maxRuns` conservative unless Phase 17-05 activation evidence says otherwise; do not set it to 25-30/day. Keep `concurrency: 1` as a literal type and runtime value. Add `src/claimer.test.ts` coverage for quiet-hours suppression, budget-cap suppression, and a guard that fails if `config.concurrency !== 1`. If `claimer.ts` needs tiny dependency injection to make these tests possible, keep it local and avoid changing one-cycle launchd behavior.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && bun test src/claimer.test.ts</automated>
    <automated>cd /Users/admin/dev/autopilot && grep -R "concurrency: 1" autopilot.config.ts</automated>
    <automated>cd /Users/admin/dev/autopilot && test "$(grep -R "concurrency: [2-9]" autopilot.config.ts src | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - No code path raises concurrency above 1.
    - Tests prove run-cap and quiet-hours remain suppressors.
    - Plan ships tunable mechanism with conservative default and records that real cap re-probe is post-activation.
  </acceptance_criteria>
  <done>Autopilot throughput can be tuned safely later while current live volume and concurrency remain conservative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add rate-limit defer helper that releases claim without failed-fix poisoning</name>
  <files>/Users/admin/dev/autopilot/src/lib/claim.ts, /Users/admin/dev/autopilot/src/lib/claim.test.ts</files>
  <read_first>
    - /Users/admin/dev/autopilot/src/lib/claim.ts
    - /Users/admin/dev/autopilot/src/lib/claim.test.ts
    - .planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-RESEARCH.md
  </read_first>
  <behavior>
    - Rate-limit defer writes a distinct `rate_limit_defer` event.
    - Ticket is returned to `new` with jittered `next_attempt_at`.
    - Defer count is tracked separately from genuine fix failure accounting.
  </behavior>
  <action>Add `releaseClaimForRateLimit()` or an equivalent focused helper. It must preserve the atomic status guard (`eq("status", "in_progress")`), write a `rate_limit_defer` ticket event, set `status = "new"`, set a jittered `next_attempt_at`, and avoid adding a failed-fix outcome. Do not decrement `attempts` unless the executor proves race safety; prefer a separate `rate_limit_defer_count` update if the schema/row supports it or trust event/detail metadata if not. Add tests using the existing mocked DB pattern to verify event type, status update, backoff timing, and no failure/escalation event. This implements D-05 and protects TRU-01 denominators.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && bun test src/lib/claim.test.ts</automated>
    <automated>cd /Users/admin/dev/autopilot && grep -R "rate_limit_defer" src/lib/claim.ts src/lib/claim.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Rate-limit branch produces a retryable defer, not a failed fix or rejection.
    - Backoff includes jitter and does not immediately hot-loop.
    - Helper fails closed if ticket is no longer `in_progress`.
  </acceptance_criteria>
  <done>Claim release has a first-class rate-limit defer path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route runner rate-limit transcripts to defer and cleanup</name>
  <files>/Users/admin/dev/autopilot/src/runner.ts, /Users/admin/dev/autopilot/src/runner.test.ts</files>
  <read_first>
    - /Users/admin/dev/autopilot/src/runner.ts
    - /Users/admin/dev/autopilot/src/lib/evidence.ts
    - /Users/admin/dev/autopilot/src/lib/claim.ts
    - .planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-PATTERNS.md
  </read_first>
  <behavior>
    - Transcript rate-limit patterns return `deferred:rate-limit`.
    - Ledger status is `requeued`, gate verdict is `skipped`, and gate stage is `rate_limit`.
    - Worktree cleanup still runs.
    - Catch path uses transcript evidence when available and does not log rate-limit as `failed`.
  </behavior>
  <action>In `runner.ts`, branch immediately after transcript capture and again in the catch path when a transcript exists: if `detectRateLimit(transcript)` is true, call the rate-limit defer helper, finish the runner_run with `status: "requeued"`, `outcome: "deferred:rate-limit"`, `gate_verdict: "skipped"`, `gate_stage: "rate_limit"`, and detail metadata `{ reason: "rate-limit", rate_limit_suspected: true }`. Return the distinct result and let the existing `finally` path destroy the worktree and idle the runner. Do not route this through verification-failed, max-attempt escalation, survival failure, or approval paths. Add focused tests or extracted helper tests for the success transcript branch and catch-path transcript branch.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && bun test src/runner.test.ts src/lib/claim.test.ts src/lib/evidence.test.ts</automated>
    <automated>cd /Users/admin/dev/autopilot && grep -R "deferred:rate-limit\\|gate_stage.*rate_limit\\|rate_limit_suspected" src/runner.ts src/runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Rate-limit transcripts never produce `failed:*`, `released:verification-failed`, or max-attempt escalation.
    - Runner ledger records defer distinctly enough for Brain metrics to exclude it.
    - Worktree cleanup remains guaranteed.
  </acceptance_criteria>
  <done>Subscription rate limits are retryable defers, not failed fixes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude/agent transcript -> runner control flow | Untrusted output text can affect defer/failure classification |
| runner -> Supabase tickets | Service-role daemon releases claims and writes events |
| local scheduler -> machine resources | Cap/cadence choices affect Andrew's shared interactive Claude quota |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10 | Tampering | rate-limit detector branch | mitigate | Only known rate-limit patterns trigger defer; defer still releases to normal queue with backoff, not gate bypass success. |
| T-19-11 | Denial of Service | throughput cap/cadence | mitigate | Keep conservative default, quiet-hours suppression, and concurrency literal 1; re-probe after Phase 17-05 only. |
| T-19-12 | Repudiation | defer accounting | mitigate | Write distinct ticket event and runner_runs detail for every rate-limit defer. |
| T-19-13 | Elevation of Privilege | spoofed defer to skip approval gate | mitigate | Defer does not merge, approve, resolve, or mark survival; it only releases claim for retry. |
| T-19-SC | Tampering | packages | accept | No new packages; Autopilot remains Bun/TypeScript with existing tests. |
</threat_model>

<verification>
- `cd /Users/admin/dev/autopilot && bun test src/claimer.test.ts src/lib/claim.test.ts src/runner.test.ts src/lib/evidence.test.ts`
- `cd /Users/admin/dev/autopilot && bun run typecheck`
- Static grep confirming `concurrency: 1` and no `concurrency: [2-9]`
</verification>

<success_criteria>
ACT-02 mechanism is shippable without live volume raise, and D-05 rate-limit defers cannot corrupt TRU-01 survival metrics or approval gates.
</success_criteria>

<output>
Create `.planning/phases/19-throughput-scaleup-trust-survival-autonomy/19-03-SUMMARY.md` when done.
</output>
