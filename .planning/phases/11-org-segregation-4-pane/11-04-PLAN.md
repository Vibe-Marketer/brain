---
phase: 11-org-segregation-4-pane
plan: 04
type: execute
wave: 2
depends_on: [11-02]
files_modified:
  - src/pages/CallDetailPage.tsx
  - src/pages/Analytics.tsx
  - src/components/layout/AppShell.tsx
autonomous: false
requirements: [PANE-03, PANE-04]

must_haves:
  truths:
    - "Call detail opens as a modal overlay (existing CallDetailDialogue), not as a standalone page"
    - "All major pages follow the Sidebar -> List -> Workspace -> Detail pane hierarchy"
    - "Complex/dynamic items (call detail, setup wizards) open as modals; simple config/preview appears in Pane 4"
    - "Analytics page uses AppShell with proper pane structure"
  artifacts:
    - path: "src/pages/CallDetailPage.tsx"
      provides: "Redirects to modal-based call detail instead of standalone page"
      contains: "redirect\|navigate\|CallDetailDialogue"
    - path: "src/pages/Analytics.tsx"
      provides: "Analytics using consistent AppShell layout"
      contains: "AppShell"
  key_links:
    - from: "src/pages/CallDetailPage.tsx"
      to: "CallDetailDialogue"
      via: "opens modal or redirects to calls page with modal"
      pattern: "CallDetailDialogue\|navigate.*calls"
    - from: "src/pages/Analytics.tsx"
      to: "src/components/layout/AppShell.tsx"
      via: "uses AppShell layout"
      pattern: "AppShell"
---

<objective>
Audit all major pages for 4-pane hierarchy compliance and enforce modal vs Pane 4 rules.

Purpose: Per PANE-01, all major pages must follow the Sidebar->List->Workspace->Detail hierarchy. Per PANE-03/PANE-04 and D-07 through D-10, complex items like call detail open as modals while simple config opens in Pane 4. The CallDetailPage currently renders as a standalone page but should use the modal pattern. Analytics needs layout fixes.

Output: All pages compliant with 4-pane hierarchy; call detail routed through modal; modal vs Pane 4 rules documented in code.
</objective>

<execution_context>
@/Users/Naegele/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Naegele/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-org-segregation-4-pane/11-CONTEXT.md

<interfaces>
From src/components/layout/AppShell.tsx:
```typescript
export interface AppShellConfig {
  showNavRail?: boolean;
  secondaryPane?: React.ReactNode;
  secondaryPaneTitle?: string;
  showDetailPane?: boolean;
}
```

From CONTEXT.md decisions:
- D-07: Call detail opens as modal overlay (existing CallDetailDialogue)
- D-08: Pane 4 for simple/quick config: workspace settings, folder details, tag management, member info, import source config, quick call preview
- D-09: Modals for complex/deep content: full call detail + transcript, onboarding wizard, first-time source setup, bulk import selection, advanced org settings
- D-10: Dividing line — scrollable multi-section content or focused attention = modal; quick glance or single-action = Pane 4
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CallDetailPage to use modal pattern and audit Analytics page</name>
  <files>src/pages/CallDetailPage.tsx, src/pages/Analytics.tsx, src/components/layout/AppShell.tsx</files>
  <read_first>
    - src/pages/CallDetailPage.tsx (full file — understand current standalone page pattern)
    - src/pages/Analytics.tsx (full file — understand current layout)
    - src/components/layout/AppShell.tsx (AppShellConfig)
    - src/components/calls/CallDetailDialogue.tsx (or similar name — the existing modal component)
    - src/App.tsx (or router config — to understand route definitions)
    - src/pages/TranscriptsNew.tsx (reference for 4-pane pattern, first 30 lines)
  </read_first>
  <action>
**CallDetailPage.tsx** (per D-07):

The current CallDetailPage.tsx renders call detail as a standalone page (522 lines). Per D-07, call detail must open as a modal overlay. Two approaches — choose based on what you find:

**Option A (preferred if CallDetailDialogue exists and works):** Convert CallDetailPage to a thin redirect that:
1. Extracts the call ID from the URL params
2. Navigates back to the Calls page (`/`) with a search param like `?callId={id}`
3. The Calls page (TranscriptsNew.tsx) already has or should have logic to open CallDetailDialogue when this param is present

**Option B (if the modal doesn't fully replace the page yet):** Keep CallDetailPage but wrap it in AppShell with proper 4-pane layout:
1. Wrap content in `<AppShell>` instead of bare div
2. The standalone page becomes a fallback for direct URL access (bookmarks, shared links)
3. Add a comment: `// TODO: Phase 12+ — fully replace with modal-only pattern via CallDetailDialogue`

For **either option**, ensure the existing call detail modal (CallDetailDialogue) is the primary path from the transcript table. Check TranscriptsNew.tsx — if clicking a row navigates to `/call/:id` as a page route, change it to open the modal instead.

**Analytics.tsx:**

Read the current Analytics page (97 lines). It likely uses AppShell already (most pages do), but CONTEXT.md says it's "3-pane only (no detail outlet)". Fixes:
1. Ensure it uses `<AppShell>` wrapper
2. If it has meaningful sidebar content, pass it as `secondaryPane`
3. If it doesn't need Pane 4, that's fine — not every page needs all 4 panes. But the hierarchy should be consistent: Sidebar (Pane 1) always present, content in Pane 3.

**Document modal vs Pane 4 rules as a code comment:**

Add a JSDoc comment block in `src/components/layout/AppShell.tsx` (near the top, below the existing architecture comment) documenting the rules from D-08/D-09/D-10:

```typescript
/**
 * ## Modal vs Pane 4 Rules (Phase 11 Decision)
 *
 * **Pane 4 (showDetailPane):** Quick config, single-action views
 * - Workspace settings, folder detail/rename, tag management
 * - Member role/info, import source config, quick call preview (title + summary)
 *
 * **Modal overlay:** Complex, multi-section, focused-attention views
 * - Full call detail + transcript (CallDetailDialogue)
 * - Onboarding wizard, first-time source setup
 * - Bulk import selection, advanced org settings
 *
 * **Rule of thumb:** If it has scrollable content with multiple sections
 * or requires focused user attention, use a modal. If it's a quick glance
 * or single-action config, use Pane 4.
 */
```
  </action>
  <verify>
    <automated>grep -n "AppShell\|CallDetailDialogue\|navigate\|modal" src/pages/CallDetailPage.tsx | head -10 && grep -n "AppShell" src/pages/Analytics.tsx | head -5 && grep -c "Modal vs Pane 4" src/components/layout/AppShell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CallDetailPage.tsx either: (a) redirects to calls page with callId param, OR (b) wraps in AppShell with TODO comment for modal migration
    - Analytics.tsx uses `<AppShell>` wrapper
    - src/components/layout/AppShell.tsx contains "Modal vs Pane 4 Rules" comment block
    - The comment block references Pane 4 use cases (workspace settings, folder detail, tag management)
    - The comment block references modal use cases (call detail, onboarding wizard)
    - No TypeScript errors
  </acceptance_criteria>
  <done>CallDetailPage uses modal pattern (or is wrapped in AppShell with migration TODO). Analytics uses AppShell. Modal vs Pane 4 rules documented in AppShell.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Visual verification of 4-pane layout and org switch behavior</name>
  <files>src/pages/ImportPage.tsx, src/pages/TranscriptsNew.tsx</files>
  <read_first>
    - src/pages/ImportPage.tsx
    - src/pages/TranscriptsNew.tsx
    - src/components/layout/AppShell.tsx
  </read_first>
  <action>
Use dev-browser to verify the complete Phase 11 implementation:

1. Start dev server if not running: `npm run dev`
2. Navigate to http://localhost:3001 and log in with test credentials from .env.local
3. **Import page verification:**
   - Navigate to Import page
   - Screenshot: Verify Pane 2 shows source list with icons and connection status dots
   - Screenshot: Verify "Routing Rules" and "Import History" appear below a divider in Pane 2
   - Screenshot: With no source selected, verify Pane 3 shows overview dashboard with summary cards
   - Click a source in Pane 2 — screenshot to verify Pane 3 updates without full page reload
4. **Org switch verification** (if multiple orgs available):
   - Apply a filter or search query on the Calls page
   - Switch orgs via the header switcher
   - Verify content area fades briefly
   - Verify you land on the Calls page with all filters/search cleared
5. **Call detail verification:**
   - Click a call in the transcript table
   - Verify it opens as a modal overlay, not a standalone page
6. **Analytics verification:**
   - Navigate to Analytics page
   - Verify it uses the consistent sidebar layout

If any issues found, fix them in the relevant files. This task is the final quality gate before Phase 11 is marked complete.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Import page renders with Pane 2 showing 4 sources with connection indicators
    - Import page Pane 3 shows overview dashboard when no source selected
    - Analytics page renders with AppShell sidebar
    - Call detail opens as modal from transcript table
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>All Phase 11 features verified visually via dev-browser. Import page 4-pane layout works. Org switch resets state. Call detail opens as modal. Analytics uses consistent layout.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors
2. All major pages (Calls, Import, Analytics, Settings) use AppShell
3. Call detail opens as modal from transcript table
4. Modal vs Pane 4 rules documented in AppShell.tsx
</verification>

<success_criteria>
- CallDetailPage routes through modal pattern (per D-07)
- Analytics page uses consistent AppShell layout
- Modal vs Pane 4 rules documented as code comment in AppShell.tsx (per D-08, D-09, D-10)
- All 4-pane layouts verified via dev-browser
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-org-segregation-4-pane/11-04-SUMMARY.md`
</output>
