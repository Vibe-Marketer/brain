---
phase: 17-payments-billing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/BillingTab.tsx
  - src/hooks/useAiUsage.ts
  - src/services/ai-usage.service.ts
  - supabase/functions/polar-cancel/index.ts
autonomous: true
requirements: [PAY-01, PAY-04, PAY-06]
must_haves:
  truths:
    - "User sees Free/Pro/Team plans with correct pricing on billing settings page"
    - "User can cancel their subscription from billing settings without leaving the app"
    - "User sees their current AI usage count and monthly limit in billing settings"
  artifacts:
    - path: "src/hooks/useAiUsage.ts"
      provides: "AI usage data hook"
      exports: ["useAiUsage"]
    - path: "src/services/ai-usage.service.ts"
      provides: "AI usage service layer"
      exports: ["getMonthlyAiUsage"]
    - path: "supabase/functions/polar-cancel/index.ts"
      provides: "Cancel subscription edge function"
    - path: "src/components/settings/BillingTab.tsx"
      provides: "Updated billing tab with cancel button and usage display"
  key_links:
    - from: "src/hooks/useAiUsage.ts"
      to: "ai_usage table via RPC get_monthly_ai_usage"
      via: "supabase RPC call"
      pattern: "get_monthly_ai_usage"
    - from: "src/components/settings/BillingTab.tsx"
      to: "supabase/functions/polar-cancel"
      via: "supabase.functions.invoke"
      pattern: "polar-cancel"
---

<objective>
Add cancel subscription button and AI usage display to the billing settings page. Create the service layer and hook for fetching AI usage data, and create the polar-cancel edge function.

Purpose: Users need to cancel their subscription without leaving the app (PAY-04), see their AI usage (PAY-06), and verify plan display works correctly (PAY-01).
Output: Updated BillingTab with cancel button + usage meter, new useAiUsage hook, polar-cancel edge function.
</objective>

<execution_context>
@/Users/Naegele/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Naegele/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing types and contracts the executor needs -->

From src/hooks/useSubscription.ts:
```typescript
export type SubscriptionTier = 'free' | 'pro' | 'team';
export type SubscriptionStatus = 'active' | 'canceled' | 'revoked' | 'incomplete' | 'incomplete_expired' | 'trialing' | 'past_due' | 'unpaid' | 'free' | null;
export const AI_ACTION_LIMITS: Record<SubscriptionTier, number> = { free: 25, pro: 1000, team: 5000 };
export interface SubscriptionState {
  tier: SubscriptionTier;
  status: SubscriptionStatus;
  subscriptionId: string | null;
  isPaid: boolean;
  isTrialing: boolean;
  periodEnd: Date | null;
  aiActionsLimit: number;
  refetch: () => void;
  // ... other fields
}
```

From supabase/migrations/20260309000001_ai_credits_system.sql:
```sql
-- RPC function already exists:
CREATE OR REPLACE FUNCTION public.get_monthly_ai_usage(p_user_id UUID, p_month_year TEXT) RETURNS INTEGER
-- Table: ai_usage (id, user_id, org_id, action_type, recording_id, month_year, created_at)
```

From supabase/functions/_shared/polar-client.ts:
```typescript
export function getPolarClient(): Polar;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AI usage service + hook and polar-cancel edge function</name>
  <files>src/services/ai-usage.service.ts, src/hooks/useAiUsage.ts, supabase/functions/polar-cancel/index.ts</files>
  <read_first>
    - src/services/folders.service.ts (service pattern reference)
    - src/hooks/useSubscription.ts (hook pattern reference)
    - supabase/functions/polar-checkout/index.ts (edge function pattern reference)
    - supabase/functions/_shared/polar-client.ts (Polar SDK client)
    - supabase/functions/_shared/cors.ts (CORS helper)
  </read_first>
  <action>
    1. Create src/services/ai-usage.service.ts:
       - Export async function getMonthlyAiUsage(): calls supabase RPC `get_monthly_ai_usage` with current user's ID and current month in 'YYYY-MM' format (use `new Date().toISOString().slice(0, 7)`)
       - Returns { used: number, limit: number, tier: SubscriptionTier } by combining RPC result with AI_ACTION_LIMITS from useSubscription
       - Actually, keep it simpler: just return the count (number). The hook will combine with subscription data.
       - Use getSafeUser from @/lib/auth-utils for user ID
       - Follow existing service patterns (pure async, no React)

    2. Create src/hooks/useAiUsage.ts:
       - Export function useAiUsage() that returns { used: number, limit: number, percentUsed: number, isLoading: boolean, error: Error | null }
       - Uses TanStack Query with queryKey ['ai-usage', monthYear] where monthYear = current YYYY-MM
       - Internally calls getMonthlyAiUsage service
       - Gets limit from useSubscription().aiActionsLimit
       - percentUsed = Math.min(100, Math.round((used / limit) * 100))
       - staleTime: 30000 (30 seconds — usage can change frequently)

    3. Create supabase/functions/polar-cancel/index.ts:
       - POST endpoint, JWT-authenticated (follow polar-checkout pattern exactly)
       - Reads user's subscription_id from user_profiles
       - If no subscription_id, return 400 { error: 'No active subscription' }
       - Calls polar.subscriptions.cancel({ id: subscriptionId }) using getPolarClient()
       - On success, update user_profiles: set subscription_status = 'canceled' (keep subscription_id and current_period_end — user keeps access until period end)
       - Return { success: true, accessUntil: current_period_end }
       - Use getCorsHeaders from _shared/cors.ts
  </action>
  <verify>
    <automated>cd /Users/Naegele/dev/brain && npx tsc --noEmit src/services/ai-usage.service.ts src/hooks/useAiUsage.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getMonthlyAiUsage" src/services/ai-usage.service.ts
    - grep -q "useAiUsage" src/hooks/useAiUsage.ts
    - grep -q "get_monthly_ai_usage" src/services/ai-usage.service.ts
    - grep -q "polar-cancel" supabase/functions/polar-cancel/index.ts
    - grep -q "subscriptions" supabase/functions/polar-cancel/index.ts
  </acceptance_criteria>
  <done>AI usage service+hook created and type-check; polar-cancel edge function created following existing patterns</done>
</task>

<task type="auto">
  <name>Task 2: Add cancel button and AI usage display to BillingTab</name>
  <files>src/components/settings/BillingTab.tsx</files>
  <read_first>
    - src/components/settings/BillingTab.tsx (current state)
    - src/hooks/useAiUsage.ts (just created in Task 1)
    - src/hooks/useSubscription.ts (subscription state)
    - src/components/ui/progress.tsx (if exists, for usage bar)
  </read_first>
  <action>
    1. Add AI Usage section to BillingTab between "Current Plan" and "All Plans" sections:
       - New grid section (same layout pattern: 3-col with label left, content right)
       - Left column: heading "AI Usage" + subtext "Monthly AI actions for your plan"
       - Right column:
         a. Usage bar: Tailwind progress bar showing percentUsed (use div with bg-primary for fill, bg-muted for track, h-2 rounded-full)
         b. Text below bar: "{used} / {limit} actions used this month" in text-sm text-muted-foreground
         c. If percentUsed >= 90: show warning text "You're approaching your monthly limit" in text-sm text-amber-600
         d. If percentUsed >= 100: show "Monthly limit reached — AI features are paused" in text-sm text-red-600
         e. Loading state: Skeleton for bar and text
       - Import and call useAiUsage()

    2. Add Cancel Subscription button to the "Current Plan" section:
       - Only show when isPaid is true AND status !== 'canceled'
       - Place below the period info (pl-16 indent to match existing layout)
       - Use AlertDialog from @radix-ui/react-alert-dialog for confirmation
       - Dialog title: "Cancel subscription?"
       - Dialog description: "You'll keep access until {formatDate(periodEnd)}. After that, you'll be downgraded to the Free plan."
       - Cancel button: variant="outline" text "Keep subscription"
       - Confirm button: variant="destructive" text "Cancel subscription"
       - On confirm: call supabase.functions.invoke('polar-cancel') with auth header, show toast on success/error, call refetch() from useSubscription
       - Button text: "Cancel subscription" in text-sm text-muted-foreground hover:text-destructive (link-style, not a big red button)
       - When status === 'canceled': show "Your subscription has been canceled. Access continues until {formatDate(periodEnd)}." instead of cancel button

    3. Verify PAY-01: Plan display already works (PlanCards shows Free/Pro/Team with pricing). No changes needed — just confirm the section renders correctly.

    Use RiBankCardLine (already imported), RiAlertLine or RiErrorWarningLine from @remixicon/react for usage warnings.
  </action>
  <verify>
    <automated>cd /Users/Naegele/dev/brain && npx tsc --noEmit src/components/settings/BillingTab.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useAiUsage" src/components/settings/BillingTab.tsx
    - grep -q "polar-cancel" src/components/settings/BillingTab.tsx
    - grep -q "Cancel subscription" src/components/settings/BillingTab.tsx
    - grep -q "AI Usage" src/components/settings/BillingTab.tsx
    - grep -q "actions used" src/components/settings/BillingTab.tsx
  </acceptance_criteria>
  <done>BillingTab shows AI usage meter with limit info, cancel button with confirmation dialog, and existing plan display remains functional</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit` passes for all modified files
- Cancel button visible in BillingTab for paid users (behind AlertDialog confirmation)
- AI usage section shows usage bar and count
- polar-cancel edge function follows existing edge function patterns
</verification>

<success_criteria>
- PAY-01: Free/Pro/Team plans display with pricing (already works, preserved)
- PAY-04: Cancel subscription button exists in billing settings with confirmation dialog
- PAY-06: AI usage count and limit visible in billing settings with progress bar
</success_criteria>

<output>
After completion, create `.planning/phases/17-payments-billing/17-01-SUMMARY.md`
</output>
