---
phase: 05-connector-reliability-per-workspace-binding-unified-sync-tab
plan: 04
type: execute
wave: 3
depends_on:
  - "05-01"
files_modified:
  - src/services/sync-tab.service.ts
  - src/hooks/useExistingTranscripts.ts
  - src/components/transcripts/SyncTab.tsx
  - src/components/transcripts/SyncedTranscriptsSection.tsx
  - src/services/__tests__/sync-tab.service.test.ts
autonomous: true
requirements:
  - HRD-01
must_haves:
  truths:
    - "D-27/D-28/D-29/D-30: preserve the three-pane Transcripts structure and Import Meetings workflow while changing Synced Transcripts to canonical recordings."
    - "HRD-01: SyncTab reads from `recordings`/`workspace_entries`, not `fathom_calls`."
    - "Recording ID rule: UUID/BIGINT conversion uses `toRecordingUuid()` / `toRecordingUuidBatch()` only."
    - "Share URL rule: use `resolveShareUrl()`; do not read `recordings.share_url`."
  artifacts:
    - path: "src/services/sync-tab.service.ts"
      provides: "Canonical recordings-backed synced transcript list"
---

<objective>
Fix Import Meetings / SyncTab so the Synced Transcripts section lists canonical recordings from every source, not only Fathom rows.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-CONTEXT.md
@.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-RESEARCH.md
@.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-PATTERNS.md
@src/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical SyncTab service mapping tests</name>
  <files>src/services/__tests__/sync-tab.service.test.ts</files>
  <read_first>
    - src/services/sync-tab.service.ts
    - src/hooks/useExistingTranscripts.ts
    - src/components/transcripts/SyncedTranscriptsSection.tsx
    - src/lib/recording-source-url.ts
    - src/lib/recording-ids.ts
  </read_first>
  <action>Create or extend service tests that prove `fetchSyncedCalls()` maps canonical `recordings` rows into the existing `Meeting[]` shape for Fathom, Zoom, Fireflies, Grain, Read.ai, PLAUD, YouTube, paste transcript, and manual/MCP imports. Include date filtering, organization filtering, workspace filtering through `workspace_entries`, pagination count, tag assignment loading by UUID, and share URL resolution through `resolveShareUrl()`.</action>
  <acceptance_criteria>
    - Tests fail if `fetchSyncedCalls()` queries `fathom_calls`.
    - Tests include at least seven connector/source_app values plus paste/manual.
    - Tests assert `resolveShareUrl()` is used or direct `share_url` field is absent.
    - Tests assert UUID tag assignment lookup is used.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/services/__tests__/sync-tab.service.test.ts</automated>
  </verify>
  <done>Canonical recordings behavior is pinned before migration.</done>
</task>

<task type="auto">
  <name>Task 2: Replace `fathom_calls` reads with canonical `recordings` reads</name>
  <files>src/services/sync-tab.service.ts, src/hooks/useExistingTranscripts.ts</files>
  <read_first>
    - src/services/sync-tab.service.ts
    - src/hooks/useExistingTranscripts.ts
    - src/lib/recording-source-url.ts
    - src/lib/recording-ids.ts
    - src/types/supabase.ts
  </read_first>
  <action>Update `fetchSyncedCalls()` to query canonical `recordings` scoped to the current user and `organizationId`, joined or filtered through `workspace_entries` when a workspace filter is provided. Map rows into the existing `Meeting` shape with `recording_id` as UUID string, `source_call_id`/`legacy_recording_id` only as source metadata where needed, `recording_start_time`, `recording_end_time`, `full_transcript`, `summary`, participant fields, and share URL resolved by `resolveShareUrl()`. Add `workspaceId?: string | null` to `SyncedCallsFilters` and `useExistingTranscripts` query keys.</action>
  <acceptance_criteria>
    - `sync-tab.service.ts` no longer contains `.from("fathom_calls")` in `fetchSyncedCalls()`.
    - `fetchSyncedCalls()` contains `.from("recordings")`.
    - `useExistingTranscripts` query key includes workspace ID.
    - Tag assignment loading receives recording UUIDs, not legacy Fathom IDs.
    - `npm run build` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/services/__tests__/sync-tab.service.test.ts && npm run build</automated>
  </verify>
  <done>Synced Transcripts is backed by the canonical recordings table.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve SyncTab UI flow while adding workspace-aware synced results</name>
  <files>src/components/transcripts/SyncTab.tsx, src/components/transcripts/SyncedTranscriptsSection.tsx</files>
  <read_first>
    - src/components/transcripts/SyncTab.tsx
    - src/components/transcripts/SyncedTranscriptsSection.tsx
    - src/components/transcripts/UnsyncedMeetingsSection.tsx
    - src/components/transcript-library/TranscriptTable.tsx
  </read_first>
  <action>Wire the selected workspace from `SyncTab` into `useExistingTranscripts` so Synced Transcripts reflects the workspace/date source query. Preserve the current Pane 3 Import Meetings title, source/date selectors, active jobs, unsynced meetings, and Synced Transcripts section. Do not replace the normal Home transcript table.</action>
  <acceptance_criteria>
    - `SyncTab.tsx` still renders Import Meetings workflow sections.
    - `selectedWorkspaceId` is passed to `useExistingTranscripts`.
    - `SyncedTranscriptsSection` remains a table section and does not become a separate Home replacement.
    - No visible file-upload UI is added.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/services/__tests__/sync-tab.service.test.ts && npm run build</automated>
  </verify>
  <done>Users keep the same Import Meetings workflow but now see all-source synced calls.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-04-01 | Information Disclosure | canonical recordings query | mitigate | Scope by user, organization, and workspace_entries membership where applicable. |
| T-05-04-02 | Tampering | recording ID mapping | mitigate | Use UUID helpers and avoid raw numeric coercion of CallVault recording IDs. |
| T-05-04-03 | Reliability | share URL mapping | mitigate | Use `resolveShareUrl()` so all-source rows render working source links. |
</threat_model>

<verification>
Run `npm test -- --run src/services/__tests__/sync-tab.service.test.ts && npm run build`.
</verification>

<success_criteria>
- SyncTab's synced section lists all canonical recordings by source.
- Fathom-only `fathom_calls` read is removed from the synced-list path.
- Existing UI layout is preserved.
</success_criteria>

<output>
Create `.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-04-SUMMARY.md` when done.
</output>
