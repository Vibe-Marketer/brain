---
phase: 05-connector-reliability-per-workspace-binding-unified-sync-tab
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/connectors/ConnectionsPanel.tsx
  - src/components/connectors/ConnectorManageDialog.tsx
  - src/components/connectors/setup/ConnectorSetupCluster.tsx
  - src/components/connectors/setup/ConnectorSetupClusterView.tsx
  - src/components/connectors/ConnectorPanel.tsx
  - src/components/connectors/__tests__/ConnectionsPanel.test.tsx
  - src/components/connectors/setup/__tests__/ConnectorSetupCluster.test.tsx
autonomous: true
requirements:
  - CON-02
  - CON-03
  - CON-04
must_haves:
  truths:
    - "D-01: new connector/account setup requires a workspace at connect time."
    - "D-04/D-06/D-12/D-13/D-14/D-15/D-16: Connections rows/cards always show the bound workspace name and use compact provider/account/workspace/status rows with a single Manage action in both workspace and global settings contexts."
    - "D-17: PLAUD remains visible but can use bridge-specific Manage copy/actions."
    - "D-18: Import provider cards remain setup-first and link connected/error states into Connections."
    - "D-20/D-21/D-22/D-23/D-24/D-25: UI distinguishes passive retry/rate-limit/partial-sync states from action-needed reconnect/error states."
  artifacts:
    - path: "src/components/connectors/ConnectionsPanel.tsx"
      provides: "Reusable workspace/global connector management surface"
    - path: "src/components/connectors/ConnectorManageDialog.tsx"
      provides: "Source-specific actions for sync, reconnect, workspace change, and disconnect"
---

<objective>
Create the unified Connections management surface and require workspace selection during connector setup without replacing existing import provider cards.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-CONTEXT.md
@.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-RESEARCH.md
@.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-PATTERNS.md
@src/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin Connections row and Manage UI behavior with tests</name>
  <files>src/components/connectors/__tests__/ConnectionsPanel.test.tsx, src/components/connectors/setup/__tests__/ConnectorSetupCluster.test.tsx</files>
  <read_first>
    - src/components/connectors/ConnectorPanel.tsx
    - src/components/connectors/setup/ConnectorSetupCluster.tsx
    - src/components/connectors/setup/ConnectorSetupClusterView.tsx
    - src/components/connectors/__tests__/ConnectorPanel.registry.test.ts
    - src/components/connectors/setup/__tests__/ConnectorSetupCluster.test.tsx
  </read_first>
  <action>Add tests that require a compact Connections row showing provider label, account email/name, bound workspace name, status label, and a Manage action. Add setup tests that workspace selection is required before OAuth/API-key/token setup can continue. Include cases for workspace-filtered view, global all-connector view, PLAUD `Manage bridge`, passive `Rate limited` status, partial-sync warning, and action-needed `Reconnect required` status.</action>
  <acceptance_criteria>
    - Tests fail if a connected row lacks provider, account, workspace, status, or Manage.
    - Tests fail if setup can start without a workspace for connectors that create `import_sources`.
    - Tests assert Import provider cards remain setup-first and do not expose full management controls.
    - Tests contain no positive "AI-powered" copy.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/connectors/__tests__/ConnectionsPanel.test.tsx src/components/connectors/setup/__tests__/ConnectorSetupCluster.test.tsx</automated>
  </verify>
  <done>The UI contract is executable before component implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement reusable Connections rows and Manage detail</name>
  <files>src/components/connectors/ConnectionsPanel.tsx, src/components/connectors/ConnectorManageDialog.tsx, src/components/connectors/ConnectorPanel.tsx</files>
  <read_first>
    - src/components/connectors/hooks/useConnector.ts
    - src/services/import-sources.service.ts
    - src/components/connectors/ConnectorPanel.tsx
    - src/components/connectors/primitives/ConnectorCardRow.tsx
    - src/components/connectors/primitives/DisconnectConfirmDialog.tsx
    - src/components/connectors/registry/connectorRegistry.ts
  </read_first>
  <action>Create `ConnectionsPanel` with `scope: "workspace" | "global"` and optional `workspaceId`. Render compact rows grouped by provider/account with workspace, status, and Manage. Create `ConnectorManageDialog` with source-specific actions: sync when adapter supports it, reconnect when OAuth/API-key setup supports it, PLAUD bridge management copy for PLAUD, change future landing workspace, and disconnect. Update `ConnectorPanel` connected/error states to show light status/link hints into Connections without adding management controls to setup cards.</action>
  <acceptance_criteria>
    - Workspace scope filters rows to the supplied workspace ID.
    - Global scope shows all rows returned by the service.
    - Manage dialog contains `Change future landing workspace`, `Disconnect`, and provider-appropriate reconnect/sync/bridge action.
    - Disconnect path keeps existing imported calls and only removes/clears connector credentials/source state.
    - Components use Remix icons/shadcn/local primitives already in the repo.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/connectors/__tests__/ConnectionsPanel.test.tsx && npm run build</automated>
  </verify>
  <done>The Connections management surface exists and can be reused by workspace/global settings.</done>
</task>

<task type="auto">
  <name>Task 3: Require workspace during connector setup and pass it to source creation</name>
  <files>src/components/connectors/setup/ConnectorSetupCluster.tsx, src/components/connectors/setup/ConnectorSetupClusterView.tsx, src/components/connectors/registry/types.ts, src/components/connectors/registry/adapters/*.ts</files>
  <read_first>
    - src/components/connectors/setup/ConnectorSetupCluster.tsx
    - src/components/connectors/setup/ConnectorSetupClusterView.tsx
    - src/components/connectors/registry/types.ts
    - src/components/connectors/registry/adapters/fathom.ts
    - src/components/connectors/registry/adapters/fireflies.ts
    - src/components/connectors/registry/adapters/grain.ts
    - src/components/connectors/registry/adapters/read-ai.ts
    - src/components/connectors/registry/adapters/plaud.ts
    - src/components/connectors/registry/adapters/zoom.ts
    - src/components/connectors/registry/adapters/youtube.ts
  </read_first>
  <action>Add a workspace selector to connector setup for connectors that persist an `import_sources` row. Extend adapter setup calls to carry `workspaceId` into OAuth URL creation, API-key save, token connect, and YouTube import where applicable. Do not resurrect file-upload UI. Preserve multiple-account setup flows by passing source ID and workspace ID together when both exist.</action>
  <acceptance_criteria>
    - Setup cannot call OAuth/API-key/token save with an empty workspace ID.
    - Adapter method types accept `workspaceId?: string`.
    - Existing source ID / pending source ID flows continue to compile.
    - YouTube either receives explicit workspace ID for import or is documented in code as action-only if not persisted in Connections.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/connectors/setup/__tests__/ConnectorSetupCluster.test.tsx && npm run build</automated>
  </verify>
  <done>New connector setup records the destination workspace before future imports can land.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-02-01 | Spoofing | workspace selector | mitigate | Require membership-backed workspace choices from organization context/service data. |
| T-05-02-02 | Tampering | workspace change | mitigate | Label as future landing workspace and never move historical calls. |
| T-05-02-03 | Information Disclosure | global Connections view | mitigate | Use existing user/RLS-scoped services and no direct component queries. |
</threat_model>

<verification>
Run `npm test -- --run src/components/connectors/__tests__/ConnectionsPanel.test.tsx src/components/connectors/setup/__tests__/ConnectorSetupCluster.test.tsx && npm run build`.
</verification>

<success_criteria>
- Users can see every connected account with provider, account, workspace, status, and Manage.
- Workspace setup is required at connect time.
- Import provider cards remain setup/import oriented.
</success_criteria>

<output>
Create `.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-02-SUMMARY.md` when done.
</output>
