---
phase: 05-connector-reliability-per-workspace-binding-unified-sync-tab
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
files_modified:
  - supabase/functions/_shared/connector-function-utils.ts
  - supabase/functions/_shared/oauth-url-handler.ts
  - supabase/functions/_shared/oauth-callback-handler.ts
  - supabase/functions/sync-meetings/index.ts
  - supabase/functions/zoom-sync-meetings/index.ts
  - supabase/functions/fireflies-sync-meetings/index.ts
  - supabase/functions/grain-sync-recordings/index.ts
  - supabase/functions/read-ai-sync-meetings/index.ts
  - supabase/functions/plaud-sync-recordings/index.ts
  - supabase/functions/youtube-import/index.ts
  - supabase/functions/*webhook*/index.ts
autonomous: true
requirements:
  - CON-01
  - CON-03
  - CON-04
must_haves:
  truths:
    - "D-03/D-19/D-20/D-21: transient connector failures retry silently and only action-needed failures become user-facing."
    - "D-22: token refresh failure marks the connector `Reconnect required`."
    - "D-23: rate limits back off automatically and show passive next-retry status."
    - "D-24: partial syncs keep successful imports and surface failed-call details."
    - "D-25/D-26: webhook retries use existing low-risk mechanisms; no new user-facing replay queue unless already available."
    - "D-01/D-02/D-08/D-09/D-10: sync/webhook imports use the connector's one bound future workspace and never fan out or move old calls."
  artifacts:
    - path: "supabase/functions/_shared/connector-function-utils.ts"
      provides: "Shared workspace binding and failure status helpers"
    - path: "provider sync/webhook functions"
      provides: "Bound-workspace import behavior for all seven sources"
---

<objective>
Make every connector sync/webhook path import into its bound workspace, with consistent retry/reconnect/partial-sync failure semantics.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-CONTEXT.md
@.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-RESEARCH.md
@.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-PATTERNS.md
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared helpers for bound workspace resolution and connector failure status</name>
  <files>supabase/functions/_shared/connector-function-utils.ts, supabase/functions/_shared/__tests__/connector-function-utils.test.ts</files>
  <read_first>
    - supabase/functions/_shared/connector-function-utils.ts
    - supabase/functions/_shared/connector-pipeline.ts
    - supabase/functions/_shared/oauth-encrypt.ts
    - supabase/functions/_shared/__tests__
  </read_first>
  <action>Add helpers that load an `import_sources` row by `sourceId`, resolve `workspace_id` as the connector's CallVault destination workspace, validate that the source owner can access that workspace, and fall back to the user's default workspace when the row is legacy/unbound. Add status update helpers for `retrying`, `rate_limited`, `reconnect_required`, partial-sync warnings, and exhausted webhook/error states using first-class fields if Plan 05-01 added them, otherwise structured `connection_metadata` keys with stable names. Add tests for bound workspace, default fallback, provider PLAUD metadata naming separation, refresh failure, rate limit retry metadata, and partial-sync warning state.</action>
  <acceptance_criteria>
    - Helper returns the bound `import_sources.workspace_id` before routing defaults.
    - Helper falls back to default workspace for null binding.
    - Tests prove PLAUD provider `connection_metadata.workspace_id` is not treated as CallVault destination.
    - Refresh failure status writes a stable reconnect-required marker.
    - Rate-limit status writes next retry time without marking disconnected.
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read supabase/functions/_shared/__tests__/connector-function-utils.test.ts</automated>
  </verify>
  <done>Provider functions have one shared way to resolve workspace and status.</done>
</task>

<task type="auto">
  <name>Task 2: Persist setup workspace through OAuth/API-key/token connector creation</name>
  <files>supabase/functions/_shared/oauth-url-handler.ts, supabase/functions/_shared/oauth-callback-handler.ts, supabase/functions/fathom-oauth-callback/index.ts, supabase/functions/zoom-oauth-callback/index.ts, supabase/functions/grain-connect-token/index.ts, supabase/functions/read-ai-connect-token/index.ts, supabase/functions/fireflies-save-source/index.ts, supabase/functions/plaud-connect-token/index.ts</files>
  <read_first>
    - supabase/functions/_shared/oauth-url-handler.ts
    - supabase/functions/_shared/oauth-callback-handler.ts
    - supabase/functions/fathom-oauth-callback/index.ts
    - supabase/functions/fireflies-save-source/index.ts
    - supabase/functions/plaud-connect-token/index.ts
    - supabase/functions/_shared/fireflies-credentials.ts
  </read_first>
  <action>Accept `workspace_id` / `workspaceId` in connector setup edge functions, validate membership, and write it to `import_sources.workspace_id` when creating or activating the source row. Preserve pending source ID and multiple-account flows. Keep provider-specific metadata in `connection_metadata`; do not store CallVault destination workspace in provider metadata.</action>
  <acceptance_criteria>
    - OAuth URL/callback path stores workspace on the pending/activated source row.
    - API-key/token setup paths store workspace on `import_sources.workspace_id`.
    - Invalid workspace returns 403 or friendly JSON error before storing credentials.
    - Existing account/source ID update paths keep source identity and update only future binding.
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read supabase/functions/_shared/__tests__/connector-function-utils.test.ts && npm run build</automated>
  </verify>
  <done>All setup paths can persist the future landing workspace.</done>
</task>

<task type="auto">
  <name>Task 3: Update provider sync and webhook imports to use bound workspace</name>
  <files>supabase/functions/sync-meetings/index.ts, supabase/functions/zoom-sync-meetings/index.ts, supabase/functions/fireflies-sync-meetings/index.ts, supabase/functions/grain-sync-recordings/index.ts, supabase/functions/read-ai-sync-meetings/index.ts, supabase/functions/plaud-sync-recordings/index.ts, supabase/functions/youtube-import/index.ts, supabase/functions/webhook/index.ts, supabase/functions/zoom-webhook/index.ts, supabase/functions/fireflies-webhook/index.ts, supabase/functions/grain-webhook/index.ts, supabase/functions/read-ai-webhook/index.ts</files>
  <read_first>
    - supabase/functions/_shared/connector-pipeline.ts
    - supabase/functions/_shared/connector-function-utils.ts
    - supabase/functions/sync-meetings/index.ts
    - supabase/functions/zoom-sync-meetings/index.ts
    - supabase/functions/fireflies-sync-meetings/index.ts
    - supabase/functions/grain-sync-recordings/index.ts
    - supabase/functions/read-ai-sync-meetings/index.ts
    - supabase/functions/plaud-sync-recordings/index.ts
    - supabase/functions/youtube-import/index.ts
    - supabase/functions/webhook/index.ts
  </read_first>
  <action>For each provider, resolve the import source row and pass its bound workspace into `runPipeline()` as `record.workspace_id`. Continue to honor explicit user-selected workspace for one-off selected imports only when that action is intentionally present, and persist or validate it against the connector binding as needed. Update refresh failure handling to mark reconnect-required, rate limit handling to passive retry/rate-limited metadata, partial syncs to completed-with-warning state, and webhook exhausted failures to a visible connector error state.</action>
  <acceptance_criteria>
    - Every provider sync/webhook import path has a source-row workspace resolution before `runPipeline()`.
    - Unbound source rows call the default-workspace fallback helper.
    - `runPipeline()` receives `workspace_id` for bound connector imports.
    - Refresh-token failure writes reconnect-required status.
    - Partial sync keeps successes and records failed external IDs.
    - No new user-facing webhook replay queue is introduced.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "resolve.*Workspace|workspace_id:.*workspace|runPipeline" supabase/functions/{sync-meetings,zoom-sync-meetings,fireflies-sync-meetings,grain-sync-recordings,read-ai-sync-meetings,plaud-sync-recordings,youtube-import,webhook,zoom-webhook,fireflies-webhook,grain-webhook,read-ai-webhook}/index.ts</automated>
  </verify>
  <done>Future connector imports land in the connector's bound workspace across all sources.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-03-01 | Elevation of Privilege | sync workspace routing | mitigate | Validate source owner and workspace membership before passing workspace_id to pipeline. |
| T-05-03-02 | Tampering | webhook imports | mitigate | Resolve workspace from authenticated/source-token-bound import_sources rows, not request bodies. |
| T-05-03-03 | Denial of Service | retries/rate limits | mitigate | Back off and expose passive state; avoid noisy user alerts for transient failures. |
</threat_model>

<verification>
Run provider/shared Deno tests, source guards, and `npm run build`. Credential-backed provider checks belong in Plan 05-05.
</verification>

<success_criteria>
- All seven connectors use future landing workspace binding.
- Refresh/rate-limit/partial/webhook failure states are consistent.
- Historical calls remain untouched.
</success_criteria>

<output>
Create `.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-03-SUMMARY.md` when done.
</output>
