---
phase: 01-paste-pipeline-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/ImportPage.tsx
  - src/lib/import-source-flow.ts
  - src/config/source-registry.ts
  - src/components/panes/ImportSourcePane.tsx
  - src/components/onboarding/OnboardingModal.tsx
  - src/components/connectors/registry/adapters/file-upload.ts
  - src/lib/onboarding-connectors.ts
  - src/pages/__tests__/ImportPage.connector-routing.test.ts
  - src/lib/__tests__/import-source-flow.test.ts
  - src/components/panes/__tests__/ImportSourcePane.registry.test.ts
  - src/components/onboarding/__tests__/OnboardingModal.registry.test.ts
  - src/lib/__tests__/onboarding-connectors.test.ts
  - src/components/connectors/registry/__tests__/connectorRegistry.test.ts
autonomous: true
requirements:
  - MAN-06
must_haves:
  truths:
    - "Audio/video upload entry points are no longer reachable from the import page, source panes, or onboarding per D-07, D-09, and D-10."
    - "Compatibility for historical `file-upload` records and internal source IDs is preserved unless a verified safe removal path is proven per D-09."
    - "Phase 1 does not accidentally become a navigation redesign; only import-surface copy/visibility changes land per D-12."
  artifacts:
    - path: "src/pages/ImportPage.tsx"
      provides: "Primary import route without `FileUploadDropzone` user reachability"
    - path: "src/config/source-registry.ts"
      provides: "Registry visibility rules that hide `file-upload` from user-facing source lists"
    - path: "src/components/onboarding/OnboardingModal.tsx"
      provides: "Onboarding surfaces with upload cues removed"
  key_links:
    - "SOURCE_REGISTRY visibility drives ImportPage and ImportSourcePane; hide the source in the registry before removing local UI branches."
    - "Compatibility audit informs whether `file-upload` stays internal-only rather than being deleted."
    - "ImportPage must not render `FileUploadDropzone` even if a stale `file-upload` source id is encountered from older records."
---

<objective>
Deliver MAN-06 by hiding every user-facing audio/video upload surface while preserving safe compatibility per D-07 through D-10 and keeping Phase 1 narrowly scoped per D-12.

Purpose: remove the misleading upload/transcription story from the shipped UI without breaking old `file-upload` data, adapters, or in-flight callers.
Output: registry-driven hidden upload surfaces, cleaned import routing, and onboarding/import pane surfaces that no longer expose audio/video upload.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-paste-pipeline-polish/01-CONTEXT.md
@.planning/phases/01-paste-pipeline-polish/01-RESEARCH.md
@.planning/phases/01-paste-pipeline-polish/01-UI-SPEC.md
@src/pages/ImportPage.tsx
@src/lib/import-source-flow.ts
@src/config/source-registry.ts
@src/components/panes/ImportSourcePane.tsx
@src/components/onboarding/OnboardingModal.tsx
@src/components/connectors/registry/adapters/file-upload.ts
@src/lib/onboarding-connectors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit compatibility and remove `file-upload` route reachability from the import flow</name>
  <files>src/pages/ImportPage.tsx, src/lib/import-source-flow.ts, src/config/source-registry.ts, src/components/connectors/registry/adapters/file-upload.ts, src/pages/__tests__/ImportPage.connector-routing.test.ts, src/lib/__tests__/import-source-flow.test.ts, src/components/connectors/registry/__tests__/connectorRegistry.test.ts</files>
  <read_first>
    - src/pages/ImportPage.tsx
    - src/lib/import-source-flow.ts
    - src/config/source-registry.ts
    - src/components/connectors/registry/adapters/file-upload.ts
    - src/pages/__tests__/ImportPage.connector-routing.test.ts
    - src/lib/__tests__/import-source-flow.test.ts
    - src/components/connectors/registry/__tests__/connectorRegistry.test.ts
    - .planning/phases/01-paste-pipeline-polish/01-CONTEXT.md
  </read_first>
  <action>Start with the D-09 compatibility audit: search current code/tests for `file-upload` consumers and, if the executor has Supabase read credentials available, run a non-destructive query to see whether existing `recordings`/source metadata rows still reference `file-upload`. Preserve compatibility code by default if runtime verification is unavailable. Then remove user reachability: make `file-upload` non-visible in `SOURCE_REGISTRY`, stop `ImportPage.tsx` from routing users into `FileUploadDropzone`, and keep the adapter/source id only as an internal compatibility surface for historical rows and in-flight callers. Do not introduce a replacement upload CTA, per D-10.</action>
  <acceptance_criteria>
    - `ImportPage.tsx` cannot route a normal user into `FileUploadDropzone`.
    - `file-upload` remains internal-compatible only if needed; it is not surfaced in registry-driven UI lists.
    - Any compatibility audit findings are captured in the execution summary so later cleanup does not guess about historical data.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm test -- --run src/pages/__tests__/ImportPage.connector-routing.test.ts src/lib/__tests__/import-source-flow.test.ts src/components/connectors/registry/__tests__/connectorRegistry.test.ts</automated>
  </verify>
  <done>The import route no longer exposes audio/video upload, while older `file-upload` identifiers remain safe to read if still present.</done>
</task>

<task type="auto">
  <name>Task 2: Remove upload cues from import panes and onboarding without redesigning navigation</name>
  <files>src/components/panes/ImportSourcePane.tsx, src/components/onboarding/OnboardingModal.tsx, src/lib/onboarding-connectors.ts, src/components/panes/__tests__/ImportSourcePane.registry.test.ts, src/components/onboarding/__tests__/OnboardingModal.registry.test.ts, src/lib/__tests__/onboarding-connectors.test.ts</files>
  <read_first>
    - src/components/panes/ImportSourcePane.tsx
    - src/components/onboarding/OnboardingModal.tsx
    - src/lib/onboarding-connectors.ts
    - src/components/panes/__tests__/ImportSourcePane.registry.test.ts
    - src/components/onboarding/__tests__/OnboardingModal.registry.test.ts
    - src/lib/__tests__/onboarding-connectors.test.ts
    - .planning/phases/01-paste-pipeline-polish/01-UI-SPEC.md
  </read_first>
  <action>Use the registry/onboarding helpers as the single visibility controls for D-07 and D-10. Remove audio/video upload cards, pane rows, and onboarding cues from `ImportSourcePane.tsx`, `OnboardingModal.tsx`, and `onboarding-connectors.ts` without broadening into a sidebar/nav rewrite (D-12). Keep manual transcript import visible where appropriate, but do not add any new upload or transcription language in place of the removed cues.</action>
  <acceptance_criteria>
    - Import panes and onboarding do not show File Upload or audio/video upload cues.
    - Manual transcript import remains reachable where intended, but the phase does not rename the global nav or redesign unrelated navigation.
    - The registry and onboarding tests prove hidden-source behavior rather than relying on one-off local branches.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/panes/__tests__/ImportSourcePane.registry.test.ts src/components/onboarding/__tests__/OnboardingModal.registry.test.ts src/lib/__tests__/onboarding-connectors.test.ts</automated>
  </verify>
  <done>User-facing import and onboarding surfaces no longer advertise audio/video upload, and the hidden-source behavior is locked by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source registry -> import UI | Registry metadata determines which sources users can see and launch |
| onboarding helpers -> onboarding modal | Hidden-source rules control whether upload cues reappear in the launch flow |
| historical data -> current UI | Existing `file-upload` rows may still be loaded by the app even after the source is hidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Information Disclosure | Source registry visibility | mitigate | Hide `file-upload` at the registry/flow level so user-facing surfaces cannot accidentally re-expose the deferred audio/video path. |
| T-01-11 | Tampering | Import routing | mitigate | Remove `sourceFlow === "file-upload"` reachability from `ImportPage.tsx` so stale UI state cannot launch the dropzone. |
| T-01-12 | Denial of Service | Historical `file-upload` compatibility | mitigate | Preserve adapter/source-id handling unless D-09 runtime evidence proves safe removal; do not break old rows while hiding the UI. |
| T-01-13 | Elevation of Privilege | Onboarding/import entry surfaces | mitigate | Keep hidden-source behavior centralized in registry/onboarding helpers so a later local branch cannot quietly reintroduce upload access. |
</threat_model>

<verification>
Build the app, run the import/onboarding registry tests, and confirm by source inspection that no normal import path renders `FileUploadDropzone`.
</verification>

<success_criteria>
- `FileUploadDropzone` is not reachable from `/import`, import source panes, or onboarding.
- Compatibility for existing `file-upload` data remains intact unless a verified safe cleanup path is proven.
- No replacement audio/video upload CTA is introduced.
- Phase 1 stays scoped to import surfaces rather than a broader nav redesign.
</success_criteria>

<output>
Create `.planning/phases/01-paste-pipeline-polish/01-03-SUMMARY.md` when done
</output>
