---
phase: 01-paste-pipeline-polish
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - supabase/functions/save-pasted-transcript/__tests__/save-pasted-transcript.integration.test.ts
  - supabase/functions/_shared/__tests__/loom-parser.test.ts
  - src/test/integration-setup.ts
autonomous: true
requirements:
  - MAN-04
must_haves:
  truths:
    - "Real-Supabase behavioral coverage guards auth rejection, workspace/org membership rejection, dedup behavior, supported format detection, and raw fallback per D-13 and D-14."
    - "Loom behavior is pinned by dedicated automated coverage so the existing path from D-05 cannot regress quietly."
    - "Phase 1 execution has a concrete build-plus-browser verification pass proving transcript import remains reachable while audio/video upload is not, per D-15."
  artifacts:
    - path: "supabase/functions/save-pasted-transcript/__tests__/save-pasted-transcript.integration.test.ts"
      provides: "HTTP-level real-Supabase behavioral coverage for the manual import endpoint"
    - path: "supabase/functions/_shared/__tests__/loom-parser.test.ts"
      provides: "Dedicated Loom parser regression coverage"
    - path: "src/test/integration-setup.ts"
      provides: "Environment-gated real-DB test harness behavior"
  key_links:
    - "Integration tests hit `save-pasted-transcript` over HTTP with real Supabase credentials rather than mocking the client."
    - "Loom unit coverage protects the preserved parser path while the integration suite protects the endpoint contract."
    - "Wave 2 verification depends on all Wave 1 implementation slices being present first."
---

<objective>
Deliver the Phase 1 verification slice for MAN-04 by turning D-13, D-14, and D-15 into enforced tests and a concrete execution-time verification pass.

Purpose: ensure the manual import flow is proven at the HTTP boundary against real Supabase behavior and that Loom/upload-surface regressions are caught before verification.
Output: expanded real-DB integration coverage, dedicated Loom regression tests, and an execution checklist that proves the Phase 1 UI boundary.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-paste-pipeline-polish/01-CONTEXT.md
@.planning/phases/01-paste-pipeline-polish/01-VALIDATION.md
@.planning/codebase/TESTING.md
@supabase/functions/save-pasted-transcript/index.ts
@supabase/functions/save-pasted-transcript/__tests__/save-pasted-transcript.integration.test.ts
@supabase/functions/share-call/__tests__/share-call.integration.test.ts
@supabase/functions/_shared/loom-parser.ts
@src/test/integration-setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand `save-pasted-transcript` real-Supabase integration coverage</name>
  <files>supabase/functions/save-pasted-transcript/__tests__/save-pasted-transcript.integration.test.ts, src/test/integration-setup.ts</files>
  <read_first>
    - supabase/functions/save-pasted-transcript/__tests__/save-pasted-transcript.integration.test.ts
    - supabase/functions/share-call/__tests__/share-call.integration.test.ts
    - src/test/integration-setup.ts
    - supabase/functions/save-pasted-transcript/index.ts
    - .planning/phases/01-paste-pipeline-polish/01-VALIDATION.md
  </read_first>
  <action>Upgrade `save-pasted-transcript.integration.test.ts` into the D-13/D-14 behavioral gate. It must stay HTTP-level and real-Supabase only: no mocked client, no source-artifact substitution. Cover unauthenticated requests, invalid JWT rejection, workspace/org membership rejection, dedup enforcement, supported-format detection across VTT, SRT, Otter TXT, Loom/manual URL, Markdown/raw text, and the no-data-loss raw fallback path for malformed structured input. If the current environment-gated skip behavior in `src/test/integration-setup.ts` is too weak or missing one of the documented `SUPABASE_TEST_*` prerequisites, tighten it so the suite either runs correctly with credentials or skips explicitly and safely.</action>
  <acceptance_criteria>
    - The integration suite exercises real endpoint behavior for auth, membership, dedup, supported formats, and raw fallback per D-13/D-14.
    - No test in this file mocks Supabase or bypasses the HTTP handler.
    - Missing real-DB credentials cause an explicit skip path rather than a false green.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/save-pasted-transcript/__tests__/save-pasted-transcript.integration.test.ts</automated>
  </verify>
  <done>The save endpoint has a real-Supabase behavioral test gate that would catch the core Phase 1 failure modes before release.</done>
</task>

<task type="auto">
  <name>Task 2: Pin Loom behavior with dedicated parser regression coverage</name>
  <files>supabase/functions/_shared/__tests__/loom-parser.test.ts</files>
  <read_first>
    - supabase/functions/_shared/loom-parser.ts
    - supabase/functions/_shared/__tests__/vtt-parser.test.ts
    - supabase/functions/_shared/__tests__/fathom-transcript-parser.test.ts
    - .planning/phases/01-paste-pipeline-polish/01-CONTEXT.md
  </read_first>
  <action>Create `supabase/functions/_shared/__tests__/loom-parser.test.ts` if it does not already exist, and cover the D-05 Loom preservation contract directly: `loom.com/share/` detection, share-token extraction, speaker fallback to `Unknown Speaker`, timestamp parsing, and raw fallback for weak/malformed Loom transcript text. Keep the test focused on parser behavior so it complements, rather than duplicates, the endpoint-level integration suite.</action>
  <acceptance_criteria>
    - Loom detection and share-token extraction are asserted directly.
    - Missing/weak speaker labels resolve to `Unknown Speaker` and malformed content can still fall back to raw behavior.
    - The new Loom test runs under the standard Vitest suite with no external network dependency.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/_shared/__tests__/loom-parser.test.ts</automated>
  </verify>
  <done>Loom is no longer protected only by indirect behavior; a dedicated parser test makes regressions obvious.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner -> deployed/local Edge Function | The integration suite validates the real HTTP/auth/data path instead of mocks |
| integration harness -> Supabase test project | Seeded credentials and org/workspace fixtures determine whether permission and dedup checks are meaningful |
| parser unit tests -> Loom parser | Dedicated unit coverage protects the preserved Loom branch from regressions hidden by broader endpoint tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Spoofing | Endpoint auth behavior | mitigate | Add explicit unauthenticated and invalid-JWT HTTP assertions in the real integration suite per D-13/D-14. |
| T-01-15 | Elevation of Privilege | Workspace/org membership checks | mitigate | Exercise cross-org and wrong-workspace requests against real Supabase data so permission failures return the correct rejection path. |
| T-01-16 | Tampering | Dedup and raw fallback behavior | mitigate | Assert duplicate imports and malformed structured inputs at the HTTP layer so code changes cannot bypass dedup or destroy transcript content. |
| T-01-17 | Information Disclosure | Hidden upload path regression | mitigate | Require the execution summary for this plan to include a browser walkthrough proving transcript import remains reachable while upload surfaces remain hidden per D-15. |
</threat_model>

<verification>
Run the real-Supabase integration suite and the dedicated Loom parser test. During execution, also run `npm run build` and perform a browser walkthrough of `/import` and onboarding surfaces to prove the D-15 UI boundary; record the result in the plan summary even though the walkthrough itself is not a test file edit.
</verification>

<success_criteria>
- The save endpoint has real-DB behavioral coverage for auth, membership, dedup, supported formats, and raw fallback.
- Loom behavior has explicit automated regression coverage.
- The execution checklist for Wave 2 includes build plus browser proof that audio/video upload is unreachable while transcript import still works.
</success_criteria>

<output>
Create `.planning/phases/01-paste-pipeline-polish/01-04-SUMMARY.md` when done
</output>
