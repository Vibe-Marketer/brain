---
phase: 09-lint-brand-and-documentation-hygiene
plan: 05
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
  - 09-03
  - 09-04
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - Review follow-up from full-codebase audit 2026-06-09

must_haves:
  truths:
    - "Root CLAUDE.md Hard Constraints section documents the lint:docs grep gate and commands"
    - "npm run lint warning count is materially below 237 (target: under 170)"
    - "npm run lint:docs exits 0 with no WARNING lines"
    - "npm run type-check exits 0"
    - "npm run build passes with built in X.XXs"
    - "No new errors introduced versus the pre-phase baseline"
  artifacts:
    - path: "CLAUDE.md"
      provides: "lint:docs command documented in Hard Constraints section"
    - path: ".planning/phases/09-lint-brand-and-documentation-hygiene/09-05-SUMMARY.md"
      provides: "Phase completion record with final warning count"
  key_links:
    - from: "CLAUDE.md Hard Constraints"
      to: "npm run lint:docs"
      via: "documented command contributors can run"
      pattern: "lint:docs"
---

<objective>
Document the doc lint guardrail in root CLAUDE.md, then run the full verification suite to confirm all four prior plans landed correctly. Record the final warning count as proof of material reduction.

Purpose: Completing the guardrail loop (D-09): the grep gate exists as a script (Plan 02), now it needs to be discoverable from CLAUDE.md so contributors know to run it. Final gate confirms the phase achieves its success criteria.
Output: CLAUDE.md updated with guardrail doc; phase verification passing.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-lint-brand-and-documentation-hygiene/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document lint:docs guardrail in root CLAUDE.md Hard Constraints section (per D-09)</name>
  <read_first>
    CLAUDE.md (full file — find the Hard Constraints table to locate the insertion point)
  </read_first>
  <action>
Read the root CLAUDE.md. Locate the `## HARD CONSTRAINTS` section (it contains a table with AI-02, FOUND-09, Icons, No AI label, Vibe orange rows).

Add a new row to that constraints table for the doc lint guardrail. Add it after the existing rows:

| `Docs guardrail` | Run `npm run lint:docs` before committing changes to `docs/`. Catches: `lucide-react` imports, positive `framer-motion` usage, `AI-powered` product descriptors. Excludes `docs/archive/`. Exits 0 (warning, not error). |

Then, below the Hard Constraints table (or in a new "Verification Commands" subsection if one exists), add:

```
### Doc Lint Gate

To check active docs for forbidden patterns:

\`\`\`bash
npm run lint:docs
\`\`\`

Patterns detected:
- `lucide-react` imports in `docs/**/*.md` (excluding `docs/archive/`)
- `framer-motion` in positive/recommending context in docs
- `"AI-powered"` as a product descriptor (exclusions: "not AI-powered", "AI-ready, not")

This is a warning-only gate. Violations are printed; script exits 0. Fix before committing to active docs.
```

Use the Edit tool for surgical insertion. Do not reformat or rewrite any other section of CLAUDE.md.
  </action>
  <verify>
    <automated>grep -n "lint:docs" CLAUDE.md | wc -l</automated>
  </verify>
  <done>
    - `grep -n "lint:docs" CLAUDE.md` returns at least 1 line
    - The Hard Constraints table still renders as a valid markdown table (no broken rows)
    - CLAUDE.md is otherwise unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Full phase verification — warning count, build, type-check, doc gates</name>
  <read_first>
    package.json (scripts block — confirm lint:docs script is present from Plan 02)
  </read_first>
  <action>
Run the complete verification suite for Phase 9. Record the actual output of each command in the SUMMARY.md.

1. Final lint warning count:
   `npm run lint 2>&1 | tail -1`
   Expected: fewer than 170 warnings (down from 237 baseline; ~70+ removed by Plans 01+03+04)

2. No regressions — type-check:
   `npm run type-check`
   Expected: exit 0, no output

3. No regressions — build:
   `npm run build 2>&1 | grep "built in\|error" | tail -3`
   Expected: "✓ built in X.XXs" with no error lines

4. Active doc violations cleared:
   `grep -rn "lucide-react" docs/ --include="*.md" | grep -v "docs/archive/"`
   Expected: no output

   `grep -rn "AI-powered" docs/ --include="*.md" | grep -Ev "docs/archive/|not AI-powered|AI-ready, not"`
   Expected: no output

5. lint:docs clean:
   `npm run lint:docs`
   Expected: "lint:docs complete" with no WARNING lines

If any check fails, diagnose and fix the regression before committing. The SUMMARY.md must record the actual final warning count.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -1 && npm run type-check && npm run build 2>&1 | grep "built in" | tail -1 && npm run lint:docs</automated>
  </verify>
  <done>
    - `npm run lint` warning count is materially below 237 (under 170 is the target)
    - `npm run type-check` exits 0 with no output
    - `npm run build` reports "built in X.XXs" with no errors
    - `npm run lint:docs` exits 0 with "lint:docs complete" and no WARNING lines
    - No active doc file contains a lucide-react import or positive AI-powered descriptor
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md → future contributors | CLAUDE.md is authoritative; incorrect commands documented here will be followed blindly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Information Disclosure | lint:docs grep patterns | accept | Patterns are heuristic; novel phrasings may slip through. Document limitation in the CLAUDE.md addition. |
| T-09-05-02 | Tampering | CLAUDE.md Hard Constraints table | mitigate | Use Edit tool for surgical insertion only; verify table still renders after edit |
</threat_model>

<verification>
All five verification commands pass:
1. `npm run lint 2>&1 | tail -1` — under 170 warnings
2. `npm run type-check` — exit 0
3. `npm run build 2>&1 | grep "built in"` — one passing line
4. `grep -rn "lucide-react" docs/ --include="*.md" | grep -v "docs/archive/"` — no output
5. `npm run lint:docs` — "lint:docs complete" with no WARNING lines
</verification>

<success_criteria>
Phase 9 complete: lint warnings reduced materially from 237 baseline, active-doc violations removed, lint:docs guardrail documented in CLAUDE.md and runnable via npm, build and type-check unchanged.
</success_criteria>

<output>
Create `.planning/phases/09-lint-brand-and-documentation-hygiene/09-05-SUMMARY.md` when done
</output>
