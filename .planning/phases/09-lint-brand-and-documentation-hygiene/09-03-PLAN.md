---
phase: 09-lint-brand-and-documentation-hygiene
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/components/panes/WorkspaceSidebarPane.tsx
  - src/pages/TranscriptsNew.tsx
  - src/pages/OrganizationPage.tsx
  - src/components/dialogs/WorkspaceInviteDialog.tsx
  - src/pages/WorkspaceJoin.tsx
  - src/components/sharing/OrgChartView.tsx
  - src/components/settings/WorkspaceManagement.tsx
  - src/components/transcripts/TranscriptsTab.tsx
  - src/hooks/useFolders.ts
  - src/components/import/PasteTranscriptModal.tsx
  - src/components/layout/AppShell.tsx
  - src/components/contacts/ReengagementEmailModal.tsx
  - src/components/header/OrganizationSwitcher.tsx
  - src/components/import/ImportProgress.tsx
  - src/components/debug-panel/DebugPanel.tsx
  - src/components/panels/SettingHelpPanel.tsx
  - src/components/panels/TagDetailPanel.tsx
  - src/components/panes/AnalyticsDetailPane.tsx
  - src/components/panes/SortingDetailPane.tsx
  - src/components/settings/MCPTab.tsx
  - src/components/settings/OrganizationsTab.tsx
  - src/components/tags/FoldersTab.tsx
  - src/components/tags/RulesTab.tsx
  - src/components/youtube/YouTubeVideoDetailModal.tsx
  - src/hooks/useContactFolders.ts
  - src/hooks/useOrganizationContext.ts
  - src/hooks/usePersonalFolders.ts
  - src/hooks/usePersonalTags.ts
  - src/hooks/useSyncTabState.ts
  - src/hooks/useWorkspaceMemberMutations.ts
  - src/hooks/useWorkspaceMutations.ts
  - src/lib/folder-icons.ts
  - src/pages/Analytics.tsx
  - src/pages/OrganizationJoin.tsx
  - src/pages/SetupWizard.tsx
  - src/components/AssignFolderDialog.tsx
autonomous: true
requirements:
  - Review follow-up from full-codebase audit 2026-06-09

must_haves:
  truths:
    - "All src/ unused-vars warnings are eliminated by renaming to _prefix"
    - "No variable that is actually used is renamed"
    - "npm run lint warning count drops by ~65 from post-plan-01 baseline"
    - "npm run type-check and npm run build still pass"
  artifacts:
    - path: "src/components/panes/WorkspaceSidebarPane.tsx"
      provides: "9 unused vars prefixed with _"
    - path: "src/pages/TranscriptsNew.tsx"
      provides: "9 unused vars prefixed with _"
  key_links:
    - from: "eslint.config.js argsIgnorePattern"
      to: "renamed _vars"
      via: "^_ pattern suppresses the warning"
      pattern: "argsIgnorePattern.*\\^_"
---

<objective>
Rename all unused variables and imports to the `_prefix` convention in the identified src/ files. This is the highest-ROI manual lint fix: ~65 warnings eliminated by simple identifier renames, zero logic change.

Purpose: Reduce warning count materially (per D-04 success threshold). The `argsIgnorePattern: "^_"` and `varsIgnorePattern: "^_"` rules in eslint.config.js already suppress any identifier starting with `_` — these renames are the established convention, not a workaround.
Output: ~35 src/ files with unused vars renamed to `_prefix`.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-lint-brand-and-documentation-hygiene/09-CONTEXT.md
@.planning/phases/09-lint-brand-and-documentation-hygiene/09-RESEARCH.md
</context>

<interfaces>
From eslint.config.js — active suppression rules:
  "@typescript-eslint/no-unused-vars": ["warn", {
    "argsIgnorePattern": "^_",
    "varsIgnorePattern": "^_"
  }]

Pattern: rename `varName` to `_varName` at the declaration site only. Do not rename usage sites (there are none — these are unused).

For destructured callback arguments (e.g. `(value, index) => ...` where `index` is unused):
  Change to `(value, _index) => ...`

For named imports that are unused:
  Change `import { Foo, Bar }` to `import { _Foo, Bar }` — OR remove the import entirely if it is the ONLY import from that module and the module isn't needed. Prefer removal over prefixing for unused imports.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rename unused vars in highest-ROI src/ files (18 files, ~55 warnings)</name>
  <read_first>
    src/components/panes/WorkspaceSidebarPane.tsx
    src/pages/TranscriptsNew.tsx
    src/pages/OrganizationPage.tsx
    src/components/dialogs/WorkspaceInviteDialog.tsx
    src/pages/WorkspaceJoin.tsx
    src/components/sharing/OrgChartView.tsx
    src/components/settings/WorkspaceManagement.tsx
    src/components/transcripts/TranscriptsTab.tsx
    src/hooks/useFolders.ts
    src/components/import/PasteTranscriptModal.tsx
    src/components/layout/AppShell.tsx
    src/components/contacts/ReengagementEmailModal.tsx
    src/components/header/OrganizationSwitcher.tsx
    src/components/import/ImportProgress.tsx
    src/components/debug-panel/DebugPanel.tsx
    src/components/panels/SettingHelpPanel.tsx
    src/components/panels/TagDetailPanel.tsx
    src/components/panes/AnalyticsDetailPane.tsx
  </read_first>
  <action>
For each file, read it first, then rename unused identifiers to `_prefix`. The exact targets from RESEARCH.md audit (verified 2026-06-10):

`src/components/panes/WorkspaceSidebarPane.tsx`:
  Rename: WorkspaceMemberPanel, OrganizationMemberPanel, queryKeys, RiMoreLine, RiShieldUserLine, WorkspaceRole, queryClient, error, personalFoldersLoading
  All 9 are imports or destructured values — rename at declaration/import site. For Remix icon imports that are the sole unused import from @remixicon/react, remove from the import list rather than prefixing.

`src/pages/TranscriptsNew.tsx`:
  Rename: TabsList, TabsTrigger, Button, useCreateFolder, usePersonalTags, foldersLoading, hiddenFolders, toggleHidden, totalCount
  These are unused imports/destructures — remove from import statements where possible; rename to _prefix where the binding is a destructure from a hook call.

`src/pages/OrganizationPage.tsx`:
  Rename: RiGroupLine, RiInformationLine, CardHeader, CardTitle, useDeleteOrganization (5 unused imports — remove from import lists)

`src/components/dialogs/WorkspaceInviteDialog.tsx`:
  Rename: RiCloseLine, activeTab, setActiveTab, expiresInDays

`src/pages/WorkspaceJoin.tsx`:
  Rename: RiSafeLine, RiCheckLine, RiArrowLeftLine, getErrorToastMessage

`src/components/sharing/OrgChartView.tsx`:
  Rename: RiUserSettingsLine, TeamMembershipWithUser as _TeamMembershipWithUser (type import), index callback arg → _index

`src/components/settings/WorkspaceManagement.tsx`:
  Rename: RiArrowRightLine, RiInformationLine, navigate

`src/components/transcripts/TranscriptsTab.tsx`:
  Rename: ErrorBoundary, BulkActionToolbarEnhanced, useWorkspaces

`src/hooks/useFolders.ts`:
  Rename: renameFolder, workspaceId callback args (×2) → _workspaceId

`src/components/import/PasteTranscriptModal.tsx`:
  Rename: destinationProviderName → _destinationProviderName

`src/components/layout/AppShell.tsx`:
  Rename: handleLibraryToggle → _handleLibraryToggle (or remove if not passed to any prop)

`src/components/contacts/ReengagementEmailModal.tsx`:
  Rename: cn → remove from import (it's imported but not called)

`src/components/header/OrganizationSwitcher.tsx`:
  Rename: toast, WorkspaceWithMembership

`src/components/import/ImportProgress.tsx`:
  Rename: errorHappenedBefore → _errorHappenedBefore, Icon callback arg → _Icon

`src/components/debug-panel/DebugPanel.tsx`:
  Rename: DebugDump → remove from import

`src/components/panels/SettingHelpPanel.tsx`:
  Rename: RiRobot2Line → remove from import

`src/components/panels/TagDetailPanel.tsx`:
  Rename: Tag (type import) → _Tag or remove if unused type

`src/components/panes/AnalyticsDetailPane.tsx`:
  Rename: RiArrowLeftLine, AnalyticsPlaceholder

RULE: Never rename a variable that appears in JSX, a return statement, a function call, or any expression after its declaration. These are confirmed unused from the lint output — but READ each file before editing to catch any false positives from the audit.

After editing all 18 files, run a scoped lint check:
  `npm run lint -- src/components/ src/pages/ src/hooks/ src/lib/ 2>&1 | grep "no-unused-vars\|warning" | tail -20`
  </action>
  <verify>
    <automated>npm run lint -- src/components/ src/pages/ src/hooks/ src/lib/ 2>&1 | grep "no-unused-vars" | wc -l</automated>
  </verify>
  <done>
    - `grep "no-unused-vars"` count in the scoped run drops significantly from the ~55 warnings these 18 files contributed
    - `npm run type-check` still exits 0 (run after edits)
    - No file has a renamed variable that appears in an expression after its declaration site
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename remaining unused vars in src/ hooks, pages, and test files (~18 files, ~10 warnings)</name>
  <read_first>
    src/components/panes/SortingDetailPane.tsx
    src/components/settings/MCPTab.tsx
    src/components/settings/OrganizationsTab.tsx
    src/components/tags/FoldersTab.tsx
    src/components/tags/RulesTab.tsx
    src/components/youtube/YouTubeVideoDetailModal.tsx
    src/hooks/useContactFolders.ts
    src/hooks/useOrganizationContext.ts
    src/hooks/usePersonalFolders.ts
    src/hooks/usePersonalTags.ts
    src/hooks/useSyncTabState.ts
    src/hooks/useWorkspaceMemberMutations.ts
    src/hooks/useWorkspaceMutations.ts
    src/lib/folder-icons.ts
    src/pages/OrganizationJoin.tsx
    src/components/AssignFolderDialog.tsx
    cloudflare/api-proxy/__tests__/worker.test.ts
    src/components/dialogs/__tests__/CreateWorkspaceDialog.phase25.test.tsx
  </read_first>
  <action>
Continue the rename pass on the remaining files from the RESEARCH.md audit:

`src/components/panes/SortingDetailPane.tsx`: Rename RiArrowLeftLine
`src/components/settings/MCPTab.tsx`: Rename UpgradeButton
`src/components/settings/OrganizationsTab.tsx`: Rename orgRole → _orgRole (destructured hook return)
`src/components/tags/FoldersTab.tsx`: Rename color, icon, description callback args → _color, _icon, _description
`src/components/tags/RulesTab.tsx`: Rename Tag (type import)
`src/components/youtube/YouTubeVideoDetailModal.tsx`: Rename workspaceId callback arg → _workspaceId
`src/hooks/useContactFolders.ts`: Rename organizationId callback arg → _organizationId
`src/hooks/useOrganizationContext.ts`: Rename useEffect (unused import — remove from import statement)
`src/hooks/usePersonalFolders.ts`: Rename moveCallToPersonalFolder → _moveCallToPersonalFolder
`src/hooks/usePersonalTags.ts`: Rename updatePersonalTag → _updatePersonalTag
`src/hooks/useSyncTabState.ts`: Rename realtimeConnected → _realtimeConnected
`src/hooks/useWorkspaceMemberMutations.ts`: Rename workspaceId callback arg → _workspaceId
`src/hooks/useWorkspaceMutations.ts`: Rename WorkspaceInsert (type import), variables callback arg → _variables
`src/lib/folder-icons.ts`: Rename value callback arg → _value
`src/pages/OrganizationJoin.tsx`: Rename RiCheckLine, switchOrganization, organizations
`src/components/AssignFolderDialog.tsx`: Rename resolved → _resolved
`cloudflare/api-proxy/__tests__/worker.test.ts`: Rename vi, beforeEach (unused test imports — remove from import list)
`src/components/dialogs/__tests__/CreateWorkspaceDialog.phase25.test.tsx`: Rename React → remove (unused default import)

Additionally check and fix these test files if they appear in the lint output:
  - `src/components/settings/__tests__/MCPTab.permissions.test.tsx`: within → _within
  - `src/components/tags/__tests__/FoldersTab.integration.test.tsx`: within, user → _within, _user
  - `src/hooks/__tests__/useWorkspaceMutations.workspaceOrder.test.ts`: updateChain → _updateChain
  - `src/services/__tests__/tags.service.test.ts`: tagsService → _tagsService
  - `src/stores/__tests__/panelStore.test.ts`: goBack → _goBack

After all edits, run full lint to confirm total warning count:
  `npm run lint 2>&1 | tail -1`
  Target: below 170 warnings (from ~237 baseline; ~70 removed across plans 01 + 03).
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -1 && npm run type-check && npm run build 2>&1 | grep "built in" | tail -1</automated>
  </verify>
  <done>
    - `npm run lint 2>&1 | tail -1` shows fewer than 170 warnings
    - `npm run type-check` exits 0
    - `npm run build` reports "built in X.XXs" with no errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declaration rename → downstream uses | Risk: rename a variable that IS used somewhere (false negative in lint audit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | unused-var rename pass | mitigate | Read each file before editing; never rename a variable that appears after its declaration site; run type-check after every 5-file batch |
</threat_model>

<verification>
- `npm run lint 2>&1 | tail -1` shows fewer than 170 warnings
- `npm run type-check` exits 0 with no output
- `npm run build 2>&1 | grep "built in"` returns one line with no errors
</verification>

<success_criteria>
All confirmed unused-var warnings in src/ and cloudflare/ eliminated. Warning count drops ~65 from post-plan-01 baseline. Build and type-check unchanged.
</success_criteria>

<output>
Create `.planning/phases/09-lint-brand-and-documentation-hygiene/09-03-SUMMARY.md` when done
</output>
