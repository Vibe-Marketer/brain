---
phase: 09-lint-brand-and-documentation-hygiene
plan: 04
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-03
files_modified:
  - src/components/SmartExportDialog.tsx
  - src/components/import/PasteTranscriptModal.tsx
  - src/pages/OrganizationPage.tsx
  - src/components/contacts/ReengagementEmailModal.tsx
  - src/hooks/useSyncTabState.ts
  - src/hooks/useCategorySync.ts
  - src/hooks/useUserPreferences.ts
  - src/pages/Analytics.tsx
  - src/pages/SetupWizard.tsx
  - src/components/connectors/ConnectionsPanel.tsx
autonomous: true
requirements:
  - Review follow-up from full-codebase audit 2026-06-09

must_haves:
  truths:
    - "Safe hook dep warnings resolved by adding missing deps or removing unnecessary ones"
    - "Risky hook dep warnings suppressed with eslint-disable-next-line and an explanatory comment"
    - "No semantic behavior changes introduced — hook effects still fire at the same times"
    - "npm run type-check still exits 0"
    - "npm run build still passes"
  artifacts:
    - path: "src/hooks/useSyncTabState.ts"
      provides: "Realtime subscription effects suppressed with explanatory comments"
    - path: "src/components/SmartExportDialog.tsx"
      provides: "useMemo dep array includes availableWorkspaces.length and excludedWorkspaces.length"
  key_links:
    - from: "useSyncTabState.ts suppress comment"
      to: "realtime subscription lifecycle"
      via: "eslint-disable-next-line with explanation prevents re-subscription on job state changes"
      pattern: "eslint-disable-next-line react-hooks/exhaustive-deps"
---

<objective>
Fix the `react-hooks/exhaustive-deps` warnings using the safe-vs-risky split from RESEARCH.md. Safe fixes add or remove dependencies where there is no semantic change risk. Risky fixes (realtime subscription effects, inline-function patterns) get `// eslint-disable-next-line react-hooks/exhaustive-deps` with a mandatory explanatory comment explaining WHY the dep is intentionally omitted.

Purpose: Eliminate the plausible-runtime-impact hook dep warnings per D-05 without introducing re-render loops or realtime re-subscription bugs.
Output: ~10 hook dep warnings resolved across 10 files.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-lint-brand-and-documentation-hygiene/09-CONTEXT.md
@.planning/phases/09-lint-brand-and-documentation-hygiene/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply safe hook dep fixes (add/remove deps where no semantic risk)</name>
  <read_first>
    src/components/SmartExportDialog.tsx (line 187 — useMemo missing .length deps)
    src/components/import/PasteTranscriptModal.tsx (line 439 — useMemo missing optional chain dep)
    src/pages/OrganizationPage.tsx (line 105 — useEffect has unnecessary dep)
    src/components/contacts/ReengagementEmailModal.tsx (line 69 — useEffect missing contact dep)
    src/components/connectors/ConnectionsPanel.tsx (line 45 — useMemo accounts expression)
    src/hooks/useSyncTabState.ts (line 186 — useCallback missing queryClient)
  </read_first>
  <action>
Apply safe fixes only — these changes add or remove deps with no semantic behavior change:

Fix 1 — `src/components/SmartExportDialog.tsx` line 187:
  Find the `useMemo` call. Add `availableWorkspaces.length` and `excludedWorkspaces.length` to its dependency array.
  Rationale: `.length` on an array is a scalar value. Adding it means the memo recomputes when the array length changes — correct behavior. No re-render loop risk.

Fix 2 — `src/components/import/PasteTranscriptModal.tsx` (useMemo near line 439):
  Add `sourceLinkMetadata?.author_name` to the dependency array of the identified useMemo.
  Rationale: Optional chain value is a stable scalar from an object reference. Correct to recompute when it changes.

Fix 3 — `src/pages/OrganizationPage.tsx` line 105:
  Find the useEffect that ESLint flags as having an *unnecessary* dep `activeOrganization.name`. Remove that dep from the dep array.
  Rationale: ESLint explicitly says to remove it — the effect does not consume this value.

Fix 4 — `src/components/contacts/ReengagementEmailModal.tsx` line 69:
  Read the useEffect body. If the effect body references `contact` (e.g. initializes form state from contact), add `contact` to the dep array. If the intent is clearly run-once initialization, suppress with comment instead.

Fix 5 — `src/components/connectors/ConnectionsPanel.tsx` line 45:
  Find the useMemo where `accounts` logical expression changes on every render. Move the `accounts` initialization inside the useMemo callback body so the dependency is captured correctly rather than evaluated outside.

Fix 6 — `src/hooks/useSyncTabState.ts` line 186:
  Find the useCallback missing `queryClient`. `queryClient` from TanStack Query's `useQueryClient()` is a stable singleton. Add it to the dep array.
  Rationale: Safe add. TanStack Query guarantees a stable queryClient reference — adding it to deps won't cause callback churn.

After each fix, run: `npm run type-check` to confirm no type regressions.
  </action>
  <verify>
    <automated>npm run lint -- src/components/SmartExportDialog.tsx src/components/import/PasteTranscriptModal.tsx src/pages/OrganizationPage.tsx src/components/contacts/ReengagementEmailModal.tsx src/components/connectors/ConnectionsPanel.tsx src/hooks/useSyncTabState.ts 2>&1 | grep "exhaustive-deps"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "exhaustive-deps"` in the scoped lint run returns zero lines for the 6 fixed files
    - `npm run type-check` exits 0 with no output
  </acceptance_criteria>
  <done>
    - `grep "exhaustive-deps"` in the scoped lint run returns zero lines for the 6 fixed files
    - `npm run type-check` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Suppress risky hook dep warnings with explanatory comments</name>
  <read_first>
    src/hooks/useSyncTabState.ts (lines 330, 332, 339 — realtime subscription effects)
    src/hooks/useCategorySync.ts (lines 19, 175 — inline loadTags, activeOrgId callback)
    src/hooks/useUserPreferences.ts (line 99 — inline loadPreferences pattern)
    src/pages/Analytics.tsx (line 44 — selectedCategory useEffect)
    src/pages/SetupWizard.tsx (line 147 — connectedMeta, connectedSources polling effect)
  </read_first>
  <action>
For each risky warning, add `// eslint-disable-next-line react-hooks/exhaustive-deps` on the line immediately before the dep array closing bracket, followed by a comment on the same line or the next line explaining the specific reason.

`src/hooks/useSyncTabState.ts` line ~330 (completedJobTimeoutsRef.current stale ref):
  Add above the dep array: `// eslint-disable-next-line react-hooks/exhaustive-deps`
  Add inline comment: `// completedJobTimeoutsRef.current intentionally read at cleanup time; adding to deps would re-subscribe on every timeout map change`

`src/hooks/useSyncTabState.ts` lines ~332 (realtime subscription — missing handleJobCompleted, recentlyCompletedJobs, removeNewlySyncedMeetings):
  Add: `// eslint-disable-next-line react-hooks/exhaustive-deps`
  Comment: `// Realtime subscription — deps intentionally omitted to prevent re-subscription on job state changes`

`src/hooks/useSyncTabState.ts` line ~339 (missing loadTags inline function):
  Add: `// eslint-disable-next-line react-hooks/exhaustive-deps`
  Comment: `// loadTags is defined inline and not memoized; adding it would re-run on every render`

`src/hooks/useCategorySync.ts` line ~19 (missing loadTags):
  Same pattern as above. Comment: `// loadTags defined inline; not memoized — adding to deps causes re-run on every render`

`src/hooks/useCategorySync.ts` line ~175 (useCallback missing activeOrgId):
  Read the context hook to confirm stability. If `activeOrgId` is returned from a memoized context selector, add it (safe). If it's from a plain context read that might not be stable, suppress with: `// activeOrgId from org context — suppressed pending context stability audit`

`src/hooks/useUserPreferences.ts` line ~99 (missing loadPreferences inline):
  Add: `// eslint-disable-next-line react-hooks/exhaustive-deps`
  Comment: `// loadPreferences defined inline; adding to deps causes re-run on every render`

`src/pages/Analytics.tsx` line ~44 (missing selectedCategory):
  Read the effect body. If selectedCategory drives the effect's data fetch, add it. If intentionally run-once, suppress with: `// Intentionally run once on mount; selectedCategory filtering handled downstream`

`src/pages/SetupWizard.tsx` line ~147 (missing connectedMeta, connectedSources):
  Default to suppress — these are polling/initialization effects. Comment: `// Initialization effect — adding connectedMeta/connectedSources would cause re-run loop as effect populates them`

After all suppressions, run full lint to check exhaustive-deps count is near zero:
  `npm run lint 2>&1 | grep "exhaustive-deps" | wc -l`
  </action>
  <verify>
    <automated>npm run lint 2>&1 | grep "exhaustive-deps" | wc -l && npm run type-check && npm run build 2>&1 | grep "built in" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint 2>&1 | grep "exhaustive-deps" | wc -l` returns 0 or close to 0
    - `npm run type-check` exits 0 with no output
    - `npm run build 2>&1 | grep "built in"` returns one line containing "built in X.XXs" with no error lines
    - Every `eslint-disable-next-line react-hooks/exhaustive-deps` has an adjacent explanatory comment naming the specific reason
  </acceptance_criteria>
  <done>
    - `grep "exhaustive-deps" | wc -l` returns 0 or close to 0 (all identified warnings resolved)
    - `npm run type-check` exits 0
    - `npm run build` reports "built in X.XXs" with no errors
    - Every suppression has an accompanying explanatory comment on the same or next line
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook dep array → effect re-run logic | Wrong dep additions can cause re-render loops; wrong suppressions can cause stale closures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering | safe dep additions | mitigate | Only add scalar values (.length, stable singletons); run type-check after each file |
| T-09-04-02 | Tampering | realtime subscription suppression | mitigate | Explanatory comment is mandatory — no bare suppresses allowed; comment must name the specific re-subscription risk |
</threat_model>

<verification>
- `npm run lint 2>&1 | grep "exhaustive-deps" | wc -l` returns 0 or near 0
- `npm run type-check` exits 0
- `npm run build 2>&1 | grep "built in"` returns one passing line
- Every `eslint-disable-next-line react-hooks/exhaustive-deps` has an adjacent comment explaining the rationale
</verification>

<success_criteria>
All identified react-hooks/exhaustive-deps warnings resolved — safe ones by adding deps, risky ones by suppression with explanatory comments. No behavior changes introduced.
</success_criteria>

<output>
Create `.planning/phases/09-lint-brand-and-documentation-hygiene/09-04-SUMMARY.md` when done
</output>
