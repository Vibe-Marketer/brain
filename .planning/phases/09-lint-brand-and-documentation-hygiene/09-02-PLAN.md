---
phase: 09-lint-brand-and-documentation-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/design/BUTTON_VARIANTS.md
  - docs/help/export-system.md
  - package.json
autonomous: true
requirements:
  - Review follow-up from full-codebase audit 2026-06-09

must_haves:
  truths:
    - "BUTTON_VARIANTS.md no longer contains a lucide-react import example"
    - "export-system.md no longer uses positive AI-powered language"
    - "npm run lint:docs runs and exits with no WARNING lines"
    - "package.json has a lint:docs script"
  artifacts:
    - path: "docs/design/BUTTON_VARIANTS.md"
      provides: "Remix Icon import example replacing lucide-react at line 268"
    - path: "docs/help/export-system.md"
      provides: "AI-ready framing at line 66 replacing positive AI-powered descriptor"
    - path: "package.json"
      provides: "lint:docs script in scripts block"
  key_links:
    - from: "package.json lint:docs"
      to: "docs/ active markdown"
      via: "grep patterns excluding docs/archive/"
      pattern: "lucide-react|AI-powered"
---

<objective>
Fix the two confirmed active-doc violations (lucide-react import example, positive AI-powered language) and add the `lint:docs` npm script that guards against future drift. This plan covers all three doc-related decisions: D-07 (fix the two files), D-09/D-10 (guardrail as npm script in package.json).

Purpose: Remove active examples of forbidden patterns from living docs so contributors don't copy them. Add a lightweight self-service check.
Output: Two doc files fixed, one npm script added.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-lint-brand-and-documentation-hygiene/09-CONTEXT.md
@.planning/phases/09-lint-brand-and-documentation-hygiene/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix BUTTON_VARIANTS.md and export-system.md (per D-07)</name>
  <read_first>
    docs/design/BUTTON_VARIANTS.md (line 268 — current lucide-react import example)
    docs/help/export-system.md (line 66 — current positive AI-powered descriptor)
  </read_first>
  <action>
Fix exactly two lines. Do not change anything else in either file.

Fix 1 — `docs/design/BUTTON_VARIANTS.md` line 268:
Replace the lucide-react import line:
  `import { Eye, Pencil, Download, X, ChevronLeft, ChevronRight, Search, Users } from "lucide-react";`
With the @remixicon/react equivalent (per D-07 and RESEARCH.md verified icon mappings):
  `import { RiEyeLine, RiPencilLine, RiDownloadLine, RiCloseLine, RiArrowLeftSLine, RiArrowRightSLine, RiSearchLine, RiUserLine } from "@remixicon/react";`

Fix 2 — `docs/help/export-system.md` line 66:
Replace the positive AI-powered descriptor:
  `For AI-powered summaries and insights:`
With the AI-ready brand framing:
  `For AI-ready export with summaries and action items:`

Do NOT change `docs/design/brand-guidelines-v4.4.md`, `docs/product-overview.md`, `docs/brand-guidelines-changelog.md`, or anything in `docs/archive/` — these are correct as-is per D-08.
  </action>
  <verify>
    <automated>grep -n "lucide-react" docs/design/BUTTON_VARIANTS.md | grep -v "docs/archive/" && echo "FAIL: lucide still present" || echo "OK: lucide removed"; grep -n "AI-powered" docs/help/export-system.md && echo "FAIL: AI-powered still present" || echo "OK: AI-powered removed"</automated>
  </verify>
  <done>
    - `grep -n "lucide-react" docs/design/BUTTON_VARIANTS.md` returns no output
    - `grep -n "AI-powered" docs/help/export-system.md` returns no output
    - Both files changed at exactly one line each (verify with git diff)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add lint:docs npm script to package.json (per D-10)</name>
  <read_first>
    package.json (full scripts block — confirm current scripts, confirm no lint:docs exists yet)
  </read_first>
  <action>
Add a `"lint:docs"` script to the `"scripts"` block in `package.json`. Place it after the existing `"lint"` entry.

The script value (per D-10 and RESEARCH.md — minimal inline bash, warning-only, no new dependencies):
`"lint:docs": "echo '--- lint:docs ---' && (grep -rn 'lucide-react' docs/ --include='*.md' | grep -v 'docs/archive/' && echo 'WARNING: lucide-react found in active docs' || true) && (grep -rn 'framer-motion' docs/ --include='*.md' | grep -Ev 'docs/archive/|NEVER use|do not use|forbidden' && echo 'WARNING: framer-motion positive use found in active docs' || true) && (grep -rn 'AI-powered' docs/ --include='*.md' | grep -Ev 'docs/archive/|not AI-powered|AI-ready, not' && echo 'WARNING: positive AI-powered found in active docs' || true) && echo 'lint:docs complete'"`

Per D-10: exits 0 always (warning, not error). Per D-11: gates lucide-react imports, framer-motion in positive context, and "AI-powered" as product descriptor. Per D-12: no CI wiring.

Use the Edit tool to insert one line in the scripts block. Do not reformat the rest of package.json.
  </action>
  <verify>
    <automated>npm run lint:docs</automated>
  </verify>
  <done>
    - `npm run lint:docs` runs without shell error
    - Output includes "lint:docs complete" with no "WARNING" lines (post Task 1 fixes the doc violations are already gone)
    - `package.json` scripts block contains `"lint:docs"` entry
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ → contributors | Docs are read and copied by future contributors; forbidden examples propagate if not removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Information Disclosure | BUTTON_VARIANTS.md lucide example | mitigate | Replace with @remixicon/react equivalent so copied code is correct |
| T-09-02-02 | Tampering | lint:docs grep patterns | accept | Patterns are heuristic; false negatives possible for novel phrasings; document in CLAUDE.md (Plan 05) |
</threat_model>

<verification>
- `grep -rn "lucide-react" docs/ --include="*.md" | grep -v "docs/archive/"` returns no output
- `grep -rn "AI-powered" docs/ --include="*.md" | grep -Ev "docs/archive/|not AI-powered|AI-ready, not"` returns no output
- `npm run lint:docs` exits 0 and prints "lint:docs complete" with no WARNING lines
</verification>

<success_criteria>
Both active-doc violations removed. lint:docs script added to package.json and runs clean after the doc fixes.
</success_criteria>

<output>
Create `.planning/phases/09-lint-brand-and-documentation-hygiene/09-02-SUMMARY.md` when done
</output>
