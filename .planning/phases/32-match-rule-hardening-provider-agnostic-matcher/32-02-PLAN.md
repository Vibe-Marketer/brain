---
phase: 32-match-rule-hardening-provider-agnostic-matcher
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - supabase/functions/_shared/event-resolver.ts
  - supabase/functions/_shared/__tests__/event-resolver.test.ts
  - src/test/event-resolution-metadata-tier.integration.test.ts
autonomous: false
requirements: [MATCH-03, MATCH-06, MATCH-08, MATCH-11]
user_setup: []

must_haves:
  truths:
    - "The matcher proposes candidate merges reading recordings + call_participants across any provider, not just Zoom/zoom_raw_calls"
    - "The metadata tier writes only decision='merge_proposed' with tier='metadata' — it can never write merge_applied and never calls the apply RPC"
    - "Metadata proposals require nonzero time overlap and suppress recurring-title similarity as evidence"
    - "Thresholds are asymmetric: a high combined bar to propose a merge"
    - "Existing Zoom dedup behavior and the dedup_priority_mode/dedup_platform_order read path are unchanged"
  artifacts:
    - path: "supabase/functions/_shared/event-resolver.ts"
      provides: "findMetadataCandidates (pure scorer) + metadata-tier wiring in runShadowSweep (MATCH-03/06/08)"
      contains: "findMetadataCandidates"
    - path: "src/test/event-resolution-metadata-tier.integration.test.ts"
      provides: "TEST proof: provider-agnostic read, same-org-only, propose-only, Zoom preserved"
  key_links:
    - from: "findMetadataCandidates"
      to: "shouldSuppressTitleSignal (Plan 01)"
      via: "recurring_call_titles.occurrence_count lookup per (owner,title)"
      pattern: "shouldSuppressTitleSignal"
    - from: "metadata proposer"
      to: "event_match_decisions insert"
      via: ".insert() with tier='metadata', decision='merge_proposed', applied=false (never upsert/update)"
      pattern: "merge_proposed"
---

<objective>
Add the provider-agnostic metadata tier to `event-resolver.ts` (MATCH-06): a same-org matcher that reads `recordings` + `call_participants` (not the Zoom-only `zoom_raw_calls` path), scores candidates on time overlap + participant overlap + recurring-title-suppressed title similarity with asymmetric thresholds (MATCH-08), and writes ONLY `merge_proposed`/`tier='metadata'` rows (MATCH-03 — never auto-merges alone). Also lock that `dedup_priority_mode`/`dedup_platform_order` remain a display-order concern (MATCH-11), discarding nothing.

Purpose: This tier is load-bearing — Phase 33 (content-proof + alibi) extends this exact function shape. The design is gated by Task 1 because the signal set, scoring, and asymmetric thresholds are a real, forward-affecting choice.
Output: `findMetadataCandidates`, its sweep wiring, unit + integration proofs, and a MATCH-11 preservation guard.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-CONTEXT.md
@.planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-RESEARCH.md
@supabase/CLAUDE.md

<read_first>
- supabase/functions/_shared/event-resolver.ts (extend the SAME module; reuse Tier1Candidate, TIER1_SIGNAL_EXTRACTORS centralization, isUniqueViolation, and the Plan-01 shouldSuppressTitleSignal/RECURRING_TITLE_OCCURRENCE_THRESHOLD)
- supabase/functions/_shared/__tests__/event-resolver.test.ts (extend)
- supabase/functions/_shared/dedup-fingerprint.ts (reuse calculateTimeOverlap / calculateTitleSimilarity / calculateParticipantOverlap / normalizeParticipant / MATCH_THRESHOLDS as the scoring primitives — do NOT re-implement Levenshtein)
- supabase/functions/resolve-events/index.ts (the sweep caller; runShadowSweep is invoked here — the new tier runs inside runShadowSweep, no edge-function edit needed)
- src/test/event-resolution-shadow.integration.test.ts (the recording-fixture + sweep integration pattern to mirror)
</read_first>

<interfaces>
call_participants Row (live): { id, recording_id, organization_id, event_id, role, has_confirmed_speech, email (string|null), name (string|null), participant_type, sources (string[]), created_at }. Read `email` (normalized lowercased/trimmed) per recording for participant overlap; fall back to `name` only if email is null.

recordings columns needed: id, organization_id, owner_user_id, title, recording_start_time, recording_end_time, source_app, source_metadata, event_id, created_at.

event_match_decisions valid values already include tier='metadata' and decision='merge_proposed' — NO migration needed (32-RESEARCH.md Code Examples). Partial unique index on (recording_id_a, recording_id_b, tier) WHERE decision='merge_proposed' gives Pattern-3 idempotency for metadata rows too.

Reuse from Plan 01: shouldSuppressTitleSignal(occurrenceCount, threshold?), RECURRING_TITLE_OCCURRENCE_THRESHOLD.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Metadata-tier design gate (signals, scoring, asymmetric thresholds)</name>
  <action>Present the two design options below and STOP for Andrew's selection. Do NOT implement Tasks 2-3 until the signal set, scoring blend, and asymmetric threshold values are confirmed. Record the chosen option (and any threshold overrides) verbatim in the SUMMARY — Phase 33 builds on this locked contract.</action>
  <decision>Lock the metadata tier's signal set, scoring function, and asymmetric threshold values before implementation. Phase 33 (content-proof + alibi) builds on this exact contract, so it is a reversibility gate.</decision>
  <context>
    The metadata tier proposes (never applies) same-org candidate merges from recordings + call_participants. MATCH-08 requires asymmetric thresholds: a HIGH bar to propose a merge (a false merge is a data-exposure incident). MATCH-05's recurring-title suppression must feed the title signal. The nonzero-time-overlap discipline from MATCH-04 must apply here too. The recommended concrete design below (option-a) is what Plans 30/31 called a "reversibility gate" and were resolved as option-a outside the executor — present it and STOP for confirmation.
  </context>
  <options>
    <option id="option-a">
      <name>Recommended: 3-signal weighted score, nonzero-time-required, title-suppressed, high merge bar</name>
      <pros>
        - Reuses the already-vetted primitives (calculateTimeOverlap/TitleSimilarity/ParticipantOverlap) — no new distance math.
        - Signals: timeOverlap (MUST be > 0 or the pair is discarded, mirroring MATCH-04), participantOverlap (Jaccard on normalized call_participants emails), titleSimilarity (Levenshtein) BUT contributes 0 when shouldSuppressTitleSignal(occurrence_count) is true for that (owner,title).
        - Score = weighted blend biased to participants+time (e.g. participant 0.45, time 0.35, title 0.20) since title is the F5 trap.
        - Asymmetric: propose merge only when score >= MERGE_PROPOSE_THRESHOLD (high, e.g. 0.80) AND timeOverlap > 0 AND at least participants OR time is independently strong; never propose on a suppressed-title + weak-time pair. "Low bar to split" = the reverse/kill-switch path already makes un-merging cheap (Plan 03); the tier itself only ever proposes.
        - decided_by='auto', applied=false, tier='metadata', decision='merge_proposed' — MATCH-03.
      </pros>
      <cons>Threshold constants (0.80 / weights / occurrence threshold) are first-pass and will be validated by SAFE-06 (Plan 05) against Andrew's real data; may need one tuning pass.</cons>
    </option>
    <option id="option-b">
      <name>Strict AND-of-signals (no weighted score)</name>
      <pros>Simplest to reason about: propose only if timeOverlap>0 AND participantOverlap>=0.60 AND (non-suppressed title>=0.80). Very high precision.</pros>
      <cons>Likely too conservative — misses same-event captures where one source has a sparse participant list; discards recall the weighted model keeps. Harder for Phase 33 to layer content-proof onto a boolean.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (recommended) or option-b, or specify exact weights/thresholds.</resume-signal>
  <done>Andrew has selected a design (option-a/option-b or explicit weights+thresholds); the locked contract is recorded for Tasks 2-3 and Phase 33.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: findMetadataCandidates pure scorer + propose-only write path (MATCH-03, MATCH-06, MATCH-08)</name>
  <files>supabase/functions/_shared/event-resolver.ts, supabase/functions/_shared/__tests__/event-resolver.test.ts</files>
  <read_first>
    - supabase/functions/_shared/event-resolver.ts
    - supabase/functions/_shared/dedup-fingerprint.ts (scoring primitives)
  </read_first>
  <behavior>
    Unit-test the PURE scorer `findMetadataCandidates(candidates, opts)` (DB-free — like findDeterministicMatches):
    - Two same-org recordings, overlapping time, high participant overlap, non-recurring title -> proposed (score >= threshold), canonically ordered a<b.
    - Same two but ZERO time overlap -> NOT proposed (nonzero-time gate).
    - Two same-org recordings whose (owner,title) occurrenceCount is >= threshold -> title signal contributes 0; if time+participants alone do not clear the bar, NOT proposed (recurring-title trap closed).
    - Cross-org pair with identical everything -> NEVER proposed (same-org-only).
    - Every emitted candidate carries tier='metadata' intent and a 0..1 score; NONE carries a merge_applied/apply intent.
    - Fail-closed on malformed candidate rows (missing times/participants) -> skipped, never throws.
  </behavior>
  <action>
    In event-resolver.ts add an exported interface `MetadataCandidate` (extends the recording shape with recording_start_time, recording_end_time, title, owner_user_id, and a resolved participant email list + occurrence_count) and a pure function `findMetadataCandidates(candidates: MetadataCandidate[], opts?: { thresholds }): MetadataMatch[]` implementing the locked option-a design: pairwise within the same organization_id, compute timeOverlap (discard if <= 0), participantOverlap, and titleSimilarity zeroed when shouldSuppressTitleSignal(occurrence_count) is true; blend to a 0..1 score; emit a MetadataMatch { recording_id_a, recording_id_b, score, signals } only when the asymmetric MERGE_PROPOSE_THRESHOLD is cleared. Canonically order a<b. Export the threshold/weight constants. Reuse dedup-fingerprint.ts's calculate* helpers (import them; do NOT duplicate). Then add a metadata write path in runShadowSweep-adjacent code (Task 3 wires the fetch) that inserts each MetadataMatch with `.insert({ tier:'metadata', score, signals, decision:'merge_proposed', decided_by:'auto', applied:false })` tolerating unique_violation (Pattern 3) — the SAME idempotent idiom as the tier-1 path, NEVER .upsert()/.update(). Add unit tests per the behavior block.
  </action>
  <verify>
    <automated>npx vitest run supabase/functions/_shared/__tests__/event-resolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "findMetadataCandidates" supabase/functions/_shared/event-resolver.ts` present and exported; unit tests cover all behavior cases incl. cross-org=never and suppressed-title.
    - Source-scan: the metadata write path uses `.insert(` and never `.upsert(`/`.update(` (`grep -nE "\.upsert\(|\.update\(" supabase/functions/_shared/event-resolver.ts` returns nothing) — MATCH-03 propose-only proven by inspection, mirroring the SAFE-02 gate.
    - No `apply_event_match_atomic` reference anywhere in event-resolver.ts (`grep -c apply_event_match_atomic` is 0) — the tier never applies.
  </acceptance_criteria>
  <done>findMetadataCandidates scores same-org pairs with nonzero-time + title-suppression + asymmetric threshold, emits only merge_proposed/metadata intent, and unit tests + source-scan gates prove it never applies.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the metadata tier into the sweep (provider-agnostic reads) + MATCH-11 preservation</name>
  <files>supabase/functions/_shared/event-resolver.ts, src/test/event-resolution-metadata-tier.integration.test.ts</files>
  <read_first>
    - supabase/functions/_shared/event-resolver.ts (runShadowSweep)
    - src/test/event-resolution-shadow.integration.test.ts (fixture + sweep pattern)
    - supabase/functions/zoom-webhook/index.ts lines 288-340 (shouldNewMeetingBePrimary — the dedup_priority_mode/dedup_platform_order read path, MATCH-11)
  </read_first>
  <action>
    Extend runShadowSweep so that, per flagged org, in addition to the tier-1 deterministic pass it: (1) fetches the same unresolved recordings PLUS their call_participants rows (a second query keyed by recording_id IN the batch, same-org — do NOT read zoom_raw_calls; this is the MATCH-06 provider-agnostic seam) and the recurring_call_titles occurrence_count for each (owner,title); (2) builds MetadataCandidate rows (resolve participant emails from call_participants.email normalized, occurrence_count from the view); (3) calls findMetadataCandidates; (4) writes each result as a tier='metadata' merge_proposed row via the Pattern-3 insert. Keep it same-org-only and fail-closed (a fetch error skips that org, logs, continues — mirror the existing tier-1 error handling). Update ShadowSweepSummary counters to also report metadata proposals. Do NOT modify zoom-webhook/index.ts, zoom-sync-meetings/index.ts, or zoom_raw_calls — the old Zoom path is preserved unchanged (MATCH-06).

    Author src/test/event-resolution-metadata-tier.integration.test.ts (mirror event-resolution-shadow.integration.test.ts): seed a flagged TEST org with several same-org recordings across DIFFERENT source_app values (e.g. one fathom, one grain — providers with NO tier-1 signal) that share participants + overlapping times, plus call_participants rows; run the sweep; assert tier='metadata' merge_proposed rows are created for the genuine pairs, NONE for a recurring-title-only pair, NONE cross-org, and assert zero rows have decision='merge_applied'. Also assert recordings.event_id stayed NULL for all (propose-only). Honor the cleanup contract.

    MATCH-11 preservation: add a lightweight guard test asserting the existing dedup settings read path is intact — a Vitest test that reads supabase/functions/zoom-webhook/index.ts source and asserts it still references `dedup_priority_mode` and `dedup_platform_order` and retains the four priority-mode branches in shouldNewMeetingBePrimary, AND asserts src/types/supabase.ts still declares both user_settings columns. Add a one-line note in the SUMMARY that event-aware display-order UI is deferred to a later UI phase (MATCH-11 this phase = preservation, nothing discarded). Place this guard in the metadata-tier integration file or a small co-located unit test — whichever keeps the automated command below meaningful.
  </action>
  <verify>
    <automated>npm run test:integration -- src/test/event-resolution-metadata-tier.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The integration test proves provider-agnostic proposals (fathom/grain recordings proposed via call_participants, with NO tier-1 signal involved), same-org-only, propose-only, recurring-title suppressed, and recordings.event_id untouched.
    - `grep -c "zoom_raw_calls" supabase/functions/_shared/event-resolver.ts` is 0 (the new tier never touches the Zoom-only table).
    - The MATCH-11 preservation guard passes; zoom-webhook/index.ts and zoom-sync-meetings/index.ts are byte-unchanged (`git diff --stat`).
  </acceptance_criteria>
  <done>The sweep proposes provider-agnostic metadata merges (propose-only, same-org, recurring-title-suppressed) proven on TEST; the old Zoom path and the dedup-settings read path are untouched; MATCH-11 preservation guard passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadow sweep (service-role) -> event_match_decisions | The sweep runs as service-role; the ONLY safe write it may make for this tier is a propose row |
| Org A recordings <-> Org B recordings | The matcher must never pair across orgs (defense-in-depth to RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering / Information Disclosure | Metadata tier auto-merging instead of proposing | mitigate | Write path hardcodes decision='merge_proposed', applied=false, tier='metadata'; `.upsert(`/`.update(`/`apply_event_match_atomic` are grep-absent (source-scan gate, mirroring SAFE-02). MATCH-03 proven by inspection + test. |
| T-32-03 | Information Disclosure | Cross-org pairing in the metadata matcher | mitigate | findMetadataCandidates pairs only within one organization_id; unit test asserts identical cross-org rows are never proposed. RLS layer (Plan 03 SAFE-04) is the backstop. |
| T-32-06 | Information Disclosure | Recurring-title false merge re-entering via the new tier | mitigate | Nonzero-time gate + shouldSuppressTitleSignal zeroing the title contribution; unit + integration tests pin the recurring-title-only pair as NOT proposed. |
| T-32-SC | Tampering | Supply chain | accept | No new packages this phase (32-RESEARCH.md Package Legitimacy Audit: N/A); reuses fastest-levenshtein already pinned in dedup-fingerprint.ts. |
</threat_model>

<verification>
- `npx vitest run supabase/functions/_shared/__tests__/event-resolver.test.ts` + `npm run test:integration -- src/test/event-resolution-metadata-tier.integration.test.ts` green.
- Full `npx vitest run` + `npm run test:integration` show no regressions to the Phase-31 tier-1 shadow tests.
- Known deferred gap recorded: apply_event_match_atomic hardcodes tier='deterministic' (32-RESEARCH.md Pitfall 2) — NOT touched here because MATCH-03 means this tier never calls apply; flag for the future phase that first builds an approve-a-metadata-proposal action.
</verification>

<success_criteria>
- MATCH-06: the matcher reads recordings + call_participants provider-agnostically and proposes for non-Zoom providers; old Zoom path preserved.
- MATCH-03: metadata tier only ever writes merge_proposed (proven by test + source-scan).
- MATCH-08: asymmetric high-bar-to-propose thresholds implemented and locked via the design gate.
- MATCH-11: dedup_priority_mode/dedup_platform_order read path preserved; display-order-under-event deferral documented.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 02)
- `findMetadataCandidates` + metadata-tier sweep wiring + threshold constants in `supabase/functions/_shared/event-resolver.ts` (deployed to prod in Plan 04 via resolve-events).
- Extended unit tests in `supabase/functions/_shared/__tests__/event-resolver.test.ts`.
- `src/test/event-resolution-metadata-tier.integration.test.ts` (provider-agnostic, propose-only, same-org proofs).
- MATCH-11 preservation guard test.
</artifacts>

<output>
Create `.planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-02-SUMMARY.md` when done. Record the locked design (chosen option + final threshold/weight values) so Plan 05's precision measurement and Phase 33 can reference it.
</output>
