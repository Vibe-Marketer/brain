---
phase: 32-match-rule-hardening-provider-agnostic-matcher
plan: 04
type: execute
wave: 3
depends_on: ["32-01", "32-02", "32-03"]
files_modified:
  - src/types/supabase.ts
autonomous: false
requirements: [MATCH-03, MATCH-04, MATCH-05, MATCH-06, MATCH-08, SAFE-03]
user_setup: []

must_haves:
  truths:
    - "The hardened checkMatch is LIVE in production, closing the F5 bug in the live Zoom ingestion path"
    - "The provider-agnostic metadata tier is deployed to production (inert — no org flag enabled yet)"
    - "The kill-switch RPC exists in production and is denied to anon/authenticated"
    - "src/types/supabase.ts matches the live database after the migration(s)"
  artifacts:
    - path: "src/types/supabase.ts"
      provides: "regenerated types reflecting the kill-switch RPC (+ optional view fix)"
  key_links:
    - from: "supabase functions deploy"
      to: "zoom-webhook + zoom-sync-meetings + resolve-events"
      via: "--use-api (Docker-less), prod-ref-guarded"
      pattern: "use-api"
---

<objective>
Guarded production apply for Phase 32: deploy the hardened `dedup-fingerprint.ts` (closes the LIVE F5 bug — MATCH-04) and the provider-agnostic `event-resolver.ts` metadata tier (MATCH-03/05/06/08, inert until a flag is enabled), apply the SAFE-03 kill-switch migration (and the conditional recurring_call_titles security_invoker fix if Plan 01 authored one) to prod, and re-sync `src/types/supabase.ts`. Mirrors Phase 31 Plan 04's guarded-apply discipline.

Purpose: Phase 32's goal is to CLOSE THE LIVE F5 BUG — that only happens once dedup-fingerprint.ts ships to the live zoom-webhook. And Plan 05's precision measurement needs the metadata tier live before Andrew's org flag is enabled. This is the heavy prod-mutation gate, kept separate from Plan 05's light org-confirmation gate by design.
Output: Production carrying the hardened matcher + kill switch; types re-synced; nothing enabled for any org.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-RESEARCH.md
@supabase/CLAUDE.md
@.planning/phases/31-deterministic-resolution-shadow-mode-only/31-04-SUMMARY.md

<read_first>
- .planning/phases/31-deterministic-resolution-shadow-mode-only/31-04-SUMMARY.md (the exact guarded-apply + prod-ref-guard + --use-api deploy sequence proven last phase)
- 32-01-SUMMARY.md and 32-03-SUMMARY.md (the actual migration filenames to apply; whether Plan 01 authored the view fix)
- CLAUDE.md "Environment files — DO NOT CONFUSE" (migrations/DDL read .env, must verify prod ref vltmrnjsubfzrgrtdqey) and supabase/CLAUDE.md "Deploying Edge Functions" (--use-api, Docker not running)
</read_first>

<interfaces>
Prod Supabase ref: vltmrnjsubfzrgrtdqey (guard: the connection/link must resolve to this ref BEFORE and AFTER apply, verified 3x per Phase 30/31 discipline).
Edge functions to redeploy (all bundle Phase-32-changed shared modules):
- zoom-webhook (bundles dedup-fingerprint.ts — MATCH-04 goes live here)
- zoom-sync-meetings (also bundles dedup-fingerprint.ts)
- resolve-events (bundles event-resolver.ts — metadata tier; inert until a flag row is enabled)
Migrations to apply: the SAFE-03 kill-switch migration (32-03) + the conditional security_invoker view fix (32-01, only if it exists).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Prod-apply gate — confirm exactly what ships to production</name>
  <action>Present the exact prod-mutation set below (migrations + the three edge-function redeploys) and STOP for approval before Task 2 runs anything against production. Do NOT apply migrations or deploy functions until Andrew approves.</action>
  <what-built>
    Phase 32 Plans 01-03 are proven on TEST + unit suites: hardened checkMatch (F5 fix), provider-agnostic metadata tier (propose-only), and the SAFE-03 kill-switch RPC. This step deploys/applies them to production.
  </what-built>
  <how-to-verify>
    Confirm the exact prod mutation set before it runs:
    1. Migrations to apply to prod (vltmrnjsubfzrgrtdqey): the kill-switch RPC migration [filename from 32-03-SUMMARY.md], and the recurring_call_titles security_invoker fix [filename from 32-01-SUMMARY.md] IF Plan 01 found a regression (else none).
    2. Edge functions to redeploy via `supabase functions deploy <fn> --use-api`: zoom-webhook, zoom-sync-meetings (both carry the F5 fix — this CHANGES live Zoom dedup behavior, making it reject zero-time-overlap merges), resolve-events (carries the metadata tier — INERT, no org flag enabled).
    3. This does NOT enable event_resolution for any org (that is Plan 05, Andrew's org only). Zero organization_feature_flags rows change here.
    4. Optional read-only historical F5 audit (see verification section) may be run to gauge blast radius — non-remediating.
    Reply "approved" to proceed, or name changes.
  </how-to-verify>
  <resume-signal>Type "approved" or describe required changes.</resume-signal>
  <done>Andrew approved the exact migration + edge-function-deploy set (or named changes); Task 2 may proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Execute guarded prod apply + type re-sync</name>
  <files>src/types/supabase.ts</files>
  <read_first>
    - .planning/phases/31-deterministic-resolution-shadow-mode-only/31-04-SUMMARY.md (proven command sequence)
    - supabase/CLAUDE.md "Deploying Edge Functions"
  </read_first>
  <action>
    Following Phase 31 Plan 04's exact discipline: (1) verify the Supabase CLI link / .env DATABASE_URL resolves to prod ref vltmrnjsubfzrgrtdqey BEFORE any apply. (2) Apply the Phase-32 migration(s) to prod (`supabase db push --linked` or the Phase-31 apply method recorded in 31-04-SUMMARY.md) — the kill-switch RPC migration and, only if it exists, the security_invoker view fix. (3) Deploy edge functions server-side without Docker: `supabase functions deploy zoom-webhook --use-api`, `supabase functions deploy zoom-sync-meetings --use-api`, `supabase functions deploy resolve-events --use-api`. (4) Regenerate types: `supabase gen types typescript --linked > src/types/supabase.ts` (the project's gen:types path per Phase 30) and confirm the diff reflects only the kill-switch RPC (+ view fix if applied) — run the type-check the way Phase 30/31 did (tsc against tsconfig.app.json; if regen introduces zero new errors leave type-baseline.json untouched). (5) Verify prod-ref STILL vltmrnjsubfzrgrtdqey AFTER apply, and confirm via direct SQL introspection that: `kill_switch_revert_event_merges` exists with EXECUTE denied to anon/authenticated; organization_feature_flags still has ZERO enabled event_resolution rows (nothing was turned on); the three functions report the new deploy. Do NOT enable any flag. Do NOT merge the feature branch to main (milestone branch discipline — merge only when Andrew is comfortable).
  </action>
  <verify>
    <automated>node scripts/type-check.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Prod-ref guard confirmed vltmrnjsubfzrgrtdqey before AND after (record both in SUMMARY).
    - Migration list shows the Phase-32 migration(s) Local==Remote on prod; SQL introspection confirms kill_switch_revert_event_merges present + REVOKEd from anon/authenticated.
    - The three edge functions redeployed via --use-api; zoom-webhook now carries the F5 gate (spot-check the deployed bundle or the functions list timestamp).
    - organization_feature_flags has zero enabled event_resolution rows post-apply (mechanism still inert).
    - src/types/supabase.ts regenerated; type-check baseline unchanged (no new errors) or baseline updated only if regen introduced expected drift, documented in SUMMARY.
  </acceptance_criteria>
  <done>Migration(s) applied to prod under the 3x prod-ref guard, the three edge functions redeployed via --use-api, types re-synced, kill-switch RPC live + revoked, and zero org flags enabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI -> PRODUCTION Supabase (vltmrnjsubfzrgrtdqey) | Migrations + function deploys mutate live prod with real customer data |
| deploy -> live zoom-webhook | The F5 fix changes live dedup verdicts — must be strictly safer (rejects zero-overlap), never more permissive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07 | Tampering | Migration/DDL applied to the WRONG Supabase project | mitigate | Prod-ref guard vltmrnjsubfzrgrtdqey verified 3x (before/after), the exact Phase 30/31 discipline; .env is prod, .env.test/.local are TEST (CLAUDE.md). |
| T-32-06 | Information Disclosure | F5 fix deploy accidentally loosens dedup | mitigate | The only change is an ADDITIONAL gate (timeOverlap>0) — strictly more restrictive; unit tests (Plan 01) pin preserved legitimate matches. |
| T-32-04 | Information Disclosure | Accidentally enabling a flag during apply | mitigate | Explicit acceptance check: zero enabled event_resolution rows post-apply; flag enablement is Plan 05 only. |
| T-32-SC | Tampering | Supply chain | accept | No new packages; `--use-api` server-side bundle of existing pinned deps. |
</threat_model>

<verification>
- Post-apply SQL introspection (read-only, prod): kill-switch RPC present + REVOKEd; zero enabled flags; three functions redeployed.
- `node scripts/type-check.mjs` passes (project's real type-check, not the hollow root tsconfig).
- OPTIONAL read-only historical F5 audit (non-remediating, Andrew's knowing decision): count zoom_raw_calls rows with non-null fuzzy_match_score AND is_primary=false, cross-referenced against recurring_call_titles.occurrence_count for the same (owner,title). Record the count in the SUMMARY as blast-radius evidence. Remediation of historical rows is OUT of the forward-only scope (STATE.md decision) and `is_primary` has zero src read sites (no UX regression) — do not remediate without an explicit Andrew decision.
</verification>

<success_criteria>
- The live F5 bug is closed in production (hardened checkMatch deployed to zoom-webhook + zoom-sync-meetings).
- The metadata tier + kill-switch RPC are live in prod but inert (no org enabled), ready for Plan 05.
- Types re-synced; prod-ref guarded throughout; no flag enabled.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 04)
- Production deployment of hardened dedup-fingerprint.ts (via zoom-webhook + zoom-sync-meetings) and the metadata tier (via resolve-events).
- SAFE-03 kill-switch RPC (+ optional view fix) live in prod.
- Regenerated `src/types/supabase.ts`.
- Recorded blast-radius audit count (optional) + prod-ref-guard evidence in the SUMMARY.
</artifacts>

<output>
Create `.planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-04-SUMMARY.md` when done. Record prod-ref-guard confirmations, migration Local==Remote proof, and the zero-enabled-flags post-state.
</output>
