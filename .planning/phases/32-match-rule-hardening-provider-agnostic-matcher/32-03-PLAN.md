---
phase: 32-match-rule-hardening-provider-agnostic-matcher
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/2026090200000X_kill_switch_revert_event_merges.sql
  - src/test/event-resolution-kill-switch.integration.test.ts
  - src/test/rls-regression.test.ts
autonomous: true
requirements: [SAFE-03, SAFE-04]
user_setup: []

must_haves:
  truths:
    - "An admin can revert every applied event merge inside a time range in a single atomic DB call, each reversal recorded in the ledger"
    - "A non-service-role (anon/authenticated) caller cannot execute the kill-switch RPC"
    - "After two same-org recordings are resolved into one event, an unrelated org still reads zero rows for that event AND for both merged recordings"
  artifacts:
    - path: "supabase/migrations/2026090200000X_kill_switch_revert_event_merges.sql"
      provides: "kill_switch_revert_event_merges RPC (SAFE-03) + EXECUTE revokes"
      contains: "kill_switch_revert_event_merges"
    - path: "src/test/event-resolution-kill-switch.integration.test.ts"
      provides: "apply -> kill-switch-revert round-trip proof on TEST"
    - path: "src/test/rls-regression.test.ts"
      provides: "SAFE-04 cross-org no-audience-widening assertions in the bespoke events block"
  key_links:
    - from: "kill_switch_revert_event_merges"
      to: "event_match_decisions WHERE decision='merge_applied' in [start,end]"
      via: "single PL/pgSQL FOR-loop, one implicit transaction"
      pattern: "merge_applied"
    - from: "resolution (apply_event_match_atomic)"
      to: "cross-org RLS isolation"
      via: "events RLS grants only via participation/owned capture, never organization_id"
      pattern: "events"
---

<objective>
Build the SAFE-03 kill switch (revert all applied event merges in a time range, in one operation) and prove SAFE-04 (cross-org false merges cannot widen readable audience) with a live isolation test. Both are TEST-only + code this plan; the migration reaches prod in Plan 04.

Purpose: A false merge is treated as a data-exposure incident (MATCH-08 posture). SAFE-03 is the emergency undo; SAFE-04 proves the RLS blast wall holds even when recordings are resolved into a shared event. Neither auto-merge path is wired yet (metadata tier is propose-only, tier-1 apply is not wired to the sweep), so this is proven-but-inert safety infrastructure that Phase 33+ relies on.
Output: One new SECURITY DEFINER RPC, one apply->revert integration test, and new cross-org assertions in the existing RLS regression harness.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-CONTEXT.md
@.planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-RESEARCH.md
@supabase/CLAUDE.md

<read_first>
- supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql (the apply/reverse pattern this RPC extends; copy its SECURITY DEFINER + REVOKE idioms exactly)
- supabase/migrations/20260901000002_create_event_match_decisions.sql (ledger schema; decision/tier/decided_by CHECK values)
- src/test/event-match-apply-reverse.integration.test.ts (the fixture/cleanup + apply RPC calling pattern to mirror for the kill-switch test)
- src/test/rls-regression.test.ts lines 1163-1250 (the bespoke `events` isolation block to extend) and lines 40-110 (CROSS_ORG_TABLES / CLIENT_DENY_TABLES / BESPOKE_CLIENT_DENY_TABLES registries)
- supabase/migrations/20260611200000_autopilot_queue_runner_state.sql (kill-switch HALT precedent — mirror the flag concept, NOT the RPC)
</read_first>

<interfaces>
Ledger columns (event_match_decisions): recording_id_a, recording_id_b, event_id, tier ('deterministic'|'content_proof'|'metadata'), score, signals jsonb, decision ('merge_proposed'|'merge_applied'|'reversed'|'rejected'), decided_by ('auto'|'user'|'admin'), applied bool, reverses_decision_id, created_at. CHECK: recording_id_a < recording_id_b.

Phase-31 RPCs (live, service-role-only): `apply_event_match_atomic(p_recording_id_a,p_recording_id_b,p_event_id,p_decided_by,p_signals,p_owner_user_id) RETURNS uuid` (creates the event, sets event_id on both recordings, writes a merge_applied row). `reverse_event_match_atomic(p_decision_id,p_owner_user_id)` (per-decision, ownership-by-parameter). The kill switch must NOT loop-call reverse_event_match_atomic from TS (N transactions, and its per-owner check does not fit a bulk admin action) — it is one new SQL function that loops internally.

recordings has no org column on event_match_decisions; join `recordings.organization_id` via recording_id_a for the optional org filter.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: kill_switch_revert_event_merges bulk-reversal RPC (SAFE-03)</name>
  <files>supabase/migrations/2026090200000X_kill_switch_revert_event_merges.sql, src/test/event-resolution-kill-switch.integration.test.ts</files>
  <read_first>
    - supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql (full)
    - src/test/event-match-apply-reverse.integration.test.ts (full — mirror its beforeAll/afterAll, two-recording fixture, service-role client)
  </read_first>
  <action>
    Author supabase/migrations/2026090200000X_kill_switch_revert_event_merges.sql (next unused timestamp; follow supabase/CLAUDE.md migration header/section style) creating `public.kill_switch_revert_event_merges(p_start_time TIMESTAMPTZ, p_end_time TIMESTAMPTZ, p_organization_id UUID DEFAULT NULL) RETURNS INTEGER` — LANGUAGE plpgsql, SECURITY DEFINER, `SET search_path = public`. Behavior: validate `p_start_time <= p_end_time` (RAISE EXCEPTION otherwise). Loop `FOR v_rec IN SELECT emd.id, emd.recording_id_a, emd.recording_id_b, emd.tier FROM event_match_decisions emd JOIN recordings r ON r.id = emd.recording_id_a WHERE emd.decision = 'merge_applied' AND emd.created_at >= p_start_time AND emd.created_at <= p_end_time AND (p_organization_id IS NULL OR r.organization_id = p_organization_id) LOOP`: (1) `UPDATE recordings SET event_id = NULL, updated_at = NOW() WHERE id IN (v_rec.recording_id_a, v_rec.recording_id_b);` (2) `INSERT INTO event_match_decisions (recording_id_a, recording_id_b, event_id, tier, score, signals, decision, decided_by, applied, reverses_decision_id) VALUES (v_rec.recording_id_a, v_rec.recording_id_b, NULL, v_rec.tier, NULL, '{}'::jsonb, 'reversed', 'admin', false, v_rec.id);` increment a counter; RETURN the count. The whole loop is one implicit transaction (SAFE-03 "in one operation"). Do NOT add a per-caller ownership-by-parameter check (this is a bulk admin action across potentially many owners — 32-RESEARCH.md Pitfall 3); authorization is service-role-only via REVOKE. Add `REVOKE EXECUTE ON FUNCTION public.kill_switch_revert_event_merges FROM PUBLIC, anon, authenticated;`. In the migration comment, state that the HALT half of the kill switch (stop the sweep proposing/applying further) is achieved by setting the existing `organization_feature_flags` `event_resolution` row `enabled=false` (the sweep already skips non-enabled orgs — resolve-events/index.ts), so no new flag/table is added (avoids new-object FORCE-RLS surface). Do NOT create any new table.

    Then author src/test/event-resolution-kill-switch.integration.test.ts (mirror event-match-apply-reverse.integration.test.ts: `describe.skipIf(!integrationDbReachable)`, service-role client, TEST-tagged fixture users/org/recordings, capture-and-restore afterAll per supabase/CLAUDE.md cleanup contract). Prove: seed >=2 pairs of same-org recordings, apply merges via apply_event_match_atomic (Phase-31 RPC) so several merge_applied rows exist with known created_at; call kill_switch_revert_event_merges over a window covering them; assert (a) the return count equals the number of applied merges in range, (b) every affected recording's event_id is back to NULL, (c) a 'reversed' ledger row with reverses_decision_id set exists per reverted decision, (d) an applied merge OUTSIDE the time window is untouched, (e) the org filter reverts only the named org's merges.
  </action>
  <verify>
    <automated>npm run test:integration -- src/test/event-resolution-kill-switch.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Migration applies cleanly to TEST (`supabase db push --linked` against callvault-test per Phase 30/31 discipline; relink to prod after, do NOT apply to prod here).
    - Integration test passes and proves the time-range + org-filter + atomic-count + ledger-write behavior; the out-of-window applied merge is provably untouched.
    - `grep -nE "REVOKE EXECUTE ON FUNCTION public.kill_switch_revert_event_merges FROM" supabase/migrations/2026090200000X_kill_switch_revert_event_merges.sql` shows PUBLIC, anon, authenticated revoked.
    - No new table is created by the migration (`grep -vi '^--' | grep -ci 'create table'` is 0).
  </acceptance_criteria>
  <done>kill_switch_revert_event_merges reverts all in-range merge_applied decisions in one transaction (optionally org-scoped), writes reversed ledger rows, is revoked from non-service roles, applies cleanly to TEST, and the integration round-trip passes.</done>
</task>

<task type="auto">
  <name>Task 2: SAFE-04 cross-org no-audience-widening proof (extend rls-regression)</name>
  <files>src/test/rls-regression.test.ts</files>
  <read_first>
    - src/test/rls-regression.test.ts lines 700-840 (beforeAll fixtures incl. event + recording seeds) and 1163-1250 (bespoke events block)
    - supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql (apply_event_match_atomic signature)
  </read_first>
  <action>
    Extend the bespoke `events` isolation block in rls-regression.test.ts to prove SAFE-04: resolution must never widen either recording's readable audience. In beforeAll, seed a SECOND Org-A recording owned by Org A's user (mirror the existing recordingA2 fixture pattern used for the event_match_decisions block), then call apply_event_match_atomic via the service-role `admin` client with Org A's owner id and both Org-A recordings to merge them into one event (event_id now set on both). Add assertions in the bespoke block: (1) the unrelated Org B JWT client (`clientB`) reads ZERO rows when selecting that merged event by id; (2) `clientB` reads ZERO rows selecting either merged recording by id from `recordings`; (3) the service-role `admin` client CAN see the merged event + both recordings (existence proof so the zero-row assertions cannot pass on an empty table — mirror the T-31-03-03 existence-proof precedent already in this file). Tear down the new recording + created event + ledger rows in afterAll with per-step try/catch (cleanup contract). Do NOT add `events` to CROSS_ORG_TABLES (its filterColumn union has no member — that is exactly why the bespoke block exists). Same-org-only matching is proven separately at the unit level (event-resolver.test.ts findDeterministicMatches cross-org case; Plan 02 adds the metadata-tier same-org proof) — reference that as the matcher-level defense-in-depth in a comment.
  </action>
  <verify>
    <automated>npm run test:integration -- src/test/rls-regression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The RLS regression suite passes with the new merged-event assertions; a deliberately-broken RLS policy would fail with the existing `RLS LEAK:` message shape.
    - The merged event + both merged recordings are proven invisible to the unrelated org from an authenticated JWT (not service-role), AND visible to service-role (no empty-table false pass).
    - afterAll restores TEST to its pre-run state (suite is re-runnable N times).
  </acceptance_criteria>
  <done>A live JWT-driven test proves that resolving two same-org recordings into one event leaves that event and both recordings invisible to an unrelated org, with a service-role existence proof guarding against an empty-table false pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/authenticated -> kill_switch_revert_event_merges | A bulk reversal is a privileged admin action; PostgREST exposes RPCs to JWT callers unless revoked |
| Org A resolution -> Org B readable audience | Merging two recordings into one event must not become a cross-org read path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02 | Elevation of Privilege / Tampering | `kill_switch_revert_event_merges` callable by non-admin | mitigate | SECURITY DEFINER + `REVOKE EXECUTE FROM PUBLIC, anon, authenticated` (service-role only), mirroring apply/reverse_event_match_atomic exactly. |
| T-32-03 | Information Disclosure | Cross-org false merge widens readable audience | mitigate | events RLS grants only via participation/owned capture (EVT-04, no organization_id column); SAFE-04 test proves the merged event + recordings stay invisible to the unrelated org from a JWT. Matcher-level same-org-only pairing is defense-in-depth. |
| T-32-02b | Tampering | Kill switch reverts merges outside the intended window | mitigate | `p_start_time <= p_end_time` guard; test asserts an out-of-window applied merge is untouched and the optional org filter scopes correctly. |
</threat_model>

<verification>
- `npm run test:integration -- src/test/event-resolution-kill-switch.integration.test.ts src/test/rls-regression.test.ts` green against TEST.
- Migration list shows the kill-switch migration applied to callvault-test and CLI relinked to prod (prod NOT touched this plan).
- Full `npx vitest run` shows no unit regressions.
</verification>

<success_criteria>
- SAFE-03: one atomic RPC reverts all applied merges in a time range (optionally org-scoped), writing a reversed ledger row per reversal, service-role-only, proven on TEST.
- SAFE-04: a live JWT-driven test proves resolution never widens either recording's readable audience across orgs.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 03)
- `supabase/migrations/2026090200000X_kill_switch_revert_event_merges.sql` — new SAFE-03 RPC, TEST-applied here, prod-applied in Plan 04.
- `src/test/event-resolution-kill-switch.integration.test.ts` — apply->revert round-trip proof.
- SAFE-04 cross-org no-audience-widening assertions added to `src/test/rls-regression.test.ts`.
</artifacts>

<output>
Create `.planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-03-SUMMARY.md` when done. Record the exact kill-switch migration filename for Plan 04's prod-apply list.
</output>
