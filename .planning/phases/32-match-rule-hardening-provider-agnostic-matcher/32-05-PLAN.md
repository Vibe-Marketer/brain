---
phase: 32-match-rule-hardening-provider-agnostic-matcher
plan: 05
type: execute
wave: 4
depends_on: ["32-04"]
files_modified:
  - scripts/shadow-precision-eval.ts
autonomous: false
requirements: [SAFE-06]
user_setup: []

must_haves:
  truths:
    - "Andrew's own CallVault organization_id is identified read-only from his known account emails and confirmed by him"
    - "event_resolution is enabled for exactly that ONE org_id and no other org"
    - "The shadow sweep produces real event_match_decisions proposals for that org, which are hand-scored for false-merge rate"
    - "A false-merge rate is recorded against the <=0.1% target BEFORE SAFE-01 is enabled for any other (customer) org"
  artifacts:
    - path: "scripts/shadow-precision-eval.ts"
      provides: "read-only scorer over event_match_decisions using false-discovery-rate framing (SAFE-06)"
      contains: "event_match_decisions"
  key_links:
    - from: "organization_feature_flags"
      to: "exactly one enabled event_resolution row (Andrew's org)"
      via: "scoped insert + expansion assertion"
      pattern: "event_resolution"
    - from: "shadow-precision-eval.ts"
      to: "false-merge rate over the PROPOSED set"
      via: "wrong merges / merges proposed (not / total pair-space)"
      pattern: "proposed"
---

<objective>
Prove shadow precision (SAFE-06) on REAL data before SAFE-01 is enabled for any customer org. Per Andrew's locked decision, use his OWN CallVault org/account only — never a customer's data, never a synthetic/dedicated internal org. Identify his org read-only, get a quick single-question confirmation, enable `event_resolution` for that one org_id, let the sweep run, and hand-score the resulting proposals against the <=0.1% false-merge target.

Purpose: SAFE-06 is the gate before customer rollout. Flipping the flag for ANY org for the first time in this milestone is a materially different action category than the inert prior phases — so it gets its own light, explicitly-scoped checkpoint. Shadow mode means even enabled, nothing auto-merges (propose-only), so the risk is low and the gate is a single confirmation, not a heavy review.
Output: A reusable precision scorer, real proposals for Andrew's org, and a recorded false-merge rate.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-CONTEXT.md
@.planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-RESEARCH.md
@supabase/CLAUDE.md

<read_first>
- scripts/get-user-id.ts (the read-only pattern for resolving a user by email; note the supabase.auth.admin.listUsers() fallback when a direct auth.users select is blocked)
- supabase/functions/resolve-events/index.ts (how flagged orgs are resolved: organization_feature_flags WHERE flag_key='event_resolution' AND enabled=true; and the X-Reconcile-Secret trigger contract to run the sweep manually)
- supabase/migrations/20260901000002_create_event_match_decisions.sql (ledger columns to read/score)
- 32-02-SUMMARY.md (the locked metadata-tier thresholds — the scorer reports precision per tier)
</read_first>

<interfaces>
Andrew's known account emails (per project memory — try each read-only): a@vibeos.com (described as the only ADMIN), andrew@aisimple.co, andrew@vibeos.com, naegele412@gmail.com. Resolve user id(s), then their organization membership -> candidate organization_id(s).

Enabling a flag = INSERT INTO organization_feature_flags (organization_id, flag_key, enabled) VALUES (<andrew_org_id>, 'event_resolution', true) [or UPDATE ... enabled=true if a row exists]. resolve-events then picks it up.

Sweep trigger: POST resolve-events with header X-Reconcile-Secret: <RECONCILE_SECRET> and body {"mode":"shadow"} (same secret that gates fathom-reconcile), OR wait for the event-resolution-sweep pg_cron tick.

Precision framing (32-RESEARCH.md Don't Hand-Roll): false-merge rate = wrong merges / merges PROPOSED (the positive set), NOT / total O(n^2) pair-space (which is dominated by true negatives and trivially scores ~0).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the precision scorer + identify Andrew's org (read-only)</name>
  <files>scripts/shadow-precision-eval.ts</files>
  <read_first>
    - scripts/get-user-id.ts (read-only user/org lookup pattern + auth.admin.listUsers fallback)
    - supabase/migrations/20260901000002_create_event_match_decisions.sql (ledger columns)
  </read_first>
  <action>
    Author scripts/shadow-precision-eval.ts (a Node/tsx read-only tool, following scripts/get-user-id.ts's env + service-role client setup; NO writes). It has two read-only capabilities: (1) `--find-org` mode: for each of Andrew's known emails, resolve the user id (direct auth query, falling back to supabase.auth.admin.listUsers() like get-user-id.ts), then resolve that user's organization membership, and print each candidate organization_id with its name, the matched email, and a count of recordings in that org (so Andrew can judge "enough real call history"). (2) `--score <organization_id>` mode: read event_match_decisions rows for recordings in that org (join recordings.organization_id), group by tier ('deterministic' vs 'metadata') and decision, and emit a review worksheet — each proposed pair (recording_id_a/b, tier, score, signals, plus each recording's title + start time so a human can judge same-event-or-not) — plus the denominator (total merge_proposed rows) so the false-merge rate = hand-labeled-wrong / total-proposed can be computed. Output the worksheet as a readable table AND a JSON file under .planning/phases/32-.../ for the record. Do NOT auto-label; SAFE-06 requires human judgment (32-VALIDATION.md manual-only). Guard the script to refuse to run any write/UPDATE/INSERT (read-only by construction) and to require an explicit org id for --score (never scan all orgs).
  </action>
  <verify>
    <automated>node scripts/shadow-precision-eval.ts --find-org</automated>
  </verify>
  <acceptance_criteria>
    - `--find-org` prints candidate org_id(s) for Andrew's emails with org name, matched email, and recording count — all read-only (no rows mutated).
    - `--score <org_id>` produces a review worksheet + JSON with per-tier proposed pairs and the total-proposed denominator, and refuses to run without an explicit org id.
    - `grep -niE "\.insert\(|\.update\(|\.upsert\(|\.delete\(" scripts/shadow-precision-eval.ts` returns nothing (read-only by construction).
  </acceptance_criteria>
  <done>A read-only scorer exists: --find-org lists Andrew's candidate org(s) with recording counts, --score builds a per-tier proposal worksheet + JSON with a total-proposed denominator, and the script provably performs no writes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm Andrew's org (light single-question gate)</name>
  <action>Present the candidate org(s) from `--find-org` and STOP for a single-question confirmation before enabling any flag in Task 3. Do NOT enable event_resolution until Andrew confirms the specific org_id.</action>
  <what-built>Task 1 identified Andrew's candidate organization_id(s) read-only from his known account emails, with org name and recording count.</what-built>
  <how-to-verify>
    Present the candidate org(s) from `--find-org`: organization_id, org name, which email matched, and how many recordings it has. Single question: is this the right org to enable event_resolution shadow mode on, and does it have enough real call history to make a precision measurement meaningful? This is pre-approved in principle (Andrew already chose "my own org/account") — this gate only confirms the specific org_id and that it is not a customer org. Reply with the chosen organization_id (or "approved" if a single unambiguous candidate) to proceed, or correct it.
  </how-to-verify>
  <resume-signal>Reply with the confirmed organization_id, or "approved" for the single candidate.</resume-signal>
  <done>Andrew confirmed the specific organization_id (his own org, not a customer); Task 3 may enable the flag for exactly that org.</done>
</task>

<task type="auto">
  <name>Task 3: Enable flag for Andrew's org only, run the sweep, hand-score precision (SAFE-06)</name>
  <files>scripts/shadow-precision-eval.ts</files>
  <read_first>
    - supabase/functions/resolve-events/index.ts (flag lookup + X-Reconcile-Secret trigger)
  </read_first>
  <action>
    With the confirmed organization_id: (1) verify prod-ref vltmrnjsubfzrgrtdqey. (2) Enable the flag for EXACTLY that org: upsert organization_feature_flags (organization_id=<confirmed>, flag_key='event_resolution', enabled=true). IMMEDIATELY assert scope: query `SELECT organization_id FROM organization_feature_flags WHERE flag_key='event_resolution' AND enabled=true` and assert the result set is EXACTLY {confirmed org_id} — if any other org appears, STOP and surface it (T-32-04: the flag must never silently expand beyond Andrew's org). (3) Trigger the sweep: POST resolve-events with X-Reconcile-Secret and {"mode":"shadow"} (or wait for one pg_cron tick), confirming it processed that org. (4) Run `node scripts/shadow-precision-eval.ts --score <org_id>` to produce the worksheet, then HAND-LABEL each proposed pair as correct/false merge (human judgment — this is the SAFE-06 label set). (5) Compute the false-merge rate = false / total-proposed per tier and overall, and record it against the <=0.1% target in the SUMMARY as the SAFE-06 evidence. (6) Leave the flag ENABLED for Andrew's own org (shadow-only, propose-only — safe and continues generating data); explicitly record that SAFE-01 for any CUSTOMER org remains gated on this measurement and is NOT enabled here. Do NOT enable any other org. Do NOT wire any auto-apply.
  </action>
  <verify>
    <automated>node scripts/shadow-precision-eval.ts --score ${ANDREW_ORG_ID}</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one org has enabled event_resolution post-run, and it is the confirmed Andrew org (scope assertion output recorded in SUMMARY — T-32-04).
    - The sweep produced real event_match_decisions proposals for that org (count recorded); the worksheet + JSON label set exist.
    - A false-merge rate (false / total-proposed), per tier and overall, is recorded against the 0.1% target. If it exceeds 0.1%, the SUMMARY states SAFE-01 stays disabled for customers and flags the tuning follow-up (thresholds locked in 32-02) — do NOT enable customers regardless.
    - No customer org flag was enabled; recordings.event_id untouched (propose-only, shadow).
  </acceptance_criteria>
  <done>event_resolution is enabled for exactly Andrew's org (scope-asserted), the sweep produced real proposals, and a per-tier + overall false-merge rate is recorded against the 0.1% target with the hand-label worksheet as evidence — no customer org enabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flag enable -> production sweep on real data | First-ever event_resolution enablement in this milestone; must be scoped to exactly Andrew's org |
| precision scorer -> event_match_decisions (prod read) | Read-only; must never mutate the ledger or recordings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Information Disclosure / Tampering | Flag silently enabled for more than Andrew's org (customer data drawn into the sweep) | mitigate | Scope assertion after enable: exactly {Andrew org_id} enabled or STOP; light human gate confirms the specific org is not a customer; per Andrew's locked decision only his own org is used. |
| T-32-04b | Information Disclosure | Scorer writes to / mutates prod | mitigate | Read-only-by-construction script; grep gate proves no insert/update/upsert/delete; requires explicit org id for --score. |
| T-32-01 | Tampering | Sweep applying merges while measuring | mitigate | Shadow mode is propose-only (Plan 02 MATCH-03 + Phase-31 SAFE-02); recordings.event_id stays NULL — asserted post-run. |
| T-32-06 | Information Disclosure | Using a customer org for the label set | mitigate | Locked decision: Andrew's own org/account only, never a customer's data, never a synthetic org. |
</threat_model>

<verification>
- Scope assertion output: exactly one enabled event_resolution row = Andrew's org.
- Recorded false-merge rate vs 0.1% target, per tier + overall, with the hand-label worksheet/JSON as evidence.
- recordings.event_id for Andrew's org still NULL after the sweep (propose-only proof).
- SAFE-06 evidence recorded BEFORE any customer org is enabled (none is, this phase).
</verification>

<success_criteria>
- SAFE-06: shadow precision measured against a hand-labeled set from Andrew's own org, false-merge rate recorded against the <=0.1% target, before SAFE-01 is enabled for any customer org.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 05)
- `scripts/shadow-precision-eval.ts` — read-only org finder + precision scorer (reusable for future precision checks).
- Enabled event_resolution flag for Andrew's org only (shadow, propose-only).
- Hand-labeled proposal worksheet + JSON + recorded false-merge rate (SAFE-06 evidence) in the phase folder / SUMMARY.
</artifacts>

<output>
Create `.planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-05-SUMMARY.md` when done. Record the confirmed org_id, the scope assertion, the proposal counts per tier, and the final false-merge rate vs the 0.1% target.
</output>
