---
phase: 32-match-rule-hardening-provider-agnostic-matcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/dedup-fingerprint.ts
  - supabase/functions/_shared/__tests__/dedup-fingerprint.test.ts
  - supabase/functions/_shared/event-resolver.ts
  - supabase/functions/_shared/__tests__/event-resolver.test.ts
autonomous: true
requirements: [MATCH-04, MATCH-05]
user_setup: []

must_haves:
  truths:
    - "Two instances of a recurring meeting (same title + participants, zero time overlap) are NOT reported as a match by checkMatch"
    - "A genuine same-event cross-source pair with real (nonzero) time overlap is still reported as a match"
    - "A title that recurs above the occurrence threshold is flagged as suppressible so it cannot act as match evidence"
    - "The recurring_call_titles view runs with security_invoker=true in the live database (no RLS-bypass regression)"
  artifacts:
    - path: "supabase/functions/_shared/dedup-fingerprint.ts"
      provides: "checkMatch hardened with a mandatory nonzero-time-overlap gate (MATCH-04)"
      contains: "timeOverlap"
    - path: "supabase/functions/_shared/__tests__/dedup-fingerprint.test.ts"
      provides: "RED-first unit proof of the F5 fix"
    - path: "supabase/functions/_shared/event-resolver.ts"
      provides: "shouldSuppressTitleSignal predicate + occurrence threshold constant (MATCH-05)"
      contains: "shouldSuppressTitleSignal"
  key_links:
    - from: "checkMatch"
      to: "timeOverlap <= 0 rejection"
      via: "hard gate independent of the 2-of-3 criteriaMetCount"
      pattern: "timeOverlap\\s*<=\\s*0"
---

<objective>
Close the live F5 false-merge bug inside `checkMatch` (MATCH-04) and add the pure recurring-title suppression primitive (MATCH-05) that the Phase-32 metadata tier (Plan 02) will consume. This plan ships only shared-module + test changes; nothing reaches production until Plan 04's guarded apply.

Purpose: F5 is a LIVE production bug — `checkMatch`'s 2-of-3 rule (title >=0.80 OR time >=0.50 OR participants >=0.60, any two) never requires time to be one of the two, so two occurrences of the same recurring meeting (identical title + participants, zero time overlap) merge today on every Zoom webhook delivery. MATCH-04 closes it inside the function so the fix lands in the existing caller with no caller edit.
Output: A hardened `checkMatch`, its first-ever dedicated test file, a reusable title-suppression predicate in `event-resolver.ts`, and a verified/repaired `recurring_call_titles` view.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-CONTEXT.md
@.planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-RESEARCH.md

<read_first>
- supabase/functions/_shared/dedup-fingerprint.ts (the whole file; the bug is in `checkMatch`, lines ~246-296, and `calculateTimeOverlap`, ~178-203)
- supabase/functions/_shared/event-resolver.ts (add the suppressor here; mirror the file's fail-closed, DB-free-primitive conventions)
- supabase/functions/_shared/__tests__/event-resolver.test.ts (extend; follow its describe/it + Vitest import style)
- supabase/migrations/20260310125000_migrate_call_recording_id_to_uuid.sql lines 509-527 (the LAST recurring_call_titles redefinition — note it does NOT restate `WITH (security_invoker = true)`)
- supabase/migrations/20260305000001_fix_view_rls_bypass.sql lines 93-100 (the hotfix that originally added security_invoker to this exact view)
</read_first>

<interfaces>
Current `checkMatch` verdict logic (dedup-fingerprint.ts, to be hardened):
- Computes `titleSimilarity`, `timeOverlap`, `participantOverlap` (each 0..1).
- `titleMet = titleSimilarity >= 0.80`, `timeMet = timeOverlap >= 0.50`, `participantsMet = participantOverlap >= 0.60`.
- BUG: `isMatch = [titleMet,timeMet,participantsMet].filter(Boolean).length >= 2` — time is never required.
- `calculateTimeOverlap(start1,dur1,start2,dur2)` returns `overlapDuration / shortestMeeting`, and returns exactly `0` when the windows do not overlap.

Live `recurring_call_titles` view Row shape (src/types/supabase.ts Views): `{ user_id, title, occurrence_count, first_occurrence, last_occurrence, current_tags }`. It already reads `recordings` (provider-agnostic) keyed by `owner_user_id`/`title` — no migration needed to USE it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden checkMatch with a mandatory nonzero-time-overlap gate (MATCH-04)</name>
  <files>supabase/functions/_shared/dedup-fingerprint.ts, supabase/functions/_shared/__tests__/dedup-fingerprint.test.ts</files>
  <read_first>
    - supabase/functions/_shared/dedup-fingerprint.ts (full)
    - supabase/functions/_shared/__tests__/event-resolver.test.ts (test style, Vitest import from '../<module>.ts')
  </read_first>
  <behavior>
    Write these as failing tests FIRST (RED) against the current buggy behavior, then implement the gate (GREEN):
    - RED anchor: two fingerprints with identical normalized title and identical participant_emails but time windows that do NOT overlap (e.g. start_time_bucket 2026-09-01T10:00 dur 60, vs 2026-09-02T10:00 dur 60 -> timeOverlap === 0) currently return is_match=true; after the fix they MUST return is_match=false. This is the exact F5 recurring-meeting shape.
    - Preserved: two fingerprints with a real overlap (timeOverlap > 0) AND title+participants met still return is_match=true.
    - Necessary-not-sufficient: nonzero time overlap alone (timeOverlap just above 0 but titleMet and participantsMet both false) returns is_match=false — the gate does not by itself force a match.
    - Reporting intact: details.time_overlap / title_similarity / participant_overlap and criteria_met still reflect the real computed values regardless of the gate.
    - Boundary: timeOverlap exactly 0 with all three thresholds otherwise met returns is_match=false (the gate is absolute).
  </behavior>
  <action>
    Add the new test file supabase/functions/_shared/__tests__/dedup-fingerprint.test.ts covering the five behaviors above (this module has no test file today — this is Wave 0 scaffolding required by 32-VALIDATION.md). Import from '../dedup-fingerprint.ts', mirroring event-resolver.test.ts's plain-TS-module import convention. Then modify `checkMatch` in dedup-fingerprint.ts: keep all three similarity computations and the `criteria_met`/`details` reporting unchanged, but compute `isMatch` as `timeOverlap > 0 && criteriaMetCount >= 2` — i.e. a candidate with `timeOverlap <= 0` can NEVER match, independent of the 2-of-3 count. This is MATCH-04's locked "mandatory nonzero time-overlap guard, inside the function, not at the caller." Do NOT touch the caller (zoom-webhook/index.ts), do NOT touch `findPotentialDuplicates`' +-24h candidate window (that is a selection filter, not a validity guard — anti-pattern per 32-RESEARCH.md), and do NOT change `MATCH_THRESHOLDS.time_overlap` (0.50 stays the 2-of-3 threshold; the new gate is a separate strictly-greater-than-zero requirement). Add a code comment tying the gate to MATCH-04 / F5.
  </action>
  <verify>
    <automated>npx vitest run supabase/functions/_shared/__tests__/dedup-fingerprint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The new test file exists and all cases pass; the zero-time-overlap recurring-meeting case is proven to flip from is_match=true (pre-fix, demonstrated by the RED commit) to is_match=false.
    - `grep -n "timeOverlap" supabase/functions/_shared/dedup-fingerprint.ts` shows the gate `timeOverlap > 0` (or `timeOverlap <= 0` early rejection) guarding `isMatch`.
    - zoom-webhook/index.ts is byte-unchanged (`git diff --stat` shows no edit to it).
  </acceptance_criteria>
  <done>checkMatch rejects zero-time-overlap candidates regardless of the 2-of-3 count; the new dedup-fingerprint.test.ts passes RED->GREEN; the caller is unedited.</done>
</task>

<task type="auto">
  <name>Task 2: Add recurring-title suppression primitive + verify the live recurring_call_titles view (MATCH-05)</name>
  <files>supabase/functions/_shared/event-resolver.ts, supabase/functions/_shared/__tests__/event-resolver.test.ts</files>
  <read_first>
    - supabase/functions/_shared/event-resolver.ts (extend; keep it DB-free and fail-closed like extractTier1Signal)
    - supabase/migrations/20260310125000_migrate_call_recording_id_to_uuid.sql lines 509-527
    - supabase/migrations/20260305000001_fix_view_rls_bypass.sql lines 93-100
  </read_first>
  <action>
    Part A (pure primitive): In event-resolver.ts export a constant `RECURRING_TITLE_OCCURRENCE_THRESHOLD` (default 3 — the count of same-(owner,title) recordings at or above which a title is considered recurring, per F10) and a pure predicate `shouldSuppressTitleSignal(occurrenceCount: number | null | undefined, threshold = RECURRING_TITLE_OCCURRENCE_THRESHOLD): boolean` that returns true iff `occurrenceCount` is a finite number `>= threshold`, and treats null/undefined/NaN/negative as NOT recurring (returns false) so a missing occurrence count never suppresses a real signal by accident. Do NOT read the DB in this function — the metadata tier (Plan 02) supplies the count from the recurring_call_titles view. Add unit tests in event-resolver.test.ts: below threshold -> false; exactly at threshold -> true; above -> true; null/undefined/0/negative/NaN -> false; a custom threshold argument is honored.

    Part B (live view verification, MATCH-05 substrate integrity): The last redefinition of recurring_call_titles (migration 20260310125000, line 515) did NOT restate `WITH (security_invoker = true)`, so the live view may have silently regressed to running as owner — the exact RLS-bypass class that 20260305000001 already had to hotfix once for this same view. Query the live database (read-only) to confirm the view's current security_invoker setting: `select relname, reloptions from pg_class where relname = 'recurring_call_titles';` (or `select * from pg_views where viewname='recurring_call_titles';` plus a reloptions check). If `security_invoker=true` is present, author NOTHING — record "verified security_invoker=true, no regression" in the SUMMARY. If it is ABSENT, author a corrective migration `supabase/migrations/2026090200000X_recurring_call_titles_security_invoker.sql` that runs `ALTER VIEW recurring_call_titles SET (security_invoker = true);` (idempotent; no view-body change), apply it to TEST only in this plan, and note it for Plan 04's prod apply. Use the next unused timestamp after inspecting existing migration filenames.
  </action>
  <verify>
    <automated>npx vitest run supabase/functions/_shared/__tests__/event-resolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "shouldSuppressTitleSignal" supabase/functions/_shared/event-resolver.ts` shows the exported predicate and `RECURRING_TITLE_OCCURRENCE_THRESHOLD`.
    - New suppression unit tests pass, including the null/undefined/0/negative -> false cases.
    - The live security_invoker state of recurring_call_titles is recorded in the SUMMARY with the exact query output; if a corrective migration was authored it exists, is idempotent, applies cleanly to TEST, and is listed for Plan 04.
  </acceptance_criteria>
  <done>shouldSuppressTitleSignal + threshold exported and unit-tested; the live recurring_call_titles security_invoker state is verified (and a corrective TEST-applied migration authored iff it regressed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zoom webhook -> checkMatch (live prod) | Untrusted meeting metadata drives an auto-applied merge verdict today; the F5 gate is the correctness boundary |
| authenticated user -> recurring_call_titles view | If the view lost security_invoker, every user reads every other user's title aggregation (cross-user disclosure) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Information Disclosure | `checkMatch` F5 false-merge (recurring meetings) | mitigate | Mandatory `timeOverlap > 0` gate independent of the 2-of-3 count; RED-first test pins the exact recurring-meeting shape. NOTE: fix is inert until Plan 04 deploys dedup-fingerprint.ts to prod (bundled into zoom-webhook + zoom-sync-meetings). |
| T-32-05 | Information Disclosure | `recurring_call_titles` view RLS bypass (security_invoker dropped by 20260310125000) | mitigate | Verify live reloptions; if regressed, `ALTER VIEW ... SET (security_invoker = true)` corrective migration (Pitfall 5). Fix applied TEST-only here, prod in Plan 04. |
| T-32-06b | Tampering | Historical F5 merges already applied in prod | accept | `zoom_raw_calls.is_primary` has ZERO read sites in `src/` (verified) so a past false-merge does not hide a call from any UI — no data loss, contained to zoom_raw_calls state. Remediation of historical rows is out of the milestone's forward-only scope; an optional read-only audit query is offered in Plan 04 for Andrew's knowing decision. |
</threat_model>

<verification>
- `npx vitest run supabase/functions/_shared/__tests__/dedup-fingerprint.test.ts supabase/functions/_shared/__tests__/event-resolver.test.ts` green.
- Full unit suite `npx vitest run` shows no new failures (existing event-resolver tier-1 tests still pass — the suppressor is additive).
- `git diff --stat` confirms zoom-webhook/index.ts and zoom-sync-meetings/index.ts are unedited (MATCH-06 preserve-don't-delete).
</verification>

<success_criteria>
- MATCH-04: the zero-time-overlap recurring-meeting pair is rejected by checkMatch, proven by test inside the function.
- MATCH-05: a reusable `shouldSuppressTitleSignal` predicate + threshold exist and are unit-tested; the live view's RLS posture is verified (and repaired if regressed).
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 01)
- Hardened `checkMatch` (nonzero-time-overlap gate) in `supabase/functions/_shared/dedup-fingerprint.ts` — NOT yet deployed (Plan 04).
- New unit test file `supabase/functions/_shared/__tests__/dedup-fingerprint.test.ts`.
- `shouldSuppressTitleSignal` + `RECURRING_TITLE_OCCURRENCE_THRESHOLD` in `supabase/functions/_shared/event-resolver.ts`, consumed by Plan 02.
- Extended `supabase/functions/_shared/__tests__/event-resolver.test.ts`.
- CONDITIONAL: `supabase/migrations/2026090200000X_recurring_call_titles_security_invoker.sql` (only if the live view regressed) — TEST-applied here, prod-applied in Plan 04.
</artifacts>

<output>
Create `.planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-01-SUMMARY.md` when done. Record the live recurring_call_titles security_invoker query output verbatim.
</output>
