---
plan: 36-06
phase: 36
title: BUG-05, BUG-06, BUG-07 — Import UI Wiring
wave: 3
depends_on: []
files_modified:
  - src/components/panes/ImportSourcePane.tsx
  - src/pages/ImportPage.tsx
  - src/components/import/ImportSourceGrid.tsx
  - src/components/import/AddImportSourceDialog.tsx
  - src/components/import/ImportHistoryPanel.tsx
requirements: [BUG-05, BUG-06, BUG-07]
autonomous: true
---

# Plan 36-06: Import UI Wiring (BUG-05, BUG-06, BUG-07)

## Goal

The Import page has working entry points for:
- **BUG-05** Manual paste / file upload — accessible from the Import dashboard, not buried
- **BUG-06** "Import History" button — opens a real Import History view (not just FailedImportsSection)
- **BUG-07** "+" button at top of Import Source Manager — opens an "Add Import Source" dialog where the user picks Fathom / Zoom / YouTube / File Upload / Paste

## Current State (audit findings)

`src/pages/ImportPage.tsx` already imports `PasteTranscriptModal` and wires `pasteModalOpen` state. The "Save Transcript" button exists on the no-source-selected overview view (line 138) BUT is only visible when no source is selected and is positioned as a floating overlay — easy to miss.

`src/components/panes/ImportSourcePane.tsx` already has:
- The "+" button (line 86-92) — `aria-label="Add integration"` — but NO `onClick` handler (BUG-07 confirmed)
- "Import History" entry in `SECONDARY_NAV` (line 58) that DOES select `'import-history'` and renders the `FailedImportsSection` (line 230-243 of ImportPage.tsx) — works in part but only shows failed imports, not full import history (BUG-06 partial fix needed)

## Tasks

<task id="36-06-T1">
<read_first>
- src/components/panes/ImportSourcePane.tsx (full file — find the "+" button at line 86)
- src/pages/ImportPage.tsx (find existing handlers and modal state)
</read_first>
<action>
**BUG-07 fix.** Wire the "+" button in `ImportSourcePane.tsx`:

1. Add an `onAddSource?: () => void` prop to `ImportSourcePaneProps`
2. Add `onClick={onAddSource}` to the "+" button (line 86-92)
3. In `ImportPage.tsx`, pass `onAddSource={() => setAddSourceDialogOpen(true)}` and add `const [addSourceDialogOpen, setAddSourceDialogOpen] = useState(false)`
4. Create new component `src/components/import/AddImportSourceDialog.tsx` — a Radix Dialog with a grid of source choices (Fathom, Zoom, YouTube, File Upload, Paste Transcript). Each tile, when clicked, closes the dialog and either: (a) for Fathom/Zoom: invokes the existing `connectFathom` / `connectZoom` flows, (b) for YouTube/File Upload: sets `selectedSource` to the respective ID, (c) for Paste: opens `pasteModalOpen`
5. The dialog must include `DialogDescription` (per BUG-09 sibling plan)
6. Render `<AddImportSourceDialog open={addSourceDialogOpen} onOpenChange={setAddSourceDialogOpen} ...>` inside `ImportPage.tsx`
</action>
<acceptance_criteria>
- `src/components/panes/ImportSourcePane.tsx` "+" button has an `onClick` prop bound to `onAddSource`
- `src/components/import/AddImportSourceDialog.tsx` exists
- The dialog component contains `<DialogDescription>`
- `ImportPage.tsx` renders `<AddImportSourceDialog>`
- Clicking "+" opens the dialog (dev-browser verification in T4)
- Clicking a tile inside the dialog triggers the correct flow
- `npm run typecheck` exits 0
</acceptance_criteria>
</task>

<task id="36-06-T2">
<read_first>
- src/pages/ImportPage.tsx lines 230-243 (current "Import History" branch — renders FailedImportsSection)
- src/components/import/FailedImportsSection.tsx (to understand the existing pattern)
- src/services/import-sources.service.ts (look for an import_history or sync_log query helper)
- supabase migrations directory — look for `import_history`, `sync_log`, or `import_runs` tables
</read_first>
<action>
**BUG-06 fix.** Build a real Import History panel.

1. Check if `import_history` / `sync_log` / similar table exists in `supabase/migrations/`. If yes, use it. If NO such table exists, we need a lightweight aggregation: query `recordings` grouped by `synced_at::date` + `source_app` and project counts. This is a quick win that gives the user something useful while a dedicated audit-log table is BACKLOG.
2. Create `src/components/import/ImportHistoryPanel.tsx` — a panel that lists past import runs. Each row: source app, date, # of calls imported, # failed, success/partial-success/failed badge.
3. Include the existing `FailedImportsSection` AT THE BOTTOM of this panel for continuity (don't lose existing functionality).
4. In `ImportPage.tsx` (line 230-243), replace `<FailedImportsSection />` with `<ImportHistoryPanel />` inside the `import-history` branch.
</action>
<acceptance_criteria>
- File `src/components/import/ImportHistoryPanel.tsx` exists
- The panel renders both an "Import Runs" list AND the FailedImportsSection
- The Import Runs list pulls from either `import_history` table (if exists) OR aggregates from `recordings`
- ImportPage.tsx "import-history" branch renders `<ImportHistoryPanel />` (not `<FailedImportsSection />` directly)
- `npm run typecheck` exits 0
- Dev-browser: clicking "Import History" in the secondary nav shows a richer panel than just FailedImportsSection
</acceptance_criteria>
</task>

<task id="36-06-T3">
<read_first>
- src/pages/ImportPage.tsx (full)
- src/components/import/ImportOverviewDashboard.tsx (if exists)
</read_first>
<action>
**BUG-05 fix.** Expose Manual Paste / Upload UI more prominently.

Currently `Save Transcript` is a floating button on the no-source-selected overview. Either:

(a) **Preferred**: Add `Paste Transcript` and `File Upload` as first-class entries in the `PRIMARY_SOURCES` list in `ImportSourcePane.tsx` (File Upload may already exist there — verify). If not in PRIMARY_SOURCES, add a `paste-transcript` entry with `RiClipboardLine` icon. Wire its click to set `selectedSource` to `'paste-transcript'`, and in `ImportPage.tsx` `renderPane3()`, add a `if (selectedSource === 'paste-transcript')` branch that auto-opens the `PasteTranscriptModal` and shows a placeholder "Paste a transcript" call-to-action below.

(b) **Acceptable alternate**: Make the floating "Save Transcript" button visible across ALL import detail views (not just the overview), so paste is always one click away from the Import page.

Pick (a). Add the `paste-transcript` source ID to `ImportSourceId` union type. Update the type union in `ImportSourcePane.tsx`.
</action>
<acceptance_criteria>
- `ImportSourceId` type union in `src/components/panes/ImportSourcePane.tsx` contains `'paste-transcript'`
- `PRIMARY_SOURCES` array contains a `paste-transcript` entry
- `ImportPage.tsx` has a branch for `selectedSource === 'paste-transcript'`
- Clicking the new entry opens (or shows a CTA to open) the `PasteTranscriptModal`
- `npm run typecheck` exits 0
</acceptance_criteria>
</task>

<task id="36-06-T4" type="verification">
<read_first>
- (no files — dev-browser verification)
</read_first>
<action>
Use dev-browser on `https://app.callvaultai.com`:

1. Sign in
2. Navigate to `/import`
3. **BUG-07**: Click the "+" button at top of Pane 2 → expect: AddImportSourceDialog opens
4. In the dialog, click "Paste Transcript" tile → expect: dialog closes AND PasteTranscriptModal opens
5. Close the modal. Click "+" again → click "Fathom" tile → expect: dialog closes AND a new tab opens for Fathom OAuth (`connectFathom` flow)
6. **BUG-06**: Close all modals/tabs. In Pane 2, click "Import History" → expect: a panel showing import runs (with date, source, count) AND the failed-imports section
7. **BUG-05**: In Pane 2, locate the "Paste Transcript" entry in PRIMARY_SOURCES → click it → expect: PasteTranscriptModal opens directly OR a CTA visible

Take a screenshot of each working interaction.
</action>
<acceptance_criteria>
- "+" button opens AddImportSourceDialog
- Tile clicks inside the dialog route correctly (Paste → modal; Fathom → OAuth tab)
- "Import History" button shows a real Import History panel
- "Paste Transcript" entry visible in PRIMARY_SOURCES and opens the modal
- Screenshots saved to `.planning/phases/36-critical-bug-sweep/screenshots/bug-05-06-07-{name}.png`
</acceptance_criteria>
</task>

## Verification

- `npm run typecheck` exits 0
- `npm run build` exits 0
- Dev-browser per T4
- Atomic commits per bug:
  - `fix(36-06): wire AddImportSourceDialog to "+" button [BUG-07]`
  - `fix(36-06): real ImportHistoryPanel replaces FailedImportsSection-only branch [BUG-06]`
  - `fix(36-06): paste-transcript primary source entry exposes manual flow [BUG-05]`

## Must-Haves (Goal-Backward)

1. Every Import button on the screen does the thing its label says.
2. Manual paste flow is reachable in <= 2 clicks from `/import`.
3. Import History is a real surface, not a stub.

## Out of Scope

- Dedicated `import_history` table with full retry/dedupe semantics (BACKLOG — v2.3)
- Bulk paste of multiple transcripts at once (BACKLOG)
- Drag-and-drop transcript upload (already exists via FileUploadDropzone — out of scope)
