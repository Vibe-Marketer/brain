---
phase: 08-full-suite-test-recovery
plan: 06
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
  - 08-05
files_modified: []
# Gate-only plan — no new file modifications in this plan itself.
# The 5 files committed in Task 1 were produced by Wave 1 plans (08-01 through 08-05).
autonomous: false
requirements:
  - SC-6
must_haves:
  truths:
    - "npm test exits 0 with 0 failing tests"
    - "npm run type-check exits 0, or any non-zero exit is due to pre-existing errors not introduced by this phase (documented in SUMMARY)"
    - "npm run build exits 0"
    - "All 5 previously-failing test files now pass"
  artifacts: []
  key_links: []
---

<objective>
Run the full quality gate (npm test, npm run type-check, npm run build) after all 5 Wave 1 fixes complete. Human verifies the green output and commits.

Purpose: Confirms that all 5 targeted fixes together produce a clean test suite with no regressions introduced. This is the gate task for Phase 8 completion.

Output: Green npm test, green type-check, green build. Committed to main.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-full-suite-test-recovery/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full quality gate — npm test, type-check, build</name>
  <files></files>
  <read_first>
    - .planning/phases/08-full-suite-test-recovery/08-01-SUMMARY.md
    - .planning/phases/08-full-suite-test-recovery/08-02-SUMMARY.md
    - .planning/phases/08-full-suite-test-recovery/08-03-SUMMARY.md
    - .planning/phases/08-full-suite-test-recovery/08-04-SUMMARY.md
    - .planning/phases/08-full-suite-test-recovery/08-05-SUMMARY.md
  </read_first>
  <action>
    Run these three commands in order, capturing their exit codes:

    1. npm test  (runs Vitest unit suite — exclude integration tests)
    2. npm run type-check
    3. npm run build

    If npm test reports any failures, identify which test file is failing and diagnose whether it is:
    (a) one of the 5 files this phase targeted — indicates the Wave 1 fix for that file was incomplete
    (b) a different test file — indicates a regression introduced by one of the Wave 1 fixes (unlikely since all Wave 1 plans only touch test files, not production code)

    For (a): apply the additional fix inline using the RESEARCH.md guidance as reference.
    For (b): investigate the specific regression before proceeding.

    If npm run type-check exits non-zero with errors unrelated to the 5 test files (e.g., pre-existing type errors not introduced by this phase), document them in the SUMMARY and continue — this phase only owns SC-1 through SC-5. Do NOT block completion on pre-existing type errors.

    If npm run build fails for any reason unrelated to the test changes (e.g., a pre-existing build error), note it in the SUMMARY.

    After all three commands are green (or type-check non-zero is documented as pre-existing), commit:
      git add supabase/functions/_shared/__tests__/connector-function-utils.test.ts
      git add src/lib/__tests__/mcp-tool-categories.test.ts
      git add src/components/settings/__tests__/MCPTab.permissions.test.tsx
      git add src/components/settings/__tests__/IntegrationsTab.test.tsx
      git add src/components/connectors/registry/adapters/__tests__/fathom.test.ts
      git commit -m "test(08): restore full Vitest suite — 5 test files fixed"
      git push origin main
  </action>
  <verify>
    <automated>cd /Users/admin/dev/brain && npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with 0 test failures (integration tests remain excluded per vitest.config.ts)
    - The 5 previously-failing files all show as PASS in the test output:
        PASS supabase/functions/_shared/__tests__/connector-function-utils.test.ts
        PASS src/lib/__tests__/mcp-tool-categories.test.ts
        PASS src/components/settings/__tests__/MCPTab.permissions.test.tsx
        PASS src/components/settings/__tests__/IntegrationsTab.test.tsx
        PASS src/components/connectors/registry/adapters/__tests__/fathom.test.ts
    - `npm run type-check` exits 0, OR any non-zero exit is documented in SUMMARY as pre-existing errors not introduced by Wave 1 changes
    - `npm run build` exits 0
    - All 5 changed test files are committed to main in a single commit with message starting "test(08):"
  </acceptance_criteria>
  <done>All 5 failing test files pass; full quality gate green; changes committed and pushed to main</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All 5 Wave 1 test fixes applied. The full npm test suite, type-check, and build have been run and committed to main.</what-built>
  <how-to-verify>
    1. Run: npm test
       Expected: exits 0, all 5 previously-failing files show PASS
    2. Run: npm run type-check
       Expected: exits 0 (or pre-existing errors only — check SUMMARY for documentation)
    3. Run: npm run build
       Expected: exits 0
    4. Confirm commit is on main: git log --oneline -3
  </how-to-verify>
  <resume-signal>Type "approved" if all three commands are green (or type-check non-zero is pre-existing and documented), or describe any remaining failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate | Quality gate confirming no regressions from test-only changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-SC | Tampering | npm/git operations | accept | Only test files changed; build verifies no production code broken |
</threat_model>

<verification>
Full quality gate:
  npm test         → 0 failures, all 5 target files PASS
  npm run type-check → exits 0 (or pre-existing errors documented in SUMMARY)
  npm run build    → exits 0
</verification>

<success_criteria>
Phase 8 complete when:
- npm test exits 0 with zero failures
- All 5 files (connector-function-utils, mcp-tool-categories, MCPTab.permissions, IntegrationsTab, fathom) show PASS
- npm run type-check exits 0 or non-zero exit documented as pre-existing in SUMMARY
- npm run build exits 0
- Changes committed to main
</success_criteria>

<output>
Create `.planning/phases/08-full-suite-test-recovery/08-06-SUMMARY.md` when done
</output>
