---
phase: 08-full-suite-test-recovery
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/connectors/registry/adapters/__tests__/fathom.test.ts
autonomous: true
requirements:
  - SC-5
must_haves:
  truths:
    - "fathom.test.ts deep-equality assertions include syncState, recordingUuid, localTitle, remoteTitle"
    - "All existing fathom adapter tests pass"
  artifacts:
    - path: src/components/connectors/registry/adapters/__tests__/fathom.test.ts
      provides: "Fathom adapter tests with 4 new normalized fields in fixture"
      contains: "syncState: \"imported\""
  key_links:
    - from: src/components/connectors/registry/adapters/__tests__/fathom.test.ts
      to: src/components/connectors/registry/adapters/fathom.ts
      via: fathomAdapter.searchAvailable call
      pattern: "fathomAdapter"
---

<objective>
Add the 4 missing normalized fields (syncState, recordingUuid, localTitle, remoteTitle) to the two item fixtures inside the expect(result).toEqual() block in fathom.test.ts.

Purpose: The mapItem function in fathom.ts was extended to return 4 new fields. The test fixture was never updated, so the deep-equal check fails with extra keys in the actual output. The source is correct — only the test fixture needs the 4 missing fields.

Output: Both item fixtures updated with the correct derived field values; the test passes.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-full-suite-test-recovery/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 normalized fields to both item fixtures in fathom.test.ts</name>
  <files>src/components/connectors/registry/adapters/__tests__/fathom.test.ts</files>
  <read_first>
    - src/components/connectors/registry/adapters/__tests__/fathom.test.ts (full file — locate the expect(result).toEqual block with the two item objects, around lines 66–88)
    - src/components/connectors/registry/adapters/fathom.ts (confirm mapItem returns syncState/recordingUuid/localTitle/remoteTitle and the derivation logic: syncState = m.sync_state ?? (wasAlreadySynced(m) ? "imported" : "available"))
  </read_first>
  <action>
    In the "requests paged Fathom meetings and normalizes them for the import wizard" test, inside the expect(result).toEqual({ nextCursor: ..., items: [ ... ] }) block:

    For item 1 (externalId: "123", the synced one — mock has `synced: true`, no `sync_state` field):
    Add these 4 fields to the item object, after `externalUrl: "https://fathom.video/share/123"`:
      syncState: "imported",
      recordingUuid: null,
      localTitle: null,
      remoteTitle: null,

    Derivation rationale for syncState: mock item 1 has no `sync_state` field, so the null-coalesce falls through to the wasAlreadySynced check. `synced: true` → wasAlreadySynced returns true → "imported".

    For item 2 (externalId: "456", the not-synced one — mock has `synced: false`, no `sync_state` field):
    Add these 4 fields to the item object, after `externalUrl: "https://fathom.video/calls/456"`:
      syncState: "available",
      recordingUuid: null,
      localTitle: null,
      remoteTitle: null,

    Derivation rationale for syncState: mock item 2 has no `sync_state` field, `synced: false` → wasAlreadySynced returns false → "available".

    Do NOT change any other tests in the file. The "throws Supabase invoke errors" test and any other tests remain unchanged.
  </action>
  <verify>
    <automated>cd /Users/admin/dev/brain && npx vitest run src/components/connectors/registry/adapters/__tests__/fathom.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "syncState: \"imported\"" src/components/connectors/registry/adapters/__tests__/fathom.test.ts` returns 1
    - `grep -c "syncState: \"available\"" src/components/connectors/registry/adapters/__tests__/fathom.test.ts` returns 1
    - `grep -c "recordingUuid: null" src/components/connectors/registry/adapters/__tests__/fathom.test.ts` returns 2 (once per item)
    - `grep -c "localTitle: null" src/components/connectors/registry/adapters/__tests__/fathom.test.ts` returns 2
    - `grep -c "remoteTitle: null" src/components/connectors/registry/adapters/__tests__/fathom.test.ts` returns 2
    - `npx vitest run src/components/connectors/registry/adapters/__tests__/fathom.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>fathom.test.ts deep-equality assertions include all 4 normalized fields; all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → adapter | No auth boundary; fixture update only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-SC | Tampering | test fixture update | accept | Test-only change; no production code touched; no npm installs |
</threat_model>

<verification>
Run targeted test file:
  npx vitest run src/components/connectors/registry/adapters/__tests__/fathom.test.ts

Grep for new fields:
  grep -c "syncState" src/components/connectors/registry/adapters/__tests__/fathom.test.ts
  (expected: 2)
</verification>

<success_criteria>
- `npx vitest run src/components/connectors/registry/adapters/__tests__/fathom.test.ts` exits 0
- Both item fixtures contain syncState, recordingUuid, localTitle, remoteTitle
- Item 1 has syncState: "imported"; item 2 has syncState: "available"
</success_criteria>

<output>
Create `.planning/phases/08-full-suite-test-recovery/08-05-SUMMARY.md` when done
</output>
