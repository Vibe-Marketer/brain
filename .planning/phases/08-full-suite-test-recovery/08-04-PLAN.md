---
phase: 08-full-suite-test-recovery
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/__tests__/IntegrationsTab.test.tsx
autonomous: true
requirements:
  - SC-3
must_haves:
  truths:
    - "IntegrationsTab.test.tsx mounts without 'useAuth must be used within AuthProvider' error"
    - "All existing connector-panel and section-header assertions still pass"
  artifacts:
    - path: src/components/settings/__tests__/IntegrationsTab.test.tsx
      provides: "IntegrationsTab unit tests with useApiTokens properly mocked"
      contains: "vi.mock('@/hooks/useApiTokens'"
  key_links:
    - from: src/components/settings/__tests__/IntegrationsTab.test.tsx
      to: src/hooks/useApiTokens.ts
      via: vi.mock stub added before IntegrationsTab import
      pattern: "vi.mock.*useApiTokens"
---

<objective>
Fix IntegrationsTab.test.tsx by adding the missing vi.mock for @/hooks/useApiTokens so ApiTokensSection can mount without triggering the useAuth requirement.

Purpose: IntegrationsTab renders ApiTokensSection, which calls useApiTokensList from @/hooks/useApiTokens. That hook calls useAuth() at line 34 of useApiTokens.ts. The test already mocks useOrganizations (which also calls useAuth) but misses useApiTokens. Adding the mock for useApiTokens (providing all three exports the section uses) unblocks the render.

Output: IntegrationsTab mounts successfully; all connector-panel and adapter tests pass.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-full-suite-test-recovery/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useApiTokens mock in IntegrationsTab.test.tsx before the IntegrationsTab import</name>
  <files>src/components/settings/__tests__/IntegrationsTab.test.tsx</files>
  <read_first>
    - src/components/settings/__tests__/IntegrationsTab.test.tsx (full file — identify the line where `import IntegrationsTab` appears; the new mock must go before it)
    - src/hooks/useApiTokens.ts (confirm the three hook names exported: useApiTokensList, useGenerateApiToken, useRevokeApiToken)
  </read_first>
  <action>
    Make exactly 1 change to src/components/settings/__tests__/IntegrationsTab.test.tsx:

    Insert this vi.mock call in the mock block, BEFORE the `import IntegrationsTab from '../IntegrationsTab';` line. Place it after the last existing vi.mock block and before the component import:

      vi.mock('@/hooks/useApiTokens', () => ({
        useApiTokensList: () => ({ tokens: [], isLoading: false, error: null }),
        useGenerateApiToken: () => ({ mutate: vi.fn(), isPending: false }),
        useRevokeApiToken: () => ({ mutate: vi.fn(), isPending: false }),
      }));

    Do NOT change any other lines. All existing mocks (supabase client, auth-utils, sonner, DefaultDestinationBar, useImportSources, useOrgContext, useWorkspaces, fathom adapter) stay exactly as they are.

    Vitest hoists vi.mock() calls to the top of the file; placing the mock call before the component import in source order is sufficient — it does not need to be the very first line in the file.
  </action>
  <verify>
    <automated>cd /Users/admin/dev/brain && npx vitest run src/components/settings/__tests__/IntegrationsTab.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vi.mock('@/hooks/useApiTokens'" src/components/settings/__tests__/IntegrationsTab.test.tsx` returns 1
    - `grep -c "useApiTokensList" src/components/settings/__tests__/IntegrationsTab.test.tsx` returns at least 1
    - `grep -c "useGenerateApiToken" src/components/settings/__tests__/IntegrationsTab.test.tsx` returns at least 1
    - `grep -c "useRevokeApiToken" src/components/settings/__tests__/IntegrationsTab.test.tsx` returns at least 1
    - `npx vitest run src/components/settings/__tests__/IntegrationsTab.test.tsx` exits 0 with no test failures
    - No "useAuth must be used within AuthProvider" error in test output
  </acceptance_criteria>
  <done>IntegrationsTab.test.tsx mounts without AuthProvider error and all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → hooks | No auth boundary; test-only mock addition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-SC | Tampering | test mock addition | accept | Test-only change; no production code touched; no npm installs |
</threat_model>

<verification>
Run targeted test file:
  npx vitest run src/components/settings/__tests__/IntegrationsTab.test.tsx

Confirm mock is present:
  grep -c "vi.mock('@/hooks/useApiTokens'" src/components/settings/__tests__/IntegrationsTab.test.tsx
  (expected: 1)
</verification>

<success_criteria>
- `npx vitest run src/components/settings/__tests__/IntegrationsTab.test.tsx` exits 0
- No AuthProvider error in output
- All connector-panel and Fathom-section assertions pass
</success_criteria>

<output>
Create `.planning/phases/08-full-suite-test-recovery/08-04-SUMMARY.md` when done
</output>
