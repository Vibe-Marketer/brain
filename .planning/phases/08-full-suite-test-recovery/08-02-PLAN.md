---
phase: 08-full-suite-test-recovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/__tests__/mcp-tool-categories.test.ts
autonomous: true
requirements:
  - SC-4
must_haves:
  truths:
    - "mcp-tool-categories.test.ts expects 45 tools total and 16 write tools"
    - "Byte-match assertion passes against the canonical sibling"
    - "All existing assertions for 17 read, 8 admin, 4 ai tools still pass"
  artifacts:
    - path: src/lib/__tests__/mcp-tool-categories.test.ts
      provides: "Updated count expectations matching live tool surface"
      contains: "toHaveLength(45)"
  key_links:
    - from: src/lib/__tests__/mcp-tool-categories.test.ts
      to: src/lib/mcp-tool-categories.ts
      via: TOOL_CATEGORIES import
      pattern: "from '@/lib/mcp-tool-categories'"
    - from: src/lib/__tests__/mcp-tool-categories.test.ts
      to: supabase/functions/_shared/mcp-tool-categories.ts
      via: readFileSync for byte-match
      pattern: "CANONICAL_PATH"
---

<objective>
Update the four stale count assertions in mcp-tool-categories.test.ts from the Phase 23 surface (41 tools / 12 write) to the current Phase 4 surface (45 tools / 16 write). The source files are already correct — only the test expectations need updating.

Purpose: Phase 4 added ingest_transcript, append_to_transcript, update_call_metadata, and set_speakers to the write category. The frontend mirror and canonical sibling both reflect 45 total / 16 write already. The test file was not updated.

Output: 4 assertion lines changed, all tests passing, byte-match still green.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-full-suite-test-recovery/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 4 stale count assertions in mcp-tool-categories.test.ts</name>
  <files>src/lib/__tests__/mcp-tool-categories.test.ts</files>
  <read_first>
    - src/lib/__tests__/mcp-tool-categories.test.ts (full file — identify exact line numbers for the 4 assertions to change)
    - src/lib/mcp-tool-categories.ts (confirm TOOL_CATEGORIES has 45 entries and TOOL_DESCRIPTIONS has 45 entries)
  </read_first>
  <action>
    Make exactly 4 line changes in src/lib/__tests__/mcp-tool-categories.test.ts:

    1. Line ~29 — `expect(Object.keys(TOOL_CATEGORIES)).toHaveLength(41)` → change 41 to 45
       (test: "contains exactly 41 entries" — title can stay unchanged)

    2. Line ~44 — `expect(writeTools).toHaveLength(12)` → change 12 to 16
       (test: "contains exactly 12 write tools")

    3. Line ~146 — `expect(Object.keys(TOOL_DESCRIPTIONS)).toHaveLength(41)` → change 41 to 45
       (test: "has 41 entries (one per tool)")

    4. Line ~222 — `expect(a.length).toBe(41)` → change 41 to 45
       (inside the 'TOOL_CATEGORIES values byte-match canonical sibling' test)

    Do NOT change any other lines. In particular:
    - The write-tool name assertions at lines ~87–103 are already correct (they list 12 original write tools; the 4 new tools are already in TOOL_CATEGORIES but tested via the toHaveLength count, not by explicit name in that block — if they happen to NOT be listed by name there, do NOT add them; the count assertion at line 2 above covers them)
    - The read/admin/ai count assertions (17/8/4) are already correct — do NOT touch them
    - The it.skip on TOOL_DESCRIPTIONS byte-match stays skipped
    - No production files are touched
  </action>
  <verify>
    <automated>cd /Users/admin/dev/brain && npx vitest run src/lib/__tests__/mcp-tool-categories.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "toHaveLength(41)\|toBe(41)" src/lib/__tests__/mcp-tool-categories.test.ts` returns 0 (no stale 41 count left)
    - `grep -c "toHaveLength(12)" src/lib/__tests__/mcp-tool-categories.test.ts` returns 0 (no stale 12 write count)
    - `grep -c "toHaveLength(45)" src/lib/__tests__/mcp-tool-categories.test.ts` returns 2 (for TOOL_CATEGORIES and TOOL_DESCRIPTIONS)
    - `grep -c "toBe(45)" src/lib/__tests__/mcp-tool-categories.test.ts` returns 1 (byte-match assertion)
    - `grep -c "toHaveLength(16)" src/lib/__tests__/mcp-tool-categories.test.ts` returns 1 (write tools count)
    - `npx vitest run src/lib/__tests__/mcp-tool-categories.test.ts` exits 0 with no failing tests
    - The it.skip for TOOL_DESCRIPTIONS byte-match remains skipped (not converted to active test)
  </acceptance_criteria>
  <done>All 4 stale count assertions updated to match the live 45-tool / 16-write surface; all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → source modules | No auth boundary; test-only change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-SC | Tampering | test expectation edit | accept | Test-only change; no production code touched; no npm installs |
</threat_model>

<verification>
Run targeted test file:
  npx vitest run src/lib/__tests__/mcp-tool-categories.test.ts

Grep check for stale counts:
  grep -c "toHaveLength(41)\|toBe(41)\|toHaveLength(12)" src/lib/__tests__/mcp-tool-categories.test.ts
  (expected: 0)
</verification>

<success_criteria>
- `npx vitest run src/lib/__tests__/mcp-tool-categories.test.ts` exits 0
- Zero assertions with stale 41 or 12 counts remain
- toHaveLength(45) appears twice and toBe(45) appears once
</success_criteria>

<output>
Create `.planning/phases/08-full-suite-test-recovery/08-02-SUMMARY.md` when done
</output>
