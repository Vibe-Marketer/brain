---
phase: 08-full-suite-test-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/__tests__/connector-function-utils.test.ts
autonomous: true
requirements:
  - SC-1
must_haves:
  truths:
    - "connector-function-utils.test.ts runs under Vitest without Deno.test"
    - "All 9 behavioral tests pass (sync id resolution, validation errors, date windows, workspace binding)"
  artifacts:
    - path: supabase/functions/_shared/__tests__/connector-function-utils.test.ts
      provides: "Vitest-native unit tests for shared connector utilities"
      contains: "import { describe, it, expect } from 'vitest'"
  key_links:
    - from: supabase/functions/_shared/__tests__/connector-function-utils.test.ts
      to: supabase/functions/_shared/connector-function-utils.ts
      via: direct import at file top
      pattern: "from.*connector-function-utils"
---

<objective>
Convert the hand-rolled Deno.test wrapper in connector-function-utils.test.ts to standard Vitest imports, making the file runnable by the project's existing Vitest 4.x test runner.

Purpose: The file defines its own describe/it/expect shims where `it` calls `Deno.test()`. The global Deno shim in src/test/setup.ts only stubs Deno.env — not Deno.test — so every test call throws at runtime. The behavioral tests themselves are correct; only the runner harness needs to change.

Output: connector-function-utils.test.ts using standard Vitest globals, all 9 tests passing.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-full-suite-test-recovery/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Deno.test harness with Vitest imports in connector-function-utils.test.ts</name>
  <files>supabase/functions/_shared/__tests__/connector-function-utils.test.ts</files>
  <read_first>
    - supabase/functions/_shared/__tests__/connector-function-utils.test.ts (current file — lines 1–91 are the harness to remove; lines 92–end are the tests to keep)
    - supabase/functions/_shared/__tests__/vtt-parser.test.ts (canonical working reference for Vitest pattern in _shared tests)
  </read_first>
  <action>
    Remove lines 12–90 (the inline describe/it/expect/assertDeepEqual/assertMatchObject/matchesObject hand-rolled wrappers) from connector-function-utils.test.ts.

    Replace the file header (before the first import) with a single Vitest import line:
      import { describe, it, expect } from 'vitest';

    The 9 test bodies starting at "describe('connector-function-utils sync id resolution'" (currently around line 92) stay exactly as-is — their test logic is correct. Only the runner shim lines above them are removed.

    For every assertion in the tests that uses the hand-rolled expect chain:
    - .resolves.toEqual(x) stays as-is — Vitest's native expect supports this syntax with await prefix; ensure all such calls are preceded by `await` (they already are in the test bodies)
    - .rejects.toBeInstanceOf(ErrorClass) stays as-is — same Vitest native support with await
    - .rejects.toThrow(message) stays as-is — Vitest native

    Do NOT change the createMockSupabase / MockQuery helpers (around lines 384–444 in the original file) — they are pure JavaScript with no Deno dependencies and require no changes.

    Do NOT change any import for the module under test:
      import { ConnectorRequestValidationError, getConnectorDateWindow, ... } from "../connector-function-utils.ts";
    Keep the .ts extension on the import (Deno-style import, valid for _shared/ files shimmed under Vitest).
  </action>
  <verify>
    <automated>cd /Users/admin/dev/brain && npx vitest run supabase/functions/_shared/__tests__/connector-function-utils.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `import { describe, it, expect } from 'vitest';` as the first non-comment line
    - No `Deno.test` call anywhere in the file (grep: `grep -c "Deno\.test" supabase/functions/_shared/__tests__/connector-function-utils.test.ts` returns 0)
    - No inline describe/it/expect function definitions (grep: `grep -c "^function describe\|^function it\|^function expect" supabase/functions/_shared/__tests__/connector-function-utils.test.ts` returns 0)
    - `npx vitest run supabase/functions/_shared/__tests__/connector-function-utils.test.ts` exits 0
    - Output shows 9 passing tests (or the count matches the test count in the file — at minimum the sync-id-resolution, validation-error, date-window, and workspace-binding describe blocks all pass)
    - createMockSupabase and MockQuery are still present in the file
  </acceptance_criteria>
  <done>connector-function-utils.test.ts runs under Vitest with all tests passing and no Deno.test references remaining</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → module under test | No auth boundary; test-only file change with zero production impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-SC | Tampering | test file edit | accept | Test-only change; no production code touched; no npm installs |
</threat_model>

<verification>
Run the targeted test file individually to confirm all tests pass:
  npx vitest run supabase/functions/_shared/__tests__/connector-function-utils.test.ts

Then confirm no Deno.test references remain:
  grep -c "Deno\.test" supabase/functions/_shared/__tests__/connector-function-utils.test.ts
  (expected: 0)
</verification>

<success_criteria>
- `npx vitest run supabase/functions/_shared/__tests__/connector-function-utils.test.ts` exits 0
- All tests in the file pass (no skips, no failures)
- `grep -c "Deno\.test"` on the file returns 0
</success_criteria>

<output>
Create `.planning/phases/08-full-suite-test-recovery/08-01-SUMMARY.md` when done
</output>
