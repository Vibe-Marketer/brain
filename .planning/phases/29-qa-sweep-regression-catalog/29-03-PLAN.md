---
phase: 29-qa-sweep-regression-catalog
plan: 03
type: execute
wave: 3
depends_on: [29-01]
files_modified:
  - .planning/phases/29-qa-sweep-regression-catalog/screenshots/
  - .planning/phases/29-qa-sweep-regression-catalog/29-03-PERSONA-B-SWEEP-NOTES.md
autonomous: true
requirements: [QA-01]
must_haves:
  truths:
    - "Persona B (fresh signup, target email soren@vibeos.com) has attempted the full email-signup → pricing → payment → onboarding flow on production"
    - "Every screen Persona B sees is captured as a screenshot — pricing page presence/absence, signup error text verbatim, payment gate enforcement, onboarding wizard entry, or the actual failure point if signup never completes"
    - "AUTH-01..05 each have a re-verification or no-repro tag in the notes file"
    - "If the soren@vibeos.com account already exists from prior testing, that fact is itself recorded as a finding (per D-01 edge case) and an alternative is documented but NOT actually created"
  artifacts:
    - path: ".planning/phases/29-qa-sweep-regression-catalog/29-03-PERSONA-B-SWEEP-NOTES.md"
      provides: "Structured Persona B observation log following same format as Plan 02 notes; documents every screen + error in the fresh-signup E2E"
      contains: "soren@vibeos.com"
    - path: ".planning/phases/29-qa-sweep-regression-catalog/screenshots/"
      provides: "Per-screen PNGs of the Persona B signup flow named persona-b-{seq}-{slug}.png"
  key_links:
    - from: "Production signup form"
      to: "AUTH-01..05 verification"
      via: "Persona B drives signup with soren@vibeos.com; observes each error/redirect/screen"
      pattern: "AUTH-0[1-5]"
    - from: "29-03-PERSONA-B-SWEEP-NOTES.md"
      to: "Plan 29-05"
      via: "Plan 05 consumes findings + Sweep Status values for AUTH-01..05"
      pattern: "\\[NEW\\]|\\[RE-VERIFY-AUTH-|\\[NO-REPRO-AUTH-"
---

<objective>
Persona B drives the fresh-signup end-to-end flow on production using `soren@vibeos.com` (the canonical free-tier canary named in REQUIREMENTS.md AUTH-03). The sweep documents every screen the new user sees — pricing page (AUTH-02), payment gate (AUTH-03), error messages (AUTH-04), onboarding wizard entry, or the actual failure point if signup never completes (AUTH-01 symptom). The output is a structured notes file Plan 29-05 consumes for AUTH-01..05 Sweep Status entries.

Purpose: Without running Persona B, the sweep cannot verify the highest-priority blocker (signup completely broken per CONTEXT.md highest priority). Owner-only sweeping would never touch the signup form. Persona B is the ONLY sweep arm that exercises the front-door customer flow that determines whether new customers can use the product at all.

Output: One structured notes file + per-screen screenshots, all using the same finding format as Plan 02. NO formal write-back to REQUIREMENTS.md — Plan 29-05 owns that.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-qa-sweep-regression-catalog/29-CONTEXT.md
@.planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md
@CLAUDE.md

<interfaces>
<!-- Persona B target email and flow expectations from REQUIREMENTS.md and CONTEXT.md D-01. -->

**Persona B canonical target:** `soren@vibeos.com` (named in REQUIREMENTS.md AUTH-03 and CONTEXT.md D-01).

**Expected screens in the happy-path flow (per ROADMAP Phase 31 success criteria):**
1. Public landing / sign-up entry (currently `/login` per src/App.tsx — there may or may not be a separate signup CTA)
2. **Pricing / plan-selection page shown BEFORE account creation** (AUTH-02) — if this is skipped, that is a P0 finding
3. Email + password form
4. Payment gate (AUTH-03) — must redirect to pricing for non-grandfathered accounts
5. Successful signup → onboarding wizard at `/setup` (per src/App.tsx)
6. Onboarding completion → `/`

**Currently-known failure modes from REQUIREMENTS.md:**
- AUTH-01: signup fails with "An unexpected error occurred" — symptom keyword to search the page for
- AUTH-02: pricing page may be skipped silently
- AUTH-03: payment gate may not be enforced (soren@vibeos.com may hit /setup with no plan)
- AUTH-04: errors may be generic instead of useful

**Account-exists edge case (per D-01):** If `soren@vibeos.com` already has an account from prior testing:
- Record as a finding: title "Free-tier signup blocked because soren@vibeos.com account already exists" — Severity P1
- Do NOT delete the existing account
- Do NOT use a different real email for the alternative; instead document an alternative test email pattern (e.g., `qa-sweep-{timestamp}@example.com` or `soren+qa-sweep-{date}@vibeos.com`) and attempt the signup with that alternative. If the alternative also fails, that confirms the bug; if it succeeds, document the difference.

**Persona B is signed out at start.** Plan 29-01 logged in as Persona A. Sign out Persona A via the org/account menu before starting Persona B work.

**Finding format:** Same template as Plan 02 (Tag / Surface / Persona / Steps / Observed / Expected / Severity / Maps-to / Screenshot / Backend log). Persona field is `B`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persona B — fresh-signup E2E with soren@vibeos.com + per-screen evidence capture</name>
  <files>
    .planning/phases/29-qa-sweep-regression-catalog/screenshots/
    .planning/phases/29-qa-sweep-regression-catalog/29-03-PERSONA-B-SWEEP-NOTES.md
  </files>
  <read_first>
    - .planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md (gate: must be READY-FOR-SWEEP)
    - .planning/phases/29-qa-sweep-regression-catalog/29-CONTEXT.md (D-01 Persona B definition + account-exists edge case)
    - .planning/REQUIREMENTS.md (AUTH-01..05 spec details)
  </read_first>
  <action>
    Confirm `29-01-PRECHECK.md` Status is `READY-FOR-SWEEP`. Start the raw notes file:

    ```markdown
    # Phase 29 — Persona B Sweep Raw Notes (Plan 29-03)

    **Driver:** Claude via dev-browser MCP
    **Persona:** B — Fresh signup, target soren@vibeos.com
    **Started:** {ISO-8601}
    **Target:** https://app.callvaultai.com

    ## Pre-flight
    - Persona A signed out: yes / no
    - Dev-browser session: fresh (cookies cleared) / continued
    - Screenshot of starting state (no auth): screenshots/persona-b-00-starting-state.png

    ## Screen Trace

    | Step | Screen | Captured | Notes |
    |------|--------|----------|-------|
    | 1 | Landing / sign-in page (signed out) | | |
    | 2 | Click "Sign up" CTA | | If absent, that's a finding |
    | 3 | Pricing / plan-selection (AUTH-02) | | |
    | 4 | Email + password form | | |
    | 5 | Submit → response | | success / error text verbatim |
    | 6 | Payment gate (AUTH-03) | | |
    | 7 | Onboarding wizard `/setup` | | |
    | 8 | Wizard completion → `/` | | |

    ## Findings

    {finding entries appended below, format per template}
    ```

    **Step 0: Sign out + clear cookies.** Use `mcp__dev-browser__navigate("https://app.callvaultai.com")` then trigger logout via the account menu, or alternately call a fresh-context API on dev-browser. Confirm signed-out state by screenshotting the landing page.

    **Step 1: Locate signup entry.** From the signed-out landing, look for the "Sign up" CTA. Capture `persona-b-01-landing-signed-out.png`. If there is NO visible sign-up affordance: tag `[NEW]` finding "No sign-up CTA visible from public landing" Severity P0 (signup completely inaccessible).

    **Step 2: Click the signup entry.** Capture what loads next: `persona-b-02-after-signup-click.png`.

    **Step 3: Check for pricing/plan page (AUTH-02).** If the next screen is a pricing/plan-selection page: capture `persona-b-03-pricing-page.png`, tag `[NO-REPRO-AUTH-02]` (or `[RE-VERIFY-AUTH-02]` if it renders but with bugs). If the flow skipped pricing and went straight to the signup form: tag `[RE-VERIFY-AUTH-02]` Severity P0, with Observed "Pricing page bypassed — signup form rendered before any plan selection."

    **Step 4: Attempt signup with soren@vibeos.com.** Fill email = `soren@vibeos.com`, password = a strong throwaway (record only the password length in notes, never the literal value — apply same rule from Plan 01 T-29-01-01). Capture the form filled state: `persona-b-04-signup-form.png`. Click submit. Capture the result: `persona-b-05-signup-result.png`.

    **Step 5: Branch on outcome.**

    **Branch A — Account already exists.** If the response says "this email is already registered" / "account exists" / similar:
    - Tag `[NEW]` finding "Free-tier signup blocked: soren@vibeos.com account already exists from prior testing" Severity P1
    - **Do NOT delete the existing account.**
    - **Alternative attempt:** Try `soren+qa-sweep-{YYYY-MM-DD}@vibeos.com` (Gmail/Plus alias) as a substitute fresh-signup target. Repeat Step 4 with this alternative. Capture screens with prefix `persona-b-alt-{seq}-{slug}.png`. Document the result. Continue with subsequent steps using whichever email path actually progressed.
    - If alternative also fails: stop the signup branch and proceed to Steps 6-9 with whatever state is reachable.

    **Branch B — Generic error "An unexpected error occurred" (AUTH-01 symptom).** If signup fails with that exact text or similar uninformative error:
    - Capture the error: `persona-b-05-signup-error.png`
    - Open browser console + network panel; capture any 4xx/5xx response with `mcp__dev-browser__console_messages()` and include in the Backend log block per D-05
    - Tag `[RE-VERIFY-AUTH-01]` Severity P0 with the exact error text in Observed and the actual network response in Backend log
    - Tag `[RE-VERIFY-AUTH-04]` (signup error not useful) Severity P0
    - Stop the signup branch; proceed to evaluating what AUTH-04 should have said vs what it did say

    **Branch C — Generic error with specific text (e.g., "Password too short").** If the error IS informative:
    - Tag `[NO-REPRO-AUTH-04]` with the exact informative text in Observed
    - If signup completed despite the error, continue to Step 6
    - If signup failed for a different reason: capture and tag accordingly

    **Branch D — Signup succeeds.** Continue to Step 6.

    **Step 6: Payment gate check (AUTH-03).** If signup succeeded and the user lands somewhere, observe what screen renders:
    - If `/setup` (onboarding wizard) renders without seeing a payment screen: tag `[RE-VERIFY-AUTH-03]` Severity P0 with Observed "Payment gate skipped — non-grandfathered account reached /setup without selecting a paid plan"
    - If a payment/checkout screen renders: tag `[NO-REPRO-AUTH-03]`, capture `persona-b-06-payment-gate.png`. Do NOT submit any payment information. Do NOT enter test card numbers (this could trigger Polar test-mode side effects). Note the screen as "payment gate present, not completed."
    - If `/onboarding` or a redirect to pricing happens: capture and document accordingly

    **Step 7: Onboarding wizard.** If the flow reaches `/setup`, capture the entry screen: `persona-b-07-setup-wizard.png`. Do NOT click through onboarding (it permanently changes account state and is out of Phase 29 scope per CONTEXT.md "no fixes here"). Note what's visible and exit.

    **Step 8: Forgot-password flow.** Independently of the signup branch, navigate to `/forgot-password`, capture `persona-b-08-forgot-password.png`. Type `soren@vibeos.com` (or the alternative if needed). Submit. Capture the response. This is not an explicit AUTH-NN target but is a primary front-door surface worth a single capture.

    **Step 9: Google sign-in path (AUTH-05).** Per CONTEXT.md D-02, fresh Google OAuth signup is out of scope (covered by AUTH-05 in Phase 31). Do NOT initiate Google OAuth. **But:** capture a screenshot of the "Sign in with Google" button on the login page so AUTH-05's downstream phase has a visual baseline: `persona-b-09-google-signin-button.png`. Note "AUTH-05 deferred to Phase 31 per D-02 — captured visual baseline only."

    **Step 10: Cleanup / account state.**
    - Did Persona B create a usable account? If yes, document the account email in the notes file with a note `[manual review needed]` — do NOT delete (free-tier accounts may be analytics-relevant for the team)
    - If the alternative email was used (`soren+qa-sweep-{date}@vibeos.com`), record that as a "test account created during sweep" entry — Plan 29-05 will surface this as a P3 cleanup note for the user to decide whether to delete

    **Format every finding entry with Persona: B.** Per D-05 backend logs go into a fenced code block on each Finding that observed console errors / 4xx-5xx / RLS errors. Per T-29-03-02 redact any Bearer tokens in pasted log lines.

    Mask the literal password value everywhere. Refer to it only as `<strong-throwaway, NN chars>` in the notes.

    **Read-only stance:** Persona B inevitably creates an account state if signup succeeds. That is the test. But: no payment submitted, no onboarding wizard clicked through, no other production data touched. The created account (if any) is the only mutation; it stays in place.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-qa-sweep-regression-catalog/29-03-PERSONA-B-SWEEP-NOTES.md && grep -q "soren@vibeos.com" .planning/phases/29-qa-sweep-regression-catalog/29-03-PERSONA-B-SWEEP-NOTES.md && grep -E "AUTH-0[1-5]" .planning/phases/29-qa-sweep-regression-catalog/29-03-PERSONA-B-SWEEP-NOTES.md | wc -l | awk '{exit ($1 < 5) ? 1 : 0}' && ls .planning/phases/29-qa-sweep-regression-catalog/screenshots/persona-b-*.png 2>/dev/null | wc -l | awk '{exit ($1 < 5) ? 1 : 0}' && ! grep -E "Bearer [A-Za-z0-9._-]+" .planning/phases/29-qa-sweep-regression-catalog/29-03-PERSONA-B-SWEEP-NOTES.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `29-03-PERSONA-B-SWEEP-NOTES.md` exists with the Screen Trace table filled in (every row has either Captured + Notes, or "n/a, flow halted at step N" with reason)
    - File contains literal `soren@vibeos.com`
    - Every AUTH requirement (AUTH-01 through AUTH-05) appears at least once as a tag (`[RE-VERIFY-AUTH-NN]`, `[NO-REPRO-AUTH-NN]`, or `[NEW]` with `Related: AUTH-NN`) — grep `AUTH-0[1-5]` returns ≥ 5 occurrences
    - At least 5 PNG files named `persona-b-*.png` exist under `screenshots/`
    - File contains NO literal Bearer tokens or full JWTs (grep for `Bearer [A-Za-z0-9._-]+` returns empty)
    - File contains NO literal password value (passwords referenced only as `<strong-throwaway, NN chars>`)
    - Branch outcome (A/B/C/D) is documented explicitly in the file
    - AUTH-05 Google sign-in captured visual baseline only (the button screenshot exists; no OAuth flow initiated)
  </acceptance_criteria>
  <done>Persona B has attempted the full signup E2E, every reachable screen is captured, every AUTH requirement has a sweep tag, and any account state created is documented for the user's manual review in Plan 29-05. No payment was submitted; no onboarding was completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fresh-signup form → production auth backend | Persona B may exercise undiscovered failure paths; error responses may include stack traces |
| Persona B email (real address) → committed notes | soren@vibeos.com is real-user PII referenced by REQUIREMENTS.md AUTH-03 — committed file will mention it |
| Test payment screen → Polar test-mode | Submitting any test card could trigger Polar test webhook activity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-01 | Information Disclosure | Persona B password in notes file | mitigate | Password is referenced only as `<strong-throwaway, NN chars>` in the notes file; the literal value is never written; acceptance criterion checks no literal password value is present |
| T-29-03-02 | Information Disclosure | Stack traces / JWTs / Bearer tokens in error console output | mitigate | Per D-05 backend-log capture, any Bearer/JWT pasted into the notes file is redacted to `Bearer <redacted>` / `<jwt-redacted>`; acceptance criterion enforces no raw Bearer tokens in the file |
| T-29-03-03 | Tampering | Polar test-mode side effects | mitigate | Step 6 explicitly forbids submitting any payment information including test card numbers; payment-gate screen is captured only |
| T-29-03-04 | Information Disclosure | soren@vibeos.com address in committed file | accept | This email is already named in REQUIREMENTS.md AUTH-03 as the canonical free-tier canary; it is intentional product documentation, not a leak |
</threat_model>

<verification>
- The Screen Trace table is fully filled in (8 rows)
- Every AUTH-NN identifier (01–05) has a finding tag
- ≥ 5 PNG screenshots under screenshots/persona-b-*.png
- No Bearer/JWT/password literals in the notes file
- Branch outcome (A/B/C/D) documented
- If a test account was created, it is documented for user manual-review decision in Plan 29-05 cleanup notes
</verification>

<success_criteria>
- Plan 29-05 can author AUTH-01..05 Sweep Status entries from the notes alone
- The actual current production behavior of signup is documented (what happens to a real new user trying to sign up today)
- No production payment / billing state was modified
- The captured screens give downstream Phase 31 implementers the exact visual baseline they need to verify their fix
</success_criteria>

<output>
After completion, create `.planning/phases/29-qa-sweep-regression-catalog/29-03-SUMMARY.md` summarizing: branch outcome A/B/C/D, screens captured (count), AUTH findings by tag type, account-state created (yes/no with email), any blocking issue surfaced during signup, recommendation for Plan 05 mapping.
</output>
