---
phase: 29-qa-sweep-regression-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/29-qa-sweep-regression-catalog/screenshots/.gitkeep
  - .planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md
autonomous: true
requirements: [QA-01]
must_haves:
  truths:
    - "The screenshots/ subdirectory exists inside the Phase 29 directory and is git-trackable"
    - "Persona A credentials (CALLVAULTAI_LOGIN + CALLVAULTAI_LOGIN_PASSWORD) are readable from .env.local"
    - "Production URL https://app.callvaultai.com responds with HTTP 200"
    - "Persona A can sign into production end-to-end using those credentials via dev-browser MCP"
    - "All 3 owner-cycled orgs (AI SIMPLE, BUSINESS, GOVIBEY) are visible in the org switcher after Persona A login"
    - "A precheck status file 29-01-PRECHECK.md is written documenting all four checks"
  artifacts:
    - path: ".planning/phases/29-qa-sweep-regression-catalog/screenshots/.gitkeep"
      provides: "Empty marker file that lets the screenshots/ directory commit to git"
    - path: ".planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md"
      provides: "Documented evidence that credentials work, production is up, and 3 orgs are visible"
      contains: "AI SIMPLE"
  key_links:
    - from: ".env.local"
      to: "dev-browser MCP login flow"
      via: "CALLVAULTAI_LOGIN env var read at sweep start"
      pattern: "CALLVAULTAI_LOGIN="
    - from: "29-01-PRECHECK.md"
      to: "Plan 29-02 (Persona A sweep)"
      via: "Plan 02 reads PRECHECK.md to confirm 3 orgs are visible before starting cycle"
      pattern: "AI SIMPLE.*BUSINESS.*GOVIBEY"
---

<objective>
Set up the Phase 29 sweep infrastructure before any browser walkthrough begins: create the screenshots/ subdirectory so screenshots commit cleanly, verify Persona A's credentials work against production, verify all 3 owner-cycled orgs (AI SIMPLE / BUSINESS / GOVIBEY) are visible in the org switcher, and write a precheck status file that downstream sweep plans read as their start gate.

Purpose: Every sweep plan (29-02, 29-03, 29-04) is wasted work if the credentials don't log in, production is down, or one of the 3 orgs has disappeared. Catching these problems here — before 50+ screenshots get taken against an unstable baseline — is cheap. The precheck file is the contract every downstream plan reads to know the environment is good.

Output: `screenshots/.gitkeep` so the dir exists in git, and `29-01-PRECHECK.md` with explicit pass/fail status for each of the four checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/29-qa-sweep-regression-catalog/29-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- dev-browser MCP tools used by every Phase 29 sweep plan -->
<!-- Tool names follow mcp__dev-browser__* prefix; the executor calls them directly. -->

Core sweep tools the executor will use:
- `mcp__dev-browser__navigate(url)` — open a URL in the persistent dev-browser session
- `mcp__dev-browser__screenshot({ path, fullPage })` — capture and save a PNG
- `mcp__dev-browser__type(selector, text)` — fill form fields (login email, password)
- `mcp__dev-browser__click(selector)` — click buttons, links, nav items
- `mcp__dev-browser__evaluate(jsExpression)` — read DOM state, extract text content
- `mcp__dev-browser__console_messages()` / network log readers — capture error evidence for D-05 backend-log entries

Test credentials live in `.env.local`:
- CALLVAULTAI_LOGIN — Persona A owner email
- CALLVAULTAI_LOGIN_PASSWORD — Persona A password

The 3 Persona A orgs (per CONTEXT.md D-01): **AI SIMPLE**, **BUSINESS**, **GOVIBEY**.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create screenshots/ subdirectory with .gitkeep marker</name>
  <files>.planning/phases/29-qa-sweep-regression-catalog/screenshots/.gitkeep</files>
  <read_first>
    - .planning/phases/29-qa-sweep-regression-catalog/29-CONTEXT.md (D-04 locks the screenshots/ path)
  </read_first>
  <action>
    Create the directory `.planning/phases/29-qa-sweep-regression-catalog/screenshots/` and write a single empty file named `.gitkeep` inside it (use the Write tool, content can be the literal empty string or a one-line comment like `# Phase 29 sweep screenshots`). This is the canonical screenshot storage location locked by CONTEXT.md D-04 — every downstream sweep plan writes screenshots here using the filename pattern `qa-NN-{kebab-slug}.png`. The `.gitkeep` file exists only so git tracks the otherwise-empty directory.
  </action>
  <verify>
    <automated>test -d .planning/phases/29-qa-sweep-regression-catalog/screenshots/ && test -f .planning/phases/29-qa-sweep-regression-catalog/screenshots/.gitkeep && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Directory `.planning/phases/29-qa-sweep-regression-catalog/screenshots/` exists
    - File `.planning/phases/29-qa-sweep-regression-catalog/screenshots/.gitkeep` exists (size may be 0)
    - `git status` shows the new file as untracked (it will be staged in Plan 06)
  </acceptance_criteria>
  <done>The screenshots directory exists and is git-trackable via the .gitkeep file.</done>
</task>

<task type="auto">
  <name>Task 2: Verify production credentials + 3-org visibility, write 29-01-PRECHECK.md</name>
  <files>.planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md</files>
  <read_first>
    - .env.local (read CALLVAULTAI_LOGIN and CALLVAULTAI_LOGIN_PASSWORD values — DO NOT log the password value into the PRECHECK.md file; only confirm it is non-empty)
    - .planning/phases/29-qa-sweep-regression-catalog/29-CONTEXT.md (D-01 names the 3 orgs: AI SIMPLE, BUSINESS, GOVIBEY)
    - CLAUDE.md (HARD RULE: dev-browser does all verification — never ask user to log in)
  </read_first>
  <action>
    Run a four-check precheck using dev-browser MCP and write the result to `.planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md`. Do NOT include any password value in the file — only assert that the env var is non-empty.

    **Check 1 — Credentials present.** Read `.env.local` and confirm both `CALLVAULTAI_LOGIN` and `CALLVAULTAI_LOGIN_PASSWORD` are set (non-empty values). Record only the email address; mask the password as `present (length: NN chars)`.

    **Check 2 — Production reachable.** Use `mcp__dev-browser__navigate("https://app.callvaultai.com")`. Capture the HTTP status by checking that the page loads (no network error / no DNS failure). Take a screenshot to `.planning/phases/29-qa-sweep-regression-catalog/screenshots/precheck-01-prod-loaded.png`. Confirm the login page rendered (look for the email input field).

    **Check 3 — Login succeeds.** Type the email into the email field, type the password into the password field (use `mcp__dev-browser__type`), click the sign-in button. After redirect, take a screenshot to `.planning/phases/29-qa-sweep-regression-catalog/screenshots/precheck-02-logged-in.png`. Confirm the app shell rendered (look for the sidebar or main page content).

    **Check 4 — 3 orgs visible.** Open the org switcher (top-bar org selector — typically clicked to open a dropdown). Use `mcp__dev-browser__evaluate` to extract the visible org names from the dropdown. Take a screenshot to `.planning/phases/29-qa-sweep-regression-catalog/screenshots/precheck-03-org-switcher.png`. Confirm that **AI SIMPLE**, **BUSINESS**, and **GOVIBEY** are all visible in the dropdown (case-insensitive match — production capitalization may vary).

    Write the PRECHECK.md with this structure:
    ```markdown
    # Phase 29 — Sweep Precheck (Plan 29-01)

    **Run:** {ISO-8601 timestamp}
    **Driver:** Claude via dev-browser MCP (per CLAUDE.md HARD RULE)
    **Target:** https://app.callvaultai.com

    ## Check 1 — Credentials Present
    - CALLVAULTAI_LOGIN: {email} ✓ / ✗
    - CALLVAULTAI_LOGIN_PASSWORD: present (length: NN chars) ✓ / ✗
    - **Result:** PASS / FAIL

    ## Check 2 — Production Reachable
    - Navigated to https://app.callvaultai.com
    - Login page rendered: ✓ / ✗
    - Screenshot: ![](screenshots/precheck-01-prod-loaded.png)
    - **Result:** PASS / FAIL

    ## Check 3 — Login Succeeds
    - Filled email + password
    - Clicked sign-in
    - App shell rendered after redirect: ✓ / ✗
    - Screenshot: ![](screenshots/precheck-02-logged-in.png)
    - **Result:** PASS / FAIL

    ## Check 4 — All 3 Orgs Visible
    - Opened org switcher dropdown
    - AI SIMPLE visible: ✓ / ✗
    - BUSINESS visible: ✓ / ✗
    - GOVIBEY visible: ✓ / ✗
    - Screenshot: ![](screenshots/precheck-03-org-switcher.png)
    - **Result:** PASS / FAIL

    ## Overall
    - **Status:** READY-FOR-SWEEP / BLOCKED
    - **Blocking notes (if any):** {description of what failed; otherwise "none"}
    ```

    If ANY check fails, do NOT mark status READY-FOR-SWEEP. Mark it BLOCKED, note what failed, and stop — Plans 02/03/04 cannot proceed against a broken environment. Per CLAUDE.md "Do the work first" — investigate and fix the failure (e.g., re-read .env.local with corrected path, retry the login flow) before escalating.

    **No destructive operations.** Do not modify any production data during this precheck. Read-only login + dropdown inspection only.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md && grep -q "Status:.*READY-FOR-SWEEP" .planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md && grep -qi "AI SIMPLE" .planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md && grep -qi "BUSINESS" .planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md && grep -qi "GOVIBEY" .planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `29-01-PRECHECK.md` exists with all 4 check sections present (Check 1, Check 2, Check 3, Check 4)
    - File contains `Status: READY-FOR-SWEEP` (or `BLOCKED` with documented reason — if BLOCKED, the executor MUST fix and re-run before declaring the plan done)
    - File mentions all 3 orgs literally: `AI SIMPLE`, `BUSINESS`, `GOVIBEY` (case-insensitive)
    - At least 3 precheck-NN-*.png files exist under `screenshots/`
    - File contains NO password value (only `length: NN chars` reference) — verify with `! grep -i "$(grep CALLVAULTAI_LOGIN_PASSWORD .env.local | cut -d= -f2)" .planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md`
  </acceptance_criteria>
  <done>The PRECHECK file documents 4 passing checks and the sweep is unblocked to start Plan 29-02. If any check fails, the plan does NOT complete — the executor must remediate before marking done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.local → PRECHECK.md | Test credentials must not leak into git-committed precheck file |
| Persona A → production | Read-only inspection only; no destructive operations during precheck |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Information Disclosure | PRECHECK.md file | mitigate | Acceptance criterion checks the file does NOT contain the literal password value; password is documented only as `length: NN chars` |
| T-29-01-02 | Tampering | Production data | mitigate | Plan is strictly read-only — no clicks past sign-in + dropdown open + dropdown close; no form submissions touching data |
| T-29-01-03 | Information Disclosure | precheck-02-logged-in.png | accept | Screenshot may show the user's email in the top bar — this is the test account email already committed to .env.local references and brand-public; not a leak risk |
</threat_model>

<verification>
- `.planning/phases/29-qa-sweep-regression-catalog/screenshots/.gitkeep` exists
- `.planning/phases/29-qa-sweep-regression-catalog/29-01-PRECHECK.md` exists with Status: READY-FOR-SWEEP
- All 3 org names (AI SIMPLE, BUSINESS, GOVIBEY) appear in the precheck file
- At least 3 PNG files exist under `screenshots/` from the precheck navigation
- No password literal in any committed file under `.planning/phases/29-qa-sweep-regression-catalog/`
</verification>

<success_criteria>
- Phase 29 sweep is unblocked: env is verified, credentials work, 3 orgs are present, screenshot directory exists
- Plans 29-02, 29-03, 29-04 can begin with confidence that login + 3-org cycle will work end-to-end
- The precheck file remains in the phase directory as evidence the environment was sound at sweep start
</success_criteria>

<output>
After completion, create `.planning/phases/29-qa-sweep-regression-catalog/29-01-SUMMARY.md` summarizing: env vars verified, production reachable, login flow tested, 3-org visibility confirmed, and the path to the precheck file for downstream plans.
</output>
