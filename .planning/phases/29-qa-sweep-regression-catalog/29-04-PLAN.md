---
phase: 29-qa-sweep-regression-catalog
plan: 04
type: execute
wave: 4
depends_on: [29-02]
files_modified:
  - .planning/phases/29-qa-sweep-regression-catalog/screenshots/
  - .planning/phases/29-qa-sweep-regression-catalog/29-04-PERSONA-C-SWEEP-NOTES.md
autonomous: true
requirements: [QA-01]
must_haves:
  truths:
    - "Persona C (a different signed-in account from Persona A) opens the share link Persona A created in Plan 29-02 (addressed to naegele412@gmail.com)"
    - "The wrong-account experience on /s/:token is documented verbatim (the exact error text shown today)"
    - "SHARE-01, SHARE-02, SHARE-03, SHARE-04 each have a sweep tag in the notes file"
    - "The Share Call modal visual baseline is captured (was captured in Plan 02 from Persona A's side; Plan 04 captures the recipient side of the same share)"
    - "No anonymous (signed-out) recipient flow is exercised — that is deferred to Phase 32 per D-02"
  artifacts:
    - path: ".planning/phases/29-qa-sweep-regression-catalog/29-04-PERSONA-C-SWEEP-NOTES.md"
      provides: "Persona C observation log with SHARE-NN tags, current wrong-account error text verbatim, and the share token consumed"
      contains: "SHARE-02"
    - path: ".planning/phases/29-qa-sweep-regression-catalog/screenshots/"
      provides: "Per-screen PNGs persona-c-{seq}-{slug}.png covering wrong-account view of /s/:token"
  key_links:
    - from: "Plan 29-02 share-link token (recipient: naegele412@gmail.com)"
      to: "Persona C signed-in different-account session"
      via: "Persona C opens https://app.callvaultai.com/s/{token} while authenticated as a different real account"
      pattern: "/s/[A-Za-z0-9-]+"
    - from: "29-04-PERSONA-C-SWEEP-NOTES.md"
      to: "Plan 29-05"
      via: "Plan 05 consumes SHARE-01..04 Sweep Status values + may add new QA-NN entries if Persona C surfaces new bugs"
      pattern: "SHARE-0[1-4]"
---

<objective>
Persona C is a CallVault user signed into a DIFFERENT account than Persona A. Persona C opens the share link Persona A created in Plan 29-02 (addressed to `naegele412@gmail.com`). This is the only sweep arm that surfaces the SHARE-02 "wrong account" error path live, on production, with a real second account. Persona C documents the verbatim error text, the modal/page rendering, and any console output for Phase 32 to consume directly.

Purpose: SHARE-01 / SHARE-02 / SHARE-03 are highest-impact UX issues for a Loom-style product. Owner-only sweeping can't surface the recipient experience because Persona A IS the inviter. The anonymous (signed-out) recipient flow is explicitly deferred to Phase 32 per D-02 — Phase 29 only covers the signed-in-wrong-account case.

Output: One structured notes file + screenshots of the wrong-account experience. NO formal write-back to REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-qa-sweep-regression-catalog/29-CONTEXT.md
@.planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md
@CLAUDE.md

<interfaces>
<!-- The share token to consume is recorded in Plan 02's notes file under the Cleanup List entry. -->

**Source of the share token:** `.planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md` — search for "preserved for Plan 04" in the Cleanup List section. The token follows the pattern `/s/{token}` from the actual share URL Persona A generated.

**Persona C account requirements:**
- Must be a real CallVault account that is NOT Persona A's (`CALLVAULTAI_LOGIN`)
- Must NOT be the recipient address (`naegele412@gmail.com`) — that would make it the right account, defeating the test
- Practical options (in priority order):
  1. Persona B's account (if Plan 29-03 successfully created soren@vibeos.com or the alt soren+qa-sweep-{date}@vibeos.com)
  2. The user's own developer account if known and distinct from Persona A
  3. If neither is available: document the gap as a finding, tag `[CANNOT-VERIFY-SHARE-02]` and exit cleanly — do NOT create a new test account just for Persona C (that would inflate signups on production)

**Expected current behavior per REQUIREMENTS.md SHARE-02:**
- Today: "Call Not Found / invalid or expired" (or similar uninformative text)
- Desired (Phase 32 target): "This call was shared with na***@gmail.com — sign out and sign in with that email" with an explicit logout button

**Expected current behavior per SHARE-01 (the public landing page that doesn't exist yet):**
- Today: redirect to /login or a bare signin wall
- Desired (Phase 32 target): Loom-style public landing page with sender name, call title, two CTAs

**Per CONTEXT.md D-02, do NOT** sweep the signed-out anonymous recipient flow. That is SHARE-01's full surface and Phase 32 owns it. Persona C is signed-in only.

**Cmd+K / search modal cross-check:** While signed in as Persona C, also exercise the search modal briefly — it's the cross-check that the "different signed-in account" actually has its own data context and isn't seeing Persona A's data (cross-org / cross-account leak surface).

**Finding format:** Same as Plans 02 and 03. Persona field is `C`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persona C — open Plan 02's share link from a wrong-account signed-in session</name>
  <files>
    .planning/phases/29-qa-sweep-regression-catalog/screenshots/
    .planning/phases/29-qa-sweep-regression-catalog/29-04-PERSONA-C-SWEEP-NOTES.md
  </files>
  <read_first>
    - .planning/phases/29-qa-sweep-regression-catalog/29-02-PERSONA-A-SWEEP-NOTES.md (read the Cleanup List → extract the share token preserved for Plan 04)
    - .planning/phases/29-qa-sweep-regression-catalog/29-CONTEXT.md (D-01 Persona C definition; D-02 deferred scopes)
    - .planning/REQUIREMENTS.md (SHARE-01..04 specs)
  </read_first>
  <action>
    Confirm `29-01-PRECHECK.md` Status is `READY-FOR-SWEEP`. Extract the share token from `29-02-PERSONA-A-SWEEP-NOTES.md` — search for the `[x] (preserved for Plan 04 — share token: {token})` line in its Cleanup List.

    Start the raw notes file:
    ```markdown
    # Phase 29 — Persona C Sweep Raw Notes (Plan 29-04)

    **Driver:** Claude via dev-browser MCP
    **Persona:** C — Signed-in wrong-account share recipient
    **Started:** {ISO-8601}
    **Target:** https://app.callvaultai.com/s/{token}
    **Share token consumed:** {token from Plan 02}
    **Persona C account used:** {email of the actual second account signed in}

    ## Pre-flight
    - Persona A signed out of dev-browser session: yes / no
    - Persona C signed in: yes / no  (or "cannot-verify — no second account available")
    - Cookies cleared between sessions: yes / no

    ## Screen Trace

    | Step | Screen | Captured | Notes |
    |------|--------|----------|-------|
    | 1 | /s/{token} loaded as Persona C | | |
    | 2 | Wrong-account error rendering | | exact verbatim text |
    | 3 | Logout button presence | | yes / no |
    | 4 | Sender / call-title visible | | yes / no |

    ## Findings
    ```

    **Step 0: Switch from Persona A to Persona C session.**
    - Open the dev-browser session. If Persona A is signed in, sign out. Capture `persona-c-00-signed-out.png`.
    - If a second account is available (per the priority list in interfaces): sign in as Persona C. Capture `persona-c-01-signed-in-as-{persona-c-email-slug}.png`.
    - If NO second account is available: tag `[CANNOT-VERIFY-SHARE-02]` Severity (informational) with Observed "No second CallVault account available during sweep — SHARE-02 wrong-account error path could not be exercised." Set the Persona C account field in the header to `cannot-verify`. Skip Steps 1-4. Plan 05 will reflect `Cannot-verify` for SHARE-02.

    **Step 1: Open the share link as Persona C.** Navigate to `https://app.callvaultai.com/s/{token}`. Capture `persona-c-02-share-link-opened.png`. Wait for the page to fully render.

    **Step 2: Document the current wrong-account experience.**
    - Capture verbatim error text or page heading. Use `mcp__dev-browser__evaluate` to extract: page `<title>`, primary heading text, body paragraph text, any visible button labels.
    - Take a full-page screenshot: `persona-c-03-wrong-account-state.png`.
    - Check console for any errors via `mcp__dev-browser__console_messages()`. Capture any 4xx (especially 403/404) network responses. Include in Backend log block per D-05.
    - Tag findings:
      - `[RE-VERIFY-SHARE-01]` if the response is a bare login redirect / no landing page rendered (since SHARE-01 says "today: redirect to login")
      - `[RE-VERIFY-SHARE-02]` if the error text is uninformative ("Call Not Found", "invalid or expired", etc.) — Severity P0 if signup completely walls off Persona C; P1 if Persona C can at least navigate away
      - Severity per D-10 rubric

    **Step 3: Logout button presence.** Look on the wrong-account error screen for an explicit "Sign out" / "Use different account" button. If absent: tag adds detail to the `[RE-VERIFY-SHARE-02]` finding ("no logout button on wrong-account error"). If present: note its label verbatim.

    **Step 4: Sender / call-title visible.** Check whether the wrong-account screen reveals (a) the inviter's name, (b) the call title, or (c) the recipient email it was actually addressed to. The desired Phase 32 behavior reveals (c) with masking (`na***@gmail.com`). The current behavior likely reveals none. Record what is visible.

    **Step 5: Cross-account data context cross-check.** While still signed in as Persona C, navigate to `/` (main app). Take a screenshot: `persona-c-04-persona-c-home-view.png`. Confirm via `mcp__dev-browser__evaluate` that the calls/folders shown belong to Persona C's account, NOT Persona A's. Specifically check the org switcher dropdown — confirm the orgs visible to Persona C do NOT include `AI SIMPLE`, `BUSINESS`, or `GOVIBEY` (those are Persona A's orgs). If any of Persona A's data appears: tag `[NEW]` finding "Cross-account session leak — Persona A's orgs visible in Persona C's session" Severity P0 (this would be a far worse security finding than SHARE-02). Capture evidence.

    **Step 6: Open `/s/:token` flow check — share-link route works at all.** SHARE-04 says "single-call share still works end-to-end." Persona C is the wrong account so the link won't actually grant access, but the **route should still render something** (not a 500 / blank page / TypeError). If the route fails to render at all: tag `[RE-VERIFY-SHARE-04]` Severity P0 with the actual failure mode. If the route renders an error screen (even an unhelpful one): the route is functioning at the routing layer; tag `[NO-REPRO-SHARE-04]` for the route-resolution piece.

    **Step 7: Sign out Persona C.** End the session cleanly. Capture `persona-c-05-signed-out.png`. Do NOT delete Persona C's account state.

    **Notes file completion.** Fill in the Screen Trace table and append every finding with Persona: C.

    **Per T-29-04-01: redact the recipient email in finding text.** When writing finding bodies, render `naegele412@gmail.com` as `na***@gmail.com` (matching the Phase 32 SHARE-02 target wording — both for security hygiene and to give the implementer the exact target string to render). The literal `naegele412@gmail.com` may appear ONCE in the file (in the Pre-flight `Share token consumed` metadata for traceability), but not in finding body steps/observations.

    Per CONTEXT.md D-02: do NOT initiate any sign-out → anonymous recipient view. That is Phase 32 / SHARE-01's full surface.

    **No destructive operations.** Persona C session activity is read-only.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-qa-sweep-regression-catalog/29-04-PERSONA-C-SWEEP-NOTES.md && grep -E "SHARE-0[1-4]" .planning/phases/29-qa-sweep-regression-catalog/29-04-PERSONA-C-SWEEP-NOTES.md | wc -l | awk '{exit ($1 < 4) ? 1 : 0}' && grep -q "na\*\*\*@gmail.com" .planning/phases/29-qa-sweep-regression-catalog/29-04-PERSONA-C-SWEEP-NOTES.md && [ "$(ls .planning/phases/29-qa-sweep-regression-catalog/screenshots/persona-c-*.png 2>/dev/null | wc -l)" -ge 2 -o "$(grep -c "CANNOT-VERIFY-SHARE-02" .planning/phases/29-qa-sweep-regression-catalog/29-04-PERSONA-C-SWEEP-NOTES.md)" -ge 1 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `29-04-PERSONA-C-SWEEP-NOTES.md` exists
    - Every SHARE requirement (SHARE-01 through SHARE-04) appears at least once as a tag in the file — grep `SHARE-0[1-4]` returns ≥ 4 occurrences
    - File contains masked email form `na***@gmail.com` (used in finding bodies per T-29-04-01)
    - Either: (a) at least 2 PNG files named `persona-c-*.png` exist under `screenshots/` AND the file documents a real Persona C account session, OR (b) the file explicitly contains `[CANNOT-VERIFY-SHARE-02]` tag with reason "no second account available"
    - The Screen Trace table rows are filled in (or rows are explicitly marked n/a if Persona C was unavailable)
    - File mentions the share token consumed (read from Plan 02's notes)
    - File contains NO Bearer-token or full-JWT strings (grep for `Bearer [A-Za-z0-9._-]+` returns empty)
  </acceptance_criteria>
  <done>The wrong-account share-link experience has been documented (or marked Cannot-verify with reason), every SHARE-NN has a sweep tag, and the recipient-side visual baseline is captured for Phase 32 implementers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persona A session → Persona C session | Cookies / localStorage must NOT bleed across personas in the dev-browser session |
| Real recipient email (naegele412@gmail.com) → committed notes | The recipient is a real address (the user's personal Gmail per MEMORY.md) and should be masked in finding bodies |
| Persona C account data → screenshots | If Persona C is the user's developer account, screenshots may reveal real-user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | Information Disclosure | Recipient email (naegele412@gmail.com) in finding bodies | mitigate | Mask to `na***@gmail.com` in finding Step/Observation text; the literal address may appear only in the metadata header as the share-token-consumed traceability line |
| T-29-04-02 | Spoofing / cross-account session leak | dev-browser cookie state between Persona A and Persona C | mitigate | Step 0 explicitly signs out Persona A and clears cookies before signing in Persona C; Step 5 cross-checks that Persona C's session shows ONLY Persona C's data |
| T-29-04-03 | Information Disclosure | Bearer / JWT in console output captured for Backend log block | mitigate | Same redaction rule from Plan 02 T-29-02-03 / Plan 03 T-29-03-02 — `Bearer <redacted>`, `<jwt-redacted>` |
| T-29-04-04 | Elevation of Privilege | Persona C accessing Persona A's call via wrong-account flow | mitigate | The whole point of the test is to confirm the wrong account CANNOT access. If they CAN, that's a P0 finding documented and routed to Phase 32 |
</threat_model>

<verification>
- File `29-04-PERSONA-C-SWEEP-NOTES.md` exists with the Screen Trace table filled in
- Every SHARE-NN tag is present
- Masked recipient form `na***@gmail.com` used in finding bodies
- Either screenshot evidence OR cannot-verify gap documented with reason
- Cross-account session leak (Step 5) is explicitly checked and result recorded
- No Bearer tokens / JWTs in file
</verification>

<success_criteria>
- Plan 29-05 has SHARE-01..04 Sweep Status data ready to write to the traceability table
- Phase 32 implementers have the exact current error text + visual baseline to design against
- If a cross-account session leak was observed, it is flagged as a P0 finding routed to Phase 32 (or escalated higher per D-09)
- No production state was modified (Persona C's account state is whatever it was before the sweep)
</success_criteria>

<output>
After completion, create `.planning/phases/29-qa-sweep-regression-catalog/29-04-SUMMARY.md` summarizing: Persona C account used (or Cannot-verify reason), share token consumed, wrong-account error text verbatim, SHARE findings by tag type, cross-account leak check result.
</output>
