---
phase: "08.1"
plan: "01"
type: summary
status: complete
completed_at: "2026-06-10"
production_commit: "47b57c50"
requirements:
  - CON-01
  - CON-02
  - HRD-01 follow-up
---

# Plan 01 Summary: Preserve Canonical Transcript Segments

## What Changed

- Added structured transcript turn preservation to the canonical connector contract.
- Converted `CanonicalTranscriptTurn[]` into persisted `transcript_segments` with normalized speaker names, emails, provider speaker ids, timestamps, and offsets.
- Persisted `record.transcript_segments` through `connector-pipeline` into `recordings.transcript_segments`.
- Replaced name-only speaker participant insertion with identity-aware participant collection from transcript segments and source metadata.
- Added targeted tests for canonical segment conversion and participant identity dedupe.

## Commits

| Commit | Type | Description |
| --- | --- | --- |
| `47b57c50` | production/test | Preserve canonical transcript segments and identity-aware transcript participants |

## Files Changed

- `supabase/functions/_shared/canonical-recording.ts`
- `supabase/functions/_shared/connector-pipeline.ts`
- `supabase/functions/_shared/__tests__/canonical-recording.test.ts`
- `supabase/functions/_shared/__tests__/connector-pipeline.test.ts`

## Deviations

- Added a focused `connector-pipeline.test.ts` for the newly exported participant identity collector. The original plan only listed the canonical test file, but the participant dedupe behavior needed a direct unit-level guard.
- Did not run live connector imports. This plan changed the shared contract and pipeline plumbing only; provider-specific adapter execution is covered by later plans in this phase.

## Verification

```bash
npx vitest run supabase/functions/_shared/__tests__/canonical-recording.test.ts supabase/functions/_shared/__tests__/connector-pipeline.test.ts
```

Result: passed, 2 test files, 19 tests.

```bash
npm run type-check
```

Result: passed.

## Self-Check

PASSED

- `CanonicalRecording` now accepts `transcriptTurns?: CanonicalTranscriptTurn[] | null`.
- `canonicalTurnsToSegments()` is exported and tested.
- `canonicalToConnectorRecord()` includes `transcript_segments` when structured turns are present.
- `insertRecording()` writes `transcript_segments` without changing `full_transcript`.
- Participant identities dedupe by normalized email first and normalized name as fallback.
- Speakerless or unknown-only rows do not create duplicate Unknown participant spam.
