---
phase: "08.1"
plan: "05"
type: execute
wave: 5
depends_on:
  - "08.1-01"
  - "08.1-02"
  - "08.1-03"
  - "08.1-04"
files_modified:
  - supabase/migrations/*_backfill_transcript_segments.sql
  - src/test/*
  - supabase/functions/**/__tests__/*.test.ts
autonomous: false
requirements:
  - CON-01
  - CON-02
  - HRD-01 follow-up
must_haves:
  truths:
    - "Existing recordings can be normalized without duplicate recordings or workspace entries"
    - "Backfill does not overwrite CallVault-owned transcript edits"
    - "Final quality gate passes type-check and build"
  artifacts:
    - path: supabase/migrations/*_backfill_transcript_segments.sql
      provides: "safe existing-recording normalization support if DB-side backfill is chosen"
      contains: "transcript_segments"
---

<objective>
Add safe existing-recording normalization and complete full-phase verification.

Purpose: New imports are only half the fix. Existing connector recordings need a safe path to structured segments, and Phase 08.1 must close with tests proving no duplicates or user-edit overwrites.
</objective>

<context>
@.planning/phases/08.1-connector-transcript-normalization-preserve-provider-speaker/08.1-VALIDATION.md
@supabase/CLAUDE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement lazy normalization or guarded backfill path</name>
  <files>supabase/migrations/*_backfill_transcript_segments.sql, supabase/functions/_shared/*, src/hooks/useCallDetailQueries.ts</files>
  <read_first>
    - supabase/functions/save-pasted-transcript/index.ts
    - supabase/functions/_shared/fathom-transcript-parser.ts
    - src/hooks/useCallDetailQueries.ts
  </read_first>
  <action>
Choose the narrowest safe path:
- lazy normalization in application/Edge code if a DB migration is unnecessary, OR
- a guarded migration/RPC if DB-side backfill is cleaner.

Rules:
- only fill `recordings.transcript_segments` when it is null or empty
- never create new `recordings` rows
- never create new `workspace_entries` rows
- never overwrite user-edited transcript text/speaker fields
- skip unparseable raw transcripts and leave `full_transcript` fallback intact
  </action>
  <acceptance_criteria>
    - existing Fathom/Zoom-style full_transcript can produce structured segments
    - unparseable transcripts are skipped without data loss
    - no duplicate recording/workspace rows are created
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Run real-DB integration gate if migration/backfill touches data</name>
  <files>supabase/migrations/*_backfill_transcript_segments.sql</files>
  <read_first>
    - supabase/CLAUDE.md
    - .env.test.example
  </read_first>
  <action>
If Task 1 creates a migration or RPC that mutates existing rows, run it only against the dedicated integration/test Supabase project first. Do not use production-linked DB for this plan's proof.

Required guard:
- `VITEST_INTEGRATION_OK=true`
- `VITE_SUPABASE_TEST_URL` must not equal `VITE_SUPABASE_URL`
- `SUPABASE_TEST_SERVICE_ROLE_KEY` must not equal `SUPABASE_SERVICE_ROLE_KEY`
  </action>
  <acceptance_criteria>
    - executor records whether the backfill was tested against dedicated test DB, local DB, or skipped because only lazy normalization was used
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Final Phase 08.1 quality gate</name>
  <files>all Phase 08.1 touched files</files>
  <read_first>
    - .planning/phases/08.1-connector-transcript-normalization-preserve-provider-speaker/08.1-VALIDATION.md
  </read_first>
  <action>
Run the final gate set:
- all provider fixture tests added in Phase 08.1
- call-detail/transcript utility tests
- API/export tests touched by Phase 08.1
- `npm run type-check`
- `npm run build`

If touching `src/config/source-registry.ts` or `supabase/functions/mcp-server/index.ts`, also run `npm run build` against the committed tree before push.
  </action>
  <acceptance_criteria>
    - `npm run type-check` exits 0
    - `npm run build` exits 0
    - test failures, if any, are classified as pre-existing or fixed before closeout
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backfill/lazy normalizer -> existing customer data | Existing rows must not be duplicated or overwritten unsafely |
| integration test -> Supabase DB | Real-DB tests must never target production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-09 | Tampering | backfill | mitigate | Fill only null/empty `transcript_segments`, preserve `full_transcript`, and skip edited/user-owned data |
| T-08.1-10 | Denial of Service | bulk normalization | mitigate | Prefer lazy or batched work; avoid large unbounded synchronous updates |
| T-08.1-11 | Information Disclosure | integration tests | mitigate | Use dedicated test DB guards from `supabase/CLAUDE.md` |
| T-08.1-SC | Tampering | npm dependencies | accept | No package installation in this plan |
</threat_model>

<verification>
```bash
npm run type-check
npm run build
```

If integration tests are added:
```bash
npm run test:integration
```
</verification>

<success_criteria>
- Existing recordings have a safe normalization path.
- No duplicate recordings or workspace entries are produced.
- User-owned transcript/speaker edits are preserved.
- Phase 08.1 final quality gate passes or documents pre-existing unrelated failures exactly.
</success_criteria>

