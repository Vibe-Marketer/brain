---
phase: "08.1"
plan: "02"
type: execute
wave: 2
depends_on:
  - "08.1-01"
files_modified:
  - supabase/functions/_shared/read-ai-connector.ts
  - supabase/functions/_shared/grain-connector.ts
  - supabase/functions/_shared/fireflies-connector.ts
  - supabase/functions/_shared/plaud-connector.ts
  - supabase/functions/zoom-sync-meetings/index.ts
  - supabase/functions/zoom-webhook/index.ts
  - supabase/functions/youtube-import/index.ts
  - supabase/functions/mcp-server/tools/write/ingest_transcript.ts
autonomous: true
requirements:
  - CON-01
  - CON-02
  - HRD-01 follow-up
must_haves:
  truths:
    - "Every provider has a documented normalization path into the canonical segment contract"
    - "Provider time units are normalized before pipeline insert"
    - "YouTube remains explicitly speakerless"
  artifacts:
    - path: supabase/functions/_shared/read-ai-connector.ts
      provides: "Read.ai turn name/email/ms normalization"
      contains: "transcriptTurns"
    - path: supabase/functions/_shared/grain-connector.ts
      provides: "Grain participant_id to speaker email enrichment"
      contains: "participant_id"
---

<objective>
Route all provider adapters into the structured canonical segment contract.

Purpose: After Plan 01, the shared pipeline can persist structured segments. This plan makes each source actually supply those turns and tests provider-specific quirks at the adapter boundary, not in UI rendering.
</objective>

<context>
@.planning/phases/08.1-connector-transcript-normalization-preserve-provider-speaker/08.1-RESEARCH.md
@.planning/phases/08.1-connector-transcript-normalization-preserve-provider-speaker/08.1-PATTERNS.md
@supabase/functions/_shared/canonical-recording.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate transcriptTurns in canonical adapters</name>
  <files>supabase/functions/_shared/read-ai-connector.ts, supabase/functions/_shared/grain-connector.ts, supabase/functions/_shared/fireflies-connector.ts, supabase/functions/_shared/plaud-connector.ts</files>
  <read_first>
    - supabase/functions/_shared/read-ai-connector.ts
    - supabase/functions/_shared/grain-connector.ts
    - supabase/functions/_shared/fireflies-connector.ts
    - supabase/functions/_shared/plaud-connector.ts
  </read_first>
  <action>
For each canonical adapter, add `transcriptTurns: turns` to the returned `CanonicalRecording`.

Provider-specific requirements:
- Read.ai: preserve `speaker.name`, `speaker.email`, `start_time_ms`, and `end_time_ms` through existing `readAiTranscriptToTurns()`.
- Grain: build a participant map by `participants[].id` and enrich `CanonicalTranscriptTurn.speakerEmail` when `segment.participant_id` maps to a participant email.
- Fireflies: preserve sentence start/end seconds and speaker name; carry attendee/host emails in participant metadata.
- PLAUD: preserve speaker labels and millis offsets; leave email null when unavailable.
  </action>
  <acceptance_criteria>
    - each adapter returns `transcriptTurns`
    - all adapter tests assert at least one turn with start offset and text
    - Read.ai or Grain tests assert speaker email preservation
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Normalize legacy/special source paths</name>
  <files>supabase/functions/zoom-sync-meetings/index.ts, supabase/functions/zoom-webhook/index.ts, supabase/functions/youtube-import/index.ts, supabase/functions/mcp-server/tools/write/ingest_transcript.ts</files>
  <read_first>
    - supabase/functions/zoom-sync-meetings/index.ts
    - supabase/functions/zoom-webhook/index.ts
    - supabase/functions/youtube-import/index.ts
    - supabase/functions/mcp-server/tools/write/ingest_transcript.ts
    - supabase/functions/save-pasted-transcript/index.ts
  </read_first>
  <action>
Update non-canonical or special-source paths to supply `transcript_segments` or `transcriptTurns`:
- Zoom VTT sync/webhook: map parsed `TranscriptSegment` rows into canonical segment JSON and pass it to `runPipeline()`.
- YouTube import: when source transcript arrays include `start` and `duration`, store speakerless segments with start/end seconds and empty speaker name.
- MCP `ingest_transcript`: when text is Fathom-style timestamped transcript, reuse shared parsing logic to populate segments; when unstructured, leave segments null and preserve current behavior.
- Paste/manual path remains compatible and should not be duplicated.
  </action>
  <acceptance_criteria>
    - Zoom imports write structured segments without removing legacy `fathom_transcripts` compatibility rows
    - YouTube imports write speakerless structured segments when timestamp data exists
    - MCP manual import can persist parsed segments when input has timestamps
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add provider fixture coverage</name>
  <files>supabase/functions/_shared/__tests__/*.test.ts, supabase/functions/*/__tests__/*.test.ts</files>
  <read_first>
    - supabase/functions/_shared/__tests__/canonical-recording.test.ts
    - supabase/functions/_shared/__tests__/vtt-parser.test.ts
    - supabase/functions/save-pasted-transcript/__tests__/save-pasted-transcript.test.ts
  </read_first>
  <action>
Add or extend fixture tests so Read.ai, Grain, Fireflies, PLAUD, Zoom, YouTube, paste/manual, and MCP manual import each prove:
- multi-speaker transcript segments survive
- offsets are seconds in stored canonical shape
- duration is seconds
- provider participant identities become `call_participants` inputs when available
  </action>
  <acceptance_criteria>
    - test fixtures cover every provider listed in Phase 08.1 success criteria
    - each fixture contains at least two speakers unless the source is intentionally speakerless
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider API -> adapter | Unit conversion and identity extraction happen on untrusted external payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-03 | Tampering | provider offsets | mitigate | Clamp invalid offsets to null and assert provider unit conversions in tests |
| T-08.1-04 | Spoofing | provider speaker emails | accept | Emails are attribution metadata from connected provider payloads, not auth decisions |
| T-08.1-SC | Tampering | npm dependencies | accept | No package installation in this plan |
</threat_model>

<verification>
Run provider fixture tests:
```bash
npx vitest run supabase/functions/_shared/__tests__/read-ai-connector.test.ts supabase/functions/_shared/__tests__/grain-connector.test.ts supabase/functions/_shared/__tests__/fireflies-connector.test.ts supabase/functions/_shared/__tests__/plaud-connector.test.ts
npm run type-check
```
</verification>

<success_criteria>
- All listed providers have an explicit structured normalization path.
- Provider fixture tests prove speaker/timing/duration preservation.
- No UI component owns provider-specific parsing.
</success_criteria>

