---
phase: "08.1"
plan: "03"
type: execute
wave: 3
depends_on:
  - "08.1-01"
  - "08.1-02"
files_modified:
  - src/types/meetings.ts
  - src/lib/transcriptUtils.ts
  - src/hooks/useCallDetailQueries.ts
  - src/components/call-detail/CallTranscriptTab.tsx
autonomous: true
requirements:
  - CON-02
  - HRD-01 follow-up
must_haves:
  truths:
    - "Call detail transcript data uses recordings.transcript_segments before regex full_transcript fallback"
    - "Speaker bubble grouping uses canonical structured segments"
    - "Speakerless YouTube transcripts do not fake speaker identity"
  artifacts:
    - path: src/hooks/useCallDetailQueries.ts
      provides: "Structured-segment-first transcript query"
      contains: "transcript_segments"
    - path: src/lib/transcriptUtils.ts
      provides: "shared structured segment display mapper"
      contains: "normalizeTranscriptSegments"
---

<objective>
Make call detail rendering prefer canonical structured segments.

Purpose: The UI currently reparses `full_transcript` first and loses provider identity data. This plan changes the data hook and shared transcript utilities so the existing bubble UI gets structured speaker, email, and timestamp data before falling back to regex parsing.
</objective>

<context>
@.planning/phases/08.1-connector-transcript-normalization-preserve-provider-speaker/08.1-RESEARCH.md
@src/hooks/useCallDetailQueries.ts
@src/components/call-detail/CallTranscriptTab.tsx
@src/lib/transcriptUtils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend structured segment types and mapper</name>
  <files>src/types/meetings.ts, src/lib/transcriptUtils.ts</files>
  <read_first>
    - src/types/meetings.ts
    - src/lib/transcriptUtils.ts
  </read_first>
  <action>
Add a frontend `StoredTranscriptSegment` type compatible with `recordings.transcript_segments` JSON.

Add `normalizeTranscriptSegments(value, recordingId): TranscriptSegment[]` in `src/lib/transcriptUtils.ts`:
- accept unknown JSON
- return [] for invalid or empty arrays
- map `speaker_name`, `speaker_email`, `text`, `timestamp`, `start_seconds`, `end_seconds`
- generate stable ids from stored `id` or index
- keep empty `speaker_name` for speakerless sources
  </action>
  <acceptance_criteria>
    - invalid JSON returns [] without throwing
    - valid structured segments produce `TranscriptSegment[]`
    - speakerless source remains `speaker_name: ""`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fetch transcript_segments in call detail hook before parsing full_transcript</name>
  <files>src/hooks/useCallDetailQueries.ts, src/services/recordings.service.ts</files>
  <read_first>
    - src/hooks/useCallDetailQueries.ts
    - src/services/recordings.service.ts
  </read_first>
  <action>
Update recording detail column selection and fallback fetches to include `transcript_segments`.

In `useCallDetailQueries`, before regex parsing `full_transcript`, call `normalizeTranscriptSegments(call.transcript_segments, call.recording_id)` or fetch `transcript_segments` from `recordings` when the loaded call omitted it. If structured segments are non-empty, return them immediately.

Keep existing fallback order after structured segments:
1. regex parse `full_transcript`
2. YouTube format parse
3. legacy `fathom_transcripts` for numeric IDs
  </action>
  <acceptance_criteria>
    - structured segments are returned before regex parsing
    - legacy numeric Fathom fallback still works
    - no component calls a service directly
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add call detail rendering tests</name>
  <files>src/hooks/__tests__/useCallDetailQueries.test.ts, src/lib/__tests__/transcriptUtils.test.ts</files>
  <read_first>
    - src/lib/__tests__/transcriptUtils.test.ts
    - existing call-detail tests under src/components/call-detail/__tests__
  </read_first>
  <action>
Add tests proving:
- structured segment data with speaker email drives display segment output
- regex fallback is not used when structured segments exist
- consecutive segments from same speaker still group into one bubble group
- speakerless YouTube-style segments render as paragraph/timestamp rows
  </action>
  <acceptance_criteria>
    - tests fail if `useCallDetailQueries` ignores `transcript_segments`
    - tests cover host-alignment inputs through speaker email/name data
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB JSON -> browser UI | Stored JSON is treated as unknown and normalized with type guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-05 | Tampering | transcript_segments JSON | mitigate | Parse unknown JSON defensively and render text as React text, not HTML |
| T-08.1-06 | Information Disclosure | speaker email display | accept | Email data is already available to authorized users through call participant surfaces |
| T-08.1-SC | Tampering | npm dependencies | accept | No package installation in this plan |
</threat_model>

<verification>
```bash
npx vitest run src/lib/__tests__/transcriptUtils.test.ts
npm run type-check
```
</verification>

<success_criteria>
- Call detail uses structured segments first.
- Bubble grouping and host alignment work for non-Fathom connectors when identity data exists.
- Unknown/speakerless transcripts degrade intentionally.
</success_criteria>

