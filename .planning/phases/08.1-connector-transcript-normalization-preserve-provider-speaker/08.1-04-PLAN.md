---
phase: "08.1"
plan: "04"
type: execute
wave: 4
depends_on:
  - "08.1-03"
files_modified:
  - src/lib/export-utils.ts
  - src/lib/export-utils-advanced.ts
  - src/services/api-tokens.service.ts
  - supabase/functions/callvault-api/routes/calls.ts
  - supabase/functions/mcp-server/tools/read/get_transcript.ts
  - supabase/functions/mcp-server/tools/read/get_recording_context.ts
autonomous: true
requirements:
  - CON-02
  - HRD-01 follow-up
must_haves:
  truths:
    - "Exports prefer structured transcript segments when present"
    - "REST call detail exposes structured segments while keeping transcript text fallback"
    - "MCP read tools still return content[].text markdown"
  artifacts:
    - path: supabase/functions/mcp-server/tools/read/get_transcript.ts
      provides: "MCP transcript markdown from canonical segments"
      contains: "transcript_segments"
---

<objective>
Move exports, REST, and MCP reads to structured-segment-first transcript formatting.

Purpose: Once the app displays canonical segments, external surfaces should not keep flattening away speaker/timing data. This plan updates export and API consumers while preserving existing text fallback and MCP markdown response shape.
</objective>

<context>
@.planning/phases/08.1-connector-transcript-normalization-preserve-provider-speaker/08.1-RESEARCH.md
@src/lib/export-utils.ts
@supabase/functions/mcp-server/tools/read/get_transcript.ts
@supabase/functions/callvault-api/routes/calls.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared frontend segment-to-markdown formatting</name>
  <files>src/lib/transcriptUtils.ts, src/lib/export-utils.ts, src/lib/export-utils-advanced.ts</files>
  <read_first>
    - src/lib/transcriptUtils.ts
    - src/lib/export-utils.ts
    - src/lib/export-utils-advanced.ts
  </read_first>
  <action>
Add a shared formatter that turns normalized transcript segments into markdown/text with timestamp, speaker name, and speaker email where appropriate.

Update TXT, MD, DOCX/PDF text inputs, AI export, and Obsidian export to call the formatter when `transcript_segments` exist. Keep `full_transcript` as fallback.
  </action>
  <acceptance_criteria>
    - Obsidian export can include structured transcript body
    - smart exports no longer need to regex-parse full_transcript when structured data exists
    - exports still work for old recordings with only full_transcript
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Include transcript_segments in Obsidian export fetch</name>
  <files>src/services/api-tokens.service.ts, src/lib/export-utils.ts</files>
  <read_first>
    - src/services/api-tokens.service.ts
    - src/lib/export-utils.ts
  </read_first>
  <action>
Extend `ExportableCall` and `fetchAllCallsForObsidianExport()` to include `transcript_segments`.

Do not remove `full_transcript` from the select list. Keep current workspace mapping behavior unchanged.
  </action>
  <acceptance_criteria>
    - `fetchAllCallsForObsidianExport()` selects `transcript_segments`
    - exported call objects include segments without breaking callers that omit them
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update REST and MCP read surfaces</name>
  <files>supabase/functions/callvault-api/routes/calls.ts, supabase/functions/mcp-server/tools/read/get_transcript.ts, supabase/functions/mcp-server/tools/read/get_recording_context.ts</files>
  <read_first>
    - supabase/functions/callvault-api/routes/calls.ts
    - supabase/functions/mcp-server/tools/read/get_transcript.ts
    - supabase/functions/mcp-server/tools/read/get_recording_context.ts
  </read_first>
  <action>
For REST call detail:
- select `transcript_segments`
- return `transcript_segments` in the JSON detail response
- keep `transcript: full_transcript ?? formattedSegmentsText ?? null` or equivalent compatibility field

For MCP:
- select `transcript_segments`
- format structured segments into markdown when present
- keep `mcpOk(id, string)` and `content[].text` behavior unchanged
  </action>
  <acceptance_criteria>
    - MCP `get_transcript` still returns markdown text, not JSON
    - REST detail includes structured segments and does not remove existing `transcript` field
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app export/API -> user filesystem/client | Transcript data is exported only through authenticated, already-authorized surfaces |
| MCP server -> MCP client | Response must remain markdown text in `content[].text` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-07 | Information Disclosure | API transcript segments | mitigate | Preserve existing workspace/token access checks before returning transcript data |
| T-08.1-08 | Tampering | export formatting | mitigate | Format plain text/markdown only; do not render untrusted transcript as HTML |
| T-08.1-SC | Tampering | npm dependencies | accept | No package installation in this plan |
</threat_model>

<verification>
```bash
npx vitest run src/services/__tests__/api-tokens.service.test.ts
npm run type-check
```
</verification>

<success_criteria>
- Obsidian, AI, REST, and MCP surfaces preserve speaker/timestamp structure when available.
- Existing `full_transcript` consumers continue working.
- MCP result shape remains `content[].text` markdown.
</success_criteria>

