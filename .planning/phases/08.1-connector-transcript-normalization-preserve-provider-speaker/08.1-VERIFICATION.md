---
phase: "08.1"
slug: connector-transcript-normalization-preserve-provider-speaker
status: human_needed
verified: 2026-06-11
verifier: Retroactive bookkeeping audit (01-09 archive audit follow-up) — static probes + full-suite re-run
---

# Phase 08.1 — Retroactive Verification

> Phase executed 2026-06-10 (5/5 plans, all SUMMARYs present; production commit `4532c608` per 08.1-05). No formal phase-level verification record existed; this record was created retroactively on 2026-06-11. All of this phase's named test files ran inside today's full-suite probe (`npm test -- --run` → 1702 passed / 0 failed, this session). No deploys, no live connector syncs, no browser checks were run for this record.

## Success Criteria (from ROADMAP)

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | Canonical contract carries structured turns through `runCanonicalConnectorPipeline()` into `recordings.transcript_segments` without losing speakerName/speakerEmail/startSeconds/endSeconds/text; `full_transcript` stays fallback | **passed** (test-level) | Probe today: `connector-pipeline.ts` persists `transcript_segments` (lines 34-35, 270). `canonical-recording.test.ts` + `connector-pipeline.test.ts` (19 tests at 08.1-01, re-run green in today's full suite). |
| 2 | Documented normalization path per source (Read.ai, Grain, Fireflies, PLAUD, Zoom, Fathom, YouTube, paste/manual, MCP import); provider quirks normalized in adapters/helpers, not UI | **passed** (test-level) | 08.1-02 gate ran 8 files / 75 tests (read-ai, grain, fireflies, plaud, vtt-parser, routing-overrides, youtube-import-regression, mcp contract-surface) — all green again today. Zoom path covered via VTT parser fixtures (Zoom VTT pastes asserted in `vtt-parser.test.ts`); Fathom via `fathom-transcript-parser.test.ts`. Note: paste/manual path intentionally left with `transcript_segments` null per existing integration expectations (recorded deviation in 08.1-02-SUMMARY). |
| 3 | Call detail renders from structured segments before regex fallback; bubble grouping + blue host alignment work for non-Fathom connectors; speakerless degrades intentionally | **human_needed** (code passed) | `transcriptUtils.test.ts` + `useCallDetailQueries.test.ts` (22-26 tests, green today) prove structured-first ordering at code level. Actual bubble grouping / blue host alignment rendering was never browser-verified — visual walkthrough still required. |
| 4 | `call_participants` populated from speakers + provider participants with deterministic email/name dedupe, no duplicate "Unknown" rows | **passed** (test-level) | Participant upsert shipped in 08.1-01 (identity-aware upsert); participant assertions present in `canonical-recording.test.ts` / `connector-pipeline.test.ts`, green today. Not verified against live provider syncs. |
| 5 | Existing recordings backfilled or lazily normalized without duplicate recordings/entries or overwriting user edits | **passed** (by design + tests) | 08.1-05 chose lazy READ-TIME normalization (`parseSpeakerTimestampTranscript()` in `src/lib/transcriptUtils.ts`, on disk today) — zero DB mutation, zero new rows, `full_transcript` preserved. Tests prove parseable→structured, unparseable→skip, fallback read-only (26 tests, green today). No mutating backfill exists to verify. |
| 6 | Provider fixture tests: multi-speaker + timestamps + duration for Read.ai, Grain, Fireflies, PLAUD, Zoom, Fathom, YouTube/paste; call-detail test proves structured-first before regex fallback | **passed** | Fixture test files on disk and green in today's full suite: `read-ai-connector`, `grain-connector`, `fireflies-connector`, `plaud-connector`, `vtt-parser` (Zoom), `fathom-transcript-parser`, `youtube-import-regression`; structured-first call-detail behavior in `transcriptUtils.test.ts`. |

## Human-needed items

1. Browser walkthrough of a non-Fathom call detail (e.g., Grain or Read.ai recording) confirming speaker bubbles, grouping, and blue host alignment render from structured segments (criterion 3).
2. One live connector sync per provider to confirm end-to-end persistence of `transcript_segments` and `call_participants` on real provider payloads (criteria 1, 4 — currently fixture-proven only).

## What this record does NOT prove

- No live connector syncs were run; all provider evidence is fixture-based.
- Lazy normalization behavior on real legacy production rows is inferred from tests, not observed in production.

## Sign-off

- [x] All 6 criteria assessed; 5 passed at fixture/test level via today's green full suite.
- [ ] 2 live/visual items remain for human verification (listed above).
