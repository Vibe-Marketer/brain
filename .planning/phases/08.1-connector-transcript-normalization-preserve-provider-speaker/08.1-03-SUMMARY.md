---
phase: "08.1"
plan: "03"
type: summary
status: complete
completed_at: "2026-06-10"
production_commit: "d0e778b5"
requirements:
  - CON-02
  - HRD-01 follow-up
---

# Plan 03 Summary: Structured-Segment-First Call Detail Rendering

## What Changed

- Added frontend types for stored transcript segments and allowed transcript rows to carry UUID recording ids.
- Added `normalizeTranscriptSegments()` to defensively map unknown `recordings.transcript_segments` JSON into display transcript rows.
- Updated call detail transcript loading to prefer structured segments before regex parsing `full_transcript`.
- Updated the direct `recordings` fallback fetch to include `transcript_segments`.
- Passed `transcript_segments` through workspace recording to meeting mapping when present.
- Added tests for invalid JSON, structured speaker/email mapping, speakerless YouTube rows, grouping inputs, and source-order guarding.

## Commits

| Commit | Type | Description |
| --- | --- | --- |
| `d0e778b5` | production/test | Prefer structured transcript segments in call detail rendering |

## Files Changed

- `src/types/meetings.ts`
- `src/types/workspace.ts`
- `src/lib/transcriptUtils.ts`
- `src/hooks/useCallDetailQueries.ts`
- `src/hooks/useWorkspaces.ts`
- `src/lib/__tests__/transcriptUtils.test.ts`
- `src/hooks/__tests__/useCallDetailQueries.test.ts`

## Deviations

- Did not alter `CallTranscriptTab.tsx`; its existing speakerless YouTube branch and grouped bubble rendering already consume the normalized segment shape.
- Used a source-order regression test for the hook instead of a full rendered hook integration test because the existing hook test file is currently focused on pure exported helpers and static contract guards.

## Verification

```bash
npx vitest run src/lib/__tests__/transcriptUtils.test.ts src/hooks/__tests__/useCallDetailQueries.test.ts
```

Result: passed, 2 test files, 22 tests.

```bash
npm run type-check
```

Result: passed.

## Self-Check

PASSED

- Invalid stored JSON returns `[]` without throwing.
- Valid structured segments preserve speaker name, speaker email, text, timestamp, and recording id.
- Empty speaker names remain empty for speakerless YouTube-style rendering.
- Consecutive structured rows from the same speaker still group together.
- `useCallDetailQueries` loads `transcript_segments` before regex parsing `full_transcript`.
- Legacy full-transcript parsing and numeric Fathom fallback remain in place.
