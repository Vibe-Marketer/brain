---
phase: 35
plan: "02"
plan_name: Remove Folder + Duration Filters
requirements:
  - FILTER-01
  - FILTER-02
status: ready
---

# Plan 35-02 — Remove Folder + Duration Filters

## Scope

- **FILTER-01** — Remove Folder filter pill (redundant — folder selection is the 2nd-pane).
- **FILTER-02** — Remove Duration filter pill (low-value).

## Files

- `src/components/transcript-library/FilterBar.tsx` — pill triggers + active-pill rendering
- `src/components/transcript-library/FolderFilterPopover.tsx` — DELETE
- `src/components/transcript-library/DurationFilterPopover.tsx` — DELETE
- `src/components/transcripts/TranscriptsTab.tsx` — drop `folders` / `onCreateFolder` props passed to FilterBar; KEEP merge logic for search-syntax integration

## Implementation

### FILTER-01 + FILTER-02 — FilterBar.tsx

1. Remove imports:
   - `import { FolderFilterPopover } from "./FolderFilterPopover";`
   - `import { DurationFilterPopover } from "./DurationFilterPopover";`
2. Remove the `<FolderFilterPopover>` block and `<DurationFilterPopover>` block from the trigger row.
3. Remove the active filter pills:
   - The `filters.folders` pill block.
   - The `filters.durationMin/Max` pill block.
4. Remove `formatDuration` helper (only used by the pill block).
5. Remove `folders` and `onCreateFolder` from the `FilterBarProps` interface and the function signature.
6. Update `hasActiveFilters` check — drop `filters.folders` and `filters.durationMin/Max` clauses (UI no longer offers them; search-syntax still can set them but that's fine, the "clear all" still resets).
7. Keep `handleClearAll` resetting `folders: []` and `durationMin/Max: undefined` so search-syntax-derived values still get cleared.

### Caller cleanup — TranscriptsTab.tsx

1. Drop `folders={folders}` and `onCreateFolder={...}` props from `<FilterBar>`.
2. Keep the merging logic for `mergedFolders` / `durationMin/Max`.

### Delete dead files

```bash
rm src/components/transcript-library/FolderFilterPopover.tsx
rm src/components/transcript-library/DurationFilterPopover.tsx
```

`git grep "FolderFilterPopover\|DurationFilterPopover"` must return zero before deletion.

## Test Strategy

- `npm run build` must pass.
- `npm run lint` must pass.
- Dev-browser: filter bar has no Folder or Duration pill triggers.
- Dev-browser: search syntax `folder:"My Folder"` still narrows results (search-syntax → filters flow intact).

## Verification

- [ ] `npm run build` succeeds.
- [ ] `npm run lint` clean.
- [ ] `git grep "FolderFilterPopover\|DurationFilterPopover" src/` empty.
- [ ] Dev-browser: filter bar shows Date / Tags / Contacts / Source only.

## Commit

```
git commit -m "feat(35-02): FILTER-01,02 remove Folder and Duration filter pills"
```
