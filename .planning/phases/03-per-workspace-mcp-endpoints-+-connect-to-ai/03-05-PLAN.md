---
phase: 03-per-workspace-mcp-endpoints-+-connect-to-ai
plan: 05
type: execute
wave: 4
depends_on:
  - "03-03"
  - "03-04"
files_modified:
  - src/components/settings/MCPTab.tsx
  - src/components/settings/McpSetupSnippets.tsx
  - src/components/settings/mcp-provider-capabilities.ts
  - src/components/settings/__tests__/McpSetupSnippets.test.tsx
autonomous: true
requirements:
  - MCP-02
must_haves:
  truths:
    - "D-09/D-10: Provider setup actions are capability-gated by verified support, not assumed."
    - "MCP-02: Every manual token/snippet surface uses `https://api.callvaultai.com/mcp` for org scope or `https://api.callvaultai.com/mcp/w/{workspace_uuid}` for workspace scope, never the raw Supabase URL."
    - "D-14: Token/manual setup stays visible as a fallback even when OAuth is the primary path."
  artifacts:
    - path: "src/components/settings/mcp-provider-capabilities.ts"
      provides: "Explicit provider capability registry for one-click vs guided setup"
    - path: "src/components/settings/McpSetupSnippets.tsx"
      provides: "Workspace-aware public-endpoint snippets for Claude Desktop, Cursor, and generic MCP clients"
    - path: "src/components/settings/__tests__/McpSetupSnippets.test.tsx"
      provides: "Provider honesty and public-endpoint regression coverage"
  key_links:
    - from: "src/components/settings/McpSetupSnippets.tsx"
      to: "src/components/settings/mcp-provider-capabilities.ts"
      via: "provider action gating"
      pattern: "Copy setup|Open setup guide|Connect with OAuth"
---

<objective>
Add the user-facing setup surface on top of the new management UI: provider-aware actions, honest capability gating, and public-endpoint snippets that keep workspace scope explicit.

Purpose: Complete the “connect this AI” experience without over-promising unsupported provider deep links.
Output: Provider capability registry, setup snippet UI, and tests that forbid raw Supabase URLs.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-CONTEXT.md
@.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-RESEARCH.md
@.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-UI-SPEC.md
@src/components/settings/MCPTab.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add provider setup coverage for public endpoints and honest capability gating</name>
  <files>src/components/settings/__tests__/McpSetupSnippets.test.tsx</files>
  <read_first>
    - .planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-RESEARCH.md
    - .planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-UI-SPEC.md
    - src/components/settings/MCPTab.tsx
  </read_first>
  <behavior>
    - Test 1: org snippets use `https://api.callvaultai.com/mcp` and workspace snippets use `https://api.callvaultai.com/mcp/w/{workspace_uuid}` only.
    - Test 2: providers without verified one-click support render guided/copy actions instead of fake `Add to X` flows per D-10.
    - Test 3: token fallback remains visible per D-14.
  </behavior>
  <action>Create `McpSetupSnippets.test.tsx` to lock the provider setup contract: public API URLs only, provider-specific action labels based on verified capability, and no raw Supabase function URL leakage. Include at least Claude Desktop, Cursor, and generic MCP cases, plus conditional assertions for ChatGPT, Perplexity, Gemini, and Manus based on the capability registry evidence from 03-RESEARCH.md.</action>
  <acceptance_criteria>
    - The test file fails if any snippet or manual token URL contains `/functions/v1/mcp-server`.
    - Tests distinguish one-click actions from guided/manual actions by provider capability flags.
    - Tests confirm token fallback remains visible.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Wave 0 must create src/components/settings/__tests__/McpSetupSnippets.test.tsx first</automated>
  </verify>
  <done>The provider setup contract is pinned before UI behavior is expanded.</done>
</task>

<task type="auto">
  <name>Task 2: Implement provider capability registry and workspace-aware snippet surface</name>
  <files>src/components/settings/mcp-provider-capabilities.ts, src/components/settings/McpSetupSnippets.tsx, src/components/settings/MCPTab.tsx</files>
  <read_first>
    - .planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-RESEARCH.md
    - src/components/settings/MCPTab.tsx
    - src/pages/OAuthConsentPage.tsx
    - src/components/settings/IntegrationsTab.tsx
  </read_first>
  <action>Create a small provider capability registry that encodes which providers have verified one-click/add flows versus guided/copy-only setup, citing the Phase 03 research outcome rather than assumptions per D-10. Build `McpSetupSnippets.tsx` to render public endpoint snippets for org and workspace scope, with Claude Desktop, Cursor, and generic MCP config shapes using `https://api.callvaultai.com/mcp` or `https://api.callvaultai.com/mcp/w/{workspace_uuid}` only. Integrate the setup surface into `MCPTab.tsx` so D-09 is pursued honestly: show `Connect with OAuth` only where CallVault can start a real OAuth flow, otherwise show `Copy setup` or `Open setup guide` labels.</action>
  <acceptance_criteria>
    - Provider behavior is data-driven from `mcp-provider-capabilities.ts`, not hardcoded ad hoc in JSX.
    - Org-scoped snippets and token rows use `/mcp`; workspace-scoped snippets and token rows use `/mcp/w/{workspace_uuid}`.
    - No snippet or helper exposes the raw Supabase URL.
    - Unsupported one-click providers are clearly labeled as guided/manual instead of silently omitted or falsely promoted.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/settings/__tests__/McpSetupSnippets.test.tsx src/components/settings/__tests__/McpConnectionsTab.test.tsx && npm run build</automated>
  </verify>
  <done>Users can copy or launch the right setup path per provider without being misled about unsupported one-click flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CallVault setup UI -> external AI client docs/flows | Outbound actions must not misrepresent actual provider capabilities |
| Setup snippets -> bearer configuration | Users copy secrets and endpoint URLs directly from this surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Information Disclosure | setup snippets | mitigate | Use only public `api.callvaultai.com` endpoints; never show raw Supabase function URLs. |
| T-03-05-02 | Spoofing | provider one-click labels | mitigate | Gate action labels from verified capability data per D-10. |
| T-03-05-03 | Tampering | copied configuration | mitigate | Generate scope-specific snippets from current workspace/org context and pin them in tests. |
</threat_model>

<verification>
Run:

`npm test -- --run src/components/settings/__tests__/McpSetupSnippets.test.tsx src/components/settings/__tests__/McpConnectionsTab.test.tsx && npm run build`
</verification>

<success_criteria>
- Provider actions are honest about one-click vs guided setup.
- All snippets use the public CallVault API host and preserve workspace scope.
- Manual/token fallback remains visible alongside OAuth-first flows.
</success_criteria>

<output>
Create `.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-05-SUMMARY.md` when done.
</output>
