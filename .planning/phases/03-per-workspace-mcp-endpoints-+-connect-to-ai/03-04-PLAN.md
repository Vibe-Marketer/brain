---
phase: 03-per-workspace-mcp-endpoints-+-connect-to-ai
plan: 04
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/components/settings/MCPTab.tsx
  - src/components/settings/__tests__/McpConnectionsTab.test.tsx
  - src/services/mcp-oauth-grants.service.ts
  - src/hooks/useMcpOAuthGrants.ts
  - src/services/mcp-tokens.service.ts
  - src/hooks/useMcpTokens.ts
autonomous: true
requirements:
  - MCP-02
  - MCP-03
must_haves:
  truths:
    - "D-11/D-12/D-13/D-14: Settings shows grouped AI connector management with OAuth clients first and manual tokens second."
    - "D-02: Manual tokens remain visibly available for category-scoped and fallback setup."
    - "D-03: Permission-change messaging explains that CallVault enforces changes immediately even if the AI client needs refresh/reconnect."
  artifacts:
    - path: "src/components/settings/MCPTab.tsx"
      provides: "AI connectors management UI with grouped OAuth and manual token sections"
    - path: "src/services/mcp-oauth-grants.service.ts"
      provides: "OAuth grant list/revoke data access layer"
    - path: "src/components/settings/__tests__/McpConnectionsTab.test.tsx"
      provides: "Grouped UI and action coverage for AI connector management"
  key_links:
    - from: "src/components/settings/MCPTab.tsx"
      to: "src/hooks/useMcpOAuthGrants.ts"
      via: "OAuth connection list and revoke actions"
      pattern: "Connected AI clients|revoke"
---

<objective>
Turn the current token-only MCP tab into the Phase 03 AI connectors control surface: connected OAuth clients at the top, manual tokens below, and revoke/rotate/list behavior that reflects the new grant model.

Purpose: Satisfy the management half of MCP-02 and MCP-03 inside the approved Settings IA.
Output: Settings UI, hooks/services, and grouped-section tests.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-CONTEXT.md
@.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-UI-SPEC.md
@src/CLAUDE.md
@src/components/settings/MCPTab.tsx
@src/components/settings/IntegrationsTab.tsx
@src/services/mcp-tokens.service.ts
@src/hooks/useMcpTokens.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add grouped AI connectors UI and action coverage</name>
  <files>src/components/settings/__tests__/McpConnectionsTab.test.tsx</files>
  <read_first>
    - src/components/settings/MCPTab.tsx
    - src/components/settings/__tests__/MCPTab.permissions.test.tsx
    - src/components/settings/__tests__/IntegrationsTab.test.tsx
    - .planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: OAuth-connected AI clients render before manual tokens per D-12 and D-13.
    - Test 2: manual token controls remain visible and secondary, not hidden behind advanced settings, per D-14.
    - Test 3: permission refresh messaging explains immediate server-side enforcement per D-03.
  </behavior>
  <action>Create `McpConnectionsTab.test.tsx` to cover the grouped Settings layout from 03-UI-SPEC.md: `AI connectors` heading, `Connected AI clients` section above `Manual token connectors`, revoke/rotate/list actions, empty state copy, and the refresh/reconnect note. Keep the tests on the real component surface rather than isolated helper render fragments so section ordering and copy remain pinned.</action>
  <acceptance_criteria>
    - The test file checks section order, visible CTAs, and refresh/reconnect messaging.
    - The tests fail if manual token setup is hidden or OAuth is not the primary section.
    - The assertions use the approved copy contract, including no positive “AI-powered” wording.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Wave 0 must create src/components/settings/__tests__/McpConnectionsTab.test.tsx first</automated>
  </verify>
  <done>The grouped Settings IA is pinned before the UI is refactored.</done>
</task>

<task type="auto">
  <name>Task 2: Add OAuth-grant hooks/services and wire grouped management actions</name>
  <files>src/services/mcp-oauth-grants.service.ts, src/hooks/useMcpOAuthGrants.ts, src/services/mcp-tokens.service.ts, src/hooks/useMcpTokens.ts</files>
  <read_first>
    - src/services/mcp-tokens.service.ts
    - src/hooks/useMcpTokens.ts
    - src/services/mcp-token-capabilities.service.ts
    - src/hooks/useMcpTokenCapabilities.ts
    - src/types/supabase.ts
  </read_first>
  <action>Create a pure async `mcp-oauth-grants.service.ts` plus `useMcpOAuthGrants.ts` hook wrappers for list and revoke operations against the Phase 03 grant table. Extend the existing manual-token services/hooks only where necessary to expose prefixed token display, scope metadata, and rotate/delete actions without breaking D-02 category control semantics. Keep all data access in services and all React cache/optimism in hooks per the repo’s service+hook separation rule.</action>
  <acceptance_criteria>
    - OAuth grant list/revoke logic lives in a service + hook pair, not in the component.
    - Manual-token hooks still own token CRUD and continue to invalidate their namespace after mutations.
    - The returned data shape includes connection type, scope, workspace/org labels, endpoint/resource URL, categories summary, and audit timestamps needed by the UI contract.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/settings/__tests__/McpConnectionsTab.test.tsx src/hooks/__tests__/useMcpTokenCapabilities.test.ts src/services/__tests__/mcp-token-capabilities.service.test.ts</automated>
  </verify>
  <done>The Settings surface has clean data primitives for OAuth grants and manual tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor the MCP tab into the approved AI connectors management surface</name>
  <files>src/components/settings/MCPTab.tsx</files>
  <read_first>
    - src/components/settings/MCPTab.tsx
    - src/components/settings/IntegrationsTab.tsx
    - .planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-UI-SPEC.md
    - src/pages/OAuthConsentPage.tsx
  </read_first>
  <action>Rework `MCPTab.tsx` into the `AI connectors` Settings surface defined by D-11 through D-14 and the Phase 03 UI contract. Put `Connected AI clients` first with revoke/list metadata, keep `Manual token connectors` second with visible token creation and category controls, and include the D-03 note that CallVault applies permission changes immediately even if the client tool list needs refresh/reconnect. Preserve existing shadcn primitives, Remix icons, and compact Settings layout. Do not introduce marketing cards, new routing, or positive “AI-powered” copy.</action>
  <acceptance_criteria>
    - The rendered Settings surface has grouped OAuth/manual sections in the approved order.
    - OAuth rows show connection type, scope, workspace/org, endpoint/resource URL, categories summary, last-used, and revoke action.
    - Manual-token rows still expose create, rotate, revoke, and category controls.
    - The page remains inside the existing Settings shell and uses only approved UI primitives.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/settings/__tests__/McpConnectionsTab.test.tsx && npm run build</automated>
  </verify>
  <done>Settings now presents OAuth-first AI connector management without hiding token fallback controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI -> grant/token mutation APIs | Destructive actions from the browser change live connection state |
| Hooks -> service layer | React cache state must not become the authorization source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | connection list UI | mitigate | Show only the signed-in user’s grants/tokens through RLS-backed services. |
| T-03-04-02 | Repudiation | revoke/rotate actions | mitigate | Surface explicit destructive actions and persist audit fields from the backend in list rows. |
| T-03-04-03 | Elevation of Privilege | UI permission messaging | mitigate | State clearly that server-side enforcement is the source of truth per D-03; UI visibility is informational only. |
</threat_model>

<verification>
Run:

`npm test -- --run src/components/settings/__tests__/McpConnectionsTab.test.tsx src/hooks/__tests__/useMcpTokenCapabilities.test.ts src/services/__tests__/mcp-token-capabilities.service.test.ts && npm run build`
</verification>

<success_criteria>
- Settings contains the approved grouped `AI connectors` management surface.
- OAuth-connected clients are primary; manual tokens remain visible and controllable.
- Revoke/rotate/list actions are wired through services/hooks, not embedded data access.
</success_criteria>

<output>
Create `.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-04-SUMMARY.md` when done.
</output>
