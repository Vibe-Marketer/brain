---
phase: 03-per-workspace-mcp-endpoints-+-connect-to-ai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260528163000_mcp_oauth_client_grants_and_prefixed_tokens.sql
  - src/types/supabase.ts
  - supabase/functions/mcp-server/auth.ts
  - supabase/functions/mcp-server/__tests__/oauth-client-grants.integration.test.ts
autonomous: true
requirements:
  - MCP-02
  - MCP-03
must_haves:
  truths:
    - "D-01/D-04: OAuth grants default to full non-admin access and never synthesize admin access."
    - "D-02/D-11: Manual tokens remain the v1 category-scoped path and new tokens are visibly prefixed as `cv_org_` or `cv_ws_`."
    - "D-03: Revoked or narrowed OAuth grants fail closed on the next request without waiting for client-side refresh."
  artifacts:
    - path: "supabase/migrations/20260528163000_mcp_oauth_client_grants_and_prefixed_tokens.sql"
      provides: "First-class per-client OAuth grant table, legacy binding migration path, and prefixed token generation rules"
    - path: "supabase/functions/mcp-server/auth.ts"
      provides: "Grant-backed manual-token and OAuth-JWT authentication with legacy-token fallback"
    - path: "supabase/functions/mcp-server/__tests__/oauth-client-grants.integration.test.ts"
      provides: "Wave 0 regression coverage for per-client grants, revocation, and prefixed-token parsing"
  key_links:
    - from: "supabase/functions/mcp-server/auth.ts"
      to: "supabase/migrations/20260528163000_mcp_oauth_client_grants_and_prefixed_tokens.sql"
      via: "grant and token lookups"
      pattern: "mcp_oauth_client_grants|cv_(org|ws)_"
    - from: "supabase/functions/mcp-server/__tests__/oauth-client-grants.integration.test.ts"
      to: "supabase/functions/mcp-server/auth.ts"
      via: "request-level auth assertions"
      pattern: "authenticateMcpRequest|last_used_at|revoked_at"
---

<objective>
Create the Phase 03 authorization substrate: a first-class OAuth client-grant model, backward-compatible prefixed manual tokens, and the Wave 0 auth tests that let later routing/UI plans build on a real security boundary.

Purpose: Replace the one-row-per-user OAuth binding with per-client grants and keep manual tokens as the D-02 fallback path.
Output: Migration, auth-layer changes, and grant/token integration coverage.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-CONTEXT.md
@.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-RESEARCH.md
@.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-PATTERNS.md
@.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-VALIDATION.md
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 grant/token auth regression coverage</name>
  <files>supabase/functions/mcp-server/__tests__/oauth-client-grants.integration.test.ts</files>
  <read_first>
    - supabase/functions/mcp-server/auth.ts
    - supabase/functions/mcp-server/__tests__/category-gating.test.ts
    - supabase/functions/mcp-server/__tests__/contract-surface.test.ts
    - .planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: a JWT with `client_id` resolves exactly one active CallVault grant and returns non-admin categories per D-01 and D-04.
    - Test 2: a revoked grant or revoked token is rejected on the next request per D-03.
    - Test 3: `cv_org_` and `cv_ws_` tokens authenticate, while legacy 64-char hex tokens still authenticate for backward compatibility per D-02.
  </behavior>
  <action>Create `oauth-client-grants.integration.test.ts` as the Wave 0 auth gate from 03-VALIDATION.md. Exercise grant lookup by `user_id + client_id + org_id/workspace_id`, `revoked_at` fail-closed behavior, `last_used_at` updates, new `cv_org_` / `cv_ws_` token parsing, and legacy 64-char fallback. Mark any helper setup needed for migration-backed fixtures directly in the test file so later plans do not infer auth semantics.</action>
  <acceptance_criteria>
    - The new test file covers active grant, revoked grant, prefixed token, and legacy token cases.
    - The assertions explicitly distinguish 401 invalid bearer from 403 authorization failures.
    - `<automated>` no longer says `MISSING` for Phase 03 OAuth grant coverage.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Wave 0 must create supabase/functions/mcp-server/__tests__/oauth-client-grants.integration.test.ts first</automated>
  </verify>
  <done>Phase 03 has a failing-then-passing auth regression file that pins the intended grant and token behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace legacy OAuth binding with per-client grants and prefixed tokens</name>
  <files>supabase/migrations/20260528163000_mcp_oauth_client_grants_and_prefixed_tokens.sql, src/types/supabase.ts, supabase/functions/mcp-server/auth.ts</files>
  <read_first>
    - supabase/migrations/20260415120000_mcp_oauth_org_bindings.sql
    - supabase/migrations/20260310160000_mcp_tokens.sql
    - supabase/migrations/20260507151835_regenerate_mcp_token_with_categories.sql
    - supabase/functions/mcp-server/auth.ts
    - src/types/supabase.ts
  </read_first>
  <behavior>
    - Test 1: multiple OAuth grants for the same user can coexist by distinct `client_id` and scope.
    - Test 2: OAuth grant defaults are `read`, `write`, `ai` and exclude `admin` per D-01 and D-04.
    - Test 3: manual token auth accepts new prefixes and legacy tokens, with manual tokens still owning category controls per D-02.
  </behavior>
  <action>Create `20260528163000_mcp_oauth_client_grants_and_prefixed_tokens.sql` to add a first-class `mcp_oauth_client_grants` table, indexes, RLS, and any helper RPC/default updates needed to mint `cv_org_` / `cv_ws_` tokens and preserve legacy validation. Backfill or reconcile existing `mcp_oauth_org_bindings` rows into the new table without inventing admin rights, and update `supabase/functions/mcp-server/auth.ts` to resolve OAuth JWTs through the new grant table keyed by `user_id`, `client_id`, and org/workspace scope. Keep manual tokens as the only granular category path per D-02, default OAuth grants to full non-admin categories per D-01, and fail closed immediately on revoked or narrowed grants per D-03.</action>
  <acceptance_criteria>
    - The migration creates `mcp_oauth_client_grants` with explicit org/workspace scope columns, revocation/audit fields, and indexes for `user_id`, `client_id`, and scope lookups.
    - `auth.ts` no longer depends on `mcp_oauth_org_bindings` for primary authorization.
    - OAuth grant resolution never returns `admin` by default.
    - Manual-token parsing recognizes `cv_org_` and `cv_ws_` plus legacy 64-char tokens.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/oauth-client-grants.integration.test.ts</automated>
  </verify>
  <done>The auth layer is grant-backed, prefixed tokens are supported, and old one-row-per-user OAuth binding is no longer the live authorization source.</done>
</task>

<task type="auto">
  <name>[BLOCKING] Task 3: Push the schema changes to Supabase and record proof before later verification</name>
  <files>supabase/migrations/20260528163000_mcp_oauth_client_grants_and_prefixed_tokens.sql</files>
  <read_first>
    - .env
    - .env.local
    - supabase/migrations/20260528163000_mcp_oauth_client_grants_and_prefixed_tokens.sql
    - supabase/CLAUDE.md
  </read_first>
  <action>After Task 2 passes locally, run `supabase db push` against the linked or configured Phase 03 Supabase project and record the exact proof in the plan summary. If non-TTY/project credentials are unavailable, record that live schema push proof is blocked and do not let downstream verification claim the migration is applied. This gate is mandatory for the schema-relevant phase per the planning context.</action>
  <acceptance_criteria>
    - `supabase db push` succeeds and the output is captured in the summary, or the summary explicitly states why live push proof could not be obtained.
    - Later plans do not claim schema-backed behavior is verified without this proof.
    - Any push failure is treated as a blocking execution issue, not a warning.
  </acceptance_criteria>
  <verify>
    <automated>supabase db push</automated>
  </verify>
  <done>The schema push requirement is satisfied with live proof or explicitly blocks further verification claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuth JWT -> CallVault grant lookup | External bearer claims are translated into internal authorization state |
| Manual token -> MCP auth parser | Raw bearer strings cross from client config into server-side secret validation |
| Migration -> live Supabase project | Schema intent becomes enforceable runtime policy only after push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Elevation of Privilege | OAuth grant defaults | mitigate | Default new grants to `read/write/ai` only per D-01 and D-04; never synthesize admin access. |
| T-03-01-02 | Tampering | manual token format migration | mitigate | Accept new `cv_org_` / `cv_ws_` prefixes plus legacy 64-char fallback without relaxing token lookup semantics. |
| T-03-01-03 | Repudiation | grant revoke enforcement | mitigate | Persist `revoked_at` and `last_used_at`; test next-request rejection per D-03. |
| T-03-01-04 | Information Disclosure | stale legacy binding behavior | mitigate | Move primary OAuth authorization off `mcp_oauth_org_bindings` and fail closed when no active grant exists. |
| T-03-01-SC | Tampering | migration application | mitigate | [BLOCKING] `supabase db push` proof required before schema-backed verification. |
</threat_model>

<verification>
Run the new auth integration test, then execute the blocking schema push:

`npm test -- --run supabase/functions/mcp-server/__tests__/oauth-client-grants.integration.test.ts`

`supabase db push`
</verification>

<success_criteria>
- `mcp_oauth_client_grants` exists as the new per-client authorization source.
- OAuth defaults are full non-admin only; manual tokens remain the category-scoped fallback.
- Prefixed tokens authenticate without breaking legacy tokens.
- Live schema push proof is recorded or execution remains blocked.
</success_criteria>

<output>
Create `.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-01-SUMMARY.md` when done.
</output>
