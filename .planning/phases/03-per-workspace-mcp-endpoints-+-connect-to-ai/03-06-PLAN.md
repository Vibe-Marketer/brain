---
phase: 03-per-workspace-mcp-endpoints-+-connect-to-ai
plan: 06
type: execute
wave: 5
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
  - "03-04"
  - "03-05"
files_modified:
  - supabase/functions/mcp-server/__tests__/workspace-scope.integration.test.ts
  - supabase/functions/mcp-server/__tests__/oauth-client-grants.integration.test.ts
  - supabase/functions/mcp-oauth-metadata/__tests__/workspace-resource.test.ts
  - src/components/settings/__tests__/McpConnectionsTab.test.tsx
  - src/components/settings/__tests__/McpSetupSnippets.test.tsx
  - docs/operations/mcp-runbook.md
  - docs/architecture/mcp-connectors.md
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
must_haves:
  truths:
    - "Phase 03 verification uses targeted tests, `npm run build`, and live endpoint proof instead of inferred completion."
    - "Production smoke proves workspace metadata, valid initialize/tools/list, and wrong-workspace 403 behavior on `api.callvaultai.com`."
    - "Runbook/docs reflect the shipped Phase 03 setup and revocation reality."
  artifacts:
    - path: "docs/operations/mcp-runbook.md"
      provides: "Operational runbook updated for workspace URLs, OAuth grants, and smoke commands"
    - path: "docs/architecture/mcp-connectors.md"
      provides: "User-facing technical setup reference for OAuth-first plus token fallback"
  key_links:
    - from: "docs/operations/mcp-runbook.md"
      to: "https://api.callvaultai.com/mcp/w/{workspace_uuid}"
      via: "live smoke commands"
      pattern: "mcp/w/"
---

<objective>
Close Phase 03 with real verification: final targeted tests, build, docs/runbook updates, deploy/live smoke, and production proof for the public workspace-scoped MCP surface.

Purpose: Prevent “planned and wired” from being confused with “verified and shippable.”
Output: Updated docs, final test passes, and live endpoint evidence.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-VALIDATION.md
@docs/operations/mcp-runbook.md
@.env
@.env.local
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full targeted Phase 03 automated verification set</name>
  <files>supabase/functions/mcp-server/__tests__/workspace-scope.integration.test.ts, supabase/functions/mcp-server/__tests__/oauth-client-grants.integration.test.ts, supabase/functions/mcp-oauth-metadata/__tests__/workspace-resource.test.ts, src/components/settings/__tests__/McpConnectionsTab.test.tsx, src/components/settings/__tests__/McpSetupSnippets.test.tsx</files>
  <read_first>
    - .planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-VALIDATION.md
    - supabase/functions/mcp-server/__tests__/workspace-scope.integration.test.ts
    - supabase/functions/mcp-server/__tests__/oauth-client-grants.integration.test.ts
    - supabase/functions/mcp-oauth-metadata/__tests__/workspace-resource.test.ts
    - src/components/settings/__tests__/McpConnectionsTab.test.tsx
    - src/components/settings/__tests__/McpSetupSnippets.test.tsx
  </read_first>
  <action>Run the targeted Phase 03 suites created in earlier plans, plus `npm run build`, and fix any final coverage drift in those same test files only if the assertions or fixtures need tightening to match the shipped behavior. Do not broaden production scope here; this plan is for final verification and small verification-alignment fixes only.</action>
  <acceptance_criteria>
    - The workspace routing, OAuth grant, metadata, Settings UI, and setup snippet tests all pass.
    - `npm run build` exits 0 on the final tree.
    - Any edits in this task are verification-alignment fixes, not new feature work.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/workspace-scope.integration.test.ts supabase/functions/mcp-server/__tests__/oauth-client-grants.integration.test.ts supabase/functions/mcp-oauth-metadata/__tests__/workspace-resource.test.ts src/components/settings/__tests__/McpConnectionsTab.test.tsx src/components/settings/__tests__/McpSetupSnippets.test.tsx && npm run build</automated>
  </verify>
  <done>The full targeted Phase 03 automated gate is green on the final tree.</done>
</task>

<task type="auto">
  <name>Task 2: Update runbook and MCP connector docs to match the shipped surface</name>
  <files>docs/operations/mcp-runbook.md, docs/architecture/mcp-connectors.md</files>
  <read_first>
    - docs/operations/mcp-runbook.md
    - .planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-UI-SPEC.md
    - .planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-RESEARCH.md
  </read_first>
  <action>Update `docs/operations/mcp-runbook.md` with the new workspace URL shape, per-workspace protected-resource metadata paths, OAuth grant revoke semantics, 401 vs 403 troubleshooting, and the exact smoke commands for `api.callvaultai.com`. Add or update `docs/architecture/mcp-connectors.md` so the technical setup docs reflect OAuth-first setup, provider capability caveats, and manual token fallback without any raw Supabase URLs or positive “AI-powered” copy.</action>
  <acceptance_criteria>
    - The runbook includes `/mcp/w/{workspace_uuid}` and the workspace protected-resource URL.
    - Docs explain OAuth-first setup plus manual token fallback accurately.
    - Docs mention permission changes are enforced immediately server-side, with possible client refresh/reconnect lag.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "mcp/w/|oauth-protected-resource/.*/mcp/w|refresh or reconnect|api.callvaultai.com" docs/operations/mcp-runbook.md docs/architecture/mcp-connectors.md</automated>
  </verify>
  <done>The docs match the actual Phase 03 implementation and troubleshooting model.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy and capture live production proof for Phase 03</name>
  <files>docs/operations/mcp-runbook.md</files>
  <read_first>
    - .env
    - .env.local
    - docs/operations/mcp-runbook.md
  </read_first>
  <action>Deploy the relevant Supabase functions and public proxy changes if required by the repo’s normal workflow, then smoke-test the production surface at `https://api.callvaultai.com`. Capture: workspace protected-resource metadata, invalid bearer 401 with `WWW-Authenticate`, valid workspace `initialize`, valid workspace `tools/list`, and a valid wrong-workspace request returning 403. Use existing local env credentials/tokens where available without printing secrets. If deploy credentials or valid test credentials are unavailable, record the exact missing proof and do not claim live verification is complete.</action>
  <acceptance_criteria>
    - Production metadata for a workspace path resolves.
    - Production invalid bearer returns 401 with discovery/auth headers.
    - Production valid workspace `initialize` and `tools/list` succeed.
    - Production wrong-workspace access returns 403.
    - Missing deploy or credential proof is treated as an explicit verification gap, not silently ignored.
  </acceptance_criteria>
  <verify>
    <automated>export MCP_WS_URL="https://api.callvaultai.com/mcp/w/${WORKSPACE_UUID}" && curl -i -sS "https://api.callvaultai.com/.well-known/oauth-protected-resource/mcp/w/${WORKSPACE_UUID}" | rg -n "HTTP/|resource" && curl -i -sS "$MCP_WS_URL" -H "Authorization: Bearer invalid-token" -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"initialize","id":1}' | rg -n "HTTP/|WWW-Authenticate" && test -n "$CALLVAULT_MCP_TOKEN" && curl -sS "$MCP_WS_URL" -H "Authorization: Bearer $CALLVAULT_MCP_TOKEN" -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"initialize","id":2}' | jq -e '.result.serverInfo.name == "callvault"' && curl -sS "$MCP_WS_URL" -H "Authorization: Bearer $CALLVAULT_MCP_TOKEN" -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"tools/list","id":3}' | jq -e '.result.tools | length > 0' && test -n "$MISMATCH_WORKSPACE_UUID" && curl -i -sS "https://api.callvaultai.com/mcp/w/${MISMATCH_WORKSPACE_UUID}" -H "Authorization: Bearer $CALLVAULT_MCP_TOKEN" -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"initialize","id":4}' | rg -n "HTTP/.*403|403"</automated>
  </verify>
  <done>Phase 03 has production proof or an explicit documented live-verification gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Final source tree -> deployed production endpoints | Verification must prove the deployed state, not just local code |
| Production endpoint -> real AI clients | Public contract and authorization behavior must match documentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Repudiation | completion claims | mitigate | Require targeted tests, build, and live smoke evidence before marking verified. |
| T-03-06-02 | Information Disclosure | runbook/setup docs | mitigate | Ensure docs use only public endpoints and correct revocation guidance. |
| T-03-06-03 | Elevation of Privilege | production workspace mismatch | mitigate | Live smoke must include wrong-workspace 403 proof. |
</threat_model>

<verification>
Run the targeted Phase 03 suite, build, update docs, then perform production smoke:

`npm test -- --run supabase/functions/mcp-server/__tests__/workspace-scope.integration.test.ts supabase/functions/mcp-server/__tests__/oauth-client-grants.integration.test.ts supabase/functions/mcp-oauth-metadata/__tests__/workspace-resource.test.ts src/components/settings/__tests__/McpConnectionsTab.test.tsx src/components/settings/__tests__/McpSetupSnippets.test.tsx && npm run build`

Then use the runbook’s `curl` commands against `https://api.callvaultai.com`, including workspace metadata, invalid-bearer 401, valid workspace `initialize`, valid workspace `tools/list`, and wrong-workspace 403. Required environment variables are `WORKSPACE_UUID`, `CALLVAULT_MCP_TOKEN`, and `MISMATCH_WORKSPACE_UUID`; if any are unavailable, record the missing live proof explicitly in `03-06-SUMMARY.md` and do not mark live verification complete.
</verification>

<success_criteria>
- Final targeted tests and build pass.
- Docs and runbook reflect the shipped Phase 03 surface.
- Production proof exists for metadata, valid workspace access, and wrong-workspace 403 behavior.
</success_criteria>

<output>
Create `.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-06-SUMMARY.md` when done.
</output>
