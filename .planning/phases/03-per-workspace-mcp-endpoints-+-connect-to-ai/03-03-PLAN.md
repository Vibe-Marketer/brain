---
phase: 03-per-workspace-mcp-endpoints-+-connect-to-ai
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/pages/OAuthConsentPage.tsx
  - src/pages/__tests__/OAuthConsentPage.workspace-scope.test.tsx
  - src/services/mcp-oauth-grants.service.ts
  - src/hooks/useMcpOAuthGrants.ts
autonomous: true
requirements:
  - MCP-02
  - MCP-03
must_haves:
  truths:
    - "D-05/D-06/D-07: OAuth consent defaults to organization scope and exposes an optional workspace checkbox + dropdown."
    - "D-08: Starting from a workspace-specific entrypoint preselects workspace scope and that workspace when available."
    - "D-01/D-04: Approval persists a full non-admin grant before redirecting the user back to the AI client."
  artifacts:
    - path: "src/pages/OAuthConsentPage.tsx"
      provides: "Workspace-aware OAuth consent UX and grant-write reconciliation"
    - path: "src/services/mcp-oauth-grants.service.ts"
      provides: "Pure Supabase data-access helpers for OAuth grant persistence from consent"
    - path: "src/pages/__tests__/OAuthConsentPage.workspace-scope.test.tsx"
      provides: "Consent regression coverage for org default, workspace toggle, and preselection"
  key_links:
    - from: "src/pages/OAuthConsentPage.tsx"
      to: "src/hooks/useMcpOAuthGrants.ts"
      via: "approval-side grant persistence hook"
      pattern: "approveAuthorization|workspace|usePersistMcpOAuthGrant"
---

<objective>
Upgrade the consent screen so CallVault captures scope correctly before OAuth approval: org by default, workspace when requested, and fast-path reconciliation when a consent request already knows the target workspace.

Purpose: Turn the backend grant model into a user-controlled, one-screen approval flow that matches the phase decisions.
Output: Consent page behavior, test coverage, and approval-side reconciliation rules.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-CONTEXT.md
@.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-UI-SPEC.md
@.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-VALIDATION.md
@src/CLAUDE.md
@src/pages/OAuthConsentPage.tsx
@src/services/mcp-tokens.service.ts
@src/hooks/useMcpTokens.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add consent-page coverage for org default and workspace scoping</name>
  <files>src/pages/__tests__/OAuthConsentPage.workspace-scope.test.tsx</files>
  <read_first>
    - src/pages/OAuthConsentPage.tsx
    - src/components/settings/__tests__/MCPTab.permissions.test.tsx
    - .planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-UI-SPEC.md
    - .planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: org scope is the default with the workspace checkbox unchecked per D-05 and D-06.
    - Test 2: checking the box reveals the workspace picker and approval stays disabled until a workspace is selected per D-07.
    - Test 3: workspace-specific entrypoints preselect workspace scope and the matching workspace per D-08.
  </behavior>
  <action>Create `OAuthConsentPage.workspace-scope.test.tsx` to pin the UI contract from 03-UI-SPEC.md: default org copy, workspace checkbox behavior, workspace dropdown reveal, live summary sentence changes, disabled approval state, and preselected workspace flows. Mock only the frontend dependencies; do not replace the underlying decision model with simplified local-only state.</action>
  <acceptance_criteria>
    - The test file covers default org scope, explicit workspace scope, and preselected workspace flows.
    - Assertions check the user-visible helper/scope summary copy from the UI contract.
    - Tests fail if approval can proceed without the required org/workspace selection.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Wave 0 must create src/pages/__tests__/OAuthConsentPage.workspace-scope.test.tsx first</automated>
  </verify>
  <done>The consent screen behavior is pinned before implementation changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement workspace-aware OAuth consent and grant-write reconciliation</name>
  <files>src/pages/OAuthConsentPage.tsx, src/services/mcp-oauth-grants.service.ts, src/hooks/useMcpOAuthGrants.ts</files>
  <read_first>
    - src/pages/OAuthConsentPage.tsx
    - src/services/mcp-tokens.service.ts
    - src/hooks/useMcpTokens.ts
    - src/components/settings/MCPTab.tsx
    - src/components/settings/IntegrationsTab.tsx
    - .planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: approving org scope writes a full non-admin organization grant per D-01, D-04, and D-05.
    - Test 2: approving workspace scope writes the selected workspace grant and excludes other workspaces per D-06 and D-07.
    - Test 3: consent fast paths preserve workspace intent and reconcile existing grants before redirecting per D-08.
  </behavior>
  <action>Create `src/services/mcp-oauth-grants.service.ts` with pure async helpers for approval-time grant persistence against `mcp_oauth_client_grants`, then wrap those helpers in `src/hooks/useMcpOAuthGrants.ts` using TanStack Query mutation patterns. Refactor `OAuthConsentPage.tsx` so it calls the hook/service boundary instead of writing Supabase rows inline. The approval flow must persist a CallVault OAuth grant before calling `approveAuthorization`, default to organization scope per D-05, add the optional workspace checkbox + dropdown per D-06/D-07, preselect workspace scope when the inbound request or entrypoint already identifies a workspace per D-08, and keep OAuth grants full non-admin only per D-01 and D-04. Reconcile or update an existing grant for the same `client_id + org/workspace` instead of creating duplicates during fast-path approvals; if consent details lack complete client metadata, persist available fields and rely on the Plan 03-01 first-request JWT reconciliation path to complete `client_id` metadata.</action>
  <acceptance_criteria>
    - `OAuthConsentPage.tsx` does not call `supabase.from('mcp_oauth_client_grants')` directly.
    - `src/services/mcp-oauth-grants.service.ts` owns the grant upsert/reconcile query.
    - `src/hooks/useMcpOAuthGrants.ts` exposes the approval-time mutation consumed by the page.
    - The consent UI renders the workspace checkbox and conditional workspace select.
    - Approval stores org scope by default and workspace scope only when explicitly selected.
    - Existing grant rows for the same client/scope are reconciled rather than duplicated blindly.
    - Copy matches the approved UI contract and does not use positive “AI-powered” language.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/pages/__tests__/OAuthConsentPage.workspace-scope.test.tsx && npm run build</automated>
  </verify>
  <done>OAuth approval writes the right scope every time and preserves workspace intent when the flow starts from a workspace surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser consent UI -> persisted grant | User selections become authorization state |
| Authorization details -> consent fast path | Inbound OAuth request context influences preselected scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Elevation of Privilege | consent defaults | mitigate | Default to organization non-admin only; workspace scope must be explicit per D-05 through D-07. |
| T-03-03-02 | Tampering | fast-path reconciliation | mitigate | Reconcile grants by `client_id + scope` instead of creating implicit broader access on retry. |
| T-03-03-03 | Repudiation | grant creation from consent | mitigate | Persist scope choices before approval and cover both paths in component tests. |
</threat_model>

<verification>
Run:

`npm test -- --run src/pages/__tests__/OAuthConsentPage.workspace-scope.test.tsx && npm run build`
</verification>

<success_criteria>
- OAuth consent matches D-05 through D-08 exactly.
- Approval persists full non-admin grants only.
- Workspace-originated flows keep the user’s workspace intent instead of resetting to org scope.
</success_criteria>

<output>
Create `.planning/phases/03-per-workspace-mcp-endpoints-+-connect-to-ai/03-03-SUMMARY.md` when done.
</output>
