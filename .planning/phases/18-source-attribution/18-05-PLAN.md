---
phase: 18-source-attribution
plan: 05
type: execute
wave: 3
depends_on: [18-01, 18-04]
files_modified:
  - repo: brain
    paths:
      - src/services/admin-dashboard.service.ts
      - src/services/__tests__/admin-dashboard.service.test.ts
      - src/hooks/useTickets.ts
      - src/pages/admin/DashboardSection.tsx
      - src/pages/admin/TicketsSection.tsx
autonomous: true
requirements: [SRC-02, SRC-03]
must_haves:
  truths:
    - "Dashboard shows per-source volume, fix rate, and cycle time."
    - "Tickets page shows source mix metrics near the source controls."
    - "Metrics use the same plain-English source labels and no Phase 19 survival/autonomy ladder appears."
  artifacts:
    - path: "src/services/admin-dashboard.service.ts"
      provides: "Typed source metrics service contract"
      exports: ["TicketSourceMetrics"]
    - path: "src/hooks/useTickets.ts"
      provides: "TanStack Query hook for Tickets page source metrics"
      contains: "useTicketSourceMetrics"
    - path: "src/pages/admin/DashboardSection.tsx"
      provides: "Dashboard Tickets by Source metrics surface"
      contains: "Tickets by Source"
    - path: "src/pages/admin/TicketsSection.tsx"
      provides: "Tickets Source mix metrics strip"
      contains: "Source mix"
  key_links:
    - from: "public.ticket_source_metrics()"
      to: "src/services/admin-dashboard.service.ts"
      via: "supabase.rpc"
      pattern: "ticket_source_metrics"
    - from: "src/services/admin-dashboard.service.ts"
      to: "src/pages/admin/DashboardSection.tsx"
      via: "AdminDashboardStats.sourceMetrics"
      pattern: "sourceMetrics"
    - from: "src/hooks/useTickets.ts"
      to: "src/pages/admin/TicketsSection.tsx"
      via: "useTicketSourceMetrics"
      pattern: "Source mix"
---

<objective>
Surface per-source metrics where operators naturally look: Dashboard and Tickets.

Purpose: Phase 18 is the measurement substrate for tier-2 routing, QA broadening, Sentry cycle-time work, and later reporter comms. Operators need volume, fix rate, and cycle time by source without leaving existing Admin Center surfaces.
Output: Typed metrics service/hook, Dashboard `Tickets by Source`, and Tickets `Source mix` summary.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-source-attribution/18-CONTEXT.md
@.planning/phases/18-source-attribution/18-UI-SPEC.md
@.planning/phases/18-source-attribution/18-PATTERNS.md
@src/CLAUDE.md
@src/services/admin-dashboard.service.ts
@src/hooks/useAdminDashboard.ts
@src/hooks/useTickets.ts
@src/pages/admin/DashboardSection.tsx
@src/pages/admin/TicketsSection.tsx
</context>

## Artifacts this phase produces

- `TicketSourceMetrics` service contract.
- Dashboard per-source metrics card/section.
- Tickets source-mix summary near source controls, clickable as one-click source filters.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed source metrics service and hook</name>
  <files>src/services/admin-dashboard.service.ts, src/services/__tests__/admin-dashboard.service.test.ts, src/hooks/useTickets.ts</files>
  <read_first>
    - supabase/migrations/20260613180500_source_attribution_backfill_metrics.sql
    - src/services/admin-dashboard.service.ts
    - src/services/__tests__/admin-dashboard.service.test.ts
    - src/hooks/useTickets.ts
    - src/lib/query-config.ts
  </read_first>
  <behavior>
    - Service maps `ticket_source_metrics` RPC rows to `{ source, volume, resolved, fixRate, averageCycleTimeHours }`.
    - `fetchDashboardStats()` includes source metrics without removing existing ticket status stats.
    - Tickets page has a hook-level TanStack Query wrapper; components do not call services directly.
  </behavior>
  <action>Add a `TicketSourceMetrics` interface and a `getTicketSourceMetrics()` service helper in `admin-dashboard.service.ts` or an equivalent existing admin service location. Use `supabase.rpc('ticket_source_metrics')` and normalize snake_case RPC fields to camelCase. Extend `AdminDashboardStats` to include `sourceMetrics`, and add a `useTicketSourceMetrics()` hook in `useTickets.ts` with a stable query key. Keep service + hook separation and avoid any frontend AI or new packages. Add tests for RPC mapping, null cycle time, whole-number fix-rate formatting inputs if formatting helpers are added, and failure behavior consistent with existing dashboard stats tests.</action>
  <verify>
    <automated>npm test -- src/services/__tests__/admin-dashboard.service.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - Metrics service returns all five source buckets when the RPC returns them.
    - Existing dashboard runner/deploy/health stats still populate.
    - Tickets components consume metrics only through a hook, not direct service calls.
  </acceptance_criteria>
  <done>Per-source metrics are available through typed services/hooks for both Dashboard and Tickets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render Dashboard Tickets by Source metrics</name>
  <files>src/pages/admin/DashboardSection.tsx, src/services/__tests__/admin-dashboard.service.test.ts</files>
  <read_first>
    - .planning/phases/18-source-attribution/18-UI-SPEC.md
    - src/pages/admin/DashboardSection.tsx
    - src/services/admin-dashboard.service.ts
    - src/lib/ticket-display.ts
  </read_first>
  <behavior>
    - Dashboard renders "Tickets by Source" in the existing stats area.
    - Each source row shows source label, volume, whole-percent fixed rate, and formatted average cycle time.
    - Null/no resolved cycle time renders "No cycle time yet".
  </behavior>
  <action>Add a compact Dashboard source metrics card or section in the existing dashboard grid, satisfying D-09 for the Dashboard location. Do not replace the existing Need attention and Resolved headline stats. Use `ticketSourceLabel()` for labels, `tabular-nums` for numbers, and the UI-SPEC cycle-time formatting (`N h`, `N d`, or `No cycle time yet`). Limit metrics to D-10 volume, fix rate, and cycle time; do not add Phase 19 survival rate, autonomy ladder, or canary metrics.</action>
  <verify>
    <automated>npm test -- src/services/__tests__/admin-dashboard.service.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard source metrics are visible inside existing Admin Center Dashboard surfaces.
    - No raw enum source values render in the Dashboard.
    - The section remains readable at mobile widths and uses semantic Tailwind tokens.
  </acceptance_criteria>
  <done>Dashboard gives operators per-source volume, fixed rate, and cycle-time visibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render Tickets Source mix summary with one-click filtering</name>
  <files>src/pages/admin/TicketsSection.tsx</files>
  <read_first>
    - .planning/phases/18-source-attribution/18-UI-SPEC.md
    - src/pages/admin/TicketsSection.tsx
    - src/hooks/useTickets.ts
    - src/lib/ticket-display.ts
  </read_first>
  <behavior>
    - Tickets page renders "Source mix" near source controls.
    - Each source item shows volume, fixed rate, and average cycle time.
    - Clicking a source item sets that source filter and resets page to 1.
  </behavior>
  <action>Use `useTicketSourceMetrics()` in `TicketsSection` and render an unframed compact source summary near the source filters, satisfying D-09 for the Tickets location. Each source summary item must be a `<button type="button">` with selected state (`aria-pressed` or equivalent), plain-English source labels, and one-click filter behavior. Follow UI-SPEC copy for the D-10 metric set: `{volume} tickets`, `{fixRate}% fixed`, `{cycleTime} avg cycle` or `No cycle time yet`. Do not nest cards inside the Tickets surface and do not hide metrics on mobile.</action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npm test -- src/services/__tests__/admin-dashboard.service.test.ts src/lib/__tests__/ticket-display.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source mix summary appears on Tickets page near source controls.
    - Summary item clicks update the existing source filter and reset pagination.
    - UI copy has no raw enum values and no "AI-powered" language.
  </acceptance_criteria>
  <done>Tickets page gives operators per-source metrics and one-click source filtering in the existing surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres metrics RPC -> browser admin UI | Aggregate operational metrics leave the database and render in admin-only pages |
| operator metric buttons -> ticket list filter | Metric buttons drive source filters but must not mutate ticket source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-15 | Information Disclosure | `getTicketSourceMetrics()` | mitigate | Consume only admin-guarded RPC from Plan 01; no reporter-visible route or component uses it. |
| T-18-16 | Spoofing | source metric labels | mitigate | Render via `ticketSourceLabel()` from Plan 04, never raw enum text. |
| T-18-17 | Tampering | Source mix buttons | mitigate | Buttons set query filters only; no mutation or source edit endpoint is introduced. |
| T-18-18 | Denial of Service | metrics query | mitigate | Use aggregate RPC instead of unbounded ticket-row fetching in components. |
| T-18-SC | Tampering | npm installs | accept | No package installs are planned; use existing shadcn/Tailwind primitives. |
</threat_model>

<verification>
- `npm test -- src/services/__tests__/admin-dashboard.service.test.ts src/lib/__tests__/ticket-display.test.ts`
- `npm run type-check`
- `npm run build`
- Browser screenshots of `/admin/dashboard` and `/admin/tickets` showing source metrics with no raw enum labels.
</verification>

<success_criteria>
SRC-03 is visible and actionable in both operator contexts required by D-09, and SRC-02 source controls gain a one-click metrics-to-filter path without a new page.
</success_criteria>

<output>
Create `.planning/phases/18-source-attribution/18-05-SUMMARY.md` when done.
</output>
