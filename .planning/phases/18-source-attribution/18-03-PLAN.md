---
phase: 18-source-attribution
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - repo: autopilot
    paths:
      - /Users/admin/dev/autopilot/src/watchdog.ts
      - /Users/admin/dev/autopilot/qa/triage.ts
      - /Users/admin/dev/autopilot/src/watchdog.test.ts
      - /Users/admin/dev/autopilot/qa/triage.test.ts
autonomous: true
requirements: [SRC-01]
must_haves:
  truths:
    - "Watchdog-created operational tickets stamp `internal`, not `manual`."
    - "The current QA crawler/triage path stamps `nightly_qa`, not `manual`, without implementing Phase 20 flake suppression."
    - "Autopilot source stamping uses service-owned code paths, never client-controlled ticket source input."
  artifacts:
    - path: "/Users/admin/dev/autopilot/src/watchdog.ts"
      provides: "Internal watchdog source stamping"
      contains: "source: \"internal\""
    - path: "/Users/admin/dev/autopilot/qa/triage.ts"
      provides: "Nightly QA source stamping path"
      contains: "nightly_qa"
    - path: "/Users/admin/dev/autopilot/qa/triage.test.ts"
      provides: "Regression coverage for QA source attribution"
  key_links:
    - from: "/Users/admin/dev/brain/supabase/migrations/20260613180000_extend_ticket_source_enum.sql"
      to: "/Users/admin/dev/autopilot/src/watchdog.ts"
      via: "shared Supabase DB enum"
      pattern: "internal"
    - from: "/Users/admin/dev/autopilot/qa/triage.ts"
      to: "tickets.source"
      via: "service-role owned insert or minimal RPC"
      pattern: "nightly_qa"
---

<objective>
Fix active source mis-attribution in the external autopilot repo after the brain schema supports the new enum values.

Purpose: Updating only the brain repo would leave the real watchdog and QA paths still filing operational tickets as person reports, which would break source metrics and Phase 23 comms safety.
Output: Autopilot watchdog and QA triage stamp their real origins and have repo-native tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-source-attribution/18-CONTEXT.md
@.planning/phases/18-source-attribution/18-RESEARCH.md
@.planning/phases/18-source-attribution/18-PATTERNS.md
@docs/architecture/autopilot-brain-ownership.md
@/Users/admin/dev/autopilot/src/watchdog.ts
@/Users/admin/dev/autopilot/qa/triage.ts
@/Users/admin/dev/autopilot/src/lib/db.ts
</context>

## Artifacts this phase produces

- Watchdog/internal ticket creation stamping `internal`.
- QA crawler/triage ticket creation stamping `nightly_qa`.
- Autopilot tests proving operational sources no longer route through the person-report source.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp watchdog tickets as internal</name>
  <files>/Users/admin/dev/autopilot/src/watchdog.ts, /Users/admin/dev/autopilot/src/watchdog.test.ts</files>
  <read_first>
    - docs/architecture/autopilot-brain-ownership.md
    - .planning/phases/18-source-attribution/18-CONTEXT.md
    - .planning/phases/18-source-attribution/18-PATTERNS.md
    - /Users/admin/dev/autopilot/src/watchdog.ts
    - /Users/admin/dev/autopilot/src/lib/db.ts
  </read_first>
  <behavior>
    - Tools-health/watchdog tickets insert `source: "internal"`.
    - Existing watchdog dedupe, severity, reporter, and context fields are preserved.
    - No source value is read from ticket text or external input.
  </behavior>
  <action>In `/Users/admin/dev/autopilot`, add or extend a Bun test around the watchdog ticket insert builder/path, then change only the source stamp for watchdog-created tickets from `manual` to `internal`. Preserve `context.origin = "autopilot-watchdog"` and the existing dedupe query. This implements D-04 and SRC-01 without changing daemon concurrency, scheduling, or ticket claiming behavior.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && bun test src/watchdog.test.ts</automated>
    <automated>cd /Users/admin/dev/autopilot && bun run typecheck</automated>
    <automated>grep -n 'source: "internal"' /Users/admin/dev/autopilot/src/watchdog.ts</automated>
  </verify>
  <acceptance_criteria>
    - Watchdog ticket insert source is `internal`.
    - No watchdog-created ticket path still stamps `source: "manual"`.
    - Existing watchdog context markers and dedupe behavior remain intact.
  </acceptance_criteria>
  <done>Internal watchdog tickets can no longer be mistaken for person-reported tickets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stop QA triage from filing manual tickets</name>
  <files>/Users/admin/dev/autopilot/qa/triage.ts, /Users/admin/dev/autopilot/qa/triage.test.ts</files>
  <read_first>
    - .planning/phases/18-source-attribution/18-CONTEXT.md
    - .planning/phases/18-source-attribution/18-RESEARCH.md
    - .planning/phases/18-source-attribution/18-PATTERNS.md
    - /Users/admin/dev/autopilot/qa/triage.ts
    - /Users/admin/dev/autopilot/src/lib/db.ts
  </read_first>
  <behavior>
    - QA triage no longer calls `send-support-ticket` for new findings because that path is person-report/manual by design.
    - QA-created tickets stamp `source: "nightly_qa"` through service-owned direct insert or the minimal RPC created in Plan 01 if available.
    - File-based known-fingerprint behavior is preserved; rerun quarantine, DB dedupe replacement, and full `ingest_qa_ticket` remain Phase 20.
  </behavior>
  <action>Refactor the current QA filing helper so it uses a service-role owned ticket insert path that stamps `nightly_qa`, sets explicit QA context markers such as `origin: "qa-nightly-crawler"`, and preserves the existing known-fingerprints persistence. Do not add Phase 20 flake suppression, actionability gates, source budgets, or full `ingest_qa_ticket` DB dedupe here. Add tests that fail if triage invokes `send-support-ticket` or writes `source: "manual"` for QA findings.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && bun test qa/triage.test.ts</automated>
    <automated>cd /Users/admin/dev/autopilot && bun run typecheck</automated>
    <automated>test "$(grep -R \"send-support-ticket\" /Users/admin/dev/autopilot/qa/triage.ts | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - QA findings stamp `nightly_qa`.
    - No QA code path uses the browser/person support-ticket function for ticket creation.
    - Known-fingerprint file behavior remains intact for Phase 18.
  </acceptance_criteria>
  <done>Active QA mis-attribution to manual/person reports is gone without pulling Phase 20 into this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autopilot daemon -> shared Supabase tickets table | Service-role daemon writes operational tickets into brain-owned schema |
| QA finding payload -> ticket row | Browser-crawled finding content is untrusted and must not decide source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Spoofing | `/Users/admin/dev/autopilot/src/watchdog.ts` | mitigate | Hard-code watchdog source to `internal` in service-owned code. |
| T-18-09 | Spoofing | `/Users/admin/dev/autopilot/qa/triage.ts` | mitigate | Hard-code QA source to `nightly_qa`; do not accept source from findings or browser data. |
| T-18-10 | Information Disclosure | QA ticket context | mitigate | Preserve concise operational context; do not add reporter comms or customer notification behavior. |
| T-18-11 | Tampering | shared DB enum contract | mitigate | Depends on Plan 01 live schema/types before source values are written. |
| T-18-SC | Tampering | npm installs | accept | Autopilot uses existing Bun/TypeScript dependencies; no new packages. |
</threat_model>

<verification>
- `cd /Users/admin/dev/autopilot && bun test src/watchdog.test.ts qa/triage.test.ts`
- `cd /Users/admin/dev/autopilot && bun run typecheck`
- `grep -R 'source: "manual"' /Users/admin/dev/autopilot/src/watchdog.ts /Users/admin/dev/autopilot/qa/triage.ts` returns no operational insert source stamps.
</verification>

<success_criteria>
SRC-01 is true at the live operational intake paths: watchdog means `internal`, QA means `nightly_qa`, and neither path can look like a person report.
</success_criteria>

<output>
Create `.planning/phases/18-source-attribution/18-03-SUMMARY.md` when done.
</output>
