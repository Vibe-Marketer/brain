---
phase: 18-source-attribution
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - repo: brain
    paths:
      - supabase/functions/send-support-ticket/index.ts
      - supabase/functions/send-support-ticket/__tests__/source-stamping.test.ts
      - src/services/tickets.service.ts
      - src/services/__tests__/tickets.service.test.ts
autonomous: true
requirements: [SRC-01, SRC-02]
must_haves:
  truths:
    - "Browser support-ticket requests cannot spoof source to look like a person report or operational source."
    - "Person-reported tickets still stamp `manual` server-side per D-02."
    - "System tickets with nullable reporter IDs do not break the ticket list service."
  artifacts:
    - path: "supabase/functions/send-support-ticket/__tests__/source-stamping.test.ts"
      provides: "Regression proof that source is server-owned"
    - path: "src/services/tickets.service.ts"
      provides: "Null-safe reporter lookup and expanded TicketSource usage"
    - path: "src/services/__tests__/tickets.service.test.ts"
      provides: "Source filter and nullable reporter regression coverage"
  key_links:
    - from: "supabase/functions/send-support-ticket/index.ts"
      to: "tickets.source"
      via: "server-owned insert value"
      pattern: "source: 'manual'"
    - from: "src/services/tickets.service.ts"
      to: "src/pages/admin/TicketsSection.tsx"
      via: "TicketFilters.source typed from generated enum"
      pattern: "source?: TicketSource | 'all'"
---

<objective>
Harden the brain-side ticket intake and ticket-list service around the new source taxonomy.

Purpose: Phase 23 reporter comms must later trust that `manual` means a server-stamped person report, not a browser-supplied spoof, and AdminTab must tolerate non-person tickets.
Output: Source-spoofing regression coverage, person-report source stamping preserved, and nullable reporter handling fixed.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-source-attribution/18-CONTEXT.md
@.planning/phases/18-source-attribution/18-RESEARCH.md
@.planning/phases/18-source-attribution/18-PATTERNS.md
@supabase/CLAUDE.md
@src/CLAUDE.md
@supabase/functions/send-support-ticket/index.ts
@src/services/tickets.service.ts
@src/services/__tests__/tickets.service.test.ts
</context>

## Artifacts this phase produces

- Regression coverage proving support-ticket source is server-stamped, not trusted from client JSON.
- Ticket list service behavior that tolerates `reporter_id = NULL` system tickets.
- Source filtering tests expanded for the new enum values generated by Plan 01.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove support-ticket source is server-owned</name>
  <files>supabase/functions/send-support-ticket/index.ts, supabase/functions/send-support-ticket/__tests__/source-stamping.test.ts</files>
  <read_first>
    - .planning/phases/18-source-attribution/18-CONTEXT.md
    - .planning/phases/18-source-attribution/18-PATTERNS.md
    - supabase/functions/send-support-ticket/index.ts
    - supabase/CLAUDE.md
  </read_first>
  <behavior>
    - A request body containing `source: "internal"` or `source: "nightly_qa"` does not influence the inserted ticket source.
    - The only source stamped by the person-report Edge Function is `manual`.
    - No request schema field exposes `source`.
  </behavior>
  <action>Add a focused test for `send-support-ticket` that inspects the schema/insert boundary or invokes the handler with mocked Supabase clients, matching existing Edge Function test style. If the current implementation already ignores body `source`, keep code changes minimal and let the new test lock the behavior. Do not add a trusted-source override to this browser-facing function. This implements SRC-01, D-02, D-04, and the Phase 23 spoofing gate.</action>
  <verify>
    <automated>npm test -- supabase/functions/send-support-ticket/__tests__/source-stamping.test.ts</automated>
    <automated>grep -n "source:" supabase/functions/send-support-ticket/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test fails if `send-support-ticket` accepts client-supplied source.
    - Insert path still stamps `source: 'manual'` for authenticated person reports.
    - Edge Function auth remains `authenticateRequest(req, supabase, corsHeaders)`.
  </acceptance_criteria>
  <done>Client input cannot spoof ticket source through the support-ticket Edge Function.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make ticket list service source-aware and nullable-reporter safe</name>
  <files>src/services/tickets.service.ts, src/services/__tests__/tickets.service.test.ts</files>
  <read_first>
    - src/services/tickets.service.ts
    - src/services/__tests__/tickets.service.test.ts
    - .planning/phases/18-source-attribution/18-RESEARCH.md
    - src/CLAUDE.md
  </read_first>
  <behavior>
    - `getTickets({ source: "nightly_qa" })` applies `.eq("source", "nightly_qa")`.
    - Rows with `reporter_id: null` are returned without querying `user_profiles` for null.
    - Reporter display for system rows is stable and does not crash sorting/table rendering.
  </behavior>
  <action>Update `getTickets()` so `reporterIds` filters out null/empty IDs before querying `user_profiles`. Keep service + hook separation. Preserve the existing paginated query and source filter shape, but extend tests to cover `nightly_qa`, `internal`, and `unknown` source filters now that Plan 01 regenerated types. Do not import UI components into the service; if a reporter fallback string is needed for null reporters, keep it plain and service-local.</action>
  <verify>
    <automated>npm test -- src/services/__tests__/tickets.service.test.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - Nullable system-ticket reporters no longer enter the user profile `.in()` query.
    - New source values are accepted by TypeScript through `Database['public']['Enums']['ticket_source']`.
    - Existing pagination and status/severity filters continue to work.
  </acceptance_criteria>
  <done>Ticket reads support new source values and system tickets without broad query or UI rewrites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> send-support-ticket Edge Function | Authenticated browser can submit body fields but must not control source |
| Supabase rows -> Admin ticket list service | Rows may have system/null reporters and new source enum values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Spoofing | `send-support-ticket` | mitigate | Keep source stamped server-side as `manual`; add regression test rejecting client-controlled source. |
| T-18-06 | Tampering | request schema | mitigate | Do not add `source` to the Zod body schema for browser/person reports. |
| T-18-07 | Denial of Service | `getTickets()` reporter lookup | mitigate | Filter null reporter IDs before `.in()` so system tickets do not cause bad profile queries. |
| T-18-SC | Tampering | npm installs | accept | No package installs are planned; zero new npm packages. |
</threat_model>

<verification>
- `npm test -- supabase/functions/send-support-ticket/__tests__/source-stamping.test.ts`
- `npm test -- src/services/__tests__/tickets.service.test.ts`
- `npm run type-check`
</verification>

<success_criteria>
SRC-01 intake trust is explicit for brain-side person reports, and SRC-02 source filters can operate on system and person tickets without service-layer crashes.
</success_criteria>

<output>
Create `.planning/phases/18-source-attribution/18-02-SUMMARY.md` when done.
</output>
