---
phase: 35-speaker-resolution-across-sources
plan: 04
type: execute
wave: 4
depends_on: [35-03]
files_modified: []
autonomous: false
requirements: [IDENT-04, IDENT-05]
must_haves:
  truths:
    - "Full unit + integration suites are green and a live TEST introspection proof exists BEFORE any prod apply is authorized."
    - "Prod apply (deploy resolve-speakers + any B2 migration) happens only behind an explicit human checkpoint, prod-ref-guarded before and after."
    - "resolve-speakers ships inert-until-invoked in prod — no cron wiring, mechanism proven inert by introspection."
  artifacts:
    - path: ".planning/phases/35-speaker-resolution-across-sources/35-04-SUMMARY.md"
      provides: "Prod-apply evidence: introspection proof + prod-ref-guard confirmations"
      contains: "vltmrnjsubfzrgrtdqey"
  key_links:
    - from: "prod apply"
      to: "prod ref vltmrnjsubfzrgrtdqey"
      via: "prod-ref guard verified before AND after"
      pattern: "vltmrnjsubfzrgrtdqey"
---

<objective>
Gate the production apply behind an explicit human checkpoint after a full green-suite + live TEST introspection proof, mirroring every prior phase's Plan-N-final pattern (31-04, 32-04/05, 33-03, 34-07).

Purpose: Phase 35 touches identity-resolution provenance on live prod with real customer data on the `v2.2-event-resolution` branch. Nothing reaches prod without a proof-then-checkpoint sequence and the milestone's 3x prod-ref-guard discipline.

Output: prod-apply evidence recorded in the summary.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-speaker-resolution-across-sources/35-01-SUMMARY.md
@.planning/phases/35-speaker-resolution-across-sources/35-03-SUMMARY.md
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Green-suite + live TEST introspection proof</name>
  <read_first>
    - .planning/phases/35-speaker-resolution-across-sources/35-03-SUMMARY.md (what was applied to TEST, what's queued for prod)
    - supabase/CLAUDE.md (env-file map: .env=PROD ref vltmrnjsubfzrgrtdqey, prod-ref guard)
  </read_first>
  <action>
    Run the full unit suite (`rtk vitest run`) and the resolve-speakers integration test on TEST; both must be green. Then perform a live TEST-project introspection proof mirroring prior phases' SUMMARY discipline: confirm the resolve-speakers function's behavior, and — if Plan 01 locked B2 — confirm the ledger's FORCE RLS + EXECUTE-revoked state and zero-rows inert state on TEST. Record the exact commands + outputs. Do NOT touch prod in this task.
  </action>
  <verify>
    <automated>rtk vitest run && npm run test:integration -- resolve-speakers</automated>
  </verify>
  <acceptance_criteria>
    - Full unit suite + resolve-speakers integration green.
    - TEST introspection proof recorded (function state; ledger RLS/EXECUTE state if B2).
    - No prod mutation performed in this task.
  </acceptance_criteria>
  <done>Green suites + TEST introspection proof captured; ready for the prod gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A forward-only, X-Reconcile-Secret-gated `resolve-speakers` edge function (+ conditional `speaker_resolution_decisions` ledger migration if B2 was locked in Plan 01), proven green on TEST with a live introspection proof from Task 1. Nothing is in prod yet.
  </what-built>
  <how-to-verify>
    Review the Task 1 introspection proof and Plan 03's TEST evidence. On approval, the executor will, with the prod-ref guard confirming `vltmrnjsubfzrgrtdqey` BEFORE and AFTER each step:
    1. (if B2) apply the speaker_resolution_decisions migration to prod, then introspect to confirm FORCE RLS true + EXECUTE revoked to anon/authenticated + zero rows.
    2. Deploy resolve-speakers: `supabase functions deploy resolve-speakers --use-api --no-verify-jwt` (shared-secret auth in app code, reuses existing RECONCILE_SECRET — no new secret).
    3. Confirm the function is live and inert-until-invoked (no cron wired this phase).
    Do NOT enable event_resolution/speaker-resolution for any org beyond the existing SAFE-01-flagged test org (Clickable Impact) — and per the Phase 33 P03 standing finding, check that org's own recordings↔transcript_chunks linkage before assuming inertness.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the prod apply (or "approved, scope: ..." to expand), or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TEST-proven artifact → prod | crossing only behind explicit human approval + prod-ref guard before/after |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-08 | Tampering | applying a migration/deploy against the WRONG Supabase project (prod/TEST confusion — catastrophic per the 2026-05 incident) | mitigate | prod-ref guard confirms vltmrnjsubfzrgrtdqey before AND after every prod step; TEST work never reads .env. |
| T-35-09 | Elevation of Privilege | resolve-speakers going live active (cron-wired) before human sign-off widens the automated write surface | mitigate | ships inert-until-invoked, no cron this phase; inertness proven by introspection. |
| T-35-02 | Information Disclosure | ledger (B2) reaching prod without RLS | mitigate | post-apply introspection confirms FORCE RLS + EXECUTE revoked before the plan closes. |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase; legitimacy gate N/A. |
</threat_model>

<artifacts>
## Artifacts this phase produces (Plan 04)
- `.planning/phases/35-speaker-resolution-across-sources/35-04-SUMMARY.md` — prod-apply evidence (introspection + prod-ref-guard confirmations), or a "prod apply deferred by Andrew" note if the checkpoint is held.
</artifacts>

<verification>
- Full unit suite + integration green (Task 1).
- Post-apply prod introspection confirms resolve-speakers live + inert; ledger (if B2) FORCE RLS + EXECUTE revoked.
- Prod ref vltmrnjsubfzrgrtdqey confirmed before and after every prod step.
</verification>

<success_criteria>
- Phase 35 mechanism is live in prod, inert-until-invoked, proven by introspection — or explicitly deferred at Andrew's discretion, recorded in the summary.
</success_criteria>

<output>
Create `.planning/phases/35-speaker-resolution-across-sources/35-04-SUMMARY.md` when done.
</output>
