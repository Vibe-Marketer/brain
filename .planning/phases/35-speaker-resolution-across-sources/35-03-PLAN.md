---
phase: 35-speaker-resolution-across-sources
plan: 03
type: execute
wave: 3
depends_on: [35-02]
files_modified:
  - supabase/functions/resolve-speakers/index.ts
  - supabase/migrations/[timestamp]_speaker_resolution.sql
  - src/test/integration/resolve-speakers.integration.test.ts
  - src/test/rls-regression.test.ts
autonomous: true
requirements: [IDENT-04, IDENT-05]
must_haves:
  truths:
    - "resolve-speakers rejects any request missing/mismatching X-Reconcile-Secret with 401 BEFORE any DB work or body parse."
    - "The edge function is forward-only, delegates every matching decision to speaker-resolver.ts, and writes resolutions only through the identity spine / locked write-target — never over transcript_chunks in place."
    - "A synthetic integration fixture on TEST proves a real cross-source propagation end-to-end, and cross-org isolation is enforced (no cross-org pairing)."
  artifacts:
    - path: "supabase/functions/resolve-speakers/index.ts"
      provides: "Forward-only, shared-secret-gated edge function wrapping speaker-resolver.ts"
      contains: "X-Reconcile-Secret"
    - path: "src/test/integration/resolve-speakers.integration.test.ts"
      provides: "TEST-project synthetic end-to-end proof + cross-org isolation"
      contains: "canonical_recording_id"
  key_links:
    - from: "resolve-speakers/index.ts"
      to: "speaker-resolver.ts pure functions"
      via: "import + delegate; no matching logic in the edge body"
      pattern: "speaker-resolver"
    - from: "resolve-speakers/index.ts"
      to: "identity spine / locked write-target"
      via: "evidence row or ledger insert (never transcript_chunks overwrite)"
      pattern: "identity"
---

<objective>
Wire the Plan 02 pure module into a forward-only, shared-secret-gated `resolve-speakers` edge function; persist resolutions through the write-target locked in Plan 01; prove it end-to-end against synthetic TEST fixtures with cross-org isolation.

Purpose: Turn proven correctness into a live-invokable sweep, mirroring `resolve-identities`/`resolve-events` exactly (inert-until-invoked, no cron wiring this plan). If Plan 01 locked B2 (new ledger), this plan authors that migration TEST-only.

Output: edge function + (conditional) migration + integration proof.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-speaker-resolution-across-sources/35-DESIGN-NOTES.md
@.planning/phases/35-speaker-resolution-across-sources/35-01-SUMMARY.md
@supabase/CLAUDE.md

<interfaces>
- supabase/functions/resolve-identities/index.ts — copy this shape EXACTLY: 401-before-body-parse X-Reconcile-Secret gate, Zod `{ mode: 'forward', since?: ISO }`, service-role client, forward-only cutover default, delegate to _shared module, return summary. Deploy deferred to Plan 04.
- supabase/functions/_shared/speaker-resolver.ts — the pure functions to delegate to (Plan 02).
- src/test/rls-regression.test.ts — cross-org isolation harness (CROSS_ORG_TABLES / CLIENT_DENY_TABLES + buildClientDenySeed from Phase 34). Register any new ledger table here.
- Phase 33 fixture seeding precedent: transcript_chunks rows with `recording_id: NULL`, keyed by `canonical_recording_id`; delete workspace_entries before recordings in cleanup (Phase 31 BEFORE DELETE trigger finding); check `.error` on every cleanup step.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: resolve-speakers edge function + (conditional) write-target migration</name>
  <read_first>
    - supabase/functions/resolve-identities/index.ts (exact gate/Zod/forward-only shape to mirror)
    - .planning/phases/35-speaker-resolution-across-sources/35-01-SUMMARY.md (locked A + B decisions)
    - supabase/CLAUDE.md (migration discipline, TEST-then-prod, prod-ref guard — prod apply is Plan 04 only)
  </read_first>
  <action>
    Create `supabase/functions/resolve-speakers/index.ts` mirroring resolve-identities: (1) X-Reconcile-Secret 401 gate BEFORE body parse; (2) Zod `{ mode:'forward', since? }`; (3) service-role reads of transcript_chunks (by event_id via recordings.canonical/event linkage), call_participants, identities/identity_aliases; (4) same-org bucketing; (5) delegate ALL matching to speaker-resolver.ts; (6) write resolutions ONLY via the Plan-01 write-target — if B1, an identity_aliases evidence row preserving get_identity_evidence's owner/non-owner redaction; if B2, insert into the new ledger. NEVER UPDATE transcript_chunks.speaker_name/speaker_email in place (Anti-Pattern; reserved for Phase 37). Ship inert-until-invoked; no cron wiring. If Plan 01 locked B2, author the migration for `speaker_resolution_decisions` (mirror event_match_decisions: tier/score/signal-breakdown/decided_by/timestamp, FORCE RLS, EXECUTE-revoked/service-role-only) and apply it TEST-ONLY (relink to prod and verify unchanged after; prod apply is Plan 04). If B1, no migration.
  </action>
  <verify>
    <automated>grep -q "X-Reconcile-Secret" supabase/functions/resolve-speakers/index.ts &amp;&amp; grep -q "speaker-resolver" supabase/functions/resolve-speakers/index.ts &amp;&amp; ! grep -nE "transcript_chunks[^;]*\.update\(|update.*speaker_name" supabase/functions/resolve-speakers/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - Secret gate returns 401 before any DB work or body parse (ordering matches resolve-identities).
    - Zero matching logic in the edge body — all delegated to speaker-resolver.ts.
    - No in-place write to transcript_chunks.speaker_name/speaker_email anywhere.
    - If B2: migration authored + applied TEST-only, FORCE RLS + EXECUTE revoked, CLI relinked to prod and verified unchanged.
  </acceptance_criteria>
  <done>Forward-only, secret-gated resolve-speakers delegates to the pure module and writes through the locked target.</done>
</task>

<task type="auto">
  <name>Task 2: synthetic integration proof + cross-org isolation</name>
  <read_first>
    - src/test/rls-regression.test.ts (buildClientDenySeed, CROSS_ORG_TABLES pattern)
    - a prior integration test using canonical_recording_id-keyed transcript_chunks seeding (Phase 33)
  </read_first>
  <action>
    Write `src/test/integration/resolve-speakers.integration.test.ts` (TEST project, service-role seed): seed one synthetic event with two synthetic recordings — recording A with a named chunk whose speaker has a resolved identity_id, recording B with an anonymous `Speaker 1` chunk whose derived interval genuinely overlaps A's — and assert resolve-speakers propagates the identity to B's label via the write-target (evidence row or ledger row), NOT by overwriting transcript_chunks. Add ONE adversarial cross-org fixture: an anonymous chunk in a different org must NEVER receive a name from A. Also assert the 401 secret gate. Register any new ledger table in rls-regression.test.ts's cross-org isolation array. Seed with `recording_id: NULL`; cleanup deletes workspace_entries before recordings and checks `.error` on every step (Phase 31 findings). Keep fixtures lean/adversarial per Andrew's directive.
  </action>
  <verify>
    <automated>npm run test:integration -- resolve-speakers</automated>
  </verify>
  <acceptance_criteria>
    - End-to-end propagation proven against synthetic TEST data via the write-target (no transcript_chunks overwrite).
    - Cross-org fixture proves an anonymous chunk in another org receives no name.
    - 401 secret-gate asserted.
    - New ledger (if B2) registered in rls-regression cross-org isolation; harness green.
  </acceptance_criteria>
  <done>resolve-speakers proven end-to-end on TEST with cross-org isolation enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → resolve-speakers | untrusted; must present X-Reconcile-Secret before any DB work |
| service-role reads → cross-org data | all orgs' chunks visible to service role; must be bucketed per-org before pairing |
| resolver writes → identity evidence | must preserve get_identity_evidence owner/non-owner redaction |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-06 | Spoofing | resolve-speakers invoked without the shared secret | mitigate | X-Reconcile-Secret checked before body parse / any DB work (mirror resolve-identities ordering); asserted in integration test. |
| T-35-05 | Information Disclosure | cross-org chunk pairing widening readable audience | mitigate | same-org bucketing before pairing; cross-org isolation asserted in rls-regression harness + a dedicated integration fixture. |
| T-35-07 | Tampering | in-place overwrite of transcript_chunks.speaker_name (destroys source provenance, reserved for Phase 37) | mitigate | write-target is evidence/ledger only; grep gate + code review forbid transcript_chunks writes. |
| T-35-02 | Information Disclosure | new ledger (B2) without RLS | mitigate | FORCE RLS + EXECUTE revoked to anon/authenticated + cross-org isolation registration, mirroring event_match_decisions. |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase; legitimacy gate N/A. |
</threat_model>

<artifacts>
## Artifacts this phase produces (Plan 03)
- `supabase/functions/resolve-speakers/index.ts` — forward-only, secret-gated edge function.
- (conditional) `supabase/migrations/[timestamp]_speaker_resolution.sql` — new ledger, if Plan 01 locked B2 (applied TEST-only here).
- `src/test/integration/resolve-speakers.integration.test.ts` — synthetic end-to-end + cross-org proof.
- rls-regression.test.ts registration for the new ledger (if B2).
</artifacts>

<verification>
- `npm run test:integration -- resolve-speakers` green on TEST.
- `rtk vitest run` full unit suite green.
- If B2: TEST introspection confirms FORCE RLS + EXECUTE revoked; CLI relinked to prod and prod confirmed unchanged.
</verification>

<success_criteria>
- resolve-speakers is a live-invokable, inert-until-triggered, forward-only sweep proven end-to-end on TEST with cross-org isolation.
</success_criteria>

<output>
Create `.planning/phases/35-speaker-resolution-across-sources/35-03-SUMMARY.md` when done, including a "User Setup Required" note if cron GUC wiring is needed later.
</output>
