---
phase: 35-speaker-resolution-across-sources
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/speaker-resolver.ts
  - .planning/phases/35-speaker-resolution-across-sources/35-DESIGN-NOTES.md
autonomous: false
requirements: [IDENT-04, IDENT-05]
must_haves:
  truths:
    - "The live recordings timing column(s) usable as a cross-recording shared origin are named and confirmed populated, not assumed."
    - "The two load-bearing one-way-door decisions (time-origin anchor, resolved-name write-target) are locked before any algorithm or migration is authored."
    - "speaker-resolver.ts exists with exported contract types only (no scoring implementation yet)."
  artifacts:
    - path: "supabase/functions/_shared/speaker-resolver.ts"
      provides: "Exported TypeScript contract types for chunk-interval alignment, name propagation, and consensus collapse (interface-first, no impl)"
      contains: "export interface"
    - path: ".planning/phases/35-speaker-resolution-across-sources/35-DESIGN-NOTES.md"
      provides: "Live-verified recordings timing schema + locked decisions record"
      contains: "recording_start_time"
  key_links:
    - from: "speaker-resolver.ts contract types"
      to: "recordings timing column + transcript_chunks.timestamp_start"
      via: "AbsoluteInterval derivation (recording start + parsed relative offset)"
      pattern: "recording_start_time|start"
---

<objective>
Resolve Phase 35's two load-bearing, hard-to-reverse decisions BEFORE any resolution logic or schema change is authored, and pin the pure-module contract types.

Purpose: RESEARCH.md is MEDIUM confidence precisely because (1) the exact `recordings` timing column that anchors cross-recording alignment was never re-verified live (Open Question 1 / Assumption A1), and (2) whether resolved names persist as `identity_aliases` evidence or as a new `event_match_decisions`-style ledger was never locked at schema level (Assumption A3). Both are one-way doors: the alignment anchor shapes every downstream scorer, and a new ledger table is a prod migration. Lock them first, exactly as every prior phase in this milestone opened with a Task-1 reversibility gate.

Output: A live-verified design note and the interface-first contract types in `speaker-resolver.ts`.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-speaker-resolution-across-sources/35-RESEARCH.md
@.planning/phases/35-speaker-resolution-across-sources/35-CONTEXT.md
@supabase/CLAUDE.md

<interfaces>
Existing precedent modules the contract must mirror (read, do not modify here):
- supabase/functions/_shared/identity-resolver.ts — `DisplayNameCandidate` type-pinned non-linking pattern (`verified: false`, `identity_id: null` as LITERAL types). Reproduce this shape for the "stay unresolved" outcome.
- supabase/functions/_shared/event-resolver.ts — `scoreContentProofOverlap`, `isSpeakerAlibiViolation` (interval-overlap primitive, ~line 1367), and the same-org `Map<organization_id, candidate[]>` bucketing pattern.
- supabase/functions/_shared/canonical-recording.ts — check whether ingest-time `startSeconds` (numeric per-turn timestamp) is available; the SPEC (LOW confidence) claims it is. If it is capturable, note it as the cleaner future anchor vs parsing `timestamp_start` TEXT.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-verify the recordings timing schema and pin the contract types</name>
  <read_first>
    - .planning/phases/35-speaker-resolution-across-sources/35-RESEARCH.md (Open Questions 1 &amp; 2, Assumptions A1/A3, Pitfall 2)
    - supabase/CLAUDE.md (TEST-project guard, env-file map — .env=PROD ref vltmrnjsubfzrgrtdqey, .env.test=TEST)
    - supabase/functions/_shared/canonical-recording.ts (does startSeconds survive to persisted transcript_chunks?)
    - supabase/functions/_shared/identity-resolver.ts (DisplayNameCandidate literal-type non-linking shape to reproduce)
  </read_first>
  <action>
    Two outputs.

    (1) Introspect the LIVE `recordings` schema (read-only; TEST project via .env.test, cross-check prod ref via information_schema only — never DDL here). Enumerate every timing column that actually exists (candidates: recording_start_time, started_at, start_time, call_date, recorded_at, meeting_start) with its real type and NULL-fraction across real rows. Confirm which one is populated reliably enough (and precise enough — seconds vs minutes) to serve as the shared origin for converting each recording's relative `timestamp_start` (`HH:MM:SS` TEXT) into a comparable absolute instant. Reconfirm `transcript_chunks` offset format live. Record all findings in 35-DESIGN-NOTES.md: exact column name(s), type, populated fraction, whether canonical-recording.ts's `startSeconds` is a viable cleaner alternative, and — if NO recordings timing column is reliably populated — the coarser fallback (resolution at event granularity, no fine-grained interval attach). Do NOT assume `recording_start_time` exists just because RESEARCH cited it from the SPEC — prove the actual column.

    (2) Create `supabase/functions/_shared/speaker-resolver.ts` with EXPORTED CONTRACT TYPES ONLY (interface-first, zero scoring implementation): a chunk shape (canonical_recording_id, chunk_index, speaker_name, speaker_email, timestamp_start/end, identity_id), an `AbsoluteInterval` derived-instant shape keyed off the anchor chosen in (1), the propagation input/output types (propagation input MUST carry an identity_id/verified-alias corroboration field — never a bare speaker_name string, per Pitfall 3), the consensus input/output types, and an `UnresolvedSpeaker` type that pins `identity_id: null` and `resolved: false` as LITERAL types (mirroring DisplayNameCandidate). No function bodies — types + exported function signatures throwing `not implemented` are acceptable so it compiles.
  </action>
  <verify>
    <automated>test -f .planning/phases/35-speaker-resolution-across-sources/35-DESIGN-NOTES.md &amp;&amp; grep -qiE "recording|start" .planning/phases/35-speaker-resolution-across-sources/35-DESIGN-NOTES.md &amp;&amp; grep -q "export interface" supabase/functions/_shared/speaker-resolver.ts</automated>
  </verify>
  <acceptance_criteria>
    - 35-DESIGN-NOTES.md names the exact live recordings timing column(s), their types, and real populated fractions from actual introspection (not from RESEARCH restatement).
    - canonical-recording.ts startSeconds availability is confirmed one way or the other.
    - speaker-resolver.ts exports contract types incl. an `identity_id: null` + `resolved: false` literal-typed unresolved shape and a propagation input requiring identity_id/verified-alias corroboration.
    - `npx tsc -p tsconfig.app.json --noEmit` introduces zero new errors vs type-baseline.json.
  </acceptance_criteria>
  <done>Design note records live-verified timing schema; speaker-resolver.ts holds interface-first contract types that compile clean.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Lock the two Phase-35 one-way-door decisions: (A) the cross-recording time-origin alignment anchor, and (B) the write-target for resolved speaker names.</decision>
  <context>
    Both are load-bearing per the phase directive (REVERSIBILITY_GATES=true). (A) shapes every scorer in Plan 02 and cannot cheaply change once tests are written against it. (B) determines whether Plan 03 authors a prod migration for a new `speaker_resolution_decisions`-style ledger or reuses Phase 34's `identity_aliases` evidence rows — a schema decision that is a prod one-way door. Options below are informed by Task 1's live findings; the executor presents them filled in with the real column name and populated fractions.
  </context>
  <options>
    <option id="anchor-derived-absolute">
      <name>A1: Derived absolute instant (recording timing column + parse(timestamp_start))</name>
      <pros>True time-of-day interval overlap across two captures; matches IDENT-04/05's actual need.</pros>
      <cons>Depends on the timing column being populated + precise; noisy if minute-granularity only.</cons>
    </option>
    <option id="anchor-coarse-fallback">
      <name>A2: Coarse fallback (same-event attach, no fine-grained interval) if timing column unreliable</name>
      <pros>Fails safe; never asserts a wrong interval overlap on bad data.</pros>
      <cons>Cannot satisfy IDENT-04's per-label propagation; only viable if A1's column is genuinely unusable.</cons>
    </option>
    <option id="write-evidence">
      <name>B1: Persist resolutions as identity_aliases evidence (reuse Phase 34 shape, no new table)</name>
      <pros>No migration; surfaces through existing get_identity_evidence + IdentityEvidenceBadge; zero new RLS surface.</pros>
      <cons>identity_aliases.evidence may not hold rich per-decision provenance (donor recording, interval, confidence).</cons>
    </option>
    <option id="write-ledger">
      <name>B2: New speaker_resolution_decisions ledger (mirror event_match_decisions provenance shape)</name>
      <pros>Rich, reversible, auditable provenance ledger matching MATCH-09's established pattern.</pros>
      <cons>New prod table + RLS + cross-org isolation registration; a prod one-way door.</cons>
    </option>
  </options>
  <resume-signal>Select one A option and one B option (e.g. "A1 + B1"), or describe an override.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → live TEST DB | read-only introspection; no DDL crosses this boundary in this plan |
| decision → downstream schema | the B decision authorizes (or not) a future prod migration in Plan 03 |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | choosing an unreliable timing anchor that produces wrong interval overlaps (false name propagation = information-integrity failure, same severity class as a false merge per MATCH-08) | mitigate | Task 1 proves the column's real populated fraction/precision live before locking; A2 coarse fallback is the fail-safe if unreliable. |
| T-35-02 | Information Disclosure | a new ledger table (B2) shipping without RLS could widen the readable audience of resolved-name provenance | mitigate | If B2 chosen, Plan 03 must register it in rls-regression.test.ts cross-org isolation and FORCE RLS, mirroring event_match_decisions. |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH: zero new dependencies); legitimacy gate N/A. |
</threat_model>

<artifacts>
## Artifacts this phase produces (Plan 01)
- `supabase/functions/_shared/speaker-resolver.ts` — contract types only (interface-first; scoring impl lands in Plan 02).
- `.planning/phases/35-speaker-resolution-across-sources/35-DESIGN-NOTES.md` — live-verified timing schema + the two locked decisions.
</artifacts>

<verification>
- 35-DESIGN-NOTES.md exists and cites live introspection, not RESEARCH restatement.
- speaker-resolver.ts exports contract types and compiles (`npx tsc -p tsconfig.app.json --noEmit` introduces no new errors vs baseline).
- Both A and B decisions recorded with the chosen option IDs.
</verification>

<success_criteria>
- The alignment anchor and write-target are locked with live evidence; Plan 02 can be written against fixed contract types.
</success_criteria>

<output>
Create `.planning/phases/35-speaker-resolution-across-sources/35-01-SUMMARY.md` when done, recording the two locked decisions verbatim.
</output>
