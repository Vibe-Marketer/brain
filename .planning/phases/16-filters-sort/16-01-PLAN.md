---
phase: 16-filters-sort
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/transcript-library/SourceFilterPopover.tsx
  - src/components/transcript-library/ContactsFilterPopover.tsx
  - src/components/transcript-library/FilterBar.tsx
  - src/lib/__tests__/filter-bar-integration.test.ts
autonomous: true
requirements: [FILTER-01, FILTER-02, FILTER-03]

must_haves:
  truths:
    - "Each filter popover applies its own state without affecting other active filters"
    - "Multiple filters active simultaneously produce an AND-narrowed result set"
    - "Removing any single filter pill leaves all other filters intact"
    - "All six filter popovers use consistent staged Apply/Clear pattern"
  artifacts:
    - path: "src/components/transcript-library/SourceFilterPopover.tsx"
      provides: "Source filter with staged Apply/Clear pattern matching other popovers"
      contains: "stagedSources"
    - path: "src/components/transcript-library/ContactsFilterPopover.tsx"
      provides: "Contacts filter with proper padding in action buttons"
      contains: "handleApply"
    - path: "src/lib/__tests__/filter-bar-integration.test.ts"
      provides: "Integration tests for filter stacking and pill removal"
      contains: "AND logic"
  key_links:
    - from: "src/components/transcript-library/FilterBar.tsx"
      to: "src/components/transcript-library/SourceFilterPopover.tsx"
      via: "onSourcesChange prop"
      pattern: "onSourcesChange"
    - from: "src/components/transcript-library/FilterBar.tsx"
      to: "FilterPill onRemove"
      via: "spread operator preserving other filters"
      pattern: "onFiltersChange.*\\.\\.\\.filters"
---

<objective>
Fix filter popover consistency, verify AND stacking, and ensure pill removal works cleanly.

Purpose: FILTER-01 (popovers apply/clear correctly), FILTER-02 (AND stacking), FILTER-03 (pill removal) all depend on consistent popover behavior. The SourceFilterPopover currently applies changes immediately on checkbox toggle (no staged Apply/Clear like the other 5 popovers), which is inconsistent and can cause unexpected filter interactions.

Output: All 6 filter popovers use consistent staged Apply/Clear pattern; integration tests verify AND stacking and pill independence.
</objective>

<execution_context>
@/Users/Naegele/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Naegele/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/transcript-library/FilterBar.tsx
@src/components/transcript-library/SourceFilterPopover.tsx
@src/components/transcript-library/TagFilterPopover.tsx
@src/components/transcript-library/FilterPill.tsx
@src/components/transcript-library/ContactsFilterPopover.tsx
@src/components/transcript-library/DurationFilterPopover.tsx
@src/components/transcript-library/FolderFilterPopover.tsx

<interfaces>
<!-- Key types the executor needs -->

From src/components/transcript-library/FilterBar.tsx:
```typescript
interface FilterBarProps {
  filters: {
    dateFrom?: Date;
    dateTo?: Date;
    participants?: string[];
    durationMin?: number;
    durationMax?: number;
    tags?: string[];
    folders?: string[];
    sources?: string[];
  };
  onFiltersChange: (filters: FilterBarProps['filters']) => void;
  tags: Array<{ id: string; name: string; description?: string | null }>;
  folders: Folder[];
  searchQuery?: string;
  onSearchChange?: (query: string) => void;
  onCreateFolder?: () => void;
  availableSources?: string[];
  compact?: boolean;
}
```

From src/components/transcript-library/FilterPill.tsx:
```typescript
interface FilterPillProps {
  label: string;
  value: string;
  onRemove: () => void;
  onClick?: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SourceFilterPopover to use staged Apply/Clear pattern</name>
  <files>src/components/transcript-library/SourceFilterPopover.tsx, src/components/transcript-library/ContactsFilterPopover.tsx</files>
  <read_first>
    - src/components/transcript-library/TagFilterPopover.tsx (reference pattern for staged Apply/Clear)
    - src/components/transcript-library/SourceFilterPopover.tsx (current immediate-apply behavior)
    - src/components/transcript-library/ContactsFilterPopover.tsx (check action button padding)
  </read_first>
  <action>
    Rewrite SourceFilterPopover to use the same staged selection pattern as TagFilterPopover (per FILTER-01):
    1. Add `useState` for `isOpen` and `stagedSources` (initially from `selectedSources`)
    2. Add `handleOpenChange` that syncs `stagedSources` from `selectedSources` when opening
    3. Change `handleSourceToggle` to modify `stagedSources` instead of calling `onSourcesChange` directly
    4. Add `handleApply` that calls `onSourcesChange(stagedSources)` and closes popover
    5. Add `handleClear` that calls `onSourcesChange([])` and closes popover
    6. Change `<Popover>` to use controlled `open={isOpen} onOpenChange={handleOpenChange}`
    7. Add Apply/Clear buttons at bottom of popover content, styled identically to TagFilterPopover's buttons (border-t, p-3, flex justify-end gap-2)
    8. Use `stagedSources.includes(source)` for checkbox checked state

    Also fix ContactsFilterPopover: the action button row at bottom is missing padding (compare with TagFilterPopover which has `p-3`). Add `p-3` to the action buttons container div.

    Keep all existing functionality: source icons, dynamic availableSources list, null-return when no sources available.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - SourceFilterPopover.tsx contains `stagedSources` state variable
    - SourceFilterPopover.tsx contains `handleApply` function
    - SourceFilterPopover.tsx contains `handleClear` function
    - SourceFilterPopover.tsx contains `handleOpenChange` function
    - SourceFilterPopover.tsx renders Apply and Clear buttons
    - ContactsFilterPopover.tsx action button container has `p-3` class
    - Zero TypeScript errors
  </acceptance_criteria>
  <done>All 6 filter popovers (Tag, Folder, Contacts, Duration, Source, Date) use consistent staged Apply/Clear pattern. No immediate-apply inconsistencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integration tests for filter stacking and pill removal</name>
  <files>src/lib/__tests__/filter-bar-integration.test.ts</files>
  <read_first>
    - src/lib/__tests__/filter-utils.test.ts (existing test patterns)
    - src/components/transcript-library/FilterBar.tsx (pill removal logic)
    - src/lib/filter-utils.ts (FilterState type, URL round-trip functions)
  </read_first>
  <behavior>
    - Test: Setting tags filter then adding source filter produces combined state with both
    - Test: Setting tags+source+duration then removing tags pill leaves source+duration intact
    - Test: Setting tags+source+duration then removing source pill leaves tags+duration intact
    - Test: Setting tags+source+duration then removing duration pill leaves tags+source intact
    - Test: Clear all resets every filter field to empty/undefined
    - Test: URL round-trip preserves all filter combinations (tags+folders+sources+participants+date+duration)
    - Test: Removing one filter from URL-round-tripped state preserves the others
  </behavior>
  <action>
    Create integration test file that tests filter stacking and pill removal at the data layer (no React rendering needed -- test the filter state manipulation logic directly).

    Test approach: Simulate what FilterBar does by creating filter state objects, applying changes via spread operator (same pattern as onFiltersChange), verifying AND composition, then testing pill removal by setting individual fields to empty while preserving others.

    Also test URL round-trip of combined filter states to ensure persistence works for stacked filters.

    Use vitest (already configured). Import FilterState, filtersToURLParams, urlParamsToFilters from filter-utils.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/filter-bar-integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - filter-bar-integration.test.ts exists with at least 7 test cases
    - All tests pass
    - Tests cover: AND stacking, individual pill removal for 3+ filter types, clear all, URL round-trip with combined filters
  </acceptance_criteria>
  <done>Integration tests prove filter stacking (AND logic) works correctly and individual pill removal preserves other filters.</done>
</task>

</tasks>

<verification>
- All 6 filter popovers have Apply/Clear buttons (visual consistency)
- TypeScript compiles with zero errors
- New integration tests pass
- Existing filter-utils tests still pass (npx vitest run src/lib/__tests__/filter-utils.test.ts)
</verification>

<success_criteria>
- SourceFilterPopover uses staged Apply/Clear pattern matching other popovers
- Integration tests verify AND stacking across 3+ simultaneous filters
- Integration tests verify pill removal independence (removing one preserves others)
- Zero TypeScript errors, all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/16-filters-sort/16-01-SUMMARY.md`
</output>
