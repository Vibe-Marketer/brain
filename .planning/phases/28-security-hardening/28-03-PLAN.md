# Plan 28-03: OAuth Encryption + Auth Helper Consistency (Criteria 4, 8)

## Scope
- OAuth tokens encrypted at rest via pgcrypto
- Auth helper consistency in fathom-oauth-callback and file-upload-transcribe

## Tasks

### Task 1: DB migration for encrypted OAuth tokens
- New migration: `supabase/migrations/20260509000001_encrypt_oauth_tokens.sql`
- Enable pgcrypto extension (if not already enabled)
- Note: actual in-place encryption of existing tokens requires the encryption key
  at migration time. Instead, the migration documents the column usage and the
  edge function changes handle encrypt-on-write / decrypt-on-read.
- Existing plaintext tokens will be re-encrypted on next OAuth refresh cycle.

### Task 2: Encrypt OAuth tokens in fathom-oauth-callback
- File: `supabase/functions/fathom-oauth-callback/index.ts`
- When writing to `import_sources` and `user_settings`:
  - Use `pgp_sym_encrypt(token, key)` via raw SQL or RPC
  - Key from `OAUTH_ENCRYPTION_KEY` env var
- Add decrypt helper for reading tokens

### Task 3: Auth helper in fathom-oauth-callback
- File: `supabase/functions/fathom-oauth-callback/index.ts`
- Replace manual `authHeader.replace('Bearer ', '')` with `authenticateRequest()`
- Import from `../_shared/auth.ts`

### Task 4: Auth helper in file-upload-transcribe
- File: `supabase/functions/file-upload-transcribe/index.ts`
- Replace manual `authHeader.replace('Bearer ', '')` with `authenticateRequest()`
- Import from `../_shared/auth.ts`

## Verification
- Deploy fathom-oauth-callback, file-upload-transcribe
