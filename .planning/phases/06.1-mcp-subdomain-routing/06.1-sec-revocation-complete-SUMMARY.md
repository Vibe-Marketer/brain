---
phase: 06.1-mcp-subdomain-routing
plan: sec-revocation-complete
subsystem: auth
tags: [postgres, supabase, edge-functions, mcp, security, revocation, jwt]

# Dependency graph
requires:
  - phase: 06.1-mcp-subdomain-routing
    provides: mcp_oauth_client_grants table with revoked_at column
provides:
  - DB triggers cascading mcp token revocation on workspace/org membership removal
  - remove-org-member Edge Function with auth.admin.signOut (ISC-31)
  - Server-side org member removal with admin authorization check
affects: [mcp-server, mcp-auth, membership-management, security]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "SECURITY DEFINER trigger with inner BEGIN/EXCEPTION for non-fatal DB revocation"
    - "Edge Function wrapper for membership operations requiring signOut (ISC-31 pattern)"

key-files:
  created:
    - supabase/migrations/20260609000001_revocation_triggers.sql
    - supabase/functions/remove-org-member/index.ts
  modified:
    - src/services/organizations.service.ts
    - src/services/__tests__/organizations.service.test.ts

key-decisions:
  - "Real table names are organization_memberships/workspace_memberships (not org_members/workspace_members as plan assumed) — triggers written against correct schema"
  - "organization_memberships uses organization_id column; mcp_tokens uses org_id — trigger bridges the naming difference"
  - "removeOrganizationMember service now routes through remove-org-member Edge Function for ISC-31 compliance; DB triggers remain the primary revocation path"
  - "Pre-existing MCPTab/McpConnectionsTab test failures (tool count assertions) are unrelated to this plan and left as-is per scope boundary rule"

patterns-established:
  - "ISC-31 pattern: Edge Function wrapper for membership removal calls auth.admin.signOut(userId, 'global') non-fatally after successful DELETE"
  - "Revocation trigger pattern: SECURITY DEFINER, SET search_path = extensions public, inner BEGIN/EXCEPTION WHEN OTHERS THEN RAISE WARNING"

requirements-completed: [SEC-REVOCATION-COMPLETE]

# Metrics
duration: 20min
completed: 2026-06-10
---

# Phase 06.1 Plan sec-revocation-complete: Security Revocation Complete Summary

**DB triggers cascade mcp_tokens + mcp_oauth_client_grants revocation on workspace/org membership removal; remove-org-member Edge Function adds auth.admin.signOut(userId, 'global') for ISC-31**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-06-10T02:00:00Z
- **Completed:** 2026-06-10T02:20:00Z
- **Tasks:** 2
- **Files modified:** 4 (1 migration, 1 Edge Function, 1 service, 1 test file)

## Accomplishments
- Created `20260609000001_revocation_triggers.sql` with two AFTER DELETE triggers covering both `mcp_tokens` AND `mcp_oauth_client_grants` for workspace_memberships and organization_memberships removal events (ISC-27–30)
- Created `remove-org-member` Edge Function: server-side authorization check, membership DELETE, non-fatal auth.admin.signOut(userId, 'global') call (ISC-31)
- Updated `removeOrganizationMember()` to route through Edge Function instead of direct Supabase delete
- All triggers SECURITY DEFINER with non-fatal EXCEPTION → RAISE WARNING pattern (ISC-32); only removes OLD.user_id tokens (ISC-34 anti-pattern avoided)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create revocation triggers migration covering both token tables** - `628d7e0b` (feat)
2. **Task 2: Wire ISC-31 — admin signOut on membership removal API handler** - `943e37c2` (feat)

**Plan metadata:** (included in final docs commit)

## Files Created/Modified
- `supabase/migrations/20260609000001_revocation_triggers.sql` - Two trigger functions + triggers: `revoke_mcp_on_workspace_member_delete()` on workspace_memberships, `revoke_mcp_on_org_member_delete()` on organization_memberships; covers both mcp_tokens and mcp_oauth_client_grants
- `supabase/functions/remove-org-member/index.ts` - Edge Function: authenticates caller, verifies org admin/owner role, deletes membership (DB triggers fire), calls auth.admin.signOut(userId, 'global') non-fatally
- `src/services/organizations.service.ts` - `removeOrganizationMember()` now POSTs to remove-org-member Edge Function instead of direct delete
- `src/services/__tests__/organizations.service.test.ts` - Updated 2 tests + added 1 new test to cover Edge Function fetch pattern; 21 tests pass

## Decisions Made
- Real table names are `organization_memberships` and `workspace_memberships` (plan used `org_members`/`workspace_members` which don't exist). Triggers written against actual schema.
- `organization_memberships.organization_id` maps to `mcp_tokens.org_id` — the trigger bridges this naming difference explicitly.
- Membership removal moved to Edge Function (not an RPC) to enable server-side authorization check AND auth.admin.signOut in one atomic operation.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected membership table names from plan's assumed names**
- **Found during:** Task 1 (schema discovery)
- **Issue:** Plan assumed table names `org_members` and `workspace_members`; actual tables are `organization_memberships` and `workspace_memberships` with `organization_id` column (not `org_id`)
- **Fix:** Triggers written against actual table names; org trigger bridges `organization_memberships.organization_id` → `mcp_tokens.org_id` via OLD.organization_id
- **Files modified:** `supabase/migrations/20260609000001_revocation_triggers.sql`
- **Verification:** `supabase db push --linked --include-all` applied cleanly with only `NOTICE` about non-existent triggers (expected on first apply)
- **Committed in:** `628d7e0b` (Task 1 commit)

**2. [Rule 1 - Bug] Updated organizations.service tests to match new Edge Function pattern**
- **Found during:** Task 2 (test run after service update)
- **Issue:** 2 existing tests mocked `supabase.from` for direct delete; new implementation uses `fetch` + `supabase.auth.getSession`
- **Fix:** Rewrote `removeOrganizationMember` test block to mock `fetch` and `getSession`; added test for unauthenticated case
- **Files modified:** `src/services/__tests__/organizations.service.test.ts`
- **Verification:** 21 tests pass
- **Committed in:** `943e37c2` (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (2 Rule 1 bugs — schema name mismatch, test alignment)
**Impact on plan:** Both necessary for correctness. No scope creep.

## Issues Encountered
- `supabase db push --use-api` flag no longer recognized in current CLI version; resolved by using `--linked --include-all` which is the correct current syntax.

## Known Stubs
None — all token revocation paths are fully wired.

## Threat Flags
None — no new network surface introduced. Closes T-06.1-REV-01, T-06.1-REV-02, T-06.1-REV-03 as documented in plan threat model.

## User Setup Required
None — migration applied, Edge Function deployed.

## Next Phase Readiness
- All three threat model items mitigated: OAuth session revocation on workspace removal (T-06.1-REV-01), org removal (T-06.1-REV-02), and Supabase Auth JWT invalidation (T-06.1-REV-03)
- SEC-REVOCATION-COMPLETE requirement satisfied
- Pre-existing MCPTab/McpConnectionsTab test failures (tool count assertions, Phase 08.1 scope) remain out of scope

---
*Phase: 06.1-mcp-subdomain-routing*
*Completed: 2026-06-10*
