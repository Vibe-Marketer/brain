---
phase: 06.1-mcp-subdomain-routing
plan: ui-url-builder
type: execute
wave: 4
depends_on:
  - 06.1-slug-schema        # organizations.slug + workspaces.slug columns must exist before URLs can be built against them
  - 06.1-worker-routing     # subdomain Worker must be deployed before URLs are live
files_modified:
  - src/services/mcp-tokens.service.ts
  - src/services/__tests__/mcp-tokens-url-builder.test.ts
  - src/components/settings/McpSetupSnippets.tsx
autonomous: true
requirements:
  - ISC-112
  - ISC-113
  - ISC-114
  - ISC-115
  - ISC-116
  - ISC-117
  - ISC-118
  - ISC-119

must_haves:
  truths:
    - "buildSubdomainMcpUrl('acmecorp') returns exactly 'https://acmecorp.callvaultai.com/mcp'"
    - "buildSubdomainMcpUrl('acmecorp', 'sales') returns exactly 'https://acmecorp-sales.callvaultai.com/mcp'"
    - "McpSetupSnippets renders three distinct URL blocks when orgSlug and wsSlug are both provided"
    - "McpSetupSnippets renders two URL blocks (simple + org-scoped) when only orgSlug is provided"
    - "Legacy buildScopedMcpUrl function still exists and is unchanged (no regression)"
    - "npm test passes with new unit tests for buildSubdomainMcpUrl"
  artifacts:
    - path: "src/services/mcp-tokens.service.ts"
      provides: "buildSubdomainMcpUrl export"
      contains: "buildSubdomainMcpUrl"
    - path: "src/services/__tests__/mcp-tokens-url-builder.test.ts"
      provides: "unit tests for buildSubdomainMcpUrl"
      exports: ["describe('buildSubdomainMcpUrl'"]
    - path: "src/components/settings/McpSetupSnippets.tsx"
      provides: "3-URL display component"
      contains: "orgSlug"
  key_links:
    - from: "src/components/settings/McpSetupSnippets.tsx"
      to: "src/services/mcp-tokens.service.ts"
      via: "buildSubdomainMcpUrl import"
      pattern: "buildSubdomainMcpUrl"
    - from: "src/services/mcp-tokens.service.ts"
      to: "callvaultai.com subdomain URLs"
      via: "template literal construction"
      pattern: "callvaultai\\.com/mcp"
---

<objective>
Add `buildSubdomainMcpUrl(orgSlug, wsSlug?)` to `mcp-tokens.service.ts` and update `McpSetupSnippets.tsx` to display three URL types: legacy `https://mcp.callvaultai.com`, org-scoped `https://{orgSlug}.callvaultai.com/mcp`, and workspace-scoped `https://{orgSlug}-{wsSlug}.callvaultai.com/mcp`.

Purpose: Gives users the correct per-org and per-workspace subdomain URLs to paste into Claude Desktop / Cursor MCP config after the new Cloudflare Worker is live.

Output: Updated service file with new export, new unit test file, updated component with 3-URL display.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-mcp-subdomain-routing/06.1-CONTEXT.md
@src/CLAUDE.md
</context>

<interfaces>
<!-- Extracted from src/services/mcp-tokens.service.ts — read before implementing -->

Existing exports at lines 154-162 (DO NOT MODIFY or remove):
  export function getMcpUrl(): string — returns 'https://mcp.callvaultai.com'
  export function buildScopedMcpUrl(scope: McpTokenScope, workspaceId: string | null): string
    — legacy URL builder using /w/{uuid} path pattern; still used by toManualTokenConnection()

Existing constants:
  const PUBLIC_MCP_BASE_URL = 'https://mcp.callvaultai.com'  (line 14)

Existing prop interface in src/components/settings/McpSetupSnippets.tsx (lines 7-9):
  interface McpSetupSnippetsProps {
    workspaceId?: string | null
  }

Existing URL display pattern in McpSetupSnippets.tsx (lines 51-65):
  Two blocks, each shaped as:
    <div className="space-y-1">
      <div className="flex items-center justify-between gap-2">
        <span className="text-xs font-medium text-muted-foreground">{label}</span>
        <SnippetCopyButton text={url} label="Copy URL" />
      </div>
      <code className="block rounded-md bg-muted px-2 py-1.5 text-xs break-all">{url}</code>
    </div>

Existing imports in McpSetupSnippets.tsx:
  import { getMcpUrl } from '@/services/mcp-tokens.service'
  import { RiCheckLine, RiFileCopyLine } from '@remixicon/react'
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add buildSubdomainMcpUrl to service + unit tests</name>
  <files>src/services/mcp-tokens.service.ts, src/services/__tests__/mcp-tokens-url-builder.test.ts</files>

  <read_first>
    Read src/services/mcp-tokens.service.ts (full file, 188 lines) to confirm exact location of buildScopedMcpUrl at lines 158-162 before adding the new function.
    Read src/services/__tests__/mcp-oauth-grants.service.test.ts for the test file structure pattern (import style, describe/it shape, no mocking needed for pure functions).
  </read_first>

  <behavior>
    - buildSubdomainMcpUrl('acmecorp') returns 'https://acmecorp.callvaultai.com/mcp'
    - buildSubdomainMcpUrl('acmecorp', 'sales') returns 'https://acmecorp-sales.callvaultai.com/mcp'
    - buildSubdomainMcpUrl('abc123') returns 'https://abc123.callvaultai.com/mcp' (numeric org slug)
    - buildSubdomainMcpUrl('org1', 'ws2') returns 'https://org1-ws2.callvaultai.com/mcp'
    - buildSubdomainMcpUrl('org1', '') treats empty wsSlug as absent — returns org-only URL (falsy guard)
    - buildSubdomainMcpUrl('org1', undefined) returns org-only URL
    - buildScopedMcpUrl still exists and is not modified (regression guard)
  </behavior>

  <action>
    STEP 1 — Write the test file first at src/services/__tests__/mcp-tokens-url-builder.test.ts.

    The test file must import buildSubdomainMcpUrl from '@/services/mcp-tokens.service'. Use vitest imports (import { describe, it, expect } from 'vitest'). Write one describe block named 'buildSubdomainMcpUrl' containing the seven behavior cases above. Also add a regression test: import buildScopedMcpUrl and assert it still returns getMcpUrl() when scope is 'organization'.

    Run npm test -- --run src/services/__tests__/mcp-tokens-url-builder.test.ts — it MUST fail (buildSubdomainMcpUrl does not exist yet).

    STEP 2 — Add the function to src/services/mcp-tokens.service.ts.

    Insert the new export immediately after the existing buildScopedMcpUrl function at line 162, before the summarizeCategories function at line 164. The insertion point is after the closing brace of buildScopedMcpUrl.

    The new function signature: export function buildSubdomainMcpUrl(orgSlug: string, wsSlug?: string): string

    Logic: if wsSlug is truthy (non-empty string), return template literal 'https://${orgSlug}-${wsSlug}.callvaultai.com/mcp'. Otherwise return 'https://${orgSlug}.callvaultai.com/mcp'.

    Add a JSDoc comment above it: "Build a subdomain-scoped MCP URL using org/workspace slugs (per D-07). Slugs are alphanumeric-only [a-z0-9]; the hyphen between org and workspace slugs is the separator, not part of either slug. ISC-112-114."

    Do NOT modify getMcpUrl, buildScopedMcpUrl, PUBLIC_MCP_BASE_URL, or any other existing function.

    STEP 3 — Run tests. They MUST pass.
  </action>

  <verify>
    <automated>npm test -- --run src/services/__tests__/mcp-tokens-url-builder.test.ts</automated>
  </verify>

  <done>
    - Test file exists at src/services/__tests__/mcp-tokens-url-builder.test.ts with 7+ test cases
    - All tests pass
    - grep buildSubdomainMcpUrl src/services/mcp-tokens.service.ts returns match
    - grep buildScopedMcpUrl src/services/mcp-tokens.service.ts returns match (no regression)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update McpSetupSnippets to display three URL types</name>
  <files>src/components/settings/McpSetupSnippets.tsx</files>

  <read_first>
    Read src/components/settings/McpSetupSnippets.tsx in full (72 lines) to confirm current props interface, imports, and the two-block URL display structure before modifying.
    Confirm buildSubdomainMcpUrl is now exported from src/services/mcp-tokens.service.ts (Task 1 must be complete).
  </read_first>

  <action>
    STEP 1 — Update the props interface.

    The existing interface McpSetupSnippetsProps has workspaceId?: string | null. Extend it to add two new optional props: orgSlug?: string | null and wsSlug?: string | null. Keep workspaceId to avoid breaking existing call sites (it drives the legacy workspace URL path; keep the existing buildWorkspaceUrl function unchanged for backward compat).

    STEP 2 — Update the component function signature.

    The existing export function McpSetupSnippets({ workspaceId }) becomes export function McpSetupSnippets({ workspaceId, orgSlug, wsSlug }). Destructure all three props.

    STEP 3 — Add the import.

    Add buildSubdomainMcpUrl to the existing import from '@/services/mcp-tokens.service': import { getMcpUrl, buildSubdomainMcpUrl } from '@/services/mcp-tokens.service'

    STEP 4 — Compute the three URLs using useMemo.

    Add two new useMemo declarations after the existing orgUrl and workspaceUrl:
    - const orgSubdomainUrl = useMemo(() => orgSlug ? buildSubdomainMcpUrl(orgSlug) : null, [orgSlug])
    - const wsSubdomainUrl = useMemo(() => (orgSlug && wsSlug) ? buildSubdomainMcpUrl(orgSlug, wsSlug) : null, [orgSlug, wsSlug])

    STEP 5 — Update the JSX to show three URL blocks.

    Replace the current two URL blocks with three blocks in this order:

    Block 1 — Simple URL (always shown, no condition):
      Label: "Simple endpoint — works for everyone"
      URL: orgUrl (getMcpUrl() result, i.e., 'https://mcp.callvaultai.com')

    Block 2 — Org-scoped subdomain (conditional on orgSubdomainUrl being non-null):
      Label: "Organization endpoint"
      URL: orgSubdomainUrl
      Wrap in: {orgSubdomainUrl && (...)}

    Block 3 — Workspace-scoped subdomain (conditional on wsSubdomainUrl being non-null):
      Label: "Workspace endpoint"
      URL: wsSubdomainUrl
      Wrap in: {wsSubdomainUrl && (...)}

    Each block uses the IDENTICAL structure as the existing blocks:
      <div className="space-y-1">
        <div className="flex items-center justify-between gap-2">
          <span className="text-xs font-medium text-muted-foreground">{label}</span>
          <SnippetCopyButton text={url} label="Copy URL" />
        </div>
        <code className="block rounded-md bg-muted px-2 py-1.5 text-xs break-all">{url}</code>
      </div>

    Do NOT introduce new components, new icon imports, new styling patterns, or new animation. Use only the existing SnippetCopyButton helper and the existing block structure. Remix Icons only (already imported: RiCheckLine, RiFileCopyLine — no additions needed).

    Update the descriptive paragraph at line 47 to: "Use the organization or workspace endpoint for multi-org routing. The simple endpoint works for single-org setups."

    STEP 6 — Verify TypeScript compiles cleanly.

    Run: npm run typecheck (or npx tsc --noEmit if typecheck script is absent) and confirm zero errors on this file.
  </action>

  <verify>
    <automated>npm test -- --run && npx tsc --noEmit 2>&1 | grep McpSetupSnippets || echo "no type errors in McpSetupSnippets"</automated>
  </verify>

  <done>
    - McpSetupSnippets accepts orgSlug and wsSlug props (TypeScript-typed)
    - When orgSlug="acmecorp" and wsSlug="sales": component renders three URL code blocks containing mcp.callvaultai.com, acmecorp.callvaultai.com/mcp, acmecorp-sales.callvaultai.com/mcp
    - When orgSlug="acmecorp" only: renders two URL code blocks (simple + org-scoped)
    - When neither orgSlug nor wsSlug: renders one URL code block (simple only)
    - No TypeScript errors
    - buildWorkspaceUrl and workspaceId prop remain intact (no regression)
    - npm test passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orgSlug / wsSlug → URL string | Slug values passed into buildSubdomainMcpUrl are trusted to be pre-validated at DB storage time (^[a-z0-9]+ constraint per D-07). This function is a display/copy helper — it does not make network requests and does not validate slugs itself. |
| McpSetupSnippets display | Component renders URLs for user to copy-paste. No auth, no network, no user input accepted — read-only display layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ui-url-01 | Tampering | buildSubdomainMcpUrl | accept | Pure display function; slugs are DB-validated as ^[a-z0-9]+ before storage (D-07). No XSS surface — URLs are rendered inside React code elements and SnippetCopyButton writes to clipboard only. No innerHTML usage. |
| T-ui-url-02 | Information Disclosure | McpSetupSnippets | accept | URLs are shown only to authenticated users in the settings UI. No org/workspace slug is exposed beyond what the authenticated user already has access to. |
| T-ui-url-03 | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. All dependencies already present. |
</threat_model>

<verification>
After both tasks complete, run the full test suite:

  npm test

Confirm:
- All tests pass including mcp-tokens-url-builder.test.ts
- grep -c "buildSubdomainMcpUrl" src/services/mcp-tokens.service.ts returns >= 1
- grep -c "buildScopedMcpUrl" src/services/mcp-tokens.service.ts returns >= 1 (no regression)
- grep -c "orgSlug" src/components/settings/McpSetupSnippets.tsx returns >= 1
- grep -c "wsSlug" src/components/settings/McpSetupSnippets.tsx returns >= 1

Behavioral spot-checks (node REPL or test):
- buildSubdomainMcpUrl("acmecorp") === "https://acmecorp.callvaultai.com/mcp"
- buildSubdomainMcpUrl("acmecorp", "sales") === "https://acmecorp-sales.callvaultai.com/mcp"
</verification>

<success_criteria>
- buildSubdomainMcpUrl is exported from mcp-tokens.service.ts
- buildSubdomainMcpUrl("acmecorp") returns exactly "https://acmecorp.callvaultai.com/mcp"
- buildSubdomainMcpUrl("acmecorp", "sales") returns exactly "https://acmecorp-sales.callvaultai.com/mcp"
- buildScopedMcpUrl still exists and passes regression test
- McpSetupSnippets accepts orgSlug and wsSlug props
- McpSetupSnippets renders three URL blocks when both orgSlug and wsSlug provided
- Unit test file exists at src/services/__tests__/mcp-tokens-url-builder.test.ts with 7+ cases
- npm test passes with zero failures
- No TypeScript errors in modified files
</success_criteria>

<output>
Create `.planning/phases/06.1-mcp-subdomain-routing/06.1-ui-url-builder-SUMMARY.md` when done
</output>
