---
phase: 06.1
plan: sec-dcr-phishing
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/OAuthConsentPage.tsx
  - src/test/OAuthConsentPage.test.tsx
autonomous: false
requirements: [SEC-DCR-PHISHING]
must_haves:
  truths:
    - "Consent page displays truncated client_id UUID (first 8 chars + ellipsis + last 4 chars)"
    - "First-time connection badge appears in CardHeader when this client_id has no prior grants for this user"
    - "Redirect destination domain (origin only, not full path) shown in Client Details block"
    - "Advisory text 'Only approve if you initiated this connection from your AI client.' always shown"
    - "OAuthConsentPage.test.tsx exists with all 5 behavior cases passing"
    - "Cloudflare rate limit rule for DCR endpoint (ISC-17) is confirmed configured before plan signs off"
  artifacts:
    - path: "src/pages/OAuthConsentPage.tsx"
      provides: "Security fields: client_id display, first-time badge, redirect domain, advisory"
      contains: "isFirstTimeClient"
    - path: "src/test/OAuthConsentPage.test.tsx"
      provides: "Component tests covering all 5 security behavior cases"
      contains: "isFirstTimeClient"
  key_links:
    - from: "src/pages/OAuthConsentPage.tsx"
      to: "mcp_oauth_client_grants table"
      via: "isFirstTimeClient query on client_id + user_id"
      pattern: "mcp_oauth_client_grants"
---

<objective>
Add four security fields to `src/pages/OAuthConsentPage.tsx` per ISC-13–19 (CRITICAL security gate) and UI-SPEC.md:
1. Truncated client_id UUID display (always shown)
2. First-time connection warning badge (conditional on no prior grants)
3. Redirect destination domain
4. Advisory text

Also fix workspace_id parameter validation (ISC-35–38, HIGH security gate) in the same file. Both sec-dcr-phishing and sec-workspace-param target this file — delivering as one plan per D-16 (CONTEXT.md).

Create `src/test/OAuthConsentPage.test.tsx` with component tests for all 5 security behaviors (TDD RED before implementation, GREEN after).

Configure Cloudflare rate limit rule for the DCR endpoint (ISC-17, max 10 registrations/IP/hr) — this is a blocking checkpoint that must be verified before this plan signs off.

Purpose: DCR endpoint is public. Any attacker can register a fake client, get an authorization_id, and phish a user into granting MCP access. Today the consent page shows only attacker-controlled client_name. Adding client_id (UUID, not attacker-controlled) and first-seen detection makes phishing attacks detectable.

Output: OAuthConsentPage.tsx with security fields per UI-SPEC.md layout contract. OAuthConsentPage.test.tsx passing. Cloudflare rate limit rule active.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-mcp-subdomain-routing/06.1-RESEARCH.md
@.planning/phases/06.1-mcp-subdomain-routing/06.1-PATTERNS.md
@.planning/phases/06.1-mcp-subdomain-routing/06.1-UI-SPEC.md

<interfaces>
<!-- From src/pages/OAuthConsentPage.tsx (must read before editing) -->
<!-- Key facts from PATTERNS.md:
  - AuthorizationDetails interface (lines 54–64): client_id, client.id, redirect_uri already present
  - RiShieldCheckLine and RiErrorWarningLine already imported (lines 13–14)
  - Badge is NOT currently imported — add: import { Badge } from '@/components/ui/badge'
  - workspace_id useEffect at lines 101–109 reads searchParams.get('workspace_id')
  - selectedOrgId guard at line 102: if (!selectedOrgId) return — confirm this blocks cross-org workspace param
  - appName computation at line ~336 — add computed values near here

  UI-SPEC.md layout contract:
  1. First-time badge: inside CardHeader space-y-1 div, below h2, above subtitle paragraph
     <Badge className="border-amber-400 text-amber-700 bg-amber-50 gap-1.5"> with RiErrorWarningLine h-3 w-3
  2. Client Details block: new bg-muted/50 rounded-lg p-4 space-y-2 after permissions section
     Contains: "Client Details" label, RiShieldCheckLine + "Client ID" + truncated UUID
     Also: "Redirect destination" + RiShieldCheckLine + origin URL
  3. Advisory text: standalone paragraph below Client Details block
     Copy: "Only approve if you initiated this connection from your AI client."

  isFirstTimeClient: query mcp_oauth_client_grants WHERE user_id = user.id AND client_id = clientId AND revoked_at IS NULL
  If count === 0 → isFirstTimeClient = true. Fail-safe: if query errors, default false (do not block flow).
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create OAuthConsentPage.test.tsx (RED), then add security fields to OAuthConsentPage (GREEN)</name>
  <files>src/test/OAuthConsentPage.test.tsx, src/pages/OAuthConsentPage.tsx</files>
  <read_first>
    src/pages/OAuthConsentPage.tsx — read fully before editing; note exact line numbers for CardHeader div, CardContent structure, existing imports, and workspace_id useEffect
    src/test/ — scan existing test files for the test structure and mock patterns used (e.g., src/test/rls-regression.test.ts or any component test for mocking useOrganizations, useUser)
  </read_first>
  <behavior>
    - Test 1: When authDetails.client.id = "a1b2c3d4-e5f6-7890-abcd-ef1234567890", rendered Client ID span shows "a1b2c3d4…7890"
    - Test 2: When no prior grants exist for clientId + userId, isFirstTimeClient = true and badge renders
    - Test 3: When redirect_uri = "https://claude.ai/oauth/callback", redirect destination shows "https://claude.ai"
    - Test 4: Advisory text "Only approve if you initiated this connection from your AI client." always renders
    - Test 5: supplying ?workspace_id=&lt;cross-org-uuid&gt; does not auto-select a workspace not in the current org's workspaces array
  </behavior>
  <action>
    STEP 1 — RED (create test file first):

    Create src/test/OAuthConsentPage.test.tsx. Use the existing test patterns found in src/test/ (vitest + @testing-library/react). Write a describe block named "OAuthConsentPage security fields" with 5 test cases covering the behavior cases above.

    For Test 1: render the component with a mock authDetails where client.id = "a1b2c3d4-e5f6-7890-abcd-ef1234567890". Assert the DOM contains "a1b2c3d4…7890" (the truncated UUID).
    For Test 2: mock the Supabase query for mcp_oauth_client_grants to return count = 0. Assert the "First-time connection" badge renders (test for text "First-time connection" or the border-amber-400 class).
    For Test 3: render with redirect_uri = "https://claude.ai/oauth/callback". Assert redirect destination shows "https://claude.ai".
    For Test 4: assert text "Only approve if you initiated this connection from your AI client." is present.
    For Test 5: mock workspaces filtered to only org-A workspaces. Simulate the component receiving ?workspace_id=&lt;org-B-workspace-uuid&gt;. Assert no workspace is auto-selected (selectedWorkspaceId remains empty or undefined).

    Run npm run test -- --run src/test/OAuthConsentPage.test.tsx — tests MUST FAIL (RED phase). The component doesn't have these features yet.

    STEP 2 — GREEN (implement security fields in OAuthConsentPage.tsx):

    Read src/pages/OAuthConsentPage.tsx fully.

    Add import: import { Badge } from '@/components/ui/badge' (Badge not currently imported).

    Add computed values near the appName computation (before JSX):
    - const clientId = authDetails?.client?.id ?? authDetails?.client_id ?? null;
    - const truncatedClientId = clientId ? clientId.slice(0, 8) + '…' + clientId.slice(-4) : null;
    - let redirectDomain = authDetails?.redirect_uri ?? ''; try { redirectDomain = new URL(redirectDomain).origin; } catch { /* use raw */ }
    - const [isFirstTimeClient, setIsFirstTimeClient] = useState(false);

    Add useEffect to detect first-time connection (after authDetails loads):
    Query the supabase client (already available) for mcp_oauth_client_grants WHERE user_id = user.id AND client_id = clientId AND revoked_at IS NULL. If count === 0 and no error → setIsFirstTimeClient(true). Wrap in try/catch — on any error, leave isFirstTimeClient = false (fail-safe per UI-SPEC.md).

    Add first-time badge inside CardHeader space-y-1 div, below the h2, above the subtitle paragraph (per UI-SPEC.md layout):
    {isFirstTimeClient && (
      &lt;div className="flex justify-center pt-1"&gt;
        &lt;Badge className="border-amber-400 text-amber-700 bg-amber-50 gap-1.5"&gt;
          &lt;RiErrorWarningLine className="h-3 w-3" aria-hidden="true" /&gt;
          First-time connection
        &lt;/Badge&gt;
      &lt;/div&gt;
    )}

    Add Client Details block inside CardContent after the permissions section:
    &lt;div className="bg-muted/50 rounded-lg p-4 space-y-2"&gt;
      &lt;p className="text-xs font-inter font-medium text-muted-foreground uppercase tracking-wide"&gt;Client Details&lt;/p&gt;
      &lt;div className="flex items-center gap-2"&gt;
        &lt;RiShieldCheckLine className="h-4 w-4 text-muted-foreground flex-shrink-0" aria-hidden="true" /&gt;
        &lt;div className="space-y-0.5"&gt;
          &lt;p className="text-xs text-muted-foreground font-inter font-light"&gt;Client ID&lt;/p&gt;
          &lt;span className="font-mono text-xs text-foreground"&gt;{truncatedClientId ?? 'Unknown'}&lt;/span&gt;
        &lt;/div&gt;
      &lt;/div&gt;
      {redirectDomain && (
        &lt;div className="flex items-center gap-2 mt-1.5"&gt;
          &lt;RiShieldCheckLine className="h-4 w-4 text-muted-foreground flex-shrink-0" aria-hidden="true" /&gt;
          &lt;div className="space-y-0.5"&gt;
            &lt;p className="text-xs text-muted-foreground font-inter font-light"&gt;Redirect destination&lt;/p&gt;
            &lt;span className="text-xs font-inter text-foreground"&gt;{redirectDomain}&lt;/span&gt;
          &lt;/div&gt;
        &lt;/div&gt;
      )}
    &lt;/div&gt;

    Add advisory text paragraph directly below Client Details block:
    &lt;p className="text-muted-foreground text-sm font-inter font-light"&gt;Only approve if you initiated this connection from your AI client.&lt;/p&gt;

    Workspace param fix (ISC-35–38): Verify the existing useEffect for workspace_id auto-population has selectedOrgId guard active. The existing workspaces.find(ws =&gt; ws.id === requestedWorkspaceId) already provides the validation (workspace won't be found if not in the filtered array). Confirm the if (!selectedOrgId) return; guard at ~line 102 runs before any workspace param application. No structural change needed if this guard is already present — add a comment: // ISC-36: workspace param silently ignored if not in selected org's workspaces. If the guard is absent, add it before any workspace state assignment.

    STEP 3 — Verify GREEN: Run npm run test -- --run src/test/OAuthConsentPage.test.tsx — all 5 tests MUST PASS.
  </action>
  <verify>
    <automated>ls /Users/admin/dev/brain/src/test/OAuthConsentPage.test.tsx && echo "test file exists"</automated>
    <automated>grep -c "isFirstTimeClient" /Users/admin/dev/brain/src/pages/OAuthConsentPage.tsx; echo "expected: >= 2"</automated>
    <automated>grep -c "truncatedClientId" /Users/admin/dev/brain/src/pages/OAuthConsentPage.tsx; echo "expected: >= 1"</automated>
    <automated>grep -c "border-amber-400" /Users/admin/dev/brain/src/pages/OAuthConsentPage.tsx; echo "expected: 1"</automated>
    <automated>grep -c "Only approve if" /Users/admin/dev/brain/src/pages/OAuthConsentPage.tsx; echo "expected: 1"</automated>
    <automated>npm run test -- --run src/test/OAuthConsentPage.test.tsx 2>&1 | tail -10</automated>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    src/test/OAuthConsentPage.test.tsx exists with 5 behavior cases — all pass. OAuthConsentPage.tsx shows truncated client_id, first-time badge (conditional), redirect origin, and advisory text. Workspace param guard confirmed. npm run build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Configure Cloudflare rate limit rule for DCR endpoint (ISC-17)</name>
  <what-built>
    Task 1 complete — OAuthConsentPage security fields are implemented and tests pass. This checkpoint configures the Cloudflare rate limit rule that blocks DCR endpoint abuse. This is one of the 7 Critical/High security gates blocking DNS provisioning (per D-03). It cannot remain as a code comment only — the CDN-tier control must be active.
  </what-built>
  <how-to-verify>
    Configure the rate limit rule via Cloudflare API (CLOUDFLARE_API_TOKEN and CLOUDFLARE_ZONE_ID are already present in the environment per CONTEXT.md):

    Run the following curl command to create the rate limit rule:
    curl -s -X POST "https://api.cloudflare.com/client/v4/zones/${CLOUDFLARE_ZONE_ID}/rate_limits" \
      -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" \
      -H "Content-Type: application/json" \
      -d '{
        "match": {
          "request": {
            "url_pattern": "*.callvaultai.com/mcp-register",
            "methods": ["POST"]
          }
        },
        "threshold": 10,
        "period": 3600,
        "action": {
          "mode": "ban",
          "timeout": 3600,
          "response": {
            "content_type": "application/json",
            "body": "{\"error\":\"rate_limit_exceeded\",\"message\":\"Too many registration attempts. Try again later.\"}"
          }
        },
        "description": "ISC-17: DCR endpoint — max 10 registrations per IP per hour"
      }'

    Expected: response JSON contains "success":true and an "id" field for the new rule.

    If the Cloudflare zone does not support legacy rate limits (Zones on Business/Enterprise may use Ruleset-based rate limits instead), use the Ruleset API:
    curl -s -X POST "https://api.cloudflare.com/client/v4/zones/${CLOUDFLARE_ZONE_ID}/rulesets/phases/http_ratelimit/entrypoint/rules" \
      -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" \
      -H "Content-Type: application/json" \
      -d '{"description":"ISC-17 DCR rate limit","expression":"(http.host matches \".*\\\\.callvaultai\\\\.com\" and http.request.uri.path eq \"/mcp-register\" and http.request.method eq \"POST\")","action":"block","ratelimit":{"characteristics":["ip.src"],"period":3600,"requests_per_period":10,"mitigation_timeout":3600}}'

    After successful API call, verify the rule exists:
    curl -s "https://api.cloudflare.com/client/v4/zones/${CLOUDFLARE_ZONE_ID}/rate_limits" \
      -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" | grep -c "ISC-17\|mcp-register"
    Expected: 1 (rule appears in the list).
  </how-to-verify>
  <resume-signal>
    Type "rate-limit-configured" after the Cloudflare API call returns "success":true and the rule ID is visible in the rate limits list. If the API call fails, paste the error response.
  </resume-signal>
  <files></files>
  <action>No automated action. Execute the curl commands in how-to-verify to create and confirm the Cloudflare rate limit rule for ISC-17.</action>
  <verify>
    <human-check>Cloudflare rate limit rule for *.callvaultai.com/mcp-register exists (verified via API list call); threshold is 10 req/hr per IP; action is ban/block. Rate limit rule ID returned in creation response.</human-check>
  </verify>
  <done>
    Cloudflare rate limit rule ISC-17 active: max 10 POST requests per IP per hour to *.callvaultai.com/mcp-register; excess requests return 429/ban. Rule ID confirmed in Cloudflare API response.
  </done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1

**Threats addressed by this plan:**
- [T-06.1-DCR-01]: Social Engineering — DCR phishing. Attacker registers client, phishes user to consent page. User sees only attacker-controlled client_name. Mitigation: consent page now shows client_id UUID (not attacker-controlled), first-time badge warns when client never seen before, redirect domain shows where access will be returned to (ISC-13–19).
- [T-06.1-WP-01]: Tampering — Cross-org workspace_id injection. Attacker sets ?workspace_id=&lt;other-org-workspace&gt; to lock consent UI to adversarial workspace. Mitigation: workspace param validated against workspaces filtered by selectedOrgId; param silently ignored if workspace not found in current org (ISC-35–38).
- [T-06.1-DCR-02]: Denial of Service / Mass Registration — Automated DCR endpoint abuse. Attacker registers hundreds of fake clients to exhaust system resources or flood the consent screen. Mitigation: Cloudflare rate limit rule caps DCR registrations at 10/IP/hr (ISC-17) — configured in Task 2 blocking checkpoint.
- [T-06.1-SC]: Tampering — npm installs. No new packages; Badge already in codebase via shadcn.

**Residual risks:** None — DCR rate limiting (ISC-17: 10 registrations/IP/hr) is now enforced at CDN tier via Cloudflare rule (Task 2 blocking checkpoint), not just a code comment. The consent page security fields address ISC-13–19. Workspace param guard addresses ISC-35–38.

**Block condition:** Any HIGH severity threat not mitigated = executor must stop and surface to operator. Task 2 (rate limit checkpoint) is blocking — plan does not complete until rate limit rule is confirmed active.
</threat_model>

<verification>
- src/test/OAuthConsentPage.test.tsx exists with 5 behavior cases, all pass
- grep -c "isFirstTimeClient" OAuthConsentPage.tsx >= 2
- grep -c "border-amber-400" OAuthConsentPage.tsx = 1
- grep -c "truncatedClientId" OAuthConsentPage.tsx >= 1
- grep -c "Only approve if" OAuthConsentPage.tsx = 1
- npm run test -- --run src/test/OAuthConsentPage.test.tsx passes
- npm run build passes
- Cloudflare rate limit rule for mcp-register exists (Task 2 human checkpoint confirmed)
</verification>

<success_criteria>
OAuthConsentPage.tsx renders: (1) truncated client_id UUID always, (2) amber first-time badge conditionally, (3) redirect origin, (4) advisory text. Workspace param guard prevents cross-org workspace pre-population. OAuthConsentPage.test.tsx has 5 passing tests. Cloudflare rate limit rule (max 10 DCR/IP/hr) is active. npm run build and tests pass.
</success_criteria>

<output>
Create `.planning/phases/06.1-mcp-subdomain-routing/06.1-sec-dcr-phishing-SUMMARY.md` when done.
</output>
