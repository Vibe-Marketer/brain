---
phase: 06.1
plan: sec-slug-tombstone
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260609000002_slug_tombstone.sql
autonomous: true
requirements: [SEC-SLUG-TOMBSTONE]
must_haves:
  truths:
    - "org_slug_tombstone table exists: slug VARCHAR(40) PRIMARY KEY, retired_at TIMESTAMPTZ NOT NULL DEFAULT now()"
    - "workspace_slug_tombstone table exists: (org_id UUID, slug VARCHAR(40), retired_at TIMESTAMPTZ, PRIMARY KEY (org_id, slug))"
    - "AFTER DELETE trigger on organizations inserts OLD.slug into org_slug_tombstone"
    - "AFTER DELETE trigger on workspaces inserts (OLD.org_id, OLD.slug) into workspace_slug_tombstone"
    - "supabase db push --use-api applies migration cleanly"
    - "This migration must run BEFORE 06.1-slug-schema-PLAN.md (D-08)"
  artifacts:
    - path: "supabase/migrations/20260609000002_slug_tombstone.sql"
      provides: "Tombstone tables preventing slug re-registration after org/workspace deletion"
      contains: "org_slug_tombstone"
  key_links:
    - from: "organizations DELETE"
      to: "org_slug_tombstone"
      via: "tombstone_org_slug() trigger"
      pattern: "tombstone_org_slug"
---

<objective>
Create migration `supabase/migrations/20260609000002_slug_tombstone.sql` that creates the `org_slug_tombstone` and `workspace_slug_tombstone` tables along with AFTER DELETE triggers on `organizations` and `workspaces`.

Purpose: Without tombstoning, a deleted org's slug can be re-registered by a new org — reconnecting old MCP integrations to a different org's data. Tombstone tables permanently prevent slug re-use (ISC-20–26, CRITICAL security gate).

CRITICAL: This migration MUST be applied BEFORE slug columns are added (slug-schema plan, Wave 3). The slug-schema migration's DELETE trigger references `org_slug_tombstone` — if this table doesn't exist, the trigger errors.

Output: Migration file with tombstone tables and triggers. Applied via supabase db push --use-api.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-mcp-subdomain-routing/06.1-RESEARCH.md
@.planning/phases/06.1-mcp-subdomain-routing/06.1-PATTERNS.md

<interfaces>
<!-- From RESEARCH.md Pattern 3 (ISC-20–26):
CREATE TABLE IF NOT EXISTS public.org_slug_tombstone (
  slug VARCHAR(40) PRIMARY KEY,
  retired_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.workspace_slug_tombstone (
  org_id UUID NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE,
  slug VARCHAR(40) NOT NULL,
  retired_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  PRIMARY KEY (org_id, slug)
);

Trigger function pattern (from PATTERNS.md):
CREATE OR REPLACE FUNCTION public.tombstone_org_slug()
RETURNS TRIGGER LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = extensions, public
AS $$
BEGIN
  IF OLD.slug IS NOT NULL THEN
    INSERT INTO public.org_slug_tombstone (slug) VALUES (OLD.slug)
    ON CONFLICT (slug) DO NOTHING;
  END IF;
  RETURN OLD;
END;
$$;

CREATE TRIGGER tr_tombstone_org_slug
AFTER DELETE ON public.organizations
FOR EACH ROW EXECUTE FUNCTION public.tombstone_org_slug();

Same pattern for workspace_slug_tombstone triggered on workspaces DELETE.
Note: At tombstone migration time, organizations.slug column does NOT yet exist.
The trigger must handle OLD.slug being NULL (ISC-21 requires slug reference, but IF OLD.slug IS NOT NULL check handles pre-slug rows).
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create slug tombstone migration and apply to database</name>
  <files>supabase/migrations/20260609000002_slug_tombstone.sql</files>
  <read_first>
    supabase/migrations/20260601054000_fix_mcp_token_crypto_search_path.sql — confirm SECURITY DEFINER SET search_path pattern
    supabase/migrations/20260528163000_mcp_oauth_client_grants_and_prefixed_tokens.sql — confirm RLS enablement pattern
  </read_first>
  <action>
    Create supabase/migrations/20260609000002_slug_tombstone.sql.

    Migration header comment block: Migration: Slug tombstone tables for org and workspace slugs; Purpose: Prevent re-registration of deleted org/workspace slugs; Date: 2026-06-09

    Section 1 — TABLES:
    CREATE TABLE IF NOT EXISTS public.org_slug_tombstone (slug VARCHAR(40) PRIMARY KEY, retired_at TIMESTAMPTZ NOT NULL DEFAULT now());
    CREATE TABLE IF NOT EXISTS public.workspace_slug_tombstone (org_id UUID NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE, slug VARCHAR(40) NOT NULL, retired_at TIMESTAMPTZ NOT NULL DEFAULT now(), PRIMARY KEY (org_id, slug));

    Section 2 — RLS:
    ALTER TABLE public.org_slug_tombstone ENABLE ROW LEVEL SECURITY;
    ALTER TABLE public.workspace_slug_tombstone ENABLE ROW LEVEL SECURITY;
    No user-facing RLS policies needed — service-role only access. Add COMMENT ON TABLE for both.

    Section 3 — TRIGGER FUNCTIONS:
    tombstone_org_slug(): SECURITY DEFINER SET search_path = extensions, public. IF OLD.slug IS NOT NULL → INSERT INTO org_slug_tombstone ON CONFLICT DO NOTHING. RETURN OLD.
    tombstone_workspace_slug(): Same pattern, inserts (OLD.org_id, OLD.slug) into workspace_slug_tombstone.

    Note: At the time this migration runs, organizations.slug and workspaces.slug columns do NOT exist yet (they are added in slug-schema plan). The triggers reference OLD.slug but the column won't be there yet. Solution: use a conditional check in the trigger body — wrap in an exception handler or check pg_attribute to see if column exists. Alternatively, since the triggers fire on DELETE and the slug column won't exist yet, Postgres will error when the trigger tries to access OLD.slug.

    CORRECT APPROACH: Define the trigger functions now (they will reference OLD.slug). The triggers themselves should be created in the slug-schema migration after the slug columns are added, not here. Here, only define the tombstone tables and the trigger FUNCTIONS (CREATE OR REPLACE FUNCTION). The CREATE TRIGGER statements go in slug-schema.sql after ADD COLUMN slug.

    Section 4 — COMMENTS:
    COMMENT ON TABLE public.org_slug_tombstone IS 'Permanently retired org slugs; prevents re-registration after org deletion';
    COMMENT ON TABLE public.workspace_slug_tombstone IS 'Permanently retired workspace slugs within an org; prevents re-registration';

    After creating migration file, run: supabase db push --use-api
    This is [BLOCKING] — slug-schema plan depends on this completing.
  </action>
  <verify>
    <automated>ls /Users/admin/dev/brain/supabase/migrations/20260609000002_slug_tombstone.sql && echo "migration file exists"</automated>
    <automated>grep -c "org_slug_tombstone" /Users/admin/dev/brain/supabase/migrations/20260609000002_slug_tombstone.sql; echo "expected: >= 3"</automated>
    <automated>grep -c "workspace_slug_tombstone" /Users/admin/dev/brain/supabase/migrations/20260609000002_slug_tombstone.sql; echo "expected: >= 3"</automated>
    <automated>supabase db push --use-api 2>&1 | tail -10</automated>
  </verify>
  <done>
    supabase/migrations/20260609000002_slug_tombstone.sql exists with org_slug_tombstone and workspace_slug_tombstone table DDL, trigger functions, and RLS enablement. supabase db push --use-api exits 0.
  </done>
</task>

</tasks>

<threat_model>
**ASVS Level:** L1

**Threats addressed by this plan:**
- [T-06.1-SLUG-01]: Spoofing — Slug re-registration after org deletion. After org "acmecorp" is deleted, a new org registers the same "acmecorp" slug. Old Claude Desktop connections to acmecorp.callvaultai.com/mcp now route to a different org's data. Mitigation: org_slug_tombstone permanently records all deleted slugs; slug creation checks both organizations.slug AND org_slug_tombstone (ISC-20–26).
- [T-06.1-SLUG-02]: Spoofing — Same attack at workspace scope. Mitigation: workspace_slug_tombstone provides the same protection per (org_id, slug) pair.

**Residual risks:** Tombstone tables are created here; the triggers that populate them (INSERT after DELETE) are deferred to the slug-schema migration because organizations.slug column doesn't exist yet. The tombstoning protection only activates after slug columns are added in Wave 3.

**Block condition:** Any HIGH severity threat not mitigated = executor must stop and surface to operator.
</threat_model>

<verification>
- supabase/migrations/20260609000002_slug_tombstone.sql exists
- grep org_slug_tombstone in migration >= 3 matches
- supabase db push --use-api exits 0
</verification>

<success_criteria>
Migration 20260609000002_slug_tombstone.sql applied. org_slug_tombstone and workspace_slug_tombstone tables exist in DB. RLS enabled on both. Trigger functions defined. supabase db push --use-api exits 0.
</success_criteria>

<output>
Create `.planning/phases/06.1-mcp-subdomain-routing/06.1-sec-slug-tombstone-SUMMARY.md` when done.
</output>
