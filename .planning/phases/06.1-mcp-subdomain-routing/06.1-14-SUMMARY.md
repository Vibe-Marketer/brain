---
phase: 06.1-mcp-subdomain-routing
plan: 06.1-14
status: completed
completed_at: 2026-06-10
implementation_commit: a7dd9c55
---

# 06.1-14 Summary: MCP Subdomain Setup URLs

## Implemented

- Added `buildSubdomainMcpUrl(orgSlug, wsSlug?)` in `mcp-tokens.service.ts`.
- Updated `McpSetupSnippets` to show:
  - simple endpoint: `https://mcp.callvaultai.com`;
  - organization endpoint: `https://{orgSlug}.callvaultai.com/mcp`;
  - workspace endpoint: `https://{orgSlug}-{wsSlug}.callvaultai.com/mcp`.
- Kept the legacy UUID workspace URL builder unchanged and available as a fallback when slugs are unavailable.
- Updated `MCPTab` to pass org/workspace slugs into setup snippets from existing organization/workspace hook data.
- Updated `useWorkspaces` queries to select `workspaces.slug`.
- Added focused URL builder tests and updated setup snippet rendering tests for three, two, and one URL states.

## Verification

- `npx tsc --noEmit --pretty false` passed.
- `node node_modules/vitest/vitest.mjs run --run src/services/__tests__/mcp-tokens-url-builder.test.ts src/components/settings/__tests__/McpSetupSnippets.test.tsx` passed: 12 tests.
- `buildScopedMcpUrl('workspace', 'ws-1')` regression test still returns `https://mcp.callvaultai.com/w/ws-1`.
- `npm test -- --run ...` remains blocked by the local `node_modules/.bin/vitest` symlink resolving to `.deno/vitest@4.1.8`; the direct Vitest 4.0.16 entrypoint was used for runnable verification.

## Notes

- The snippet test mocks Radix wrappers because the local Deno-linked packages load a duplicate React instance under direct Vitest. The tests still exercise URL rendering and MCPTab snippet wiring.
