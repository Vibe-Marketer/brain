---
phase: 37-transcript-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260910000000_create_reconciled_transcript_segments.sql
  - src/test/rls-regression.test.ts
  - src/types/supabase.ts
autonomous: false
requirements: [RECON-04, RECON-05, RECON-06]
user_setup: []

must_haves:
  truths:
    - "A new reconciled_transcript_segments table exists with FORCE RLS and a client-facing SELECT policy gated by a SECURITY DEFINER helper (first client-readable ledger in this milestone)"
    - "A user can SELECT a reconciled segment only if they could already see the underlying event's recordings/transcript_chunks; a user from another org sees zero rows"
    - "source_recording_ids and agreeing_recording_ids are stored in a stable deterministic order (sorted by recording ID) so regeneration output is reproducible"
    - "An interval with zero contributing recordings produces no segment row at all"
    - "The table shape supports full delete+rebuild per event (no UNIQUE conflict target required for the write path)"
    - "reconciled_transcript_segments is registered in rls-regression.test.ts and proven to enforce cross-org isolation (service-role seed, User A sees it, User B does not)"
  artifacts:
    - path: "supabase/migrations/20260910000000_create_reconciled_transcript_segments.sql"
      provides: "New derived-transcript ledger table + FORCE RLS + SECURITY DEFINER read helper"
      contains: "CREATE TABLE IF NOT EXISTS reconciled_transcript_segments"
    - path: "src/test/rls-regression.test.ts"
      provides: "Cross-org isolation proof for the new table"
      contains: "reconciled_transcript_segments"
  key_links:
    - from: "reconciled_transcript_segments SELECT policy"
      to: "SECURITY DEFINER helper (user_can_view_event_reconciliation or equivalent)"
      via: "RLS USING clause"
      pattern: "user_can_view.*event"
  prohibitions:
    - "The reconciliation table and its RLS must never make a reconciled segment readable to a user who could not already read the underlying recording/transcript_chunks (no widening through the derived layer)"
    - "A single-source interval must never be persisted or presented as consensus/multi-source — absence of agreement must never be rendered as agreement"
---

<objective>
Lock the one-way-door decisions for Phase 37 and land the derived-transcript storage layer. This plan resolves the reversibility gate (schema shape, RLS/SECURITY-DEFINER read design, and the reconciliation-eligibility gating condition), then authors and TEST-applies the `reconciled_transcript_segments` migration and registers its cross-org isolation proof.

Purpose: Every downstream plan (pure module, edge function, UI) implements against the schema and gating condition locked here. Getting the client-facing RLS wrong is a data-exposure incident (this is the FIRST client-readable ledger in the milestone), and getting the gating condition wrong silently excludes the dominant real-data event shape — both are one-way doors.

Output: One additive migration applied to TEST (prod deferred to Plan 05), the new table registered in the RLS regression harness, and the src/types/supabase.ts delta spliced.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-transcript-reconciliation/37-CONTEXT.md
@.planning/phases/37-transcript-reconciliation/37-RESEARCH.md
@.planning/phases/37-transcript-reconciliation/37-PATTERNS.md
@supabase/CLAUDE.md

<interfaces>
Ledger-table precedent to mirror (live in repo):
- supabase/migrations/20260908120000_create_speaker_resolution_decisions.sql — id UUID PK, event_id UUID REFERENCES events ON DELETE SET NULL, tier/decision/decided_by CHECK columns, signals JSONB, FORCE RLS, "Service role full access" policy, dense COMMENT ON TABLE/COLUMN.
- supabase/migrations/20260901000002_create_event_match_decisions.sql — decision CHECK constraint values: 'merge_proposed','merge_applied','reversed','rejected'.

SECURITY DEFINER read-helper precedents (live): user_participates_in_event (Phase 30 CR-01 fix, migration 20260831020000), user_can_view_identity (Phase 34, migration 20260906000001). Both are SECURITY DEFINER, avoid the RLS-recursion trap that CR-01 documented.

rls-regression.test.ts registries: CROSS_ORG_TABLES (generic org_id column loop), CLIENT_DENY_TABLES / BESPOKE_CLIENT_DENY_TABLES (zero-client-policy deny-all tables). This table fits NEITHER cleanly — it needs a real client SELECT policy, so it needs a NEW bespoke isolation block (seed via service-role, assert User A's JWT sees it, User B's does not), mirroring the events/identities bespoke blocks.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Reversibility gate — lock schema, client-facing RLS, and reconciliation-eligibility gating condition</name>
  <read_first>
    - .planning/phases/37-transcript-reconciliation/37-RESEARCH.md (Open Questions 1-2, Pitfalls 2 & 3, Security Domain, Assumptions A3/A4)
    - .planning/phases/37-transcript-reconciliation/37-CONTEXT.md (Timeline Alignment & Trigger Condition, Regeneration & Non-Destructive Layering)
    - supabase/migrations/20260908120000_create_speaker_resolution_decisions.sql
    - supabase/functions/_shared/speaker-resolver.ts (lines 41-90, 226-320 — confirm deriveAbsoluteInterval / alignChunksAcrossRecordings are already exported; note whether SpeakerChunk fits chunk-group alignment)
  </read_first>
  <decision>Lock five one-way-door choices before any implementation: (a) `reconciled_transcript_segments` column shape; (b) the client-facing RLS SELECT policy + SECURITY DEFINER helper design; (c) the reconciliation-ELIGIBILITY gating condition (the open design question); (d) whether to re-use speaker-resolver.ts's interval primitives by import/re-export vs. defining compatible local types; (e) fastest-levenshtein alignment-path resolution (does it expose a path, or is a hand-rolled DP required).</decision>
  <context>
    Gating condition is the substantive open question (RESEARCH Pitfall 3 / Open Question 2): `speaker_resolution_decisions` only records POSITIVE resolutions, so an event whose recordings already carry named speakers (the dominant real-data case) produces ZERO rows there — gating purely on "has a speaker_resolution_decisions row" would permanently exclude the majority shape. Reconciliation must ALSO gate on event_match_decisions (metadata-tier resolution), NOT merely on recordings sharing a raw event_id (RESEARCH Pitfall 2).
    Client-facing RLS is genuinely new risk surface: unlike event_match_decisions / speaker_resolution_decisions (service-role-only, no client policy), this table is read by the new Reconciled UI tab, so it needs a real SELECT policy scoped at least as tightly as the requesting user's existing transcript_chunks/recordings access.
  </context>
  <options>
    <option id="option-a">
      <name>Gate on event_match_decisions.decision='merge_applied' alone; speaker resolution best-effort/non-blocking</name>
      <pros>Fires for the dominant all-already-named event shape (no speaker_resolution_decisions row required). Simple, single queryable gate. Matches forward-only philosophy. RECON-04 delete+rebuild is idempotent regardless.</pros>
      <cons>Reconciliation may run before a genuinely-pending speaker resolution finishes for an anonymous-label event; acceptable because delete+rebuild re-runs when new data appears (CONTEXT: "can re-run whenever new source data appears").</cons>
    </option>
    <option id="option-b">
      <name>Gate on event_match_decisions.decision='merge_applied' PLUS a forward-only cutover proxy (recordings created after both event- and speaker-resolution cutovers)</name>
      <pros>Approximates "speaker resolution has been considered" without a new tracking column; mirrors resolve-speakers' own cutover pattern.</pros>
      <cons>Two conditions to reason about; the cutover proxy is a heuristic, not a proof that resolve-speakers actually ran.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - A recorded decision (in STATE.md decisions or the plan SUMMARY) names: the exact column list for reconciled_transcript_segments; the SELECT-policy/SECURITY-DEFINER helper name and its access predicate; the chosen gating option (a or b) with the exact event_match_decisions.decision value(s) accepted; the import-vs-local-type choice for interval primitives; and the fastest-levenshtein alignment-path finding (from a 2-minute node_modules/.d.ts check).
    - No implementation task (Task 2+) proceeds until this is resolved.
  </acceptance_criteria>
  <resume-signal>Select: option-a or option-b, and confirm/override the schema + RLS-helper naming.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author + TEST-apply the reconciled_transcript_segments migration with client-facing RLS</name>
  <read_first>
    - supabase/migrations/20260908120000_create_speaker_resolution_decisions.sql (table + FORCE RLS + service-role policy + COMMENT density to mirror)
    - supabase/migrations/20260906000001 and 20260831020000 (SECURITY DEFINER read-helper precedents)
    - supabase/CLAUDE.md (TEST-then-prod discipline, prod-ref guard vltmrnjsubfzrgrtdqey, .env vs .env.local/.env.test)
    - Task 1's locked decision record
  </read_first>
  <action>
    Create supabase/migrations/20260910000000_create_reconciled_transcript_segments.sql (bump the timestamp if a later migration has landed) implementing exactly the shape locked in Task 1 (per RECON-04/05/06): id UUID PK DEFAULT gen_random_uuid(); event_id UUID REFERENCES events(id) ON DELETE CASCADE; segment_text TEXT NOT NULL; start_time / end_time TIMESTAMPTZ; source_recording_ids UUID[] NOT NULL; agreeing_recording_ids UUID[] NOT NULL; signals JSONB NOT NULL DEFAULT '{}'::jsonb (per-segment provenance/vote metadata); organization_id UUID NOT NULL (bucketing/RLS scope); created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(). Store the two UUID[] columns sorted ascending by recording id (document this ordering contract in a COMMENT). NO UNIQUE(...) conflict target — the write path is full delete+rebuild per event (RESEARCH Pitfall 5), so no upsert conflict key is needed. ENABLE + FORCE ROW LEVEL SECURITY. Add the service-role "full access" policy (writes). Add the NEW client-facing SELECT policy gating on the SECURITY DEFINER helper locked in Task 1 (mirror user_can_view_identity's SECURITY DEFINER structure to avoid the CR-01 RLS-recursion trap) — the predicate must resolve TRUE only for users who can already see the event's recordings/transcript_chunks for that organization_id. Add dense COMMENT ON TABLE / COMMENT ON COLUMN documenting: derived/non-destructive posture, full delete+rebuild semantics, single-source-vs-consensus meaning of agreeing_recording_ids length, and that this is the first client-readable ledger. Apply ONLY to TEST (callvault-test) via the supabase CLI; verify the CLI is NOT linked to prod ref vltmrnjsubfzrgrtdqey before applying, then introspect the applied shape (columns, FORCE RLS true, both policies present). Splice the resulting type delta into src/types/supabase.ts by hand (mirror the Plan 34-02 / 36-03 precedent — do NOT regenerate the whole file). Prod apply is Plan 05's job only.
  </action>
  <verify>
    <automated>grep -c "FORCE ROW LEVEL SECURITY" supabase/migrations/20260910000000_create_reconciled_transcript_segments.sql | grep -qv '^0$' && grep -Eq "source_recording_ids UUID\[\]|agreeing_recording_ids UUID\[\]" supabase/migrations/20260910000000_create_reconciled_transcript_segments.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists, contains the FORCE RLS + service-role write policy + a client SELECT policy referencing a SECURITY DEFINER helper.
    - No UNIQUE constraint on the table (delete+rebuild, not upsert).
    - Applied to TEST only; CLI confirmed NOT on prod ref vltmrnjsubfzrgrtdqey at apply time; introspection confirms columns + FORCE RLS + both policies.
    - src/types/supabase.ts includes reconciled_transcript_segments with zero unrelated drift.
  </acceptance_criteria>
  <done>TEST has the table live with client-facing RLS proven present by introspection; types spliced; prod untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Register reconciled_transcript_segments cross-org isolation in rls-regression.test.ts</name>
  <read_first>
    - src/test/rls-regression.test.ts (CROSS_ORG_TABLES loop; CLIENT_DENY_TABLES / BESPOKE_CLIENT_DENY_TABLES; the events/identities bespoke isolation blocks; the speaker_resolution_decisions bespoke entry)
    - STATE.md decisions on prior bespoke-block additions (Phase 31 P03, Phase 35 P03) — multi-FK-parent seed pattern
  </read_first>
  <action>
    Add a NEW bespoke isolation block for reconciled_transcript_segments (do NOT add it to CLIENT_DENY_TABLES/BESPOKE_CLIENT_DENY_TABLES — those are zero-client-policy deny-all tables; this table has a real client SELECT policy). Seed one reconciled segment via the service-role client tied to Org A's event/recordings, then assert: User A (member/participant with underlying recording access) CAN SELECT it via their JWT, and User B (different org, no access to the underlying recordings) sees ZERO rows. This is a negative/adversarial assertion (seed via service-role, prove the wrong client cannot read) per the standing test-quality directive. Reuse the multi-FK-parent seed helper pattern established for event_match_decisions/speaker_resolution_decisions; clean up workspace_entries before recordings on teardown and check .error on every cleanup call (the recurring protect_recording_delete trigger bug — Phase 31/36).
  </action>
  <verify>
    <automated>npm run test:integration -- rls-regression 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The new block seeds via service-role and proves User A reads / User B cannot (cross-org isolation).
    - Test is green on TEST; teardown leaves no orphaned fixtures.
    - The assertion targets real RLS behavior, not row-count ceremony.
  </acceptance_criteria>
  <done>Cross-org isolation for the new client-readable table is proven by an adversarial test on TEST.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (user JWT) → reconciled_transcript_segments | First client-readable ledger in the milestone; untrusted read crosses RLS here |
| service_role (edge fn) → reconciled_transcript_segments | Trusted write path (delete+rebuild) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Information Disclosure / Elevation | reconciled_transcript_segments SELECT policy | mitigate | Client SELECT gated by SECURITY DEFINER helper scoped to the user's existing recording/transcript_chunks access for that organization_id; proven by the new rls-regression bespoke block (User A reads, User B cannot). |
| T-37-04 | Tampering | Derived layer overwriting source | mitigate | Table is a separate derived ledger; transcript_chunks never referenced as a write target in this migration (enforced downstream in Plan 03). |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — fastest-levenshtein already installed and audited (Phase 32). No new install task exists, so no legitimacy checkpoint required. |
</threat_model>

<artifacts>
## Artifacts this plan produces
- SQL table: `reconciled_transcript_segments` (columns: id, event_id, segment_text, start_time, end_time, source_recording_ids UUID[], agreeing_recording_ids UUID[], signals JSONB, organization_id, created_at)
- SQL policies: "Service role full access" (write), client SELECT policy on `reconciled_transcript_segments`
- SQL function: SECURITY DEFINER read helper (name locked in Task 1, e.g. `user_can_view_event_reconciliation`)
- Migration file: `supabase/migrations/20260910000000_create_reconciled_transcript_segments.sql`
- Test block: reconciled_transcript_segments bespoke cross-org isolation block in `src/test/rls-regression.test.ts`
- Type delta: `reconciled_transcript_segments` in `src/types/supabase.ts`
</artifacts>

<verification>
- Migration contains FORCE RLS + service-role write policy + client SELECT policy referencing a SECURITY DEFINER helper; no UNIQUE conflict target.
- TEST apply performed with prod-ref guard (CLI not on vltmrnjsubfzrgrtdqey); introspection confirms shape.
- rls-regression bespoke block green on TEST (User A reads, User B denied).
</verification>

<success_criteria>
- All five one-way-door decisions locked and recorded.
- reconciled_transcript_segments live on TEST with client-facing RLS.
- Cross-org isolation proven adversarially; prod untouched.
</success_criteria>

<output>
Create `.planning/phases/37-transcript-reconciliation/37-01-SUMMARY.md` when done.
</output>
