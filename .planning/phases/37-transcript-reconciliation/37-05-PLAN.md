---
phase: 37-transcript-reconciliation
plan: 05
type: execute
wave: 5
depends_on: [37-01, 37-02, 37-03, 37-04]
files_modified:
  - supabase/migrations/20260910000000_create_reconciled_transcript_segments.sql
  - supabase/functions/reconcile-transcripts/index.ts
autonomous: false
requirements: [RECON-01, RECON-02, RECON-03, RECON-04, RECON-05, RECON-06, RECON-07]
user_setup:
  - service: supabase-prod
    why: "Applying the reconciliation migration + deploying the edge function to production requires Andrew's explicit authorization (v2.2 touches live customer data)"
    env_vars: []
    dashboard_config:
      - task: "Confirm reconcile-transcripts-sweep cron GUCs if a cron is added (app.supabase_url / app.reconcile_secret) — same unset-GUC failure mode as event-resolution-sweep"
        location: "Supabase Dashboard → Settings → Database → Custom postgres settings"

must_haves:
  truths:
    - "The reconciled_transcript_segments migration is applied to production (vltmrnjsubfzrgrtdqey) with the prod-ref guard verified before AND after"
    - "reconcile-transcripts is deployed to production with FORCE RLS, client SELECT policy, and EXECUTE gating confirmed by direct prod introspection"
    - "The reconciliation mechanism is proven inert-by-default in prod (no reconciled rows appear except for explicitly-eligible flagged-org events)"
    - "transcript_chunks and embedded_at remain untouched in prod after any manual sweep trigger"
  artifacts:
    - path: "supabase/migrations/20260910000000_create_reconciled_transcript_segments.sql"
      provides: "The migration applied to prod in this plan"
  key_links:
    - from: "prod database"
      to: "reconciled_transcript_segments"
      via: "applied migration"
      pattern: "reconciled_transcript_segments"
  prohibitions:
    - "The migration must never be applied to production without the prod-ref guard (vltmrnjsubfzrgrtdqey) verified immediately before connecting"
    - "Production apply must never proceed without explicit human authorization (auto_advance does not apply to this gate)"
---

<objective>
Apply Phase 37 to production: the reconciled_transcript_segments migration + the reconcile-transcripts edge function, under the milestone's TEST-then-prod, human-authorized, prod-ref-guarded discipline — then prove the mechanism live and inert-by-default via direct prod introspection.

Purpose: Every prior v2.2 phase ends with a guarded prod-apply plan (31-04, 33-03, 36-06). This is that plan for Phase 37.

Output: Migration + edge function live on prod, proven by introspection; STATE.md/REQUIREMENTS.md completion notes.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-transcript-reconciliation/37-01-SUMMARY.md
@.planning/phases/37-transcript-reconciliation/37-02-SUMMARY.md
@.planning/phases/37-transcript-reconciliation/37-03-SUMMARY.md
@.planning/phases/37-transcript-reconciliation/37-04-SUMMARY.md
@supabase/CLAUDE.md

<interfaces>
Prod-apply precedents (STATE.md): 31-04, 33-03, 36-06. Pattern: verify CLI/DATABASE_URL prod ref = vltmrnjsubfzrgrtdqey BEFORE connecting; apply migration; redeploy edge function (--use-api per supabase/CLAUDE.md; add --no-verify-jwt if a platform JWT gate blocks the secret-gated function, per Phase 32 P05); verify prod ref AGAIN after; introspect: table exists + FORCE RLS true + both policies present + EXECUTE denied to anon/authenticated where applicable; confirm zero reconciled rows for non-eligible orgs.

Operational note (STATE.md): event-resolution-sweep pg_cron has failed every tick since creation (unset app.supabase_url / app.reconcile_secret GUCs, requires Andrew via Dashboard). If a reconcile-transcripts-sweep cron is added, it hits the identical failure mode — prefer manual/direct-invocation proof (as SAFE-06 was proven) unless Andrew has fixed the GUCs.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Authorize production apply</name>
  <read_first>
    - .planning/phases/37-transcript-reconciliation/37-01-SUMMARY.md through 37-04-SUMMARY.md (what's proven on TEST)
    - supabase/CLAUDE.md (.env = PROD, prod-ref guard)
    - STATE.md Blockers/Concerns (transcript_chunks linkage note before enabling reconciliation for any org)
  </read_first>
  <decision>Authorize applying the reconciled_transcript_segments migration and deploying reconcile-transcripts to production (vltmrnjsubfzrgrtdqey), and decide whether to add a reconcile-transcripts-sweep cron now or defer (given the known unset-GUC cron failure).</decision>
  <context>
    This is a one-way door: the migration lands on live customer data. It is additive and inert by default (no org is flagged for reconciliation unless event_resolution + eligible data exist), but must not be applied without explicit sign-off. auto_advance does NOT apply to this gate.
  </context>
  <options>
    <option id="apply-no-cron">
      <name>Apply migration + deploy function; defer cron (manual/direct-trigger proof only)</name>
      <pros>Avoids the known dead-cron GUC failure; matches how SAFE-06 was proven; mechanism stays inert until manually triggered.</pros>
      <cons>No automatic sweep until GUCs are fixed by Andrew.</cons>
    </option>
    <option id="apply-with-cron">
      <name>Apply migration + deploy function + add the sweep cron</name>
      <pros>Automatic sweep once GUCs are set.</pros>
      <cons>Cron will fail every tick until Andrew sets app.supabase_url / app.reconcile_secret in the Dashboard.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Explicit human authorization recorded, with the cron decision (apply-no-cron or apply-with-cron).
    - No prod connection attempted before this is resolved.
  </acceptance_criteria>
  <resume-signal>Select: apply-no-cron or apply-with-cron (or hold).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply migration + deploy edge function to prod; prove live and inert</name>
  <read_first>
    - supabase/CLAUDE.md (prod-ref guard, --use-api deploy convention)
    - Task 1 authorization + cron decision
  </read_first>
  <action>
    Verify the CLI/DATABASE_URL prod ref is exactly vltmrnjsubfzrgrtdqey BEFORE connecting. Apply the reconciled_transcript_segments migration to prod. Deploy reconcile-transcripts to prod (--use-api; add --no-verify-jwt only if a platform JWT gate blocks the secret-gated function, mirroring Phase 32 P05). If Task 1 chose apply-with-cron, add the sweep cron mirroring event_resolution_sweep_cron.sql and note the GUC prerequisite in the SUMMARY's User Setup Required section. Verify the prod ref AGAIN after. Introspect prod: reconciled_transcript_segments exists with FORCE RLS true + service-role write policy + client SELECT policy present; the SECURITY DEFINER read helper exists; the function is deployed. Prove inert-by-default: confirm zero reconciled_transcript_segments rows for any org not explicitly eligible (per STATE.md's transcript_chunks-linkage caution, do NOT enable reconciliation for a new org without checking that org's chunk linkage first). Optionally trigger a single manual forward sweep against the one eligible flagged org (mirroring how SAFE-06 was proven) and confirm transcript_chunks + embedded_at are byte-unchanged afterward (RECON-04/07 live proof). Update REQUIREMENTS.md (RECON-01..07 → checked) and STATE.md.
  </action>
  <verify>
    <automated>echo "Manual introspection task — evidence recorded in 37-05-SUMMARY.md (prod ref verified, table/policies/function live, inert-by-default confirmed)"</automated>
  </verify>
  <acceptance_criteria>
    - Prod ref vltmrnjsubfzrgrtdqey verified before AND after apply.
    - Migration + function live on prod; FORCE RLS + both policies + SECURITY DEFINER helper confirmed by introspection.
    - Mechanism proven inert-by-default (no reconciled rows for non-eligible orgs); any manual sweep left transcript_chunks/embedded_at unchanged.
    - REQUIREMENTS.md RECON-01..07 checked; STATE.md updated.
  </acceptance_criteria>
  <done>Phase 37 is live in production, proven by direct introspection, non-destructive and inert by default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → PRODUCTION db (vltmrnjsubfzrgrtdqey) | Highest-blast-radius boundary; guarded by prod-ref check + human authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-09 | Tampering | Wrong-project migration apply (TEST creds vs PROD) | mitigate | Prod ref vltmrnjsubfzrgrtdqey verified before AND after; human authorization gate (Task 1) precedes any prod connection. |
| T-37-01 | Information Disclosure | Client-readable table live in prod | mitigate | FORCE RLS + client SELECT policy + SECURITY DEFINER helper confirmed present by prod introspection before declaring done. |
| T-37-07 | Tampering | Live re-embedding | mitigate | Post-apply manual-sweep check confirms transcript_chunks.embedded_at byte-unchanged in prod. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase. |
</threat_model>

<artifacts>
## Artifacts this plan produces
- Production: `reconciled_transcript_segments` table + policies + SECURITY DEFINER read helper (live)
- Production: `reconcile-transcripts` edge function (live)
- Optional: `reconcile-transcripts-sweep` pg_cron (only if apply-with-cron chosen)
- Updated: REQUIREMENTS.md (RECON-01..07), STATE.md
</artifacts>

<verification>
- Prod-ref guard verified twice; introspection confirms table/policies/helper/function live.
- Inert-by-default confirmed; manual sweep (if run) left source + embeddings untouched.
</verification>

<success_criteria>
- Phase 37 shipped to production non-destructively, human-authorized, prod-ref-guarded, and proven by introspection.
</success_criteria>

<output>
Create `.planning/phases/37-transcript-reconciliation/37-05-SUMMARY.md` when done.
</output>
