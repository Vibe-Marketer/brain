---
phase: 37-transcript-reconciliation
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - supabase/functions/_shared/transcript-reconciler.ts
  - supabase/functions/_shared/__tests__/transcript-reconciler.test.ts
  - supabase/functions/_shared/speaker-resolver.ts
autonomous: true
requirements: [RECON-01, RECON-02, RECON-03, RECON-05, RECON-06]
user_setup: []

must_haves:
  truths:
    - "Two recordings' overlapping chunks align on a shared content-derived timeline within ±20s tolerance; non-overlapping chunks remain unaligned single-source"
    - "Adjacent segments that touch but do not overlap remain separate segments — merging happens only for genuinely overlapping content intervals"
    - "A single-source interval (only one contributing recording) is marked single-source, never consensus"
    - "Token-level disagreements resolve by weighted vote across hardcoded per-provider source-accuracy priors + per-token confidence where exposed + majority"
    - "A per-workspace entity lexicon (passed in as a lookup set) breaks ties, preferring the seeded correct spelling (ChatGPT over ChatGBT)"
    - "A true 3-way tie resolves deterministically via a fixed provider-priority fallback order — identical output across repeated runs, never random"
    - "buildReconciledSegment emits source_recording_ids and agreeing_recording_ids sorted deterministically by recording id"
    - "Token comparison is fuzzy (fastest-levenshtein distance, length-scaled threshold), so single-char misspellings align as the same token rather than corrupting the surrounding alignment"
  artifacts:
    - path: "supabase/functions/_shared/transcript-reconciler.ts"
      provides: "Pure DB-free reconciliation module: alignChunksToTimeline, tokenizeAndAlignText, resolveTokenDisagreement, buildReconciledSegment"
      min_lines: 120
      exports: ["tokenizeAndAlignText", "resolveTokenDisagreement", "buildReconciledSegment"]
    - path: "supabase/functions/_shared/__tests__/transcript-reconciler.test.ts"
      provides: "Exhaustive unit coverage RECON-01/02/03/05/06 incl. adversarial determinism + single-source tests"
      contains: "deterministic"
  key_links:
    - from: "transcript-reconciler.ts"
      to: "speaker-resolver.ts"
      via: "import of deriveAbsoluteInterval / alignChunksAcrossRecordings / CLOCK_DRIFT_TOLERANCE_MS"
      pattern: "from ['\"].*speaker-resolver"
    - from: "transcript-reconciler.ts"
      to: "fastest-levenshtein"
      via: "esm.sh import of distance()"
      pattern: "esm.sh/fastest-levenshtein"
  prohibitions:
    - "Reconciled segment text must never contain a token that no contributing recording actually transcribed — resolution selects among real source tokens (or an entity-lexicon-preferred variant of a real token), never fabricates content"
    - "A single-source interval must never be emitted with agreeing_recording_ids implying consensus"
    - "Tie resolution must never be random or otherwise non-reproducible across regenerations"
---

<objective>
Build the one genuinely novel capability of Phase 37: token-level cross-recording transcript reconciliation as a pure, DB-free, exhaustively-tested module — mirroring the exact shape of speaker-resolver.ts. This is the phase's real risk surface; the surrounding infrastructure is precedented.

Purpose: All alignment + weighted-vote + entity-lexicon-tie-break + coverage-marking logic lives here as pure functions the edge function (Plan 03) merely orchestrates. Test-first (RED→GREEN) per this milestone's convention.

Output: `transcript-reconciler.ts` + its test file, plus any additive exports needed in `speaker-resolver.ts` to reuse the interval primitives.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-transcript-reconciliation/37-RESEARCH.md
@.planning/phases/37-transcript-reconciliation/37-PATTERNS.md
@.planning/phases/37-transcript-reconciliation/37-01-SUMMARY.md
@supabase/functions/_shared/speaker-resolver.ts
@supabase/functions/_shared/dedup-fingerprint.ts

<interfaces>
Reuse (already exported in speaker-resolver.ts, confirmed live):
- deriveAbsoluteInterval(chunk, recordingStartTime) → AbsoluteInterval (anchor + parsed timestamp_start/end, fails closed to null)
- alignChunksAcrossRecordings(input: AlignmentInput) → AlignedChunk[]
- CLOCK_DRIFT_TOLERANCE_MS = 20_000
- Types: SpeakerChunk, AbsoluteInterval, AlignmentInput, AlignedChunk
- Discriminated-result / fail-closed pattern: PropagationResult = PropagatedResolution | UnresolvedSpeaker; ConsensusResult = ConsensusCollapse | ConsensusRefusal (mirror this shape for reconciliation results).

fastest-levenshtein import (proven Phase 32, dedup-fingerprint.ts):
  import { distance } from 'https://esm.sh/fastest-levenshtein@1.0.16';

Hardcoded-priors precedent (dedup-fingerprint.ts): exported named const object (e.g. MATCH_THRESHOLDS) + weighted-sum formula (a*wA + b*wB + c*wC) — mirror for PROVIDER_ACCURACY_PRIORS and the vote formula.

transcript_chunks columns available: chunk_text, chunk_index, entities JSONB {companies:[],people:[],products:[]}, timestamp_start/end, canonical_recording_id, speaker_name/email, source_platform.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contract types + interval-alignment reuse + RED test scaffold</name>
  <read_first>
    - supabase/functions/_shared/speaker-resolver.ts (lines 1-181 types/doc-comment; 197-320 interval primitives + alignChunksAcrossRecordings — confirm export shape; add exports if any needed primitive is currently file-private)
    - .planning/phases/37-transcript-reconciliation/37-PATTERNS.md (transcript-reconciler.ts section)
    - .planning/phases/37-transcript-reconciliation/37-01-SUMMARY.md (Task 1 import-vs-local-type decision)
  </read_first>
  <behavior>
    - alignChunksToTimeline groups chunks from 2+ recordings of one event into aligned interval-groups using the reused ±20s-tolerance primitives; non-overlapping chunks form single-recording groups.
    - Adjacent-but-not-overlapping chunks stay in separate groups (do not merge).
    - An interval with zero contributing recordings yields no group.
  </behavior>
  <action>
    Create transcript-reconciler.ts with the pure/DB-free module doc-comment (mirror speaker-resolver.ts lines 1-31, declaring no DB access). Import deriveAbsoluteInterval / alignChunksAcrossRecordings / CLOCK_DRIFT_TOLERANCE_MS from speaker-resolver.ts per the Task-1 (Plan 01) locked import decision — do NOT reimplement interval math (RESEARCH anti-pattern). If any needed primitive is not currently exported, add the export in speaker-resolver.ts (small additive change, no behavior change). Define contract types with the discriminated-union / fail-closed shape: ReconChunk (compatible with SpeakerChunk plus source_platform + chunk_text + entities), AlignedChunkGroup, TokenAlignment, TokenDisagreement { position, candidates: {token, recordingId, provider, confidence?}[] }, ReconciledSegmentResult (resolved segment_text + source_recording_ids + agreeing_recording_ids + coverage: 'single_source'|'consensus'), and a Refusal shape for anchor-unavailable/insufficient-overlap. Implement alignChunksToTimeline() delegating to alignChunksAcrossRecordings. Write the RED test file covering the alignment behaviors above (align, adjacency-stays-separate, empty-interval, single-source grouping) — tests fail until GREEN.
  </action>
  <verify>
    <automated>npx vitest run supabase/functions/_shared/__tests__/transcript-reconciler.test.ts -t align 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - transcript-reconciler.ts imports interval primitives from speaker-resolver.ts (no duplicated DP math).
    - Contract types use the fail-closed discriminated-union shape.
    - Alignment tests exist and pass; adjacency/empty/single-source edge cases covered.
  </acceptance_criteria>
  <done>alignChunksToTimeline is implemented against reused primitives and green under its unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Token alignment (fuzzy DP) + weighted-vote disagreement resolution</name>
  <read_first>
    - supabase/functions/_shared/dedup-fingerprint.ts (distance() import + usage; MATCH_THRESHOLDS const-object shape)
    - .planning/phases/37-transcript-reconciliation/37-RESEARCH.md (Pattern 3 token-DP, Pitfall 4 fuzzy match, Open Question 3 length-scaled threshold, RECON-02/03)
    - .planning/phases/37-transcript-reconciliation/37-01-SUMMARY.md (fastest-levenshtein alignment-path finding)
  </read_first>
  <behavior>
    - tokensMatch("ChatGPT","ChatGBT") → true (fuzzy, length-scaled threshold); tokensMatch("a","I") → false (short-token guard).
    - tokenizeAndAlignText over 2+ aligned chunks produces per-position TokenDisagreement records for positions where sources differ, using the fuzzy predicate inside a Wagner-Fischer-style DP so single-char variants align as substitutions not insert/delete pairs.
    - resolveTokenDisagreement picks the winning token by weighted vote (PROVIDER_ACCURACY_PRIORS + per-token confidence when present, else majority-only); entity-lexicon lookup breaks otherwise-tied cases in favor of a lexicon-present candidate; a true 3-way tie resolves via the fixed PROVIDER_PRIORITY_ORDER fallback — deterministic, identical across repeated runs.
    - The winning token is always one of the real candidate tokens (or a lexicon-preferred variant of a candidate) — never a fabricated token.
  </behavior>
  <action>
    Implement tokensMatch (length-scaled threshold, e.g. Math.min(2, Math.floor(len*0.25)), short-token exact-match guard), tokenizeAndAlignText (word-tokenize on whitespace, hand-rolled edit-distance DP using tokensMatch as the equality predicate per RESEARCH Pattern 3 — bounded by chunk length), and resolveTokenDisagreement (weighted vote: named exported PROVIDER_ACCURACY_PRIORS const object + weighted-sum formula; per-token confidence consumed only when the source exposes it, else majority; entity-lexicon Set<string> tie-break; deterministic PROVIDER_PRIORITY_ORDER for true 3-way ties). Every branch returns a literal discriminated result. Add exhaustive unit tests including: the ChatGPT/ChatGBT lexicon case, the majority-vote case, the determinism/tie case (run twice, assert byte-identical output), and a fabrication guard (winner ∈ candidate set).
  </action>
  <verify>
    <automated>npx vitest run supabase/functions/_shared/__tests__/transcript-reconciler.test.ts -t "weighted.vote|deterministic|entity.*lexicon" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Fuzzy tokensMatch + DP alignment correct on the near-miss fixtures; short-token guard prevents "a"/"I" false matches.
    - Weighted-vote resolution uses named exported constants (no inline magic numbers).
    - Determinism test proves identical output across repeated runs; fabrication-guard test proves winner is always a real candidate.
  </acceptance_criteria>
  <done>RECON-02/03 logic green, including adversarial determinism and no-fabrication assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: buildReconciledSegment — provenance + coverage marking</name>
  <read_first>
    - supabase/functions/_shared/speaker-resolver.ts (collapsePhantomSpeaker lines 384-431 — fail-closed-guards-first shape)
    - .planning/phases/37-transcript-reconciliation/37-RESEARCH.md (RECON-05/06)
    - The edge coverage report rows for RECON-05/RECON-06 in 37-VALIDATION.md
  </read_first>
  <behavior>
    - buildReconciledSegment assembles a group's resolved token stream into segment_text, and records source_recording_ids (all recordings contributing to the interval) + agreeing_recording_ids (recordings whose text agreed on the winning tokens), both sorted ascending by recording id.
    - agreeing_recording_ids length 1 ⇒ coverage 'single_source'; length ≥2 ⇒ 'consensus'.
    - A single-source group is never marked consensus (no false agreement claim).
    - start_time/end_time derived from the group's aligned interval.
  </behavior>
  <action>
    Implement buildReconciledSegment(group, resolvedTokens) returning ReconciledSegmentResult with segment_text, sorted source_recording_ids/agreeing_recording_ids, coverage marker, and interval bounds. Fail-closed guards first (missing interval / empty group → Refusal). Add unit tests for: single-source group → 'single_source' + no consensus claim (adversarial negative), multi-source agreement → 'consensus', deterministic array ordering, and interval bounds.
  </action>
  <verify>
    <automated>npx vitest run supabase/functions/_shared/__tests__/transcript-reconciler.test.ts -t "single.source|provenance|coverage" 2>&1 | tail -15 && npx vitest run supabase/functions/_shared/__tests__/transcript-reconciler.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Provenance arrays deterministically sorted; coverage marker correct for 1 vs 2+ agreeing sources.
    - Single-source-not-consensus proven by an adversarial negative test.
    - Full unit suite green.
  </acceptance_criteria>
  <done>RECON-05/06 logic green; the pure module is complete and exhaustively tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in pure module) | Module is DB-free; it processes already-fetched, already-org-bucketed chunk data supplied by the edge function (Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05 | Tampering / Information Disclosure | resolveTokenDisagreement fabricating text | mitigate | Winner constrained to the real candidate set (or a lexicon-preferred variant of a candidate); proven by a fabrication-guard unit test. |
| T-37-02 | Information Disclosure | entity-lexicon cross-org leak | mitigate | Lexicon is passed in as a per-workspace Set by the caller (Plan 03 scopes it per org); this module never queries globally. Documented in the function contract. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No installs — fastest-levenshtein already present/audited (Phase 32). |
</threat_model>

<artifacts>
## Artifacts this plan produces
- Pure module: `supabase/functions/_shared/transcript-reconciler.ts`
- Exported functions: `alignChunksToTimeline`, `tokenizeAndAlignText`, `tokensMatch`, `resolveTokenDisagreement`, `buildReconciledSegment`
- Exported constants: `PROVIDER_ACCURACY_PRIORS`, `PROVIDER_PRIORITY_ORDER`, `TOKEN_FUZZY_MATCH_THRESHOLD` (or length-scaled equivalent)
- Exported types: `ReconChunk`, `AlignedChunkGroup`, `TokenAlignment`, `TokenDisagreement`, `ReconciledSegmentResult`
- Test file: `supabase/functions/_shared/__tests__/transcript-reconciler.test.ts`
- Additive exports (if needed): interval primitives re-exported from `supabase/functions/_shared/speaker-resolver.ts`
</artifacts>

<verification>
- Full unit suite green: `npx vitest run supabase/functions/_shared/__tests__/transcript-reconciler.test.ts`.
- Interval math imported from speaker-resolver.ts (grep confirms import, no duplicated DP).
- Determinism, fuzzy-match, single-source-not-consensus, and no-fabrication assertions all present and passing.
</verification>

<success_criteria>
- RECON-01/02/03/05/06 pure logic implemented and exhaustively, adversarially tested.
- Zero DB access in the module.
</success_criteria>

<output>
Create `.planning/phases/37-transcript-reconciliation/37-02-SUMMARY.md` when done.
</output>
