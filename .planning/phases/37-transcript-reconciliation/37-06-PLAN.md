---
phase: 37-transcript-reconciliation
plan: 06
type: execute
wave: 1
depends_on: [37-05]
gap_closure: true
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/37-transcript-reconciliation/37-VERIFICATION.md
autonomous: false
requirements: [RECON-04, RECON-05, RECON-06]
user_setup: []

must_haves:
  truths:
    - "Migration 20260910010000 (reconcile_transcript_segments_atomic RPC + explicit REVOKE EXECUTE from anon/authenticated) is applied to production (vltmrnjsubfzrgrtdqey), prod-ref guarded via `supabase projects list` ● marker BEFORE and AFTER the push (closes VERIFICATION missing #1)."
    - "reconcile-transcripts is redeployed to prod via `--use-api --no-verify-jwt` carrying the CR-01/WR-01/WR-02 fixes, applied migration-FIRST then function (closes VERIFICATION missing #2 + 37-REVIEW-FIX.md's deploy-order caution)."
    - "Prod introspection proves the live reconcile-transcripts function version is > 1 (bumped past the original 2026-09-10 19:47:00 UTC v1 deploy) and the downloaded deployed source contains ZERO `everyAgreeingId` OR-semantics dissent logic AND calls `reconcile_transcript_segments_atomic` (not raw .delete()+.insert()) — RECON-05/06 accuracy is now true of the code LIVE in prod, not just the repo (closes VERIFICATION missing #3 + the single failed truth)."
    - "Prod introspection confirms reconcile_transcript_segments_atomic exists SECURITY DEFINER with EXECUTE denied to anon/authenticated (only postgres/service_role retain it) — the orchestrator-corrected RPC-exposure gap from 37-REVIEW-FIX.md holds live."
    - "STATE.md decisions log + a 37-VERIFICATION.md closure note record that the review-fix deploy happened, so 'Phase 37 complete, shipped to production' is true of the REVIEWED code (closes VERIFICATION missing #4)."
  artifacts:
    - path: "supabase/migrations/20260910010000_create_reconcile_transcript_segments_atomic_rpc.sql"
      provides: "Atomic per-event delete+rebuild RPC (WR-02 fix); already committed — this plan only APPLIES it to prod"
      contains: "reconcile_transcript_segments_atomic"
    - path: ".planning/phases/37-transcript-reconciliation/37-VERIFICATION.md"
      provides: "Closure note recording the gap is resolved (deploy done, introspection captured)"
      contains: "37-06"
    - path: ".planning/STATE.md"
      provides: "Decisions-log entry recording the review-fix prod deploy"
      contains: "37-06"
  key_links:
    - from: "prod apply/deploy"
      to: "prod-ref guard vltmrnjsubfzrgrtdqey"
      via: "supabase projects list ● marker verified before + after every prod write"
      pattern: "vltmrnjsubfzrgrtdqey"
    - from: "deployed reconcile-transcripts/index.ts"
      to: "reconcile_transcript_segments_atomic RPC"
      via: "supabase.rpc(...) call in the write path (replaces raw delete+insert)"
      pattern: "reconcile_transcript_segments_atomic"
    - from: "deployed _shared/transcript-reconciler.ts"
      to: "AND-semantics dissent tracking"
      via: "dissenting_recording_ids threaded per-position (everyAgreeingId absent)"
      pattern: "dissenting_recording_ids"
  prohibitions:
    - "MUST NOT apply the migration or deploy the function without the linked-project ref confirmed to be vltmrnjsubfzrgrtdqey via `supabase projects list` ● marker immediately BEFORE AND AFTER the action (no .env exists in this checkout — use CLI linked-state, per 31-04/35-04/37-05 precedent)."
    - "MUST NOT deploy the function BEFORE the migration is applied — that opens a window where the deployed function calls an RPC that does not yet exist in prod (37-REVIEW-FIX.md deploy-order caution). Migration first, function second."
    - "MUST NOT run `supabase db push --linked` if any pending (Local-only) migration OTHER than 20260910010000 appears — halt and surface it; never apply an unreviewed migration to live customer data."
    - "MUST NOT modify any application/source code — the fixes are already committed (a3a5ef48/9a2c1884/75ac36fd/ca3c3b71/28eb0e15). This is a deploy-only closure."
    - "MUST NOT add a reconcile-transcripts-sweep cron — the apply-no-cron posture from 37-05 stands; the unset app.supabase_url/app.reconcile_secret GUCs would make a new cron fail every tick identically to event-resolution-sweep."
    - "MUST NOT create a new git branch — stay on v2.2-event-resolution (PROJECT.md Key Decision; milestone branch)."
---

<objective>
Close the single Phase 37 verification gap (37-VERIFICATION.md, score 6/7): production is still running the PRE-review-fix `reconcile-transcripts` (version 1, deployed 2026-09-10 19:47:00 UTC) and does NOT have migration 20260910010000 applied. The CR-01 (Critical — OR-semantics overstated consensus), WR-01 (non-deterministic fetch order), and WR-02 (non-atomic delete+insert) fixes are all committed and proven on TEST but were never deployed to prod. This plan applies the migration and redeploys the function under the milestone's guarded-apply discipline, then proves by direct prod introspection that the reviewed/fixed code is what is now live.

Purpose: make "Phase 37 complete, shipped to production" true of the REVIEWED code, not just the pre-review code — before any org is ever enabled for reconciliation (the mechanism is provably inert today: zero `event_match_decisions.decision='merge_applied'` rows in prod).
Output: migration 20260910010000 live on prod; reconcile-transcripts redeployed (version > 1) carrying CR-01/WR-01/WR-02; introspection proof; STATE.md + ROADMAP.md + 37-VERIFICATION.md closure records.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/admin/dev/brain/main/CLAUDE.md
@/Users/admin/dev/brain/main/supabase/CLAUDE.md
@/Users/admin/dev/brain/main/.planning/STATE.md
@/Users/admin/dev/brain/main/.planning/phases/37-transcript-reconciliation/37-VERIFICATION.md
@/Users/admin/dev/brain/main/.planning/phases/37-transcript-reconciliation/37-REVIEW-FIX.md
@/Users/admin/dev/brain/main/.planning/phases/37-transcript-reconciliation/37-05-SUMMARY.md

<deployment_facts>
<!-- Everything the executor needs inline — no codebase scavenger hunt. -->

- **Prod project ref:** `vltmrnjsubfzrgrtdqey` (callvault-ai). No `.env` in this checkout → prod-ref guard = the ● marker on this ref in `supabase projects list` (per 31-04/35-04/37-05 precedent).
- **TEST project ref:** `swjzxiddcrtaqixsfaac` (callvault-test) — migration 20260910010000 already applied here (confirmed by 37-VERIFICATION.md).
- **Migration to apply:** `supabase/migrations/20260910010000_create_reconcile_transcript_segments_atomic_rpc.sql` — creates `public.reconcile_transcript_segments_atomic(p_event_id UUID, p_organization_id UUID, p_rows JSONB)` SECURITY DEFINER, with explicit `REVOKE EXECUTE ... FROM PUBLIC/anon/authenticated` + `GRANT ... TO service_role`.
- **Function to redeploy:** `reconcile-transcripts` (folder `supabase/functions/reconcile-transcripts/`). Deploy command (mirrors 37-05 exactly): `supabase functions deploy reconcile-transcripts --use-api --no-verify-jwt` (shared-secret gated via X-Reconcile-Secret, NOT user-JWT — the --no-verify-jwt flag is required).
- **OR-semantics marker (must be ABSENT from deployed source):** the identifier `everyAgreeingId` (and the pattern `if (!everyAgreeingId.has(id)) dissenting.add(id)`). It appears ONLY in the pre-fix buggy code — 0 occurrences in the fixed repo files (verified), not even in comments. Its absence in the downloaded live source is the definitive proof the fix is deployed.
- **AND-semantics fix marker (must be PRESENT in deployed source):** `dissenting_recording_ids` (threaded from index.ts into buildReconciledSegment).
- **WR-02 fix marker (must be PRESENT in deployed index.ts):** `reconcile_transcript_segments_atomic` (the `supabase.rpc(...)` call replacing raw `.delete()`+`.insert()`).
- **WR-01 fix marker (deployed index.ts):** `.order('canonical_recording_id'...)` then `.order('chunk_index'...)` on the transcript_chunks fetch.
- **Original prod deploy to beat:** reconcile-transcripts VERSION 1, updated_at 2026-09-10 19:47:00 UTC. A successful redeploy bumps this to > 1 with a newer updated_at.
- **Fix commits (already in the branch):** a3a5ef48 (CR-01), 9a2c1884 (WR-01), 75ac36fd (WR-02), ca3c3b71, 28eb0e15 (REVOKE correction).
- **Inert-by-default fact:** prod `event_match_decisions` has zero `decision='merge_applied'` rows (only 2 `merge_proposed` from SAFE-06), so reconcile-transcripts' own eligibility gate has nothing to sweep — this deploy corrects code that is a genuine no-op today, before any org is enabled.
</deployment_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight gate — repo artifacts correct, fixes unit-tested, CLI linked to prod</name>
  <files></files>
  <read_first>
    - /Users/admin/dev/brain/main/.planning/phases/37-transcript-reconciliation/37-REVIEW-FIX.md (what CR-01/WR-01/WR-02 changed; the outstanding deploy action)
    - /Users/admin/dev/brain/main/.planning/phases/37-transcript-reconciliation/37-REVIEW.md (iteration-2 confirmation all three fixes are correct)
    - /Users/admin/dev/brain/main/supabase/functions/_shared/transcript-reconciler.ts (lines ~526-595 — the AND-semantics buildReconciledSegment)
    - /Users/admin/dev/brain/main/supabase/functions/reconcile-transcripts/index.ts (lines ~271-286 WR-01 ORDER BY, ~440-451 the atomic RPC call)
    - /Users/admin/dev/brain/main/supabase/migrations/20260910010000_create_reconcile_transcript_segments_atomic_rpc.sql (the REVOKE EXECUTE statements)
  </read_first>
  <action>
    Prove the deployable artifacts are correct in the repo BEFORE requesting prod authorization — no prod writes in this task.
    1. Grep `supabase/functions/_shared/transcript-reconciler.ts`: confirm `everyAgreeingId` has ZERO occurrences (OR-semantics gone) and `dissenting_recording_ids` is present (AND-semantics fix).
    2. Grep `supabase/functions/reconcile-transcripts/index.ts`: confirm `reconcile_transcript_segments_atomic` is called via `supabase.rpc(` (WR-02 fix), the `.order('canonical_recording_id'` + `.order('chunk_index'` ORDER BY is present (WR-01 fix), and there is NO raw `.from('reconciled_transcript_segments').delete()` / `.insert()` write path.
    3. Confirm `supabase/migrations/20260910010000_create_reconcile_transcript_segments_atomic_rpc.sql` exists and contains `REVOKE EXECUTE ... FROM anon` AND `REVOKE EXECUTE ... FROM authenticated` (grep both) plus `GRANT EXECUTE ... TO service_role`.
    4. Run the transcript-reconciler unit suite — must be all-green including the CR-01 regression test (`transcript-reconciler.test.ts` "CR-01 regression", lines ~379-409).
    5. Migration 20260910010000 is already confirmed TEST-applied per 37-VERIFICATION.md (`supabase migration list --linked` against swjzxiddcrtaqixsfaac showed it Local==Remote). Re-confirming on TEST is OPTIONAL; if you relink to TEST to check, you MUST relink back to prod afterward.
    6. Confirm the CLI is linked to PROD at task end: `supabase projects list` shows the ● marker on `vltmrnjsubfzrgrtdqey` (callvault-ai). This task performs NO prod mutation.
  </action>
  <verify>
    <automated>npx vitest run supabase/functions/_shared/__tests__/transcript-reconciler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "everyAgreeingId" supabase/functions/_shared/transcript-reconciler.ts` returns 0; `grep -c "dissenting_recording_ids" supabase/functions/_shared/transcript-reconciler.ts` returns > 0.
    - `supabase/functions/reconcile-transcripts/index.ts` contains `supabase.rpc('reconcile_transcript_segments_atomic'` and both `.order('canonical_recording_id'` and `.order('chunk_index'`; contains no raw reconciled_transcript_segments `.delete()`/`.insert()` write path.
    - Migration 20260910010000 contains `REVOKE EXECUTE ... FROM anon`, `REVOKE EXECUTE ... FROM authenticated`, and `GRANT EXECUTE ... TO service_role`.
    - The transcript-reconciler unit suite is all-green (29/29 incl. the CR-01 regression test).
    - `supabase projects list` shows the ● marker on `vltmrnjsubfzrgrtdqey` at task end; no prod mutation was performed.
  </acceptance_criteria>
  <done>Repo artifacts proven correct and unit-tested; CLI linked to prod; ready to request prod authorization.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Authorize production apply + redeploy (one-way door on live customer data)</name>
  <files></files>
  <read_first>
    - /Users/admin/dev/brain/main/.planning/phases/37-transcript-reconciliation/37-REVIEW-FIX.md ("Outstanding Human Action Required" — the exact apply + redeploy this authorizes)
    - /Users/admin/dev/brain/main/CLAUDE.md (Environment files DB-safety: migrations/DDL read prod ref vltmrnjsubfzrgrtdqey)
    - /Users/admin/dev/brain/main/.planning/phases/37-transcript-reconciliation/37-05-SUMMARY.md (the guarded-apply pattern this mirrors)
  </read_first>
  <decision>Authorize applying migration 20260910010000 (reconcile_transcript_segments_atomic RPC + REVOKE EXECUTE) and redeploying the reconcile-transcripts edge function (carrying CR-01/WR-01/WR-02) to the production Supabase project (vltmrnjsubfzrgrtdqey). This is a one-way door: the RPC + function redeploy land on live customer infrastructure; undoing them requires a new migration/redeploy.</decision>
  <context>
    The fixes are committed and proven on TEST (Task 1 + 37-REVIEW.md iteration 2 + 37-VERIFICATION.md). The change is additive (one new service-role-only RPC) and non-destructive (no existing table/column/FK altered; the function body is a corrected version of what is already live). The mechanism is provably inert in prod today — zero `event_match_decisions.decision='merge_applied'` rows — so this deploys a corrected no-op BEFORE any org is enabled, closing a known Critical correctness bug (CR-01) that would otherwise overstate transcript consensus the moment an org reaches merge_applied. Prod apply is gated here per the milestone-wide rule that every prod apply requires explicit Andrew approval, guarded by the prod-ref check.
  </context>
  <options>
    <option id="approve">
      <name>approve — apply migration 20260910010000 + redeploy reconcile-transcripts to prod (migration first)</name>
      <pros>Makes RECON-05/06 accuracy + WR-01/WR-02 fixes true of the code LIVE in prod; matches every prior v2.2 guarded-apply; closes the sole Phase 37 gap.</pros>
      <cons>Live change on the production project (additive/non-destructive; reversible only via a follow-up migration/redeploy).</cons>
    </option>
    <option id="hold">
      <name>hold — leave prod on the pre-fix v1 (accept the VERIFICATION override instead)</name>
      <pros>No prod change now; mechanism is inert either way today.</pros>
      <cons>Phase 37 stays "complete of the pre-review code only"; the known CR-01 Critical bug remains the version live in prod until a later deploy.</cons>
    </option>
  </options>
  <action>Halt and present the prod-apply authorization above. Record approve or hold before Task 3 runs; if hold, Task 3 does not execute. Under yolo/auto_advance this may be pre-resolved by Andrew outside the executor invocation, per prior-phase precedent (37-05, 33-03, 36-06 were all pre-resolved this way).</action>
  <verify>
    <automated>N/A — authorization checkpoint. Under yolo/auto_advance this may be pre-resolved by Andrew outside the executor invocation, per prior-phase precedent (see STATE.md).</automated>
  </verify>
  <acceptance_criteria>
    - An explicit approve/hold decision is recorded before Task 3 runs. If hold, Task 3 does not execute.
  </acceptance_criteria>
  <done>Prod-apply authorization decision recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Guarded prod apply (migration first, then function) + prod introspection + doc closure</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/phases/37-transcript-reconciliation/37-VERIFICATION.md</files>
  <read_first>
    - /Users/admin/dev/brain/main/CLAUDE.md (Environment files DB-safety — prod ref vltmrnjsubfzrgrtdqey; migrations read .env, prod-ref-guarded)
    - /Users/admin/dev/brain/main/supabase/CLAUDE.md (--use-api function deploy; Docker-not-running caveat)
    - /Users/admin/dev/brain/main/.planning/phases/37-transcript-reconciliation/37-05-SUMMARY.md (exact prod-apply sequence to mirror: `supabase projects list` ● guard, `supabase db push --linked`, `--no-verify-jwt` deploy, `supabase db query --linked` introspection — no psql/.env in this checkout)
    - /Users/admin/dev/brain/main/.planning/phases/37-transcript-reconciliation/37-VERIFICATION.md (the gap + the 4 "missing" items this task closes)
  </read_first>
  <action>
    Only after Task 2 = approve.

    **Prod-ref guard (BEFORE):** confirm `supabase projects list` shows the ● marker on `vltmrnjsubfzrgrtdqey` (callvault-ai). If not linked to prod, `supabase link --project-ref vltmrnjsubfzrgrtdqey` first, then re-confirm.

    **Pending-migration safety check:** run `supabase migration list --linked` (prod). Confirm the ONLY Local-only (pending, blank Remote) migration is `20260910010000`. If any OTHER pending migration appears, HALT and surface it — do not apply unreviewed migrations to live customer data.

    **Apply migration FIRST:** `supabase db push --linked`. Confirm it applies exactly `20260910010000_create_reconcile_transcript_segments_atomic_rpc.sql`.

    **THEN deploy the function:** `supabase functions deploy reconcile-transcripts --use-api --no-verify-jwt`. Confirm it reports ACTIVE.

    **Prod-ref guard (AFTER):** re-confirm `supabase projects list` ● marker is still on `vltmrnjsubfzrgrtdqey`.

    **Prod introspection (all four proofs):**
    1. `supabase migration list --linked` (prod) now shows `20260910010000` Local==Remote.
    2. `supabase functions list` shows reconcile-transcripts with VERSION > 1 and updated_at newer than 2026-09-10 19:47:00 UTC.
    3. RPC EXECUTE posture via `supabase db query --linked`: query `information_schema.routine_privileges` (and/or `has_function_privilege` + `pg_proc.prosecdef`) for `reconcile_transcript_segments_atomic` — confirm `prosecdef=true` (SECURITY DEFINER) and EXECUTE granted ONLY to postgres/service_role, NOT to anon or authenticated.
    4. Deployed-source proof: download the live function and grep the bundled source (see the <verify> command). Confirm `everyAgreeingId` has ZERO occurrences (OR-semantics gone) and `reconcile_transcript_segments_atomic` is present (WR-02 fix live). Restore the repo tree afterward via `git checkout -- supabase/functions/` (the deployed source == the committed source after redeploy, so restore is clean; confirm `git status` shows no lingering changes to functions/).

    **Doc closure (this closes VERIFICATION missing #4):**
    - Append a short closure note to `.planning/phases/37-transcript-reconciliation/37-VERIFICATION.md` (e.g. a `## Gap Closure (37-06)` section) recording: migration applied, function redeployed (version bump), the four introspection proofs, and that the sole failed truth (RECON-05/06 accuracy live) is now satisfied. Do NOT rewrite the original gap analysis — append.
    - Add a STATE.md `### Decisions` entry: `[Phase 37] 37-06 gap closure — applied migration 20260910010000 + redeployed reconcile-transcripts to prod (vltmrnjsubfzrgrtdqey) carrying CR-01/WR-01/WR-02; prod-ref guarded before+after; live version bumped past v1; deployed source confirmed free of the everyAgreeingId OR-semantics bug and calling reconcile_transcript_segments_atomic; RPC EXECUTE confirmed denied to anon/authenticated. Phase 37 now complete of the REVIEWED code.` Also update Current Position / Session Continuity to reference 37-06.
    - Update ROADMAP.md: flip the 37-06 gap-closure plan checkbox to `[x]`.

    **Do NOT:** modify any source/application code; regenerate `src/types/supabase.ts` (the new RPC is service-role-only with no frontend consumer, and the reconciled_transcript_segments table types are already present from prior plans — type regen is unnecessary scope); add any cron.
  </action>
  <verify>
    <automated>supabase functions download reconcile-transcripts --project-ref vltmrnjsubfzrgrtdqey && grep -rq "reconcile_transcript_segments_atomic" supabase/functions/reconcile-transcripts/ && ! grep -rq "everyAgreeingId" supabase/functions/reconcile-transcripts/; RC=$?; git checkout -- supabase/functions/ 2>/dev/null; exit $RC</automated>
  </verify>
  <acceptance_criteria>
    - `supabase projects list` ● marker confirmed on `vltmrnjsubfzrgrtdqey` BOTH before and after the apply+deploy.
    - The only pending migration before `db push` was `20260910010000`; after push, `supabase migration list --linked` (prod) shows it Local==Remote.
    - `supabase functions list` shows reconcile-transcripts VERSION > 1, ACTIVE, updated_at > 2026-09-10 19:47:00 UTC.
    - `reconcile_transcript_segments_atomic` on prod: `prosecdef=true`; EXECUTE denied to anon AND authenticated (only postgres/service_role retain it).
    - Downloaded live source: `everyAgreeingId` returns 0 occurrences; `reconcile_transcript_segments_atomic` present. Repo tree restored (`git status` clean for `supabase/functions/`).
    - 37-VERIFICATION.md has an appended `## Gap Closure (37-06)` note; STATE.md has the 37-06 decisions entry; ROADMAP.md 37-06 checkbox is `[x]`.
    - No source/application code changed; no `src/types/supabase.ts` regen; no cron added.
  </acceptance_criteria>
  <done>Migration 20260910010000 + the CR-01/WR-01/WR-02 fixes are live on prod, proven by direct introspection; Phase 37 is complete of the reviewed code; the sole verification gap is closed and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → production Supabase project | Highest-consequence boundary; a mis-linked CLI could mutate the wrong project (2026-05 prod-mutation incident precedent). No `.env` in this checkout → the guard is the `supabase projects list` ● marker. |
| new SECURITY DEFINER RPC → client roles (anon/authenticated) | If EXECUTE is not revoked, any client could call reconcile_transcript_segments_atomic directly with arbitrary event_id/org_id/rows and write fabricated reconciled-transcript content, bypassing the sweep's gating (the exact orchestrator-corrected finding in 37-REVIEW-FIX.md). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-09 | Tampering (wrong-target write) | prod apply/deploy | mitigate | Prod-ref guard: confirm `supabase projects list` ● == vltmrnjsubfzrgrtdqey immediately BEFORE and AFTER every prod write; authorization checkpoint (Task 2) precedes any prod mutation; pending-migration safety check refuses to apply any migration other than 20260910010000. |
| T-37-10 | Elevation of Privilege | reconcile_transcript_segments_atomic RPC | mitigate | Migration ships explicit `REVOKE EXECUTE ... FROM anon`/`FROM authenticated`; Task 3 post-apply introspection asserts EXECUTE is denied to both roles (only postgres/service_role retain it) — the orchestrator-corrected gap in 37-REVIEW-FIX.md, confirmed live not assumed. |
| T-37-07 | Tampering (silent re-embedding) | deployed reconcile-transcripts source | mitigate | The redeploy introduces no transcript_chunks/embedding write (RECON-07 held pre- and post-fix); the downloaded live source is grep-clean for upsert/embedded_at/embed-chunks/process-embeddings, and the mechanism is inert (zero eligible events) regardless. |
| T-37-SC | Tampering (supply chain) | package installs | accept | No packages installed this plan — deploy-only closure of already-committed code; nothing to audit. |
</threat_model>

<verification>
- Prod-ref guard (vltmrnjsubfzrgrtdqey ● marker) confirmed before + after every prod write.
- Prod `supabase migration list --linked`: 20260910010000 Local==Remote (and no other migration applied).
- Prod `supabase functions list`: reconcile-transcripts VERSION > 1, ACTIVE, updated_at > 2026-09-10 19:47:00 UTC.
- Prod introspection: reconcile_transcript_segments_atomic SECURITY DEFINER, EXECUTE denied to anon/authenticated.
- Downloaded live source: `everyAgreeingId` absent, `reconcile_transcript_segments_atomic` present; repo tree restored clean.
- STATE.md + ROADMAP.md + 37-VERIFICATION.md closure records written.
</verification>

<success_criteria>
- The sole Phase 37 verification gap (37-VERIFICATION.md truth #7 — RECON-05/06 accuracy + WR-01/WR-02 fixes live in prod) is closed: the reviewed/fixed code is confirmed LIVE in production by direct introspection, not assumed.
- All four VERIFICATION "missing" items are satisfied (guarded migration apply, migration-first redeploy, post-deploy version+source introspection, STATE/VERIFICATION closure record).
- "Phase 37 complete, shipped to production" is now true of the reviewed code. No source code changed; no cron added; mechanism remains inert-by-default.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- Production apply of migration `20260910010000_create_reconcile_transcript_segments_atomic_rpc.sql` (already-committed file; this plan makes it live on prod).
- Production redeploy of the `reconcile-transcripts` edge function (version > 1) carrying the CR-01/WR-01/WR-02 fixes (already-committed source).
- `.planning/phases/37-transcript-reconciliation/37-VERIFICATION.md` — appended `## Gap Closure (37-06)` note.
- `.planning/STATE.md` — 37-06 decisions-log entry + Current Position/Session Continuity update.
- `.planning/ROADMAP.md` — 37-06 gap-closure plan checkbox flipped to `[x]`.
- No new source/application code files (deploy-only closure).

<output>
Create `.planning/phases/37-transcript-reconciliation/37-06-SUMMARY.md` when done.
</output>
