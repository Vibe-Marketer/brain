---
phase: 37-transcript-reconciliation
plan: 04
type: execute
wave: 4
depends_on: [37-01, 37-03]
files_modified:
  - src/services/reconciledTranscript.service.ts
  - src/hooks/useReconciledTranscript.ts
  - src/lib/query-config.ts
  - src/components/call-detail/CallReconciledTranscriptTab.tsx
  - src/components/call-detail/ReconciledSegmentProvenanceBadge.tsx
  - src/components/CallDetailDialog.tsx
  - src/components/call-detail/CallDetailHeader.tsx
autonomous: true
requirements: [RECON-05, RECON-06]
user_setup: []

must_haves:
  truths:
    - "A new read-only 'Reconciled' tab appears inside CallDetailDialog (alongside Overview/Transcript/Invitees/Participants), never replacing the per-recording transcript views"
    - "The Reconciled tab renders only when the open call's event has 2+ recordings that passed metadata-tier resolution; it is entirely absent (not disabled) otherwise"
    - "Multi-source/consensus segments show a subtle ReconciledSegmentProvenanceBadge (RiGitMergeLine, text-muted-foreground); single-source segments render with zero badge"
    - "The provenance popover reads 'Confirmed by N recordings' and lists the agreeing source recording labels"
    - "Empty (reconciliation pending), loading (Skeleton), and error states render per the UI-SPEC copywriting contract"
    - "CallDetailHeader shows an outline 'N recordings' Badge when the event has 2+ resolved recordings"
    - "The reconciled read path is user-JWT-authenticated and RLS-gated — a user only sees segments for events whose recordings they can already access"
  artifacts:
    - path: "src/components/call-detail/CallReconciledTranscriptTab.tsx"
      provides: "Read-only reconciled-transcript tab"
      min_lines: 40
    - path: "src/components/call-detail/ReconciledSegmentProvenanceBadge.tsx"
      provides: "Per-segment consensus provenance popover badge"
    - path: "src/hooks/useReconciledTranscript.ts"
      provides: "TanStack Query hook over the reconciled segments read"
    - path: "src/services/reconciledTranscript.service.ts"
      provides: "Pure async fetch of reconciled_transcript_segments by event_id"
  key_links:
    - from: "CallReconciledTranscriptTab.tsx"
      to: "useReconciledTranscript"
      via: "hook call"
      pattern: "useReconciledTranscript"
    - from: "reconciledTranscript.service.ts"
      to: "reconciled_transcript_segments"
      via: "supabase select by event_id (RLS-gated)"
      pattern: "reconciled_transcript_segments"
    - from: "CallDetailDialog.tsx"
      to: "CallReconciledTranscriptTab"
      via: "new Tabs SelectionButton + TabsContent"
      pattern: "CallReconciledTranscriptTab"
  prohibitions:
    - "A single-source segment must never render a consensus/provenance badge (absence of badge is the single-source signal)"
    - "The reconciled view must never modify, re-request regeneration of, or write back to any transcript source — it is strictly read-only"
---

<objective>
Surface the reconciled transcript as a read-only "Reconciled" tab inside the existing CallDetailDialog, with subtle per-segment provenance badges on consensus segments only, per the approved 37-UI-SPEC.md. Existing per-recording transcript views are untouched (non-destructiveness extends to the UI).

Purpose: Gives the user the event-level canonical transcript with visible, opt-in-to-notice provenance, without adding a new route or any regenerate CTA.

Output: service + hook + query key + tab component + provenance badge + dialog/header wiring.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-transcript-reconciliation/37-UI-SPEC.md
@.planning/phases/37-transcript-reconciliation/37-PATTERNS.md
@.planning/phases/37-transcript-reconciliation/37-01-SUMMARY.md
@src/components/shared/IdentityEvidenceBadge.tsx
@src/hooks/useIdentityEvidence.ts
@src/services/identity-evidence.service.ts
@src/components/call-detail/CallTranscriptTab.tsx

<interfaces>
Read-path analogs (live, mirror exactly):
- src/services/identity-evidence.service.ts — pure async fn over supabase client; throw new Error on error; `?? []` fallback. Reconciled service does `.from('reconciled_transcript_segments').select(...).eq('event_id', eventId).order('start_time')` (direct RLS-gated select, not an RPC).
- src/hooks/useIdentityEvidence.ts — useQuery + queryKeys.<domain>.detail(id) + typed result object. Add queryKeys.reconciledTranscript.detail(eventId) to src/lib/query-config.ts (queryKeys object at line 6; identityEvidence factory at line 227 — mirror its shape, DO NOT reinvent).
- src/components/shared/IdentityEvidenceBadge.tsx — Popover/PopoverTrigger/PopoverContent (w-64 p-3), h-5 w-5 icon-only trigger, Skeleton loading, text-muted-foreground. Copy verbatim; swap RiShieldCheckLine → RiGitMergeLine; popover copy "Confirmed by N recordings" + agreeing-source list. agreeing_recording_ids is already on the segment row, so no separate lazy RPC is needed.
- src/components/call-detail/CallTranscriptTab.tsx — memo + TabsContent + ScrollArea wrapper; reconciled tab is simpler (read-only, no editing state).

Dialog wiring (src/components/CallDetailDialog.tsx): activeTab useState union at line 65 (add "reconciled"); Tabs value/onValueChange at 561-562; SelectionButton row at 567-590 (add a new SelectionButton icon RiGitMergeLine label "Reconciled"); render <CallReconciledTranscriptTab/> inside <Tabs>. Conditional-render the trigger+content only when the event qualifies (2+ resolved recordings) — this file currently renders all tabs unconditionally, so the conditional is a genuinely new addition.

Copywriting contract (37-UI-SPEC.md): empty heading "Reconciliation pending"; empty body per spec; error "Couldn't load the reconciled transcript. Each recording's own transcript is still available in its tab above — try again in a moment."; tab label "Reconciled" / heading "Reconciled Transcript". No vibe-orange on badges (text-muted-foreground only).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Service + hook + query key for reconciled segments</name>
  <read_first>
    - src/services/identity-evidence.service.ts
    - src/hooks/useIdentityEvidence.ts
    - src/lib/query-config.ts (queryKeys object at line 6; identityEvidence factory at 227)
    - src/types/supabase.ts (reconciled_transcript_segments row type from Plan 01)
  </read_first>
  <action>
    Add queryKeys.reconciledTranscript.detail(eventId) to src/lib/query-config.ts mirroring the identityEvidence factory. Create reconciledTranscript.service.ts: getReconciledTranscript(eventId) → direct RLS-gated `.from('reconciled_transcript_segments').select('id, event_id, segment_text, start_time, end_time, source_recording_ids, agreeing_recording_ids').eq('event_id', eventId).order('start_time', { ascending: true })`; throw new Error(`Failed to fetch reconciled transcript: ${error.message}`) on error; `?? []` fallback; export the row type. Create useReconciledTranscript(eventId, enabled) using useQuery + the new query key + staleTime 5*60*1000 + `enabled: enabled && !!eventId`; return { data, isLoading, error }.
  </action>
  <verify>
    <automated>rtk tsc 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - query key added via the existing factory (not an inline array).
    - Service is a pure async fn with the throw + `?? []` conventions; read is RLS-gated (direct select, relies on Plan 01 SELECT policy).
    - Hook mirrors useIdentityEvidence; type-check clean (no new baseline errors).
  </acceptance_criteria>
  <done>Reconciled segments are fetchable via a typed hook, RLS-gated by event_id.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CallReconciledTranscriptTab + ReconciledSegmentProvenanceBadge</name>
  <read_first>
    - src/components/shared/IdentityEvidenceBadge.tsx (full popover pattern)
    - src/components/call-detail/CallTranscriptTab.tsx (memo + TabsContent + ScrollArea)
    - .planning/phases/37-transcript-reconciliation/37-UI-SPEC.md (Visual Hierarchy, Copywriting Contract, UI Considerations, Color, Typography)
  </read_first>
  <action>
    Create ReconciledSegmentProvenanceBadge.tsx by copying IdentityEvidenceBadge's popover-trigger structure verbatim; swap RiShieldCheckLine → RiGitMergeLine; render only when agreeing_recording_ids.length >= 2; popover heading "Confirmed by {N} recordings", body lists the agreeing source recording labels (reuse existing recording-title formatting). text-muted-foreground trigger, never vibe-orange. Create CallReconciledTranscriptTab.tsx as a memo'd TabsContent(value="reconciled"): calls useReconciledTranscript(eventId, isOpen); renders section heading "Reconciled Transcript" (font-montserrat font-extrabold uppercase tracking-wide text-sm per Typography); maps segments in a ScrollArea (text-sm body, same wrapping as CallTranscriptTab); renders ReconciledSegmentProvenanceBadge inline at end of each multi-source segment (single-source segments get NO badge); loading → Skeleton; empty (no rows) → "Reconciliation pending" heading + spec body; error → spec error copy. Long-text/overflow are inherited-behavior backstops — verify visually at human-check.
  </action>
  <verify>
    <automated>grep -q "RiGitMergeLine" src/components/call-detail/ReconciledSegmentProvenanceBadge.tsx && grep -q "agreeing_recording_ids.length" src/components/call-detail/ReconciledSegmentProvenanceBadge.tsx && grep -q "Reconciliation pending" src/components/call-detail/CallReconciledTranscriptTab.tsx && rtk tsc 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Badge renders only for 2+ agreeing sources; single-source segments render zero badge (grep + code path).
    - Empty/loading/error states match the UI-SPEC copywriting contract exactly.
    - No vibe-orange on the badge; icon is RiGitMergeLine; type-check clean.
  </acceptance_criteria>
  <done>The read-only reconciled tab renders segments with correct provenance-badge conditionality and all state copy.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire the tab into CallDetailDialog + CallDetailHeader "N recordings" badge</name>
  <read_first>
    - src/components/CallDetailDialog.tsx (activeTab union line 65; Tabs 561-562; SelectionButton row 567-590)
    - src/components/call-detail/CallDetailHeader.tsx (existing badge patterns, e.g. RoutingTraceBadge placement)
    - .planning/phases/37-transcript-reconciliation/37-UI-SPEC.md (Architecture Notes: conditional rendering + entry-point affordance)
  </read_first>
  <action>
    In CallDetailDialog.tsx: extend the activeTab useState union with "reconciled"; add a new SelectionButton (icon RiGitMergeLine, label "Reconciled") and a <CallReconciledTranscriptTab eventId={...} isOpen={activeTab === "reconciled"} /> inside the existing <Tabs>. Conditionally render BOTH the trigger and the content only when the open call's event qualifies (event_id present AND the event has 2+ recordings that passed metadata-tier resolution) — derive this from existing call/event data already loaded by the dialog; if that fact isn't already available client-side, gate on presence of reconciled rows / event recording count via the smallest additional read, but never render a shown-then-empty disabled tab. In CallDetailHeader.tsx: add a small outline Badge reading "{N} recordings" (same visual weight as RoutingTraceBadge) when the event has 2+ resolved recordings — informational, not a CTA.
  </action>
  <verify>
    <automated>grep -q "CallReconciledTranscriptTab" src/components/CallDetailDialog.tsx && grep -q "reconciled" src/components/CallDetailDialog.tsx && rtk tsc 2>&1 | tail -5 && rtk vitest run 2>&1 | tail -8</automated>
    <human-check>End-of-phase (human_verify_mode=end-of-phase): on a real multi-recording event, open its CallDetailDialog and confirm the Reconciled tab appears, consensus segments show the RiGitMergeLine badge (popover "Confirmed by N recordings") while single-source segments show none, per-recording tabs are unaffected, and long-text/overflow wrap acceptably (the two UI-SPEC backstops).</human-check>
  </verify>
  <acceptance_criteria>
    - The Reconciled tab is wired inside the existing Tabs block; per-recording tabs unchanged.
    - Tab trigger+content are absent entirely (not disabled) when the event has <2 resolved recordings.
    - CallDetailHeader shows the "N recordings" outline badge only for 2+ resolved recordings; type-check + unit suite clean.
  </acceptance_criteria>
  <done>Users reach the read-only reconciled transcript via a conditional tab, with a header affordance pointing to it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (user JWT) → reconciled_transcript_segments | Client read crosses RLS (Plan 01 SELECT policy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Information Disclosure / Elevation | Reconciled tab read path | mitigate | Read is a direct RLS-gated select; the client cannot bypass the Plan-01 SECURITY DEFINER SELECT policy. Cross-org isolation already proven in Plan 01's rls-regression block. |
| T-37-08 | Transparency | Single-source shown as consensus | mitigate | Badge renders only for agreeing_recording_ids.length >= 2; enforced in code and asserted by grep in Task 2 verify. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No installs — all shadcn primitives (tabs/popover/badge/scroll-area/skeleton) already present. |
</threat_model>

<artifacts>
## Artifacts this plan produces
- Service: `src/services/reconciledTranscript.service.ts` → `getReconciledTranscript(eventId)` + row type
- Hook: `src/hooks/useReconciledTranscript.ts` → `useReconciledTranscript(eventId, enabled)`
- Query key: `queryKeys.reconciledTranscript.detail(eventId)` in `src/lib/query-config.ts`
- Component: `src/components/call-detail/CallReconciledTranscriptTab.tsx`
- Component: `src/components/call-detail/ReconciledSegmentProvenanceBadge.tsx` (icon RiGitMergeLine)
- Modified: `src/components/CallDetailDialog.tsx` (activeTab "reconciled" + SelectionButton + TabsContent, conditional)
- Modified: `src/components/call-detail/CallDetailHeader.tsx` ("N recordings" outline Badge)
</artifacts>

<verification>
- `rtk tsc` clean (no new baseline errors); `rtk vitest run` green.
- Badge conditionality (>=2 agreeing) and single-source-no-badge enforced in code.
- Empty/loading/error copy matches 37-UI-SPEC.md.
</verification>

<success_criteria>
- Read-only reconciled tab live behind a conditional trigger; per-recording views untouched.
- Provenance badges appear only on consensus segments; header affordance present.
</success_criteria>

<output>
Create `.planning/phases/37-transcript-reconciliation/37-04-SUMMARY.md` when done.
</output>
