---
phase: 37-transcript-reconciliation
plan: 03
type: execute
wave: 3
depends_on: [37-01, 37-02]
files_modified:
  - supabase/functions/reconcile-transcripts/index.ts
  - supabase/functions/reconcile-transcripts/__tests__/reconcile-transcripts.integration.test.ts
autonomous: true
requirements: [RECON-01, RECON-02, RECON-03, RECON-04, RECON-07]
user_setup: []

must_haves:
  truths:
    - "reconcile-transcripts rejects any request missing/mismatching X-Reconcile-Secret with 401 before any DB work"
    - "The sweep gates on event_match_decisions (decision value locked in Plan 01) — NOT merely recordings.event_id IS NOT NULL — so raw-event_id-only recordings are never reconciled"
    - "Recordings are bucketed by (event_id, organization_id) before any chunk pairing; cross-org data is never paired"
    - "The entity lexicon is read scoped per-workspace/org (never a global cross-org aggregate) and passed into the pure module"
    - "Each event is reconciled by full DELETE then INSERT (never upsert/onConflict) into reconciled_transcript_segments"
    - "transcript_chunks rows are never mutated, and transcript_chunks.embedded_at/embedding are never written, and no embedding-pipeline function is ever called"
    - "The function is Zod-validated forward-only ({ mode:'forward', since? }) mirroring resolve-speakers"
  artifacts:
    - path: "supabase/functions/reconcile-transcripts/index.ts"
      provides: "Forward-only reconciliation sweep edge function"
      contains: "reconciled_transcript_segments"
    - path: "supabase/functions/reconcile-transcripts/__tests__/reconcile-transcripts.integration.test.ts"
      provides: "Adversarial synthetic-fixture integration tests (RECON-04/07 negative, gating, secret gate)"
      contains: "embedded_at"
  key_links:
    - from: "reconcile-transcripts/index.ts"
      to: "transcript-reconciler.ts"
      via: "delegates all scoring to the pure module"
      pattern: "transcript-reconciler"
    - from: "reconcile-transcripts/index.ts"
      to: "event_match_decisions"
      via: "gating join before pairing"
      pattern: "event_match_decisions"
    - from: "reconcile-transcripts/index.ts"
      to: "reconciled_transcript_segments"
      via: "delete-then-insert per event"
      pattern: "\\.delete\\(\\)"
  prohibitions:
    - "The reconciliation write path must never mutate, overwrite, or delete any source transcript_chunks row"
    - "The reconciliation write path must never write transcript_chunks.embedded_at/embedding nor invoke any embedding-pipeline function (no silent re-embed)"
    - "The sweep must never pair or reconcile recordings across organization boundaries"
---

<objective>
Wire the pure reconciler into a forward-only `reconcile-transcripts` edge function that gates correctly (event_match_decisions, not raw event_id), buckets same-org-only, reads a per-workspace entity lexicon, and persists via full delete+rebuild — proven on TEST with adversarial synthetic fixtures.

Purpose: This is the tracer's backend end-to-end completion — real chunk rows in, real reconciled_transcript_segments rows out on TEST, with the two negative guarantees (no source mutation, no re-embed) proven before the UI expands on top of it.

Output: The edge function + its integration test suite, verified against TEST (deploy-deferred `deno run` pattern from Phase 35).
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-transcript-reconciliation/37-RESEARCH.md
@.planning/phases/37-transcript-reconciliation/37-PATTERNS.md
@.planning/phases/37-transcript-reconciliation/37-01-SUMMARY.md
@.planning/phases/37-transcript-reconciliation/37-02-SUMMARY.md
@supabase/functions/resolve-speakers/index.ts
@supabase/CLAUDE.md

<interfaces>
resolve-speakers/index.ts is the exact structural analog (live). Reuse verbatim:
- X-Reconcile-Secret header check vs Deno.env RECONCILE_SECRET, BEFORE any DB work/body parse (returns 401).
- Zod requestSchema { mode: z.literal('forward'), since: z.string().datetime().optional() }; DEFAULT_CUTOVER fallback.
- recordingsByEvent / recordingsByOrg bucketing (SAFE-04) before pairing.
- try/catch whole handler; console.error('[reconcile-transcripts] ...') prefix; error instanceof Error ? .message : 'Unknown error'; typed summary response.

DEVIATION 1 (RESEARCH Pitfall 2): resolve-speakers queries `.from('recordings').select('id, event_id, recording_start_time, created_at').not('event_id','is',null)` ONLY. reconcile-transcripts MUST additionally require the recordings' event to have event_match_decisions rows with the decision value locked in Plan 01 (e.g. 'merge_applied') — add the join/filter; do NOT copy the resolve-speakers query verbatim.

DEVIATION 2 (RESEARCH Pitfall 5): resolve-speakers writes via `.upsert(..., { onConflict: ... })`. reconcile-transcripts MUST instead, per event: `.from('reconciled_transcript_segments').delete().eq('event_id', eventId)` then `.insert(freshRows)`. Never upsert. On delete error: fail closed, skip that event, log, continue the sweep.

Pure module (Plan 02): alignChunksToTimeline, tokenizeAndAlignText, resolveTokenDisagreement, buildReconciledSegment — the edge function contains ZERO scoring logic.

Entity lexicon (RESEARCH Pattern 4): per-org aggregate over transcript_chunks.entities (people/companies/products), fetched once per org bucket before the per-event loop, passed as a Set into resolveTokenDisagreement; handle NULL/missing keys defensively (fail closed to empty lexicon, never error the sweep).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sweep skeleton — secret gate, forward-only contract, event_match_decisions gating, same-org bucketing, per-workspace lexicon read</name>
  <read_first>
    - supabase/functions/resolve-speakers/index.ts (full — doc-comment, secret gate lines ~130-141, zod ~77-83/144-153, query ~175-180, bucketing ~197-269)
    - .planning/phases/37-transcript-reconciliation/37-RESEARCH.md (Pitfalls 2 & 3, Pattern 4 entity lexicon; Security Domain shared-secret + same-org rows)
    - .planning/phases/37-transcript-reconciliation/37-01-SUMMARY.md (locked gating condition + exact event_match_decisions.decision value)
  </read_first>
  <action>
    Create reconcile-transcripts/index.ts with the resolve-speakers doc-comment shape (adjusted for reconciliation's gating/write rules). Implement: CORS/OPTIONS; X-Reconcile-Secret gate vs RECONCILE_SECRET before any DB work; Zod forward-only body + DEFAULT_CUTOVER; service-role read of recordings resolved to events, filtered by the Plan-01-locked gating condition (join/filter on event_match_decisions with the locked decision value — implement per Plan 01 option a/b; NOT event_id-only); recordingsByEvent then recordingsByOrg bucketing before any pairing (SAFE-04); fetch transcript_chunks for the bucketed recordings; fetch the per-org entity lexicon once per org bucket (defensive NULL handling → empty Set). Do not write anything yet — return a summary counting eventsScanned/bucketsScanned/chunksScanned. Keep zero scoring logic in this file (delegation comes in Task 2).
  </action>
  <verify>
    <automated>grep -q "event_match_decisions" supabase/functions/reconcile-transcripts/index.ts && grep -q "X-Reconcile-Secret" supabase/functions/reconcile-transcripts/index.ts && ! grep -q "\.not('event_id', 'is', null')\s*$" supabase/functions/reconcile-transcripts/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Secret gate precedes all DB work; forward-only Zod contract present.
    - Gating filters on event_match_decisions with the locked decision value (grep-confirmed), not raw event_id alone.
    - Same-org bucketing precedes any pairing; entity lexicon fetched per-org, not globally.
  </acceptance_criteria>
  <done>The sweep enumerates eligible, same-org-bucketed events with their chunks + per-org lexicon, no writes yet.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delegate to pure module + full delete+rebuild persistence</name>
  <read_first>
    - supabase/functions/_shared/transcript-reconciler.ts (Plan 02 exports)
    - supabase/functions/resolve-speakers/index.ts (delegation discipline ~341-349; upsert block ~362-380 — the pattern to REPLACE)
    - .planning/phases/37-transcript-reconciliation/37-RESEARCH.md (Pitfall 5)
  </read_first>
  <action>
    Per org bucket, per event: call alignChunksToTimeline → tokenizeAndAlignText → resolveTokenDisagreement (passing the per-org lexicon Set) → buildReconciledSegment to produce the fresh reconciled rows (segment_text, sorted source_recording_ids/agreeing_recording_ids, coverage marker, interval bounds, organization_id, signals). Then persist via DELETE-then-INSERT: `.from('reconciled_transcript_segments').delete().eq('event_id', eventId)` and, if delete succeeded, `.insert(freshRows)`; on delete error fail closed (skip event, log, continue). NEVER upsert/onConflict. NEVER touch transcript_chunks (read-only) and NEVER write embedded_at/embedding or call any embedding function. Accumulate a typed summary (segmentsWritten, eventsReconciled, errors) and return it. Full try/catch handler with the console.error('[reconcile-transcripts] ...') prefix.
  </action>
  <verify>
    <automated>grep -q "\.delete()" supabase/functions/reconcile-transcripts/index.ts && ! grep -Eq "\.upsert\(|onConflict|embedded_at|process-embeddings|embed-chunks" supabase/functions/reconcile-transcripts/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Write path is delete-then-insert per event; zero upsert/onConflict.
    - Zero references to transcript_chunks writes, embedded_at, or embedding-pipeline functions (grep-clean).
    - All scoring delegated to the pure module; edge function body carries no vote logic.
  </acceptance_criteria>
  <done>The sweep produces and persists reconciled_transcript_segments via full delete+rebuild, non-destructively.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Adversarial synthetic-fixture integration tests on TEST (RECON-04/07 negative, gating, secret gate)</name>
  <read_first>
    - supabase/CLAUDE.md (triple-guarded test:integration TEST-project discipline)
    - .planning/phases/37-transcript-reconciliation/37-RESEARCH.md (Pitfall 1 — no real disagreeing data exists; seed synthetic near-miss fixtures; deploy-deferred deno-run pattern from Phase 35)
    - Phase 35's resolve-speakers deploy-deferred integration test (spawn index.ts under `deno run` against TEST) as the analog
  </read_first>
  <behavior>
    - Secret gate: request without valid X-Reconcile-Secret → 401, zero DB effect.
    - Gating: an event whose recordings share only a raw event_id (no event_match_decisions merge_applied row) produces zero reconciled rows.
    - RECON-04 (negative): capture a source transcript_chunks row's content hash before the sweep; assert byte-unchanged after.
    - RECON-07 (negative): assert no transcript_chunks.embedded_at value changes across the sweep, and grep the function source for zero embedding-pipeline calls.
    - Happy path: 2 synthetic recordings on one synthetic event with deliberately near-miss text ("ChatGPT"/"ChatGBT", punctuation variant, one dropped word) → reconciled rows written; single-source interval marked single_source, overlapping interval marked consensus with both recording ids in agreeing_recording_ids.
  </behavior>
  <action>
    Seed synthetic fixtures (2+ recordings, one event with event_match_decisions merge_applied, transcript_chunks with adversarial near-miss text, recording_start_time set) via the service-role client on TEST, mirroring Phase 33/35's canonical_recording_id-keyed seeding. Spawn reconcile-transcripts/index.ts under `deno run --allow-net --allow-env` pointed at TEST (no Cloud deploy). Assert the behaviors above. Teardown: delete workspace_entries before recordings, check .error on every cleanup call (protect_recording_delete trigger bug). Assertions must prove real behavior (negative/adversarial), not ceremony.
  </action>
  <verify>
    <automated>npm run test:integration -- reconcile-transcripts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Secret-gate, gating-exclusion, RECON-04 no-mutation, and RECON-07 no-re-embed assertions all present and green on TEST.
    - Happy-path proves single_source vs consensus marking on synthetic near-miss text.
    - No orphaned fixtures after teardown.
  </acceptance_criteria>
  <done>Backend tracer proven end-to-end on TEST: eligible events reconcile, source data untouched, no re-embed, cross-org never paired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron/service caller → reconcile-transcripts | Untrusted invocation; gated by X-Reconcile-Secret |
| edge function (service_role) → transcript_chunks | Read-only; must never write |
| edge function → reconciled_transcript_segments | Trusted write (delete+rebuild) |
| per-org bucket → entity lexicon | Must stay scoped per-org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03 | Spoofing | reconcile-transcripts invocation | mitigate | X-Reconcile-Secret checked before any DB work/body parse; proven by an integration test asserting 401 + zero DB effect. |
| T-37-02 | Information Disclosure | entity-lexicon cross-org leak | mitigate | Lexicon fetched scoped to organization_id per bucket, never a global aggregate; same-org bucketing precedes pairing (SAFE-04). |
| T-37-06 | Tampering | source transcript_chunks mutation | mitigate | transcript_chunks referenced read-only; RECON-04 negative integration test asserts source rows byte-unchanged. |
| T-37-07 | Tampering / DoS(cost) | silent re-embedding | mitigate | Zero writes to embedded_at/embedding, zero embedding-pipeline calls; grep gate + RECON-07 negative integration test. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No installs — fastest-levenshtein already present/audited. |
</threat_model>

<artifacts>
## Artifacts this plan produces
- Edge function: `supabase/functions/reconcile-transcripts/index.ts` (env: RECONCILE_SECRET; header: X-Reconcile-Secret; contract: { mode:'forward', since? })
- Integration test: `supabase/functions/reconcile-transcripts/__tests__/reconcile-transcripts.integration.test.ts`
- Response summary shape: { eventsScanned, bucketsScanned, chunksScanned, eventsReconciled, segmentsWritten, errors }
</artifacts>

<verification>
- `npm run test:integration -- reconcile-transcripts` green on TEST.
- grep confirms event_match_decisions gating, delete-then-insert (no upsert), and zero embedded_at/embedding writes.
- Secret gate + gating-exclusion + RECON-04 + RECON-07 negative assertions all present.
</verification>

<success_criteria>
- Forward-only sweep reconciles eligible same-org events into reconciled_transcript_segments via delete+rebuild.
- Source transcripts untouched; no re-embed; cross-org never paired — all proven adversarially on TEST.
</success_criteria>

<output>
Create `.planning/phases/37-transcript-reconciliation/37-03-SUMMARY.md` when done.
</output>
