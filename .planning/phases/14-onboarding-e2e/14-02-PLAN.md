---
phase: 14-onboarding-e2e
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified: []
autonomous: false
requirements: [ONBOARD-01, ONBOARD-02, ONBOARD-03]

must_haves:
  truths:
    - "Full onboarding flow verified visually via dev-browser"
    - "OnboardingModal renders on first visit for a new-like user"
    - "Connect buttons open new tabs without closing wizard"
    - "Wizard completion lands user at / with 4-pane layout"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete onboarding E2E flow using dev-browser — from login through wizard to default workspace.

Purpose: Phase 14 is a verification phase. Code fixes are meaningless without confirming the flow actually works end-to-end in a real browser.

Output: Visual confirmation that all three ONBOARD requirements are satisfied.
</objective>

<execution_context>
@/Users/Naegele/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Naegele/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-onboarding-e2e/14-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify onboarding E2E flow</name>
  <read_first>
    - src/components/Layout.tsx
    - src/components/onboarding/OnboardingModal.tsx
    - src/components/ProtectedRoute.tsx
  </read_first>
  <what-built>
    Complete onboarding flow fixes from Plan 14-01:
    - ProtectedRoute simplified to auth-only guard (no wizard/transcript redirects)
    - OnboardingModal connect buttons open new tabs via window.open
    - Post-completion navigation to / (default workspace)
  </what-built>
  <how-to-verify>
    Use dev-browser to verify these scenarios at http://localhost:3001 (or https://callvault.vercel.app if localhost has OAuth issues):

    **ONBOARD-01 — Sign up lands in wizard:**
    1. Open login page
    2. Verify Sign Up tab is accessible with email/password and Google OAuth options
    3. For an existing test user with onboarding_completed=false, verify the OnboardingModal appears on login
    4. Screenshot the Welcome step (Step 0) of the wizard

    **ONBOARD-02 — Connect sources work:**
    1. Click "Get Started" to advance to Step 1 (Connect your first source)
    2. Screenshot Step 1 showing Fathom, Zoom, and Upload cards
    3. Click "Connect Fathom" — verify a new tab opens (not a navigation in the same tab)
    4. Verify the wizard is still open in the original tab after clicking connect
    5. Click "I'll do this later" to advance to Step 2

    **ONBOARD-03 — Post-onboarding landing:**
    1. Navigate through Step 2 (How It Works) and Step 3 (You're all set)
    2. Click "Go to my calls" on the final step
    3. Verify the URL is / and the 4-pane layout (TranscriptsNew) is rendered
    4. Screenshot the final landing state
  </how-to-verify>
  <acceptance_criteria>
    - OnboardingModal appears for users with onboarding_completed=false
    - All 4 wizard steps render correctly with proper animations
    - Connect buttons open new tabs without closing the wizard
    - Completing the wizard navigates to / with the 4-pane layout visible
  </acceptance_criteria>
  <resume-signal>Type "approved" if the flow works, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
Visual verification via dev-browser confirms all three ONBOARD requirements are met end-to-end.
</verification>

<success_criteria>
- User approves the onboarding flow after visual verification
- All three ONBOARD requirements confirmed working
</success_criteria>

<output>
After completion, create `.planning/phases/14-onboarding-e2e/14-02-SUMMARY.md`
</output>
