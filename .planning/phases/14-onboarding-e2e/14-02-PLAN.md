---
phase: 14-onboarding-e2e
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified: []
autonomous: true
requirements: [ONBOARD-01, ONBOARD-02, ONBOARD-03]

must_haves:
  truths:
    - "Full onboarding flow verified visually via dev-browser"
    - "Sign-up page presents all three auth methods (email/password, Google OAuth, magic link)"
    - "OnboardingModal renders on first visit for a new-like user"
    - "Connect buttons open new tabs without closing wizard"
    - "Wizard completion lands user at / with 4-pane layout"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete onboarding E2E flow using dev-browser — from sign-up page through wizard to default workspace.

Purpose: Phase 14 is a verification phase. Code fixes are meaningless without confirming the flow actually works end-to-end in a real browser.

Output: Visual confirmation that all three ONBOARD requirements are satisfied, with screenshots.
</objective>

<execution_context>
@/Users/Naegele/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Naegele/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-onboarding-e2e/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify onboarding E2E flow with dev-browser</name>
  <read_first>
    - src/components/Layout.tsx
    - src/components/onboarding/OnboardingModal.tsx
    - src/components/ProtectedRoute.tsx
  </read_first>
  <action>
    Use dev-browser to verify all three ONBOARD scenarios at http://localhost:3001 (or https://callvault.vercel.app if localhost has OAuth issues). Take screenshots at each key state.

    **ONBOARD-01 — Sign-up page and wizard entry:**
    1. Navigate to the sign-up page (typically /login with a Sign Up tab or /signup)
    2. Screenshot the sign-up page and confirm all three auth methods are present/accessible:
       - Email/password sign-up form
       - Google OAuth button
       - Magic link option
    3. Log in with the test credentials from .env.local (CALLVAULTAI_LOGIN / CALLVAULTAI_LOGIN_PASSWORD)
    4. If the test user has onboarding_completed=true, temporarily set it to false via Supabase:
       ```sql
       UPDATE user_profiles SET onboarding_completed = false WHERE id = (SELECT id FROM auth.users WHERE email = '<test_email>');
       ```
    5. After login, verify the OnboardingModal appears (not a redirect to /settings)
    6. Screenshot the Welcome step (Step 0) of the wizard

    **ONBOARD-02 — Connect sources work without closing wizard:**
    1. Click "Get Started" to advance to Step 1 (Connect your first source)
    2. Screenshot Step 1 showing Fathom, Zoom, and Upload cards
    3. Verify all three source cards are visible and have action buttons
    4. Click "Connect Fathom" — verify a new tab/window opens (check that the wizard is still visible in the original tab)
    5. Close the new tab and return to the wizard
    6. Click "I'll do this later" to advance to Step 2

    **ONBOARD-03 — Post-onboarding landing:**
    1. Navigate through Step 2 (How It Works) and Step 3 (You're all set)
    2. Click "Go to my calls" on the final step
    3. Verify the URL is / and the 4-pane layout (TranscriptsNew) is rendered
    4. Screenshot the final landing state

    **After verification:** Reset the test user's onboarding state:
    ```sql
    UPDATE user_profiles SET onboarding_completed = true WHERE id = (SELECT id FROM auth.users WHERE email = '<test_email>');
    ```

    If any scenario fails, document the failure with a screenshot and the specific behavior observed vs expected.
  </action>
  <verify>
    <automated>echo "Dev-browser verification is visual — check screenshots captured during task execution. PASS if all 3 ONBOARD scenarios have screenshots showing correct behavior."</automated>
  </verify>
  <done>
    - Sign-up page screenshot shows email/password, Google OAuth, and magic link options
    - OnboardingModal screenshot shows wizard appearing after login for user with onboarding_completed=false
    - Step 1 screenshot shows all three source cards (Fathom, Zoom, Upload)
    - Connect button opens new tab without closing wizard
    - Final screenshot shows / URL with 4-pane TranscriptsNew layout after wizard completion
  </done>
</task>

</tasks>

<verification>
Visual verification via dev-browser confirms all three ONBOARD requirements are met end-to-end. Screenshots captured for each key state.
</verification>

<success_criteria>
- All three ONBOARD scenarios verified with screenshots
- Sign-up page confirmed accessible with all auth methods
- OnboardingModal renders for users with onboarding_completed=false
- Connect buttons open new tabs without closing the wizard
- Wizard completion navigates to / with 4-pane layout visible
</success_criteria>

<output>
After completion, create `.planning/phases/14-onboarding-e2e/14-02-SUMMARY.md`
</output>
