---
phase: 33-content-proof-matching-alibi-constraint
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - src/types/supabase.ts
autonomous: false
requirements: [MATCH-02, MATCH-07]

must_haves:
  truths:
    - "The p_tier migration is applied to TEST and then to production (prod ref vltmrnjsubfzrgrtdqey), guarded, with the 7-arg apply_event_match_atomic confirmed live by introspection."
    - "resolve-events is redeployed so the content-proof pass + alibi veto ship in production."
    - "In production the tier is INERT: transcript_chunks has ~0 rows, zero tier='content_proof' ledger rows exist, and apply_event_match_atomic EXECUTE stays revoked from anon/authenticated."
  artifacts:
    - path: "src/types/supabase.ts"
      provides: "Regenerated types reflecting apply_event_match_atomic's new p_tier arg"
      contains: "apply_event_match_atomic"
  key_links:
    - from: "supabase db push --linked (prod)"
      to: "apply_event_match_atomic 7-arg"
      via: "prod-ref-guarded migration apply of 20260905130000"
      pattern: "vltmrnjsubfzrgrtdqey"
    - from: "resolve-events redeploy"
      to: "runShadowSweep content-proof + alibi"
      via: "supabase functions deploy resolve-events --use-api"
      pattern: "resolve-events"
---

<objective>
Ship Phase 33 to production, guarded and INERT — following the exact prod-apply discipline of Phase 31/32. Apply the one additive migration and redeploy resolve-events so the content-proof pass and alibi veto go live, then prove by introspection that the tier does nothing in production today (empty transcript_chunks, zero content_proof rows, apply RPC still service-role-only).

Purpose: The milestone's rhythm is TEST-then-prod guarded apply per phase. This phase ships correctly inert (same posture as Phase 31's shadow mode and Phase 32's propose-only tier): the logic is live and provably harmless because its substrate (transcript_chunks, has_confirmed_speech) has no live writer.
Output: 20260905130000 applied to prod; resolve-events redeployed; types re-synced; inertness confirmed.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-content-proof-matching-alibi-constraint/33-02-SUMMARY.md

<interfaces>
<!-- Apply discipline + gotchas from the prior guarded applies. Follow exactly. -->
- Prod Supabase ref is `vltmrnjsubfzrgrtdqey`. Verify the linked project ref BEFORE and AFTER any
  prod push (Phase 31/32 verified 2-3x). Migrations read prod creds from `.env` DATABASE_URL, prod-ref
  guarded (root CLAUDE.md "Environment files" + supabase/CLAUDE.md).
- Migration to apply: supabase/migrations/20260905130000_content_proof_apply_tier_param.sql (authored in Plan 01).
- Edge deploy uses `--use-api` (Docker is not running; plain deploy hangs). See supabase/CLAUDE.md "Deploying Edge Functions".
- Types regen gotcha (Phase 32 P04, Rule 1): the Supabase CLI update-nag banner can leak onto stdout past
  the stderr redirect, appending non-TS garbage after the `} as const` terminator — strip any trailing
  non-TS lines before committing src/types/supabase.ts.
- type-baseline.json holds the accepted pre-existing type-check errors (incl. event-resolver.ts's Deno
  esm.sh type-only import, TS2307 under Node tsc). If regen surfaces only that known class, --update-baseline; do not fix.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply to TEST and gate on green</name>
  <files>(none in-repo — applies the Plan 01 migration to the TEST database; no repo file changes)</files>
  <read_first>
    - supabase/migrations/README_APPLY_MIGRATION.md
    - .planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-04-SUMMARY.md (the exact TEST-then-prod steps used last phase)
    - supabase/CLAUDE.md → "Running integration tests safely"
  </read_first>
  <action>
    Apply 20260905130000 to the TEST project (link TEST, `supabase db push --linked`). Confirm by
    introspection that apply_event_match_atomic now has 7 arguments and the old 6-arg overload is GONE
    (`SELECT pg_get_function_arguments(oid) FROM pg_proc WHERE proname='apply_event_match_atomic';` — expect
    exactly one row, ending in `p_tier text DEFAULT 'deterministic'::text`). Re-run the content-proof
    integration test against TEST as the final gate evidence
    (`VITEST_INTEGRATION_OK=true npx vitest run src/test/event-resolution-content-proof.integration.test.ts`).
    Do NOT touch prod in this task.
  </action>
  <verify>
    <automated>VITEST_INTEGRATION_OK=true npx vitest run src/test/event-resolution-content-proof.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Migration applied to TEST; introspection shows exactly one apply_event_match_atomic with the trailing p_tier arg (no 6-arg overload remains).
    - Content-proof integration test green against TEST.
    - No prod database or edge function was touched.
  </acceptance_criteria>
  <done>Migration live on TEST, single 7-arg function confirmed, integration test green, prod untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve production apply</name>
  <files>(none — approval gate before the prod apply in Task 3)</files>
  <action>
    Present the TEST evidence below and STOP. Do not run Task 3 until the operator types "approved".
    This gate authorizes a DDL apply to production (real customer data) plus an edge-function redeploy.
  </action>
  <what-built>
    Additive p_tier migration + content-proof/alibi logic, proven on TEST: content-proof attach,
    zero-transcript fallback, alibi rejection, and the direct apply_event_match_atomic(p_tier='content_proof')
    capability all pass; the metadata/shadow suites stayed green.
  </what-built>
  <how-to-verify>
    1. Review 33-02-SUMMARY.md and the TEST introspection output from Task 1.
    2. Confirm you approve applying migration 20260905130000 to PRODUCTION (ref vltmrnjsubfzrgrtdqey)
       and redeploying the resolve-events edge function.
    3. This ships INERT: transcript_chunks has no live writer and has_confirmed_speech is unpopulated,
       so the content-proof pass proposes nothing and the alibi veto fires on nothing in prod today.
  </how-to-verify>
  <verify>
    <automated>MANUAL — human approval gate; Task 3 does not run until "approved".</automated>
  </verify>
  <resume-signal>Type "approved" to authorize the prod apply + resolve-events redeploy, or describe concerns.</resume-signal>
  <done>Operator approved the prod apply + redeploy (or supplied overrides).</done>
</task>

<task type="auto">
  <name>Task 3: Guarded prod apply + resolve-events redeploy + type re-sync + inertness proof</name>
  <files>src/types/supabase.ts</files>
  <read_first>
    - .planning/phases/32-match-rule-hardening-provider-agnostic-matcher/32-04-SUMMARY.md (prod-ref-guard steps, --use-api deploy, types regen stdout gotcha)
    - root CLAUDE.md → "Environment files" (prod ref guard)
  </read_first>
  <action>
    After approval: link the prod project and verify the linked ref equals `vltmrnjsubfzrgrtdqey` BEFORE
    doing anything. Apply the migration to prod (`supabase db push --linked`). Redeploy the sweep entry
    point: `supabase functions deploy resolve-events --use-api`. Regenerate types:
    `supabase gen types typescript --linked > src/types/supabase.ts` (strip any CLI banner garbage after
    the `} as const` terminator before saving). Then confirm INERTNESS by prod introspection:
    (a) `SELECT COUNT(*) FROM transcript_chunks;` — expect 0 / near-0 (Open Question 2 resolution: this is
    the cheap read the research said to run; record the actual number in the SUMMARY).
    (b) `SELECT COUNT(*) FROM event_match_decisions WHERE tier='content_proof';` — expect 0.
    (c) Confirm EXECUTE on apply_event_match_atomic is still revoked from anon + authenticated (the DROP+CREATE
    re-revoked it; verify via has_function_privilege or pg_proc/acl introspection).
    Verify the linked ref is STILL `vltmrnjsubfzrgrtdqey` AFTER the push. Finally run
    `npx tsc -p tsconfig.app.json`; if the only new errors are the known Deno esm.sh type-only class,
    `--update-baseline`, else fix.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.app.json 2>&1 | tail -5; grep -q "apply_event_match_atomic" src/types/supabase.ts && echo TYPES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Prod ref confirmed vltmrnjsubfzrgrtdqey before AND after the push; migration 20260905130000 live in prod.
    - resolve-events redeployed (--use-api); src/types/supabase.ts regenerated and clean (no banner garbage).
    - Inertness proven in prod: transcript_chunks count recorded (~0), zero tier='content_proof' rows, apply RPC EXECUTE still revoked from anon/authenticated.
    - tsc -p tsconfig.app.json clean vs baseline (baseline updated only for the known esm.sh class if needed).
  </acceptance_criteria>
  <done>Migration + redeploy live in prod, inertness proven by introspection, types clean vs baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → production Postgres (vltmrnjsubfzrgrtdqey) | Applying DDL to a live system with real customer data; wrong-project apply is catastrophic (see supabase/CLAUDE.md 2026-05 incident). |
| redeployed resolve-events (service-role) → prod data | Ships the new sweep logic into the live 15-min cron path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06 | Tampering | migration applied to the wrong project | mitigate | Prod-ref guard verifies vltmrnjsubfzrgrtdqey before AND after; TEST apply proven first in Task 1; human checkpoint authorizes the prod step. |
| T-33-01 | Information Disclosure | content-proof auto-attach fires in prod on real data | mitigate | Ships inert: transcript_chunks ~0 rows (proven in-task), sweep is propose-only (SAFE-02), apply RPC EXECUTE revoked from anon/authenticated — no unreviewed merge can occur. |
| T-33-07 | Elevation of Privilege | DROP+CREATE of apply_event_match_atomic loses REVOKE, exposing it to authenticated callers | mitigate | Migration re-issues all three REVOKE EXECUTE statements (Plan 01 Task 2); Task 3 confirms EXECUTE still revoked by prod introspection. |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No packages introduced this phase (RESEARCH Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- TEST introspection: exactly one apply_event_match_atomic, trailing p_tier arg; content-proof integration test green on TEST.
- Prod introspection: migration live, transcript_chunks count ~0, zero content_proof ledger rows, apply RPC EXECUTE revoked from anon/authenticated, ref vltmrnjsubfzrgrtdqey before+after.
- `npx tsc -p tsconfig.app.json` clean vs type-baseline.json.
</verification>

<success_criteria>
- The p_tier migration and the content-proof/alibi sweep logic are live in production (MATCH-02, MATCH-07).
- The tier ships INERT and provably so — no content_proof rows, empty transcript_chunks, apply RPC service-role-only.
- Types re-synced; type-check clean vs baseline.
</success_criteria>

<artifacts_produced>
- `src/types/supabase.ts` — regenerated to include apply_event_match_atomic's p_tier arg.
- (Production state) migration 20260905130000 applied; resolve-events redeployed with the content-proof + alibi logic, inert.
</artifacts_produced>

<output>
Create `.planning/phases/33-content-proof-matching-alibi-constraint/33-03-SUMMARY.md` when done. Record: prod-ref-guard confirmations, the actual prod transcript_chunks COUNT observed, confirmation of zero content_proof rows + revoked EXECUTE, and any User Setup Required (e.g. the still-broken event-resolution-sweep pg_cron GUCs from Phase 32, if relevant).
</output>
