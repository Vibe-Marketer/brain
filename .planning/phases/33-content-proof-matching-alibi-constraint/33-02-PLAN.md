---
phase: 33-content-proof-matching-alibi-constraint
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - supabase/functions/_shared/event-resolver.ts
  - src/test/event-resolution-content-proof.integration.test.ts
autonomous: true
requirements: [MATCH-02, MATCH-07]

must_haves:
  truths:
    - "When two same-org recordings have transcript_chunks that share enough rare shingles, the sweep writes exactly one tier='content_proof' merge_proposed row for the pair."
    - "When a candidate recording has zero transcript_chunks, the content-proof pass records nothing and raises no error — the sweep falls through to the metadata tier (the dominant real-world path today)."
    - "A candidate pair is skipped (no proposal written) when a confirmed speaker ties the two recordings to time-disjoint intervals, across every tier's write path."
    - "apply_event_match_atomic(p_tier='content_proof') writes a merge_applied ledger row with tier='content_proof' and sets event_id on both recordings — proven by a direct call, never by the sweep (SAFE-02)."
  artifacts:
    - path: "supabase/functions/_shared/event-resolver.ts"
      provides: "runShadowSweep content-proof fetch/score/propose pass + alibi veto threaded across tier-1/tier-2/tier-3 writes; ShadowSweepSummary extended with contentProofProposed + alibiRejected"
      contains: "contentProofProposed"
    - path: "src/test/event-resolution-content-proof.integration.test.ts"
      provides: "End-to-end TEST proof: seeded synthetic transcript_chunks conclusive attach; dominant zero-transcript fallback; alibi rejection with seeded has_confirmed_speech; direct apply RPC auto-attach at tier='content_proof'"
      contains: "transcript_chunks"
  key_links:
    - from: "runShadowSweep"
      to: "transcript_chunks"
      via: "fetch keyed by canonical_recording_id IN (recordingIds), fail-closed isolation try/catch"
      pattern: "transcript_chunks"
    - from: "runShadowSweep alibi veto"
      to: "isSpeakerAlibiViolation"
      via: "checked before every tier's proposal write, using has_confirmed_speech + recording intervals"
      pattern: "isSpeakerAlibiViolation"
    - from: "integration test"
      to: "apply_event_match_atomic"
      via: "supabase.rpc('apply_event_match_atomic', { ..., p_tier: 'content_proof' })"
      pattern: "p_tier"
---

<objective>
Wire the Plan 01 primitives into `runShadowSweep`: add the content-proof fetch/score/propose pass (between the existing tier-1 and tier-3 passes) and thread the speaker-alibi veto across all tier write paths. Then prove the whole thing end-to-end on TEST with SEEDED synthetic `transcript_chunks` — because that table has no live writer, seeding is the only way to exercise the shingle path — while treating the zero-transcript fallback as the primary case.

Purpose: This turns the pure primitives into observable sweep behavior and produces the phase's real proof-of-correctness against a database. It follows Phase 31/32's fail-closed tier-isolation pattern exactly, so a `transcript_chunks` failure can never unwind an already-written tier-1 proposal or block tier-3.
Output: content-proof tier is live inside runShadowSweep (propose-only, SAFE-02 preserved); alibi veto is active across all tiers; a new integration test proves attach, fallback, alibi rejection, and the auto-attach capability.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-content-proof-matching-alibi-constraint/33-CONTEXT.md
@.planning/phases/33-content-proof-matching-alibi-constraint/33-RESEARCH.md
@.planning/phases/33-content-proof-matching-alibi-constraint/33-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 + the sweep this plan edits. Use directly. -->

runShadowSweep structure to extend (event-resolver.ts, current shape):
- Driving fetch at :268-276 selects recordings (id, organization_id, owner_user_id, title,
  source_app, source_metadata, recording_start_time, recording_end_time) WHERE event_id IS NULL,
  org IN flaggedOrgIds. `candidates: ShadowSweepCandidate[]`.
- Tier-1 write loop :292-322 (inserts tier='deterministic' merge_proposed rows).
- Metadata/tier-3 block :324-437 (its own try/catch; fetches call_participants at :336-339 selecting
  ONLY 'recording_id, email, name'; builds MetadataCandidate[]; findMetadataCandidates → writeMetadataProposals).
- `ShadowSweepSummary` = { organizationsScanned, recordingsScanned, proposed, metadataProposed, skipped, errors }.
- Idempotent write idiom (reuse): plain `.insert(...)` + `isUniqueViolation(error)` tolerated as a no-op (:301-321).

From Plan 01 (event-resolver.ts), now available to import/use in the sweep:
- findContentProofMatches(candidatesWithChunks) → ContentProofMatch[] (tier:'content_proof', score, signals)
- isSpeakerAlibiViolation(a, b) → boolean  (a/b = { start, end, participants:[{email, has_confirmed_speech}] })
- SHINGLE_SIZE, CONTENT_PROOF_MIN_SHARED_SHINGLES.

apply_event_match_atomic (after Plan 01's migration) — for the direct-call test only:
  rpc('apply_event_match_atomic', { p_recording_id_a, p_recording_id_b, p_event_id, p_decided_by, p_signals, p_owner_user_id, p_tier }) RETURNS event UUID.

transcript_chunks seeding contract (critical — 20251125000001 + 20260303000009):
  Required NOT-NULL columns when inserting a synthetic chunk: user_id (a real auth user id),
  recording_id (BIGINT — the FK to fathom_calls was DROPPED in 20251201000001, so any unique-ish
  bigint works), chunk_text, chunk_index. Set canonical_recording_id = the fixture recording UUID
  (this is what the sweep joins on). Do NOT set embedding. Do NOT rely on timestamp_start/end.

Integration-test templates to mirror:
- src/test/event-resolution-metadata-tier.integration.test.ts (fixture user/org/recording seeding,
  workspace_entries-first cleanup contract, describe.skipIf(!integrationDbReachable), 60s hooks).
- supabase/functions/_shared/__tests__ has no DB; the DB-touching proof lives under src/test/.
- Direct apply RPC call shape: see src/test/event-match-apply-reverse.integration.test.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content-proof fetch/score/propose pass in runShadowSweep</name>
  <files>supabase/functions/_shared/event-resolver.ts</files>
  <read_first>
    - supabase/functions/_shared/event-resolver.ts (runShadowSweep :239-445 — insert the new pass BETWEEN the tier-1 loop ending at :322 and the metadata block starting at :324)
    - 33-RESEARCH.md → Architecture Patterns "Pattern 1" (fail-closed tier isolation — the exact try/catch shape to copy) and the system-architecture diagram (zero-chunks = fall through, never error)
  </read_first>
  <action>
    Add a content-proof pass to runShadowSweep, positioned AFTER the tier-1 write loop and BEFORE the
    metadata block, wrapped in its OWN try/catch that fails closed (log + summary.errors++, never
    rethrow) exactly like the metadata block. In the pass: collect `recordingIds = candidates.map(c => c.id)`;
    if empty, do nothing. Otherwise fetch transcript_chunks
    (`.from('transcript_chunks').select('canonical_recording_id, chunk_index, chunk_text').in('canonical_recording_id', recordingIds)`).
    Group chunks by canonical_recording_id. Build the per-recording chunk arrays and call
    findContentProofMatches over the same-org candidate set. Write each returned ContentProofMatch as a
    propose-only row using the SAME idempotent idiom as tier-1/metadata:
    `.insert({ recording_id_a, recording_id_b, tier:'content_proof', score: match.score, signals: match.signals, decision:'merge_proposed', decided_by:'auto', applied:false })`, tolerating unique_violation
    as a no-op. Increment a NEW summary counter `contentProofProposed`. Per the Task-1 decision
    (option-a), this pass NEVER calls apply_event_match_atomic and NEVER writes recordings.event_id —
    it only proposes, preserving SAFE-02.
    Extend `ShadowSweepSummary` with `contentProofProposed: number` (and initialize it to 0). Adding a
    field is additive — existing tests assert specific fields, not object shape, so they stay green.
    CRITICAL — the dominant path: when a recording has NO chunks (the normal case today), it simply
    contributes an empty chunk array; findContentProofMatches proposes nothing for it; the pass records
    nothing and raises no error; the sweep proceeds to the metadata tier untouched. This IS Success
    Criterion 3 and must be the behavior when the transcript_chunks fetch returns zero rows.
  </action>
  <verify>
    <automated>npx vitest run supabase/functions/_shared/__tests__/event-resolver.test.ts && grep -q "contentProofProposed" supabase/functions/_shared/event-resolver.ts</automated>
  </verify>
  <acceptance_criteria>
    - New content-proof pass sits between the tier-1 loop and the metadata block, in its own fail-closed try/catch.
    - transcript_chunks is fetched by canonical_recording_id; chunks grouped per recording.
    - Content-proof rows are propose-only (decision='merge_proposed', tier='content_proof', decided_by='auto', applied=false); no apply RPC call, no recordings.event_id write anywhere in the pass.
    - ShadowSweepSummary has contentProofProposed; existing unit tests + the tier-1/metadata logic are unaffected.
    - Zero-chunk recordings cause the pass to write nothing and error nothing (fall-through preserved).
  </acceptance_criteria>
  <done>Content-proof propose-only pass live in runShadowSweep with fail-closed isolation and contentProofProposed counter; unit suite green; zero-chunk fall-through preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Thread the speaker-alibi veto across all tier write paths</name>
  <files>supabase/functions/_shared/event-resolver.ts</files>
  <read_first>
    - supabase/functions/_shared/event-resolver.ts (the tier-1 write loop, the new content-proof pass from Task 1, and the metadata writeMetadataProposals call site)
    - 33-RESEARCH.md → Architecture Patterns "Pattern 3" (alibi is a veto layered across tiers, not a tier itself)
  </read_first>
  <action>
    Make the alibi veto available to every write path. Hoist a single participant fetch to run once,
    early in runShadowSweep (right after the candidates fetch): select
    `recording_id, email, has_confirmed_speech` from call_participants for all recordingIds. Build an
    alibi lookup: `Map<recording_id, { start, end, participants:[{email, has_confirmed_speech}] }>`,
    where start/end come from the candidate's recording_start_time/recording_end_time. Before writing
    ANY proposal — tier-1, content-proof, and metadata — call
    `isSpeakerAlibiViolation(lookup[a], lookup[b])`; if it returns true, SKIP the write (per option-a:
    silent skip, no ledger row) and increment a new summary counter `alibiRejected`. Extend
    ShadowSweepSummary with `alibiRejected: number` (init 0). Wrap the hoisted fetch in fail-closed
    handling: if it fails, log + summary.errors++ and proceed with an EMPTY alibi lookup — an empty
    lookup means isSpeakerAlibiViolation returns false for every pair (fail toward NOT vetoing, so a
    fetch error never silently blocks a genuine merge; the veto only ever ADDS a rejection on positive
    evidence). Note the metadata block already fetches call_participants (recording_id, email, name) for
    its own scoring — do NOT remove that; the alibi fetch is a separate concern reading
    has_confirmed_speech. If you prefer, widen the existing metadata fetch to also select
    has_confirmed_speech and reuse it, but keep the alibi lookup available to the tier-1 and
    content-proof paths that run before the metadata block.
    Because every existing fixture sets has_confirmed_speech = NULL (default), the veto fires on no
    existing test — the shadow and metadata-tier integration tests MUST stay green.
  </action>
  <verify>
    <automated>npx vitest run supabase/functions/_shared/__tests__/event-resolver.test.ts && grep -q "isSpeakerAlibiViolation" supabase/functions/_shared/event-resolver.ts && grep -q "alibiRejected" supabase/functions/_shared/event-resolver.ts</automated>
  </verify>
  <acceptance_criteria>
    - A single hoisted call_participants fetch (including has_confirmed_speech) builds an alibi lookup available to all three write paths.
    - isSpeakerAlibiViolation is checked before every tier-1, content-proof, and metadata proposal write; a violation skips the write and increments alibiRejected.
    - Alibi fetch failure fails toward NOT vetoing (empty lookup), logged, counted in errors — never blocks a merge on a fetch error.
    - `npx tsc -p tsconfig.app.json` introduces zero new errors vs type-baseline.json.
  </acceptance_criteria>
  <done>Alibi veto checked before every tier's write, silent-skip + alibiRejected counter; fail-open-toward-not-vetoing on fetch error; unit suite green; tsc clean vs baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Integration test — attach, fallback (dominant), alibi rejection, auto-attach capability</name>
  <files>src/test/event-resolution-content-proof.integration.test.ts</files>
  <read_first>
    - src/test/event-resolution-metadata-tier.integration.test.ts (copy the fixture/cleanup/skipIf template exactly)
    - supabase/CLAUDE.md → "Running integration tests safely" + "Cleanup contract" (workspace_entries deleted FIRST; every cleanup step in its own try/catch checking .error; cleanup_test_fixture_users RPC)
    - src/test/event-match-apply-reverse.integration.test.ts (direct apply RPC call shape + ownership-by-parameter)
  </read_first>
  <action>
    Create `src/test/event-resolution-content-proof.integration.test.ts`, gated with
    `describe.skipIf(!integrationDbReachable)`, service-role client, SUITE_TAG
    "[phase-33-02 event-resolution-content-proof]", following the metadata-tier suite's fixture and
    cleanup contract exactly. Seed one fixture user, one org (flagged event_resolution=true), and these
    recordings (all event_id NULL initially; NO tier-1 zoom signal, so tier-1 stays out of it):
    (A) Content-proof pair — two same-org recordings (fathom + grain) whose seeded transcript_chunks
        share a long verbatim passage (insert >= 6 chunks each so the shared 7-grams clear
        CONTENT_PROOF_MIN_SHARED_SHINGLES). Seed transcript_chunks per the seeding contract:
        user_id=fixture user, recording_id=synthetic bigint (unique per row), canonical_recording_id=
        the recording UUID, chunk_text=the shared passage split across chunk_index 0..N, chunk_index set.
        Give this pair OVERLAPPING recording times and NO confirmed-speaker alibi conflict.
    (B) Zero-transcript recording(s) — one or two same-org recordings with NO transcript_chunks at all
        (the dominant real-world case). Assert the sweep completes with summary.errors === 0 and writes
        NO content_proof row for them. THIS is the first-class assertion, not an afterthought.
    (C) Alibi pair — two same-org recordings with time-DISJOINT intervals, sharing one participant whose
        call_participants row has has_confirmed_speech=true on the relevant side, AND seeded chunks that
        WOULD otherwise be a conclusive content-proof match. Assert NO merge_proposed row is written for
        this pair (alibi veto fired) and summary.alibiRejected >= 1.
    Tests to assert:
    1. `runShadowSweep(admin, { flaggedOrgIds:[orgId] })` → summary.errors===0; summary.contentProofProposed===1
       (only pair A); exactly one event_match_decisions row for pair A with tier='content_proof',
       decision='merge_proposed', decided_by='auto', applied=false, event_id NULL, score a number in [0,1].
    2. Zero-transcript recordings: no content_proof row involving them; sweep raised no error (SC-3, dominant path).
    3. Alibi pair: zero merge_proposed rows for that pair despite conclusive chunks; summary.alibiRejected>=1.
    4. Re-running the sweep is idempotent — still exactly one content_proof row for pair A (unique_violation no-op).
    5. Auto-attach CAPABILITY (proven by DIRECT call, never via the sweep — SAFE-02): call
       `admin.rpc('apply_event_match_atomic', { p_recording_id_a: A1, p_recording_id_b: A2, p_event_id: null, p_decided_by: 'admin', p_signals: { source:'phase-33-test' }, p_owner_user_id: fixtureUserId, p_tier: 'content_proof' })`.
       Assert it returns an event UUID; both recordings now have that event_id; and a
       tier='content_proof', decision='merge_applied', applied=true ledger row exists. (Use two dedicated
       recordings for this so it doesn't disturb pair A's propose-only assertions, or reverse it in cleanup.)
    Cleanup MUST also delete seeded transcript_chunks (by canonical_recording_id) before deleting
    recordings, and follow the workspace_entries-first ordering. Idempotent across re-runs.
    Run it via the integration entrypoint (TEST project env), not the default unit run.
  </action>
  <verify>
    <automated>VITEST_INTEGRATION_OK=true npx vitest run src/test/event-resolution-content-proof.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file created; runs green against TEST (or skips cleanly if TEST env is absent — never falls back to prod).
    - Content-proof pair A produces exactly one tier='content_proof' merge_proposed row; score in [0,1].
    - Zero-transcript recordings produce no content_proof row and no error (SC-3 asserted as a first-class case).
    - Alibi pair produces no proposal despite conclusive chunks; alibiRejected>=1.
    - Direct apply_event_match_atomic(p_tier='content_proof') writes a tier='content_proof' merge_applied row and sets event_id on both recordings.
    - Existing event-resolution-shadow + event-resolution-metadata-tier integration tests still pass (zero-transcript fallback confirmed by their continued green).
    - All seeded fixtures (incl. transcript_chunks) cleaned up; suite is idempotent.
  </acceptance_criteria>
  <done>Integration test green on TEST: content-proof attach, dominant zero-transcript fallback, alibi rejection, and direct-call auto-attach at tier='content_proof' all proven; existing suites still green; fixtures cleaned up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service-role sweep → Postgres (recordings, call_participants, transcript_chunks, event_match_decisions) | RLS bypassed; a false merge widens readable audience; a mis-scoped fetch could cross orgs. |
| provider free-text chunk_text → shingle scorer | Untrusted stored text drives the conclusive-match decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | content-proof pass writes a false merge | mitigate | Pass is propose-only (SAFE-02); shared>=5 rare-7-gram bar; alibi veto layered on top; integration test proves a coincidental-phrase pair does not propose. |
| T-33-02 | Tampering (of trust) | alibi treated as confirmation | mitigate | Veto only ever SKIPS a write; empty/failed alibi lookup returns false (no confirmation), never auto-attaches; test C proves a would-be match is suppressed, and no path adds confidence from absence-of-alibi. |
| T-33-03 | Information Disclosure | cross-org content-proof pair (SAFE-04) | mitigate | findContentProofMatches buckets same-org only; fetch is scoped to flagged-org recordingIds; existing SAFE-04 cross-org RLS test stays green. |
| T-33-04 | Tampering (of trust) | alibi ships inert (has_confirmed_speech unpopulated) yet assumed active | mitigate | Test C proves the LOGIC with seeded has_confirmed_speech=true fixtures; SUMMARY states the population gap is out of scope (no speech detector this phase) so the inertness is documented, not silently assumed. |
| T-33-05 | Denial of Service | huge/hostile chunk_text during sweep | mitigate | Content-proof pass is fail-closed (own try/catch → log + errors++); extractShingles tolerates arbitrary input; a failure cannot unwind tier-1's writes or block tier-3. |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No packages introduced this phase (RESEARCH Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `npx vitest run supabase/functions/_shared/__tests__/event-resolver.test.ts` green.
- `VITEST_INTEGRATION_OK=true npx vitest run src/test/event-resolution-content-proof.integration.test.ts` green on TEST.
- `VITEST_INTEGRATION_OK=true npx vitest run src/test/event-resolution-metadata-tier.integration.test.ts src/test/event-resolution-shadow.integration.test.ts` still green (regression + fallback proof).
- `npx tsc -p tsconfig.app.json` adds zero new errors vs type-baseline.json.
</verification>

<success_criteria>
- Content-proof tier is live inside runShadowSweep as a propose-only pass; conclusive seeded pairs propose exactly once (MATCH-02).
- Zero-transcript captures fall through to the metadata tier with no error — proven as the primary case (SC-3).
- Alibi veto is active across all tier write paths; a confirmed-speaker-disjoint-interval pair is suppressed (MATCH-07).
- The auto-attach capability (apply RPC with p_tier='content_proof') is proven by direct call, never by the sweep (SAFE-02 intact).
</success_criteria>

<artifacts_produced>
- `supabase/functions/_shared/event-resolver.ts` — content-proof pass + alibi veto wired into runShadowSweep; ShadowSweepSummary extended (contentProofProposed, alibiRejected).
- `src/test/event-resolution-content-proof.integration.test.ts` — end-to-end TEST proof (attach, fallback, alibi, auto-attach capability).
</artifacts_produced>

<output>
Create `.planning/phases/33-content-proof-matching-alibi-constraint/33-02-SUMMARY.md` when done. Record: the seeded-fixture strategy used, the actual contentProofProposed/alibiRejected counts observed, confirmation the metadata/shadow suites stayed green, and an explicit note that has_confirmed_speech and transcript_chunks have no live writer so the tier ships inert on real data.
</output>
