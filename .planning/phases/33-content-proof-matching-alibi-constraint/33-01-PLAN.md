---
phase: 33-content-proof-matching-alibi-constraint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260905130000_content_proof_apply_tier_param.sql
  - supabase/functions/_shared/event-resolver.ts
  - supabase/functions/_shared/__tests__/event-resolver.test.ts
autonomous: false
requirements: [MATCH-02, MATCH-07]

must_haves:
  truths:
    - "A pair of transcripts sharing enough rare (7-token) shingles is scored conclusive; a pair that shares only coincidental short phrases is not."
    - "apply_event_match_atomic can legally write a ledger row with tier='content_proof' (and still 'deterministic' for existing callers) without a second, parallel merge implementation."
    - "An identity with has_confirmed_speech=true in one recording, present in a time-disjoint other recording, is flagged as an alibi violation; attendance (has_confirmed_speech NULL/false) never flags."
    - "isSpeakerAlibiViolation returns only a boolean veto — a false result is never treated as a confirmation of a merge."
  artifacts:
    - path: "supabase/migrations/20260905130000_content_proof_apply_tier_param.sql"
      provides: "Additive p_tier parameter on apply_event_match_atomic (old 6-arg signature dropped, 7-arg created, EXECUTE re-revoked)"
      contains: "p_tier TEXT DEFAULT 'deterministic'"
    - path: "supabase/functions/_shared/event-resolver.ts"
      provides: "Pure content-proof scorer (extractShingles, scoreContentProofOverlap, findContentProofMatches) + pure alibi predicate (isSpeakerAlibiViolation)"
      exports: ["extractShingles", "scoreContentProofOverlap", "findContentProofMatches", "isSpeakerAlibiViolation", "SHINGLE_SIZE", "CONTENT_PROOF_MIN_SHARED_SHINGLES"]
    - path: "supabase/functions/_shared/__tests__/event-resolver.test.ts"
      provides: "Unit tests for the shingle scorer and the alibi predicate (DB-free, synthetic)"
      contains: "isSpeakerAlibiViolation"
  key_links:
    - from: "scoreContentProofOverlap"
      to: "chunk_index ordering"
      via: "relative-offset alignment by ordinal chunk position, never timestamp_start/end"
      pattern: "chunk_index"
    - from: "apply_event_match_atomic INSERT"
      to: "event_match_decisions.tier"
      via: "p_tier parameter replaces the hardcoded 'deterministic' literal"
      pattern: "p_tier"
    - from: "isSpeakerAlibiViolation"
      to: "call_participants.has_confirmed_speech"
      via: "only has_confirmed_speech === true anchors an alibi"
      pattern: "has_confirmed_speech"
---

<objective>
Build the two pure, DB-free primitives of Phase 33 — the content-proof shingle scorer (MATCH-02) and the speaker-alibi predicate (MATCH-07) — plus the one additive migration that lets a content-proof match legally write `tier='content_proof'` to the ledger. Lock the design decisions that Plan 02's sweep-wiring and Plan 03's prod apply build on, at a single Task-1 checkpoint.

Purpose: This is the load-bearing logic layer. Everything else in the phase (sweep wiring, integration proof, prod apply) consumes the functions and the migration built here. Keeping the algorithm pure and DB-free mirrors the established Phase 31/32 pattern (`findDeterministicMatches`, `findMetadataCandidates`, `shouldSuppressTitleSignal`) so it is unit-testable without a database and portable to the Deno edge runtime.
Output: `event-resolver.ts` gains the content-proof + alibi pure functions with full unit coverage; a new migration extends `apply_event_match_atomic`.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-content-proof-matching-alibi-constraint/33-CONTEXT.md
@.planning/phases/33-content-proof-matching-alibi-constraint/33-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase this session — use directly, no exploration required. -->

EXISTING, in supabase/functions/_shared/event-resolver.ts (extend, do not rewrite):
- The pure-predicate style to mirror for BOTH new functions is `shouldSuppressTitleSignal`
  (event-resolver.ts:469-476): fail-closed (never throws), returns a plain value, reads no DB.
- `findDeterministicMatches` (:178-203) and `findMetadataCandidates` (:676-721) show the
  same-org `Map<organization_id, candidate[]>` bucketing pattern that content-proof MUST reuse
  verbatim so cross-org pairs are never even compared (SAFE-04 defense-in-depth).
- Canonical pair ordering everywhere: `const [a, b] = x.id < y.id ? [x.id, y.id] : [y.id, x.id];`
  (matches event_match_decisions_pair_ordered CHECK: recording_id_a < recording_id_b).

EXISTING RPC to extend, in supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql:
```sql
CREATE OR REPLACE FUNCTION public.apply_event_match_atomic(
  p_recording_id_a UUID, p_recording_id_b UUID, p_event_id UUID,
  p_decided_by TEXT, p_signals JSONB, p_owner_user_id UUID
) RETURNS UUID LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$ ... $$;
-- line 137 hardcodes the literal 'deterministic' in the INSERT INTO event_match_decisions.
-- SECURITY DEFINER; EXECUTE revoked from PUBLIC/anon/authenticated (lines 249-255).
-- Only caller anywhere is a direct integration test (event-match-apply-reverse.integration.test.ts).
```

EXISTING CHECK constraints, in supabase/migrations/20260901000002_create_event_match_decisions.sql:
- `tier TEXT NOT NULL CHECK (tier IN ('deterministic', 'content_proof', 'metadata'))`  (line 49 — 'content_proof' ALREADY valid, no table migration needed)
- `decision TEXT NOT NULL CHECK (decision IN ('merge_proposed','merge_applied','reversed','rejected'))`  (line 52)
- `decided_by TEXT NOT NULL CHECK (decided_by IN ('auto','user','admin'))`  (line 53)

transcript_chunks columns (20251125000001 + 20260303000009_update_transcript_chunks_fk.sql):
- chunk_text TEXT NOT NULL; chunk_index INTEGER NOT NULL ("Order within recording, 0-indexed");
  canonical_recording_id UUID (FK recordings.id) — the column the resolver joins on;
  speaker_name TEXT; speaker_email TEXT; timestamp_start/timestamp_end TEXT (nullable, format
  unconfirmed, NEVER populated by any live writer — DO NOT parse; use chunk_index for ordering).

call_participants.has_confirmed_speech (20260831000001:155-157): BOOLEAN, nullable.
  NULL = unknown/not analyzed; false = analyzed, did not speak; true = confirmed spoke.
  Only `=== true` anchors an alibi (attendance is never an alibi).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Design gate — lock the content-proof + alibi contract</name>
  <files>(none — decision gate; output feeds Tasks 2-3)</files>
  <action>
    Present the three decisions below to the operator and STOP. Do NOT implement Tasks 2-3 until an
    option is selected. Record the chosen option and any override constants in 33-01-SUMMARY.md, then
    build Tasks 2-3 to that decision. option-a is the recommended safe default (ships inert, preserves
    SAFE-02) and may be pre-approved outside this invocation, matching how Phase 30/31/32 design gates
    were resolved.
  </action>
  <decision>
    Lock three design decisions the rest of the phase builds on. All three have a recommended
    option-a; option-a is the safe default that ships INERT in production (per the phase's locked
    scoping guidance) and preserves the SAFE-02 boundary.
  </decision>
  <context>
    Research (33-RESEARCH.md) recommends a concrete design; these three points are where a real
    choice exists. `transcript_chunks` has zero live writers and `has_confirmed_speech` is set by
    no code path anywhere, so on real data every path below is currently inert — the choice is
    about correctness-of-shape and the safety posture, not about production behavior today.
  </context>
  <options>
    <option id="option-a">
      <name>RECOMMENDED — propose-only sweep + apply capability proven separately; silent alibi veto; shingles k=7 / shared>=5</name>
      <pros>
        - Content-proof posture: the sweep's content-proof pass writes PROPOSE-ONLY rows
          (decision='merge_proposed', tier='content_proof', score, decided_by='auto'), exactly like
          tier-1 and the metadata tier do today. The auto-attach CAPABILITY (calling the p_tier-extended
          apply_event_match_atomic with p_tier='content_proof') is built and PROVEN by a direct
          integration test in Plan 02 — identically to how Phase 31 built the apply/reverse pair: built,
          tested, and NEVER called by the automatic sweep. This is what "content-proof CAN auto-attach,
          unlike metadata" means here — content-proof gains the apply capability (metadata never did),
          while the sweep stays propose-only. Preserves SAFE-02's documented boundary
          ("apply MUST NEVER be called by the automatic shadow sweep") byte-for-byte.
        - Alibi veto: a violation SILENTLY skips the write (no ledger row). Matches MATCH-07's literal
          "vetoes a candidate; never confirms one" and avoids unbounded duplicate 'rejected' rows on
          every 15-min sweep tick (the unique index only covers merge_proposed, so 'rejected' rows are
          not dedup-protected).
        - Shingles: 7-token word shingles, exact-set overlap; conclusive when the two recordings share
          >= 5 distinct 7-grams (CONTENT_PROOF_MIN_SHARED_SHINGLES=5). Long shingles are rare by
          construction (research: 6+ tokens; no corpus IDF needed). Ledger score = Jaccard of the two
          shingle sets, for provenance.
      </pros>
      <cons>
        - Content-proof and metadata behave identically INSIDE the sweep (both propose); the behavioral
          difference (auto-attach) is deferred until transcript ingestion exists and precision is proven
          (the same SAFE-06 discipline the whole milestone follows). Acceptable and intended per the
          "ship inert" scoping guidance.
        - Alibi vetoes are not audit-logged this phase (no provenance trail for a rejection).
      </cons>
    </option>
    <option id="option-b">
      <name>Auto-attach wired INTO the sweep + 'rejected' ledger rows for alibi vetoes</name>
      <pros>
        - Content-proof genuinely auto-attaches from the automatic sweep (calls apply RPC directly).
        - Alibi vetoes leave an auditable 'rejected' ledger row (MATCH-09 provenance-first reading).
      </pros>
      <cons>
        - Relaxes SAFE-02's "sweep never applies" invariant — the central safety guarantee of Phases
          31/32. A false content-proof auto-attach is a data-exposure incident (threat T-33-01) with no
          human in the loop and no precision-proving step first. Contradicts the phase's locked "ship
          inert, same pattern as shadow mode" scoping.
        - 'rejected' rows need their own idempotency design (not dedup-protected) or they grow unbounded
          on every sweep tick.
      </cons>
    </option>
  </options>
  <verify>
    <automated>MANUAL — decision gate; no automated check. The selected option is recorded in 33-01-SUMMARY.md before Tasks 2-3 run.</automated>
  </verify>
  <resume-signal>Select: option-a (recommended) or option-b, or state specific overrides (e.g. a different SHINGLE_SIZE or CONTENT_PROOF_MIN_SHARED_SHINGLES).</resume-signal>
  <done>An option is selected and recorded in 33-01-SUMMARY.md; Tasks 2-3 implement exactly that option.</done>
</task>

<task type="auto">
  <name>Task 2: Additive migration — apply_event_match_atomic gains p_tier</name>
  <files>supabase/migrations/20260905130000_content_proof_apply_tier_param.sql</files>
  <read_first>
    - supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql (the function to extend; note the hardcoded 'deterministic' at line 137 and the REVOKE EXECUTE block at 249-255)
    - supabase/migrations/20260901000002_create_event_match_decisions.sql (line 49 — tier CHECK already accepts 'content_proof')
    - supabase/CLAUDE.md → "Migration Conventions" (file header + section-comment style)
  </read_first>
  <action>
    Create `supabase/migrations/20260905130000_content_proof_apply_tier_param.sql` (timestamp is
    after the latest existing migration 20260905120000). The migration must, in order:
    (1) `DROP FUNCTION IF EXISTS public.apply_event_match_atomic(UUID, UUID, UUID, TEXT, JSONB, UUID);`
    — this drops the OLD 6-argument signature. This is mandatory, not optional: adding a 7th parameter
    with a DEFAULT creates a SEPARATE overload, and Postgres would resolve a 6-argument call to the old
    overload (still hardcoding 'deterministic'). Dropping the 6-arg forces every call through the new
    7-arg function. Safe because the only caller anywhere is a direct integration test (zero production
    callers — verified in 33-RESEARCH.md Pitfall 2); no view/trigger depends on it.
    (2) `CREATE FUNCTION public.apply_event_match_atomic(...)` re-declaring the body IDENTICAL to the
    existing one EXCEPT: add a trailing 7th parameter `p_tier TEXT DEFAULT 'deterministic'`; replace the
    hardcoded literal `'deterministic'` in the `INSERT INTO event_match_decisions` VALUES list with
    `p_tier`; and add, near the top of the body (after BEGIN, before the ownership checks), a guard:
    `IF p_tier NOT IN ('deterministic','content_proof','metadata') THEN RAISE EXCEPTION 'Invalid tier: %', p_tier; END IF;`
    Keep SECURITY DEFINER, SET search_path = public, and the ownership-by-parameter NOT EXISTS checks
    exactly as they are. Do NOT touch reverse_event_match_atomic.
    (3) Re-issue the three `REVOKE EXECUTE ON FUNCTION public.apply_event_match_atomic FROM PUBLIC/anon/authenticated;`
    statements — grants are dropped with the old function, so they must be re-applied to the new one.
    (4) Update the `COMMENT ON FUNCTION` to note the new p_tier param and that the SAFE-02 boundary is
    unchanged (still never called by the automatic sweep; per option-a, content-proof auto-attach is a
    separately-proven capability, not a sweep behavior).
    Do NOT apply this migration to any database in this task — Plan 03 owns the guarded TEST→prod apply.
  </action>
  <verify>
    <automated>grep -q "p_tier TEXT DEFAULT 'deterministic'" supabase/migrations/20260905130000_content_proof_apply_tier_param.sql && grep -q "DROP FUNCTION IF EXISTS public.apply_event_match_atomic(UUID, UUID, UUID, TEXT, JSONB, UUID)" supabase/migrations/20260905130000_content_proof_apply_tier_param.sql && grep -cE "REVOKE EXECUTE ON FUNCTION public.apply_event_match_atomic" supabase/migrations/20260905130000_content_proof_apply_tier_param.sql | grep -qx 3 && echo MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the exact path with the `20260905130000` timestamp.
    - Old 6-arg signature is dropped; new 7-arg function created with `p_tier TEXT DEFAULT 'deterministic'`.
    - The `INSERT INTO event_match_decisions` uses `p_tier`, not the literal `'deterministic'`.
    - Function-body guard rejects any p_tier outside ('deterministic','content_proof','metadata').
    - All three REVOKE EXECUTE statements re-issued (grep count == 3).
    - reverse_event_match_atomic is untouched; no database was mutated by this task.
  </acceptance_criteria>
  <done>Migration authored, grep gate passes, no database mutated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pure content-proof scorer + alibi predicate, with unit tests</name>
  <files>supabase/functions/_shared/event-resolver.ts, supabase/functions/_shared/__tests__/event-resolver.test.ts</files>
  <read_first>
    - supabase/functions/_shared/event-resolver.ts (mirror shouldSuppressTitleSignal:469-476 fail-closed style; reuse the same-org Map bucketing from findMetadataCandidates:676-721; reuse canonical pair ordering)
    - supabase/functions/_shared/__tests__/event-resolver.test.ts (existing unit-test file to extend — imports at :16-27, describe-block style)
    - 33-RESEARCH.md → Architecture Patterns "Pattern 3" (alibi pseudocode) and Anti-Patterns (why k must be long; why NOT timestamp_start)
  </read_first>
  <behavior>
    Write these unit tests FIRST (RED), in event-resolver.test.ts, then implement to green.
    Content-proof scorer:
    - extractShingles("the quick brown fox jumps over the lazy dog today", k=7) returns a Set of the
      overlapping 7-token joined shingles (tokens = lowercased, split on non-alphanumeric, empties dropped).
    - extractShingles tolerates arbitrary Unicode, empty string, and text shorter than k (returns empty Set, never throws).
    - scoreContentProofOverlap(chunksA, chunksB) concatenates each side's chunks in chunk_index order
      into one token stream, extracts 7-grams, and returns { sharedShingles, jaccard, conclusive }.
      Two transcripts that share a long verbatim passage (>= 5 shared 7-grams) → conclusive=true.
      Two transcripts sharing only short stock phrases ("thank you so much everyone") → conclusive=false.
      Chunks supplied out of order still align (sorted by chunk_index internally).
    - findContentProofMatches buckets by organization_id (cross-org pairs are never scored), emits a
      canonically-ordered ContentProofMatch { recording_id_a, recording_id_b, tier:'content_proof', score, signals:{shared_shingles} } only for conclusive pairs.
    Alibi predicate:
    - isSpeakerAlibiViolation(a, b) where a/b = { start, end, participants:[{email, has_confirmed_speech}] }:
      returns true when the SAME email has has_confirmed_speech===true on one side, is present on the
      other side, AND the two intervals are time-disjoint (a.end <= b.start || b.end <= a.start).
      Checks BOTH directions (a-confirmed-in-b, b-confirmed-in-a).
    - Attendance is never an alibi: a participant with has_confirmed_speech NULL or false never triggers a violation.
    - Overlapping intervals never trigger a violation, even with a shared confirmed speaker.
    - Reject-only invariant (threat T-33-02): a pair with NO alibi violation returns false — and the
      test asserts false means "not vetoed", never "confirmed". Add an explicit test comment + assertion
      that the function's only truthy meaning is "veto this candidate".
    - Fails closed: malformed/missing inputs (null participants, unparseable timestamps) return false
      (do not veto a genuine merge on bad data), never throw.
  </behavior>
  <action>
    Implement in event-resolver.ts, following option-a's locked constants (or the overrides from Task 1):
    export `SHINGLE_SIZE = 7`, `CONTENT_PROOF_MIN_SHARED_SHINGLES = 5`. Implement `extractShingles`,
    `scoreContentProofOverlap`, `findContentProofMatches`, and `isSpeakerAlibiViolation` as pure,
    DB-free, fail-closed functions in the style of shouldSuppressTitleSignal. Alignment is by
    chunk_index ordinal position ONLY (never timestamp_start/timestamp_end — those are unreliable TEXT,
    per 33-RESEARCH.md Pitfall 4). Add the ContentProofMatch / ContentProofChunk / AlibiCandidate
    interfaces. Do NOT wire anything into runShadowSweep in this task — that is Plan 02. These functions
    stand alone and are exercised only by the unit tests here.
  </action>
  <verify>
    <automated>npx vitest run supabase/functions/_shared/__tests__/event-resolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All new unit tests pass; the pre-existing event-resolver.test.ts tests still pass.
    - extractShingles/scoreContentProofOverlap/findContentProofMatches/isSpeakerAlibiViolation exported and pure (no import of a Supabase client value — type-only imports only, matching the file header contract at :49).
    - No fenced production code was pasted into runShadowSweep; the sweep is byte-unchanged this plan.
    - `npx tsc -p tsconfig.app.json` introduces zero NEW type errors over the committed baseline (type-baseline.json).
  </acceptance_criteria>
  <done>Unit suite green (new + existing); the four functions exported and pure; runShadowSweep unchanged; tsc clean vs baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service-role edge function → Postgres | The matcher runs as service-role (RLS bypassed); a false merge here can widen a recording's readable audience. |
| provider-sourced `transcript_chunks.chunk_text` → shingle extractor | Arbitrary provider free text (already stored, not new input) crosses into the tokenizer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | content-proof auto-attach (false merge widens audience) | mitigate | 7-token shingles + shared>=5 conclusive bar makes coincidental collision implausible; per option-a the sweep is propose-only and the apply capability is proven separately (SAFE-02 preserved), so no unreviewed auto-attach ships. |
| T-33-02 | Tampering (of trust) | isSpeakerAlibiViolation used as a confirmation | mitigate | Function returns a boolean veto only; unit test asserts `false` means "not vetoed", never "confirmed"; no caller may treat absence-of-alibi as a merge signal. |
| T-33-03 | Information Disclosure | cross-org pair compared (SAFE-04) | mitigate | findContentProofMatches reuses the same-org `Map<organization_id,...>` bucketing verbatim; cross-org pairs are never grouped, never scored. |
| T-33-05 | Denial of Service | shingle extractor on hostile/huge/Unicode chunk_text | mitigate | extractShingles is fail-closed (try/catch → empty Set), tolerates arbitrary Unicode and length; no raw string reaches SQL (reads are parameterized by the Supabase client in Plan 02). |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No packages introduced this phase (33-RESEARCH.md Package Legitimacy Audit: not applicable — nothing to install); slopcheck N/A. |
</threat_model>

<verification>
- `npx vitest run supabase/functions/_shared/__tests__/event-resolver.test.ts` green (new + existing).
- Migration file present, grep gate (Task 2 verify) passes.
- `npx tsc -p tsconfig.app.json` adds zero new errors vs type-baseline.json (root `npm run type-check` is hollow — use the app tsconfig).
- No change to runShadowSweep or any shipped tier this plan (grep confirms runShadowSweep body unchanged).
</verification>

<success_criteria>
- Content-proof scorer distinguishes a shared verbatim passage (conclusive) from coincidental short phrases (not) — proven by unit test.
- Alibi predicate rejects a confirmed-speaker-in-a-disjoint-interval pair, never flags on attendance alone, and never confirms — proven by unit test.
- apply_event_match_atomic migration authored (not yet applied) so tier='content_proof' can be written legally.
- Design contract for Plans 02/03 is locked at the Task-1 checkpoint.
</success_criteria>

<artifacts_produced>
- `supabase/migrations/20260905130000_content_proof_apply_tier_param.sql` — additive p_tier param on apply_event_match_atomic (authored; applied in Plan 03).
- `supabase/functions/_shared/event-resolver.ts` — pure content-proof scorer + alibi predicate (consumed by Plan 02's sweep wiring).
- `supabase/functions/_shared/__tests__/event-resolver.test.ts` — unit coverage for both.
</artifacts_produced>

<output>
Create `.planning/phases/33-content-proof-matching-alibi-constraint/33-01-SUMMARY.md` when done. Record: the option chosen at Task 1 (and any override constants), and confirmation the sweep body is unchanged this plan.
</output>
