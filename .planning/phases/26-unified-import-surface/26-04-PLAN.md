---
phase: 26-unified-import-surface
plan: 04
type: execute
wave: 4
depends_on: ["26-03"]
files_modified:
  - src/components/connectors/ConnectorImportWizard.tsx
  - src/components/connectors/__tests__/ConnectorImportWizard.test.tsx
  - src/components/connectors/__tests__/ConnectorImportWizard.capabilities.test.ts
  - src/components/connectors/__tests__/connectorSearch.test.ts
  - src/components/connectors/connectorSearch.ts
  - src/components/transcripts/SyncTab.tsx
  - src/components/transcripts/SyncTab.registry.test.ts
  - src/components/transcripts/UnsyncedMeetingsSection.tsx
  - src/components/transcripts/UnsyncedMeetingsSection.registry.test.ts
  - src/components/transcripts/SyncedTranscriptsSection.tsx
  - src/hooks/useSyncTabSelection.ts
  - src/hooks/useSyncTabOrchestration.ts
  - src/hooks/useSyncTabStateBridge.ts
autonomous: true
requirements: [TBL-03]
user_setup: []

must_haves:
  truths:
    - "The forked ConnectorImportWizard is deleted; nothing imports it"
    - "The duplicate useSyncTab* selection/orchestration/bridge hooks and the folded sections are deleted; nothing imports them"
    - "connectorSearch.ts is deleted ONLY after BOTH its consumers (wizard + orchestration) are gone, and only if ImportSurface does not depend on it"
    - "The app still boots — OAUTH_CALLBACK_ROUTES non-empty against a committed-tree build; full suite + lint:docs green"
    - "Job-status UI (SyncStatusIndicator, ActiveSyncJobsCard) and useSyncTabState are PRESERVED for Phase 27"
  artifacts:
    - path: ".planning/phases/26-unified-import-surface/26-04-SUMMARY.md"
      provides: "Deletion record + build-green confirmation"
  key_links:
    - from: "src/components/import/ImportSurface.tsx"
      to: "find-new paging"
      via: "self-contained paging after fork removal"
      pattern: "ImportSurface"
---

<objective>
The DELETE wave. Remove the forked `ConnectorImportWizard`, the duplicate `useSyncTab*` selection/orchestration/bridge hooks, and the now-folded sections — in dependency-leaf-first order, gated behind a build-green-on-the-committed-tree check. `connectorSearch.ts` deletes LAST, only after BOTH its consumers (wizard + orchestration) are gone (Pitfall 3) and only if ImportSurface does not depend on it.

CRITICAL preservation (locked decision): `SyncStatusIndicator.tsx`, `ActiveSyncJobsCard.tsx`, and `useSyncTabState.ts` (the job-poll/Realtime bits) CARRY to Phase 27 — DO NOT delete them in this plan. Only the selection/orchestration/bridge fork plumbing and the wizard are removed here.

Purpose: TBL-03 — the fork is gone once both surfaces share <ImportSurface>, and OAUTH_CALLBACK_ROUTES.length passes against a committed-tree build.

Output: deleted files + migrated registry tests + a green full suite, build, and lint:docs.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-unified-import-surface/26-RESEARCH.md
@src/CLAUDE.md

<interfaces>
<!-- Deletion graph — enumerated this session via grep. -->

Files to DELETE (and what still references them — all consumers must be gone first):
  - src/components/connectors/ConnectorImportWizard.tsx — consumer ImportPage.tsx (rewired in Plan 03). Also delete its tests: ConnectorImportWizard.test.tsx, ConnectorImportWizard.capabilities.test.ts.
  - src/components/transcripts/SyncTab.tsx — consumer TranscriptsNew.tsx (rewired in Plan 03). Also SyncTab.registry.test.ts (migrate coverage to ImportSurface).
  - src/components/transcripts/UnsyncedMeetingsSection.tsx — consumer SyncTab.tsx (deleted here). Also UnsyncedMeetingsSection.registry.test.ts (migrate).
  - src/components/transcripts/SyncedTranscriptsSection.tsx — consumer SyncTab.tsx (deleted here).
  - src/hooks/useSyncTabSelection.ts — consumer SyncTab.tsx (deleted here).
  - src/hooks/useSyncTabOrchestration.ts — consumer SyncTab.tsx (deleted here). NOTE: it imports searchAllAvailableConnectorCalls from connectorSearch.ts — so connectorSearch can only delete after THIS is gone.
  - src/hooks/useSyncTabStateBridge.ts — consumer SyncTab.tsx (deleted here).
  - src/components/connectors/connectorSearch.ts — consumers: ConnectorImportWizard (deleted here) AND useSyncTabOrchestration (deleted here). DELETE LAST. Also connectorSearch.test.ts. ONLY delete if ImportSurface (Plan 02) does NOT import it — verify first; if ImportSurface still imports appendUniqueAvailableCalls/searchAllAvailableConnectorCalls, KEEP connectorSearch.ts and its test (it is then a live dependency, not dead code).

PRESERVE (Phase 27 — do NOT delete):
  - src/components/transcripts/SyncStatusIndicator.tsx
  - src/components/transcripts/ActiveSyncJobsCard.tsx
  - src/hooks/useSyncTabState.ts (job-poll/Realtime; if it imports a deleted hook, sever that import cleanly rather than deleting the file)

Boot-crash gate: src/lib/__tests__/oauth-callback-routing.test.ts (OAUTH_CALLBACK_ROUTES.length non-zero). Build against the COMMITTED tree before pushing.

Other references found by grep (verify each is dead before deleting target; some are unrelated string matches):
  src/lib/transcript-download.ts, src/services/integrations.service.ts, src/services/sync-tab.service.ts, src/components/transcript-library/BulkActionToolbarEnhanced.tsx — confirm whether these import a deleted module or merely contain a substring; sever real imports, leave substring-only matches alone.

RTK note: RTK blanks build/vitest/tsc/grep — run via `rtk proxy` or `/usr/bin/grep`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate registry/coverage tests onto <ImportSurface>, then delete the wizard + fork hooks (leaf-first)</name>
  <read_first>
    - src/components/transcripts/SyncTab.registry.test.ts (what it asserts — migrate the still-relevant assertions to ImportSurface coverage)
    - src/components/transcripts/UnsyncedMeetingsSection.registry.test.ts (same)
    - src/components/connectors/__tests__/ConnectorImportWizard.capabilities.test.ts (capability-gating assertions — ensure equivalent coverage exists for ImportSurface from Plan 02; add if missing)
    - src/hooks/useSyncTabState.ts (confirm which deleted hooks it imports so those imports can be severed without deleting the file — it is PRESERVED for Phase 27)
    - src/pages/ImportPage.tsx + src/pages/TranscriptsNew.tsx (confirm Plan 03 already removed the wizard/SyncTab JSX usage; only the import lines may remain)
  </read_first>
  <files>src/components/connectors/ConnectorImportWizard.tsx, src/components/connectors/__tests__/ConnectorImportWizard.test.tsx, src/components/connectors/__tests__/ConnectorImportWizard.capabilities.test.ts, src/components/transcripts/SyncTab.tsx, src/components/transcripts/SyncTab.registry.test.ts, src/components/transcripts/UnsyncedMeetingsSection.tsx, src/components/transcripts/UnsyncedMeetingsSection.registry.test.ts, src/components/transcripts/SyncedTranscriptsSection.tsx, src/hooks/useSyncTabSelection.ts, src/hooks/useSyncTabOrchestration.ts, src/hooks/useSyncTabStateBridge.ts</files>
  <action>
    First, migrate coverage: ensure the still-relevant assertions from SyncTab.registry.test.ts, UnsyncedMeetingsSection.registry.test.ts, and ConnectorImportWizard.capabilities.test.ts have equivalents covering <ImportSurface> (capability gating for all 7 providers, the two-section structure, source-registry/OAUTH route coverage). Add to the Plan 01/02 ImportSurface test files if a gap exists; do not lose capability-gating coverage.

    Then delete in dependency-leaf-first order (RESEARCH "Safe deletion order" Wave B):
    1. Remove the now-dead ConnectorImportWizard import line from ImportPage.tsx (Plan 03 removed the JSX usage); delete ConnectorImportWizard.tsx + ConnectorImportWizard.test.tsx + ConnectorImportWizard.capabilities.test.ts.
    2. Remove the now-dead SyncTab import line from TranscriptsNew.tsx; delete SyncTab.tsx + SyncTab.registry.test.ts.
    3. Delete UnsyncedMeetingsSection.tsx + UnsyncedMeetingsSection.registry.test.ts and SyncedTranscriptsSection.tsx (their only consumer was SyncTab).
    4. Delete useSyncTabSelection.ts, useSyncTabOrchestration.ts, useSyncTabStateBridge.ts (their only consumer was SyncTab). If useSyncTabState.ts imports any of these, sever that import cleanly — useSyncTabState.ts is PRESERVED for Phase 27, NOT deleted.

    DO NOT delete connectorSearch.ts in this task (it has the orchestration consumer being deleted here + possibly ImportSurface) — that is Task 2 after confirming all consumers are gone. DO NOT delete SyncStatusIndicator.tsx, ActiveSyncJobsCard.tsx, or useSyncTabState.ts.

    After deletions, fix any remaining broken imports surfaced by tsc/build (sever dead substring-only refs vs. real imports per the grep list).
  </action>
  <verify>
    <automated>rtk proxy npx tsc -p tsconfig.app.json 2>&1 | tail -20 ; /usr/bin/grep -rln "ConnectorImportWizard|useSyncTabSelection|useSyncTabOrchestration|useSyncTabStateBridge" src/ 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Deleted files gone: ConnectorImportWizard.tsx, SyncTab.tsx, useSyncTabSelection.ts, useSyncTabOrchestration.ts, useSyncTabStateBridge.ts, UnsyncedMeetingsSection.tsx, SyncedTranscriptsSection.tsx all absent (verify with `/usr/bin/test ! -f <path>` for each).
    - No remaining source references to the deleted modules: `/usr/bin/grep -rlnE "ConnectorImportWizard|useSyncTabSelection|useSyncTabOrchestration|useSyncTabStateBridge" src/` returns nothing.
    - PRESERVED files still exist: `/usr/bin/test -f src/components/transcripts/SyncStatusIndicator.tsx && /usr/bin/test -f src/components/transcripts/ActiveSyncJobsCard.tsx && /usr/bin/test -f src/hooks/useSyncTabState.ts` all succeed.
    - tsc has no NEW errors introduced by the deletions (no dangling imports referencing deleted modules): review `rtk proxy npx tsc -p tsconfig.app.json` output.
  </acceptance_criteria>
  <done>Wizard, SyncTab, the folded sections, and the duplicate selection/orchestration/bridge hooks are deleted with coverage migrated; job-status UI + useSyncTabState preserved for Phase 27; no dangling imports.</done>
</task>

<task type="auto">
  <name>Task 2: Delete connectorSearch.ts (if dead) + [BLOCKING] build-green boot gate + full suite + lint:docs</name>
  <read_first>
    - src/components/import/ImportSurface.tsx (confirm whether it imports appendUniqueAvailableCalls / searchAllAvailableConnectorCalls from connectorSearch — if yes, connectorSearch is a LIVE dependency and must be KEPT)
    - src/components/connectors/connectorSearch.ts + connectorSearch.test.ts
    - src/lib/__tests__/oauth-callback-routing.test.ts (boot gate)
  </read_first>
  <files>src/components/connectors/connectorSearch.ts, src/components/connectors/__tests__/connectorSearch.test.ts</files>
  <action>
    Determine whether connectorSearch.ts still has ANY consumer after Task 1's deletions:
    - If ImportSurface.tsx (or anything else in src/) imports from connectorSearch, KEEP connectorSearch.ts + its test (it is a live dependency, not dead code — note this in the summary). Skip the delete and remove connectorSearch.ts/connectorSearch.test.ts from this plan's effective deletions.
    - If NOTHING imports it (both wizard and orchestration are gone and ImportSurface has its own paging), delete connectorSearch.ts + connectorSearch.test.ts (Pitfall 3 — delete LAST, only after all consumers gone).

    Then run the [BLOCKING] gates against the committed tree (the source-registry boot-crash guard from STATE.md Standing Constraints): commit the deletions, then run `npm run build` (boot-crash gate — OAUTH_CALLBACK_ROUTES must stay non-empty / no white screen), the OAUTH route test, the full vitest suite, and `npm run lint:docs`. The build MUST be green BEFORE this plan is considered complete and before any push.
  </action>
  <verify>
    <automated>/usr/bin/grep -rln "connectorSearch" src/ 2>/dev/null ; rtk proxy npm run build 2>&1 | tail -15 ; rtk proxy npx vitest run src/lib/__tests__/oauth-callback-routing.test.ts 2>&1 | tail -8 ; rtk proxy npm run lint:docs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - connectorSearch.ts is either deleted (if `/usr/bin/grep -rln "connectorSearch" src/` returns nothing after Task 1) OR kept-as-live-dependency with a documented reason in the summary (if ImportSurface imports it). One of the two outcomes, never a dangling import.
    - `rtk proxy npm run build` exits zero (boot-crash gate green; OAUTH_CALLBACK_ROUTES non-empty against the committed tree). This is the [BLOCKING] gate.
    - `rtk proxy npx vitest run src/lib/__tests__/oauth-callback-routing.test.ts` passes (OAUTH_CALLBACK_ROUTES.length assertion holds).
    - `rtk proxy npm run lint:docs` exits zero (no forbidden doc patterns).
    - Full suite green: `rtk proxy npx vitest run` reports no failures introduced by this plan (pre-existing unrelated baseline failures noted in deferred-items.md are out of scope).
  </acceptance_criteria>
  <done>connectorSearch.ts deleted (or kept as a documented live dependency); build green against the committed tree (boot gate passes); OAUTH route test, full suite, and lint:docs all green. TBL-03 complete — the fork is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed tree → prod boot | source-registry.ts / OAUTH_CALLBACK_ROUTES is load-bearing at mount; a deletion that zeroes/breaks it white-screens prod |
| uncommitted files → build | the uncommitted-files-as-real-code pattern can hide a broken registry until push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-DoS | Denial of Service | OAUTH_CALLBACK_ROUTES boot init after deletions | mitigate | [BLOCKING] `npm run build` against the COMMITTED tree + oauth-callback-routing.test.ts before completion/push (prod white-screen guard from STATE.md Standing Constraints). |
| T-26-04-T | Tampering | premature connectorSearch.ts delete | mitigate | Delete connectorSearch LAST, only after both consumers gone (Pitfall 3); keep it if ImportSurface depends on it — never leave a dangling import. |
| T-26-04-SC | Tampering | npm/connector churn | accept | No package installs in this plan; build gate covers boot integrity. |
</threat_model>

<verification>
- [BLOCKING] `rtk proxy npm run build` — zero exit against the committed tree (boot gate).
- `rtk proxy npx vitest run src/lib/__tests__/oauth-callback-routing.test.ts` — passes.
- `rtk proxy npx vitest run` — full suite green (no new failures).
- `rtk proxy npm run lint:docs` — zero exit.
- `/usr/bin/grep -rlnE "ConnectorImportWizard|useSyncTabSelection|useSyncTabOrchestration|useSyncTabStateBridge" src/` — empty.
</verification>

<success_criteria>
- The forked ConnectorImportWizard + duplicate useSyncTab* selection/orchestration/bridge hooks + folded sections are deleted; nothing imports them.
- connectorSearch.ts deleted (or documented as a live ImportSurface dependency) — no dangling imports.
- Job-status UI (SyncStatusIndicator, ActiveSyncJobsCard) and useSyncTabState preserved for Phase 27.
- Build green against the committed tree; OAUTH_CALLBACK_ROUTES non-empty; full suite + lint:docs green. TBL-03 done.
</success_criteria>

<output>
Create `.planning/phases/26-unified-import-surface/26-04-SUMMARY.md` when done.
</output>
