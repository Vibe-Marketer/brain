---
phase: 34
plan_id: 34-02
title: "BRAND-01: Reorder sidebar nav items (CALLS → IMPORT → RULES → PEOPLE → ORGANIZATION)"
wave: 1
depends_on: [34-01]
autonomous: true
requirements: [BRAND-01]
files_modified:
  - src/components/ui/sidebar-nav.tsx
---

# 34-02 — Sidebar Nav Order

Locked order from CONTEXT.md / REQUIREMENTS.md: **CALLS → IMPORT → RULES → PEOPLE → ORGANIZATION**.

Current order in `navItems` (sidebar-nav.tsx lines 61-116): Calls → People → Organization → Import → Rules. Re-order array.

## Tasks

<task id="34-02-1">
<read_first>
- src/components/ui/sidebar-nav.tsx (current order: Calls, People, Organization, Import, Rules)
</read_first>
<action>
In `src/components/ui/sidebar-nav.tsx`, reorder the `navItems` array entries so they appear in this exact order (top-to-bottom):

1. `home` (Calls) — keep as-is at index 0
2. `import` — move from index 3 to index 1
3. `rules` — move from index 4 to index 2
4. `people` — move from index 1 to index 3
5. `organization` — move from index 2 to index 4

Resulting array: `[home, import, rules, people, organization]`. Do not modify any individual item's content — only reorder.

Note: the feature-flag filter (`isFeatureEnabled('beta_imports')` gates `import` and `rules`) continues to work because it filters by `item.id`, not array position.
</action>
<acceptance_criteria>
- `awk '/const navItems/,/^\];/' src/components/ui/sidebar-nav.tsx | grep -nE "id: '(home|import|rules|people|organization)'"` returns 5 lines in this order: home, import, rules, people, organization.
- `grep -c "id: '" src/components/ui/sidebar-nav.tsx` unchanged (still 6 — 5 nav + 1 settings).
- `npx tsc --noEmit` exits 0.
</acceptance_criteria>
</task>

<task id="34-02-2">
<action>
```bash
git add src/components/ui/sidebar-nav.tsx
git commit -m "feat(34-02): reorder sidebar to CALLS → IMPORT → RULES → PEOPLE → ORGANIZATION (BRAND-01)"
```
</action>
<acceptance_criteria>
- `git log -1 --format='%s'` includes "34-02" and "BRAND-01".
</acceptance_criteria>
</task>

## Must-haves

- [ ] Nav items render in locked order in dev-browser
- [ ] TypeScript compiles
