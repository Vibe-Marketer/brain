---
phase: 27-observable-jobs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useSyncJobs.ts
  - src/hooks/__tests__/useSyncJobs.test.ts
autonomous: true
requirements: [JOB-01, JOB-03, JOB-04]
tags: [react, realtime, sync-jobs, hook, supabase]

must_haves:
  truths:
    - "Job status is readable on every <ImportSurface> (both tabs) via one shared hook"
    - "Job progress survives a page refresh (durable DB row, not volatile state)"
    - "Live updates arrive via Realtime postgres_changes with a polling fallback when the channel errors"
    - "Failed / completed_with_errors jobs do NOT auto-dismiss after 8s — they stay until explicit dismissal"
    - "Recording ids are strings end-to-end (matches live TEXT[] columns); no numeric coercion"
    - "Jobs are filtered to the surface's source_app and organizationId (no hardcoded 'fathom')"
  artifacts:
    - path: "src/hooks/useSyncJobs.ts"
      provides: "Shared Realtime+poll sync-jobs hook returning activeJobs/terminalJobs as string-id SyncJob[]"
      exports: ["useSyncJobs", "SyncJob"]
      min_lines: 120
    - path: "src/hooks/__tests__/useSyncJobs.test.ts"
      provides: "Unit coverage: string ids, source_app/org filter, channel lifecycle, poll fallback, no 8s timer"
  key_links:
    - from: "src/hooks/useSyncJobs.ts"
      to: "supabase.channel(...).on('postgres_changes', { table: 'sync_jobs' })"
      via: "Realtime subscription with user_id=eq filter"
      pattern: "postgres_changes"
    - from: "src/hooks/useSyncJobs.ts"
      to: "supabase.from('sync_jobs').select"
      via: "poll fallback + initial fetch"
      pattern: "from\\(['\"]sync_jobs['\"]\\)"
---

<objective>
Create the ONE shared `useSyncJobs` hook (JOB-01) by lifting the proven hybrid Realtime+poll machinery out of the PRESERVED `useSyncTabState.ts` into a clean, provider-agnostic hook — fixing the two carry-forward landmines (number[]→string[], hardcoded "fathom") and removing the 8s auto-dismiss (JOB-03) — with the Realtime `postgres_changes` subscription + polling fallback intact (JOB-04).

Purpose: Both the Import tab and Sync tab render the same `<ImportSurface>`; one hook gives both surfaces observable, refresh-surviving job status. This hook is the data source the Plan 27-03 banner/chip consume.
Output: `src/hooks/useSyncJobs.ts` + its unit test. This plan does NOT mount anything in `<ImportSurface>` (that is Plan 27-03) and does NOT touch the DB/edge function (that is Plan 27-02).
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-observable-jobs/27-CONTEXT.md
@.planning/phases/27-observable-jobs/27-RESEARCH.md

# The file to lift FROM (PRESERVED — do not delete; this plan creates the clean replacement):
@src/hooks/useSyncTabState.ts

# Frontend binding rules (dual-ID: never coerce; npm; Remix icons; motion/react):
@src/CLAUDE.md

<interfaces>
<!-- Live schema facts (verified Phase 24-04 introspection — DO NOT re-introspect): -->
<!-- sync_jobs columns: id (UUID), user_id (UUID), status (TEXT), type (TEXT), -->
<!--   created_at/updated_at/started_at/completed_at (TIMESTAMPTZ), error (TEXT), -->
<!--   recording_ids/synced_ids/failed_ids = TEXT[]  <-- STRING ids, NOT number[], -->
<!--   progress_current/progress_total (INT), skipped_count (INT), -->
<!--   source_app (TEXT), organization_id (UUID), workspace_id (UUID), source_id (UUID), -->
<!--   mode (TEXT), date_start/date_end (TIMESTAMPTZ), provider_cursor (TEXT), -->
<!--   last_heartbeat_at (TIMESTAMPTZ). -->
<!-- sync_jobs is in supabase_realtime (all columns); RLS = user_id policy OR org policy (live). -->

From src/hooks/useSyncTabState.ts (the proven mechanism to lift — see lines 214-358):
  - channel: supabase.channel(`sync_jobs_${user.id}`).on('postgres_changes', { event:'*', schema:'public', table:'sync_jobs', filter:`user_id=eq.${user.id}` }, handler).subscribe(statusCb)
  - statusCb: 'SUBSCRIBED' -> poll every 10000ms (backup); 'CLOSED'|'CHANNEL_ERROR' -> poll every 2000ms (fallback)
  - cleanup return: clearInterval(pollInterval); supabase.removeChannel(channel)
  - getSafeUser() from @/lib/auth-utils gives { user, error }

From @/hooks/useOrganizationContext: useOrganizationContext() -> { activeOrgId }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED — useSyncJobs unit test scaffold</name>
  <read_first>
    - src/hooks/useSyncTabState.ts (the effect at 214-358; the 8s setTimeout at 176-184; the number[] typing at 22-26; the hardcoded "fathom" at 209)
    - src/test/migrations/phase24-sync-status-foundation.integration.test.ts (for the project's mock/test idioms — but this is a UNIT test with a mocked supabase client, NOT integration)
    - one existing hook unit test using a mocked @/integrations/supabase/client (grep: `grep -rl "vi.mock.*supabase/client" src/hooks/__tests__/`)
  </read_first>
  <files>src/hooks/__tests__/useSyncJobs.test.ts</files>
  <behavior>
    - Test: hook accepts `{ sourceApp, organizationId }` and only surfaces rows whose source_app matches (a row with a different source_app is excluded). Use a mocked supabase client returning seeded rows.
    - Test: ids in returned jobs are typed/handled as string[] — seed synced_ids/failed_ids/recording_ids as ["143800259","zoom-uuid-abc"] and assert they pass through untouched (no parseInt/Number).
    - Test: a `failed` job and a `completed_with_errors` job appear in `terminalJobs` and are NOT removed after fake-advancing timers 8000ms+ (vi.useFakeTimers + vi.advanceTimersByTime(9000) — proves no 8s auto-dismiss). A `completed` (clean) job MAY be absent/fadeable.
    - Test: on mount the hook subscribes a channel and calls supabase.from('sync_jobs') at least once; on unmount it calls supabase.removeChannel and clears the poll interval.
    - Test: when the subscribe status callback fires 'CHANNEL_ERROR', the poll fallback interval is (re)established (assert setInterval invoked with the 2000ms fallback cadence).
  </behavior>
  <action>Write the RED unit test for the not-yet-created `useSyncJobs` hook using @testing-library/react `renderHook` + a `vi.mock('@/integrations/supabase/client', ...)` that exposes a fake `channel().on().subscribe()` capturing the status callback, and a `from('sync_jobs').select()...` chain returning seeded rows. Mock `@/lib/auth-utils` getSafeUser to return a stable user id. Mock `@/hooks/useOrganizationContext` to return a fixed activeOrgId. Seed rows MUST include mixed-source rows and STRING ids (one numeric-looking, one UUID-looking) to lock the dual-ID rule. Tests reference `useSyncJobs` and `SyncJob` from `@/hooks/useSyncJobs` (import will fail RED). Do not implement the hook in this task.</action>
  <verify>
    <automated>rtk proxy npx vitest run src/hooks/__tests__/useSyncJobs.test.ts</automated>
  </verify>
  <acceptance_criteria>Test file exists, imports `useSyncJobs`/`SyncJob` from `@/hooks/useSyncJobs`, and FAILS RED with a module-not-found / undefined-export error (proving the test runs and the hook does not yet exist). At least 5 test cases covering: source_app filter, string ids, no-8s-dismiss for failures, channel lifecycle (subscribe+removeChannel), poll fallback on CHANNEL_ERROR.</acceptance_criteria>
  <done>RED test committed; running it fails because the hook is unimplemented (not because the test itself is malformed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement useSyncJobs (string ids, real source_app/org filter, no 8s timer, Realtime+poll)</name>
  <read_first>
    - src/hooks/useSyncTabState.ts (lift the effect mechanism verbatim in structure; see 214-358)
    - src/lib/auth-utils.ts (getSafeUser shape)
    - src/hooks/useOrganizationContext.ts (activeOrgId)
    - the RED test from Task 1 (the exact contract to satisfy)
  </read_first>
  <files>src/hooks/useSyncJobs.ts</files>
  <action>
    Create `src/hooks/useSyncJobs.ts` exporting a `SyncJob` interface and a `useSyncJobs({ sourceApp, organizationId })` hook.

    SyncJob interface: type `recording_ids`, `synced_ids`, `failed_ids` as `string[] | null` (NEVER number[] — live columns are TEXT[]); include id, user_id, status, type, created_at, updated_at, started_at, completed_at, error, progress_current, progress_total, skipped_count, source_app, organization_id, workspace_id, source_id, mode, date_start, date_end, provider_cursor, last_heartbeat_at.

    Lift the hybrid Realtime+poll effect from useSyncTabState's effect (214-358) PRESERVING its structure: one channel `sync_jobs_${user.id}` filtered `user_id=eq.${user.id}`; INSERT adds, UPDATE merges, DELETE drops from the active list; subscribe status callback sets the poll cadence (10000ms backup when SUBSCRIBED, 2000ms fallback on CLOSED/CHANNEL_ERROR); cleanup clears the interval AND calls supabase.removeChannel. Keep `user_id=eq` as the ONLY channel filter (postgres_changes supports one predicate); apply source_app + organizationId narrowing CLIENT-SIDE inside the handler and in the poll query result, per RESEARCH Pattern 1 (RLS user-OR-org is the real isolation boundary).

    CHANGE three things vs the lifted source: (a) all id fields are string[] — remove the `Set<number>` and any String()/Number() coercion (drop the meeting-removal `removeNewlySyncedMeetings`/`processedSyncedIdsRef` logic entirely — that is SyncTab-specific and out of this hook's responsibility); (b) filter returned rows by `source_app === sourceApp` and (when organizationId is set) `organization_id === organizationId || organization_id == null` (legacy NULL-org rows stay visible, matching the live OR-combined RLS); (c) DELETE the 8000ms setTimeout auto-dismiss — do NOT re-add any timer that removes terminal jobs.

    Return shape: `{ activeJobs, terminalJobs }` where activeJobs = status in (pending, processing) and terminalJobs = status in (completed, failed, completed_with_errors). Failures/completed_with_errors persist in terminalJobs (dismissal is local UI concern, owned by Plan 27-03's banner — this hook never auto-removes terminal jobs). Optionally allow a clean `completed` job to be filtered out after a grace window IF you do so without a setTimeout that drops failures (discretion — but the test forbids dropping failures). Use `motion/react` only if needed elsewhere — not in this hook.

    Resolve sourceApp/organizationId from the hook args — there is NO literal "fathom" anywhere in this file (grep gate enforces it).
  </action>
  <verify>
    <automated>rtk proxy npx vitest run src/hooks/__tests__/useSyncJobs.test.ts && rtk proxy npx tsc -p tsconfig.app.json --noEmit 2>&1 | rtk grep -v '^#' | rtk grep -c "useSyncJobs" | rtk grep -q '^0$' && echo TSC_CLEAN_FOR_HOOK</automated>
  </verify>
  <acceptance_criteria>
    - Task 1's unit test goes GREEN (all cases pass).
    - `tsc -p tsconfig.app.json --noEmit` produces ZERO errors referencing `useSyncJobs.ts` (pre-existing unrelated errors in other files are deferred per 26-04).
    - `grep -v '^#' src/hooks/useSyncJobs.ts | grep -c '"fathom"\|parseInt\|Number(\|Set<number>'` returns 0 (no hardcoded provider, no numeric coercion).
    - The cleanup return calls `supabase.removeChannel` AND `clearInterval` (grep both present).
  </acceptance_criteria>
  <done>useSyncJobs is implemented, the unit suite is green, ids are strings, the source_app/org filter replaces the hardcoded "fathom", the 8s timer is gone, and Realtime+poll lifecycle is intact with proper cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Postgres (Realtime + REST read) | The hook subscribes to and polls `sync_jobs`; another tenant's job rows must never reach this client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-I | Information Disclosure | `useSyncJobs` Realtime subscription on sync_jobs | mitigate | Channel filter `user_id=eq.${user.id}` + RLS (user OR org policy, live per 24-04) is the isolation boundary; source_app/org narrowing is client-side ON TOP of RLS, never a substitute. sync_jobs is in CROSS_ORG_TABLES (CI RLS-regression gate). |
| T-27-01-I2 | Information Disclosure | Realtime DELETE events (unfilterable, bypass RLS) | mitigate | Never derive synced/status truth from DELETE; DELETE only drops a row from the local active list (same as the proven source). Status is driven by INSERT/UPDATE only. |
| T-27-01-T | Tampering | client-side id coercion | mitigate | Ids stay opaque string[]; no parseInt/Number — prevents the dual-ID drop bug for UUID providers (grep gate in acceptance). |
</threat_model>

<verification>
- `rtk proxy npx vitest run src/hooks/__tests__/useSyncJobs.test.ts` green.
- `rtk proxy npx tsc -p tsconfig.app.json --noEmit` — zero NEW errors in `useSyncJobs.ts`.
- Grep gates: no `"fathom"`, no numeric coercion, no 8000ms timer in the hook; `removeChannel` + `clearInterval` present in cleanup.
</verification>

<success_criteria>
A single `useSyncJobs({ sourceApp, organizationId })` hook returns string-id `activeJobs`/`terminalJobs` from `sync_jobs`, live-updated via Realtime with a polling fallback, scoped to the surface's provider+org, with terminal failures that never auto-vanish — ready for Plan 27-03 to render.
</success_criteria>

<output>
Create `.planning/phases/27-observable-jobs/27-01-SUMMARY.md` when done.
</output>
