---
phase: 27-observable-jobs
plan: 04
type: execute
wave: 3
depends_on: ["27-02", "27-03"]
files_modified: []
autonomous: false
requirements: [JOB-02]
tags: [blocking, prod-push, pg_cron, edge-deploy, phase-gate, supabase]

must_haves:
  truths:
    - "The sync-jobs-reaper cron exists in PROD (and TEST) — SELECT jobname FROM cron.job returns it"
    - "The sync-meetings edge function with heartbeat-at-INSERT/UPDATE is deployed to PROD"
    - "The reaper integration test passes against the TEST project (real DB, zero mocks)"
    - "The full unit suite has zero NEW failures vs the 26-04 baseline and npm run build exits 0"
  artifacts:
    - path: ".planning/phases/27-observable-jobs/27-04-SUMMARY.md"
      provides: "Record of prod-ref-verified migration push, edge deploy, cron presence, and phase-gate results"
  key_links:
    - from: "supabase/migrations/20260623120000_sync_jobs_reaper.sql"
      to: "PROD cron.job 'sync-jobs-reaper'"
      via: "supabase db push (prod-ref vltmrnjsubfzrgrtdqey verified first)"
      pattern: "sync-jobs-reaper"
    - from: "supabase/functions/sync-meetings/index.ts"
      to: "PROD edge runtime"
      via: "supabase functions deploy sync-meetings --use-api"
      pattern: "last_heartbeat_at"
---

<objective>
Ship the JOB-02 backend to production so the reaper actually runs and heartbeats actually get written, then run the phase gate. The reaper migration + the sync-meetings heartbeat change are NO-OPs until they reach prod — writing the files alone does nothing (RESEARCH Runtime State Inventory: the `cron.job` table won't have `sync-jobs-reaper` until the migration is pushed). The frontend (Plans 27-01/27-03) is batched to milestone end and is NOT pushed here — but it MUST build green.

Purpose: Make a dead job a visible failed job IN PRODUCTION, and gate the phase as done.
Output: prod-ref-verified migration push (PROD + TEST), edge deploy to PROD, cron-presence + reaper-test + build/suite gate results recorded in the SUMMARY. No source files change in this plan.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-observable-jobs/27-RESEARCH.md

# Push/deploy discipline + prod-ref guard:
@supabase/CLAUDE.md
@CLAUDE.md

<interfaces>
<!-- PROD Supabase ref: vltmrnjsubfzrgrtdqey (migrations read DATABASE_URL from .env). -->
<!-- TEST Supabase ref: swjzxiddcrtaqixsfaac (.env.test). -->
<!-- Edge deploy (Docker NOT running): supabase functions deploy sync-meetings --use-api -->
<!-- Cron presence check: SELECT jobname FROM cron.job WHERE jobname = 'sync-jobs-reaper'; (expect 1 row) -->
<!-- 26-04 baseline (full vitest run): 7 files / 21 tests failing — known pre-existing flake. Zero NEW failures allowed. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Push reaper migration to TEST, run reaper test, then push to PROD + deploy edge fn</name>
  <files>supabase/migrations/20260623120000_sync_jobs_reaper.sql, supabase/functions/sync-meetings/index.ts</files>
  <what-built>
    Plan 27-02 created the additive reaper migration (`supabase/migrations/20260623120000_sync_jobs_reaper.sql`) and added `last_heartbeat_at` writes to `sync-meetings/index.ts`, plus a TEST-DB reaper integration test. These are inert until pushed/deployed.
  </what-built>
  <action>
    Claude AUTOMATES every step (it does the work — this is the human approval gate for the prod-write, per CLAUDE.md destructive-work rule), in order:
    1. PRE-PUSH SAFETY: `grep -iE "ALTER TABLE|DROP|RENAME|ALTER COLUMN" supabase/migrations/20260623120000_sync_jobs_reaper.sql` returns nothing (strictly additive).
    2. TEST FIRST: `supabase link --project-ref swjzxiddcrtaqixsfaac` then `supabase db push --linked`; then `rtk proxy npm run test:integration -- phase27-sync-jobs-reaper` MUST pass.
    3. PROD-REF GUARD: read DATABASE_URL from `.env`, assert it contains `vltmrnjsubfzrgrtdqey` BEFORE connecting (same guard as the autopilot gsd-runner). Abort on mismatch.
    4. PUSH TO PROD: `supabase db push` against the prod ref (additive); verify `SELECT jobname FROM cron.job WHERE jobname='sync-jobs-reaper'` = 1 row on PROD and TEST.
    5. DEPLOY EDGE FN: `supabase functions deploy sync-meetings --use-api` (Docker not running — `--use-api` mandatory); confirm success.
    The human approves the prod write (3→4) and the edge deploy (5).
  </action>
  <how-to-verify>
    Claude will AUTOMATE every step it can (it does the work — this checkpoint is the human approval for the prod-write, per CLAUDE.md destructive-work rule). Sequence:

    1. PRE-PUSH SAFETY: confirm the migration is strictly additive (no ALTER/DROP/retype) — `grep -iE "ALTER TABLE|DROP|RENAME|ALTER COLUMN" supabase/migrations/20260623120000_sync_jobs_reaper.sql` returns nothing.
    2. TEST FIRST: apply migrations to the TEST project (`supabase link --project-ref swjzxiddcrtaqixsfaac` then `supabase db push --linked`), then run `rtk proxy npm run test:integration -- phase27-sync-jobs-reaper` — MUST pass (stale + NULL-old reaped; fresh + NULL-young spared; idempotent). This proves the reaper SQL is correct before it ever touches prod.
    3. PROD-REF GUARD: verify the prod connection target contains `vltmrnjsubfzrgrtdqey` BEFORE connecting (read DATABASE_URL from `.env`, assert the ref substring — same guard as the autopilot gsd-runner). Abort if it does not match.
    4. PUSH TO PROD: `supabase db push` against the prod ref (additive). Then verify the cron exists: `SELECT jobname FROM cron.job WHERE jobname='sync-jobs-reaper';` returns exactly 1 row on PROD (and on TEST).
    5. DEPLOY EDGE FN: `supabase functions deploy sync-meetings --use-api` (Docker not running — `--use-api` is mandatory). Confirm a successful deploy response.

    Human: approve the PROD write (step 3→4) and the edge deploy (step 5). This is a one-way prod change; the migration is additive and the reaper is idempotent, but the prod-write gate is required by CLAUDE.md.
  </how-to-verify>
  <verify>
    <automated>grep -iE "ALTER TABLE|DROP|RENAME|ALTER COLUMN" supabase/migrations/20260623120000_sync_jobs_reaper.sql | wc -l | grep -q '^ *0$' && echo ADDITIVE_OK</automated>
  </verify>
  <done>Reaper migration pushed to PROD + TEST (prod-ref guard matched vltmrnjsubfzrgrtdqey first), `sync-jobs-reaper` present in cron.job on both, reaper integration test green against TEST, sync-meetings deployed to PROD via --use-api.</done>
  <resume-signal>Type "approved" to authorize the PROD migration push + edge deploy, or describe concerns. (Legitimacy note: zero new packages this phase — no package-legitimacy checkpoint required; this gate is purely the prod-write authorization.)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Phase gate — build green + zero new unit-suite failures + record</name>
  <read_first>
    - .planning/phases/26-unified-import-surface/deferred-items.md (the pre-existing baseline failures + pre-existing tsc errors that are OUT of scope)
    - all four 27-* SUMMARYs (to roll up the phase result)
  </read_first>
  <files>.planning/phases/27-observable-jobs/27-04-SUMMARY.md</files>
  <action>
    Run the phase gate and record results:
    - `rtk proxy npm run build` → MUST exit 0 (frontend builds green even though it is not pushed this phase — operator batches frontend to milestone end).
    - `rtk proxy npm run test` (full unit suite) → diff failures vs the 26-04 baseline (7 files / 21 tests, the known flake). Zero NEW failures allowed; document any delta and prove pre-existing (isolation flake) if the count moves.
    - Confirm `SELECT jobname FROM cron.job WHERE jobname='sync-jobs-reaper'` returned 1 on PROD + TEST (from Task 1) and the edge deploy succeeded.
    - Write `27-04-SUMMARY.md` recording: prod-ref-verified push, cron presence (PROD+TEST), edge deploy result, reaper integration-test result, build exit code, unit-suite delta. Note the frontend is intentionally NOT pushed (milestone-end batch) but builds green.
  </action>
  <verify>
    <automated>rtk proxy npm run build 2>&1 | rtk grep -iE "built in|error|exit" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - Full unit suite shows zero NEW failures vs the documented 26-04 baseline (any delta proven pre-existing).
    - SUMMARY records the cron presence on PROD+TEST, the successful edge deploy, and the green reaper integration test.
  </acceptance_criteria>
  <done>Phase 27 backend is live in prod (reaper cron present, heartbeat deployed), the reaper test passed against TEST, the frontend builds green, and the phase result is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev → PROD database / Edge runtime | A one-way prod write (migration + cron registration) and an edge deploy cross into production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04-T | Tampering (wrong-DB push) | `supabase db push` targeting prod | mitigate | Prod-ref guard asserts DATABASE_URL contains `vltmrnjsubfzrgrtdqey` before connect (the 2026-05 test-mutated-prod incident class); TEST-first push + reaper test before prod; additive-only verified by grep; blocking-human approval. |
| T-27-04-T2 | Tampering (untested reaper in prod) | reaper SQL correctness | mitigate | Reaper proven against the real TEST DB (Plan 27-02 test) BEFORE the prod push; idempotent so re-runs are no-ops. |
| T-27-04-D | Denial of Service (bad edge deploy) | sync-meetings deploy | mitigate | `--use-api` (Docker-less, the only reliable path here); deploy response confirmed; heartbeat change is additive (one field on existing writes) — no behavior regression to the import path. |
| T-27-04-SC | Tampering | package installs | n/a | Zero new packages this phase — no legitimacy gate. |
</threat_model>

<verification>
- Prod-ref guard matched `vltmrnjsubfzrgrtdqey` before push; TEST reaper test green before prod push.
- `SELECT jobname FROM cron.job WHERE jobname='sync-jobs-reaper'` = 1 row on PROD and TEST.
- `supabase functions deploy sync-meetings --use-api` succeeded.
- `npm run build` exit 0; full unit suite zero NEW failures vs 26-04 baseline.
</verification>

<success_criteria>
The reaper cron is live in production and TEST, the heartbeat-writing sync-meetings is deployed to prod, the reaper is proven against the real TEST DB, and the (unpushed but green-building) frontend leaves the phase ready for milestone-end batch deploy.
</success_criteria>

<output>
Create `.planning/phases/27-observable-jobs/27-04-SUMMARY.md` when done.
</output>
