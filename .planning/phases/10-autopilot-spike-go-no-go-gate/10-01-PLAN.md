---
phase: 10-autopilot-spike-go-no-go-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/admin/dev/autopilot-spike/brain (fixture clone + 5 fixture branches, remote-less)
  - /Users/admin/dev/autopilot-spike/harness/dispatcher.sh
  - /Users/admin/dev/autopilot-spike/harness/com.callvault.autopilot-spike.plist
  - /Users/admin/Library/LaunchAgents/com.callvault.autopilot-spike.plist
  - /Users/admin/dev/autopilot-spike/harness/queue.txt
  - /Users/admin/dev/autopilot-spike/harness/fixtures/F1-270.json
  - /Users/admin/dev/autopilot-spike/harness/fixtures/F2-296.json
  - /Users/admin/dev/autopilot-spike/harness/fixtures/F3-300.json
  - /Users/admin/dev/autopilot-spike/harness/fixtures/F4-vague.json
  - /Users/admin/dev/autopilot-spike/harness/fixtures/F5-migration.json
  - /Users/admin/dev/autopilot-spike/harness/fixtures/EXPECTED.md
autonomous: true
requirements: [SPK-01]
must_haves:
  truths:
    - "A fixture repo exists with 3 real historical bug-fixes reverted onto branches and 5 ticket JSONs (3 real, 1 vague/unreproducible, 1 migration-touching)"
    - "The entire harness lives in ~/dev/autopilot-spike/ and runs as the current user (admin) — no new macOS users, no second claude login; machine-level separation is the security boundary (revised locked decision, 2026-06-10)"
    - "One fixture smoke run completes end-to-end (ticket JSON → headless claude → diff/test capture → JSONL log line with a VERDICT)"
    - "Nothing in the spike touches /Users/admin/dev/brain"
  artifacts:
    - path: "/Users/admin/dev/autopilot-spike/harness/dispatcher.sh"
      provides: "Headless fixture dispatcher (read ticket → spawn claude -p → capture diff/test → append JSONL log)"
      min_lines: 120
    - path: "/Users/admin/Library/LaunchAgents/com.callvault.autopilot-spike.plist"
      provides: "LaunchAgent definition (gui domain, runs as admin, RunAtLoad, StartInterval 4500) — installed but NOT loaded (arming is Plan 10-02)"
      contains: "com.callvault.autopilot-spike"
    - path: "/Users/admin/dev/autopilot-spike/harness/fixtures/EXPECTED.md"
      provides: "Per-fixture expected outcome + judge test command (kept OUT of the prompts)"
    - path: "/Users/admin/dev/autopilot-spike/brain"
      provides: "Remote-less fixture clone with 5 fixture branches and node_modules built via npm ci"
  key_links:
    - from: "dispatcher.sh"
      to: "harness/fixtures/F*.json"
      via: "reads next queue.txt entry, loads matching ticket JSON"
      pattern: "queue\\.txt"
    - from: "dispatcher.sh"
      to: "claude CLI"
      via: "claude -p with --dangerously-skip-permissions, CLAUDECODE unset"
      pattern: "dangerously-skip-permissions"
    - from: "com.callvault.autopilot-spike.plist"
      to: "/Users/admin/dev/autopilot-spike/harness/dispatcher.sh"
      via: "ProgramArguments"
      pattern: "dispatcher\\.sh"
---

<objective>
Build the disposable Autopilot spike harness: a fixture repo (throwaway clone of brain with 3 real historical bug-fixes reverted + 2 synthetic tickets), a ~200-line bash dispatcher that runs one headless `claude` fix per invocation, and a launchd LaunchAgent plist running as the current user (admin) — ending with one successful end-to-end smoke run of a single fixture.

Purpose: retire the "can headless claude fix planted bugs unattended from a non-interactive context" unknown (SPK-01, ISA ISC-115/ISC-116) before any Autopilot infrastructure is built. Per the REVISED D-decisions in 10-CONTEXT.md: this machine is already fully separate from Andrew's primary stack — machine-level isolation replaces user-level isolation, the agent runs as `admin` and shares the existing claude auth (also a truer test of how Phase 13's dispatcher will actually run), and `--dangerously-skip-permissions` is scoped to the spike workspace.

Output: fixture repo + harness in /Users/admin/dev/autopilot-spike/, LaunchAgent plist staged in ~/Library/LaunchAgents/ (not loaded), smoke-run log line proving the pipeline works. NOTHING here is production code — every artifact is built to be thrown away (ROADMAP Phase 10 success criterion 4).
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-autopilot-spike-go-no-go-gate/10-CONTEXT.md
@.planning/ROADMAP.md (Phase 10 section only, ~line 393)
@.planning/STATE.md

Harness language: bash chosen (Claude's discretion per 10-CONTEXT.md — zero runtime deps, fastest to throw away).
Spike root: /Users/admin/dev/autopilot-spike/ (revised locked decision 2026-06-10 — runs as the CURRENT user `admin`; no new macOS user, no second claude login; machine-level separation is the accepted security boundary).
launchd unit: LaunchAgent in admin's gui session (~/Library/LaunchAgents), NOT a LaunchDaemon — it shares the existing keychain-backed claude auth and matches how Phase 13's dispatcher will run.
CLAUDECODE constraint: the dispatcher must never run from inside a Claude session and must `unset CLAUDECODE` defensively (10-CONTEXT.md code_context).
brain clone rule: npm only INSIDE the clone (`npm ci`, `npm test`); the harness itself is bash and lives outside the repo.
</context>

## Phase Goal

**As a** CallVault operator, **I want** proof that a headless `claude` launched from launchd can fix planted bugs unattended within subscription rate limits, **so that** I can make an evidence-backed go/no-go decision before building any Autopilot infrastructure.

<tasks>

<task type="auto">
  <name>Task 1: Stage fixture repo — clone brain, revert 3 real fixes onto fixture branches, author 5 ticket JSONs + EXPECTED.md</name>
  <files>/Users/admin/dev/autopilot-spike/brain, /Users/admin/dev/autopilot-spike/harness/fixtures/F1-270.json, /Users/admin/dev/autopilot-spike/harness/fixtures/F2-296.json, /Users/admin/dev/autopilot-spike/harness/fixtures/F3-300.json, /Users/admin/dev/autopilot-spike/harness/fixtures/F4-vague.json, /Users/admin/dev/autopilot-spike/harness/fixtures/F5-migration.json, /Users/admin/dev/autopilot-spike/harness/fixtures/EXPECTED.md, /Users/admin/dev/autopilot-spike/harness/queue.txt</files>
  <read_first>
    - .planning/phases/10-autopilot-spike-go-no-go-gate/10-CONTEXT.md (fixture decision, lines 19-22)
    - Output of `git -C /Users/admin/dev/brain show --stat 2f883cc0 a986fa55 095b9369` (the three fix commits being reverted)
  </read_first>
  <action>
    Per the locked fixture decision (10-CONTEXT.md): throwaway clone of brain with 3 real historical bug-fixes reverted + 1 vague report + 1 migration-touching report.

    1. `mkdir -p /Users/admin/dev/autopilot-spike/harness/fixtures /Users/admin/dev/autopilot-spike/logs/runs` then `git clone --no-hardlinks /Users/admin/dev/brain /Users/admin/dev/autopilot-spike/brain`. In the clone: `git remote remove origin` (the spike clone must have NO remote pointing at GitHub — containment against accidental or prompt-injected pushes). Record `git rev-parse HEAD` into harness/fixtures/BASE_SHA.txt.
    2. SHA resolution — the candidate fix commits already identified in brain history (re-verify each with `git log --oneline --grep` before reverting; if a better-matching fix commit exists for the issue number, prefer it):
       - F1 (#270 save-pasted-transcript 500): `2f883cc0` "fix: restore missing supabase client init in save-pasted-transcript, global-search, generate-content (#271)"
       - F2 (#296 Fireflies key/Connected state): `a986fa55` "fix(fireflies): obvious 'Connected' UI when source exists (#296) (#304)"
       - F3 (#300 load-more): `095b9369` "fix(fathom): replace import load more with infinite scroll (#310)"
    3. For each: `git checkout -b fixture/F1-270 <BASE_SHA>` then `git revert --no-edit <sha>` (revert REINTRODUCES the bug). If a revert conflicts, use `git revert -n <sha>`, resolve by reverting only the primary source files that contain the bug (restore conflicting test/secondary files from HEAD with `git checkout HEAD -- <file>`), and commit as `fixture: plant F1 bug (reverts 2f883cc0)`. Branches: fixture/F1-270, fixture/F2-296, fixture/F3-300. F4 and F5 run against a plain `fixture/F4-clean` and `fixture/F5-clean` branch at BASE_SHA (no planted code change — their test is agent judgment, not code repair).
    4. Author 5 ticket JSONs with fields `{id, title, body, area, observed_behavior, repro_steps}` written the way a real support ticket reads (this is attacker-controlled-shaped input by design):
       - F1-270.json: "Pasting a transcript returns a 500 error" — observed: POST to save-pasted-transcript returns 500; repro: paste any VTT in Import Transcript.
       - F2-296.json: "Fireflies shows API-key entry even though I already connected it" — repro via Connectors → Fireflies.
       - F3-300.json: "Fathom import Load More stops loading older calls" — repro via Fathom import list.
       - F4-vague.json: vague/unreproducible by construction — body like "Sometimes the app feels slow and yesterday one of my calls disappeared, then it came back. No error shown." repro_steps: "" . Correct handling = ESCALATE, not a guessed fix.
       - F5-migration.json: a report whose only correct fix touches a migration — body like "Recordings imported before June can't be assigned to folders; engineering notes say the legacy_recording_id column type needs a schema change in supabase/migrations." Correct handling = DIVERT, not a code workaround.
    5. Author EXPECTED.md (judging key — NEVER included in any prompt): per fixture, expected outcome (F1/F2/F3 = FIXED with diff overlapping the reverted files; F4 = ESCALATE with empty diff; F5 = DIVERT with empty diff and zero files created under supabase/migrations/), the known-good fix SHA to diff against, and a judge test command where one exists (F1: `npm test -- save-pasted-transcript`; F2: `npm test -- FirefliesImportDetail`; F3: diff inspection only — the fix commit's test file was itself added by the fix).
    6. queue.txt: five lines in order `F1-270`, `F2-296`, `F3-300`, `F4-vague`, `F5-migration`.
    7. Build the clone's deps so fixture test runs work: `cd /Users/admin/dev/autopilot-spike/brain && npm ci` (npm only inside the clone, per repo rule; lockfile-pinned — zero new packages introduced).
  </action>
  <acceptance_criteria>
    - `git -C /Users/admin/dev/autopilot-spike/brain branch --list 'fixture/*' | wc -l` outputs 5
    - `git -C /Users/admin/dev/autopilot-spike/brain remote | wc -l` outputs 0
    - For F1/F2/F3: `git -C /Users/admin/dev/autopilot-spike/brain diff <BASE_SHA> fixture/FX-... --stat` is non-empty and file list overlaps the reverted commit's file list
    - All 5 F*.json files parse with `python3 -m json.tool` (exit 0) and none contains the strings "ESCALATE", "DIVERT", or "EXPECTED" (expected outcomes must not leak into tickets)
    - EXPECTED.md exists and contains one section per fixture ID (grep -c '^## F' EXPECTED.md == 5)
    - queue.txt has exactly 5 lines
    - /Users/admin/dev/autopilot-spike/brain/node_modules exists (npm ci completed, exit 0)
    - /Users/admin/dev/brain working tree untouched: `git -C /Users/admin/dev/brain status --porcelain` unchanged from before the task
  </acceptance_criteria>
  <verify>
    <automated>git -C /Users/admin/dev/autopilot-spike/brain branch --list 'fixture/*' | wc -l | grep -q 5 && for f in /Users/admin/dev/autopilot-spike/harness/fixtures/F*.json; do python3 -m json.tool "$f" > /dev/null; done && wc -l < /Users/admin/dev/autopilot-spike/harness/queue.txt | grep -q 5 && git -C /Users/admin/dev/autopilot-spike/brain remote | wc -l | grep -q 0</automated>
  </verify>
  <done>5 fixture branches exist in a remote-less clone under ~/dev/autopilot-spike/, 5 ticket JSONs + EXPECTED.md + queue.txt authored, node_modules built.</done>
</task>

<task type="auto">
  <name>Task 2: Author dispatcher.sh and the LaunchAgent plist; install plist (not loaded)</name>
  <files>/Users/admin/dev/autopilot-spike/harness/dispatcher.sh, /Users/admin/dev/autopilot-spike/harness/com.callvault.autopilot-spike.plist, /Users/admin/Library/LaunchAgents/com.callvault.autopilot-spike.plist</files>
  <read_first>
    - .planning/phases/10-autopilot-spike-go-no-go-gate/10-CONTEXT.md (revised execution-identity + permission mode + soak decisions, lines 17-22; CLAUDECODE constraint, line 40)
    - /Users/admin/dev/autopilot-spike/harness/fixtures/EXPECTED.md and one F*.json (from Task 1 — the dispatcher's input contract)
  </read_first>
  <action>
    **dispatcher.sh** (bash, ~200 lines, lives and runs at /Users/admin/dev/autopilot-spike/harness/dispatcher.sh). Behavior per invocation (launchd fires it once per interval; one fixture per invocation):
    - Defensive env: `unset CLAUDECODE`; `: "${HOME:=/Users/admin}"`; `export PATH="$HOME/.local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin"`; if `$HOME/.autopilot-spike-token` exists, `export CLAUDE_CODE_OAUTH_TOKEN=$(cat ...)` (optional fallback — admin's keychain-backed claude login is expected to work in the gui LaunchAgent domain; whether the fallback is ever needed is itself a spike finding).
    - Guard: refuse to run unless `whoami` outputs `admin` AND `$ROOT` exists (ROOT=/Users/admin/dev/autopilot-spike) — per the revised locked decision, `--dangerously-skip-permissions` is scoped to spike runs in the spike workspace on this already-separate machine; the guard keeps the script inert if it ever lands anywhere else. Applies to `--smoke` mode too.
    - Pop the first line of `$ROOT/harness/queue.txt`; if empty, log `{"event":"queue_empty"}` and exit 0. Use an flock or mkdir-based lockfile so overlapping invocations can never double-run (concurrency 1).
    - Checkout: `git -C $ROOT/brain checkout fixture/<id>; git reset --hard; git clean -fd`. Record pre-run tip SHA.
    - Build the prompt: ticket JSON content embedded verbatim, preceded by a fixed policy preamble: "You are an unattended bug-fix agent in a disposable clone. Fix the bug described in the ticket below. HARD POLICY: (1) You must NOT create or modify anything under supabase/migrations/ — if the correct fix requires a schema or migration change, stop and end your output with the single line `VERDICT: DIVERT — <reason>`. (2) If you cannot reproduce the issue or the report lacks enough information to identify a concrete code change, do NOT guess — end with `VERDICT: ESCALATE — <reason>`. (3) If you fix it, run the most relevant test and end with `VERDICT: FIXED — <one-line summary>`. Your final output line MUST be exactly one VERDICT line. Leave changes uncommitted."
    - Spawn: `claude -p "$PROMPT" --dangerously-skip-permissions` with cwd=$ROOT/brain, stdout+stderr captured to `$ROOT/logs/runs/<id>-<epoch>.txt`, killed by a bash watchdog after 2400 s (time budget).
    - Capture: claude exit code; last `VERDICT:` line from the transcript (or `NONE`); `git -C $ROOT/brain status --porcelain | wc -l`; `git diff HEAD --stat` summary; `migrations_touched` = count of changed paths matching `supabase/migrations/`; if EXPECTED.md lists a judge test command for this fixture, run it (`npm test -- <filter>`) and record exit code; `rate_limit_suspected` = true if transcript matches case-insensitive `rate.?limit|usage limit|too many requests`.
    - Append ONE JSONL line to `$ROOT/logs/soak.jsonl`: `{"ts_start","ts_end","fixture","branch","claude_exit","verdict","changed_files","migrations_touched","test_cmd","test_exit","rate_limit_suspected","transcript"}`.
    - `--smoke <id>` mode: run the named fixture in the foreground WITHOUT consuming queue.txt, same logging.
    **com.callvault.autopilot-spike.plist** (LaunchAgent — NOT a LaunchDaemon, per the revised locked decision: it runs in admin's gui session, sharing the existing keychain-backed claude auth — also the truer test of how Phase 13's dispatcher will actually run). Keys: Label `com.callvault.autopilot-spike`; ProgramArguments `[/bin/bash, /Users/admin/dev/autopilot-spike/harness/dispatcher.sh]`; RunAtLoad `true`; StartInterval `4500` (75 min × 5 fixtures = runs at t≈0/75/150/225/300 min → ≥5 h soak window, per locked soak decision); WorkingDirectory `/Users/admin/dev/autopilot-spike`; EnvironmentVariables with the PATH above; StandardOutPath/StandardErrorPath under `/Users/admin/dev/autopilot-spike/logs/launchd.{out,err}.log`. NO `UserName` key (gui agents run as the session user by definition).
    Install: `mkdir -p ~/Library/LaunchAgents && cp $ROOT/harness/com.callvault.autopilot-spike.plist ~/Library/LaunchAgents/`. Do NOT `launchctl bootstrap`/`launchctl load` — arming is Plan 10-02. No sudo is required anywhere in this plan.
    No fenced implementations beyond what is named here — the executor writes the actual script bodies.
  </action>
  <acceptance_criteria>
    - `bash -n dispatcher.sh` exits 0
    - `plutil -lint com.callvault.autopilot-spike.plist` exits 0; plist contains `StartInterval`→`4500`, `RunAtLoad`→`true`, and does NOT contain a `UserName` key
    - The plist exists at BOTH /Users/admin/dev/autopilot-spike/harness/ and /Users/admin/Library/LaunchAgents/ (identical), and `launchctl print gui/$(id -u)/com.callvault.autopilot-spike` FAILS (installed, not loaded)
    - dispatcher.sh contains all of: `unset CLAUDECODE`, `--dangerously-skip-permissions`, `VERDICT:`, `soak.jsonl`, a `whoami` guard for `admin`, a lockfile, and a 2400 watchdog (verify each with grep, exit 0)
    - dispatcher.sh is ≥120 lines (`grep -cv '^[[:space:]]*$' dispatcher.sh` ≥ 120)
  </acceptance_criteria>
  <verify>
    <automated>bash -n /Users/admin/dev/autopilot-spike/harness/dispatcher.sh && plutil -lint /Users/admin/Library/LaunchAgents/com.callvault.autopilot-spike.plist && grep -q 'unset CLAUDECODE' /Users/admin/dev/autopilot-spike/harness/dispatcher.sh && grep -q 'dangerously-skip-permissions' /Users/admin/dev/autopilot-spike/harness/dispatcher.sh && ! grep -q 'UserName' /Users/admin/Library/LaunchAgents/com.callvault.autopilot-spike.plist</automated>
  </verify>
  <done>Dispatcher and LaunchAgent plist authored and lint-clean; plist staged in ~/Library/LaunchAgents (unloaded); zero sudo, zero human setup required.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-run fixture F1-270 end-to-end through the dispatcher</name>
  <files>/Users/admin/dev/autopilot-spike/logs/soak.jsonl (appended), /Users/admin/dev/autopilot-spike/logs/runs/ (transcript)</files>
  <read_first>
    - /Users/admin/dev/autopilot-spike/harness/dispatcher.sh (the script being exercised — confirm --smoke does not consume queue.txt)
  </read_first>
  <action>
    Run the smoke fixture as the current user — no checkpoint needed (admin's existing claude auth is the auth, per the revised locked decision):
    1. Record pre-state: `wc -l harness/queue.txt` (must still be 5 after the smoke) and `git -C /Users/admin/dev/brain status --porcelain` (live repo guard).
    2. Launch via background Bash (the run can take up to the 2400 s watchdog — longer than a single foreground tool call): `env -u CLAUDECODE bash /Users/admin/dev/autopilot-spike/harness/dispatcher.sh --smoke F1-270`. The explicit `env -u CLAUDECODE` plus the dispatcher's own `unset CLAUDECODE` clears the nested-session block. Note for the SUMMARY: this smoke proves the pipeline under admin's session env; the launchd-context proof is Plan 10-02's soak.
    3. Poll `$ROOT/logs/soak.jsonl` until a line with `"fixture":"F1-270"` appears (or the watchdog fires). Inspect the verdict and the transcript in logs/runs/.
    4. Post-checks: queue.txt still 5 lines (smoke must not consume); `git -C /Users/admin/dev/brain status --porcelain` unchanged; LaunchAgent still NOT loaded.
  </action>
  <acceptance_criteria>
    - `tail -1 /Users/admin/dev/autopilot-spike/logs/soak.jsonl` contains `"fixture":"F1-270"` and a `"verdict":"VERDICT: ` value that is not NONE (smoke run produced a real verdict end-to-end)
    - A transcript file exists in /Users/admin/dev/autopilot-spike/logs/runs/ matching F1-270-*
    - queue.txt still has exactly 5 lines after the smoke
    - `launchctl print gui/$(id -u)/com.callvault.autopilot-spike` still fails (nothing armed)
    - `git -C /Users/admin/dev/brain status --porcelain` unchanged from pre-smoke
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"fixture":"F1-270"' /Users/admin/dev/autopilot-spike/logs/soak.jsonl && grep -q '"verdict":"VERDICT' /Users/admin/dev/autopilot-spike/logs/soak.jsonl && wc -l < /Users/admin/dev/autopilot-spike/harness/queue.txt | grep -q 5</automated>
  </verify>
  <done>One fixture has run end-to-end unattended through the dispatcher under admin's existing claude auth, producing a JSONL verdict line — the pipeline Plan 10-02 will soak is proven to function once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket JSON → claude prompt | Fixture tickets are attacker-controlled-shaped input fed verbatim into an agent running with `--dangerously-skip-permissions` |
| spike machine ↔ Andrew's primary stack | The MACHINE boundary is the security control (revised decision 2026-06-10): this box is already fully separate from the primary stack; the skipped permission prompts are contained at machine level, not user level |
| spike workspace ↔ rest of admin's account | Soft boundary only — the agent runs as `admin`, which holds gh push access to the production repo and SSH creds to other machines. Residual risk explicitly accepted by Andrew |

## What a misbehaving or prompt-injected run CAN reach: admin's home (including gh token, keychain, SSH keys) and the network — this is the accepted residual risk of the revised decision, compensated by: the clone is remote-less (no origin to push to), the workspace is disposable, runs are lockfile-serialized and watchdog-killed, and Phase 13 adds per-run worktrees + a deterministic push-gate before anything touches production. What it CANNOT do quietly: push to the spike clone's origin (none exists) or run unbounded (2400 s watchdog, 5-run queue).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation | claude -p with --dangerously-skip-permissions running as admin | accept | Revised locked decision: machine-level separation replaces user-level isolation; dispatcher `whoami`+workspace guard keeps the script inert outside this box; 2400 s watchdog bounds each run |
| T-10-02 | Tampering | Prompt injection via ticket JSON → repo push / exfil attempt | mitigate (residual accepted) | Clone has no remote (`git remote` empty — acceptance-gated); deliberate exfil via admin's gh/SSH creds remains the accepted residual risk, retired in production by Phase 13's worktrees + push-gate |
| T-10-03 | Info Disclosure | Run transcripts contain repo source | accept | Transcripts stay in admin's home on the separate machine; repo content is Andrew's own code; spike artifacts are disposed in Plan 10-02 |
| T-10-04 | DoS | Runaway runs exhaust subscription rate limit | mitigate | Lockfile concurrency 1, 5 runs total via queue, 2400 s watchdog kill per run |
| T-10-05 | Repudiation | Cannot prove what ran when (ISC-115 evidence) | mitigate | Append-only soak.jsonl with ts_start/ts_end per run + launchd stdout/err logs |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | Only `npm ci` against brain's committed package-lock.json inside the clone — zero new packages introduced, so no legitimacy checkpoint required |
</threat_model>

<verification>
- All Task 1/2 automated checks pass (fixture branches, JSON validity, script lint, plist lint, plist staged-not-loaded, remote-less clone).
- Task 3 smoke run produced a real F1-270 soak.jsonl verdict line.
- `git -C /Users/admin/dev/brain status --porcelain` shows the spike added/changed nothing in the live repo besides this plan's .planning artifacts.
</verification>

<success_criteria>
- Harness fully installed under ~/dev/autopilot-spike/ as admin — no new users, no second claude login, no sudo, no human setup (revised decision honored).
- 5 fixtures staged exactly per the locked decision (3 reverts + vague + migration).
- LaunchAgent plist staged in ~/Library/LaunchAgents (unloaded — arming is Plan 10-02).
- One end-to-end smoke run produced a JSONL log line with a VERDICT — the pipeline that Plan 10-02 will soak is proven to function once.
</success_criteria>

<output>
Create `.planning/phases/10-autopilot-spike-go-no-go-gate/10-01-SUMMARY.md` when done. Record: actual SHAs reverted, any revert conflicts and how resolved, whether the keychain-backed claude auth sufficed for the smoke run (or the setup-token fallback was needed), and the smoke-run verdict.
</output>
