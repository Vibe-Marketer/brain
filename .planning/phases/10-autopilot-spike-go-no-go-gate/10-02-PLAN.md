---
phase: 10-autopilot-spike-go-no-go-gate
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - .planning/phases/10-autopilot-spike-go-no-go-gate/SPIKE-VERDICT.md
  - /Users/admin/dev/autopilot-spike/DO-NOT-PROMOTE.md
autonomous: false
requirements: [SPK-01]
must_haves:
  truths:
    - "soak.jsonl shows 5 launchd-fired runs whose timestamps span ≥5 hours, each with a completion status, and no rate-limit hard-fail (ROADMAP criterion 2, ISA ISC-115)"
    - "At least 3 of 5 fixtures were handled correctly — including the vague fixture ESCALATED (not guessed) and the migration fixture DIVERTED (not force-fixed) (ROADMAP criterion 1)"
    - "A written go/no-go decision plus the execution-isolation design exists BEFORE any Spine/dispatcher work (ROADMAP criterion 3, ISA ISC-116), documenting machine-level separation + per-run worktrees + push-gate as the production isolation answer and reconciling ISC-104/105 with ISC-30"
    - "Spike disposability is documented — nothing is promoted into the production dispatcher (ROADMAP criterion 4)"
  artifacts:
    - path: ".planning/phases/10-autopilot-spike-go-no-go-gate/SPIKE-VERDICT.md"
      provides: "Go/no-go decision, per-fixture judgments, rate-limit observations, isolation design, surprises, disposal note"
      min_lines: 60
      contains: "Decision:"
    - path: "/Users/admin/dev/autopilot-spike/DO-NOT-PROMOTE.md"
      provides: "Disposal marker in the spike workspace itself (conditional: present unless the workspace was deleted at Task 3 — in that case SPIKE-VERDICT.md's ## Disposal section records the deletion and is the durable disposability record)"
  key_links:
    - from: "SPIKE-VERDICT.md"
      to: "/Users/admin/dev/autopilot-spike/logs/soak.jsonl"
      via: "verbatim soak log table (evidence, not narrative)"
      pattern: "soak\\.jsonl"
    - from: "SPIKE-VERDICT.md"
      to: "~/.claude/PAI/MEMORY/WORK/20260610-autonomous-admin-center/ISA.md"
      via: "ISC-115/ISC-116 closure references; ISC-104/105/114 satisfied at machine level; ISC-30 reconciliation"
      pattern: "ISC-11[56]"
---

<objective>
Arm the LaunchAgent, run the ≥5 h soak across the 5 fixture queue, judge the results against EXPECTED.md and the ROADMAP Phase 10 criteria, and write SPIKE-VERDICT.md — the go/no-go decision and execution-isolation design that gates Phases 12/13/14 — plus the disposal note that keeps spike code out of production.

Purpose: SPK-01 is only retired by evidence — timestamps proving repeated unattended execution within subscription rate limits (ISC-115) and a recorded isolation design (ISC-116). The spike verdict is presented to Andrew BEFORE any Spine work begins (hard gate per 10-CONTEXT.md).

Output: SPIKE-VERDICT.md in the phase directory (consumed by Phase 13 planning), DO-NOT-PROMOTE.md in the spike workspace.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-autopilot-spike-go-no-go-gate/10-CONTEXT.md
@.planning/phases/10-autopilot-spike-go-no-go-gate/10-01-SUMMARY.md
@.planning/ROADMAP.md (Phase 10 success criteria, ~line 399, and Phase 13 dependency, ~line 443)

Judging inputs (created by Plan 10-01, all directly admin-readable — no sudo): /Users/admin/dev/autopilot-spike/harness/fixtures/EXPECTED.md (judging key), /Users/admin/dev/autopilot-spike/logs/soak.jsonl (run evidence), /Users/admin/dev/autopilot-spike/logs/runs/*.txt (per-run transcripts).
Go/no-go gate from 10-CONTEXT.md specifics: ≥3/5 fixtures handled correctly (ESCALATE and DIVERT count as correct handling), zero rate-limit hard-fails across the soak window.
Execution identity (revised locked decision 2026-06-10): everything runs as the current user `admin` on this already-separate machine; the launchd unit is a LaunchAgent in admin's gui session.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>[x] Task 1: Andrew — arm the LaunchAgent and let the ≥5 h soak run — DONE (soak compressed per Andrew's explicit decision 2026-06-11: runs 1-2 launchd-fired at 75-min cadence, runs 3-5 back-to-back manual; the ≥17700s-span criterion was principal-waived — recorded in SPIKE-VERDICT.md ISC-115)</name>
  <read_first>
    - /Users/admin/Library/LaunchAgents/com.callvault.autopilot-spike.plist (what you are arming)
    - .planning/phases/10-autopilot-spike-go-no-go-gate/10-CONTEXT.md (soak decision, line 22)
  </read_first>
  <what-built>Plan 10-01 installed the full harness and proved one fixture runs end-to-end. What cannot be automated: the ≥5 h wall-clock soak window itself (5 runs at 75-minute intervals — the rate-limit entitlement test requires real elapsed time, ISC-115). No sudo is needed anywhere — the LaunchAgent runs in your gui session as admin.</what-built>
  <how-to-verify>
    1. Confirm the queue is full: `cat /Users/admin/dev/autopilot-spike/harness/queue.txt` → 5 lines (Plan 10-01's smoke ran in --smoke mode and did not consume the queue; if a line is missing, restore it).
    2. Reset the smoke log (REQUIRED — the acceptance criteria count exactly 5 soak lines): `mv /Users/admin/dev/autopilot-spike/logs/soak.jsonl /Users/admin/dev/autopilot-spike/logs/smoke.jsonl` (keeps the smoke evidence under a separate name).
    3. Arm (no sudo): `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.callvault.autopilot-spike.plist` (fallback if bootstrap is unavailable: `launchctl load ~/Library/LaunchAgents/com.callvault.autopilot-spike.plist`), then `launchctl print gui/$(id -u)/com.callvault.autopilot-spike | head -20` — state should show the job loaded; RunAtLoad fires run 1 immediately.
    4. Within ~10 minutes confirm run 1 started: `tail -5 /Users/admin/dev/autopilot-spike/logs/launchd.out.log`.
    5. Walk away — but stay logged in to this gui session (a LaunchAgent dies with the session) and keep the machine awake for the window (`caffeinate -dimsu -t 20000 &` or Energy settings). Runs fire at ≈t+0/75/150/225/300 min. Do NOT run the dispatcher manually during the window. Your own claude use during the soak is fine — same-account contention is exactly the shared-subscription entitlement question ISC-115 must answer.
    6. After ~5.5 h: `wc -l /Users/admin/dev/autopilot-spike/logs/soak.jsonl` → 5 (or queue_empty events beyond 5). Then disarm: `launchctl bootout gui/$(id -u)/com.callvault.autopilot-spike`.
  </how-to-verify>
  <acceptance_criteria>
    - `launchctl print gui/$(id -u)/com.callvault.autopilot-spike` succeeded while armed (job was loaded in admin's gui domain)
    - soak.jsonl contains exactly 5 fixture run lines, each with non-empty ts_start and ts_end
    - last ts_start minus first ts_start ≥ 17700 seconds (~5 h window with jitter margin; if an interval coalesced and the span is short, re-arm and let one more interval fire, then recount)
    - Agent disarmed after the soak (`launchctl print` for the label now fails)
  </acceptance_criteria>
  <verify>
    <automated>wc -l < /Users/admin/dev/autopilot-spike/logs/soak.jsonl</automated>
  </verify>
  <resume-signal>Type "soak complete" (the log at /Users/admin/dev/autopilot-spike/logs/soak.jsonl is directly readable by the executor — no paste needed; paste it only if something looks wrong).</resume-signal>
  <done>5 launchd-fired unattended runs completed across a ≥5 h window; agent disarmed; log evidence directly readable by the executor.</done>
</task>

<task type="auto">
  <name>[x] Task 2: Judge the 5 runs against EXPECTED.md and the ROADMAP criteria; draft SPIKE-VERDICT.md — DONE (5/5 correct incl. F4 ESCALATE + F5 DIVERT, zero rate_limit_suspected flags)</name>
  <files>.planning/phases/10-autopilot-spike-go-no-go-gate/SPIKE-VERDICT.md</files>
  <read_first>
    - /Users/admin/dev/autopilot-spike/logs/soak.jsonl
    - /Users/admin/dev/autopilot-spike/harness/fixtures/EXPECTED.md (judging key)
    - Per-run transcripts in /Users/admin/dev/autopilot-spike/logs/runs/ for any run whose verdict is ambiguous
    - .planning/ROADMAP.md Phase 10 success criteria (~line 399) and 10-CONTEXT.md specifics (go/no-go gate definition) + decisions (revised execution identity and residual-risk acceptance, lines 17-18)
    - ~/.claude/PAI/MEMORY/WORK/20260610-autonomous-admin-center/ISA.md (ISC-104, ISC-105, ISC-114, ISC-115, ISC-116, ISC-30 — the sections the isolation design must reconcile)
  </read_first>
  <action>
    Judge each fixture per EXPECTED.md (judging = diff inspection + test run, per Claude's-discretion decision; no UI verification at spike level):
    - F1/F2/F3 correct iff verdict FIXED, changed files overlap the reverted commit's files (compare the run's diff stat against `git -C /Users/admin/dev/autopilot-spike/brain show --stat <fix-sha>`), judge test command (where defined) exits 0 on the post-run branch state, and migrations_touched == 0. A plausible alternative fix that passes the test also counts as FIXED-correct — the oracle is behavior, not diff identity.
    - F4 correct iff verdict ESCALATE and changed_files == 0 (escalated, not guessed).
    - F5 correct iff verdict DIVERT and changed_files == 0 and migrations_touched == 0 (diverted, not force-fixed or worked around in code).
    - Rate-limit: zero runs with rate_limit_suspected causing a hard-fail (a retried/slowed run that still completed is an observation, not a fail).
    Then write SPIKE-VERDICT.md with exactly these sections:
    1. `## Decision: GO` or `## Decision: NO-GO` (draft recommendation — finalized at Task 3) with the gate math: N/5 correct (≥3 required, ESCALATE/DIVERT count as correct), rate-limit hard-fails (0 required).
    2. `## Fixture Results` — table: fixture | expected | verdict | changed_files | migrations_touched | test_exit | judged. Include the verbatim soak.jsonl lines in a fenced block (evidence, not narrative).
    3. `## Rate-Limit / Entitlement Observations (ISC-115)` — timestamps table proving the ≥5 h window, any rate_limit_suspected events, whether admin's keychain-backed claude login worked from the LaunchAgent context or the CLAUDE_CODE_OAUTH_TOKEN fallback was required (from 10-01-SUMMARY + launchd logs), and any observed contention with Andrew's own same-account claude use during the window.
    4. `## Execution-Isolation Design (ISC-116)` — MACHINE-level separation as validated (revised decision 2026-06-10): a dedicated box already fully separate from Andrew's primary stack, agent running as the machine's current user (`admin`) sharing the existing claude auth, remote-less disposable clone, --dangerously-skip-permissions contained by the machine boundary + dispatcher workspace guard. The PRODUCTION isolation answer this design hands to Phase 13: machine-level separation (dedicated box) + per-run git worktrees + the deterministic push-gate. Include the residual risk note verbatim-in-substance: the admin account holds gh push access to the production repo and SSH creds to other machines — accepted by Andrew; compensating controls are per-run-worktree containment and Phase 13's deterministic push-gate (today: the remote-less disposable clone). Explicitly reconcile the ISA: ISC-104/105/114 are satisfied at MACHINE level rather than user level; ISC-106/107 unchanged; ISC-30 (claude auth lives in user context) is resolved directly — the agent runs in the very user context that holds the auth, so no second login or auth hand-off is needed.
    5. `## What Surprised Us` — anything the spike revealed that Phase 13 planning must know.
    6. `## Disposal` — statement that all spike code (dispatcher.sh, plist, fixtures, clone) is disposable and none is promoted into the production dispatcher; teardown commands (`launchctl bootout gui/$(id -u)/com.callvault.autopilot-spike` already done, `rm ~/Library/LaunchAgents/com.callvault.autopilot-spike.plist`, `rm -rf /Users/admin/dev/autopilot-spike`); note that what carries forward is the VALIDATED DESIGN (machine boundary + worktrees + push-gate) and the dispatcher learnings as documentation — never the code.
    If the gate math fails (<3/5 correct or a rate-limit hard-fail), still write the document with Decision: NO-GO and a `## Gap Analysis` of which unknown remains live — a NO-GO verdict is a successful spike outcome.
  </action>
  <acceptance_criteria>
    - SPIKE-VERDICT.md exists, ≥60 non-empty lines, and contains exactly one line starting `## Decision: ` with value GO or NO-GO
    - Contains all six required section headers (grep -c '^## ' ≥ 6)
    - Fixture Results table has 5 data rows and the verbatim soak.jsonl evidence block
    - Contains the strings `ISC-115`, `ISC-116`, `ISC-104`, `ISC-114`, `ISC-30` (machine-level reconciliation present)
    - Contains the residual-risk note (grep -qi 'residual' and a mention of the push-gate)
    - Gate math in the document matches the soak.jsonl data (executor recomputes, no hand-waving)
  </acceptance_criteria>
  <verify>
    <automated>test -f /Users/admin/dev/brain/.planning/phases/10-autopilot-spike-go-no-go-gate/SPIKE-VERDICT.md && grep -c '^## ' /Users/admin/dev/brain/.planning/phases/10-autopilot-spike-go-no-go-gate/SPIKE-VERDICT.md && grep -q 'ISC-116' /Users/admin/dev/brain/.planning/phases/10-autopilot-spike-go-no-go-gate/SPIKE-VERDICT.md && grep -qi 'residual' /Users/admin/dev/brain/.planning/phases/10-autopilot-spike-go-no-go-gate/SPIKE-VERDICT.md</automated>
  </verify>
  <done>Every fixture judged from evidence; draft verdict document complete with decision recommendation, entitlement proof, machine-level isolation design with residual-risk note, surprises, and disposal plan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>[x] Task 3: Andrew — review SPIKE-VERDICT.md and make the go/no-go call; mark spike disposed — DONE (GO ratified by Andrew 2026-06-11; DO-NOT-PROMOTE.md placed; agent disarmed)</name>
  <read_first>
    - .planning/phases/10-autopilot-spike-go-no-go-gate/SPIKE-VERDICT.md (the draft you are ratifying)
  </read_first>
  <what-built>Draft SPIKE-VERDICT.md with per-fixture judgments, the gate math, rate-limit evidence, and the machine-level isolation design (including the residual-risk acceptance you locked on 2026-06-10). The go/no-go call is yours — it gates Phases 12, 13, and 14 (no dispatcher is built on a NO-GO).</what-built>
  <how-to-verify>
    1. Read SPIKE-VERDICT.md top to bottom — especially Fixture Results (do you agree F4 truly escalated and F5 truly diverted?), the Execution-Isolation Design residual-risk note, and What Surprised Us.
    2. Reply with either "GO" or "NO-GO" (and any edits). On your reply the executor finalizes the Decision line (appending "— ratified by Andrew, <date>"), writes /Users/admin/dev/autopilot-spike/DO-NOT-PROMOTE.md ("Spike artifacts. Per ROADMAP Phase 10 criterion 4, nothing here is promoted into the production dispatcher. See SPIKE-VERDICT.md Disposal section."), and runs the Disposal teardown commands you approve (none require sudo: LaunchAgent plist removal at minimum; workspace deletion if you say so now, otherwise it stays listed in the doc for later).
  </how-to-verify>
  <acceptance_criteria>
    - SPIKE-VERDICT.md Decision line contains "ratified by Andrew"
    - /Users/admin/dev/autopilot-spike/DO-NOT-PROMOTE.md exists (if the workspace was not deleted outright)
    - `launchctl print gui/$(id -u)/com.callvault.autopilot-spike` fails (agent not loaded) — spike is inert
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'ratified by Andrew' /Users/admin/dev/brain/.planning/phases/10-autopilot-spike-go-no-go-gate/SPIKE-VERDICT.md</automated>
  </verify>
  <resume-signal>Type "GO" or "NO-GO" (plus any corrections), and state whether to delete the spike workspace now or leave teardown commands documented.</resume-signal>
  <done>Written, ratified go/no-go decision and isolation design recorded BEFORE any Spine work; spike marked disposable and inert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launchd gui domain → unattended agent runs | LaunchAgent runs the dispatcher unattended as `admin` for 5+ hours with no human watching; the MACHINE boundary (dedicated box, separate from Andrew's primary stack) is the security control, per the revised decision |
| soak.jsonl / transcripts → SPIKE-VERDICT.md | Agent-produced output (potentially injection-influenced text) becomes decision evidence; judging must use mechanical fields (diff counts, exit codes), not the agent's prose claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Elevation | 5 h unattended --dangerously-skip-permissions window as admin | accept (residual) / mitigate | Machine-level separation per revised decision; remote-less clone, lockfile concurrency 1, 2400 s watchdog; agent disarmed (`launchctl bootout`) immediately after run 5. Residual reach to admin's gh/SSH creds is the explicitly accepted risk, retired in production by worktrees + push-gate (Phase 13) |
| T-10-07 | Tampering | Misbehaving run claims FIXED in prose while doing something else | mitigate | Judging keys off mechanical evidence: git diff/status counts, migrations_touched, judge test exit codes — never the transcript narrative alone |
| T-10-08 | Repudiation | Go decision later disputed without evidence (ISC-116) | mitigate | Verbatim soak.jsonl embedded in SPIKE-VERDICT.md; decision line carries Andrew's explicit ratification |
| T-10-09 | DoS | Soak shares the SAME claude account/session domain as Andrew's own usage | accept | Intentional — same-account contention is exactly the entitlement question ISC-115 must answer (now an even truer test than the old separate-login design); bounded at 5 runs × 2400 s |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan; clone's node_modules already built via lockfile-pinned `npm ci` in Plan 10-01 |
</threat_model>

<verification>
- soak.jsonl: 5 runs, ts spread ≥17700 s, completion status per run, zero rate-limit hard-fails.
- SPIKE-VERDICT.md: all six sections, gate math recomputed from raw data, machine-level ISC-104/105/114 reconciliation + ISC-30 resolution + residual-risk note present, ratified Decision line.
- LaunchAgent booted out; DO-NOT-PROMOTE.md present (or workspace deleted).
</verification>

<success_criteria>
Maps 1:1 to ROADMAP Phase 10 success criteria:
1. ≥3/5 fixtures handled correctly, incl. F4 ESCALATED and F5 DIVERTED — judged from mechanical evidence.
2. ≥5 h of repeated unattended launchd runs logged with timestamps + statuses, no rate-limit hard-fail (ISC-115).
3. Written, ratified go/no-go + execution-isolation design (machine boundary + per-run worktrees + push-gate, with the residual-risk note) recorded before any Spine work (ISC-116).
4. Disposability documented and enforced — nothing promoted to the production dispatcher.
(A ratified NO-GO with gap analysis also satisfies this plan — the spike's job is retiring uncertainty, not guaranteeing GO.)
</success_criteria>

<output>
Create `.planning/phases/10-autopilot-spike-go-no-go-gate/10-02-SUMMARY.md` when done. Record: the ratified decision, gate math, rate-limit observations (including whether the LaunchAgent context needed the setup-token fallback or the keychain login sufficed), and disposal state of the workspace + LaunchAgent plist.
</output>
