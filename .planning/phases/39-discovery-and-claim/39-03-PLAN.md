---
phase: 39-discovery-and-claim
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/__tests__/event-discovery.service.test.ts
  - src/hooks/__tests__/useEventDiscovery.test.ts
  - src/pages/__tests__/Events.test.tsx
  - src/components/settings/__tests__/AccountTab.discovery.test.tsx
  - src/components/call-detail/__tests__/CallParticipantsTab.claim-invite.test.tsx
  - src/components/notifications/__tests__/NotificationBell.test.tsx
  - src/pages/__tests__/ParticipationClaim.test.tsx
  - src/pages/__tests__/OAuthCallback.participation-claim.test.tsx
  - playwright/discovery-claim.spec.ts
autonomous: true
requirements: [DISCO-01, DISCO-02, DISCO-03]
decisions: [D-01, D-02, D-03, D-04, D-05, D-07, D-08, D-09, D-10, D-14, D-15, D-18, D-19, D-21, D-22]
must_haves:
  truths:
    - "Frontend tests reject malformed or overbroad server data before it reaches React."
    - "Every approved Settings, Events, participant, claim, and notification state has a RED component or browser contract."
    - "Claim URLs are scrubbed before render/logging and survive password, signup, and OAuth/root authentication paths."
  artifacts:
    - path: "src/pages/__tests__/Events.test.tsx"
      provides: "Privacy-safe card, grouping, loading, empty, error, pagination, and request states"
    - path: "playwright/discovery-claim.spec.ts"
      provides: "End-to-end token capture, auth return, automatic completion, URL scrub, and Events focus"
  key_links:
    - from: "playwright/discovery-claim.spec.ts"
      to: "/claim-participation and /events"
      via: "password, signup, and OAuth/root restoration paths"
      pattern: "claim-participation|events"
---

<objective>
Create Wave 0 service, hook, component, and browser contracts for the approved Phase 39 user experience.

Purpose: Lock privacy-safe rendering, exact UI states, cache invalidation, and claim navigation before production code.
Output: Collectable expected-failure frontend and Playwright suites.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-discovery-and-claim/39-CONTEXT.md
@.planning/phases/39-discovery-and-claim/39-UI-SPEC.md
@.planning/phases/39-discovery-and-claim/39-VALIDATION.md
@src/services/__tests__/recording-access.service.test.ts
@src/components/call-detail/__tests__/CallParticipantsTab.identityEvidence.test.tsx
@src/components/notifications/__tests__/NotificationBell.test.tsx
@src/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold safe service and hook contracts</name>
  <read_first>
    - src/services/__tests__/event-discovery.service.test.ts (create)
    - src/hooks/__tests__/useEventDiscovery.test.ts (create)
    - src/services/recording-access.service.ts
    - src/hooks/useRecordingAccess.ts
    - src/lib/query-config.ts
  </read_first>
  <files>src/services/__tests__/event-discovery.service.test.ts, src/hooks/__tests__/useEventDiscovery.test.ts</files>
  <action>Write RED tests for strict unknown-to-domain parsing, bounded cursor input, stable server ordering, generic claim errors, and rejection of unexpected restricted keys. Pin query-key families and require every claim/invite/disconnect/request mutation onSettled to invalidate its precise Phase 39 keys plus notifications, aliases/access as applicable, and invalidateCallListCaches(queryClient). Components must never call the service or Supabase directly.</action>
  <acceptance_criteria>
    - Malformed RPC rows and forbidden metadata fail closed.
    - Pagination is bounded and ordering is preserved rather than recomputed from hidden client data.
    - Mutation settlement invalidation runs on both success and error.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/__tests__/event-discovery.service.test.ts src/hooks/__tests__/useEventDiscovery.test.ts --maxWorkers=1 --reporter=verbose</automated>
  </verify>
  <done>The service/hook boundary and cache contract are executable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold Settings, Events, participants, and notification UI contracts</name>
  <read_first>
    - src/pages/__tests__/Events.test.tsx (create)
    - src/components/settings/__tests__/AccountTab.discovery.test.tsx (create)
    - src/components/call-detail/__tests__/CallParticipantsTab.claim-invite.test.tsx (create)
    - src/components/notifications/__tests__/NotificationBell.test.tsx
    - .planning/phases/39-discovery-and-claim/39-UI-SPEC.md
  </read_first>
  <files>src/pages/__tests__/Events.test.tsx, src/components/settings/__tests__/AccountTab.discovery.test.tsx, src/components/call-detail/__tests__/CallParticipantsTab.claim-invite.test.tsx, src/components/notifications/__tests__/NotificationBell.test.tsx</files>
  <action>Write RED Testing Library cases for the exact UI-SPEC: persistent “We found N events” with View events; card groups action/available/waiting and newest order; readable fields versus anonymous ordinal copies; request states; skeleton/empty/error/load-more; confirmed disconnect; canonical owner-only Invite to claim, one-reminder switch off by default, sent/resend/claimed states; and privacy-safe New event found navigation. Assert no restricted values in DOM, accessible labels, test IDs, or serialized props and no bulk/free-form invite affordance per D-05/D-08/D-11/D-22.</action>
  <acceptance_criteria>
    - UI copy, semantic headings/articles/time elements, 44px actions, live regions, and generic errors match 39-UI-SPEC.
    - Transcript-only/weak participant rows receive no invite control.
    - Notification metadata rejects private labels and routes only to /events.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/__tests__/Events.test.tsx src/components/settings/__tests__/AccountTab.discovery.test.tsx src/components/call-detail/__tests__/CallParticipantsTab.claim-invite.test.tsx src/components/notifications/__tests__/NotificationBell.test.tsx --maxWorkers=1 --reporter=verbose</automated>
  </verify>
  <done>All approved authenticated UI surfaces have RED behavioral/privacy coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold claim entry and authentication-return coverage</name>
  <read_first>
    - src/pages/__tests__/ParticipationClaim.test.tsx (create)
    - src/pages/__tests__/OAuthCallback.participation-claim.test.tsx (create)
    - playwright/discovery-claim.spec.ts (create)
    - src/pages/Login.tsx
    - src/pages/OAuthCallback.tsx
    - src/components/ProtectedRoute.tsx
    - src/App.tsx
    - .planning/phases/39-discovery-and-claim/39-UI-SPEC.md
  </read_first>
  <files>src/pages/__tests__/ParticipationClaim.test.tsx, src/pages/__tests__/OAuthCallback.participation-claim.test.tsx, playwright/discovery-claim.spec.ts</files>
  <action>Write RED unit and browser flows for public token capture followed immediately by history replacement, dedicated sessionStorage (never localStorage/query cache), signed-out CTA, password login, signup, OAuth/root return, and the D-14 two-step contract. Pin a non-consuming authenticated `inspect` call that returns only generic unavailable or masked invited email plus `confirmationRequired`; intended-account flow may then issue one `consume`, while different-primary flow must render the exact approved **Add email and continue** and **Use another account** choices. Assert Add email sends `confirmEmailAttachment: true` to one atomic consume, Use another account signs out without consuming and preserves the pending claim through auth, parallel/replayed consumes yield one winner, and terminal outcomes are generic. The dedicated OAuthCallback test must prove the existing callback/root bootstrap reads the session-only pending claim and returns to the clean claim route without token query parameters or duplicate inspect/consume. Scan URL/history/DOM/storage/console/network labels for token leakage and assert no private event preview per D-14/D-15/D-22.</action>
  <acceptance_criteria>
    - Raw token disappears from visible URL before private UI renders and is cleared after terminal/success.
    - pendingShareToken stays isolated.
    - Every authentication path resumes at the clean claim route; intended-account and confirmed add-email paths end at /events, while Use another account leaves the token unconsumed until later auth.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/__tests__/ParticipationClaim.test.tsx src/pages/__tests__/OAuthCallback.participation-claim.test.tsx --maxWorkers=1 --reporter=verbose &amp;&amp; npx playwright test playwright/discovery-claim.spec.ts --workers=1 --list</automated>
  </verify>
  <done>Claim navigation and privacy requirements have unit and E2E contracts before implementation.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries
- Assets: raw claim token, restricted metadata, browser session, authorization-sensitive caches.
- Boundaries: RPC JSON to service parser; hook state to components; email URL to browser/auth redirects.

| Threat ID | Category | Severity | Mitigation | Verification |
|---|---|---:|---|---|
| T-39-03-01 | Information disclosure | HIGH | Strict allowlist mapping and DOM/network privacy assertions | Service and Events tests |
| T-39-03-02 | Spoofing | HIGH | Dedicated pending key, shape validation, exactly-once mutation | Claim unit/E2E tests |
| T-39-03-03 | Information disclosure | HIGH | Immediate replaceState and no durable/analytic/log storage | Browser token scan |
| T-39-03-04 | Tampering | MEDIUM | Server ordering/status remains authoritative | UI order and stale-state refetch tests |

Any unresolved HIGH threat blocks UI implementation and preproduction.
</threat_model>

<verification>All tests must collect; RED status may name only the Phase 39 files/contracts not yet implemented.</verification>
<success_criteria>Every approved Phase 39 frontend behavior and privacy boundary has a test path.</success_criteria>
<output>Create 39-03-SUMMARY.md when done.</output>
