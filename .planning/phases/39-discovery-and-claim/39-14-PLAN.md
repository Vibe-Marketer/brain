---
phase: 39-discovery-and-claim
plan: "14"
type: execute
wave: 8
depends_on: ["39-10", "39-13"]
files_modified:
  - src/App.tsx
  - src/components/Layout.tsx
  - src/components/layout/AppShell.tsx
  - src/components/ui/sidebar-nav.tsx
  - src/components/layout/__tests__/AppShell.mobile.test.tsx
  - playwright/discovery-claim.spec.ts
autonomous: true
requirements: [DISCO-01, DISCO-02, DISCO-03]
decisions: [D-01, D-02, D-03, D-07, D-12, D-14, D-15, D-21, D-22]
must_haves:
  truths:
    - "/events is a protected standard AppShell route and /claim-participation is a public privacy-safe entry."
    - "EVENTS appears immediately after CALLS in desktop and mobile navigation with approved Remix icons/copy."
    - "The complete claim browser flow ends at focused Events with the token absent from history/storage/logs."
  artifacts:
    - path: "src/App.tsx"
      provides: "Public claim and protected Events route wiring"
    - path: "src/components/ui/sidebar-nav.tsx"
      provides: "Desktop Events navigation"
    - path: "src/components/layout/AppShell.tsx"
      provides: "Mobile Events navigation"
  key_links:
    - from: "/claim-participation"
      to: "/events"
      via: "session-backed auth completion and route replacement"
      pattern: "ParticipationClaim|Events"
---

<objective>
Wire the dedicated Events and claim surfaces into routing, page labels, desktop/mobile navigation, and browser verification.

Purpose: Make Phase 39 reachable through approved routes while keeping claim entry public and event content protected.
Output: App-shell integration and green end-to-end claim/navigation tests.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-discovery-and-claim/39-UI-SPEC.md
@src/App.tsx
@src/components/Layout.tsx
@src/components/layout/AppShell.tsx
@src/components/ui/sidebar-nav.tsx
@playwright/discovery-claim.spec.ts
@src/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public claim and protected Events routes with page labeling</name>
  <read_first>
    - src/App.tsx
    - src/components/Layout.tsx
    - src/pages/Events.tsx
    - src/pages/ParticipationClaim.tsx
    - playwright/discovery-claim.spec.ts
  </read_first>
  <files>src/App.tsx, src/components/Layout.tsx, playwright/discovery-claim.spec.ts</files>
  <action>Add lazy route wiring for the clean public /claim-participation entry and the /events ProtectedRoute inside the existing Layout/AppShell conventions. Ensure /events gets the EVENTS top label and no duplicate legacy card wrapper. Claim entry handles its own auth state and shows no protected Layout/private preview. Use route replacement after claim. Activate the RED browser cases for signed-out, password, signup, OAuth/root, non-consuming inspect, intended-account automatic consume, different-primary exact Add email and continue/Use another account choices, add-email atomic consume, use-another-account unconsumed preservation, generic terminal, replay/parallel race, retry, URL/history scrub, and final h1 focus.</action>
  <acceptance_criteria>
    - Unauthenticated /events redirects to login while /claim-participation renders only generic claim content.
    - No raw token appears in route after initial capture or browser Back history after success.
    - Intended-account and confirmed add-email branches reach /events and focus EVENTS; Use another account proves no consume before the replacement authentication completes.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test playwright/discovery-claim.spec.ts --workers=1</automated>
  </verify>
  <done>The public-to-protected claim route works end to end across every auth path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Events immediately after Calls in desktop and mobile navigation</name>
  <read_first>
    - src/components/ui/sidebar-nav.tsx
    - src/components/layout/AppShell.tsx
    - src/components/layout/__tests__/AppShell.mobile.test.tsx
    - .planning/phases/39-discovery-and-claim/39-UI-SPEC.md
  </read_first>
  <files>src/components/ui/sidebar-nav.tsx, src/components/layout/AppShell.tsx, src/components/layout/__tests__/AppShell.mobile.test.tsx</files>
  <action>Add EVENTS after CALLS in navItems and MOBILE_NAV_ITEMS with path /events, description “Meetings connected to you,” RiCalendarEventLine/Fill, existing SelectionButton active treatment, and no new navigation style. Keep mobile shell limits usable by adjusting only existing item rendering if the extra destination requires it; preserve labels, 44px targets, collapsed behavior, and Settings/admin rules. Extend mobile/desktop nav tests for order, active state, accessible labels, and route.</action>
  <acceptance_criteria>
    - Desktop and mobile nav order is Calls then Events, with Remix icons only.
    - Active /events state and collapsed/mobile accessible names work.
    - Existing calls/import/rules/people/settings/admin navigation tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/layout/__tests__/AppShell.mobile.test.tsx src/pages/__tests__/Events.test.tsx --maxWorkers=1 &amp;&amp; npm run build</automated>
  </verify>
  <done>The approved Events destination is reachable consistently from Settings, claim completion, desktop, and mobile navigation.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries
- Assets: protected Events data, raw claim token, auth-return route, app navigation.
- Boundaries: public route to auth; ProtectedRoute to Events; session claim to route replacement.

| Threat ID | Category | Severity | Mitigation | Verification |
|---|---|---:|---|---|
| T-39-14-01 | Information disclosure | HIGH | Protected /events and preview-free public route | anonymous browser tests |
| T-39-14-02 | Information disclosure | HIGH | Replace navigation and scrubbed session lifecycle | history/storage scans |
| T-39-14-03 | Spoofing | MEDIUM | Auth restoration through existing provider/session | password/signup/OAuth tests |

Any HIGH failure blocks sending real invitations and preproduction.
</threat_model>

<verification>Unit navigation tests, full claim Playwright flow, type check, and build pass.</verification>
<success_criteria>Events and participation claim routes are reachable, protected at the correct boundary, and verified in-browser.</success_criteria>
<output>Create 39-14-SUMMARY.md when done.</output>
