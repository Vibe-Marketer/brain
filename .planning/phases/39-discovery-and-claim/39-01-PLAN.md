---
phase: 39-discovery-and-claim
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/phase39-fixtures.ts
  - src/test/phase39-fixtures.integration.test.ts
  - src/test/discovery-claim.integration.test.ts
  - src/test/migrations/phase39-discovery-claim-migrations.test.ts
autonomous: true
requirements: [DISCO-01, DISCO-03]
decisions: [D-01, D-03, D-04, D-05, D-16, D-17, D-19, D-20, D-21, D-22, D-23]
must_haves:
  truths:
    - "Every Phase 39 database test rejects production ref vltmrnjsubfzrgrtdqey before creating a client."
    - "Real-DB fixtures represent confirmed primary, verified alias, disconnected alias, weak/name/calendar evidence, readable/restricted copies, webinar, and 49/50/51 participant cases."
    - "RED contracts independently cover caller-scoped discovery, direct events RLS, safe projections, ordering, pagination, disconnect, and unchanged legacy People RPC shapes."
  artifacts:
    - path: "src/test/phase39-fixtures.ts"
      provides: "Synthetic Phase 39 identity, event, copy, request, and notification graphs"
    - path: "src/test/discovery-claim.integration.test.ts"
      provides: "DISCO-01/DISCO-03 real-DB behavior matrix"
    - path: "src/test/migrations/phase39-discovery-claim-migrations.test.ts"
      provides: "Static additive migration, privilege, and privacy contract"
  key_links:
    - from: "src/test/discovery-claim.integration.test.ts"
      to: "src/test/phase39-fixtures.ts"
      via: "guarded synthetic TEST data"
      pattern: "createPhase39FixtureGraph|cleanupPhase39FixtureGraph"
---

<objective>
Create Wave 0 database fixtures and executable RED contracts before security-sensitive schema work begins.

Purpose: Make verified-email authorization, privacy, revocation, and Phase 38 compatibility measurable against the dedicated TEST project.
Output: Production-guarded fixtures plus static and real-database acceptance suites.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-discovery-and-claim/39-CONTEXT.md
@.planning/phases/39-discovery-and-claim/39-RESEARCH.md
@.planning/phases/39-discovery-and-claim/39-VALIDATION.md
@.planning/phases/39-discovery-and-claim/39-PATTERNS.md
@src/test/integration-setup.ts
@src/test/access-policy.integration.test.ts
@src/test/rls-regression.test.ts
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build guarded, idempotent Phase 39 fixture graphs</name>
  <read_first>
    - src/test/phase39-fixtures.ts (create)
    - src/test/phase39-fixtures.integration.test.ts (create)
    - src/test/integration-setup.ts
    - src/test/phase38-fixtures.ts
    - supabase/migrations/20260905140000_create_identities_and_link_tables.sql
    - supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql
  </read_first>
  <files>src/test/phase39-fixtures.ts, src/test/phase39-fixtures.integration.test.ts</files>
  <action>Create synthetic, prefix-scoped TEST fixtures for confirmed primary and verified alias owners, inactive/unverified/conflicting aliases, confirmed speaker/host/organizer evidence, invitee-only/name/org-only denials, mixed readable/restricted event copies, request states, webinar classification, 49/50/51 confirmed-participant events, and notification baselines per D-20 through D-22. Reuse the central dedicated-test client guard; reject any hostname/ref other than TEST swjzxiddcrtaqixsfaac before I/O. Cleanup must check every error, delete children before protected parents, use cleanup_test_fixture_users for auth teardown, and prove zero residue across two cycles. Never borrow customer rows or mock Supabase.</action>
  <acceptance_criteria>
    - Fixture setup exports typed create/cleanup APIs with no any and no production env fallback.
    - Two real TEST create/assert/cleanup cycles pass and leave zero marked rows/users.
    - Every authorization and privacy row named in 39-VALIDATION.md can be selected by a stable fixture key.
  </acceptance_criteria>
  <verify>
    <automated>VITEST_INTEGRATION_OK=true npx vitest run src/test/phase39-fixtures.integration.test.ts --maxWorkers=1 --reporter=verbose</automated>
  </verify>
  <done>Phase 39 has repeatable real-DB fixtures that cannot reach production.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode verified-email discovery and privacy as RED real-DB tests</name>
  <read_first>
    - src/test/discovery-claim.integration.test.ts (create)
    - src/test/phase39-fixtures.ts
    - src/test/access-policy.integration.test.ts
    - supabase/migrations/20260309120000_call_participants.sql
    - supabase/migrations/20260831020000_fix_events_participation_rls_and_updated_at_trigger.sql
    - supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql
  </read_first>
  <files>src/test/discovery-claim.integration.test.ts</files>
  <action>Write expected-failure contracts for the distinct caller-scoped RPCs `count_my_discovered_events()` and `list_my_discovered_events(p_limit,p_cursor)` plus direct events SELECT. Prove confirmed primary and active verified aliases succeed; disconnected/unverified/name/domain/org/calendar-only evidence fails; event results deduplicate, use bounded cursor pagination, group needs-action then available then waiting, and sort newest within groups per D-01/D-03/D-04. Assert raw results never contain restricted title, owner, provider, transcript, summary, roster, source ID, participant count, or stable visible restricted-copy identifier per D-05/D-21/D-22. Pin unchanged signatures, grants, and return keys for get_people_summary(UUID) and get_recordings_for_person(UUID,TEXT,TEXT) per D-23. Include `sync_my_discovered_event_notifications()` silent baseline/future notification and disconnect-revocation RED cases per D-16/D-17/D-19.</action>
  <acceptance_criteria>
    - Tests exercise caller RPCs and direct-table RLS separately with authenticated real users.
    - 49 passes while 50/51 and webinar cases stay hidden; Phase 38 request/content paths remain the authority.
    - Missing schema/functions are the only expected failures; fixture, auth, cleanup, and production guards are green.
  </acceptance_criteria>
  <verify>
    <automated>VITEST_INTEGRATION_OK=true npx vitest run src/test/discovery-claim.integration.test.ts src/test/access-policy.integration.test.ts src/test/rls-regression.test.ts --maxWorkers=1 --reporter=verbose</automated>
  </verify>
  <done>DISCO-01 and DISCO-03 have executable allow/deny/privacy/regression contracts before migration code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define static migration security invariants</name>
  <read_first>
    - src/test/migrations/phase39-discovery-claim-migrations.test.ts (create)
    - supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql
    - supabase/migrations/20260905140000_create_identities_and_link_tables.sql
    - supabase/CLAUDE.md
  </read_first>
  <files>src/test/migrations/phase39-discovery-claim-migrations.test.ts</files>
  <action>Create source assertions for the exact ordered Phase 39 migration allowlist, additive-only DDL, FORCE RLS on new tables, explicit revoke/grant, SECURITY DEFINER with search_path='', fully qualified relations, bounded pagination, hash-only token storage, atomic row locking, unique notification ledger, no legacy People RPC replacement, and no destructive participant rewrite. Use expected-failure tests until Plans 04-06 create the migrations.</action>
  <acceptance_criteria>
    - The test rejects DROP/TRUNCATE, raw-token columns, public table writes, unbounded discovery, and replacement of either legacy People RPC.
    - The exact three planned migration filenames and ordering are pinned.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/test/migrations/phase39-discovery-claim-migrations.test.ts --maxWorkers=1 --reporter=verbose</automated>
  </verify>
  <done>The schema plans cannot silently weaken privilege, privacy, atomicity, or compatibility requirements.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries
- Assets: production data, verified-email evidence, restricted event metadata, TEST credentials.
- Boundaries: environment to client construction; service-role fixture setup to authenticated RLS probes; database projection to test assertions.

| Threat ID | Category | Severity | Mitigation | Verification |
|---|---|---:|---|---|
| T-39-01-01 | Tampering | HIGH | Hard reject production ref and production-named credential fallbacks before client creation | Guard unit test plus two real TEST cycles |
| T-39-01-02 | Information disclosure | HIGH | Assert raw RPC keys and direct-table denials, not only rendered output | Discovery privacy matrix |
| T-39-01-03 | Repudiation | MEDIUM | Checked cleanup and stable fixture/run IDs | Zero-residue queries after both cycles |
| T-39-01-SC | Tampering | LOW | No dependency installs | package/lockfile diff |

Any unresolved HIGH threat or failed guard blocks Plans 04-17.
</threat_model>

<verification>Wave 0 is complete only when tests collect, guard/fixture paths are green, and implementation-dependent assertions fail for the named missing contracts.</verification>
<success_criteria>Dedicated TEST safety, complete authorization fixtures, and RED DISCO-01/DISCO-03 contracts exist.</success_criteria>
<output>Create 39-01-SUMMARY.md when done.</output>
