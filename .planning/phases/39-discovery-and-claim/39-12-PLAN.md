---
phase: 39-discovery-and-claim
plan: "12"
type: execute
wave: 7
depends_on: ["39-09"]
files_modified:
  - src/types/meetings.ts
  - src/hooks/useCallDetailQueries.ts
  - src/components/CallDetailDialog.tsx
  - src/components/call-detail/CallParticipantsTab.tsx
  - src/components/call-detail/__tests__/CallParticipantsTab.claim-invite.test.tsx
autonomous: true
requirements: [DISCO-02, DISCO-03]
decisions: [D-06, D-07, D-08, D-09, D-10, D-11, D-20, D-21, D-22]
must_haves:
  truths:
    - "D-07: Invite to claim appears beside each canonical server-eligible participant in the recording's existing Participants section, visible only to the recording owner."
    - "Each participant invitation is independent, one at a time, with status/dates and optional single reminder off by default."
    - "Transcript-only, calendar-only, no-email, owner, and already-claimed rows reveal no invitation affordance."
  artifacts:
    - path: "src/components/call-detail/CallParticipantsTab.tsx"
      provides: "Owner invitation controls and lifecycle states on canonical participant rows"
  key_links:
    - from: "useCallDetailQueries"
      to: "CallParticipantsTab"
      via: "canonical participant_id plus recording UUID"
      pattern: "participant_id"
---

<objective>
Add deliberate owner-only participation invitations to the existing call Participants section.

Purpose: Let recording owners invite one proven canonical participant without inferring identity from merged transcript display data.
Output: Canonical participant wiring, invitation/status controls, and green tests.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-discovery-and-claim/39-UI-SPEC.md
@src/hooks/useCallDetailQueries.ts
@src/components/CallDetailDialog.tsx
@src/components/call-detail/CallParticipantsTab.tsx
@src/types/meetings.ts
@src/hooks/useEventDiscovery.ts
@src/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve canonical participant identity through call-detail merging</name>
  <read_first>
    - src/types/meetings.ts
    - src/hooks/useCallDetailQueries.ts
    - src/components/CallDetailDialog.tsx
    - src/components/call-detail/CallParticipantsTab.tsx
    - src/components/call-detail/__tests__/CallParticipantsTab.claim-invite.test.tsx
  </read_first>
  <files>src/types/meetings.ts, src/hooks/useCallDetailQueries.ts, src/components/CallDetailDialog.tsx, src/components/call-detail/CallParticipantsTab.tsx, src/components/call-detail/__tests__/CallParticipantsTab.claim-invite.test.tsx</files>
  <action>Extend Speaker/CallSpeaker with optional canonical participant_id populated only from call_participants.id in the UUID query. Preserve it through mergeCallSpeakers when matching transcript display rows, but never synthesize it for transcript/calendar/contact-only speakers. Pass canonical recording UUID and participant identities from CallDetailDialog into CallParticipantsTab using toRecordingUuid boundary data already resolved by the call model; never coerce legacy numeric IDs. Invitation eligibility/status remains server-computed through the batch recording/participant status hook rather than inferred from display fields.</action>
  <acceptance_criteria>
    - Canonical rows keep participant_id after merge; synthetic transcript rows never gain one.
    - Legacy numeric calls with no canonical UUID render existing participant UI without invitation controls.
    - Existing identity evidence badge tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/call-detail/__tests__/CallParticipantsTab.claim-invite.test.tsx src/components/call-detail/__tests__/CallParticipantsTab.identityEvidence.test.tsx --maxWorkers=1</automated>
  </verify>
  <done>Invitation controls have a trustworthy canonical participant key and recording UUID.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render owner-only send, reminder, status, cancellation, and resend controls</name>
  <read_first>
    - src/components/call-detail/CallParticipantsTab.tsx
    - src/components/call-detail/__tests__/CallParticipantsTab.claim-invite.test.tsx
    - src/hooks/useEventDiscovery.ts
    - .planning/phases/39-discovery-and-claim/39-UI-SPEC.md
    - src/components/ui/switch.tsx
  </read_first>
  <files>src/components/call-detail/CallParticipantsTab.tsx, src/components/call-detail/__tests__/CallParticipantsTab.claim-invite.test.tsx</files>
  <action>Use the batch invitation-status hook for the recording and server eligibility flags. For eligible rows render Invite to claim plus a labeled “Send one reminder” switch reset/off by default. Disable only the active row during one-at-a-time send. Render exact status badge, absolute sent/available/reminded/claimed dates, cancel-reminder link, and Resend invite only when the server authorizes it after seven days; never use the client clock for authorization. Refetch stale/concurrent state and show exact UI-SPEC feedback. Hide all controls without eligibility rather than explaining private reasons. Preserve existing participant layout/evidence and stack controls on mobile.</action>
  <acceptance_criteria>
    - Owner UI tests cover send, pending, scheduled, cancelled, reminded, claimed, expired, resend, and concurrent refetch.
    - Non-owner and ineligible merged rows have no disabled hint/control.
    - No free-form email, bulk selection, or invite-everyone affordance exists.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/call-detail/__tests__/CallParticipantsTab.claim-invite.test.tsx src/components/call-detail/__tests__/CallParticipantsTab.identityEvidence.test.tsx --maxWorkers=1 --reporter=verbose &amp;&amp; npm run type-check</automated>
  </verify>
  <done>Recording owners can manage one secure invitation per eligible participant from the existing Participants section.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries
- Assets: canonical participant/email eligibility, invitation status, recording ownership.
- Boundaries: merged display data to canonical participant ID; UI visibility to server-authorized mutation.

| Threat ID | Category | Severity | Mitigation | Verification |
|---|---|---:|---|---|
| T-39-12-01 | Spoofing/IDOR | HIGH | Only persisted participant_id; server owner/eligibility remains authoritative | canonical/synthetic/non-owner tests |
| T-39-12-02 | Information disclosure | HIGH | Hide ineligible controls/reasons and never render recipient details beyond owner-visible participant row | UI privacy tests |
| T-39-12-03 | Tampering | MEDIUM | Server dates/capabilities control resend/reminder | stale/concurrency tests |

Any HIGH failure blocks preproduction.
</threat_model>

<verification>Participant, identity-evidence, service/hook, type, and TEST invitation integration suites pass.</verification>
<success_criteria>D-06 through D-11 are delivered without treating merged transcript speakers as claimable participants.</success_criteria>
<output>Create 39-12-SUMMARY.md when done.</output>
