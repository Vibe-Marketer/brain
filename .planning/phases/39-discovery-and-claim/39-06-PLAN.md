---
phase: 39-discovery-and-claim
plan: "06"
type: execute
wave: 4
depends_on: ["39-04", "39-05"]
files_modified:
  - supabase/migrations/20260920000003_phase39_notification_disconnect.sql
  - src/test/discovery-claim.integration.test.ts
  - src/test/migrations/phase39-discovery-claim-migrations.test.ts
autonomous: true
requirements: [DISCO-01, DISCO-03]
decisions: [D-17, D-18, D-19, D-20, D-21, D-22]
must_haves:
  truths:
    - "Current matches seed a silent baseline; only later first-time user/event matches create one in-app notification."
    - "Disconnect immediately removes alias-derived visibility and stops future discovery without changing participant evidence."
    - "Notification actions remain privacy-safe and cannot survive loss of authorization."
  artifacts:
    - path: "supabase/migrations/20260920000003_phase39_notification_disconnect.sql"
      provides: "Future-match ledger/sync, alias disconnect, notification neutralization, RLS/grants"
  key_links:
    - from: "sync_my_discovered_event_notifications"
      to: "user_notifications"
      via: "unique user/event ledger and active verified-email authorization"
      pattern: "ON CONFLICT"
---

<objective>
Add exact-once future-match notifications and atomic self-service verified-email disconnect.

Purpose: Fulfill ongoing discovery without historical floods, continuing email, stale access, or participant rewrites.
Output: One additive notification/disconnect migration and passing lifecycle tests.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-discovery-and-claim/39-CONTEXT.md
@.planning/phases/39-discovery-and-claim/39-RESEARCH.md
@supabase/migrations/20260919000008_phase38_notification_contracts.sql
@src/hooks/useNotifications.ts
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the silent baseline and exact-once future notification ledger</name>
  <read_first>
    - supabase/migrations/20260920000003_phase39_notification_disconnect.sql (create)
    - src/test/discovery-claim.integration.test.ts
    - src/test/migrations/phase39-discovery-claim-migrations.test.ts
    - supabase/migrations/20260919000008_phase38_notification_contracts.sql
  </read_first>
  <files>supabase/migrations/20260920000003_phase39_notification_disconnect.sql, src/test/discovery-claim.integration.test.ts, src/test/migrations/phase39-discovery-claim-migrations.test.ts</files>
  <action>Create a private forced-RLS discovery-notification ledger unique on user_id/event_id. Add the bounded idempotent authenticated RPC `sync_my_discovered_event_notifications()` that derives the caller, silently inserts every event discoverable at first activation/baseline, and on later runs inserts exactly one privacy-safe user_notifications row only for newly discoverable events per D-17/D-18. This is a caller-pull contract invoked by authenticated discovery/count/list refreshes and the existing notification query cycle; add no pg_cron or import-pipeline dependency. Keep ledger rows across disconnect/reconnect to prevent replay. Metadata may carry only a strict Phase 39 kind and event ID/action; no email, title, owner, provider, roster, recording fields, or counts. Grant no client table mutation and send no ongoing email/digest.</action>
  <acceptance_criteria>
    - Activation with historical events creates ledger rows and zero notifications.
    - One later matching event creates one notification across repeated syncs and additional copies.
    - Metadata raw-key scan and RLS tests show no private detail or cross-user visibility.
  </acceptance_criteria>
  <verify>
    <automated>VITEST_INTEGRATION_OK=true npx vitest run src/test/discovery-claim.integration.test.ts src/test/rls-regression.test.ts --maxWorkers=1 --reporter=verbose</automated>
  </verify>
  <done>Future event matching produces one in-app notification without historical floods.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add atomic alias disconnect and derived-access revocation</name>
  <read_first>
    - supabase/migrations/20260920000003_phase39_notification_disconnect.sql
    - src/test/discovery-claim.integration.test.ts
    - supabase/migrations/20260905140000_create_identities_and_link_tables.sql
    - supabase/migrations/20260831020000_fix_events_participation_rls_and_updated_at_trigger.sql
  </read_first>
  <files>supabase/migrations/20260920000003_phase39_notification_disconnect.sql, src/test/discovery-claim.integration.test.ts</files>
  <action>Add a caller-scoped disconnect RPC for non-primary active verified email aliases. Atomically deactivate current ownership evidence, preserve the identity and every call_participants row unchanged, stop future matching, and remove/neutralize actionable notifications no longer authorized through any remaining current email. Make every authorization surface read active evidence on each request so direct event SELECT, discovery RPCs, saved links, and sync all revoke immediately per D-19/D-20. Prevent disconnecting the primary login email through this surface.</action>
  <acceptance_criteria>
    - Disconnect removes only visibility derived solely from that alias; events shared through another active email remain.
    - Participant/identity row counts and immutable participant fields are unchanged.
    - Reconnect resumes future matching without replaying ledgered notifications.
  </acceptance_criteria>
  <verify>
    <automated>VITEST_INTEGRATION_OK=true npx vitest run src/test/discovery-claim.integration.test.ts src/test/access-policy.integration.test.ts src/test/rls-regression.test.ts --maxWorkers=1 --reporter=verbose</automated>
  </verify>
  <done>Users can revoke alias-derived discovery immediately without damaging source evidence.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries
- Assets: derived event visibility, alias state, notification metadata, participant evidence.
- Boundaries: caller sync/disconnect RPC to private ledger and alias rows; notification action back to Events.

| Threat ID | Category | Severity | Mitigation | Verification |
|---|---|---:|---|---|
| T-39-06-01 | Information disclosure | HIGH | Current-email authorization on every read; neutralize stale actions | post-disconnect direct/RPC tests |
| T-39-06-02 | Repudiation/DoS | MEDIUM | Unique retained ledger and transactional insert | repeated sync/reconnect tests |
| T-39-06-03 | Tampering | HIGH | Caller-derived alias ownership; primary cannot disconnect | wrong-user/primary tests |
| T-39-06-04 | Information disclosure | HIGH | Strict notification metadata allowlist | raw notification key scan |

Any HIGH failure blocks TEST push and frontend notification work.
</threat_model>

<verification>Notification, disconnect, Phase 38 access, and global RLS suites pass sequentially with zero fixture residue.</verification>
<success_criteria>D-17 through D-19 work with exact-once notifications and immediate reversible authorization revocation.</success_criteria>
<output>Create 39-06-SUMMARY.md when done.</output>
