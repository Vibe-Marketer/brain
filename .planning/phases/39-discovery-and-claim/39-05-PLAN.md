---
phase: 39-discovery-and-claim
plan: "05"
type: execute
wave: 3
depends_on: ["39-02", "39-04"]
files_modified:
  - supabase/migrations/20260920000002_phase39_participation_claims.sql
  - supabase/functions/send-participation-claim/__tests__/send-participation-claim.integration.test.ts
  - supabase/functions/participation-claim/__tests__/participation-claim.integration.test.ts
  - src/test/migrations/phase39-discovery-claim-migrations.test.ts
autonomous: true
requirements: [DISCO-02, DISCO-03]
decisions: [D-06, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-16, D-20, D-21, D-22]
must_haves:
  truths:
    - "Invitation state is service-managed, owner-authorized, canonical-participant-based, and allows at most one active invitation."
    - "D-13: Only a hash of a 32-byte token is stored; the link expires after seven days, authenticated inspection is non-consuming, and consume is an atomic single-use transaction that denies replay generically."
    - "Claim attaches the invited email to the current account and establishes all current matches without granting content."
  artifacts:
    - path: "supabase/migrations/20260920000002_phase39_participation_claims.sql"
      provides: "Invitation table, owner-safe status/create RPCs, non-consuming inspect RPC, atomic consume RPC, grants, indexes, and audit state"
  key_links:
    - from: "consume_my_participation_claim"
      to: "identity_aliases"
      via: "row-locked alias ownership and invitation state transition"
      pattern: "FOR UPDATE"
---

<objective>
Implement the database-owned invitation and atomic participation-claim lifecycle.

Purpose: Make the email proof secure under IDOR, resend, conflict, expiry, and parallel-consume races.
Output: Additive service-only schema and narrow authenticated/service RPCs.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-discovery-and-claim/39-CONTEXT.md
@.planning/phases/39-discovery-and-claim/39-RESEARCH.md
@.planning/phases/39-discovery-and-claim/39-PATTERNS.md
@supabase/migrations/20260905140000_create_identities_and_link_tables.sql
@supabase/migrations/20260905150000_create_identity_alias_verifications.sql
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create service-only invitation state and owner-safe RPCs</name>
  <read_first>
    - supabase/migrations/20260920000002_phase39_participation_claims.sql (create)
    - supabase/functions/send-participation-claim/__tests__/send-participation-claim.integration.test.ts
    - src/test/migrations/phase39-discovery-claim-migrations.test.ts
    - supabase/migrations/20260919000001_phase38_access_policy_schema.sql
  </read_first>
  <files>supabase/migrations/20260920000002_phase39_participation_claims.sql, supabase/functions/send-participation-claim/__tests__/send-participation-claim.integration.test.ts, src/test/migrations/phase39-discovery-claim-migrations.test.ts</files>
  <action>Create participation_claim_invitations with recording/participant/inviter foreign keys, normalized invited-email snapshot, unique token_hash, explicit state, sent/expires/claimed/superseded/revoked timestamps, reminder opt-in/scheduled/provider/sent/cancel fields, audit timestamps, partial uniqueness for active participant invitation, RLS enabled/forced, no anon/auth table access, and service-role operations. Add SECURITY DEFINER create/rotate/status/cancel-reminder RPCs with search_path='', qualification, explicit grants, row locks, and server-derived auth.uid(), recording owner, participant email, and confirmed eligibility. Enforce one-at-a-time per D-08, no free-form email per D-11, manual resend only at seven days with fresh hash/expiry, and one optional reminder off by default per D-09/D-10.</action>
  <acceptance_criteria>
    - Non-owner and all browser-supplied owner/email/status fields are rejected.
    - Only canonical participant IDs with confirmed evidence and a nonblank email are eligible.
    - Active uniqueness, resend rotation, status dates, and reminder limits hold under concurrent creation.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/test/migrations/phase39-discovery-claim-migrations.test.ts --maxWorkers=1 &amp;&amp; VITEST_INTEGRATION_OK=true npx vitest run supabase/functions/send-participation-claim/__tests__/send-participation-claim.integration.test.ts --maxWorkers=1</automated>
  </verify>
  <done>The database owns invitation authorization and lifecycle state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement non-consuming inspection and atomic email-wide claim consumption</name>
  <read_first>
    - supabase/migrations/20260920000002_phase39_participation_claims.sql
    - supabase/functions/participation-claim/__tests__/participation-claim.integration.test.ts
    - supabase/migrations/20260905140000_create_identities_and_link_tables.sql
    - supabase/functions/confirm-email-alias-verification/index.ts
  </read_first>
  <files>supabase/migrations/20260920000002_phase39_participation_claims.sql, supabase/functions/participation-claim/__tests__/participation-claim.integration.test.ts</files>
  <action>Add two narrow authenticated SECURITY DEFINER RPCs, both with `SET search_path=''`, fully qualified objects, explicit authenticated grants, and server-derived `auth.uid()`. `inspect_my_participation_claim(p_token_hash)` accepts only a fixed-format SHA-256 digest, validates active state/expiry/conflict for the caller, performs no row-state transition and no alias/identity attachment, and returns only generic unavailable or masked invited email plus `confirmation_required`; it never returns event/content/account metadata. `consume_my_participation_claim(p_token_hash,p_confirm_email_attachment)` locks the invitation row, rejects all terminal/conflict cases generically, allows intended-primary/owned-alias paths to consume without confirmation, requires `p_confirm_email_attachment=true` for a conflict-free different-primary caller, prevents another confirmed primary or active alias owner, creates/reuses a caller-owned identity, activates the verified email alias, marks the token claimed, and supersedes live siblings. The proof applies to all current and future email matches per D-16; null identity_id enrichment never overwrites a conflicting nonnull link and cannot be an authorization dependency. Return only success, discovered-event count, and reminder-cancellation metadata from consume.</action>
  <acceptance_criteria>
    - Two parallel consumes produce exactly one committed success.
    - Repeated or parallel inspect calls leave invitation, alias, identity, reminder, and discovery state unchanged.
    - A different-primary consume without explicit confirmation leaves the token active; the same caller with explicit confirmation atomically attaches and claims.
    - Unknown, expired, replayed, revoked, superseded, and conflict outcomes are indistinguishable externally.
    - Claim creates verified evidence and discovery while Phase 38 content denial still passes.
  </acceptance_criteria>
  <verify>
    <automated>VITEST_INTEGRATION_OK=true npx vitest run supabase/functions/participation-claim/__tests__/participation-claim.integration.test.ts src/test/discovery-claim.integration.test.ts src/test/access-policy.integration.test.ts --maxWorkers=1 --reporter=verbose</automated>
  </verify>
  <done>One link can be inspected without mutation, then proves one email exactly once after the required account confirmation and connects every authorized event without granting recording content.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries
- Assets: invitation recipient, token digest, verified alias ownership, reminder state, participant identity.
- Boundaries: authenticated caller to SECURITY DEFINER RPC; service role to invitation table; claim proof to alias ledger.

| Threat ID | Category | Severity | Mitigation | Verification |
|---|---|---:|---|---|
| T-39-05-01 | Spoofing/IDOR | HIGH | Server-derived owner/recipient/user with canonical row locks | owner and injection matrix |
| T-39-05-02 | Tampering | HIGH | Atomic FOR UPDATE transition, unique digest, supersede on resend | race/replay tests |
| T-39-05-03 | Elevation of privilege | HIGH | FORCE RLS, service-only table, narrow RPC grants, empty search path | catalog/static tests |
| T-39-05-04 | Information disclosure | HIGH | Generic denial and minimized success projection | response equivalence/key scan |

Any HIGH failure blocks Edge implementation and TEST push.
</threat_model>

<verification>Static migration checks and both real-DB Edge contract suites pass against the migration transaction/TEST schema.</verification>
<success_criteria>Invitation and claim state is atomic, least-privilege, replay-safe, and content-neutral.</success_criteria>
<output>Create 39-05-SUMMARY.md when done.</output>
