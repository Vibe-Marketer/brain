---
phase: 39-discovery-and-claim
plan: "11"
type: execute
wave: 7
depends_on: ["39-09"]
files_modified:
  - src/components/settings/AccountTab.tsx
  - src/components/settings/__tests__/AccountTab.discovery.test.tsx
  - src/services/identity-alias.service.ts
  - src/hooks/useIdentityAliases.ts
autonomous: true
requirements: [DISCO-01, DISCO-03]
decisions: [D-01, D-02, D-16, D-17, D-19, D-20, D-21]
must_haves:
  truths:
    - "Account Settings persistently shows the distinct event count and View events after verified-email management."
    - "A non-primary verified alias can be disconnected only after confirmation, with immediate server-authoritative revocation."
    - "Disconnect never removes participant records and cannot remove the primary account email."
  artifacts:
    - path: "src/components/settings/AccountTab.tsx"
      provides: "Discovery count/action and verified-email disconnect UI"
  key_links:
    - from: "AccountTab"
      to: "useEventDiscovery/useIdentityAliases"
      via: "query count, navigation, and disconnect mutation"
      pattern: "View events|Disconnect email"
---

<objective>
Add the persistent discovery completion signal and safe alias disconnect to Account Settings.

Purpose: Make verified-email discovery visible and reversible from the existing identity surface.
Output: Updated alias service/hook, Account UI, and green tests.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-discovery-and-claim/39-UI-SPEC.md
@src/components/settings/AccountTab.tsx
@src/services/identity-alias.service.ts
@src/hooks/useIdentityAliases.ts
@src/hooks/useEventDiscovery.ts
@src/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add server-side alias disconnect through service and hook</name>
  <read_first>
    - src/services/identity-alias.service.ts
    - src/hooks/useIdentityAliases.ts
    - src/components/settings/__tests__/AccountTab.discovery.test.tsx
    - src/hooks/useEventDiscovery.ts
    - src/lib/query-config.ts
  </read_first>
  <files>src/services/identity-alias.service.ts, src/hooks/useIdentityAliases.ts, src/components/settings/__tests__/AccountTab.discovery.test.tsx</files>
  <action>Add a pure service call to the Phase 39 disconnect RPC and a TanStack mutation in useIdentityAliases. Accept alias ID only, rely on server ownership/non-primary checks, and map generic errors through IdentityAliasError. Do not optimistically remove authorization-sensitive rows. On settled invalidate identity aliases, discovery count/list, notifications, Phase 38 access/event copies, and invalidateCallListCaches(queryClient). Preserve manual six-digit verification for newly added aliases; claimed email proof does not re-enter that path.</action>
  <acceptance_criteria>
    - Primary and wrong-user aliases fail closed; non-primary active alias succeeds.
    - Success/error invalidation and exact toast behavior are tested.
    - No participant/identity/event delete is called from service or hook.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/settings/__tests__/AccountTab.discovery.test.tsx src/hooks/__tests__/useEventDiscovery.test.ts --maxWorkers=1</automated>
  </verify>
  <done>Settings can atomically revoke a verified alias through the correct data layers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render persistent discovery count, View events, and disconnect confirmation</name>
  <read_first>
    - src/components/settings/AccountTab.tsx
    - src/components/settings/__tests__/AccountTab.discovery.test.tsx
    - .planning/phases/39-discovery-and-claim/39-UI-SPEC.md
    - src/components/ui/alert-dialog.tsx
  </read_first>
  <files>src/components/settings/AccountTab.tsx, src/components/settings/__tests__/AccountTab.discovery.test.tsx</files>
  <action>Extend the existing Verified Emails section in place. Render its own count skeleton/error/retry and durable neutral callout “We found N event(s)” with View events navigating to /events even at zero. Keep count distinct/server-provided. Render Primary/Verified rows and a quiet disconnect action only for non-primary aliases. Use the exact AlertDialog title/body/buttons, disable only the affected row, wait for the server, announce exact success/failure copy, and refetch open Events state through cache invalidation. Use Remix icons and UI-SPEC spacing/typography/touch/accessibility rules.</action>
  <acceptance_criteria>
    - Loading never flashes zero and count failure leaves alias management usable.
    - Dialog copy explains visibility/future matching and preservation of participant records.
    - Claimed aliases look/behave like other verified aliases; no bulk or special permanent class appears.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/settings/__tests__/AccountTab.discovery.test.tsx --maxWorkers=1 --reporter=verbose &amp;&amp; npm run type-check</automated>
  </verify>
  <done>Account Settings clearly reports discovery and lets users safely disconnect non-primary verified emails.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries
- Assets: owned email addresses, alias authorization state, derived event visibility.
- Boundaries: Account UI to hook; disconnect mutation to caller-scoped RPC; cache to revoked server state.

| Threat ID | Category | Severity | Mitigation | Verification |
|---|---|---:|---|---|
| T-39-11-01 | Tampering/IDOR | HIGH | Alias-ID only plus server-derived caller and non-primary guard | wrong-user/primary tests |
| T-39-11-02 | Information disclosure | HIGH | No optimistic authorization change; refetch every affected cache | disconnect/cache tests |
| T-39-11-03 | Repudiation | MEDIUM | Confirmation plus success/failure feedback | dialog tests |

Any HIGH failure blocks preproduction.
</threat_model>

<verification>Account component, service/hook invalidation, type, and focused real-DB disconnect tests pass.</verification>
<success_criteria>D-01 and D-19 are delivered in the existing Settings surface without weakening identity or content boundaries.</success_criteria>
<output>Create 39-11-SUMMARY.md when done.</output>
