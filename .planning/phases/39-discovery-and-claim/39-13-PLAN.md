---
phase: 39-discovery-and-claim
plan: "13"
type: execute
wave: 7
depends_on: ["39-09"]
files_modified:
  - src/lib/pending-participation-claim.ts
  - src/pages/ParticipationClaim.tsx
  - src/pages/__tests__/ParticipationClaim.test.tsx
  - src/pages/Login.tsx
  - src/pages/OAuthCallback.tsx
  - src/components/ProtectedRoute.tsx
  - src/pages/__tests__/OAuthCallback.participation-claim.test.tsx
autonomous: true
requirements: [DISCO-02, DISCO-03]
decisions: [D-12, D-13, D-14, D-15, D-16, D-21, D-22]
must_haves:
  truths:
    - "The public entry scrubs a valid-shaped token from URL/history before rendering and stores it only in sessionStorage."
    - "Password, signup, OAuth/root, and different-account paths preserve one pending claim; intended accounts auto-consume and different-primary accounts require an explicit approved choice."
    - "All terminal token states share one generic result; retryable transport failures preserve the pending claim."
  artifacts:
    - path: "src/lib/pending-participation-claim.ts"
      provides: "Dedicated validated session-only pending claim lifecycle"
    - path: "src/pages/ParticipationClaim.tsx"
      provides: "Privacy-safe capture, prompt, processing, retry, and terminal UI"
    - path: "src/pages/OAuthCallback.tsx"
      provides: "Direct OAuth/root pending-claim restoration without token query propagation"
  key_links:
    - from: "ProtectedRoute/Login"
      to: "ParticipationClaim"
      via: "pending claim restoration after every auth completion path"
      pattern: "pendingParticipationClaim"
---

<objective>
Implement secure claim capture, non-consuming account inspection, and controlled completion across all authentication paths.

Purpose: Deliver the one-click seven-day claim experience without token persistence, private previews, or OAuth loss.
Output: Session helper, claim UI, auth restoration, and green unit tests.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-discovery-and-claim/39-UI-SPEC.md
@src/pages/Login.tsx
@src/components/ProtectedRoute.tsx
@src/App.tsx
@src/hooks/useEventDiscovery.ts
@src/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the session-only pending claim lifecycle and privacy-safe page</name>
  <read_first>
    - src/lib/pending-participation-claim.ts (create)
    - src/pages/ParticipationClaim.tsx (create)
    - src/pages/__tests__/ParticipationClaim.test.tsx
    - src/hooks/useEventDiscovery.ts
    - .planning/phases/39-discovery-and-claim/39-UI-SPEC.md
  </read_first>
  <files>src/lib/pending-participation-claim.ts, src/pages/ParticipationClaim.tsx, src/pages/__tests__/ParticipationClaim.test.tsx</files>
  <action>Create a dedicated constant/key and helpers that validate fixed token shape, store/read/clear only sessionStorage, and never return the token for logs/query keys. The claim page captures the opaque token once from the public entry URL, calls history/router replacement immediately, and renders no private preview. Signed-out users get the approved Continue CTA. Authenticated users first run one nonparallel `inspectParticipationClaim`; inspection never consumes or attaches an alias. A valid intended-account result immediately runs one `consumeParticipationClaim({ confirmEmailAttachment: false })`. A valid different-primary result renders exactly **Add email and continue** and **Use another account** with only the server-provided masked invited email: Add email invokes one atomic consume with `confirmEmailAttachment: true`; Use another account signs out/returns to auth without consume and preserves the session claim. Implement generic unavailable state, retryable transport state, token clearing only after terminal/consume success, success to /events with replace/focus/count toast, and no second six-digit verification per D-12/D-14.</action>
  <acceptance_criteria>
    - URL/history/DOM/localStorage/log assertions never contain the token after capture.
    - Generic terminal text is identical for malformed, unknown, expired, used, revoked, superseded, and conflict.
    - Retryable failure retains session token; success/terminal clears it; parallel clicks issue one request.
    - Different-primary inspection changes no server state; Add email and continue is the only UI path that sends confirmation true, and Use another account sends no consume request.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/__tests__/ParticipationClaim.test.tsx --maxWorkers=1 --reporter=verbose</automated>
  </verify>
  <done>The claim entry is privacy-safe, inspects without mutation, auto-consumes only the intended account, and requires the approved explicit choice for a different primary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore pending claims after every authentication completion path</name>
  <read_first>
    - src/pages/Login.tsx
    - src/components/ProtectedRoute.tsx
    - src/lib/pending-participation-claim.ts
    - src/pages/__tests__/ParticipationClaim.test.tsx
    - src/pages/OAuthCallback.tsx
    - src/pages/__tests__/OAuthCallback.participation-claim.test.tsx
  </read_first>
  <files>src/pages/Login.tsx, src/pages/OAuthCallback.tsx, src/components/ProtectedRoute.tsx, src/pages/__tests__/ParticipationClaim.test.tsx, src/pages/__tests__/OAuthCallback.participation-claim.test.tsx</files>
  <action>Extend password sign-in, password signup/confirmation return, `OAuthCallback`, root bootstrap, and ProtectedRoute restoration to prioritize the dedicated pending claim destination without putting the raw token in next/query parameters. Return to the clean claim page so it inspects from session storage; intended-account results consume and continue to /events, while different-primary results stop at the approved choices. Preserve pendingShareToken under its separate key and existing generic pendingNext behavior. **Use another account** signs out/returns to auth while retaining only the session claim and never calls consume. In the direct OAuthCallback test, exercise callback success and root bootstrap with a pending claim, assert replacement to the clean claim route, one inspect after session establishment, no token in URL/history/logs, no duplicate consume during rerenders, and correct continuation through both intended-account and different-primary paths.</action>
  <acceptance_criteria>
    - Password, signup, OAuth/root, and use-another-account unit flows preserve and resume the same pending claim.
    - Share-link and claim pending keys cannot overwrite or redirect each other.
    - Root bootstrap cannot loop between login/claim/events.
    - OAuth callback/root tests directly prove intended-account auto-consume and different-primary prompt preservation without relying only on ParticipationClaim mocks.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/__tests__/ParticipationClaim.test.tsx src/pages/__tests__/OAuthCallback.participation-claim.test.tsx --maxWorkers=1 --reporter=verbose &amp;&amp; npm run type-check</automated>
  </verify>
  <done>Every auth path resumes automatic claim completion instead of losing or re-exposing the token.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries
- Assets: raw claim token, authenticated session, invited email mask, redirect destination.
- Boundaries: email URL to browser; sessionStorage to auth return; authenticated claim response to navigation.

| Threat ID | Category | Severity | Mitigation | Verification |
|---|---|---:|---|---|
| T-39-13-01 | Information disclosure | HIGH | Immediate URL scrub; session-only key; no logs/analytics/cache | URL/storage/console assertions |
| T-39-13-02 | Spoofing | HIGH | Shape validation plus authenticated server consume | malformed/signed-out tests |
| T-39-13-03 | Tampering | HIGH | One in-flight mutation and server atomicity | double-click/retry tests |
| T-39-13-04 | Information disclosure | HIGH | Uniform terminal UI and authorized masked prompt only | terminal equivalence tests |

Any HIGH failure blocks route wiring and email delivery.
</threat_model>

<verification>Claim unit tests, auth restoration tests, token leak scans, and type check pass.</verification>
<success_criteria>D-12 through D-15 work through every auth route with no private pre-auth data or durable token.</success_criteria>
<output>Create 39-13-SUMMARY.md when done.</output>
