---
phase: 39-discovery-and-claim
plan: "15"
type: execute
wave: 7
depends_on: ["39-09"]
files_modified:
  - src/hooks/useNotifications.ts
  - src/components/notifications/notification-metadata.ts
  - src/components/notifications/NotificationBell.tsx
  - src/components/notifications/__tests__/NotificationBell.test.tsx
autonomous: true
requirements: [DISCO-01, DISCO-03]
decisions: [D-17, D-18, D-19, D-21, D-22]
must_haves:
  truths:
    - "Existing notification polling idempotently syncs future event matches without creating a second notification center."
    - "One privacy-safe notification per new user/event routes only to authorized Events."
    - "Lost authorization neutralizes the action and reveals no reason or private metadata."
  artifacts:
    - path: "src/components/notifications/notification-metadata.ts"
      provides: "Strict future-event notification metadata guard"
    - path: "src/components/notifications/NotificationBell.tsx"
      provides: "View event action through existing bell"
  key_links:
    - from: "useNotifications"
      to: "sync_my_discovered_event_notifications"
      via: "idempotent eventDiscoveryService call before fetch"
      pattern: "sync.*Event"
---

<objective>
Extend the existing in-app notification system for exact-once future event discovery.

Purpose: Notify users of later matches without email digests, historical floods, or private event labels.
Output: Sync integration, strict metadata guard, Events navigation, and green tests.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-discovery-and-claim/39-UI-SPEC.md
@src/hooks/useNotifications.ts
@src/components/notifications/notification-metadata.ts
@src/components/notifications/NotificationBell.tsx
@src/services/event-discovery.service.ts
@src/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Synchronize future matches and validate strict notification metadata</name>
  <read_first>
    - src/hooks/useNotifications.ts
    - src/components/notifications/notification-metadata.ts
    - src/components/notifications/__tests__/NotificationBell.test.tsx
    - src/services/event-discovery.service.ts
    - src/lib/query-config.ts
  </read_first>
  <files>src/hooks/useNotifications.ts, src/components/notifications/notification-metadata.ts, src/components/notifications/__tests__/NotificationBell.test.tsx</files>
  <action>Before the existing authenticated notification list fetch/poll, call the pure event-discovery service wrapper for `sync_my_discovered_event_notifications()` once per query cycle; together with count/list refreshes in Plan 39-09 this is the complete caller-pull trigger, with database uniqueness authoritative and no pg_cron dependency. Add a strict type guard for the Phase 39 kind accepting only event_id and approved action semantics, rejecting title/owner/provider/email/recording/roster/count fields. Preserve existing notification CRUD and recording-access metadata. Ensure failures in sync do not erase existing notifications and do not surface diagnostic privacy details.</action>
  <acceptance_criteria>
    - Repeated polling calls sync safely and does not duplicate UI/database notifications.
    - Invalid/private-rich metadata is treated as nonactionable.
    - Existing health, reporter, and recording-access notification tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/notifications/__tests__/NotificationBell.test.tsx --maxWorkers=1 --reporter=verbose</automated>
  </verify>
  <done>The existing notification query safely activates exact-once future-match sync.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render privacy-safe New event found actions</name>
  <read_first>
    - src/components/notifications/NotificationBell.tsx
    - src/components/notifications/notification-metadata.ts
    - src/components/notifications/__tests__/NotificationBell.test.tsx
    - .planning/phases/39-discovery-and-claim/39-UI-SPEC.md
  </read_first>
  <files>src/components/notifications/NotificationBell.tsx, src/components/notifications/__tests__/NotificationBell.test.tsx</files>
  <action>Add the exact “New event found” / generic body / “View event →” treatment to the existing bell. Build navigation internally from validated event metadata to /events with an optional focus hint; never trust metadata.route or render event titles/details. Mark read through existing behavior. When authorization no longer exists, remove/neutralize the action and show only “This event is no longer available.” Do not add toast-only alerts, a second inbox, recurring email, or digest.</action>
  <acceptance_criteria>
    - Valid future-event notification navigates to /events and marks read.
    - Invalid/revoked metadata has no actionable private route/detail.
    - Existing notification behavior and accessible controls remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/notifications/__tests__/NotificationBell.test.tsx --maxWorkers=1 &amp;&amp; npm run type-check</automated>
  </verify>
  <done>Users receive one durable, privacy-safe in-app path to newly matched events.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries
- Assets: event existence, notification action metadata, authorization after disconnect.
- Boundaries: database metadata to type guard; notification row to router; polling sync to uniqueness ledger.

| Threat ID | Category | Severity | Mitigation | Verification |
|---|---|---:|---|---|
| T-39-15-01 | Information disclosure | HIGH | Strict metadata guard and generic copy | private-key tests |
| T-39-15-02 | Tampering | HIGH | Construct route from validated UUID; never trust metadata.route | navigation guard tests |
| T-39-15-03 | Repudiation/DoS | MEDIUM | Server unique ledger and idempotent polling | repeat-sync tests |
| T-39-15-04 | Information disclosure | HIGH | Authorization refetch neutralizes stale actions | disconnect/revocation tests |

Any HIGH failure blocks preproduction.
</threat_model>

<verification>Notification unit tests, discovery integration sync/dedupe tests, disconnect regression, and type check pass.</verification>
<success_criteria>D-18 uses the existing in-app bell exactly once per future event with no ongoing email or private metadata.</success_criteria>
<output>Create 39-15-SUMMARY.md when done.</output>
