---
phase: 39-discovery-and-claim
plan: "10"
type: execute
wave: 7
depends_on: ["39-09"]
files_modified:
  - src/pages/Events.tsx
  - src/components/events/EventCard.tsx
  - src/components/events/EventListStates.tsx
  - src/pages/__tests__/Events.test.tsx
autonomous: true
requirements: [DISCO-01, DISCO-03]
decisions: [D-02, D-03, D-04, D-05, D-16, D-17, D-21, D-22]
must_haves:
  truths:
    - "D-02: View events opens the dedicated Events page, which renders server-ordered action, available, and waiting groups with newest events first."
    - "Readable copies show permitted fields; restricted copies show anonymous ordinals/request states only."
    - "Loading, empty, error, pagination, request, and lost-authorization states are accessible and privacy-safe."
  artifacts:
    - path: "src/pages/Events.tsx"
      provides: "Protected Events surface over useEventDiscovery"
    - path: "src/components/events/EventCard.tsx"
      provides: "Safe readable/restricted recording composition and request action"
  key_links:
    - from: "src/pages/Events.tsx"
      to: "src/hooks/useEventDiscovery.ts"
      via: "paged server-ordered query"
      pattern: "use.*Event"
---

<objective>
Implement the approved dedicated card-based Events experience over the safe Phase 39 projection.

Purpose: Turn verified-email discovery into an actionable surface without exposing restricted recording details.
Output: Events page, card/state components, and green component/privacy tests.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-discovery-and-claim/39-UI-SPEC.md
@src/components/call-detail/OtherRecordingCopies.tsx
@src/hooks/useRecordingAccess.ts
@src/components/ui/card.tsx
@src/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build privacy-safe event cards and list states</name>
  <read_first>
    - src/components/events/EventCard.tsx (create)
    - src/components/events/EventListStates.tsx (create)
    - src/pages/__tests__/Events.test.tsx
    - src/components/call-detail/OtherRecordingCopies.tsx
    - src/types/event-discovery.ts
    - .planning/phases/39-discovery-and-claim/39-UI-SPEC.md
  </read_first>
  <files>src/components/events/EventCard.tsx, src/components/events/EventListStates.tsx, src/pages/__tests__/Events.test.tsx</files>
  <action>Implement semantic article cards using only the safe domain type. Choose a readable title only from an authorized readable copy; otherwise render “Event on [date].” Show semantic time, caller connection, allowed readable-copy metadata/Open recording, and restricted “N other recordings” with ordinal rows and existing Phase 38 request states/actions. Keep request accessible labels to ordinal/date only. Add UI-SPEC skeletons, empty/error/no-longer-available, trailing pagination skeleton, live regions, focus retention, and reduced-motion handling. Never enrich cards from calls/participants in React or place restricted fields in hidden DOM/test IDs/tooltips/logs.</action>
  <acceptance_criteria>
    - DOM privacy scans pass for title/owner/provider/transcript/summary/roster/source/ID fixtures.
    - Request pending/cooldown/success/refetch states use exact UI-SPEC copy and affect one row.
    - Semantic headings, list/articles, time, aria-busy/live/alert, focus ring, and mobile targets pass tests.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/__tests__/Events.test.tsx --maxWorkers=1 --reporter=verbose</automated>
  </verify>
  <done>Event cards expose useful permitted actions without transporting or rendering restricted metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose server-ordered groups and bounded load-more page</name>
  <read_first>
    - src/pages/Events.tsx (create)
    - src/components/events/EventCard.tsx
    - src/components/events/EventListStates.tsx
    - src/pages/__tests__/Events.test.tsx
    - src/hooks/useEventDiscovery.ts
    - src/pages/PeoplePage.tsx
  </read_first>
  <files>src/pages/Events.tsx, src/pages/__tests__/Events.test.tsx</files>
  <action>Create the authenticated page with title EVENTS and approved subtitle. Consume the infinite hook, preserve server priority/order without hidden-data reclassification, render Needs your action, Available to you, then Waiting for access only when nonempty, and append through “Load more events.” Use one readable column within existing AppShell patterns. Refetch after request/authorization changes, preserve focus, and handle an optional authorized event focus target from a notification without disclosing nonexistent/unauthorized IDs.</action>
  <acceptance_criteria>
    - Page never flashes zero before load and never reorders existing cards during pagination.
    - Empty action links to Account Settings; all exact copy/group ordering matches UI-SPEC.
    - No direct service/Supabase calls or alternate content authorization logic exists.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/__tests__/Events.test.tsx --maxWorkers=1 &amp;&amp; npm run type-check</automated>
  </verify>
  <done>A user can browse every authorized event and request a restricted copy from the dedicated page.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries
- Assets: restricted recording metadata, event existence, request target/state.
- Boundaries: safe hook data to DOM/accessibility tree; request control to Phase 38 mutation.

| Threat ID | Category | Severity | Mitigation | Verification |
|---|---|---:|---|---|
| T-39-10-01 | Information disclosure | HIGH | Render only discriminated safe types; no client enrichment | DOM/test-id/log privacy scans |
| T-39-10-02 | Tampering | MEDIUM | Preserve server ordering and request state | grouping/pagination/race tests |
| T-39-10-03 | IDOR | HIGH | Request uses opaque authorized server target through Phase 38 hook | request integration/component tests |

Any HIGH failure blocks route wiring and preproduction.
</threat_model>

<verification>Focused Events tests, type check, accessibility assertions, and restricted-string scans pass.</verification>
<success_criteria>The dedicated Events experience satisfies D-02 through D-05 while Phase 38 remains the content authority.</success_criteria>
<output>Create 39-10-SUMMARY.md when done.</output>
