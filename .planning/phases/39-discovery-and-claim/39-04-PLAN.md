---
phase: 39-discovery-and-claim
plan: "04"
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - supabase/migrations/20260920000001_phase39_verified_email_discovery.sql
  - src/test/discovery-claim.integration.test.ts
  - src/test/migrations/phase39-discovery-claim-migrations.test.ts
autonomous: true
requirements: [DISCO-01, DISCO-03]
decisions: [D-01, D-02, D-03, D-04, D-05, D-16, D-17, D-20, D-21, D-22, D-23]
must_haves:
  truths:
    - "Caller discovery derives identity only from confirmed primary email and active verified aliases."
    - "Direct events RLS and caller-scoped RPCs agree while Phase 38 alone governs recording content."
    - "Legacy organization-scoped People RPC signatures, grants, and response shapes remain unchanged."
  artifacts:
    - path: "supabase/migrations/20260920000001_phase39_verified_email_discovery.sql"
      provides: "Verified-email helper, hardened event/Phase 38 predicates, and bounded safe discovery/count/copy RPCs"
  key_links:
    - from: "list_my_discovered_events"
      to: "phase38_user_can_access_recording"
      via: "server-side safe projection"
      pattern: "phase38_user_can_access_recording"
---

<objective>
Create the verified-email authorization seam and privacy-safe caller-scoped discovery contract.

Purpose: Deliver DISCO-01 without widening direct table access or changing legacy People APIs.
Output: One additive migration plus passing static and real-DB authorization/privacy tests.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-discovery-and-claim/39-RESEARCH.md
@.planning/phases/39-discovery-and-claim/39-PATTERNS.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-VERIFICATION.md
@supabase/migrations/20260309120000_call_participants.sql
@supabase/migrations/20260831020000_fix_events_participation_rls_and_updated_at_trigger.sql
@supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the current-caller email and confirmed-participation security seam</name>
  <read_first>
    - supabase/migrations/20260920000001_phase39_verified_email_discovery.sql (create)
    - src/test/discovery-claim.integration.test.ts
    - src/test/migrations/phase39-discovery-claim-migrations.test.ts
    - supabase/migrations/20260905140000_create_identities_and_link_tables.sql
    - supabase/migrations/20260831020000_fix_events_participation_rls_and_updated_at_trigger.sql
    - supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql
  </read_first>
  <files>supabase/migrations/20260920000001_phase39_verified_email_discovery.sql, src/test/discovery-claim.integration.test.ts, src/test/migrations/phase39-discovery-claim-migrations.test.ts</files>
  <action>Write the RED assertions first, then add a private SECURITY DEFINER current-caller-email helper with no subject/email argument. It must derive auth.uid(), include auth.users.email only when confirmed, union normalized active verified email aliases owned by the caller, use search_path='', schema-qualified relations, and expose no direct client execute grant per D-20. Add an indexed lower(email)-to-event path. Replace the participant branch of events SELECT with current verified email plus confirmed speech/host/organizer evidence while preserving the owned-recording branch. Redefine Phase 38 confirmed-participant and event-size helper bodies without requiring call_participants.identity_id; preserve names/signatures, webinar detection, strict less-than-50 boundary, and content predicate behavior per D-21/D-22.</action>
  <acceptance_criteria>
    - Calendar-only, unverified, disconnected, name, domain, and organization evidence cannot SELECT an event.
    - Confirmed primary and active verified alias work even when identity_id is null.
    - Phase 38 49/50/webinar/content regressions pass and helper execute privileges remain least-privilege.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/test/migrations/phase39-discovery-claim-migrations.test.ts --maxWorkers=1 &amp;&amp; VITEST_INTEGRATION_OK=true npx vitest run src/test/discovery-claim.integration.test.ts src/test/access-policy.integration.test.ts src/test/rls-regression.test.ts --maxWorkers=1</automated>
  </verify>
  <done>Current verified email evidence is the single caller authorization seam across direct RLS and Phase 38 participant checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bounded caller-scoped event count/list/copy RPCs</name>
  <read_first>
    - supabase/migrations/20260920000001_phase39_verified_email_discovery.sql
    - src/test/discovery-claim.integration.test.ts
    - supabase/migrations/20260309120000_call_participants.sql
    - supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql
  </read_first>
  <files>supabase/migrations/20260920000001_phase39_verified_email_discovery.sql, src/test/discovery-claim.integration.test.ts</files>
  <action>Add the distinct authenticated caller functions `count_my_discovered_events()` and `list_my_discovered_events(p_limit,p_cursor)`; do not overload or replace get_people_summary/get_recordings_for_person per D-23. Derive caller identity internally, deduplicate by event, exclude disallowed webinar/large events, cap page size at 50, and use a stable cursor/ordering of action-needed, available, waiting, event time descending, event ID tie-break. Return only event ID/time, caller connection evidence, readable-copy allowlisted fields when Phase 38 grants content, and the exact anonymous ordinal/request-state shape for restricted copies. Compute in SQL without per-card RPC fanout and expose generic unavailable behavior.</action>
  <acceptance_criteria>
    - Count is distinct across multiple caller emails and list order/pagination is stable.
    - Restricted payloads contain no title, owner, provider, transcript, summary, roster, participant count, source ID, URL, or duration.
    - Legacy People function identity arguments, result columns, and grants match their pre-migration catalog definitions.
  </acceptance_criteria>
  <verify>
    <automated>VITEST_INTEGRATION_OK=true npx vitest run src/test/discovery-claim.integration.test.ts src/test/access-policy.integration.test.ts src/test/rls-regression.test.ts --maxWorkers=1 --reporter=verbose</automated>
  </verify>
  <done>Authenticated users can count and page every authorized event through a server-safe projection only.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries
- Assets: caller-owned email evidence, event existence, restricted recording metadata, legacy API compatibility.
- Boundaries: JWT identity to SECURITY DEFINER helper; helper to direct RLS; safe RPC projection to authenticated browser.

| Threat ID | Category | Severity | Mitigation | Verification |
|---|---|---:|---|---|
| T-39-04-01 | Spoofing | HIGH | No caller-supplied user/email; confirmed current database evidence only | authorization matrix |
| T-39-04-02 | Elevation of privilege | HIGH | search_path='', qualification, revoke internal execute, explicit grants | catalog/static assertions |
| T-39-04-03 | Information disclosure | HIGH | Safe SQL projection plus direct events policy repair | raw-key and direct-table tests |
| T-39-04-04 | Denial of service | MEDIUM | Indexed normalized lookup and max-50 cursor pagination | query/pagination tests |

Any HIGH failure blocks TEST migration push.
</threat_model>

<verification>Static security assertions, discovery real-DB matrix, Phase 38 access suite, and RLS regression must all pass sequentially.</verification>
<success_criteria>DISCO-01 and the DISCO-03 existence-only boundary work with sparse identity links and unchanged legacy People APIs.</success_criteria>
<output>Create 39-04-SUMMARY.md when done.</output>
