---
phase: 39-discovery-and-claim
plan: "09"
type: execute
wave: 6
depends_on: ["39-03", "39-08"]
files_modified:
  - src/types/event-discovery.ts
  - src/services/event-discovery.service.ts
  - src/services/__tests__/event-discovery.service.test.ts
  - src/hooks/useEventDiscovery.ts
  - src/hooks/__tests__/useEventDiscovery.test.ts
  - src/lib/query-config.ts
autonomous: true
requirements: [DISCO-01, DISCO-02, DISCO-03]
decisions: [D-01, D-03, D-04, D-05, D-06, D-09, D-10, D-12, D-13, D-16, D-17, D-18, D-19, D-20, D-21, D-22]
must_haves:
  truths:
    - "Only a narrow camelCase safe projection crosses from Supabase into React."
    - "Services are pure async TypeScript; hooks are the only React/TanStack layer."
    - "Every mutation invalidates precise Phase 39 caches and full call-list caches onSettled."
  artifacts:
    - path: "src/types/event-discovery.ts"
      provides: "Safe discovery, invitation, claim, and notification domain contracts"
    - path: "src/services/event-discovery.service.ts"
      provides: "Strict RPC/Edge parsing and mutations"
    - path: "src/hooks/useEventDiscovery.ts"
      provides: "TanStack queries, mutations, pagination, and invalidation"
  key_links:
    - from: "src/hooks/useEventDiscovery.ts"
      to: "src/services/event-discovery.service.ts"
      via: "query and mutation functions"
      pattern: "eventDiscoveryService"
---

<objective>
Build the typed service and TanStack hook layer over the proven TEST backend contract.

Purpose: Give every Phase 39 UI a single strict, privacy-safe, cache-consistent API.
Output: Domain types, pure service, query keys, hooks, and green unit tests.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-discovery-and-claim/39-PATTERNS.md
@.planning/phases/39-discovery-and-claim/39-UI-SPEC.md
@src/types/recording-access.ts
@src/services/recording-access.service.ts
@src/hooks/useRecordingAccess.ts
@src/lib/query-config.ts
@src/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define allowlisted Phase 39 domain contracts</name>
  <read_first>
    - src/types/event-discovery.ts (create)
    - src/types/recording-access.ts
    - src/types/supabase.ts
    - src/services/__tests__/event-discovery.service.test.ts
    - .planning/phases/39-discovery-and-claim/39-UI-SPEC.md
  </read_first>
  <files>src/types/event-discovery.ts, src/services/__tests__/event-discovery.service.test.ts</files>
  <action>Define discriminated types for safe event cards/groups, readable copy details, anonymous copy ordinal/request state, connection evidence, cursor/count, invitation status/dates/reminder capability, and strict future-notification metadata. Define a narrow claim inspect result of generic unavailable or valid `{ maskedInvitedEmail, confirmationRequired }`, plus generic/retryable consume results; do not allow event/account metadata in either contract. Keep raw snake_case RPC shapes private to the service and add no open metadata bag. Encode D-05/D-14/D-21/D-22 by making restricted owner/provider/title/content/roster fields unrepresentable. Update RED parser expectations first.</action>
  <acceptance_criteria>
    - Restricted copy and notification types have no private-field escape hatch.
    - Group/action/invitation/claim states are exhaustive discriminated unions.
    - Type tests compile under strict mode with no any.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/__tests__/event-discovery.service.test.ts --maxWorkers=1 &amp;&amp; npm run type-check</automated>
  </verify>
  <done>The frontend contract cannot represent a forbidden Phase 39 payload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict discovery, invitation, claim, and sync services</name>
  <read_first>
    - src/services/event-discovery.service.ts (create)
    - src/services/__tests__/event-discovery.service.test.ts
    - src/services/recording-access.service.ts
    - src/integrations/supabase/client.ts
    - src/types/event-discovery.ts
  </read_first>
  <files>src/services/event-discovery.service.ts, src/services/__tests__/event-discovery.service.test.ts</files>
  <action>Implement pure async list/count, invitation-status/send/resend/cancel-reminder, notification sync, and server-state calls using the exact generated TEST RPC/function names. Add `inspectParticipationClaim(token)` invoking `participation-claim` with `{ mode: 'inspect', token }` and `consumeParticipationClaim({ token, confirmEmailAttachment })` invoking `{ mode: 'consume', token, confirmEmailAttachment }`; callers cannot pass user/email/event identity. Validate UUIDs, token shape, cursor/limit (max 50), and every unknown response field; map snake_case to narrow domain types and reject malformed/forbidden keys. Map all unavailable claim codes to one public error, allow only masked email plus confirmationRequired from valid inspect, and never log/cache raw token or response payload. Keep request-access behavior delegated to the existing Phase 38 service/hook.</action>
  <acceptance_criteria>
    - Service performs no React/query/toast work and exports one coherent eventDiscoveryService API.
    - Malformed, extra-private-field, unbounded, and non-UUID inputs fail closed.
    - Raw claim tokens are passed only in inspect/consume request bodies and never used as cache/log identifiers.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/__tests__/event-discovery.service.test.ts --maxWorkers=1 --reporter=verbose</automated>
  </verify>
  <done>All Phase 39 network/database calls cross one strict pure-service boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement query keys, hooks, and complete mutation invalidation</name>
  <read_first>
    - src/hooks/useEventDiscovery.ts (create)
    - src/hooks/__tests__/useEventDiscovery.test.ts
    - src/lib/query-config.ts
    - src/hooks/useRecordingAccess.ts
    - src/hooks/useIdentityAliases.ts
    - src/services/event-discovery.service.ts
  </read_first>
  <files>src/hooks/useEventDiscovery.ts, src/hooks/__tests__/useEventDiscovery.test.ts, src/lib/query-config.ts</files>
  <action>Add stable eventDiscovery query families for count, paged lists, invitation status by recording/participant, and notification sync without raw tokens. On each authenticated count/list refresh, call the pure `sync_my_discovered_event_notifications()` service before fetching the requested discovery projection; keep the sync idempotent/caller-pulled and add no timer, pg_cron, or import-pipeline dependency. Implement queries/infinite pagination and mutations that call only the service. Expose a non-persisted `useInspectParticipationClaim` mutation and `useConsumeParticipationClaim`; neither places the token in a query key or durable cache. Prevent parallel inspect/consume submissions, require the UI to pass `confirmEmailAttachment: true` only from Add email and continue, and keep Use another account free of any consume call. Claim settlement must invalidate discovery, identity aliases, notifications, Phase 38 access/event-copy keys, and `invalidateCallListCaches(queryClient)`; disconnect settlement uses the same full invalidation, while invite/resend/cancel invalidates exact participant status plus affected call detail. Preserve server ordering and log only safe operation names.</action>
  <acceptance_criteria>
    - Every mutation has onSettled invalidation and tests cover success/error.
    - No component or hook calls Supabase directly for new Phase 39 behavior.
    - Claim token never appears in query keys, errors, logger calls, or durable cache state.
    - Hook tests prove inspect is non-consuming, different-account consume requires explicit confirmation, and Use another account cannot trigger consume.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/hooks/__tests__/useEventDiscovery.test.ts src/services/__tests__/event-discovery.service.test.ts --maxWorkers=1 &amp;&amp; npm run type-check</automated>
  </verify>
  <done>Phase 39 UI consumers have cache-safe hooks over strict services.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries
- Assets: safe event projection, raw claim token, cache authorization state, invitation actions.
- Boundaries: Supabase response to parser; service domain object to hook cache; mutation settlement to cross-feature caches.

| Threat ID | Category | Severity | Mitigation | Verification |
|---|---|---:|---|---|
| T-39-09-01 | Information disclosure | HIGH | Exact parser allowlists and no open metadata | malformed/forbidden-key tests |
| T-39-09-02 | Information disclosure | HIGH | No token in keys/logs/errors/durable cache | source and runtime assertions |
| T-39-09-03 | Tampering | MEDIUM | Server ordering/status authoritative; bounded inputs | service/hook tests |
| T-39-09-04 | Information disclosure | HIGH | Comprehensive onSettled invalidation after revocation | invalidation tests |

Any HIGH failure blocks all UI plans.
</threat_model>

<verification>Focused service/hook tests and zero-new-error type check pass; source scan finds no direct Phase 39 Supabase calls in components.</verification>
<success_criteria>All Phase 39 UI data crosses a strict typed service-hook boundary with complete invalidation.</success_criteria>
<output>Create 39-09-SUMMARY.md when done.</output>
