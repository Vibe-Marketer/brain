---
phase: 06.3-obsidian-sync-improvements
plan: 01
subsystem: services
tags: [obsidian-export, supabase, pagination, api-tokens]
requires:
  - phase: 06.2-callvault-rest-api
    provides: canonical API token direction and removal of Obsidian-specific token path
provides:
  - paged full-vault export retrieval for Obsidian ZIP generation
  - mocked 5,000-call export service regression coverage
affects: [obsidian-export, settings-export, api-tokens]
tech-stack:
  added: []
  patterns: [paged Supabase service retrieval, typed export row mapping]
key-files:
  created:
    - src/services/__tests__/api-tokens.service.test.ts
  modified:
    - src/services/api-tokens.service.ts
key-decisions:
  - "Full-vault export pages workspaces, workspace_entries, and recordings with a 1,000-row page size until exhausted."
  - "Provider share URLs are resolved through the shared resolveShareUrl boundary with provider-specific metadata bridged into share_url."
patterns-established:
  - "Export services should page Supabase list queries explicitly instead of relying on default PostgREST limits."
requirements-completed:
  - Phase-06.3-SC-1
  - Phase-06.3-SC-4
duration: 20min
completed: 2026-06-10
---

# Phase 06.3-01: Full-Vault Export Service Summary

**Paged Supabase export retrieval with typed row mapping and 5,000-call regression coverage**

## Performance

- **Duration:** 20 min
- **Started:** 2026-06-10T06:00:00Z
- **Completed:** 2026-06-10T06:05:00Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- Added explicit pagination for workspaces, workspace entries, and recordings used by `fetchAllCallsForObsidianExport()`.
- Added a mocked Supabase service test proving 5,000 recordings are returned without truncation and the final row is present.
- Replaced broad export-row casts with local row types and mapped source URLs through `resolveShareUrl()`.

## Task Commits

Plan implementation and summary were committed together for this short plan.

## Files Created/Modified

- `src/services/api-tokens.service.ts` - Adds paged Supabase fetch helpers, typed export rows, workspace-entry mapping, and shared share URL resolution.
- `src/services/__tests__/api-tokens.service.test.ts` - Covers 5,000-row pagination, workspace name mapping, canonical UUID output, and Fathom/Zoom/missing URL cases.

## Decisions Made

- Used a named `EXPORT_PAGE_SIZE = 1000` to match Supabase page behavior while avoiding product-level export caps.
- Kept the public `fetchAllCallsForObsidianExport(orgId)` signature unchanged.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Initial test assertion inspected only the first recording query instance. The service creates a fresh query per page, so the test now aggregates ranges across query instances.

## Verification

- `npm test -- src/services/__tests__/api-tokens.service.test.ts` - passed.
- `npm run type-check` - passed.
- `! rg -n "\\(rec as any\\)|\\(entry as any\\)|limit\\(500\\)|slice\\(0,\\s*500\\)|500-record" src/services/api-tokens.service.ts` - passed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 02 can consume the service output knowing the data path returns 5,000 mocked calls and preserves canonical UUID, workspace, transcript, summary, and source URL fields.

---
*Phase: 06.3-obsidian-sync-improvements*
*Completed: 2026-06-10*
