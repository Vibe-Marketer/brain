# Phase 06.3 Verification — Obsidian Sync Improvements

Status values: `verified`, `human_needed`, `gaps_found`.

| Criterion | Status | Evidence |
|---|---|---|
| One markdown file per included call | verified | `src/lib/__tests__/export-utils.obsidian.test.ts` asserts one file per included call and exactly 5,000 represented calls in ZIP output. Browser smoke downloaded a ZIP with 57 markdown files. |
| Required YAML front matter | verified | `export-utils.obsidian.test.ts` asserts `callvault_id`, `workspace`, `tags`, `has_transcript`, participant wikilinks, share URL, and duplicate-safe `vault_path`. Browser smoke confirmed YAML front matter in `CallVault/Lead Gen Jay/IMPORT/2025-08-22-cold-to-sold.md`. |
| Obsidian-compatible body | verified | Tests assert transcript text is preserved under `## Transcript` and missing transcript output says `Transcript not available.` Browser smoke confirmed `## Transcript` in the inspected markdown file. |
| At least 5,000 realistic transcript-bearing calls represented | verified | Service test creates 5,000 recording rows and asserts `calls.length` equals `5000` plus final row presence. ZIP test creates 5,000 transcript-bearing calls and asserts exactly 5,000 markdown files, including transcript body 4999. |
| No product-level 500-record cap | verified | Final scan passed: `! rg -n "slice\\(0,\\s*500\\)|limit\\(500\\)|500-record" src/services/api-tokens.service.ts src/lib/export-utils.ts`. |
| `obsidian-sync` not active | verified | Final scan passed: `! rg -n "/obsidian-sync|useObsidianTokens|generateObsidianToken|Generate Obsidian Token" src docs cloudflare supabase/functions`; `supabase/functions/obsidian-sync/index.ts` is absent. |
| Browser smoke | verified | Local app at `http://127.0.0.1:3001/settings/integrations` downloaded `callvault-obsidian-2026-06-10.zip`; inspected markdown path had YAML and transcript body. |

## Command Evidence

- `npm test -- src/services/__tests__/api-tokens.service.test.ts src/lib/__tests__/export-utils.obsidian.test.ts src/components/settings/__tests__/ApiTokensSection.test.tsx` - passed; 12 tests.
- `npm run type-check` - passed.
- `npm run build` - passed.
- No 500-record cap scan - passed.
- Active `obsidian-sync` absence scan - passed.

## Overall Result

`verified` - Phase 06.3 success criteria are met locally and browser-smoked. The production account used for smoke had 57 calls; the 5,000-call requirement is verified through deterministic automated tests rather than a 5,000-call live production export.
