# Phase 06.3: Obsidian Sync Improvements - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-10
**Phase:** 06.3-obsidian-sync-improvements
**Areas discussed:** Export surface and user flow, Markdown and vault format, Data source and metadata correctness, Backend and API boundary, Verification and scale

---

## Matched Todos

| Option | Description | Selected |
|--------|-------------|----------|
| Fold none | Treat compliance and Fathom metadata as separate work, not Obsidian export context. | ✓ |
| Fathom only | Carry the Fathom metadata todo as reviewed context because Obsidian export depends on call metadata quality. | |
| Review all | Bring all three matched todos into discussion, even though two looked like false positives. | |

**User's choice:** `1`
**Notes:** No matched todos were folded into Phase 06.3. The three matched todos were recorded as reviewed-but-deferred in CONTEXT.md.

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Export surface and user flow | Where should `Download all calls` live, and should Smart Export and API Tokens both keep Obsidian export entry points? | ✓ |
| Markdown and vault format | Lock folder structure, front matter fields, transcript formatting, participants, timestamps, and tags. | ✓ |
| Data source and metadata correctness | Decide how export should fetch calls, workspace names, source/share URLs, participants, and summaries. | ✓ |
| Backend and API boundary | Clarify that `obsidian-sync` is gone/deprecated, API tokens are canonical, and future plugin sync uses REST. | ✓ |
| Verification and scale | Define proof for full-vault export, YAML/filename edge cases, and browser/manual ZIP inspection. | ✓ |

**User's choice:** `RUN WITH ASSUMPTIONS AND LMK WHAT YOU NEED TO KNOW.`
**Notes:** Treated as approval to use builder assumptions for all five gray areas and capture context without further blocking questions.

---

## Export Surface and User Flow

| Option | Description | Selected |
|--------|-------------|----------|
| API Tokens as primary | Settings API Tokens owns full-vault export and API-token setup; browser ZIP requires no token. | ✓ |
| Smart Export as primary | Make export dialog the primary Obsidian entry point. | |
| Split equally | Keep both surfaces without defining priority. | |

**User's choice:** Assumed API Tokens as primary.
**Notes:** This matches Phase 06.2's REST/API-token direction while preserving selected-call export in SmartExportDialog as a secondary path.

---

## Markdown and Vault Format

| Option | Description | Selected |
|--------|-------------|----------|
| Predictable CallVault tree | `CallVault/{org}/{workspace}/{date}-{title}.md` with stable front matter and readable body. | ✓ |
| Flat folder | Put all markdown files in one folder. | |
| Plugin-specific format | Optimize around a not-yet-built plugin contract. | |

**User's choice:** Assumed predictable CallVault tree.
**Notes:** Current implementation already uses this structure. Context locks it as the canonical base export shape.

---

## Data Source and Metadata Correctness

| Option | Description | Selected |
|--------|-------------|----------|
| RLS-backed browser export | Fetch via frontend service using authenticated session and workspace_entries mapping. | ✓ |
| Backend ZIP endpoint | Recreate an Edge Function for ZIP generation. | |
| Plugin-only sync | Defer ZIP export to future plugin sync. | |

**User's choice:** Assumed RLS-backed browser export.
**Notes:** Context records cleanup needs: remove `any` casts, use canonical share URL helper patterns, and avoid new numeric-ID dependencies.

---

## Backend and API Boundary

| Option | Description | Selected |
|--------|-------------|----------|
| REST/API tokens canonical | Future Obsidian sync uses `api.callvaultai.com/v1/*` and `token_source = 'api'`. | ✓ |
| Restore obsidian-sync | Bring back a separate Obsidian-only Edge Function and token source. | |
| MCP-backed sync | Use MCP as the Obsidian integration API. | |

**User's choice:** Assumed REST/API tokens canonical.
**Notes:** Current tree has already removed `src/services/obsidian-tokens.service.ts` and `supabase/functions/obsidian-sync/index.ts`; user clarified `obsidian-sync` should be gone, or at minimum needs to be removed/deprecated wherever remnants still exist.

---

## Verification and Scale

| Option | Description | Selected |
|--------|-------------|----------|
| Automated first, manual smoke second | Test ZIP/markdown deterministically at full-vault scale, then inspect a generated ZIP as final smoke. | ✓ |
| Manual only | Download a ZIP manually and inspect it. | |
| Defer scale proof | Leave full-vault proof for later. | |

**User's choice:** Assumed automated first, manual smoke second; corrected by user.
**Notes:** User rejected a 500-record ceiling and gave an owner-scale case around 1,500 calls. Context now requires no product-level 500 cap and at least 1,500 realistic transcript-bearing calls in scale proof. If a single browser ZIP cannot handle that, planning must design batching, server-side/streaming generation, or multi-part ZIPs rather than silently truncating.

---

## User Correction: Scale and obsidian-sync

**User's correction:** `FOR 4.. YES IT SHOULD BE GONE OR AT LEAST NEEDS TO BE.. I ACTUALLY WANT MORE THAN "500" CALLS.. HOW AM I SUPPOSED TO DO A FULL EXPORT FOR MY OWN CALLS (1500?) WHEN WE HAVE A 500 LIMIT? THAT'S NOT GONNA WORK?`

**Captured changes:**
- `obsidian-sync` should be gone; if remnants remain, cleanup must remove or explicitly deprecate them.
- Full export means the user's full vault, not an arbitrary 500-record subset.
- Known owner-scale case is approximately 1,500 calls.
- The roadmap success criterion was updated from "up to 500 recordings" to "full vault, at least 1,500 recordings, no product-level 500 ceiling."

---

## the agent's Discretion

- The user delegated gray-area resolution with "run with assumptions."
- Exact test shape, fixture sizes, cleanup order, and implementation details remain planner discretion.

## Deferred Ideas

- Per-call Obsidian export — Phase 06.3.1.
- `legacy_recording_id` rename — Phase 06.3.2.
- Fathom metadata refresh quality — connector/Fathom follow-up.
- Full Obsidian plugin implementation and sync interval design — future REST/API integration work.
