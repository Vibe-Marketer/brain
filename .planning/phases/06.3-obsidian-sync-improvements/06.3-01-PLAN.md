---
phase: 06.3-obsidian-sync-improvements
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/services/api-tokens.service.ts
  - src/services/__tests__/api-tokens.service.test.ts
autonomous: true
requirements:
  - Phase-06.3-SC-1
  - Phase-06.3-SC-4
must_haves:
  truths:
    - "Full-vault Obsidian export fetches every accessible recording for the org, not only the first default Supabase page."
    - "The export service maps workspaces, transcripts, summaries, participants, source URL, and canonical UUIDs without adding new numeric-ID dependencies."
  artifacts:
    - path: "src/services/api-tokens.service.ts"
      provides: "paged full-vault export data fetch"
    - path: "src/services/__tests__/api-tokens.service.test.ts"
      provides: "no-truncation and mapping regression coverage"
---

<objective>
Make the full-vault export data path actually full-vault by paging through Supabase rows, typing the row mapping, and proving the service can return at least 5,000 calls without truncation.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3-obsidian-sync-improvements/06.3-CONTEXT.md
@.planning/phases/06.3-obsidian-sync-improvements/06.3-RESEARCH.md
@.planning/phases/06.3-obsidian-sync-improvements/06.3-VALIDATION.md
@.planning/phases/06.3-obsidian-sync-improvements/06.3-PATTERNS.md
@src/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add full-list pagination test coverage</name>
  <files>src/services/__tests__/api-tokens.service.test.ts</files>
  <read_first>
    - src/services/api-tokens.service.ts
    - src/services/__tests__/mcp-token-capabilities.service.test.ts
    - src/services/__tests__/sync-tab.service.test.ts
    - src/test/setup.ts
  </read_first>
  <action>Create `src/services/__tests__/api-tokens.service.test.ts` with a mocked Supabase query builder that exercises `fetchAllCallsForObsidianExport(orgId)`. The tests must simulate paged responses for at least 5,000 recordings, multiple workspaces, and workspace_entries. Assert that the returned call count is exactly 5,000, the last record is present, no implicit 500/1,000 truncation occurs, workspace names are mapped from `workspace_entries`, and `canonical_uuid` is populated from `recordings.id`.</action>
  <acceptance_criteria>
    - Test file `src/services/__tests__/api-tokens.service.test.ts` exists.
    - Test data includes at least 5,000 recording rows.
    - A test assertion checks `calls.length` equals `5000`.
    - A test assertion checks the final generated recording title or ID is present.
    - A test assertion checks a workspace name resolved through `workspace_entries`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/services/__tests__/api-tokens.service.test.ts</automated>
  </verify>
  <done>Failing/targeted tests describe the full-vault service contract.</done>
</task>

<task type="auto">
  <name>Task 2: Page export data until exhausted</name>
  <files>src/services/api-tokens.service.ts</files>
  <read_first>
    - src/services/api-tokens.service.ts
    - src/services/transcript-filters.service.ts
    - src/services/sync-tab.service.ts
    - src/lib/recording-source-url.ts
    - src/types/meetings.ts
  </read_first>
  <action>Update `fetchAllCallsForObsidianExport(orgId)` so it does not rely on one unbounded `.select()` call. Add explicit pagination using `.range(from, to)` with a named page size, and fetch pages until a page returns fewer rows than the page size. Apply the pattern to `workspaces`, `workspace_entries`, and `recordings`; chunk `workspaceIds` for `.in('workspace_id', ...)` if needed. Keep the function a pure async service function with no React dependencies. Do not add a 500 cap. Do not change the public function name or its return type.</action>
  <acceptance_criteria>
    - `fetchAllCallsForObsidianExport` contains `.range(` or a shared helper it calls contains `.range(`.
    - The implementation contains no `limit(500)`, no `slice(0, 500)`, and no hard-coded `500` export cap.
    - The implementation loops until a returned page is smaller than the page size.
    - `npm test -- src/services/__tests__/api-tokens.service.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/services/__tests__/api-tokens.service.test.ts && ! rg -n "limit\\(500\\)|slice\\(0,\\s*500\\)|500-record" src/services/api-tokens.service.ts</automated>
  </verify>
  <done>Export data retrieval no longer silently truncates large vaults.</done>
</task>

<task type="auto">
  <name>Task 3: Type and correct export row mapping</name>
  <files>src/services/api-tokens.service.ts, src/services/__tests__/api-tokens.service.test.ts</files>
  <read_first>
    - src/services/api-tokens.service.ts
    - src/lib/recording-source-url.ts
    - src/lib/export-utils.ts
    - src/types/meetings.ts
    - src/CLAUDE.md
  </read_first>
  <action>Replace broad `(rec as any)` and `(entry as any)` casts in `fetchAllCallsForObsidianExport()` with narrow local row types or generated Supabase types. Preserve `recording_id` compatibility, but prefer `canonical_uuid` in Obsidian front matter and avoid adding new numeric-ID behavior. Add a small local adapter or helper so URL mapping follows the same source/share URL semantics as `resolveShareUrl()` where possible; if the helper cannot be imported directly because of type shape, create a narrow object with `source_metadata`, `source_app`, and `url` fields and call the helper. Extend tests to cover Fathom, Zoom, and missing-share-url cases.</action>
  <acceptance_criteria>
    - `src/services/api-tokens.service.ts` no longer contains `(rec as any)` or `(entry as any)`.
    - Service tests cover at least one Fathom URL, one Zoom URL, and one missing URL.
    - Returned rows still include `canonical_uuid`.
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check && npm test -- src/services/__tests__/api-tokens.service.test.ts && ! rg -n "\\(rec as any\\)|\\(entry as any\\)" src/services/api-tokens.service.ts</automated>
  </verify>
  <done>Full-vault export data is complete, typed, and source URL mapping is no longer ad hoc.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06.3-01 | Data Loss | export service pagination | mitigate | Explicitly page every full-vault query until exhausted and test 5,000-row retrieval. |
| T-06.3-02 | Information Integrity | export row mapping | mitigate | Type row mapping and use canonical share URL helper pattern. |
</threat_model>

<verification>
Run `npm test -- src/services/__tests__/api-tokens.service.test.ts` after each task and `npm run type-check` after mapping cleanup.
</verification>

<success_criteria>
- `fetchAllCallsForObsidianExport()` returns at least 5,000 mocked calls in tests.
- There is no 500-record export cap in the service.
- Row mapping is typed and preserves workspace/source metadata.
</success_criteria>

<output>
Create `.planning/phases/06.3-obsidian-sync-improvements/06.3-01-SUMMARY.md` when done.
</output>
