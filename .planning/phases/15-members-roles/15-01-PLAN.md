---
phase: 15-members-roles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260330200000_align_workspace_roles_5_to_4.sql
  - src/types/workspace.ts
  - src/hooks/useWorkspaceMemberMutations.ts
  - src/components/panels/WorkspaceMemberPanel.tsx
  - src/components/dialogs/ChangeRoleDialog.tsx
  - src/components/dialogs/WorkspaceInviteDialog.tsx
  - src/hooks/useWorkspaces.ts
autonomous: true
requirements:
  - MEMBER-01
  - MEMBER-02
  - MEMBER-03
  - MEMBER-04
must_haves:
  truths:
    - "Workspace has exactly 4 roles: Owner, Admin, Contributor, Member"
    - "Owner has full control including delete workspace and manage all members"
    - "Admin has owner-equivalent permissions except ownership transfer"
    - "Contributor can route/add calls to workspace"
    - "Member has read/organize access only"
  artifacts:
    - path: "supabase/migrations/20260330200000_align_workspace_roles_5_to_4.sql"
      provides: "DB migration renaming manager->contributor, dropping guest"
      contains: "contributor"
    - path: "src/types/workspace.ts"
      provides: "WorkspaceRole type with 4 roles"
      contains: "contributor"
  key_links:
    - from: "src/types/workspace.ts"
      to: "supabase/migrations/20260330200000_align_workspace_roles_5_to_4.sql"
      via: "type definition matches DB constraint"
      pattern: "workspace_owner.*workspace_admin.*contributor.*member"
---

<objective>
Align the workspace role system from 5 roles to 4 roles (Owner/Admin/Contributor/Member) at both the database and TypeScript layers, and update all UI components that display or select roles.

Purpose: The DB currently has 5 roles (workspace_owner, workspace_admin, manager, member, guest). The v2.0 decision locks 4 roles with `manager` renamed to `contributor` and `guest` dropped entirely. Every component referencing the old roles must be updated.

Output: Migration SQL, updated types, updated role UI in member panel, change-role dialog, and invite dialog.
</objective>

<execution_context>
@/Users/Naegele/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Naegele/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-members-roles/15-CONTEXT.md

<interfaces>
From src/types/workspace.ts (CURRENT — will be modified):
```typescript
export type WorkspaceRole = 'workspace_owner' | 'workspace_admin' | 'manager' | 'member' | 'guest'
export interface WorkspaceMembership {
  id: string; workspace_id: string; user_id: string; role: WorkspaceRole; created_at: string;
}
export interface WorkspaceInvitation {
  role: 'member' | 'manager' | 'workspace_admin'
}
```

From src/hooks/useWorkspaces.ts:
```typescript
export interface WorkspaceMember {
  id: string; user_id: string; role: WorkspaceRole; created_at: string;
  email: string | null; display_name: string | null; avatar_url: string | null;
}
const ROLE_ORDER: Record<WorkspaceRole, number> = {
  workspace_owner: 0, workspace_admin: 1, manager: 2, member: 3, guest: 4,
}
```

From src/hooks/useWorkspaceMemberMutations.ts:
```typescript
const ROLE_POWER: Record<WorkspaceRole, number> = {
  workspace_owner: 0, workspace_admin: 1, manager: 2, member: 3, guest: 4,
}
const ROLE_LABELS: Record<WorkspaceRole, string> = {
  workspace_owner: 'Owner', workspace_admin: 'Admin', manager: 'Manager', member: 'Member', guest: 'Guest',
}
```

From DB (current constraint):
```sql
CHECK (role IN ('workspace_owner', 'workspace_admin', 'manager', 'member', 'guest'))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration — rename manager to contributor, drop guest</name>
  <files>supabase/migrations/20260330200000_align_workspace_roles_5_to_4.sql</files>
  <read_first>
    - supabase/migrations/20260301000001_rename_vaults_to_workspaces.sql (current CHECK constraint)
    - supabase/migrations/20260309100000_fix_invitation_rls_auth_email.sql (invitation role constraint)
    - supabase/migrations/20260316130000_restrict_member_insert_entries.sql (RLS policies referencing 'manager')
    - supabase/migrations/20260301000002_recreate_rls_policies.sql (RLS policies referencing old roles)
  </read_first>
  <action>
    Create a single migration file that:

    1. UPDATE all existing workspace_memberships rows where role='manager' to role='contributor'.
    2. UPDATE all existing workspace_memberships rows where role='guest' to role='member' (upgrade guests to members before dropping).
    3. DROP the old CHECK constraint on workspace_memberships.role.
    4. ADD new CHECK constraint: role IN ('workspace_owner', 'workspace_admin', 'contributor', 'member').
    5. UPDATE organization_memberships CHECK constraint if it references 'manager' or 'guest' (check existing constraint name first).
    6. UPDATE workspace_invitations role CHECK constraint to allow ('workspace_admin', 'contributor', 'member') — no guest, manager renamed.
    7. UPDATE organization_invitations role CHECK to use 'contributor' instead of 'manager' if applicable.
    8. UPDATE all RLS policies that reference 'manager' to reference 'contributor' instead. Specifically:
       - The restrict_member_insert_entries policy uses IN ('workspace_owner', 'workspace_admin', 'manager') — change to ('workspace_owner', 'workspace_admin', 'contributor')
       - Any other policies from 20260301000002 that reference 'manager' or 'guest'
    9. Add COMMENT explaining the migration rationale.

    Use IF EXISTS guards where appropriate. Wrap in a transaction (BEGIN/COMMIT) for safety.
    Do NOT use ALTER TYPE (these are text columns with CHECK constraints, not enums).
  </action>
  <verify>
    <automated>cd /Users/Naegele/dev/brain && grep -c "contributor" supabase/migrations/20260330200000_align_workspace_roles_5_to_4.sql && grep -c "guest" supabase/migrations/20260330200000_align_workspace_roles_5_to_4.sql</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at supabase/migrations/20260330200000_align_workspace_roles_5_to_4.sql
    - File contains UPDATE statements converting 'manager' to 'contributor' and 'guest' to 'member'
    - File contains new CHECK constraint with exactly 4 roles: workspace_owner, workspace_admin, contributor, member
    - File updates RLS policies to reference 'contributor' instead of 'manager'
    - File contains no reference to 'guest' in new constraints (only in UPDATE/migration comments)
  </acceptance_criteria>
  <done>Migration file ready for deployment; converts all manager->contributor, guest->member; new CHECK allows exactly 4 roles; RLS policies updated.</done>
</task>

<task type="auto">
  <name>Task 2: Update TypeScript types and all role constants across codebase</name>
  <files>
    src/types/workspace.ts,
    src/hooks/useWorkspaceMemberMutations.ts,
    src/hooks/useWorkspaces.ts,
    src/components/panels/WorkspaceMemberPanel.tsx,
    src/components/dialogs/ChangeRoleDialog.tsx,
    src/components/dialogs/WorkspaceInviteDialog.tsx
  </files>
  <read_first>
    - src/types/workspace.ts
    - src/hooks/useWorkspaceMemberMutations.ts
    - src/hooks/useWorkspaces.ts
    - src/components/panels/WorkspaceMemberPanel.tsx
    - src/components/dialogs/ChangeRoleDialog.tsx
    - src/components/dialogs/WorkspaceInviteDialog.tsx
  </read_first>
  <action>
    Update every file that references the old 5-role system to the new 4-role system:

    **src/types/workspace.ts:**
    - Change WorkspaceRole to: 'workspace_owner' | 'workspace_admin' | 'contributor' | 'member'
    - Remove 'guest' from all types
    - Update WorkspaceInvitation.role to: 'member' | 'contributor' | 'workspace_admin'

    **src/hooks/useWorkspaces.ts:**
    - Update ROLE_ORDER to 4 entries: workspace_owner:0, workspace_admin:1, contributor:2, member:3
    - Remove guest entry

    **src/hooks/useWorkspaceMemberMutations.ts:**
    - Update ROLE_POWER to 4 entries: workspace_owner:0, workspace_admin:1, contributor:2, member:3
    - Update ROLE_LABELS: workspace_owner:'Owner', workspace_admin:'Admin', contributor:'Contributor', member:'Member'
    - Remove guest entries

    **src/components/panels/WorkspaceMemberPanel.tsx:**
    - Update ROLE_BADGE_STYLES: remove guest, rename manager key to contributor, keep styling (emerald for contributor)
    - Update ROLE_LABELS: remove guest, rename manager to 'Contributor'

    **src/components/dialogs/ChangeRoleDialog.tsx:**
    - Update WORKSPACE_ROLES array: remove guest entry, rename manager to contributor with updated label/description
    - Contributor description: "Can add and route calls to workspace. Calls permanently copied to owner's account."
    - Member description: "Read and organize access only. On removal, owner decides call retention."
    - Update ROLE_POWER to 4 entries

    **src/components/dialogs/WorkspaceInviteDialog.tsx:**
    - Change role state type from 'member' | 'manager' | 'workspace_admin' to 'member' | 'contributor' | 'workspace_admin'
    - Update the Select options: change "Manager (Can manage folders)" to "Contributor (Can add calls)"
    - Default role remains 'member'

    Also grep for any other files referencing 'manager' as a WorkspaceRole or 'guest' as a WorkspaceRole and fix those too. Check:
    - src/components/settings/WorkspaceManagement.tsx
    - src/services/invitations.service.ts
    - Any other service or component referencing the old role names
  </action>
  <verify>
    <automated>cd /Users/Naegele/dev/brain && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - WorkspaceRole type contains exactly: 'workspace_owner' | 'workspace_admin' | 'contributor' | 'member'
    - No TypeScript file in src/ contains 'guest' as a role value (grep returns 0 matches for role.*guest in .ts/.tsx files)
    - ROLE_POWER, ROLE_ORDER, ROLE_LABELS, ROLE_BADGE_STYLES all have exactly 4 entries
    - ChangeRoleDialog shows 4 role options (Owner, Admin, Contributor, Member)
    - WorkspaceInviteDialog email tab shows 3 role options (Member, Contributor, Admin)
    - TypeScript compilation passes (npx tsc --noEmit)
  </acceptance_criteria>
  <done>All TypeScript types and UI components reflect exactly 4 workspace roles; no references to 'manager' or 'guest' as role values remain; TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `grep -rn "'manager'" src/types/workspace.ts src/hooks/useWorkspace*.ts src/components/panels/WorkspaceMemberPanel.tsx src/components/dialogs/ChangeRoleDialog.tsx src/components/dialogs/WorkspaceInviteDialog.tsx` returns no matches
2. `grep -rn "'guest'" src/types/workspace.ts src/hooks/useWorkspace*.ts src/components/panels/WorkspaceMemberPanel.tsx src/components/dialogs/ChangeRoleDialog.tsx src/components/dialogs/WorkspaceInviteDialog.tsx` returns no matches
3. `grep -c "contributor" src/types/workspace.ts` returns at least 1
4. `npx tsc --noEmit` passes
</verification>

<success_criteria>
- DB migration converts 5 roles to 4 (manager->contributor, guest->member)
- All TypeScript types aligned to 4 roles
- All UI role pickers, badges, labels show only 4 roles
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-members-roles/15-01-SUMMARY.md`
</output>
