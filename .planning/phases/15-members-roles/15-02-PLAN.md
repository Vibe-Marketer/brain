---
phase: 15-members-roles
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/components/panels/WorkspaceMemberPanel.tsx
  - src/components/panels/WorkspaceDetailPanel.tsx
  - src/components/dialogs/WorkspaceInviteDialog.tsx
  - src/services/invitations.service.ts
  - src/pages/WorkspaceJoin.tsx
autonomous: true
requirements:
  - MEMBER-05
  - MEMBER-06
  - MEMBER-07
  - MEMBER-08
  - MEMBER-09
must_haves:
  truths:
    - "Owner/Admin can invite a new member by email with role selection"
    - "Invite email is sent via send-org-invite edge function"
    - "Owner/Admin can generate a shareable invite link with copy-to-clipboard"
    - "Shareable link defaults to Member role and expires after 7 days"
    - "Invited user (new or existing) can join via /join/workspace/:token"
    - "Owner/Admin can remove a member with confirmation dialog"
    - "Owner/Admin can change a member's role after invite"
  artifacts:
    - path: "src/components/dialogs/WorkspaceInviteDialog.tsx"
      provides: "Email invite + shareable link tabs with 4-role system"
      contains: "contributor"
    - path: "src/pages/WorkspaceJoin.tsx"
      provides: "Join page handling both email and link invites"
      contains: "accept_workspace_invite"
  key_links:
    - from: "src/components/dialogs/WorkspaceInviteDialog.tsx"
      to: "supabase/functions/send-org-invite/index.ts"
      via: "supabase.functions.invoke('send-org-invite')"
      pattern: "send-org-invite"
    - from: "src/pages/WorkspaceJoin.tsx"
      to: "supabase RPC accept_workspace_invite"
      via: "supabase.rpc"
      pattern: "accept_workspace_invite"
---

<objective>
Ensure the invite and member management flows work end-to-end with the new 4-role system: email invites with role selection, shareable link generation, join page for new/existing users, member removal, and role changes.

Purpose: The existing invite dialog, join page, and member management components are mostly built but reference old roles and have some incomplete wiring. This plan verifies and fixes the full invite-to-join pipeline.

Output: Working email invite flow, working shareable link flow, working member removal and role change, all using the 4-role system.
</objective>

<execution_context>
@/Users/Naegele/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Naegele/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-members-roles/15-CONTEXT.md
@.planning/phases/15-members-roles/15-01-SUMMARY.md

<interfaces>
From src/services/invitations.service.ts:
```typescript
export async function createInvitation(workspaceId: string, invitedBy: string, email: string, role: WorkspaceInvitation['role']): Promise<WorkspaceInvitation>
export async function revokeInvitation(invitationId: string): Promise<void>
export async function acceptInvite(token: string, userId: string): Promise<void>
export function getShareableLink(token: string): string
```

From src/hooks/useWorkspaceMemberMutations.ts:
```typescript
export function useGenerateWorkspaceInvite(workspaceId: string)
export function useChangeRole(workspaceId: string)
export function useRemoveMember(workspaceId: string)
export function useLeaveWorkspace(workspaceId: string)
```

From src/services/organization-invitations.service.ts:
```typescript
export async function createOrganizationInvitation(organizationId: string, email: string, role: OrganizationInvitation['role']): Promise<OrganizationInvitation>
export function getShareableLink(token: string): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix invite dialog role options and verify email sending pipeline</name>
  <files>
    src/components/dialogs/WorkspaceInviteDialog.tsx,
    src/services/invitations.service.ts
  </files>
  <read_first>
    - src/components/dialogs/WorkspaceInviteDialog.tsx
    - src/services/invitations.service.ts
    - src/services/organization-invitations.service.ts
    - supabase/functions/send-org-invite/index.ts
  </read_first>
  <action>
    **WorkspaceInviteDialog.tsx fixes (per CONTEXT.md decisions):**

    1. The email tab role Select must show exactly 3 options with new role names:
       - "Member (Read and organize access)" value='member'
       - "Contributor (Can add and route calls)" value='contributor'
       - "Admin (Can manage members and settings)" value='workspace_admin'
    2. Verify the email send pipeline: createInvitation() -> supabase.functions.invoke('send-org-invite') with correct body shape. The edge function already exists and works. Ensure the `role` field passed to the edge function uses the new contributor name.
    3. The shareable link tab already works via useGenerateWorkspaceInvite. Verify it generates a link with `/join/workspace/:token` format and shows copy button + expiry info. No changes needed if working.
    4. Remove any references to 'manager' in this file (should already be done by Plan 01 Task 2, but verify).

    **invitations.service.ts fixes:**
    - Verify createInvitation uses the correct role type (should be updated from Plan 01).
    - No changes expected if Plan 01 updated the WorkspaceInvitation type correctly.

    **Permission gating:** The invite dialog should only be openable by workspace_owner or workspace_admin. Verify WorkspaceMemberPanel's `canManage` check gates the Invite Members button correctly. It uses: `currentUserRole === 'workspace_owner' || currentUserRole === 'workspace_admin'` — this is correct for the 4-role system.
  </action>
  <verify>
    <automated>cd /Users/Naegele/dev/brain && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - WorkspaceInviteDialog email tab Select contains exactly 3 options: member, contributor, workspace_admin
    - No reference to 'manager' or 'guest' in WorkspaceInviteDialog.tsx
    - Email invite calls supabase.functions.invoke('send-org-invite') with role field
    - Shareable link tab generates /join/workspace/:token URL
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>Invite dialog shows correct 4-role options; email invite sends via edge function; shareable link generates and copies correctly; member removal and role change work with proper permission checks.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix WorkspaceJoin page + member removal confirmation</name>
  <files>
    src/pages/WorkspaceJoin.tsx,
    src/components/panels/WorkspaceMemberPanel.tsx
  </files>
  <read_first>
    - src/pages/WorkspaceJoin.tsx
    - src/components/panels/WorkspaceMemberPanel.tsx
    - src/App.tsx (verify /join/workspace/:token route exists)
    - src/hooks/useWorkspaceMemberMutations.ts
  </read_first>
  <action>
    **WorkspaceJoin.tsx:**
    1. Verify the page handles both email-based invites (via RPC get_workspace_invite_details) and shareable link invites (via workspaces table lookup).
    2. Verify the role badge display strips old prefixes correctly. The existing code does: `role.replace('vault_', '').replace('workspace_', '')` — this will show "contributor" correctly.
    3. Verify redirect for unauthenticated users: should redirect to `/login?redirect=/join/workspace/${token}` — the existing code already does this.
    4. Verify the join flow creates both organization_membership (for shareable link path) and workspace_membership.
    5. If user has no account, CONTEXT.md says `/join/:token` page should show sign-up form. Check if the current redirect to `/login` handles this (login page likely has sign-up option). If the login page has a sign-up tab, this is sufficient. Do NOT build a separate sign-up form on the join page — the redirect is the correct pattern.

    **WorkspaceMemberPanel.tsx — member removal:**
    1. The existing handleRemoveWorkspaceMember uses window.confirm(). Per CONTEXT.md decision: "confirmation: Remove [Name]? Their calls will remain in the workspace."
    2. Replace the basic window.confirm() with a proper AlertDialog from Radix UI that says:
       - Title: "Remove [display_name or email]?"
       - Description: "Their calls will remain in the workspace."
       - Confirm button: "Remove" (destructive variant)
       - Cancel button: "Cancel"
    3. Keep the same removeMember.mutate() call after confirmation.

    **WorkspaceMemberPanel.tsx — role change (MEMBER-09):**
    The existing changeRoleTarget + ChangeRoleDialog flow already works. Verify it opens ChangeRoleDialog with correct 4-role options after Plan 01 changes. No additional changes expected.

    **Route verification:**
    Grep App.tsx to confirm `/join/workspace/:token` route maps to WorkspaceJoin component.
  </action>
  <verify>
    <automated>cd /Users/Naegele/dev/brain && grep -n "join/workspace" src/App.tsx && grep -c "AlertDialog\|alertdialog" src/components/panels/WorkspaceMemberPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - /join/workspace/:token route exists in App.tsx pointing to WorkspaceJoin
    - WorkspaceJoin handles both email and link invite paths
    - WorkspaceMemberPanel uses AlertDialog (not window.confirm) for member removal
    - Removal confirmation message includes "calls will remain in the workspace"
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>Join page handles both invite types; member removal uses proper AlertDialog with call retention message; role change dialog works with 4 roles; unauthenticated users redirect to login.</done>
</task>

</tasks>

<verification>
1. `grep -n "contributor" src/components/dialogs/WorkspaceInviteDialog.tsx` shows the contributor role option
2. `grep -n "join/workspace" src/App.tsx` confirms route exists
3. `grep -n "AlertDialog" src/components/panels/WorkspaceMemberPanel.tsx` confirms proper dialog
4. `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Email invite sends via edge function with correct role
- Shareable link generates with 7-day expiry
- Join page works for both invite types
- Member removal has proper confirmation dialog
- Role changes work post-invite
</success_criteria>

<output>
After completion, create `.planning/phases/15-members-roles/15-02-SUMMARY.md`
</output>
