---
phase: 15-support-capture-fix
plan: 03
type: execute
wave: 2
depends_on: [15-01]
external_gate: "DO NOT EXECUTE until Phase 11 plans 11-03 AND 11-04 are complete — tickets.service.ts, useTickets.ts, and TicketDetailDialog.tsx are CREATED by 11-03 and EXTENDED by 11-04; this plan extends all three"
files_modified:
  - src/services/tickets.service.ts
  - src/hooks/useTickets.ts
  - src/components/settings/TicketDetailDialog.tsx
  - src/components/settings/__tests__/TicketDetailDialog.test.tsx
  - src/services/__tests__/tickets.service.test.ts
autonomous: true
requirements: [CAP-01]
must_haves:
  truths:
    - "An admin opening a ticket's detail dialog sees an Attachments group on any message that has attachments: the screenshot renders as an inline image preview and the console log as a download/open link (D-05)"
    - "Attachment access uses short-lived signed URLs from the private bucket — getPublicUrl appears nowhere (D-04)"
    - "Tickets without attachments render the detail dialog exactly as 11-03 shipped it (no regression, no empty Attachments group)"
  artifacts:
    - path: "src/services/tickets.service.ts"
      provides: "getAttachmentSignedUrl(path) — pure async signed-URL fetch from ticket-attachments"
      exports: ["getAttachmentSignedUrl"]
    - path: "src/hooks/useTickets.ts"
      provides: "useAttachmentUrl TanStack Query wrapper keyed ['attachment-url', path]"
    - path: "src/components/settings/TicketDetailDialog.tsx"
      provides: "Attachments group per message: img preview for screenshot, link for console_log"
  key_links:
    - from: "src/components/settings/TicketDetailDialog.tsx"
      to: "src/hooks/useTickets.ts"
      via: "useAttachmentUrl per attachment (no inline supabase calls)"
      pattern: "useAttachmentUrl"
    - from: "src/services/tickets.service.ts"
      to: "ticket-attachments bucket"
      via: "supabase.storage.from('ticket-attachments').createSignedUrl"
      pattern: "createSignedUrl"
---

<objective>
Deliver the admin-visibility slice (D-05): extend 11-03's TicketDetailDialog so both Phase 15 attachments are visible from the AdminTab ticket detail — screenshot as an inline preview, console JSON as a download link — through signed URLs in the locked service+hook pattern.

Purpose: CAP-01 success criterion 3 — a submitted ticket's attachments are retrievable from the ticket detail. This closes Andrew's original loop: the admin sees what the user saw.
Output: getAttachmentSignedUrl service + useAttachmentUrl hook, Attachments rendering in TicketDetailDialog, tests.
</objective>

## Phase Goal

**As an** admin triaging a ticket, **I want to** see the reporter's screenshot and console log directly in the ticket detail, **so that** I can reconstruct the problem without contacting the reporter.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-support-capture-fix/15-CONTEXT.md
@.planning/phases/15-support-capture-fix/15-RESEARCH.md (Pattern 5 — coordinate with 11-03, do not duplicate)
@.planning/phases/11-ticket-foundation-flag-removal/11-UI-SPEC.md (detail-dialog design contract — Phase 15 extends, never contradicts)
@src/CLAUDE.md
</context>

<sequencing_gate>
**HARD EXECUTION GATE: every file this plan modifies except the test files is created by 11-03 (tickets.service.ts, useTickets.ts, TicketDetailDialog.tsx) and tickets.service.ts/useTickets.ts are further extended by 11-04 (createTicket/useCreateTicket).** Verify `.planning/phases/11-ticket-foundation-flag-removal/11-03-SUMMARY.md` AND `11-04-SUMMARY.md` exist before starting. If missing, STOP and report the gate. Wave 2 within Phase 15: also requires 15-01 complete (bucket + attachments shape live).
</sequencing_gate>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getAttachmentSignedUrl service + useAttachmentUrl hook (D-04, D-05)</name>
  <files>src/services/tickets.service.ts, src/hooks/useTickets.ts, src/services/__tests__/tickets.service.test.ts</files>
  <read_first>src/services/tickets.service.ts (11-03/11-04 shipped state — match its error style 'Failed to X: ' + error.message), src/hooks/useTickets.ts (query-key and toast idioms), src/services/support-ticket.service.ts (AttachmentDescriptor shape from 15-01)</read_first>
  <behavior>
    - getAttachmentSignedUrl('uid/abc.jpg') calls supabase.storage.from('ticket-attachments').createSignedUrl(path, 3600) and returns the signedUrl string; throws Error('Failed to load attachment: ...') on error or null data
    - useAttachmentUrl(path | undefined) wraps it with queryKey ['attachment-url', path], enabled only when path is truthy, staleTime under the 3600s expiry
  </behavior>
  <action>Extend src/services/tickets.service.ts (pure async, no React — locked pattern) with getAttachmentSignedUrl per the behavior block and re-export/import the AttachmentDescriptor type from @/services/support-ticket.service (single source for the shape from 15-01; do not redeclare). NEVER use getPublicUrl — private bucket (RESEARCH anti-pattern). Extend src/hooks/useTickets.ts with useAttachmentUrl per the behavior block following the existing hook idiom in that file. Extend src/services/__tests__/tickets.service.test.ts (created by 11-03 — append, do not rewrite existing coverage) with mocked storage.from().createSignedUrl tests: returns url, throws on error, throws on null data.</action>
  <verify>
    <automated>npx vitest run src/services/__tests__/tickets.service.test.ts && npm run build</automated>
  </verify>
  <done>Signed-URL access path exists in the locked service+hook layers; 11-03/11-04 tests still pass untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Attachments group in TicketDetailDialog + phase-level verification (D-05)</name>
  <files>src/components/settings/TicketDetailDialog.tsx, src/components/settings/__tests__/TicketDetailDialog.test.tsx</files>
  <read_first>src/components/settings/TicketDetailDialog.tsx (11-03 shipped state — extend its messages list section, do not restructure), .planning/phases/11-ticket-foundation-flag-removal/11-UI-SPEC.md (detail layout contract), src/components/settings/__tests__/ (existing test style)</read_first>
  <action>In TicketDetailDialog.tsx, inside the messages list 11-03 built, render an 'Attachments' group under any message whose attachments array is non-empty (parse the jsonb as AttachmentDescriptor[]; tolerate unknown shapes by skipping invalid entries — render nothing rather than crash). For type 'screenshot': resolve via useAttachmentUrl(descriptor.path) and render an img (signed URL src, alt 'Ticket screenshot', max-h-48 rounded-md border border-border object-contain, click opens the signed URL in a new tab with noopener); show a Skeleton while the URL loads and a muted 'Attachment unavailable' line on error. For type 'console_log': render a text-xs link row with RiFileTextLine icon and label 'Console log (JSON)' opening the signed URL in a new tab with noopener. All metadata rendered as React text nodes — no dangerouslySetInnerHTML (consistent with 11-03's T-11-12). Remix icons only; shadcn tokens; follow the 11-UI-SPEC detail idiom (text-xs sections). Messages without attachments render identically to 11-03 (no header, no spacing change). Create src/components/settings/__tests__/TicketDetailDialog.test.tsx additions (or a new test file if 11-03 didn't create one) with mocked hooks covering: both attachment types render, empty attachments renders no group, invalid descriptor entries are skipped silently. Then run the phase-level sweep: npm test, npm run build, and grep -rn "getPublicUrl" src/ must return 0 matches.</action>
  <verify>
    <automated>npx vitest run src/components/settings/__tests__/TicketDetailDialog.test.tsx && npm test 2>&1 | tail -5 && npm run build && grep -rn "getPublicUrl" src/ | wc -l</automated>
  </verify>
  <done>Admin sees screenshot preview + console JSON link in the ticket detail; full-suite + build green; zero public-URL usage. Execution-time dev-browser verification (project hard rule): submit a ticket with both attachments as a user, open AdminTab → ticket detail as admin, confirm the image preview renders the problem view and the console JSON downloads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → storage signed URLs | time-limited URLs to private objects render in the admin DOM |
| attachments jsonb → DOM | reporter-influenced descriptor strings (path, mime) reach the detail dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Information Disclosure | signed URL lifetime | mitigate | 3600s expiry; URLs fetched on-demand per dialog open, never persisted |
| T-15-08 | Injection (XSS) | descriptor strings rendered in dialog | mitigate | React text nodes only; img src exclusively from createSignedUrl return (never from descriptor strings directly); no dangerouslySetInnerHTML |
| T-15-09 | Elevation of Privilege | non-admin reading another user's attachment | mitigate | storage SELECT policy owner-or-ADMIN (15-01); UI gate cosmetic, RLS is the control (same posture as T-11-10) |
| T-15-SC | Tampering | package installs | accept | No package installs this plan — slopcheck gate not applicable |
</threat_model>

<verification>
- All vitest targets, full `npm test`, `npm run build` exit 0; `grep -rn "getPublicUrl" src/` → 0 matches
- Execution-time dev-browser end-to-end: user submits ticket with screenshot + console buffer → admin opens detail → image preview shows the problem view, console JSON link works
- Manual storage RLS spot-check from VALIDATION.md: second non-admin account cannot createSignedUrl on the first user's path
</verification>

<success_criteria>
Phase success criterion 3 satisfied: a submitted ticket's attachments are retrievable from the ticket detail, working with the Phase 11 ticket persistence (CAP-01, D-05).
</success_criteria>

<output>
Create `.planning/phases/15-support-capture-fix/15-03-SUMMARY.md` when done
</output>
