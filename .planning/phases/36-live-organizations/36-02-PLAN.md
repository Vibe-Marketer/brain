---
phase: 36-live-organizations
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - supabase/migrations/20260908140000_add_canonical_organization_id.sql
  - supabase/migrations/20260908140001_create_org_merge_unclaim_admin_rpcs.sql
  - src/test/rls-regression.test.ts
  - src/test/org-merge-unclaim-rpc.integration.test.ts
autonomous: true
requirements: [ORG-03, ORG-04]
user_setup: []

must_haves:
  truths:
    - "organizations gains canonical_organization_id (nullable self-FK), merged_at, merged_by — additive only, NULL means canonical/root (ORG-03)."
    - "merge_organizations_atomic sets the losing org's canonical_organization_id/merged_at/merged_by and touches NOTHING else — recordings.organization_id and organization_memberships are unchanged (ORG-03)."
    - "A merge is reversed by clearing canonical_organization_id (+ merged_at/merged_by), fully restoring original state (ORG-03)."
    - "The chain-prevention trigger rejects merging into an already-merged org and merging an org other orgs already point to (no chains/cycles)."
    - "merge_organizations_atomic and unclaim_organization_domain_atomic require has_role(p_admin_user_id,'ADMIN'); EXECUTE is REVOKEd from PUBLIC/anon/authenticated so they are reachable only via a service-role edge function."
    - "A member (even an admin/owner) of Org B or of a merged-away Org C cannot read Org A's recordings/captures despite Org A having a claimed domain and/or being a merge target — proven by test (ORG-04)."
    - "is_organization_member and is_organization_admin_or_owner are byte-for-byte unchanged and never reference canonical_organization_id anywhere in the schema (ORG-04)."
  artifacts:
    - path: "supabase/migrations/20260908140000_add_canonical_organization_id.sql"
      provides: "canonical_organization_id/merged_at/merged_by columns, not-self CHECK, prevent_canonical_organization_chain trigger"
      contains: "prevent_canonical_organization_chain"
    - path: "supabase/migrations/20260908140001_create_org_merge_unclaim_admin_rpcs.sql"
      provides: "merge_organizations_atomic + unclaim_organization_domain_atomic, has_role gate, REVOKE EXECUTE"
      contains: "REVOKE EXECUTE"
    - path: "src/test/rls-regression.test.ts"
      provides: "bespoke ORG-04 canonical-no-leak block (multi-org fixtures, real JWTs)"
      contains: "canonical_organization_id"
    - path: "src/test/org-merge-unclaim-rpc.integration.test.ts"
      provides: "merge pointer set/clear, FK-non-rewrite, chain rejection, has_role gating proof"
  key_links:
    - from: "merge_organizations_atomic"
      to: "has_role"
      via: "platform-admin authority by parameter"
      pattern: "has_role\\(p_admin_user_id, 'ADMIN'\\)"
    - from: "organizations canonical_organization_id UPDATE"
      to: "prevent_canonical_organization_chain trigger"
      via: "BEFORE INSERT OR UPDATE guard"
      pattern: "organizations_prevent_canonical_chain"
    - from: "unclaim_organization_domain_atomic"
      to: "organization_domains"
      via: "admin-only DELETE of a claimed domain row"
      pattern: "DELETE FROM organization_domains"
  prohibitions:
    - "MUST NOT dereference canonical_organization_id inside is_organization_member / is_organization_admin_or_owner (or any new copy of their logic) — these two functions stay byte-for-byte unrelated to the new columns; the pointer is display-only metadata and never enters a USING/WITH CHECK clause or a SECURITY DEFINER body gating a capture-bearing table."
    - "MUST NOT let org association (a claimed organization_domains row, an organization_aliases row, or a canonical_organization_id pointer) widen any user's readable capture/recording audience — org association confers zero capture access."
    - "MUST NOT gate the merge/unclaim admin RPCs with is_organization_admin_or_owner (org membership) — platform-operator actions gate on has_role(user_id,'ADMIN') only."
    - "MUST NOT rewrite or delete any existing row or FK (recordings.organization_id, organization_memberships, etc.) during a merge — merge is pointer-only and fully reversible by clearing canonical_organization_id."
    - "MUST NOT create merge chains or cycles — an org whose canonical_organization_id is set cannot be a merge target, and a merge target cannot itself be already merged."
---

<objective>
Build the non-destructive, reversible duplicate-merge mechanism (ORG-03) and prove the phase's central safety invariant (ORG-04): org association confers no capture access. Adds the canonical pointer columns + chain-prevention trigger, the two admin-only atomic RPCs, and the bespoke cross-org no-leak proof.

Purpose: make duplicate orgs mergeable via a pointer that is trivially reversible, while proving — with real JWTs against TEST — that the pointer and the new identity tables introduce no new RLS path to any capture.
Output: 2 migrations on TEST, the ORG-04 bespoke isolation block in the CI RLS gate, and an RPC integration test proving pointer semantics + chain rejection.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/admin/dev/brain/main/.planning/phases/36-live-organizations/36-RESEARCH.md
@/Users/admin/dev/brain/main/.planning/phases/36-live-organizations/36-PATTERNS.md
@/Users/admin/dev/brain/main/.planning/phases/36-live-organizations/36-01-SUMMARY.md
@/Users/admin/dev/brain/main/supabase/CLAUDE.md

<interfaces>
<!-- Contracts this plan CREATES. Plan 04 edge functions call these via the service-role client. -->
merge_organizations_atomic(p_losing_org_id UUID, p_winning_org_id UUID, p_admin_user_id UUID) RETURNS void
  - RAISEs 'Access denied: platform admin required' if NOT has_role(p_admin_user_id,'ADMIN')
  - RAISEs 'Cannot merge an organization into itself' if losing = winning
  - Sets ONLY organizations.canonical_organization_id/merged_at/merged_by on the losing org
  - EXECUTE revoked from PUBLIC/anon/authenticated (service-role only)
unclaim_organization_domain_atomic(p_domain_id UUID, p_admin_user_id UUID) RETURNS void
  - RAISEs 'Access denied: platform admin required' if NOT has_role(p_admin_user_id,'ADMIN')
  - DELETEs the organization_domains row (reversible: domain can be re-claimed later)
  - EXECUTE revoked from PUBLIC/anon/authenticated (service-role only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: canonical_organization_id columns + chain-prevention trigger (organizations DDL only)</name>
  <files>supabase/migrations/20260908140000_add_canonical_organization_id.sql</files>
  <read_first>
    - /Users/admin/dev/brain/main/.planning/phases/36-live-organizations/36-RESEARCH.md (Code Examples "Merge columns + chain-prevention trigger"; Pitfall 1 CRITICAL, Pitfall 2)
    - /Users/admin/dev/brain/main/.planning/phases/36-live-organizations/36-PATTERNS.md (section "add_canonical_organization_id.sql" — trigger mechanics + the CRITICAL non-goal block, and the reproduced is_organization_member/is_organization_admin_or_owner bodies at lines 141-162)
    - /Users/admin/dev/brain/main/supabase/migrations/20260612020000_autogen_org_workspace_slugs.sql lines 85-123 (BEFORE trigger creation mechanics: DROP TRIGGER IF EXISTS + CREATE TRIGGER + COMMENT ON FUNCTION)
  </read_first>
  <action>
    Author migration 20260908140000_add_canonical_organization_id.sql (house-style header + numbered dividers + END OF MIGRATION). ALTER TABLE organizations ADD COLUMN IF NOT EXISTS canonical_organization_id UUID REFERENCES organizations(id) ON DELETE SET NULL, ADD COLUMN IF NOT EXISTS merged_at TIMESTAMPTZ, ADD COLUMN IF NOT EXISTS merged_by UUID REFERENCES auth.users(id) ON DELETE SET NULL. Add CONSTRAINT organizations_canonical_not_self CHECK (canonical_organization_id IS NULL OR canonical_organization_id != id). Create FUNCTION prevent_canonical_organization_chain() RETURNS TRIGGER LANGUAGE plpgsql that, when NEW.canonical_organization_id IS NOT NULL, RAISEs if the target org itself has a non-NULL canonical_organization_id (no merge-into-merged) and RAISEs if any other org already points its canonical_organization_id at NEW.id (a target cannot itself be a loser). Create TRIGGER organizations_prevent_canonical_chain BEFORE INSERT OR UPDATE OF canonical_organization_id ON organizations FOR EACH ROW EXECUTE FUNCTION prevent_canonical_organization_chain(). This migration touches ONLY organizations DDL + the new trigger function — it MUST NOT contain the strings is_organization_member or is_organization_admin_or_owner at all. Apply to TEST (link callvault-test swjzxiddcrtaqixsfaac, db push, introspect the three columns + the trigger exist), relink prod.
  </action>
  <verify>
    <automated>rtk proxy grep -Eic 'is_organization_member|is_organization_admin_or_owner' supabase/migrations/20260908140000_add_canonical_organization_id.sql</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns 0 (this migration never references the two choke-point functions — the ORG-04 CRITICAL non-goal).
    - TEST introspection confirms canonical_organization_id, merged_at, merged_by columns exist on organizations and the organizations_prevent_canonical_chain trigger is attached.
    - The not-self CHECK constraint exists.
    - CLI left linked to prod at task end.
  </acceptance_criteria>
  <done>Canonical pointer + chain guard live on TEST; choke-point functions untouched.</done>
</task>

<task type="auto">
  <name>Task 2: merge_organizations_atomic + unclaim_organization_domain_atomic admin RPCs</name>
  <files>supabase/migrations/20260908140001_create_org_merge_unclaim_admin_rpcs.sql</files>
  <read_first>
    - /Users/admin/dev/brain/main/supabase/migrations/20260902000002_kill_switch_revert_event_merges.sql lines 106-111 (REVOKE EXECUTE boilerplate to copy verbatim)
    - /Users/admin/dev/brain/main/supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql lines 60-256 (authority-by-parameter atomic-RPC shape)
    - /Users/admin/dev/brain/main/supabase/migrations/00000000000000_consolidated_schema.sql lines 319-332 (has_role(_user_id UUID, _role app_role) signature)
    - /Users/admin/dev/brain/main/.planning/phases/36-live-organizations/36-RESEARCH.md (Code Examples "Admin-only atomic merge RPC"; Pitfall 5 — org-admin vs platform-admin; Anti-Patterns — no FK rewrite)
    - /Users/admin/dev/brain/main/.planning/phases/36-live-organizations/36-PATTERNS.md (section "create_merge_and_unclaim_admin_rpcs.sql")
  </read_first>
  <action>
    Author migration 20260908140001_create_org_merge_unclaim_admin_rpcs.sql. merge_organizations_atomic(p_losing_org_id UUID, p_winning_org_id UUID, p_admin_user_id UUID) RETURNS void LANGUAGE plpgsql SECURITY DEFINER SET search_path = public: first `IF NOT public.has_role(p_admin_user_id, 'ADMIN') THEN RAISE EXCEPTION 'Access denied: platform admin required'; END IF;` then `IF p_losing_org_id = p_winning_org_id THEN RAISE EXCEPTION 'Cannot merge an organization into itself'; END IF;` then UPDATE organizations SET canonical_organization_id = p_winning_org_id, merged_at = NOW(), merged_by = p_admin_user_id WHERE id = p_losing_org_id. It MUST NOT touch recordings, organization_memberships, or any table other than organizations (chain/self-ref safety is enforced by the Task 1 trigger, not repeated). unclaim_organization_domain_atomic(p_domain_id UUID, p_admin_user_id UUID) RETURNS void SECURITY DEFINER: same has_role gate, then DELETE FROM organization_domains WHERE id = p_domain_id. For BOTH functions, REVOKE EXECUTE FROM PUBLIC, anon, and authenticated. Authorization is BY PARAMETER (p_admin_user_id) because the callers are service-role edge functions where auth.uid() is NULL. Use has_role, NEVER is_organization_admin_or_owner (Pitfall 5 — this codebase shipped one incident from exactly this confusion). Apply to TEST (link swjzxiddcrtaqixsfaac, db push, introspect both functions exist prosecdef=true and that EXECUTE is denied to authenticated), relink prod.
  </action>
  <verify>
    <automated>rtk proxy grep -c 'REVOKE EXECUTE' supabase/migrations/20260908140001_create_org_merge_unclaim_admin_rpcs.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both RPCs open with `has_role(p_admin_user_id, 'ADMIN')`; neither references is_organization_admin_or_owner or organization_memberships (grep both terms → 0).
    - The REVOKE EXECUTE grep returns >= 6 (PUBLIC/anon/authenticated for each of the two functions).
    - merge_organizations_atomic's body UPDATEs only organizations; it contains no `recordings` or `organization_memberships` reference.
    - TEST introspection: both functions SECURITY DEFINER; EXECUTE denied to anon + authenticated (has_function_privilege false).
    - CLI left linked to prod at task end.
  </acceptance_criteria>
  <done>Admin merge/unclaim RPCs live on TEST, platform-admin-gated and client-unreachable.</done>
</task>

<task type="auto">
  <name>Task 3: ORG-04 bespoke canonical-no-leak block + merge/unclaim RPC integration proof</name>
  <files>src/test/rls-regression.test.ts, src/test/org-merge-unclaim-rpc.integration.test.ts</files>
  <read_first>
    - /Users/admin/dev/brain/main/src/test/rls-regression.test.ts lines 1737-1830 (the identities/identity_aliases bespoke block — the exact both-directions assertion pattern to mirror) and lines 120-165 (BESPOKE registries + multi-org fixture setup)
    - /Users/admin/dev/brain/main/.planning/phases/36-live-organizations/36-RESEARCH.md (Validation Architecture — ORG-04 row + Wave 0 Gaps; the ~15 dependent-table list the pointer must not widen)
    - /Users/admin/dev/brain/main/supabase/CLAUDE.md (integration cleanup contract; RLS regression runs from real JWTs, never service-role)
  </read_first>
  <action>
    Add a bespoke isolation block to src/test/rls-regression.test.ts (mirroring the identities block's structure at 1737-1830) proving canonical_organization_id introduces no cross-org capture leak. Seed via service-role: Org A (with a recording + a claimed organization_domains row), Org B (unrelated), and Org C merged INTO Org A (Org C.canonical_organization_id = Org A.id). Sign in a member of Org B and a member of Org C with signInWithPassword. Assert, from those real JWTs, that neither can read Org A's recordings, workspaces, or call_participants (0 rows) — i.e. the domain claim and the incoming merge pointer grant Org C's member nothing on Org A. Include a positive control (Org A's own member reads exactly Org A's recording) so a deny-everyone misconfiguration cannot pass the leak-only assertion. Additionally assert is_organization_member/is_organization_admin_or_owner behave identically for Org C's member before/after the merge pointer exists (no widening). Then write src/test/org-merge-unclaim-rpc.integration.test.ts (service-role invocation of the RPCs): (a) merge sets the pointer and leaves recordings.organization_id and both orgs' organization_memberships byte-unchanged; (b) clearing canonical_organization_id/merged_at/merged_by fully restores original state; (c) the chain-prevention trigger rejects merge-into-already-merged and rejects merging an org other orgs already point to; (d) calling merge_organizations_atomic with a p_admin_user_id lacking the ADMIN role RAISEs 'Access denied'. Honor the afterAll cleanup contract.
  </action>
  <verify>
    <automated>npx vitest run src/test/rls-regression.test.ts --reporter=verbose && VITEST_INTEGRATION_OK=true npx vitest run src/test/org-merge-unclaim-rpc.integration.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - The bespoke ORG-04 block passes: Org B member and Org C member (Org C merged into Org A) each read 0 rows of Org A's recordings/workspaces/call_participants; Org A's own member reads exactly its 1 recording (positive control).
    - The merge integration test proves recordings.organization_id and organization_memberships are byte-unchanged after merge, and fully restored after the pointer is cleared (non-destructive + reversible).
    - Chain rejection proven in both directions; non-admin p_admin_user_id rejected.
    - Full RLS regression suite (all CROSS_ORG_TABLES + bespoke blocks) green.
    - CLI left linked to prod at task end.
  </acceptance_criteria>
  <done>ORG-04 no-leak invariant and ORG-03 non-destructive/reversible merge proven on TEST.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service-role edge fn → merge/unclaim RPC | Authority passed by parameter (p_admin_user_id); the RPC re-checks has_role, never trusts the caller blindly |
| canonical_organization_id pointer → RLS choke point | The single most dangerous boundary in the phase: the pointer must never influence is_organization_member's answer |
| merged org C member → Org A captures | RLS is the enforcement line; the merge must not create a new read path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06 | Elevation of Privilege | is_organization_member / is_organization_admin_or_owner | mitigate | Task 1 migration proven (grep = 0) to never reference canonical_organization_id; the two functions are byte-for-byte unchanged this phase; bespoke ORG-04 test proves no widening for a merged org's member. This is the Pitfall-1 CRITICAL non-goal. |
| T-36-07 | Elevation of Privilege | merge_organizations_atomic / unclaim_organization_domain_atomic | mitigate | has_role(p_admin_user_id,'ADMIN') gate (NOT org-membership — Pitfall 5, which caused a prior shipped incident); REVOKE EXECUTE from PUBLIC/anon/authenticated so a forged parameter cannot be sent directly via PostgREST. |
| T-36-08 | Tampering (data integrity) | canonical_organization_id | mitigate | not-self CHECK + BEFORE INSERT/UPDATE chain-prevention trigger reject chains/cycles Postgres CHECK cannot express; merge writes only the pointer, so no existing FK/data is destroyed. |
| T-36-09 | Information Disclosure | Org A captures via merge pointer | mitigate | Bespoke ORG-04 isolation block asserts a merged-away org's member reads zero of the surviving org's recordings/workspaces/call_participants, from real JWTs. |

Canon breadcrumb (not minted): generic cross-tenant access control is canon — the bespoke, phase-specific risk (pointer dereference in the choke point) is what is minted above; blanket RLS correctness is covered by the existing CI rls-regression gate + /gsd-secure-phase.
</threat_model>

<verification>
- `npx vitest run src/test/rls-regression.test.ts` green (all CROSS_ORG + the new bespoke ORG-04 block).
- `VITEST_INTEGRATION_OK=true npx vitest run src/test/org-merge-unclaim-rpc.integration.test.ts` green.
- TEST introspection: canonical columns + trigger live; both admin RPCs SECURITY DEFINER + EXECUTE denied to client roles; the two choke-point functions unchanged.
</verification>

<success_criteria>
- ORG-03: duplicate orgs merge non-destructively (pointer only) and reversibly (clear the pointer); chains/cycles impossible.
- ORG-04: org association (domain / alias / canonical pointer) confers no capture access, enforced at RLS and proven by the bespoke test.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- Columns on `organizations`: `canonical_organization_id` (self-FK), `merged_at`, `merged_by`
- Constraint: `organizations_canonical_not_self`
- Function + trigger: `prevent_canonical_organization_chain()`, `organizations_prevent_canonical_chain`
- RPCs: `merge_organizations_atomic(uuid, uuid, uuid)`, `unclaim_organization_domain_atomic(uuid, uuid)`
- Migrations: `20260908140000_add_canonical_organization_id.sql`, `20260908140001_create_org_merge_unclaim_admin_rpcs.sql`
- Test files: `src/test/org-merge-unclaim-rpc.integration.test.ts`; bespoke ORG-04 canonical-no-leak block in `src/test/rls-regression.test.ts`

<output>
Create `.planning/phases/36-live-organizations/36-02-SUMMARY.md` when done.
</output>
