---
phase: 36-live-organizations
plan: 06
type: execute
wave: 5
depends_on: [36-01, 36-02, 36-03, 36-04, 36-05]
files_modified:
  - src/types/supabase.ts
autonomous: false
requirements: [ORG-01, ORG-02, ORG-03, ORG-04]
user_setup: []

must_haves:
  truths:
    - "All four Phase 36 migrations are applied to production (vltmrnjsubfzrgrtdqey) with the prod-ref guard confirmed BEFORE and AFTER the push (ORG-01/02/03)."
    - "merge-organizations and unclaim-organization-domain edge functions are deployed to prod via --use-api (ORG-03)."
    - "Prod introspection confirms: both new tables have FORCE ROW LEVEL SECURITY; the two admin RPCs have EXECUTE denied to anon/authenticated; the chain-prevention trigger is attached; is_organization_member/is_organization_admin_or_owner bodies are byte-identical to pre-phase and contain no reference to canonical_organization_id (ORG-04)."
    - "src/types/supabase.ts is regenerated from the live DB via supabase gen types typescript --linked and reconciled (SAFE-07 discipline); new type-check errors introduced by this phase's own files = 0."
    - "The full unit suite + the RLS regression gate are green before prod apply is authorized."
  artifacts:
    - path: "src/types/supabase.ts"
      provides: "Regenerated types including organization_domains/aliases, canonical columns, and the new RPCs"
      contains: "organization_domains"
  key_links:
    - from: "prod apply"
      to: "prod-ref guard vltmrnjsubfzrgrtdqey"
      via: "supabase projects list ● marker verified before + after"
      pattern: "vltmrnjsubfzrgrtdqey"
    - from: "src/types/supabase.ts"
      to: "live prod schema"
      via: "supabase gen types typescript --linked after prod apply"
      pattern: "organization_domains"
  prohibitions:
    - "MUST NOT apply any migration or deploy any function to production without the linked-project ref confirmed to be vltmrnjsubfzrgrtdqey immediately before AND after the action."
    - "MUST NOT let prod-applied is_organization_member / is_organization_admin_or_owner reference canonical_organization_id — confirm by introspecting the live function bodies after apply (ORG-04 CRITICAL non-goal)."
---

<objective>
Guarded production rollout of Phase 36: apply the four migrations and deploy the two admin edge functions to prod after a full green TEST verification and explicit authorization, then re-sync generated types. Follows this milestone's TEST-then-prod, prod-ref-guarded discipline exactly (the same discipline used in Plans 30-04, 31-04, 32-04, 33-03, 34-07, 35-04).

Purpose: ship Live Organizations to production safely, proving on the live database that the safety invariants (FORCE RLS, REVOKE EXECUTE, the ORG-04 choke-point non-goal) hold after apply — not by assumption.
Output: 4 migrations + 2 functions live on prod; regenerated types committed.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/admin/dev/brain/main/.planning/phases/36-live-organizations/36-01-SUMMARY.md
@/Users/admin/dev/brain/main/.planning/phases/36-live-organizations/36-02-SUMMARY.md
@/Users/admin/dev/brain/main/.planning/phases/36-live-organizations/36-04-SUMMARY.md
@/Users/admin/dev/brain/main/supabase/CLAUDE.md
@/Users/admin/dev/brain/main/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full TEST verification gate (green suite + all migrations confirmed on TEST)</name>
  <files></files>
  <read_first>
    - /Users/admin/dev/brain/main/.planning/phases/36-live-organizations/36-01-SUMMARY.md, 36-02-SUMMARY.md, 36-04-SUMMARY.md (what was applied to TEST + any deviations)
    - /Users/admin/dev/brain/main/supabase/CLAUDE.md (integration-test safety; --use-api deploy note; TEST project ref)
    - /Users/admin/dev/brain/main/.planning/STATE.md (prior guarded-apply precedent: no .env exists in this checkout → use CLI linked-state prod-ref guard)
  </read_first>
  <action>
    Confirm the four Phase 36 migrations (20260908130000, 20260908130001, 20260908140000, 20260908140001) are all present on TEST (callvault-test swjzxiddcrtaqixsfaac) via `supabase migration list --linked` showing Local==Remote for each. Run the full unit suite (npm run test) and the RLS regression gate (npx vitest run src/test/rls-regression.test.ts) and the phase's integration tests (VITEST_INTEGRATION_OK=true npx vitest run for claim-organization-domain-rpc, org-merge-unclaim-rpc, merge-organizations, unclaim-organization-domain) — all green. Confirm frontend type-check (npx tsc -p tsconfig.app.json --noEmit) has 0 new errors attributable to this phase's files (register any pre-existing/expected Deno-import baseline entries only if a genuine tsconfig.app baseline gap surfaces, mirroring prior-phase discipline — do NOT mask real new errors). Leave the CLI linked to prod at task end. This task performs NO prod writes.
  </action>
  <verify>
    <automated>npx vitest run src/test/rls-regression.test.ts --reporter=verbose && npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `supabase migration list --linked` (on TEST) shows all four Phase 36 migrations Local==Remote.
    - Unit suite + RLS regression + the four integration tests are green.
    - Frontend type-check introduces 0 new errors from this phase's files.
    - CLI left linked to prod (vltmrnjsubfzrgrtdqey) at task end; no prod mutation performed.
  </acceptance_criteria>
  <done>Everything proven green on TEST; ready to request prod authorization.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Authorize production apply (one-way door on live customer data)</name>
  <files></files>
  <read_first>
    - /Users/admin/dev/brain/main/.planning/phases/36-live-organizations/36-01-SUMMARY.md, 36-02-SUMMARY.md, 36-04-SUMMARY.md
    - /Users/admin/dev/brain/main/CLAUDE.md (env-file DB-safety: migrations/DDL read prod ref vltmrnjsubfzrgrtdqey)
  </read_first>
  <decision>Authorize applying the four Phase 36 migrations and deploying the two admin edge functions to the production Supabase project (vltmrnjsubfzrgrtdqey). This is a one-way door: undoing it requires a new migration against live customer data.</decision>
  <context>
    All four migrations and both edge functions are proven green on TEST (Task 1). The changes are additive (two new tables, three self-serve RPCs, three additive columns on organizations, two admin RPCs) and non-destructive (no existing table/column/FK is altered or dropped). Prod apply is gated here per the milestone-wide rule that every prod apply requires explicit Andrew approval, guarded by the prod-ref check.
  </context>
  <options>
    <option id="approve">
      <name>approve — apply all four migrations + deploy both functions to prod</name>
      <pros>Ships Live Organizations; matches every prior v2.2 phase's guarded-apply pattern.</pros>
      <cons>Live schema change on the production project (additive/non-destructive; reversible only via a follow-up migration).</cons>
    </option>
    <option id="hold">
      <name>hold — keep on TEST only for now</name>
      <pros>No prod change yet.</pros>
      <cons>Phase 36 does not reach done; blocks downstream phases that build on live orgs.</cons>
    </option>
  </options>
  <action>Halt and present the prod-apply authorization below. Record approve or hold before Task 3 runs; if hold, Task 3 does not execute. Under yolo/auto_advance this may be pre-resolved by Andrew outside the executor invocation, per prior-phase precedent.</action>
  <verify>
    <automated>N/A — authorization checkpoint. Under yolo/auto_advance this may be pre-resolved by Andrew outside the executor invocation, per prior-phase precedent (see STATE.md).</automated>
  </verify>
  <acceptance_criteria>
    - An explicit approve/hold decision is recorded before Task 3 runs. If hold, Task 3 does not execute.
  </acceptance_criteria>
  <done>Prod-apply authorization decision recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Guarded prod apply (4 migrations + 2 function deploys) + prod introspection + type re-sync</name>
  <files>src/types/supabase.ts</files>
  <read_first>
    - /Users/admin/dev/brain/main/CLAUDE.md (Environment files DB-safety — prod ref vltmrnjsubfzrgrtdqey; migrations read .env, prod-ref-guarded)
    - /Users/admin/dev/brain/main/supabase/CLAUDE.md (--use-api function deploy; gen:types discipline)
    - /Users/admin/dev/brain/main/.planning/STATE.md (Phase 31/34/35 guarded-apply: verify prod ref via `supabase projects list` ● marker before+after; no .env in this checkout)
    - /Users/admin/dev/brain/main/.planning/phases/36-live-organizations/36-02-SUMMARY.md (the exact function bodies to confirm live)
  </read_first>
  <action>
    Only after Task 2 = approve. Confirm the CLI is linked to the PROD project and the ref is vltmrnjsubfzrgrtdqey (`supabase projects list` shows the ● marker on callvault-ai / that ref). Apply the four migrations with `supabase db push --linked`. Deploy both functions with `supabase functions deploy merge-organizations --use-api` and `supabase functions deploy unclaim-organization-domain --use-api`. Re-confirm the prod ref is still vltmrnjsubfzrgrtdqey AFTER the push+deploys. Introspect the live prod DB to confirm: organization_domains + organization_aliases exist with relforcerowsecurity=true; claim_organization_domain/add_organization_alias/remove_organization_alias exist SECURITY DEFINER with EXECUTE granted to authenticated; canonical_organization_id/merged_at/merged_by columns + the organizations_prevent_canonical_chain trigger + the not-self CHECK exist; merge_organizations_atomic/unclaim_organization_domain_atomic exist with EXECUTE denied to anon+authenticated; and — the ORG-04 CRITICAL check — pg_get_functiondef of is_organization_member and is_organization_admin_or_owner contains no occurrence of canonical_organization_id (byte-identical to pre-phase). Finally regenerate src/types/supabase.ts via `supabase gen types typescript --linked` (guard against the CLI update-nag stdout-contamination seen in Phase 32 — strip any non-TS trailing lines past the terminating `} as const`), reconcile per SAFE-07, and confirm 0 new type-check errors. Commit types.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Prod ref vltmrnjsubfzrgrtdqey confirmed via `supabase projects list` ● marker BOTH before and after apply+deploy.
    - `supabase migration list --linked` (prod) shows all four Phase 36 migrations Local==Remote.
    - Prod introspection confirms: FORCE RLS on both new tables; the three self-serve RPCs SECURITY DEFINER + EXECUTE to authenticated; canonical columns + chain trigger + not-self CHECK present; both admin RPCs EXECUTE denied to anon/authenticated.
    - pg_get_functiondef(is_organization_member) and pg_get_functiondef(is_organization_admin_or_owner) on prod contain zero occurrences of `canonical_organization_id`.
    - src/types/supabase.ts regenerated, contains organization_domains/organization_aliases + canonical columns, ends cleanly at `} as const` (no CLI-nag contamination), and introduces 0 new type-check errors; committed.
  </acceptance_criteria>
  <human-check>
    End-of-phase human verification (per human_verify_mode=end-of-phase): on production, (1) claim a domain you own from Settings → Organization Identity and confirm the verified badge appears; (2) in /admin → Organizations, expand an org and confirm domains/aliases render; (3) confirm the merge dialog's type-to-confirm gate and that a merge sets the pointer without moving recordings. Report approved or issues.
  </human-check>
  <done>Phase 36 live on production with all safety invariants confirmed by introspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → production Supabase project | The highest-consequence boundary in the phase; a mis-linked CLI could mutate the wrong project (the 2026-05 prod-mutation incident precedent) |
| generated types → committed source | Stale/contaminated types drift the frontend from reality (F16/F17 precedent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-20 | Tampering (wrong-target write) | prod apply/deploy | mitigate | Prod-ref guard: confirm `supabase projects list` ● marker == vltmrnjsubfzrgrtdqey immediately before AND after every prod write; authorization checkpoint (Task 2) precedes any prod mutation. |
| T-36-21 | Elevation of Privilege | live RLS/RPC posture | mitigate | Post-apply prod introspection asserts FORCE RLS on both tables, REVOKE EXECUTE on both admin RPCs, and — critically — that the two choke-point functions still contain no canonical_organization_id reference (ORG-04). |
| T-36-22 | Tampering (schema drift) | src/types/supabase.ts | mitigate | Regenerate via --linked after prod apply; strip CLI-nag stdout contamination (Phase 32 lesson); verify the file terminates at `} as const` and introduces 0 new type errors before commit. |
| T-36-SC | Tampering (supply chain) | package installs | accept | No packages installed this phase; nothing to audit at deploy time. |
</threat_model>

<verification>
- Prod-ref guard (vltmrnjsubfzrgrtdqey) confirmed before + after every prod write.
- Prod `supabase migration list --linked`: 4/4 Phase-36 migrations Local==Remote.
- Prod introspection: FORCE RLS, REVOKE EXECUTE, chain trigger, and the ORG-04 function-body non-reference all confirmed live.
- `npx tsc -p tsconfig.app.json --noEmit`: 0 new errors after type regen.
</verification>

<success_criteria>
- ORG-01/02/03/04 live on production, with every safety invariant (FORCE RLS, service-role-only admin RPCs, the choke-point non-goal) confirmed by direct prod introspection rather than assumed.
- Generated types re-synced and committed; Phase 36 reaches done.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- Production deployment of all Phase 36 migrations + edge functions
- Regenerated `src/types/supabase.ts` (organization_domains, organization_aliases, canonical columns, new RPC signatures)

<output>
Create `.planning/phases/36-live-organizations/36-06-SUMMARY.md` when done.
</output>
