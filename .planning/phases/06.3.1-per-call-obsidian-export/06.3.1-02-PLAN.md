---
phase: 06.3.1-per-call-obsidian-export
plan: 02
type: execute
wave: 1
depends_on:
  - 06.3.1-01
files_modified:
  - src/hooks/useTranscriptExport.ts
  - src/components/call-detail/CallTranscriptTab.tsx
  - src/components/CallDetailDialog.tsx
  - src/hooks/useTranscriptExport.test.ts
autonomous: true
requirements:
  - Phase-06.3.1-D-01
  - Phase-06.3.1-D-02
  - Phase-06.3.1-D-03
  - Phase-06.3.1-D-08
  - Phase-06.3.1-D-09
  - Phase-06.3.1-D-14
  - Phase-06.3.1-D-17
  - Phase-06.3.1-D-18
  - Phase-06.3.1-D-20
must_haves:
  truths:
    - "D-01 D-02 D-03: The per-call Obsidian action lives in the existing call/transcript export surface and completes as a direct call-level action."
    - "D-08 D-09: Copy Obsidian markdown is optional; if included, it must use the exact same markdown string as the download path."
    - "D-14 D-17: Source/share URL resolution and data access stay in existing helpers/hooks, not direct component queries."
    - "D-18 D-20: UI copy stays in the AI-ready brand lane and tests prove the action triggers `.md` download plus copy if shipped."
  artifacts:
    - path: "src/hooks/useTranscriptExport.ts"
      provides: "single-call Obsidian export and optional copy handlers"
    - path: "src/components/call-detail/CallTranscriptTab.tsx"
      provides: "compact visible call-level Obsidian action"
    - path: "src/components/CallDetailDialog.tsx"
      provides: "existing call detail data flow into transcript export handlers"
---

<objective>
Expose `Export to Obsidian` from the current call transcript export area with minimal data threading and no new backend or Settings surface.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.3.1-per-call-obsidian-export/06.3.1-CONTEXT.md
@.planning/phases/06.3.1-per-call-obsidian-export/06.3.1-RESEARCH.md
@.planning/phases/06.3.1-per-call-obsidian-export/06.3.1-PATTERNS.md
@src/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Obsidian handlers to the transcript export hook</name>
  <files>src/hooks/useTranscriptExport.ts</files>
  <read_first>
    - src/hooks/useTranscriptExport.ts
    - src/lib/export-utils.ts
    - src/lib/recording-source-url.ts
    - src/types/meetings.ts
    - .planning/phases/06.3.1-per-call-obsidian-export/06.3.1-PATTERNS.md
  </read_first>
  <action>Extend `useTranscriptExport()` to accept enough call metadata to build an `ExportableCall` for Obsidian: `recording_id`, `canonical_uuid`, `title`, `created_at`, `recording_start_time`, `recording_end_time`, `full_transcript`, `transcript_segments`, `summary`, `recorded_by_name`, `recorded_by_email`, `calendar_invitees`, `source_metadata`, optional `workspace_name`, and `tag_names`. Add explicit `orgName` and `workspaceName` inputs to the hook. Use `resolveShareUrl(call)` to set the exportable `url`. Add `handleExportObsidian()` that calls `exportSingleCallToObsidian(exportableCall, { orgName, workspaceName })` and toasts success/failure. Add `handleCopyObsidianMarkdown()` only if it is straightforward to call `buildObsidianMarkdown()` from a click handler; it must catch clipboard failures and toast failure. Replace Fathom-specific source wording only for the new Obsidian path; do not broaden the existing TXT/MD/PDF/DOCX behavior beyond what this phase needs.</action>
  <acceptance_criteria>
    - `useTranscriptExport` returns `handleExportObsidian`.
    - `handleExportObsidian` calls `exportSingleCallToObsidian`.
    - The Obsidian adapter calls `resolveShareUrl`.
    - The Obsidian adapter passes `tag_names` into the `ExportableCall`.
    - `useTranscriptExport` requires concrete `orgName` and `workspaceName` inputs or applies documented fallbacks before calling `exportSingleCallToObsidian`.
    - No new Supabase query appears in `useTranscriptExport.ts`.
    - If `handleCopyObsidianMarkdown` exists, it calls `navigator.clipboard.writeText` with markdown produced by `buildObsidianMarkdown`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "handleExportObsidian|exportSingleCallToObsidian|resolveShareUrl" src/hooks/useTranscriptExport.ts && npm run type-check</automated>
  </verify>
  <done>The hook can export the current call as an Obsidian note through shared utilities.</done>
</task>

<task type="auto">
  <name>Task 2: Wire compact actions into the transcript export row</name>
  <files>src/components/call-detail/CallTranscriptTab.tsx, src/components/CallDetailDialog.tsx</files>
  <read_first>
    - src/components/call-detail/CallTranscriptTab.tsx
    - src/components/CallDetailDialog.tsx
    - src/hooks/useTranscriptExport.ts
    - src/components/ui/button.tsx
    - src/CLAUDE.md
  </read_first>
  <action>Extend `TranscriptHandlers` with `onExportObsidian` and, only if Task 1 shipped copy support, `onCopyObsidianMarkdown`. In `CallDetailDialog`, import `useOrganizationContext()` and derive `orgName` from `activeOrganization?.name ?? "My Organization"` and `workspaceName` from `call.workspace_name ?? activeWorkspace?.name ?? defaultWorkspace?.name ?? "Uncategorized"` so call-specific workspace metadata wins in org-wide or cross-workspace contexts. Stop destructuring `callTags` as `_callTags`; map `callTags.map((tag) => tag.name).filter(Boolean)` into `tag_names` for the Obsidian adapter. Pass the new hook handlers into the existing `transcriptHandlers` memo and include those concrete metadata inputs without direct UI queries. In `CallTranscriptTab`, add a compact `Export to Obsidian` action in the existing `EXPORT OPTIONS` row using Remix icons such as `RiMarkdownLine` or `RiDownloadLine`. Avoid adding a new primary button, Settings surface, or long row of extra text buttons. If copy support ships, expose it as a compact adjacent action or menu item named `Copy Obsidian markdown`; otherwise omit it without blocking download.</action>
  <acceptance_criteria>
    - `TranscriptHandlers` includes `onExportObsidian`.
    - `CallDetailDialog` passes `handleExportObsidian` through `transcriptHandlers`.
    - `CallDetailDialog` uses `useOrganizationContext()` for `activeOrganization`, `activeWorkspace`, and `defaultWorkspace`.
    - `CallDetailDialog` derives `workspaceName` with `call.workspace_name` before `activeWorkspace?.name`.
    - `CallDetailDialog` maps existing `callTags` to Obsidian `tag_names`.
    - `CallTranscriptTab` renders user-facing text `Export to Obsidian` or `Obsidian`.
    - `CallTranscriptTab.tsx` imports only Remix icons from `@remixicon/react`.
    - No `lucide-react`, `framer-motion`, or direct Supabase import is added.
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check && rg -n "onExportObsidian|Export to Obsidian|Obsidian" src/components/call-detail/CallTranscriptTab.tsx src/components/CallDetailDialog.tsx && ! rg -n "lucide-react|framer-motion|from \\\"@/integrations/supabase/client\\\"" src/components/call-detail/CallTranscriptTab.tsx</automated>
  </verify>
  <done>The call transcript export surface exposes the per-call Obsidian action without a new product surface.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused UI or hook verification</name>
  <files>src/hooks/useTranscriptExport.ts, src/hooks/useTranscriptExport.test.ts, src/components/call-detail/CallTranscriptTab.tsx, src/components/CallDetailDialog.tsx</files>
  <read_first>
    - src/hooks/useTranscriptExport.ts
    - src/components/call-detail/CallTranscriptTab.tsx
    - src/lib/__tests__/export-utils.obsidian.test.ts
    - src/test/setup.ts
    - vitest.config.ts
  </read_first>
  <action>Create `src/hooks/useTranscriptExport.test.ts` as the focused UI-path test. Mock `exportSingleCallToObsidian`, `buildObsidianMarkdown`, `file-saver`, `navigator.clipboard.writeText`, and Sonner toasts as needed. The test must prove `handleExportObsidian()` builds an `ExportableCall` with canonical UUID, resolved share/source URL, concrete `orgName`, concrete `workspaceName`, and `tag_names`, then calls `exportSingleCallToObsidian`. If copy support ships, test that copy and download use the same `buildObsidianMarkdown()` output.</action>
  <acceptance_criteria>
    - `src/hooks/useTranscriptExport.test.ts` exists.
    - The test includes the string `Obsidian`.
    - The test fails if `handleExportObsidian` does not call `exportSingleCallToObsidian`.
    - The test asserts `orgName`, `workspaceName`, `tag_names`, canonical UUID, and resolved share/source URL are passed to the export helper.
    - If copy support exists, the test asserts `navigator.clipboard.writeText` receives Obsidian markdown.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/lib/__tests__/export-utils.obsidian.test.ts && npm test -- src/hooks/useTranscriptExport.test.ts</automated>
  </verify>
  <done>UI/hook wiring is guarded by a focused test in addition to utility tests.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06.3.1-04 | Information Disclosure | clipboard/download | accept | Only explicit user clicks download or copy the already-visible transcript. |
| T-06.3.1-05 | Data Integrity | source URL metadata | mitigate | Resolve source/share URL through `resolveShareUrl()` instead of top-level `share_url` only. |
| T-06.3.1-06 | UX Regression | transcript export row | mitigate | Keep action compact and verify the row in browser before closeout. |
</threat_model>

<verification>
Run targeted utility and UI/hook tests, then `npm run type-check`.
</verification>

<success_criteria>
- The transcript tab exposes a compact Obsidian action.
- The action downloads a single `.md` note for the current call.
- Optional copy support, if shipped, copies the same markdown content as the download.
</success_criteria>

<output>
Create `.planning/phases/06.3.1-per-call-obsidian-export/06.3.1-02-SUMMARY.md` when done.
</output>
