# Plan 06.3.1-01 Summary

## Objective

Extract shared Obsidian markdown helpers from the ZIP export path and add a single-call `.md` download helper without changing full-vault ZIP behavior.

## Completed

- Added exported `buildObsidianFileName`, `buildObsidianVaultPath`, and `buildObsidianMarkdown` helpers in `src/lib/export-utils.ts`.
- Added optional `tag_names?: string[]` to `ExportableCall` and emitted sanitized `call-tags/{slug}` YAML tags.
- Updated `exportToObsidian()` to use the shared markdown builder while preserving ZIP paths, duplicate suffixes, workspace exclusion, and full-vault scale behavior.
- Added `exportSingleCallToObsidian()` for direct single-call markdown downloads using `file-saver` and no JSZip.
- Extended Obsidian utility tests for helper output, exact transcript preservation, call tags, single-call Blob export, missing transcript text, and the existing 5,000-call ZIP target.

## Verification

- `rg -n "export function buildObsidian(FileName|VaultPath|Markdown)|vault_path|has_transcript|## Transcript" src/lib/export-utils.ts` — passed.
- `npm test -- src/lib/__tests__/export-utils.obsidian.test.ts` — passed, 9 tests.
- `! rg -n "slice\\(0,\\s*500\\)|limit\\(500\\)|500-record" src/lib/export-utils.ts src/lib/__tests__/export-utils.obsidian.test.ts` — passed.
- `npm run type-check` — passed.

## Deviations from Plan

None - plan executed exactly as written.

## Self-Check: PASSED
