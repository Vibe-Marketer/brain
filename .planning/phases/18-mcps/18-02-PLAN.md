---
phase: 18-mcps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [MCP-04]

must_haves:
  truths:
    - "MCP OAuth consent page loads correctly when navigated to with an authorization_id"
    - "Consent page displays the requesting application name and requested permissions"
    - "Approving consent redirects back to the MCP client with a valid token"
  artifacts:
    - path: "src/pages/OAuthConsentPage.tsx"
      provides: "OAuth consent UI with approve/deny flow"
      contains: "handleApprove|handleDeny"
    - path: "src/App.tsx"
      provides: "Route registration for /oauth/consent"
      contains: "oauth/consent"
  key_links:
    - from: "src/pages/OAuthConsentPage.tsx"
      to: "supabase.auth.oauth"
      via: "getAuthorizationDetails + approveAuthorization"
      pattern: "supabase\\.auth\\.oauth"
---

<objective>
Verify MCP OAuth consent flow works end-to-end — the consent page loads, displays permissions, and approve/deny actions complete correctly.

Purpose: MCP-04 requires the OAuth consent flow to work E2E. The OAuthConsentPage exists with full approve/deny logic. Verification requires either a configured Supabase OAuth 2.1 provider or confirming the code is correct and identifying any infrastructure gaps.
Output: Verification report on OAuth consent flow status; infrastructure gaps documented if any.
</objective>

<execution_context>
@/Users/Naegele/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Naegele/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/pages/OAuthConsentPage.tsx
@src/App.tsx
</context>

<interfaces>
<!-- From OAuthConsentPage.tsx -->
```typescript
// Supabase Auth OAuth 2.1 APIs used:
supabase.auth.oauth.getAuthorizationDetails(authorizationId: string)
supabase.auth.oauth.approveAuthorization(authorizationId: string)
supabase.auth.oauth.denyAuthorization(authorizationId: string)

// Route: /oauth/consent?authorization_id=<id>
// States: loading | error-no-id | error-expired | error-fetch | error-action | consent | approving | denying
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify OAuth consent page renders and handles all states</name>
  <files></files>
  <read_first>
    - src/pages/OAuthConsentPage.tsx (full file — verify all state handling)
    - src/App.tsx (verify route registration)
  </read_first>
  <action>
    Verify the OAuth consent flow implementation is complete and correct:

    1. **Route verification**: Confirm `/oauth/consent` is registered in App.tsx as a public route (not behind ProtectedRoute). It handles its own auth check internally.

    2. **Code review** of OAuthConsentPage.tsx — verify all states are handled:
       - `loading`: Shows spinner while auth resolves
       - `error-no-id`: Shows error when authorization_id missing from URL
       - `error-expired`: Shows expiry message
       - `error-fetch`: Shows error with retry button
       - `error-action`: Shows error from approve/deny failure
       - `consent`: Shows app name, permissions, approve/deny buttons
       - `approving`/`denying`: Shows loading state on buttons

    3. **Auth redirect**: Unauthenticated users are redirected to `/login?next=/oauth/consent?authorization_id=...` — verify this preserves the authorization_id for post-login redirect.

    4. **Visual verification via dev-browser**: Navigate to `http://localhost:3001/oauth/consent` (no authorization_id) — should show "Invalid Request" error card. Screenshot to confirm.

    5. **Visual verification via dev-browser**: Navigate to `http://localhost:3001/oauth/consent?authorization_id=fake-id` — should show either "loading" then an error (expired/not found), or the consent screen if Supabase returns data. Screenshot to confirm.

    6. **Infrastructure check**: The Supabase OAuth 2.1 provider needs to be configured in the Supabase dashboard (Project Settings > Auth > OAuth 2.1 Providers) with a registered client (client_id, redirect_uri, etc.) for real MCP clients like Claude Desktop. Check if this is configured by looking at any config files or noting it as a setup requirement.

    7. If any code bugs are found (missing error handling, incorrect redirects), fix them. Based on review, the code looks complete.
  </action>
  <verify>
    <automated>cd /Users/Naegele/dev/brain && grep -c "oauth/consent" src/App.tsx && grep -c "handleApprove\|handleDeny\|error-no-id\|error-expired" src/pages/OAuthConsentPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'oauth/consent' src/App.tsx
    - grep -q 'handleApprove' src/pages/OAuthConsentPage.tsx
    - grep -q 'handleDeny' src/pages/OAuthConsentPage.tsx
    - grep -q 'error-no-id' src/pages/OAuthConsentPage.tsx
    - grep -q 'error-expired' src/pages/OAuthConsentPage.tsx
  </acceptance_criteria>
  <done>OAuth consent page verified: route registered, all error states handled, approve/deny flows implemented, unauthenticated redirect preserves authorization_id. Visual verification confirms error states render correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of OAuth consent page and infrastructure decision</name>
  <files></files>
  <action>
    Present the OAuth consent flow verification results to the user for review. The consent page code is complete but full E2E testing requires Supabase OAuth 2.1 provider configuration in the dashboard.
  </action>
  <verify>
    <automated>echo "Checkpoint — human verification required"</automated>
  </verify>
  <done>User has reviewed the OAuth consent flow and confirmed whether code-level verification is sufficient or Supabase OAuth provider setup is needed.</done>
  <what-built>MCP OAuth consent flow verification — the consent page exists at /oauth/consent with full approve/deny logic. The code handles all states (loading, errors, consent, action). However, full E2E testing requires a Supabase OAuth 2.1 provider to be configured in the dashboard with a registered MCP client.</what-built>
  <how-to-verify>
    1. Visit http://localhost:3001/oauth/consent — should show "Invalid Request" error (no authorization_id)
    2. Visit http://localhost:3001/oauth/consent?authorization_id=test — should show loading then error (invalid/expired request)
    3. Both pages should render cleanly with CallVault branding
    4. Review: Is the Supabase OAuth 2.1 provider configured in the Supabase dashboard? If not, this is infrastructure that needs to be set up for real MCP client connections (Claude Desktop, Cursor).
    5. Decide: Is code-level verification sufficient for launch, or does the OAuth provider need to be configured now?
  </how-to-verify>
  <resume-signal>Type "approved" if code verification is sufficient, or describe any issues / infrastructure needs</resume-signal>
</task>

</tasks>

<verification>
- /oauth/consent route registered and accessible
- OAuthConsentPage handles all states (loading, error variants, consent, action states)
- Unauthenticated users redirected to login with return URL preserved
- Visual verification via dev-browser confirms error states render correctly
</verification>

<success_criteria>
- OAuth consent page renders correctly in all states (MCP-04)
- Code review confirms approve/deny flow is implemented correctly
- Any infrastructure gaps (Supabase OAuth provider config) documented
</success_criteria>

<output>
After completion, create `.planning/phases/18-mcps/18-02-SUMMARY.md`
</output>
