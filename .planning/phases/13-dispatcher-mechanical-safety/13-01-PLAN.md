---
phase: 13-dispatcher-mechanical-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/migrations/20260611200000_autopilot_queue_runner_state.sql, scripts/qa/verify-autopilot-rls.ts]
autonomous: true
requirements: [AUTO-01, AUTO-04]
must_haves:
  truths:
    - "tickets has priority (int, default 0), urgent (bool, default false), attempts (int, default 0), next_attempt_at (timestamptz) columns on the LIVE database"
    - "runner_state table exists with status, current_ticket_id, run_started_at, last_heartbeat, last_result, kill_switch and exactly one row"
    - "A non-admin authenticated user cannot UPDATE priority/urgent or any runner_state column; an admin can flip runner_state.kill_switch"
    - "Phase 14 can read runner_state and priority/urgent via the existing admin session"
  artifacts:
    - path: "supabase/migrations/20260611200000_autopilot_queue_runner_state.sql"
      provides: "Queue-control columns + runner_state table + RLS"
      contains: "CREATE TABLE public.runner_state"
    - path: "scripts/qa/verify-autopilot-rls.ts"
      provides: "Service-role RLS probe for the new surface"
  key_links:
    - from: "supabase/migrations/20260611200000_autopilot_queue_runner_state.sql"
      to: "live database"
      via: "supabase db push"
      pattern: "ALTER TABLE public.tickets"
---

<objective>
Ship the repo-side schema contract for the autopilot queue: priority/urgent queue-control columns + attempts/next_attempt_at claim-backoff columns on `tickets`, the `runner_state` visibility table with kill_switch, and RLS that makes priority/urgent/runner_state admin-and-service-role-only. Push it to the LIVE database.

Purpose: This is the contract both the daemon pack (claims, heartbeat, kill switch) and Phase 14 (queue reorder UI, live status card) consume. Decisions implemented: "Queue, priority, and urgency" and "Runner status visibility" (CONTEXT.md, Andrew 2026-06-11). MVP slice: after this plan, Andrew can set `urgent=true` or reorder `priority` on a real ticket via SQL and flip a kill switch — the queue-control surface exists.
Output: One applied migration + a service-role RLS probe script proving the policies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-dispatcher-mechanical-safety/13-CONTEXT.md
@.planning/phases/13-dispatcher-mechanical-safety/13-RESEARCH.md
@.planning/phases/13-dispatcher-mechanical-safety/13-PATTERNS.md
@supabase/migrations/20260611000002_create_ticket_tables.sql
@supabase/migrations/20260611140000_tighten_ticket_messages_author_type.sql
</context>

## Phase Goal

**As an** admin operator (Andrew), **I want to** have tickets fixed autonomously by a local dispatcher whose every change is mechanically gated, evidenced, and held for my approval, **so that** real bugs get fixed without me touching the keyboard while nothing unsafe can ever reach main.

<tasks>

<task type="auto">
  <name>Task 1: Write the autopilot queue + runner_state migration</name>
  <files>supabase/migrations/20260611200000_autopilot_queue_runner_state.sql</files>
  <action>
Create the migration following the header/comment/RLS conventions of 20260611000002 (read it first — header block citing requirement IDs AUTO-01/AUTO-04, COMMENT ON for every new object).

Part A — tickets columns (per locked decision "Queue, priority, and urgency"):
- `ALTER TABLE public.tickets ADD COLUMN priority integer NOT NULL DEFAULT 0;`
- `ADD COLUMN urgent boolean NOT NULL DEFAULT false;`
- `ADD COLUMN attempts integer NOT NULL DEFAULT 0;` and `ADD COLUMN next_attempt_at timestamptz;` (rebind map in 13-PATTERNS.md: dead branch had these on support_tickets; live tickets lacks them — the dispatcher claim/backoff needs them)
- Partial index for the claim query: `CREATE INDEX idx_tickets_claim_queue ON public.tickets (urgent DESC, priority DESC, created_at ASC) WHERE status = 'new';`

Part B — RLS for priority/urgent: verify (do not assume) the existing policy set — per 13-PATTERNS.md, reporters have NO UPDATE policy on tickets and "Admins can update tickets" already gates UPDATE to has_role(auth.uid(),'ADMIN'), so column-level admin-only is already satisfied; document this finding in a SQL comment in the migration rather than adding a redundant policy. If verification shows any non-admin UPDATE path exists, add an explicit restrictive policy.

Part C — runner_state table (per locked decision "Runner status visibility"):
- Single-row table: `id smallint PRIMARY KEY DEFAULT 1 CHECK (id = 1)`, `status text NOT NULL DEFAULT 'idle' CHECK (status IN ('idle','claiming','running','awaiting_gate'))`, `current_ticket_id uuid REFERENCES public.tickets(id)`, `run_started_at timestamptz`, `last_heartbeat timestamptz`, `last_result text`, `kill_switch boolean NOT NULL DEFAULT false`, `updated_at timestamptz NOT NULL DEFAULT now()`.
- Seed the single row: `INSERT INTO public.runner_state (id) VALUES (1) ON CONFLICT DO NOTHING;`
- ENABLE ROW LEVEL SECURITY. Policies: SELECT for admins only (`public.has_role(auth.uid(),'ADMIN')` — Phase 14 status card reads it); UPDATE for admins with WITH CHECK restricted so the only admin-writable column is kill_switch (enforce via a BEFORE UPDATE trigger that rejects non-kill_switch changes when auth.uid() IS NOT NULL, mirroring the SECURITY DEFINER trigger idiom from 20260611000002 — service-role writes have auth.uid() NULL and pass). No INSERT/DELETE policies (single row, service-role only).
- updated_at touch trigger copying the update_tickets_updated_at idiom.
COMMENT ON every new column/table per live-migration convention.
  </action>
  <verify>
    <automated>grep -c 'CREATE TABLE public.runner_state\|ADD COLUMN priority\|ADD COLUMN urgent\|ADD COLUMN attempts\|next_attempt_at\|kill_switch' supabase/migrations/20260611200000_autopilot_queue_runner_state.sql | grep -v '^0$'</automated>
  </verify>
  <done>Migration file exists containing tickets ALTERs (priority, urgent, attempts, next_attempt_at), runner_state CREATE TABLE with kill_switch and CHECK(id=1), RLS enable + admin SELECT policy, kill_switch-only admin UPDATE enforcement, seed row, and COMMENT ON statements.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push the migration to the live database</name>
  <files>supabase/migrations/20260611200000_autopilot_queue_runner_state.sql</files>
  <action>
Run `supabase db push` (Supabase ORM detected — this task is mandatory; build/type checks pass WITHOUT the push because types come from config, not the live DB, creating false-positive verification). Migration history was reconciled in a prior phase — push is expected to work. If the CLI prompts interactively in a non-TTY context, set SUPABASE_ACCESS_TOKEN from the environment. If push fails on history mismatch, STOP and report — do not repair history ad hoc.
  </action>
  <verify>
    <automated>supabase migration list 2>/dev/null | grep 20260611200000</automated>
  </verify>
  <done>`supabase migration list` shows 20260611200000 applied remotely; live DB has the new columns and table.</done>
</task>

<task type="auto">
  <name>Task 3: RLS probe script proving the new surface</name>
  <files>scripts/qa/verify-autopilot-rls.ts</files>
  <action>
Create a service-role probe script following the conventions of scripts/verify-connectors-live.ts (dotenv/config import, SUPABASE_URL || VITE_SUPABASE_URL fallback, SUPABASE_SERVICE_ROLE_KEY, pass/fail/skip CheckResult shape). Checks (each prints one line, script exits non-zero on any fail):
1. Service-role can SELECT and UPDATE runner_state (heartbeat write path works).
2. Anon-key client (no auth) gets zero rows from runner_state SELECT and an error/zero-count on UPDATE.
3. Anon-key client cannot UPDATE tickets.priority/urgent (0 rows affected on a known ticket id).
4. Service-role can INSERT a ticket_messages row with author_type='agent' and anon/authenticated cannot (spoof probe — reuse the 11-05 policy expectation).
5. runner_state has exactly one row with id=1.
Run it with `npx tsx scripts/qa/verify-autopilot-rls.ts` and capture passing output. Do not commit secrets; the script reads .env.local like its analog.
  </action>
  <verify>
    <automated>npx tsx scripts/qa/verify-autopilot-rls.ts</automated>
  </verify>
  <done>Probe exits 0 with all checks pass against the live database; failures (if any) are fixed in the migration before this plan completes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → tickets.priority/urgent | reporters must not control queue order |
| authenticated client → runner_state | only admins may read; only kill_switch is admin-writable |
| service-role → all autopilot tables | daemon's write path; bypasses RLS by design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Elevation of privilege | tickets.priority/urgent UPDATE | mitigate | Verify no non-admin UPDATE policy exists on tickets; admin-only policy already gates UPDATE; probe #3 proves it |
| T-13-02 | Tampering | runner_state non-kill_switch columns via admin session | mitigate | BEFORE UPDATE trigger rejects non-kill_switch changes for authenticated callers (auth.uid() NOT NULL) |
| T-13-03 | Spoofing | ticket_messages author_type='agent' from authenticated path | mitigate | Existing 11-05 policy; probe #4 re-proves after this migration |
| T-13-04 | Information disclosure | runner_state readable by reporters | mitigate | SELECT policy admin-only |
| T-13-SC | Tampering | package installs | accept | No new packages in this plan (tsx/dotenv/supabase-js already in repo) |
</threat_model>

<verification>
- Migration applied on live DB (`supabase migration list`)
- `npx tsx scripts/qa/verify-autopilot-rls.ts` exits 0
- `npm run build` still green (no frontend impact expected)
</verification>

<success_criteria>
- Live tickets table has priority/urgent/attempts/next_attempt_at; runner_state exists, seeded, kill_switch flippable by admin only
- RLS probe proves: anon blocked everywhere, admin limited to kill_switch on runner_state, agent-row spoof blocked
</success_criteria>

<output>
Create `.planning/phases/13-dispatcher-mechanical-safety/13-01-SUMMARY.md` when done
</output>
