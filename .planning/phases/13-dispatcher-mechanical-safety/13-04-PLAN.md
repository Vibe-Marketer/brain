---
phase: 13-dispatcher-mechanical-safety
plan: 04
type: execute
wave: 2
depends_on: [13-02]
files_modified: [~/dev/autopilot/gate/push-gate.sh, ~/dev/autopilot/gate/denylist.txt, ~/dev/autopilot/gate/push-gate-test.sh]
autonomous: true
requirements: [AUTO-03, AUTO-04]
must_haves:
  truths:
    - "A diff touching supabase/migrations/**, RLS/auth/billing paths, .github/**, or package-lock.json makes push-gate.sh exit 1 — regardless of what any agent claimed"
    - "With the kill switch set (DB runner_state.kill_switch OR local flag file), push-gate.sh exits 2 before examining anything else"
    - "A branch that did not advance exactly one commit past base fails the gate"
    - "The gate is deterministic and non-LLM: same inputs always produce the same exit code; no model is invoked anywhere in it"
    - "Fixture tests prove all gate behaviors offline (synthetic git repos, no live DB needed for denylist/commit-advance cases)"
  artifacts:
    - path: "~/dev/autopilot/gate/push-gate.sh"
      provides: "Deterministic authority boundary (ISC-107/108)"
    - path: "~/dev/autopilot/gate/denylist.txt"
      provides: "Blast-radius path patterns"
    - path: "~/dev/autopilot/gate/push-gate-test.sh"
      provides: "Offline fixture harness covering every exit code"
  key_links:
    - from: "~/dev/autopilot/gate/push-gate.sh"
      to: "runner_state.kill_switch"
      via: "service-role REST query immediately pre-push"
      pattern: "kill_switch"
---

<objective>
Build the deterministic non-LLM push-gate: blast-radius denylist diff check, kill-switch recheck immediately pre-push (DB + flag file), commit-advance assertion — judged purely by exit code — plus an offline fixture test harness proving every behavior.

Purpose: This script is the authority boundary of the entire autopilot (ISC-107/108, AUTO-03/04). The agent never holds push authority; the gate does. Implements CONTEXT decisions: push-gate denylist regardless of lane; gate re-checks kill switch immediately pre-push; v1 ships nothing autonomously (gate's pass outcome is "branch may be pushed / approval-merge may proceed", never "push to main now" — the only main-merge path is the approval event in plan 06, which re-runs this gate). MVP slice: after this plan, Andrew can run the gate against any branch and get a mechanical verdict, and flipping the kill switch visibly blocks it.
Output: gate/ directory at ~/dev/autopilot/ (OUTSIDE repo) with script, denylist, and green fixture tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dispatcher-mechanical-safety/13-CONTEXT.md
@.planning/phases/13-dispatcher-mechanical-safety/13-RESEARCH.md
@.planning/phases/13-dispatcher-mechanical-safety/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: push-gate.sh + denylist.txt</name>
  <files>~/dev/autopilot/gate/push-gate.sh, ~/dev/autopilot/gate/denylist.txt</files>
  <action>
denylist.txt — one extended-regex per line, locked floor from CONTEXT plus concrete repo paths (verify against the actual repo tree before finalizing patterns): `^supabase/migrations/`, `^supabase/functions/.*(auth|billing|polar)`, `^\.github/`, `(^|/)package-lock\.json$`, plus RLS-bearing SQL anywhere (`\.sql$` is already covered by migrations; add `^src/.*[Bb]illing`, auth route/config paths found by inspecting src/ — name the exact directories in the file with comments). Comments allowed with leading `#` (gate must strip them — grep gate hygiene).

push-gate.sh usage: `push-gate.sh <worktree-or-clone-path> <base-sha>`; exit codes: 0 = in-policy, 1 = out-of-policy/unverified, 2 = kill switch. Order of checks (locked):
1. Kill-switch FIRST and immediately pre-push on every invocation (ISC-108): local flag file test (config killSwitchFlagFile path), then DB check — `curl -s` to the Supabase REST endpoint for runner_state.kill_switch with the service-role key from ~/dev/autopilot/.env (read the file directly; do not export keys into the environment of child processes). Either true → exit 2. DB unreachable → exit 2 (fail closed).
2. Commit-advance (steal-list #2): `git rev-parse HEAD` != base AND `git rev-parse HEAD~1` == base, else exit 1.
3. Denylist: `git diff --name-only <base>..HEAD` piped against denylist.txt patterns (strip `#` comments first: `grep -v '^#' denylist.txt`); any match → exit 1, printing the offending paths.
4. Otherwise exit 0.
Bash with `set -u`; no LLM, no network beyond the single kill-switch REST read; every decision printed to stdout for the evidence log. The script never pushes anything itself — callers (runner plan 03 for branch push, claimer plan 06 for approval-merge) act on the exit code.
  </action>
  <verify>
    <automated>bash -n ~/dev/autopilot/gate/push-gate.sh && grep -vc '^#' ~/dev/autopilot/gate/denylist.txt | grep -qv '^0$' && echo GATE_SYNTAX_OK</automated>
  </verify>
  <done>Gate script parses, implements the three checks in locked order with fail-closed kill-switch handling, exit codes 0/1/2 as specified; denylist covers the locked floor with repo-verified concrete paths.</done>
</task>

<task type="auto">
  <name>Task 2: Offline fixture harness — push-gate-test.sh</name>
  <files>~/dev/autopilot/gate/push-gate-test.sh</files>
  <action>
Self-contained harness: creates a temp git repo (mktemp -d), seeds a base commit, then for each fixture creates a branch with a planted diff and asserts the gate's exit code. Fixtures (each prints PASS/FAIL, harness exits non-zero on any FAIL):
1. clean-src-fix: one commit touching `src/components/Example.tsx` → expect exit 0 (with kill switch off and DB check stubbed — provide a PUSH_GATE_SKIP_DB=1 env hook in the gate for offline testing, defaulting to the real check; document that E2E/live runs never set it).
2. migration-touch: commit adding `supabase/migrations/x.sql` → expect exit 1.
3. github-touch: commit editing `.github/workflows/ci.yml` → expect exit 1.
4. lockfile-touch: commit editing `package-lock.json` → expect exit 1.
5. kill-switch-flag: touch the flag file → expect exit 2 even on a clean diff.
6. no-advance: branch at base (zero commits) → expect exit 1.
7. multi-commit: branch two commits past base → expect exit 1 (commit-advance requires exactly one).
  </action>
  <verify>
    <automated>bash ~/dev/autopilot/gate/push-gate-test.sh</automated>
  </verify>
  <done>All seven fixtures pass; harness is offline (no live DB) via the documented PUSH_GATE_SKIP_DB hook; kill-switch flag fixture proves precedence over everything.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-authored diff → push decision | the gate must judge mechanically, never trust agent output |
| kill switch (DB + file) → gate | the operator's halt authority must win over everything |
| gate env hooks (PUSH_GATE_SKIP_DB) → production | test hooks must not weaken live runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-14 | Elevation of privilege | out-of-policy diff reaching main | mitigate | Denylist by exit code, independent of agent output (ISC-107); v1 holds everything at branch regardless |
| T-13-15 | DoS-inversion (failing open) | DB unreachable during kill check | mitigate | Fail closed: unreachable → exit 2 |
| T-13-16 | Tampering | PUSH_GATE_SKIP_DB abused in production | mitigate | Hook documented test-only; plan 06 invokes the gate without it; E2E (plan 07) verifies the live DB check fires |
| T-13-17 | Tampering | denylist edited by the agent in-worktree | mitigate | Gate reads denylist from ~/dev/autopilot/gate/, never from the worktree under review |
| T-13-SC | Tampering | installs | accept | No packages — pure bash + git + curl |
</threat_model>

<verification>
- `bash ~/dev/autopilot/gate/push-gate-test.sh` exits 0, 7/7 fixtures
- Live kill-switch probe: `UPDATE runner_state SET kill_switch=true` then gate run on a clean fixture exits 2; reset to false after
</verification>

<success_criteria>
- Gate verdicts are mechanical, deterministic, and fixture-proven for every exit code
- Kill switch (either source) wins over everything, fail-closed
</success_criteria>

<output>
Create `.planning/phases/13-dispatcher-mechanical-safety/13-04-SUMMARY.md` when done
</output>
