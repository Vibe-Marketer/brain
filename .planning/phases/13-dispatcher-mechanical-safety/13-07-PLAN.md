---
phase: 13-dispatcher-mechanical-safety
plan: 07
type: execute
wave: 4
depends_on: [13-01, 13-02, 13-03, 13-04, 13-05, 13-06]
files_modified: [.planning/phases/13-dispatcher-mechanical-safety/13-E2E-PROOF.md]
autonomous: false
requirements: [AUTO-01, AUTO-02, AUTO-03, AUTO-04, AUTO-05, AUTO-06]
must_haves:
  truths:
    - "One REAL ticket (1deaa9b7, the AbortError console-noise low-risk fix) flows ticket→claim→fix→evidence→codex review→admin approval→merge→deploy-SHA verify with zero manual steps besides the approval click"
    - "During the run, ~/dev/brain's working tree is untouched (git status delta empty)"
    - "The evidence bundle on the ticket contains diff summary, test tail, codex verdict, and post-merge deploy-SHA confirmation"
    - "Kill-switch and watchdog behavior were exercised live during the proof window"
  artifacts:
    - path: ".planning/phases/13-dispatcher-mechanical-safety/13-E2E-PROOF.md"
      provides: "Timestamped proof log with SQL/CLI outputs for every stage"
  key_links:
    - from: "tickets row 1deaa9b7"
      to: "production deploy"
      via: "the full autopilot pipeline"
      pattern: "deploy-SHA"
---

<objective>
Run the end-to-end proof on a real ticket: 1deaa9b7 (AbortError console noise, low risk, currently held at awaiting_approval lifecycle start). The pipeline runs unattended through evidence; Andrew's approval click is the single human checkpoint of the phase; then merge, push, deploy, and deploy-SHA verification complete autonomously.

Purpose: This is the phase's definition of done (CONTEXT Specific Ideas; ROADMAP success criteria 1-6; the spike's realism note applies — a production ticket with no fixture tells). MVP slice: Andrew experiences the entire product promise once, for real.
Output: 13-E2E-PROOF.md — a timestamped evidence log proving each ROADMAP success criterion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dispatcher-mechanical-safety/13-CONTEXT.md
@.planning/phases/13-dispatcher-mechanical-safety/13-RESEARCH.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage and launch the proof run</name>
  <files>.planning/phases/13-dispatcher-mechanical-safety/13-E2E-PROOF.md</files>
  <action>
Pre-flight (record every output in 13-E2E-PROOF.md with timestamps):
1. Verify ticket 1deaa9b7 exists with status eligible for claiming (RESEARCH assumption A1 — if it is not status 'new'/claimable, reset it to 'new' via service-role with a note, or substitute another open low-risk ticket and record the substitution).
2. Snapshot `git -C ~/dev/brain status --porcelain` (baseline for the untouched-checkout proof).
3. Confirm watchdog job live (`launchctl list`), runner_state heartbeat advancing, gate fixtures green (`bash ~/dev/autopilot/gate/push-gate-test.sh`).
4. Kill-switch live drill (AUTO-04 proof): with kill_switch=true (left ON by plan 06), observe one launchd cycle claim nothing (runner_state.last_result = kill switch note). Record.
5. Launch: set runner_state.kill_switch=false and (if needed to beat quiet hours) run `bun run src/claimer.ts` directly. The dispatcher claims 1deaa9b7 (set its priority high or urgent=true via service-role so it is head-of-queue — this also live-exercises the urgent lane ordering).
6. Monitor to completion of the unattended half: transcript in ~/dev/autopilot/logs/runs/, ticket reaches awaiting_approval with the full evidence bundle (diff summary, test tail, codex review verdict), fix branch on origin, ~/dev/brain status delta still empty. Record all of it, including the JSONL line.
If the agent verdict is ESCALATE/DIVERT instead of FIXED: that is a VALID mechanical outcome (spike realism note) — record it, pick the next low-risk open ticket, and repeat once; if two consecutive escalations, stop and report the phase needs a better-scoped first ticket rather than forcing a fix.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-dispatcher-mechanical-safety/13-E2E-PROOF.md && grep -c 'awaiting_approval' .planning/phases/13-dispatcher-mechanical-safety/13-E2E-PROOF.md | grep -qv '^0$'</automated>
  </verify>
  <done>Proof log shows: kill-switch drill, claim of the real ticket, unattended run, evidence bundle present, branch held, live checkout untouched, ticket at awaiting_approval.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Andrew's approval click — the phase's single human checkpoint</name>
  <files>.planning/phases/13-dispatcher-mechanical-safety/13-E2E-PROOF.md</files>
  <action>PAUSE for Andrew. Present the evidence bundle location and the documented approval SQL; wait for his explicit approval or rejection event. Never author the approval event on his behalf — it is his checkpoint by design (v1 trust window, ISA ISC-66). Record his decision and timestamp in the proof log.</action>
  <verify>Approval (or rejection) event row exists in ticket_events with Andrew's admin actor_id; proof log records the decision.</verify>
  <done>Andrew has explicitly approved or rejected the held fix; his decision is recorded in 13-E2E-PROOF.md.</done>
  <what-built>The full unattended pipeline ran on real ticket 1deaa9b7: a held fix branch with evidence bundle (diff, tests, codex review) is at awaiting_approval. Nothing has merged. The approval event is yours by design (v1 trust window — ISA ISC-66).</what-built>
  <how-to-verify>
1. Read the evidence bundle on ticket 1deaa9b7 (ticket_messages, latest agent message) — diff summary, test output, codex review verdict.
2. Inspect the held branch if desired: `git -C ~/dev/autopilot/clone log origin/fix/ticket-1deaa9b7 -1 --stat`.
3. To APPROVE, author the approval event (exact SQL documented in 13-06-SUMMARY.md — service-role INSERT into ticket_events with event_type='approval' and YOUR admin actor_id).
4. To REJECT, use event_type='rejection' — the dispatcher will close the branch and post the reason.
  </how-to-verify>
  <resume-signal>Type "approved" after authoring the approval event (or "rejected"/describe issues)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify autonomous completion + close the proof</name>
  <files>.planning/phases/13-dispatcher-mechanical-safety/13-E2E-PROOF.md</files>
  <action>
After the approval event: the next dispatcher cycle must execute approval-merge unattended. Record in the proof log:
1. Gate re-run output (kill-switch recheck + denylist + commit-advance) from the cycle log.
2. ff-only merge + push to main (commit SHA).
3. Deploy-SHA verification result appended to the ticket evidence (live deploy carries the fix commit — ISC-112).
4. Ticket status resolved; full ticket_events timeline reconstructs the run end-to-end (ISC-77 — paste the SELECT output).
5. Watchdog proof (AUTO-05): stop the dispatcher job (`launchctl unload`), wait past the staleness threshold, record the user_notifications page row + notification; reload the job.
6. Close 13-E2E-PROOF.md with a criterion-by-criterion table mapping ROADMAP success criteria 1-6 and AUTO-01..06 to the recorded evidence lines. Leave the system in its decided resting state: dispatcher + watchdog loaded, kill_switch=false, quiet-hours budget active.
  </action>
  <verify>
    <automated>grep -cE 'deploy.?SHA|resolved' .planning/phases/13-dispatcher-mechanical-safety/13-E2E-PROOF.md | grep -qv '^0$' && grep -c 'AUTO-0[1-6]' .planning/phases/13-dispatcher-mechanical-safety/13-E2E-PROOF.md | grep -qv '^0$'</automated>
  </verify>
  <done>Proof log maps every AUTO requirement and ROADMAP criterion to concrete recorded evidence; zero manual steps occurred besides the approval click.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real production ticket → unattended fix | first live exposure of the whole pipeline |
| approval click → production deploy | the single human authority point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-26 | Elevation of privilege | E2E run drifting beyond the one staged ticket | mitigate | Kill switch ON until launch; single ticket prioritized; budget guards live |
| T-13-27 | Tampering | bad fix reaching production | mitigate | Andrew reads evidence before approving; deploy-SHA verify after; revert SHA recorded in evidence |
| T-13-28 | Repudiation | proof claims without receipts | mitigate | Every stage recorded as command output in 13-E2E-PROOF.md |
| T-13-SC | Tampering | installs | accept | No installs in this plan |
</threat_model>

<verification>
- 13-E2E-PROOF.md complete with timestamped outputs for all six ROADMAP criteria
- Ticket 1deaa9b7 (or recorded substitute) resolved with full evidence chain; live checkout untouched throughout
</verification>

<success_criteria>
- The phase goal happened once, for real, on a production ticket — with exactly one human action (the approval)
</success_criteria>

<output>
Create `.planning/phases/13-dispatcher-mechanical-safety/13-07-SUMMARY.md` when done
</output>
