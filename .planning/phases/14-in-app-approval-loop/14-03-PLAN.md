---
phase: 14-in-app-approval-loop
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/auto-merge.yml
  - src/__tests__/agent-pr-merge-invariant.test.ts
autonomous: true
requirements: [APPR-03]
must_haves:
  truths:
    - "The auto-merge workflow can never approve or enable auto-merge on an agent-authored PR: the job is condition-gated to skip PRs labeled 'autopilot' AND PRs whose author is the agent's gh identity"
    - "A committed invariant test fails the suite if the exclusion guard is removed from auto-merge.yml or if any other workflow gains an unguarded merge command — the no-merge-without-approval invariant is enforced by CI itself"
  artifacts:
    - path: ".github/workflows/auto-merge.yml"
      provides: "Agent-PR exclusion guard on the auto-merge job condition"
      contains: "autopilot"
    - path: "src/__tests__/agent-pr-merge-invariant.test.ts"
      provides: "Static invariant test parsing .github/workflows/*.yml"
  key_links:
    - from: "src/__tests__/agent-pr-merge-invariant.test.ts"
      to: ".github/workflows/auto-merge.yml"
      via: "fs read + assertion on the job's if-condition"
      pattern: "auto-merge"
---

<objective>
Close APPR-03's CI half: exclude agent-authored PRs from the repo's label-driven auto-merge path and pin the exclusion with an invariant test, so no agent change can reach main through CI side doors — merge happens only via the Phase 13 push-gate or the explicit admin approval event.

Purpose: APPR-03 / ROADMAP Phase 14 SC-3. The dispatcher (13) holds every fix as a branch+PR; this plan guarantees GitHub automation cannot merge those PRs behind the approval loop's back.
Output: guarded auto-merge.yml + committed invariant test.
</objective>

## Phase Goal

**As an** admin (Andrew), **I want** CI to be structurally unable to merge agent PRs, **so that** my in-app approval is the only path an autonomous fix can take to main.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-in-app-approval-loop/14-CONTEXT.md
@.planning/phases/13-dispatcher-mechanical-safety/13-CONTEXT.md (push-gate denylist; gh CLI authed as Vibe-Marketer)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agent-PR exclusion guard in auto-merge.yml</name>
  <files>.github/workflows/auto-merge.yml</files>
  <read_first>.github/workflows/auto-merge.yml (current job: fires on `github.event.label.name == 'auto-merge'`, approves via GITHUB_TOKEN, runs `gh pr merge --squash --auto`), .github/workflows/ci.yml and sentry-autofix.yml (confirm no other merge-capable steps exist — record findings in SUMMARY), ~/dev/autopilot/autopilot.config.ts (the agent's PR author identity — gh CLI is authed as Vibe-Marketer per 13-CONTEXT; verify the exact login the dispatcher will use and record it)</read_first>
  <action>
Extend the `auto-merge` job's `if:` condition in .github/workflows/auto-merge.yml so it runs ONLY when ALL hold: the existing `github.event.label.name == 'auto-merge'`, AND `!contains(github.event.pull_request.labels.*.name, 'autopilot')`, AND `github.event.pull_request.user.login != '<verified agent login>'` (use the login verified in read_first — expected 'Vibe-Marketer'; if the dispatcher uses a different account, use that and note it). Add a YAML comment block above the condition: "APPR-03 invariant: agent-authored PRs (label 'autopilot' or agent login) are NEVER auto-merged — they merge only via the autopilot push-gate on an explicit admin approval event (Phase 13/14). The invariant test src/__tests__/agent-pr-merge-invariant.test.ts pins this guard." Coordinate the label contract: Phase 13's PR-opening step labels agent PRs 'autopilot' — if 13's plans (13-04/13-05) haven't locked a label name yet, 'autopilot' is the contract; record it in the SUMMARY for 13's executors. Do not alter the human 'auto-merge' label flow otherwise.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/auto-merge.yml','utf8'); if(!/autopilot/.test(y)||!/user\.login/.test(y)) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - auto-merge.yml job condition contains both the 'autopilot' label exclusion and the agent-login exclusion
    - The human auto-merge flow (label 'auto-merge' on a human PR) is unchanged
    - SUMMARY records the survey result: which workflows (if any) besides auto-merge.yml contain merge-capable commands
  </acceptance_criteria>
  <done>GitHub's only auto-merge automation structurally skips agent PRs.</done>
</task>

<task type="auto">
  <name>Task 2: Invariant test — no unguarded merge path in CI</name>
  <files>src/__tests__/agent-pr-merge-invariant.test.ts</files>
  <read_first>.github/workflows/auto-merge.yml (post-Task-1 state), src/__tests__/ or src/services/__tests__/ (existing vitest static-test style, e.g. any test that reads repo files), vitest.config.ts (test include globs — place the file where the runner picks it up)</read_first>
  <action>
Create a vitest static-invariant test (pure fs, no network) that:
1. Reads .github/workflows/auto-merge.yml and asserts the job `if:` condition includes BOTH `!contains(github.event.pull_request.labels.*.name, 'autopilot')` and a `user.login !=` guard against the verified agent login (assert on normalized whitespace, not exact bytes).
2. Globs .github/workflows/*.yml and asserts that any file containing a merge-capable command (`gh pr merge`, `pulls.merge`, `merge_method`, `automerge`) is auto-merge.yml itself — i.e., no second merge path exists without this test failing.
3. Asserts auto-merge.yml still requires the human 'auto-merge' label (the positive trigger), so the guard wasn't satisfied by deleting the workflow's function.
Name test cases after the invariant ("agent PRs are excluded from auto-merge", "no merge-capable workflow exists outside auto-merge.yml"). If the file placement requires a vitest include-glob change, prefer placing the test under an already-included directory instead of editing config.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/agent-pr-merge-invariant.test.ts && npm test 2>&1 | tail -5 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Test passes on the post-Task-1 tree; manually deleting the 'autopilot' exclusion makes it fail (demonstrate once in SUMMARY, then restore)
    - Full `npm test` and `npm run build` exit 0
  </acceptance_criteria>
  <done>The no-merge-without-approval invariant is pinned by the suite — removing the guard breaks CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-authored PR → main | CI automation must never bridge this without the approval loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Elevation of Privilege | agent PR labeled 'auto-merge' (by agent or mistake) | mitigate | Label exclusion + author exclusion are ANDed with the trigger — either agent signal blocks the job |
| T-14-10 | Tampering | future workflow adds a merge command | mitigate | Invariant test (2) fails the suite on any new merge-capable workflow |
| T-14-11 | Repudiation | silent guard removal | mitigate | Invariant test (1) pins the condition text; removal fails CI |
| T-14-SC | Tampering | package installs | accept | No new packages (fs + vitest only) |
</threat_model>

<verification>
- Invariant test + full `npm test` + `npm run build` exit 0
- Guard-removal negative check demonstrated once and restored
- Workflow survey recorded in SUMMARY (no second merge path)
</verification>

<success_criteria>
APPR-03 CI half satisfied: agent PRs are excluded from auto-merge by condition, and the exclusion is regression-proofed by a committed invariant test. (The other half — the deterministic push-gate — is Phase 13's AUTO-03 and is already its plans' scope.)
</success_criteria>

<output>
Create `.planning/phases/14-in-app-approval-loop/14-03-SUMMARY.md` when done
</output>
