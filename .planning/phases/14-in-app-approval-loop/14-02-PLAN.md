---
phase: 14-in-app-approval-loop
plan: 02
type: execute
wave: 1
depends_on: []
external_gate: "DO NOT EXECUTE until .planning/phases/13-dispatcher-mechanical-safety/13-01-SUMMARY.md exists — runner_state table and tickets.priority/urgent columns ship with 13-01 (migration 20260611200000). If absent, STOP and report the gate."
files_modified:
  - src/services/admin-dashboard.service.ts
  - src/services/__tests__/admin-dashboard.service.test.ts
  - src/hooks/useAdminDashboard.ts
  - src/pages/admin/DashboardSection.tsx
  - src/services/admin-ticket-controls.service.ts
  - src/services/__tests__/admin-ticket-controls.service.test.ts
  - src/hooks/useAdminTicketControls.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The /admin dashboard runner card shows live runner_state (status, current ticket, heartbeat age) instead of the 16-01 'not deployed yet' stub, and shows 'runner offline' when last_heartbeat is stale (13-CONTEXT 'Runner status visibility')"
    - "An admin can flip runner_state.kill_switch from the dashboard card (confirm step) and the change persists; non-kill_switch columns remain untouchable from the admin session (13-01 trigger)"
    - "A service+hook pair exists for admin priority quick-set and URGENT toggle on tickets (direct UPDATE, RLS admin-gated — no Edge Function needed per 14-CONTEXT scope item 4); 14-04 mounts the UI"
  artifacts:
    - path: "src/services/admin-dashboard.service.ts"
      provides: "Typed runner_state read (getRunnerState) + setKillSwitch(value)"
      exports: ["getRunnerState", "setKillSwitch"]
    - path: "src/services/admin-ticket-controls.service.ts"
      provides: "updateTicketQueueControls(ticketId, {priority?, urgent?}) — pure async, admin RLS enforced"
      exports: ["updateTicketQueueControls"]
    - path: "src/hooks/useAdminTicketControls.ts"
      provides: "useUpdateTicketQueueControls mutation invalidating ticket queries"
  key_links:
    - from: "src/pages/admin/DashboardSection.tsx"
      to: "src/hooks/useAdminDashboard.ts"
      via: "useRunnerState / kill-switch mutation (no inline supabase calls)"
      pattern: "useRunnerState"
    - from: "src/services/admin-ticket-controls.service.ts"
      to: "tickets table"
      via: "supabase.from('tickets').update({priority, urgent})"
      pattern: "urgent"
---

<objective>
Bring the runner ops surface live in /admin: replace the Dashboard runner-card stub with real `runner_state` reads (status, current ticket, heartbeat staleness → "runner offline") plus the admin kill-switch toggle, and ship the service+hook layer for priority quick-set / URGENT toggle that 14-04 mounts on the ticket detail.

Purpose: 14-CONTEXT scope items 4 and 5 (decisions from 13-CONTEXT "Runner status visibility" and "Queue, priority, and urgency"). 16-01 left the read wired but stubbed ("not deployed yet" — 16-01-SUMMARY Known Stubs); 13-01 lands the table now.
Output: live runner card + kill switch, queue-control service/hook layer, tests.
</objective>

## Phase Goal

**As an** admin (Andrew), **I want to** see what the runner is doing and control the queue and kill switch from /admin, **so that** the autonomous loop is observable and stoppable without SQL.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-in-app-approval-loop/14-CONTEXT.md
@.planning/phases/13-dispatcher-mechanical-safety/13-01-PLAN.md (runner_state columns, kill_switch-only trigger, RLS posture)
@src/CLAUDE.md
</context>

<sequencing_gate>
**HARD EXECUTION GATE:** `.planning/phases/13-dispatcher-mechanical-safety/13-01-SUMMARY.md` must exist (runner_state + priority/urgent live on the DB). Files in this plan are 16-01-owned and free — 16-01-SUMMARY.md exists. No overlap with 15-02/15-03 files (tickets.service.ts, useTickets.ts, TicketDetailDialog.tsx are NOT touched here — that is why queue controls get their own service/hook files).
</sequencing_gate>

<tasks>

<task type="auto">
  <name>Task 1: Typed runner_state service + kill switch + queue-control service (TDD-friendly)</name>
  <files>src/services/admin-dashboard.service.ts, src/services/__tests__/admin-dashboard.service.test.ts, src/services/admin-ticket-controls.service.ts, src/services/__tests__/admin-ticket-controls.service.test.ts, src/hooks/useAdminDashboard.ts, src/hooks/useAdminTicketControls.ts</files>
  <read_first>src/services/admin-dashboard.service.ts (16-01 shipped state — the untyped runner_state read to replace), src/hooks/useAdminDashboard.ts (query-key idiom), supabase/migrations/20260611200000_autopilot_queue_runner_state.sql (live column names/CHECKs — source of truth), src/lib/query-config.ts (queryKeys registry), src/hooks/useTickets.ts (ticket query keys to invalidate — READ ONLY, do not edit; file owned by 15-03)</read_first>
  <action>
In admin-dashboard.service.ts: replace the untyped runner_state read with `getRunnerState()` returning a typed shape `{status: 'idle'|'claiming'|'running'|'awaiting_gate', current_ticket_id: string|null, run_started_at: string|null, last_heartbeat: string|null, last_result: string|null, kill_switch: boolean}` (select the single id=1 row; if the table read errors with relation-missing, preserve the existing graceful `null` → card renders "not deployed yet" — keeps the plan order-tolerant). Add `setKillSwitch(value: boolean)` doing `update({kill_switch: value}).eq('id', 1)` and throwing 'Failed to update kill switch: ' + error.message on error (13-01's trigger guarantees only this column is admin-writable). Add a pure helper `isRunnerOffline(lastHeartbeat: string|null, nowMs: number, staleMs: number)` (stale default 900_000 ms = 3× the 300s poll cadence from autopilot.config) — export it for tests.
Create admin-ticket-controls.service.ts: `updateTicketQueueControls(ticketId: string, patch: {priority?: number; urgent?: boolean})` — pure async UPDATE on tickets returning the updated row; throw on error AND on zero-row result (RLS-blocked non-admin yields 0 rows — surface as 'Failed to update queue controls'). Clamp priority to integer.
Hooks: in useAdminDashboard.ts add `useRunnerState` (queryKey from queryKeys registry, refetchInterval 30s) and `useSetKillSwitch` mutation invalidating it. Create useAdminTicketControls.ts with `useUpdateTicketQueueControls` invalidating the ticket list/detail keys (import key shapes from src/lib/query-config.ts — register new keys there only if the registry pattern requires it; do NOT edit useTickets.ts).
Tests: mock supabase client; cover getRunnerState typed mapping + graceful missing-table null, setKillSwitch success/error, isRunnerOffline boundary (stale vs fresh vs null), updateTicketQueueControls success / zero-row throw / error throw.
  </action>
  <verify>
    <automated>npx vitest run src/services/__tests__/admin-dashboard.service.test.ts src/services/__tests__/admin-ticket-controls.service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - admin-dashboard.service.ts exports getRunnerState, setKillSwitch, isRunnerOffline; no `any`-typed runner_state access remains
    - admin-ticket-controls.service.ts exports updateTicketQueueControls; zero-row update throws
    - useTickets.ts and tickets.service.ts have zero diff (git diff --name-only must not list them)
    - All new/updated service tests pass
  </acceptance_criteria>
  <done>Typed, tested data layer for runner ops + queue controls exists without touching any 15-03-owned file.</done>
</task>

<task type="auto">
  <name>Task 2: Live runner card + kill-switch toggle on DashboardSection</name>
  <files>src/pages/admin/DashboardSection.tsx</files>
  <read_first>src/pages/admin/DashboardSection.tsx (16-01 shipped card layout + "not deployed yet" stub to replace), src/components/ui/ (Switch/AlertDialog availability), .planning/phases/14-in-app-approval-loop/14-CONTEXT.md (scope item 5)</read_first>
  <action>
Replace the runner-card stub branch in DashboardSection.tsx with live rendering from useRunnerState: status label with semantic-token styling (idle=muted, claiming/running=default, awaiting_gate=vibe-orange accent text per existing dashboard idiom), current ticket id rendered as a click-through to the ticket dialog (reuse the Needs-You click-through mechanism already in this file), heartbeat age ("Last heartbeat Xm ago"), and an explicit "RUNNER OFFLINE" state when isRunnerOffline() is true (destructive-token text + RiAlarmWarningLine). Render "not deployed yet" only when getRunnerState returns null (table absent). Kill switch: shadcn Switch labeled "Kill switch" with an AlertDialog confirm before enabling OR disabling (state-changing, ops-critical); wire to useSetKillSwitch; disabled while pending; card is already inside AdminGuard — additionally render the toggle only for isAdmin (existing role hook used by the shell). Remix icons only (RiAlarmWarningLine, RiRobot2Line or existing card icons); no new deps. Keep all other dashboard cards byte-identical.
  </action>
  <verify>
    <automated>npx vitest run src/services/__tests__/admin-dashboard.service.test.ts && npm test 2>&1 | tail -5 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - DashboardSection.tsx no longer hard-renders the stub when runner_state row exists; "not deployed yet" remains only for null reads
    - Kill-switch toggle present, confirm-gated, calls setKillSwitch; visible only to admins
    - "RUNNER OFFLINE" branch renders when heartbeat stale; full suite + build green
    - Execution-time dev-browser check (project hard rule): /admin dashboard shows the live card; toggling kill switch persists across reload (verify against live DB row)
  </acceptance_criteria>
  <done>Runner status and kill switch are operable from /admin; 13's dispatcher halts within one poll cycle of the toggle (verified end-to-end in 13-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → runner_state.kill_switch | UI write to the ops-critical halt control |
| admin browser → tickets.priority/urgent | queue-order control surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Elevation of Privilege | non-admin flipping kill switch / queue order | mitigate | RLS from 13-01 (admin-only SELECT/UPDATE, kill_switch-only trigger); UI gate cosmetic; zero-row update surfaces as error |
| T-14-07 | Tampering | UI writing non-kill_switch runner_state columns | mitigate | service writes ONLY kill_switch; 13-01 BEFORE UPDATE trigger rejects anything else for authenticated callers |
| T-14-08 | Denial of Service | accidental kill-switch enable | mitigate | AlertDialog confirm both directions; state visible on the card |
| T-14-SC | Tampering | package installs | accept | No new packages |
</threat_model>

<verification>
- New service tests + full `npm test` + `npm run build` exit 0
- `git diff --name-only` contains no 15-03-owned file (tickets.service.ts, useTickets.ts, TicketDetailDialog.tsx)
- dev-browser: live card renders runner state; kill-switch flip persists (check runner_state row)
</verification>

<success_criteria>
14-CONTEXT scope items 4 (data layer) and 5 (full UI) delivered: runner observable + stoppable from /admin; queue-control service ready for 14-04's UI mount.
</success_criteria>

<output>
Create `.planning/phases/14-in-app-approval-loop/14-02-SUMMARY.md` when done
</output>
