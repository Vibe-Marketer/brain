---
phase: 14-in-app-approval-loop
plan: 04
type: execute
wave: 2
depends_on: [14-01, 14-02]
external_gate: "DO NOT EXECUTE until BOTH exist: (1) .planning/phases/15-support-capture-fix/15-03-SUMMARY.md — TicketDetailDialog.tsx, tickets.service.ts, useTickets.ts are OWNED by 15-03 until then; (2) .planning/phases/13-dispatcher-mechanical-safety/13-01-SUMMARY.md — priority/urgent columns. If either is missing, STOP and report the gate. Also read 13-04/13-05 SUMMARYs if they exist for the live evidence-message format; if 13's evidence writer has not landed, build to the contract in ~/dev/autopilot/src/lib/db.ts writeAgentMessage."
files_modified:
  - src/components/settings/TicketDetailDialog.tsx
  - src/components/settings/__tests__/TicketDetailDialog.test.tsx
  - src/components/admin/TicketEvidence.tsx
  - src/components/admin/__tests__/TicketEvidence.test.tsx
  - src/services/ticket-approval.service.ts
  - src/services/__tests__/ticket-approval.service.test.ts
  - src/hooks/useTicketApproval.ts
autonomous: true
requirements: [APPR-01, APPR-02]
must_haves:
  truths:
    - "An admin opening an awaiting_approval ticket in /admin sees the fix summary and evidence bundle rendered as structured blocks — diff, test-output tail, codex verdict — not raw message soup (APPR-01)"
    - "Approve and Reject buttons appear on awaiting_approval tickets for admins only; Approve calls the ticket-approval function and the UI reflects the recorded approval; Reject requires a reason and the ticket moves to rejected with the reason in the thread (APPR-02)"
    - "Admins can quick-set priority and toggle URGENT from the ticket detail; reporters never see these controls (14-CONTEXT scope item 4)"
    - "Tickets without agent evidence render exactly as 15-03 shipped them — zero regression for ordinary support tickets"
  artifacts:
    - path: "src/components/admin/TicketEvidence.tsx"
      provides: "Evidence block renderer (diff/test/verdict sections, tail-truncated <details> expanders, RevertCard) ported from dead-branch TicketDetailView and rebound to live agent ticket_messages"
      exports: ["TicketEvidence"]
    - path: "src/services/ticket-approval.service.ts"
      provides: "approveTicket(ticketId) / rejectTicket(ticketId, reason) invoking the ticket-approval Edge Function"
      exports: ["approveTicket", "rejectTicket"]
    - path: "src/components/settings/TicketDetailDialog.tsx"
      provides: "Approve/Reject bar + TicketEvidence mount + priority/urgent controls (admin-gated)"
  key_links:
    - from: "src/components/settings/TicketDetailDialog.tsx"
      to: "src/hooks/useTicketApproval.ts"
      via: "useApproveTicket / useRejectTicket mutations (no inline supabase calls)"
      pattern: "useApproveTicket"
    - from: "src/services/ticket-approval.service.ts"
      to: "supabase/functions/ticket-approval"
      via: "supabase.functions.invoke('ticket-approval')"
      pattern: "ticket-approval"
    - from: "src/components/settings/TicketDetailDialog.tsx"
      to: "src/hooks/useAdminTicketControls.ts"
      via: "useUpdateTicketQueueControls (from 14-02)"
      pattern: "useUpdateTicketQueueControls"
---

<objective>
Deliver the in-app review surface: evidence bundle rendering (APPR-01) and Approve/Reject actions (APPR-02 client half) on the live TicketDetailDialog as mounted in /admin's TicketsSection and Needs-You click-through, plus the admin priority/URGENT controls — porting the dead branch's TicketDetailView evidence/RevertCard rendering rebound to the live vocabulary (agent ticket_messages + approval events), per the dead-branch assessment's KEEP-later list (14-CONTEXT).

Purpose: this is the screen where Andrew's single human step in the autonomous loop happens (13-07's E2E approval click lands here).
Output: TicketEvidence component, approval service/hook, dialog extensions, tests.
</objective>

## Phase Goal

**As an** admin (Andrew), **I want to** read the fix evidence and approve or reject it on the ticket itself, **so that** autonomous fixes ship with one click and rejections leave a recorded reason.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-in-app-approval-loop/14-CONTEXT.md
@.planning/phases/11-ticket-foundation-flag-removal/11-UI-SPEC.md (detail-dialog design contract — extend, never contradict)
@src/CLAUDE.md
</context>

<sequencing_gate>
**HARD EXECUTION GATE (see external_gate frontmatter):** 15-03-SUMMARY.md AND 13-01-SUMMARY.md must exist. TicketDetailDialog.tsx (+ tickets.service.ts/useTickets.ts) are 15-03's files until its SUMMARY lands. Wave 2 within Phase 14: requires 14-01 (deployed function) and 14-02 (queue-control hooks) complete.
</sequencing_gate>

<tasks>

<task type="auto">
  <name>Task 1: Approval service + hooks; TicketEvidence component (port + rebind)</name>
  <files>src/services/ticket-approval.service.ts, src/services/__tests__/ticket-approval.service.test.ts, src/hooks/useTicketApproval.ts, src/components/admin/TicketEvidence.tsx, src/components/admin/__tests__/TicketEvidence.test.tsx</files>
  <read_first>Output of `git show worktree-admin-center:src/components/admin/TicketDetailView.tsx` (the tail() helper, EventBody <details>/<pre> evidence pattern, RevertCard — port these shapes), ~/dev/autopilot/src/lib/db.ts (writeAgentMessage/writeEvent — the live evidence/message format contract), .planning/phases/13-dispatcher-mechanical-safety/13-04-SUMMARY.md and 13-05-SUMMARY.md IF they exist (final evidence-bundle message format), src/services/tickets.service.ts (TicketMessage/TicketEvent types — READ ONLY here), src/services/admin-users.service.ts (functions.invoke error-handling idiom from 16-02)</read_first>
  <action>
Service: ticket-approval.service.ts with `approveTicket(ticketId)` and `rejectTicket(ticketId, reason)` calling `supabase.functions.invoke('ticket-approval', {body: {ticket_id, action, reason?}})`; throw 'Failed to approve ticket: ...' / 'Failed to reject ticket: ...' on invoke error or `{error}` payload (match 14-01's response contract). Hook: useTicketApproval.ts with `useApproveTicket`/`useRejectTicket` mutations that invalidate the ticket detail + list + admin dashboard query keys and toast success/error per the repo hook idiom.
Component: TicketEvidence.tsx takes the ticket's messages (TicketMessage[]) and events (TicketEvent[]); renders for agent-authored messages (author_type==='agent') an "Evidence" group: port the dead branch's `tail(text, 4000)` truncation and `<details><summary>View …</summary><pre class="text-xs bg-muted rounded p-2 overflow-auto max-h-64">` pattern; section-split the message body on the daemon's markdown headers (Diff / Tests / Codex review / Resolution — read the exact header strings from writeAgentMessage / 13 SUMMARYs and pin them in the test fixtures; tolerate unknown shapes by rendering the whole body in one expander — never crash). Port RevertCard rebound: derive branch/fix-SHA from the evidence content and approval/merge events (old branch derivation used dead event payloads — live source is the held-branch name the daemon writes into the evidence message; render the card only when a branch or SHA is present, with CopyButton, `git revert <sha>` hint, RiArrowGoBackLine + vibe-orange icon accent exactly as the branch did). All strings render as React text nodes — no dangerouslySetInnerHTML, no markdown-to-HTML library (no new deps). Remix icons only.
Tests: service success/error paths (mock invoke); TicketEvidence renders diff/test/verdict sections from a fixture evidence message, falls back to single expander on unknown format, renders nothing for tickets with no agent messages, RevertCard appears only with branch/SHA present.
  </action>
  <verify>
    <automated>npx vitest run src/services/__tests__/ticket-approval.service.test.ts src/components/admin/__tests__/TicketEvidence.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ticket-approval.service.ts exports approveTicket/rejectTicket; no direct ticket_events INSERT anywhere in src/ (`grep -rn "ticket_events" src/services/ticket-approval.service.ts` shows zero INSERT usage)
    - TicketEvidence.tsx contains a tail-truncation helper and <details> expanders; renders null for no-evidence tickets
    - dangerouslySetInnerHTML appears nowhere in new files
    - All new tests pass
  </acceptance_criteria>
  <done>Approval data path and evidence renderer exist as standalone, tested units — dialog wiring is Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Dialog wiring — evidence mount, Approve/Reject bar, priority/URGENT controls</name>
  <files>src/components/settings/TicketDetailDialog.tsx, src/components/settings/__tests__/TicketDetailDialog.test.tsx</files>
  <read_first>src/components/settings/TicketDetailDialog.tsx (15-03 shipped state — extend the messages section and header, do not restructure; note the existing admin status Select and 15-03's Attachments group), .planning/phases/11-ticket-foundation-flag-removal/11-UI-SPEC.md (text-xs section idiom), src/components/settings/__tests__/TicketDetailDialog.test.tsx (15-03 test harness — append), src/hooks/useAdminTicketControls.ts (14-02 mutation signature)</read_first>
  <action>
Extend TicketDetailDialog.tsx (admin-gated additions only; the dialog already renders for reporters — every new control gates on the existing admin role check used by the status Select):
1. Mount `<TicketEvidence messages={detail.messages} events={detail.events} />` above the message thread when any agent message exists.
2. Approval bar: when `ticket.status === 'awaiting_approval'` and viewer is admin, render an "APPROVE FIX" primary button and "REJECT" destructive-variant button. Approve → AlertDialog confirm ("Dispatcher will merge and push the held branch on its next cycle") → useApproveTicket; on success show a persistent inline "Approval recorded — dispatcher merges on next poll (≤5 min)" note (status stays awaiting_approval until the dispatcher advances it — set expectations in the UI, no fake optimistic 'resolved'). Reject → dialog with required reason Textarea (1–2000 chars, disabled submit until valid) → useRejectTicket; on success the existing query invalidation re-renders the rejected status.
3. Queue controls: in the header meta row, admin-only "Priority" numeric quick-set (presets 0/1/2/3 + current value display) and an "URGENT" toggle (Switch with RiAlarmWarningLine, vibe-orange when on — urgency is a structural accent use), both wired to useUpdateTicketQueueControls with optimistic-free invalidation (simple, matches repo idiom).
Keep reporter-facing rendering byte-equivalent for non-admin users and for tickets without evidence (15-03 regression truth). Append tests: approval bar renders only for admin + awaiting_approval; reject disabled without reason; approve fires mutation after confirm; priority/urgent controls admin-only; no-evidence ticket renders as before (snapshot or DOM assertions consistent with the existing harness).
  </action>
  <verify>
    <automated>npx vitest run src/components/settings/__tests__/TicketDetailDialog.test.tsx && npm test 2>&1 | tail -5 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Approve/Reject render only when status==='awaiting_approval' AND viewer is admin; reject requires reason text
    - Approve does NOT locally set status resolved/approved — it shows the "dispatcher merges next poll" note
    - Priority quick-set + URGENT toggle present, admin-gated, calling the 14-02 hook
    - Full suite + build green; non-admin/no-evidence rendering unchanged
    - Execution-time dev-browser check (project hard rule): as admin open an awaiting_approval fixture ticket in /admin → evidence blocks render, reject with reason works end-to-end (live function), approve path verified on a disposable fixture ticket created for the check and cleaned up after
  </acceptance_criteria>
  <done>Andrew's approval click exists where the Needs-You queue lands; APPR-01 and the APPR-02 client half are live in /admin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-authored evidence text → admin DOM | hostile ticket/repo content can flow into evidence messages |
| admin click → merge-triggering event | the highest-impact action in the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-12 | Injection (XSS) | evidence body rendering | mitigate | React text nodes + <pre> only; no dangerouslySetInnerHTML; no HTML/markdown rendering of agent content |
| T-14-13 | Elevation of Privilege | reporter seeing/using approval or queue controls | mitigate | Admin-gated render; server is the control (14-01 403; RLS on priority/urgent) — UI gate cosmetic |
| T-14-14 | Tampering | accidental approve | mitigate | AlertDialog confirm with explicit merge consequence text |
| T-14-15 | Spoofing | UI writing approval events directly | mitigate | Client has no ticket_events INSERT path (service-role-only RLS); only functions.invoke |
| T-14-SC | Tampering | package installs | accept | No new packages |
</threat_model>

<verification>
- All new vitest targets + full `npm test` + `npm run build` exit 0
- `grep -rn "dangerouslySetInnerHTML" src/components/admin/TicketEvidence.tsx src/components/settings/TicketDetailDialog.tsx` → 0 matches
- dev-browser end-to-end in /admin per Task 2 acceptance criteria
</verification>

<success_criteria>
APPR-01 fully satisfied and APPR-02's client half wired to 14-01's live function: evidence readable, approve/reject one click each, queue controls live — the in-app approval loop is complete pending 13-07's full-loop E2E proof.
</success_criteria>

<output>
Create `.planning/phases/14-in-app-approval-loop/14-04-SUMMARY.md` when done
</output>
