---
phase: 38-access-policy-share-link-key-migration-request-flow
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/access-policy.integration.test.ts
  - src/test/rls-regression.test.ts
  - src/services/__tests__/data-movement.dedup.integration.test.ts
  - src/test/migrations/phase38-access-migrations.test.ts
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-03, ACCESS-04, ACCESS-05, ACCESS-08, ACCESS-09, EVT-06]
must_haves:
  truths:
    - "The six access levels, default snapshot, custom/reset behavior, and owner-only mutations have a real-database contract for D-01 through D-06."
    - "Existence discovery and request creation prove D-07 through D-11 server-side, including invitee/org denial, verified evidence, provider webinar suppression, and the exact 49/50 boundary."
    - "Request, approval, denial, 30-day cooldown, revocation, notifications, and immutable audit history have a real-database contract for D-12 through D-17."
    - "All three current copy/routing signatures preserve exact event_id and apply the destination owner's current default."
  artifacts:
    - path: "src/test/access-policy.integration.test.ts"
      provides: "Real-DB policy, discovery, lifecycle, and authorization matrix"
    - path: "src/test/rls-regression.test.ts"
      provides: "Cross-org and direct-write-denial coverage for new access tables"
    - path: "src/services/__tests__/data-movement.dedup.integration.test.ts"
      provides: "All-signature event_id and destination-policy copy tests"
    - path: "src/test/migrations/phase38-access-migrations.test.ts"
      provides: "Static additive/no-drop/security-definer migration gates"
  key_links:
    - from: "src/test/access-policy.integration.test.ts"
      to: "Phase 38 RPCs"
      via: "signed-in clients on the dedicated test Supabase project"
      pattern: "set_recording_access_level|list_discoverable_recording_copies|request_recording_access"
    - from: "src/services/__tests__/data-movement.dedup.integration.test.ts"
      to: "copy_recording_to_org|copy_recording_to_organization|route_recording_cross_org"
      via: "real RPC calls"
      pattern: "event_id"
---

<objective>
Create the failing real-database and static acceptance suites that define Phase 38 authorization behavior before implementation.

Purpose: Make privacy, RLS continuity, lifecycle idempotency, and EVT-06 executable constraints rather than review-only prose.
Output: RED integration suites and migration guards covering ACCESS-01 through ACCESS-05, ACCESS-08/09, and EVT-06.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-CONTEXT.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-RESEARCH.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-VALIDATION.md
@src/test/integration-setup.ts
@src/test/rls-regression.test.ts
@src/services/__tests__/data-movement.dedup.integration.test.ts
@supabase/migrations/20260730160000_fix_cross_org_copy_dedup.sql
</context>

## Artifacts this phase produces

- `src/test/access-policy.integration.test.ts`: `ACCESS_LEVELS` matrix; policy, discovery, event-safe metadata, lifecycle, idempotency, and cooldown suites.
- `src/test/rls-regression.test.ts`: access-table isolation and client-deny inventory.
- `src/services/__tests__/data-movement.dedup.integration.test.ts`: three-function `event_id`/independent-policy cases.
- `src/test/migrations/phase38-access-migrations.test.ts`: additive migration and security-definition source gates.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define policy, discovery, and lifecycle behavior against real Supabase</name>
  <read_first>
    - src/test/access-policy.integration.test.ts (create; no prior file)
    - src/test/integration-setup.ts
    - src/test/phase38-fixtures.ts (created by Plan 01; if unavailable, define imports now and leave the suite RED)
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-CONTEXT.md
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-RESEARCH.md
    - supabase/migrations/20260831000001_create_events_and_extend_participants.sql
    - supabase/migrations/20260905140000_create_identities_and_link_tables.sql
  </read_first>
  <files>src/test/access-policy.integration.test.ts</files>
  <behavior>
    - D-01..D-06: private default, all six values, future-only default changes, explicit custom state, reset snapshot, owner-only changes.
    - D-07..D-11: confirmed verified participant gets anonymous copies; invitee-only/org-only/unrelated callers get none; payload omits protected fields; Zoom webinar types 5/6/9 and confirmed count 50 return none while 49 is allowed; direct share remains valid.
    - D-12..D-17: one pending request/owner notification/outbox item under retries; owner-only approve/deny/revoke; approval lasts until revoked; audit append-only; denial reason remains private and cooldown is exactly 30 days.
  </behavior>
  <action>
    Author one real-DB suite using service role only for setup/inspection and signed-in user clients for every behavior assertion. Exercise owner, admin, workspace/team, coach, verified confirmed attendee, verified invitee-only, org-only, active/revoked grant, token recipient, unrelated, and anonymous identities across all six policies. Assert event membership alone never reveals recording content. Verify `get_event_existence_for_participant` and discovery result keys by exact allowlist, and verify request RPC independently revalidates evidence, webinar status, and `< 50` confirmed identities.
  </action>
  <acceptance_criteria>
    - Tests enumerate all six policy values and every required actor class.
    - Discovery result assertions reject `owner`, `provider`, `title`, `transcript`, `summary`, source IDs, raw evidence, organization, workspace, URL, duration, and thumbnail fields.
    - Cooldown tests assert request permitted immediately before denial, denied through `< denied_at + interval '30 days'`, and eligible at/after the boundary using controlled timestamps.
    - Direct insert/update/delete attempts against lifecycle/audit/notification objects fail for authenticated clients.
  </acceptance_criteria>
  <verify>
    <automated>VITEST_INTEGRATION_OK=true npx vitest run --config vitest.integration.config.ts src/test/access-policy.integration.test.ts</automated>
  </verify>
  <done>The suite collects successfully and fails only on the intentionally absent Phase 38 schema/RPC behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend RLS and copy/routing regression matrices</name>
  <read_first>
    - src/test/rls-regression.test.ts
    - src/services/__tests__/data-movement.dedup.integration.test.ts
    - src/test/integration-setup.ts
    - src/test/phase38-fixtures.ts (created by Plan 01)
    - src/services/data-movement.service.ts
    - supabase/migrations/20260730160000_fix_cross_org_copy_dedup.sql
    - supabase/migrations/20260831020000_fix_events_participation_rls_and_updated_at_trigger.sql
  </read_first>
  <files>src/test/rls-regression.test.ts, src/services/__tests__/data-movement.dedup.integration.test.ts</files>
  <behavior>
    - New user-readable tables are cross-org isolated; audit/outbox and lifecycle writes are client-denied.
    - Existing owner, org-admin, workspace/team, coach, and share-token access keeps the same outcome under Private.
    - `copy_recording_to_org(UUID,UUID,UUID,BOOLEAN)`, `copy_recording_to_organization(UUID,UUID)`, and `route_recording_cross_org(UUID,UUID,UUID,BOOLEAN,UUID)` preserve non-null and null `event_id`.
    - New copies snapshot the destination owner's current default and remain independently editable.
  </behavior>
  <action>
    Add request/grant tables to the appropriate RLS inventory, audit/outbox to client-deny, and bespoke policy-continuity checks that do not rely on service-role reads. Extend the current dedup suite to invoke all three latest signatures, including retry/dedup behavior, null event IDs, and distinct source/destination policy state. Do not mock Supabase.
  </action>
  <acceptance_criteria>
    - Any leak failure includes table, actor, and expected boundary in its message.
    - All three function signatures are invoked, not inferred from source text.
    - The test proves an existing dedup target does not have its event association overwritten incorrectly.
  </acceptance_criteria>
  <verify>
    <automated>VITEST_INTEGRATION_OK=true npx vitest run --config vitest.integration.config.ts src/test/rls-regression.test.ts src/services/__tests__/data-movement.dedup.integration.test.ts</automated>
  </verify>
  <done>RLS continuity and all current copy/routing signatures are executable regression gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add additive migration and function-hardening source gates</name>
  <read_first>
    - src/test/migrations/phase38-access-migrations.test.ts (create; no prior file)
    - supabase/migrations/20260730160000_fix_cross_org_copy_dedup.sql
    - supabase/migrations/20260908130001_create_org_identity_self_serve_rpcs.sql
    - supabase/migrations/20260910010000_create_reconcile_transcript_segments_atomic_rpc.sql
    - supabase/CLAUDE.md
  </read_first>
  <files>src/test/migrations/phase38-access-migrations.test.ts</files>
  <behavior>
    - No Phase 38 migration drops or renames `call_recording_id`, regenerates share tokens, deletes share rows/logs, or replaces existing access paths destructively.
    - Every security-definer function pins an empty search path, schema-qualifies objects, revokes PUBLIC/anon execution, and grants only the intended role.
    - Latest bodies for all three copy/routing functions include `event_id` and `v_source.event_id`.
  </behavior>
  <action>
    Create focused source-artifact tests that read only the four expected Phase 38 migration files and validate additive DDL, legacy-key preservation, security-definer hardening, notification INSERT tightening, and exact copy signature coverage. Filter comments before negative grep-style assertions so prose cannot self-invalidate the gate.
  </action>
  <acceptance_criteria>
    - The test fails when `DROP COLUMN call_recording_id`, share-token replacement, or an unpinned security-definer body appears.
    - It requires `event_id` in all three current function bodies.
    - Before migrations exist, failures identify the expected filenames rather than passing vacuously.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/test/migrations/phase38-access-migrations.test.ts</automated>
  </verify>
  <done>Risky migration regressions are blocked before any database apply.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: recording content, participant identities, request evidence, audit history, existing authorization paths, and event associations.
- Boundaries: authenticated JWT to RLS/RPC; event metadata to recording content; source organization to destination copy; client mutation to append-only audit.

| Threat ID | Category | Severity | Threat | Mitigation | Verification |
|---|---|---:|---|---|---|
| T-38-02-01 | Elevation of privilege | HIGH | Event participant or invitee reads content | Signed-in matrix proves discovery and content predicates are separate | Real-DB content denial tests |
| T-38-02-02 | Information disclosure | HIGH | Anonymous row leaks owner/content metadata | Exact RPC-key allowlist plus forbidden-key assertions | RPC/network payload tests |
| T-38-02-03 | Tampering | HIGH | Client forges request/grant/audit/notification state | Direct-write denial matrix; lifecycle only through owner/requester RPCs | RLS regression |
| T-38-02-04 | Information disclosure | HIGH | 50-person/webinar event reveals copies or roster | Test 49/50 and Zoom types 5/6/9 in both discovery and request | Real-DB cutoff matrix |
| T-38-02-05 | Tampering | HIGH | Cross-org copy drops or overwrites event association | Invoke all current functions with null/non-null and retry cases | Data-movement integration suite |
| T-38-02-SC | Tampering | LOW | Test dependency changes alter results | No package changes in this plan | Lockfile diff gate |
</threat_model>

<verification>
Run all four targeted suites. RED failures must correspond to not-yet-created Phase 38 schema/RPC symbols; any environment, auth, fixture, syntax, or cleanup failure must be fixed now.
</verification>

<success_criteria>
- Every database requirement has a real-DB or static failing test before implementation.
- High-severity access and disclosure threats are explicitly exercised.
- All three copy/routing functions are covered by invocation.
</success_criteria>

<output>
Create `.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-02-SUMMARY.md` when done.
</output>
