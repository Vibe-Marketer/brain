---
phase: 38-access-policy-share-link-key-migration-request-flow
plan: "06"
type: execute
wave: 4
depends_on: ["38-04", "38-05"]
files_modified:
  - src/types/supabase.ts
  - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-SCHEMA-EVIDENCE.md
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-03, ACCESS-04, ACCESS-05, ACCESS-07, ACCESS-08, ACCESS-09, EVT-06]
must_haves:
  truths:
    - "The exact four reviewed Phase 38 migrations are applied to dedicated test project swjzxiddcrtaqixsfaac before any production change."
    - "Catalog introspection proves columns, constraints, indexes, RLS/FORCE flags, policies, function security/search paths, and grants on the test project."
    - "Real-DB policy, discovery, lifecycle, share bridge, RLS, and all copy/routing tests pass on the applied schema."
    - "Generated types contain only the reviewed Phase 38 schema delta and the CLI is relinked to production without applying anything there."
  artifacts:
    - path: "src/types/supabase.ts"
      provides: "Generated Phase 38 database types from the proven test schema"
    - path: ".planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-SCHEMA-EVIDENCE.md"
      provides: "Test-ref, migration pending/apply/history, introspection, test, type-diff, and relink evidence"
  key_links:
    - from: "four Phase 38 migrations"
      to: "swjzxiddcrtaqixsfaac"
      via: "ref-guarded dry-run then db push"
      pattern: "2026091900000[1-4]"
    - from: "swjzxiddcrtaqixsfaac schema"
      to: "src/types/supabase.ts"
      via: "supabase gen types typescript --linked"
      pattern: "recording_access_requests|default_recording_access_level|call_share_links"
---

<objective>
Apply and prove the additive schema on the dedicated test Supabase project, then regenerate typed contracts.

Purpose: This is the mandatory blocking gate between authored SQL and all application/production work.
Output: A green real-database schema, generated types, and an evidence record that production remained untouched.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/V2.2-COMPLETION-PLAN.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-VALIDATION.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-01-SUMMARY.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-02-SUMMARY.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-04-SUMMARY.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-05-SUMMARY.md
@supabase/CLAUDE.md
</context>

## Artifacts this phase produces

- Generated Supabase `Database` types for the access fields/tables/RPCs and UUID share bridge.
- `38-TEST-SCHEMA-EVIDENCE.md` with exact target/pending set, before/after history, SQL catalog results, suite output, type diff classification, and final relink state.

<tasks>

<task type="auto" gate="blocking">
  <name>[BLOCKING] Task 1: Apply only the reviewed migrations to the dedicated test project</name>
  <read_first>
    - supabase/migrations/20260919000001_phase38_access_policy_schema.sql
    - supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql
    - supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql
    - supabase/migrations/20260919000004_phase38_copy_event_preservation.sql
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-SCHEMA-EVIDENCE.md (create; no prior file)
    - .planning/V2.2-COMPLETION-PLAN.md
    - supabase/CLAUDE.md
  </read_first>
  <files>.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-SCHEMA-EVIDENCE.md</files>
  <action>
    Record current branch and git SHA. Link the Supabase CLI to `swjzxiddcrtaqixsfaac`, then independently verify `supabase/.temp/project-ref` and the active marker from `supabase projects list`; STOP if either is not the test ref or if any URL resolves to `vltmrnjsubfzrgrtdqey`. Capture `supabase migration list --linked` and a dry run. The pending set must be exactly `20260919000001` through `20260919000004` in order; STOP on any extra/missing migration or drift/repair prompt. Apply with `supabase db push --linked`, capture output, immediately re-run history, and record all evidence without secrets. Do not apply or deploy anything to production in this task.
  </action>
  <acceptance_criteria>
    - Evidence names branch, SHA, test ref, before history, exact four-item pending set, dry-run output, apply result, and after history.
    - Both independent target checks show `swjzxiddcrtaqixsfaac` before apply.
    - All four versions show Local=Remote afterward and no unrelated version was repaired or pushed.
    - Any target/pending/history discrepancy causes a hard stop before `db push`.
  </acceptance_criteria>
  <verify>
    <automated>test "$(cat supabase/.temp/project-ref)" = "swjzxiddcrtaqixsfaac" &amp;&amp; supabase migration list --linked</automated>
  </verify>
  <done>The dedicated test project contains exactly the reviewed Phase 38 schema and production remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Introspect security/compatibility shape and run the real-DB suites</name>
  <read_first>
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-SCHEMA-EVIDENCE.md
    - src/test/access-policy.integration.test.ts
    - src/test/fixtures/phase38-provider-event-kind.ts
    - src/test/rls-regression.test.ts
    - src/services/__tests__/data-movement.dedup.integration.test.ts
    - supabase/functions/share-call/__tests__/share-call.integration.test.ts
    - src/test/migrations/phase38-access-migrations.test.ts
    - src/test/integration-setup.ts
  </read_first>
  <files>.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-SCHEMA-EVIDENCE.md</files>
  <action>
    Query test `pg_catalog`/`information_schema` with the guarded test database URL. Record policy/share column types and nullability; lifecycle constraints/indexes/FKs; RLS and FORCE flags; policy definitions; `prosecdef`, `proconfig`, and ACLs for every new/replaced function; notification INSERT policy/grants; and all three copy function definitions. Run the Phase 38 migration source test and repository-supported serial real-DB policy/RLS/data-movement suites. Require the complete provider/event-kind fixture matrix, explicit-webinar-wins aggregation, neutral-unknown passage cases, verified-participant denial cases, and independent 49/50 cases to pass. Inspect the legacy share fixture after migration to prove identical ID/token/status/recipient/log and the correct UUID backfill. Record unresolved/ambiguous counts without altering them.
  </action>
  <acceptance_criteria>
    - Every new exposed table has RLS enabled; audit/outbox client access is absent; intended RPC grants exclude PUBLIC/anon.
    - SECURITY DEFINER functions show empty pinned search path and schema-qualified bodies.
    - Old-token row identity/log linkage is unchanged and UUID-only recording operations are schema-valid.
    - Access policy, RLS, and all three copy/routing real-DB suites pass with zero skipped-by-environment cases.
    - Exact Zoom mapping, every non-Zoom/internal unknown case, webinar-positive suppression, neutral-unknown passage, participation denials, and 49/50 boundary pass without skipped cases.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/test/migrations/phase38-access-migrations.test.ts src/test/rls-regression.test.ts &amp;&amp; npm run test:integration</automated>
  </verify>
  <done>The applied test schema is behaviorally and catalog-level proven, including old data compatibility.</done>
</task>

<task type="auto">
  <name>Task 3: Generate types from test, review the delta, and restore the production link</name>
  <read_first>
    - src/types/supabase.ts
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-SCHEMA-EVIDENCE.md
    - package.json
    - scripts/type-check.mjs
    - supabase/CLAUDE.md
  </read_first>
  <files>src/types/supabase.ts, .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-SCHEMA-EVIDENCE.md</files>
  <action>
    Generate types while still linked to the proven test project, strip any CLI update banner outside the generated object, and inspect the complete diff. Accept only the four migration's columns/tables/functions and expected replaced function signatures; STOP on unrelated drift. Run the repository type check. Then relink the CLI to production ref `vltmrnjsubfzrgrtdqey` without running db push, verify the temp ref and active project marker, and run production migration list read-only to prove the four Phase 38 migrations remain pending there. Record the final relink and pending evidence.
  </action>
  <acceptance_criteria>
    - Generated types contain all Phase 38 fields/tables/RPCs and no unrelated schema delta.
    - `npm run type-check` reports zero new errors under the repository baseline.
    - Final CLI ref is `vltmrnjsubfzrgrtdqey`; production history shows the four Phase 38 versions local-only/pending.
    - No production migration or Edge Function deployment occurs.
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check &amp;&amp; test "$(cat supabase/.temp/project-ref)" = "vltmrnjsubfzrgrtdqey" &amp;&amp; supabase migration list --linked</automated>
  </verify>
  <done>Typed code can target the proven schema and the CLI is safely restored to production with production still untouched.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: production database, migration history, generated schema contract, test customer isolation, function grants.
- Boundaries: local CLI link to remote project; migration files to remote DDL; test schema to generated types.

| Threat ID | Category | Severity | Threat | Mitigation | Verification |
|---|---|---:|---|---|---|
| T-38-06-01 | Tampering | CRITICAL | Migration applied to production by mistake | Two independent test-ref checks and hard prod-ref rejection before push | Evidence + shell gate |
| T-38-06-02 | Tampering | HIGH | Unexpected pending migration is included | Exact four-version dry-run allowlist; stop on mismatch | Before evidence |
| T-38-06-03 | Elevation of privilege | HIGH | RLS/function grants differ from source intent | Live catalog introspection plus signed-in RLS tests | Recorded SQL output/tests |
| T-38-06-04 | Tampering | HIGH | Generated types include test/prod drift | Complete diff allowlist; stop on unrelated delta | Evidence classification + type check |
| T-38-06-SC | Tampering | LOW | CLI/package upgrade changes behavior | Use installed versions; no install/upgrade | Version record and lockfile diff |
</threat_model>

<verification>
This plan is blocking. Do not proceed to any application or production plan unless target guards, exact migration apply, catalog introspection, real-DB tests, type diff, and final relink all pass.
</verification>

<success_criteria>
- The schema is proven on `swjzxiddcrtaqixsfaac`.
- Production remains pending and untouched.
- Generated types and security catalog match the plan.
</success_criteria>

<output>
Create `.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-06-SUMMARY.md` when done and include the evidence-file link.
</output>
