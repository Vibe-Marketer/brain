---
phase: 38-access-policy-share-link-key-migration-request-flow
plan: "16"
type: execute
wave: 9
depends_on: ["38-15"]
files_modified:
  - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-03, ACCESS-04, ACCESS-05, ACCESS-06, ACCESS-07, ACCESS-08, ACCESS-09, EVT-06]
must_haves:
  truths:
    - "[BLOCKING] Production mutation occurs only for target ref vltmrnjsubfzrgrtdqey, the exact verified git SHA, and the exact four pending Phase 38 additive migrations."
    - "Existing production share tokens continue resolving, UUID-native links resolve and revoke, authenticated policy behavior remains correct, and legacy key columns/data/logs are retained."
    - "Production catalog/data checks show no orphan, duplicate-active, unresolved-backfill, RLS, function-grant, or event_id regression before the rollout is called complete."
    - "Only Supabase migrations and the four required Edge Functions are deployed; `main` is not pushed and the production frontend is not deployed."
  artifacts:
    - path: ".planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md"
      provides: "Redacted preflight, apply/deploy, probe, stop, and rollback evidence"
  key_links:
    - from: "38-PREPRODUCTION-VERIFICATION.md"
      to: "production ref vltmrnjsubfzrgrtdqey"
      via: "matching immutable SHA and PASS marker"
      pattern: "PRODUCTION-GATE: PASS"
    - from: "Phase 38 migrations"
      to: "production schema"
      via: "exact pending-set validation followed by linked db push"
      pattern: "2026091900000[1-4]"
    - from: "share-call/mcp-server/public-recording/recording-access"
      to: "production Edge runtime"
      via: "Supabase CLI API deployment and live probes"
      pattern: "supabase functions deploy"
---

<objective>
Apply the authorized additive Phase 38 Supabase changes to production under an evidence-gated, fail-closed rollout without releasing the feature-branch frontend.

Purpose: Make the required schema and server behavior available safely while preserving all legacy access paths and a practical rollback route.
Output: Production migrations/functions applied and a redacted evidence record proving target, change set, compatibility, authorization, integrity, and rollback posture.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/V2.2-COMPLETION-PLAN.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PREPRODUCTION-VERIFICATION.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-15-SUMMARY.md
@supabase/migrations/20260919000001_phase38_access_policy_schema.sql
@supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql
@supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql
@supabase/migrations/20260919000004_phase38_copy_event_preservation.sql
@supabase/functions/share-call/index.ts
@supabase/functions/mcp-server/index.ts
@supabase/functions/public-recording/index.ts
@supabase/functions/recording-access/index.ts
@supabase/CLAUDE.md
@AGENTS.md
</context>

## Artifacts this phase produces

- `38-PRODUCTION-DEPLOYMENT-EVIDENCE.md` with target-ref proof, git SHA, exact pre/post migration history and pending set, SQL introspection, redacted token/auth probes, integrity counts, Edge deployment IDs/timestamps, PASS/STOP outcome, and rollback actions.
- Production application of `20260919000001` through `20260919000004` only.
- Production deployments of `share-call`, `mcp-server`, `public-recording`, and `recording-access` only.

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Prove production target, immutable source, exact pending set, safe data state, and controlled probes</name>
  <read_first>
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PREPRODUCTION-VERIFICATION.md
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-15-SUMMARY.md
    - .planning/V2.2-COMPLETION-PLAN.md
    - supabase/config.toml
    - supabase/.temp/project-ref
    - supabase/migrations/20260919000001_phase38_access_policy_schema.sql
    - supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql
    - supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql
    - supabase/migrations/20260919000004_phase38_copy_event_preservation.sql
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md (create; no prior file)
  </read_first>
  <files>.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md</files>
  <action>
    Stop before mutation unless the preproduction report says PASS, its SHA equals `git rev-parse HEAD`, branch is `v2.2-event-resolution`, worktree/index are clean apart from this evidence file, and `npm run build` succeeds from that committed tree. Independently verify the linked project file, Supabase project listing, and production database host/ref all identify `vltmrnjsubfzrgrtdqey`; reject test ref `swjzxiddcrtaqixsfaac` or any mismatch. Record the existing deployed function versions and prior git SHA for rollback. Fetch and record exact `Origin main before: &lt;sha&gt;` and current production frontend deployment ID/URL before any Supabase action. Compare local/remote migration history and require the pending set to be exactly `20260919000001`, `...000002`, `...000003`, `...000004` with no earlier/unexpected migration. In read-only SQL, inspect legacy share-key ambiguity, share rows with neither key, orphan/duplicate active requests or grants, current RLS/policies/function grants, event_id population, and owner-scoped bridge feasibility; every unsafe count must be zero. Select a designated noncustomer production canary only by the documented `source_metadata.integration_test = phase-38-production-canary` marker. Select one active legacy token for a read-only before/after compatibility probe, retaining it only in process and recording only a fingerprint/result class. If either controlled canary or safe legacy probe is unavailable, write STOP and do not substitute arbitrary customer data.
  </action>
  <acceptance_criteria>
    - Evidence records three independent production-ref confirmations, branch, SHA, clean status, build exit, prior function versions/SHA, migration history, and exact pending set.
    - SQL is executed in read-only mode and records only aggregate counts/catalog definitions; no customer content, email, title, transcript, summary, full token, or secret is written.
    - Ambiguous bridge, no-key row, orphan, duplicate-active, unresolved owner match, and unexpected migration counts are zero.
    - A marked noncustomer canary and redacted old-token probe exist; absence or mismatch produces STOP before any `db push` or function deploy.
  </acceptance_criteria>
  <verify>
    <automated>test "$(cat supabase/.temp/project-ref)" = "vltmrnjsubfzrgrtdqey" &amp;&amp; test "$(git branch --show-current)" = "v2.2-event-resolution" &amp;&amp; git diff --quiet -- . ':(exclude).planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md' &amp;&amp; git diff --cached --quiet &amp;&amp; test -z "$(git ls-files --others --exclude-standard | grep -v '^.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md$')" &amp;&amp; test "$(git rev-parse HEAD)" = "$(sed -n 's/^Verified git SHA: //p' .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PREPRODUCTION-VERIFICATION.md)" &amp;&amp; rg -n "PRODUCTION-GATE: PASS" .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PREPRODUCTION-VERIFICATION.md</automated>
  </verify>
  <done>The production target and change set are proven safe and controlled probes/rollback inputs exist, or rollout has stopped before mutation.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply only the four additive migrations and deploy only required Edge Functions</name>
  <read_first>
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md
    - supabase/migrations/20260919000001_phase38_access_policy_schema.sql
    - supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql
    - supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql
    - supabase/migrations/20260919000004_phase38_copy_event_preservation.sql
    - supabase/functions/share-call/index.ts
    - supabase/functions/mcp-server/index.ts
    - supabase/functions/public-recording/index.ts
    - supabase/functions/recording-access/index.ts
    - supabase/config.toml
  </read_first>
  <files>.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md</files>
  <action>
    Recheck target ref, SHA, clean tree, exact pending set, and preflight PASS immediately before mutation. Apply through the linked production project with `supabase db push --linked`, requiring the CLI preview/applied set to contain only the four Phase 38 filenames. Stop after the first failed migration and do not edit remote history. Deploy `share-call`, `mcp-server`, `public-recording`, and `recording-access` individually with `supabase functions deploy &lt;name&gt; --project-ref vltmrnjsubfzrgrtdqey --use-api` plus the repo's configured JWT verification flags; stop on any failure. Record sanitized CLI output, timestamps, and deployment identifiers. Do not run `git push`, do not checkout/merge `main`, and do not invoke Vercel/frontend deployment.
  </action>
  <acceptance_criteria>
    - Production migration history advances by exactly the four reviewed additive migrations in filename order.
    - No legacy key column, share row, token, access log, or function is destructively removed.
    - Exactly the four named Edge Functions are deployed from the verified SHA with their configured auth mode.
    - Git remote refs and the Vercel production frontend deployment remain unchanged by this task.
    - Any command failure yields STOP and enters Task 3 diagnosis/rollback without retrying destructive steps.
  </acceptance_criteria>
  <verify>
    <automated>supabase migration list --linked &amp;&amp; supabase functions list --project-ref vltmrnjsubfzrgrtdqey</automated>
  </verify>
  <done>The authorized additive database and server-only production changes are applied with an auditable record and no frontend release.</done>
</task>

<task type="auto">
  <name>Task 3: Prove live compatibility, authorization, integrity, and execute stop or rollback rules</name>
  <read_first>
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md
    - supabase/functions/share-call/index.ts
    - supabase/functions/mcp-server/index.ts
    - supabase/functions/public-recording/index.ts
    - supabase/functions/recording-access/index.ts
    - tests/integration/share-call.integration.test.ts
    - tests/integration/phase38-share-link-bridge.integration.test.ts
    - tests/integration/phase38-access-policy.integration.test.ts
  </read_first>
  <files>.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md</files>
  <action>
    Re-read remote migration history and introspect policy columns/default trigger, share-key check/FKs/indexes, RLS policies, RPC signatures, function grants, and all three copy definitions. Re-run read-only zero-count orphan, duplicate-active, no-key, unresolved bridge, and event_id-loss checks. Probe the preflight legacy token and require the same successful result class. Using only the marked noncustomer canary, create a UUID-native share link, resolve it anonymously, confirm an unauthenticated protected operation is rejected, then revoke/delete the temporary link and prove it no longer resolves. Probe authenticated owner/team/coach/admin paths, private public-endpoint denial, allowlisted public-canary response, participant eligibility plus webinar/50 suppression, request/approve/revoke/deny cooldown, email-outbox creation, and MCP markdown shape without storing payload content in evidence. Fetch again and record exact `Origin main after: &lt;sha&gt;` plus the production frontend deployment ID/URL; both must equal the preflight values. On function regression, redeploy the recorded pre-phase compatible `share-call`/`mcp-server` source from the prior SHA; leave additive schema/legacy columns intact and fail closed for new endpoints. On DB/data invariant failure, stop traffic to the affected new path and prepare a forward corrective migration; never drop the new schema or delete customer rows as rollback. End with `PRODUCTION-SERVER-GATE: PASS` only when every probe is green; otherwise record STOP, exact failed assertion, containment, and rollback/forward-fix state.
  </action>
  <acceptance_criteria>
    - Remote history/catalog exactly match reviewed migrations and expected RLS/function grants.
    - Old token behavior is unchanged; UUID-native create/resolve/revoke succeeds on the controlled canary; rejected auth returns generic denial.
    - Owner/team/coach/admin and share-token access remain unchanged, D-01 private existing behavior remains intact, and D-02..D-22 new server behaviors pass their probes.
    - Orphan, duplicate, no-key, unresolved bridge, and event_id-loss counts remain zero after probes.
    - Evidence contains no full token, secret, email, title, transcript, summary, or customer content and explicitly confirms no `main` push/frontend deploy.
  </acceptance_criteria>
  <verify>
    <automated>e=.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md; rg -n "PRODUCTION-SERVER-GATE: PASS" "$e" &amp;&amp; test "$(sed -n 's/^Origin main before: //p' "$e")" = "$(sed -n 's/^Origin main after: //p' "$e")" &amp;&amp; test "$(sed -n 's/^Production frontend before: //p' "$e")" = "$(sed -n 's/^Production frontend after: //p' "$e")"</automated>
  </verify>
  <done>Production server state is proven compatible and secure, or the affected new path is contained with a documented rollback/forward-fix status.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: production recordings, legacy/new share tokens, access policies/grants/requests, participant evidence, migration/function state, frontend release boundary.
- Boundaries: local CLI to Supabase management API; migration SQL to production Postgres; anonymous/authenticated clients to Edge Functions/RPCs; evidence writer to secrets/customer data.

| Threat ID | Category | Severity | Threat | Mitigation | Verification |
|---|---|---:|---|---|---|
| T-38-16-01 | Tampering | CRITICAL | Apply to wrong project or unexpected migration set | Three-way target proof, exact pending-set gate, immutable SHA, immediate recheck | Preflight evidence and remote history |
| T-38-16-02 | Information disclosure | CRITICAL | New public/discovery route leaks private content | Allowlist, server auth, webinar/50 suppression, live forbidden-field/auth probes | Redacted live probes |
| T-38-16-03 | Elevation of privilege | CRITICAL | RLS/RPC/function grant error broadens access | Catalog introspection plus owner/team/coach/admin/anonymous probes | RLS/grant query and live matrix |
| T-38-16-04 | Tampering | HIGH | UUID bridge corrupts or strands legacy links/logs | Additive dual-key schema, ambiguity hard stop, old-token before/after, zero-count integrity queries | Token and SQL evidence |
| T-38-16-05 | Repudiation | HIGH | Remote mutation cannot be tied to reviewed code | SHA, timestamps, CLI/deployment IDs, exact filenames | Evidence audit |
| T-38-16-06 | Denial of service | HIGH | Function rollout breaks existing share/MCP paths | Sequential deploy/probe, prior SHA recorded, compatible function redeploy rollback | Live legacy share and MCP probes |
| T-38-16-07 | Information disclosure | HIGH | Evidence stores secrets or PII | Fingerprints/result classes/aggregate counts only | Secret/PII review before commit |
| T-38-16-SC | Tampering | LOW | Package supply-chain change during deploy | No install; lockfile/tree clean | Git status/diff |
</threat_model>

<verification>
The executor must preserve the evidence gate sequence: immutable source and production target proof, read-only preflight, exact additive apply/deploy, live compatibility/security probes, then PASS or containment. No production frontend action is part of this plan.
</verification>

<success_criteria>
- Production ref, exact pending set, immutable SHA, and safe preflight are proven before mutation.
- Four additive migrations and four named Edge Functions are the only production changes.
- Old-token, UUID-token, authenticated/anonymous policy, integrity, RLS, grant, request, event_id, and MCP probes pass.
- Any failure stops safely, preserves legacy schema/data, and records containment plus rollback/forward-fix state.
- `main` and the production frontend remain untouched.
</success_criteria>

<output>
Create `.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-16-SUMMARY.md` when done.
</output>
