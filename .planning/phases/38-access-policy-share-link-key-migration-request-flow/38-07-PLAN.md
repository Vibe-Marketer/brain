---
phase: 38-access-policy-share-link-key-migration-request-flow
plan: "07"
type: execute
wave: 4
depends_on: ["38-06"]
files_modified:
  - supabase/functions/share-call/index.ts
  - supabase/functions/mcp-server/tools/write/create_share_link.ts
  - supabase/functions/mcp-server/tools/read/list_shared_calls.ts
  - supabase/functions/mcp-server/tools/write/revoke_share_link.ts
  - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-SHARING-FUNCTION-EVIDENCE.md
autonomous: true
requirements: [ACCESS-07, ACCESS-08]
must_haves:
  truths:
    - "Existing `/s/<token>` outcomes and tokens remain unchanged while every new share write uses `recordings.id` UUID."
    - "Legacy-only share rows remain readable/revocable through UUID-first, legacy-fallback resolution."
    - "MCP can create/list/revoke a UUID-only non-Fathom recording share and still emits markdown `content[].text` with `/s/<token>`."
    - "The changed functions are deployed and proven on the dedicated test project before production."
  artifacts:
    - path: "supabase/functions/share-call/index.ts"
      provides: "UUID-native share create/read/revoke with legacy fallback"
    - path: "supabase/functions/mcp-server/tools/write/create_share_link.ts"
      provides: "UUID-native MCP share creation"
    - path: "supabase/functions/mcp-server/tools/read/list_shared_calls.ts"
      provides: "UUID-first shared-call listing"
    - path: "supabase/functions/mcp-server/tools/write/revoke_share_link.ts"
      provides: "Owner-authorized UUID/legacy revoke"
  key_links:
    - from: "share-call and MCP inputs"
      to: "call_share_links.recording_id"
      via: "canonical UUID write"
      pattern: "recording_id"
    - from: "legacy-only call_share_links"
      to: "recordings.fathom_provider_id"
      via: "owner-scoped fallback"
      pattern: "call_recording_id"
---

<objective>
Move share-call and MCP sharing to UUID-first behavior while retaining the complete legacy compatibility path.

Purpose: Deliver ACCESS-07/08 across the server callers that currently depend on Fathom BIGINT IDs.
Output: Updated Edge/MCP modules and dedicated-test deployment evidence.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-06-SUMMARY.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PATTERNS.md
@supabase/functions/share-call/__tests__/share-call.integration.test.ts
@supabase/functions/mcp-server/__tests__/sharing-uuid-bridge.test.ts
@AGENTS.md
@supabase/CLAUDE.md
</context>

## Artifacts this phase produces

- UUID-first `share-call` create/get/delete/access-log paths with owner-scoped legacy fallback.
- UUID-native `create_share_link`, `list_shared_calls`, and `revoke_share_link` MCP tool behavior.
- `38-TEST-SHARING-FUNCTION-EVIDENCE.md` with dedicated-test ref, deployment IDs/output, compatibility probes, and suite results.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert share-call to UUID-first reads and writes</name>
  <read_first>
    - supabase/functions/share-call/index.ts
    - supabase/functions/share-call/__tests__/share-call.integration.test.ts
    - supabase/functions/_shared/auth.ts
    - supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql
    - src/lib/recording-ids.ts
    - src/types/supabase.ts
  </read_first>
  <files>supabase/functions/share-call/index.ts</files>
  <behavior>
    - POST accepts canonical recording UUID, authenticates with the shared helper, verifies owner authorization server-side, and writes `recording_id` without numeric coercion.
    - Token/id GET, DELETE, and access-log paths resolve `recording_id` first and use `(call_recording_id,user_id)` only when UUID is absent.
    - Existing response status codes, recipient checks, public teaser/authenticated payload, revoke state, access logging, and token strings remain unchanged.
  </behavior>
  <action>
    Update every share-link select to include both identifiers. Resolve a canonical recording by UUID when present; only for legacy-only rows resolve `fathom_provider_id` with the row owner to prevent cross-owner collision. Make new create writes UUID-only. Preserve `authenticateRequest(req, supabase, corsHeaders)`, wildcard discovery/CORS behavior, token-as-credential semantics, safe response allowlists, and access-log relationships. Do not use `parseInt`, `Number`, or string coercion for recording identity.
  </action>
  <acceptance_criteria>
    - Source contains no recording-ID `parseInt`/`Number` and all new inserts set `recording_id`.
    - Legacy fallback includes owner scoping and runs only when `recording_id IS NULL`.
    - Existing token/recipient/revocation response branches remain represented and tests pass after test deployment.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- supabase/functions/share-call/__tests__/share-call.integration.test.ts --passWithNoTests &amp;&amp; ! rg -n "parseInt\(|Number\(" supabase/functions/share-call/index.ts</automated>
  </verify>
  <done>Share-call is UUID-native for all new behavior and backward-compatible for every existing token row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert MCP sharing tools without changing the MCP contract</name>
  <read_first>
    - supabase/functions/mcp-server/tools/write/create_share_link.ts
    - supabase/functions/mcp-server/tools/read/list_shared_calls.ts
    - supabase/functions/mcp-server/tools/write/revoke_share_link.ts
    - supabase/functions/mcp-server/tools/types.ts
    - supabase/functions/mcp-server/tools/helpers.ts
    - supabase/functions/mcp-server/__tests__/sharing-uuid-bridge.test.ts
    - supabase/functions/mcp-server/__tests__/golden-replay.test.ts
    - supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql
  </read_first>
  <files>supabase/functions/mcp-server/tools/write/create_share_link.ts, supabase/functions/mcp-server/tools/read/list_shared_calls.ts, supabase/functions/mcp-server/tools/write/revoke_share_link.ts</files>
  <behavior>
    - Create validates a UUID, verifies access/ownership through existing authorization helpers, writes `recording_id`, and works with `fathom_provider_id IS NULL`.
    - List returns canonical UUIDs for new and backfilled rows while resolving legacy-only rows safely.
    - Revoke requires owner authorization and works for either bridge state.
    - All successes/errors remain markdown in `content[].text`; URLs use `/s/<token>`.
  </behavior>
  <action>
    Keep each `ToolModule` definition and `mcpOk`/`mcpError` shape. Replace legacy lookup/write logic with canonical UUIDs and an explicitly isolated fallback for old rows. Never produce `/shared/<token>`, structured JSON tool results, or a second MCP Edge Function. Keep tools/list category filtering unchanged.
  </action>
  <acceptance_criteria>
    - `rg -n "/shared/"` finds no generated share URL in the three tools.
    - The UUID-only fixture passes create/list/revoke without `fathom_provider_id`.
    - Golden replay and tool category tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- supabase/functions/mcp-server/__tests__/sharing-uuid-bridge.test.ts supabase/functions/mcp-server/__tests__/golden-replay.test.ts src/lib/__tests__/mcp-tool-categories.test.ts</automated>
  </verify>
  <done>MCP sharing supports every provider through UUIDs and preserves its external protocol exactly.</done>
</task>

<task type="auto" gate="blocking">
  <name>[BLOCKING] Task 3: Deploy share-call and mcp-server to the dedicated test project and prove compatibility</name>
  <read_first>
    - supabase/functions/share-call/index.ts
    - supabase/functions/mcp-server/index.ts
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-SCHEMA-EVIDENCE.md
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-SHARING-FUNCTION-EVIDENCE.md (create; no prior file)
    - supabase/CLAUDE.md
  </read_first>
  <files>.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-SHARING-FUNCTION-EVIDENCE.md</files>
  <action>
    Link to and independently verify dedicated test ref `swjzxiddcrtaqixsfaac`, then deploy `share-call` and the single `mcp-server` function with `supabase functions deploy <name> --use-api` and their established JWT flags. Capture target, SHA, command/output/deployment metadata without secrets. Run the complete share-call and MCP bridge suites plus old-token and UUID-only live probes. Relink to production `vltmrnjsubfzrgrtdqey` afterward without deploying there, and record that production function state was unchanged.
  </action>
  <acceptance_criteria>
    - Both target checks show the test ref before each deployment.
    - Old token returns its pre-migration outcome and the UUID-only link can create/resolve/list/revoke.
    - MCP output is markdown and uses `/s/`.
    - Final CLI link is production and no production function deployment occurred.
  </acceptance_criteria>
  <verify>
    <automated>VITEST_INTEGRATION_OK=true npx vitest run --config vitest.integration.config.ts supabase/functions/share-call/__tests__/share-call.integration.test.ts &amp;&amp; npm test -- supabase/functions/mcp-server/__tests__/sharing-uuid-bridge.test.ts</automated>
  </verify>
  <done>The UUID bridge is proven through actual dedicated-test Edge endpoints before any production deploy.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: share tokens, recipient email rules, access logs, canonical UUIDs, MCP tool authorization.
- Boundaries: public token request to service-role read; authenticated owner/MCP token to share mutation; legacy BIGINT to UUID fallback.

| Threat ID | Category | Severity | Threat | Mitigation | Verification |
|---|---|---:|---|---|---|
| T-38-07-01 | Spoofing | HIGH | Caller shares/revokes another owner's recording | Shared auth plus server ownership check; owner-scoped fallback | Owner/non-owner tests |
| T-38-07-02 | Information disclosure | HIGH | Legacy fallback resolves another owner's same provider ID | Match both provider ID and owner | Collision fixture test |
| T-38-07-03 | Tampering | HIGH | Existing token or log identity changes | Update readers/writers only; migration preserved row/token/log IDs | Before/after probes |
| T-38-07-04 | Information disclosure | HIGH | MCP returns structured/raw data | Existing `mcpOk` markdown contract and golden replay | Shape assertions |
| T-38-07-05 | Tampering | HIGH | Test deploy targets production | Two test-ref checks; `--use-api`; final relink without prod deploy | Deployment evidence |
| T-38-07-SC | Tampering | LOW | Package change modifies Edge bundle | No installs; committed lockfile unchanged | Lockfile diff |
</threat_model>

<verification>
Targeted source/unit checks, dedicated-test deployments, full live share-call matrix, MCP bridge tests, and final production relink evidence must pass.
</verification>

<success_criteria>
- UUID-only and legacy share paths both work.
- ACCESS-08 behavior is unchanged for token recipients.
- Production has not been deployed in this plan.
</success_criteria>

<output>
Create `.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-07-SUMMARY.md` when done.
</output>
