---
phase: 38-access-policy-share-link-key-migration-request-flow
plan: "18"
type: execute
wave: 12
depends_on: ["38-17"]
gap_closure: true
files_modified:
  - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-03, ACCESS-04, ACCESS-05, ACCESS-06, ACCESS-07, ACCESS-08, ACCESS-09, EVT-06]
must_haves:
  truths:
    - "Production receives exactly the nine reviewed Phase 38 migrations 20260919000001 through 20260919000009, including the minimal guarded 00003 comment and forward repair 00009."
    - "One isolated production canary is provisioned immediately before rollout with exactly six synthetic auth users; it is never replaced with customer data."
    - "The two known legacy-only unresolved rows retain the approved stable redacted fingerprint/classification and continue returning the same generic unavailable behavior; same-owner ambiguity, unsafe cross-owner UUID assignment, and keyless counts remain zero."
    - "Only share-call, mcp-server, public-recording, and recording-access are deployed, and every live compatibility, authorization, privacy, RLS, event_id, and MCP probe passes."
    - "The canary graph and all six synthetic auth users are removed after PASS or safe STOP, zero residue is proven, and main plus the production frontend remain unchanged."
  artifacts:
    - path: ".planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md"
      provides: "Redacted rerun preflight, canary lifecycle, nine-migration apply, four-function deploy, live probes, cleanup, and PASS/STOP evidence"
  key_links:
    - from: "38-PREPRODUCTION-VERIFICATION.md"
      to: "production ref vltmrnjsubfzrgrtdqey"
      via: "matching current source fingerprint, exact nine-migration authorization, and corrected unresolved baseline"
      pattern: "PRODUCTION-GATE: PASS"
    - from: "scripts/phase38-production-canary.ts"
      to: "production synthetic graph"
      via: "target-confirmed provision, verify, and cleanup with exact six-user manifest"
      pattern: "--confirm-production"
    - from: "20260919000003_phase38_share_link_uuid_bridge.sql"
      to: "20260919000009_phase38_restore_share_access_log.sql"
      via: "guarded absent-table comment followed by ordered forward restoration"
      pattern: "call_share_access_log"
    - from: "38-PRODUCTION-DEPLOYMENT-EVIDENCE.md"
      to: "Phase 38 completion"
      via: "unchanged legacy fingerprint, zero canary residue, and immutable frontend boundary"
      pattern: "PRODUCTION-SERVER-GATE: PASS"
---

<objective>
Rerun the authorized Phase 38 production server rollout with the repaired migration ordering, corrected legacy preservation invariant, and controlled synthetic canary.

Purpose: Apply the additive Supabase work without touching the production frontend, preserve every legacy row/token behavior, prove all new and existing access paths live, and leave no synthetic residue.
Output: Nine production migration records, four verified Edge Function deployments, complete redacted live evidence, and an unambiguous PASS or safely contained STOP.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/V2.2-COMPLETION-PLAN.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-CONTEXT.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-16-PLAN.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-16-STOP.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-17-PLAN.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-17-SUMMARY.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-REMEDIATION-EVIDENCE.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PREPRODUCTION-VERIFICATION.md
@scripts/phase38-production-canary.ts
@supabase/migrations/20260919000001_phase38_access_policy_schema.sql
@supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql
@supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql
@supabase/migrations/20260919000004_phase38_copy_event_preservation.sql
@supabase/migrations/20260919000005_phase38_authorization_review_fixes.sql
@supabase/migrations/20260919000006_phase38_participant_evidence_recompute.sql
@supabase/migrations/20260919000007_phase38_legacy_share_management.sql
@supabase/migrations/20260919000008_phase38_notification_contracts.sql
@supabase/migrations/20260919000009_phase38_restore_share_access_log.sql
@supabase/functions/share-call/index.ts
@supabase/functions/mcp-server/index.ts
@supabase/functions/public-recording/index.ts
@supabase/functions/recording-access/index.ts
@supabase/CLAUDE.md
@AGENTS.md
</context>

## Production rerun contract

- Production database/API target: `vltmrnjsubfzrgrtdqey`. TEST ref `swjzxiddcrtaqixsfaac` is forbidden for this plan.
- Exact database change set: pending migrations `20260919000001` through `20260919000009` only, applied in filename order. Migration 00003 may differ from its TEST-applied form only by the Plan 17 guarded comment; 00009 performs the forward restoration.
- Exact function change set: `share-call`, `mcp-server`, `public-recording`, and `recording-access` only.
- Approved legacy baseline: exactly two legacy-only unresolved rows with the Plan 17 SHA-256 fingerprint and classification breakdown (one source absent, one source present only under another owner); zero same-owner ambiguity, unsafe cross-owner UUID assignments, and keyless rows. The two unresolved tokens remain generic-unavailable and are never mapped, rewritten, revoked, or deleted by this rollout.
- Exact canary: the isolated graph created by `scripts/phase38-production-canary.ts` with six synthetic auth users and `source_metadata.integration_test = 'phase-38-production-canary'`. Customer data is never a canary substitute.
- Release boundary: do not merge or push `main`; do not invoke Vercel or deploy the frontend.

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Reprove immutable source and legacy state, then provision the six-user production canary</name>
  <files>.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md</files>
  <action>
    Start a new timestamped rerun section; preserve the prior STOP record. Stop before any production write unless Plan 17 evidence contains `PRODUCTION-GATE: PASS`, branch is `v2.2-event-resolution`, the recorded source commit is an ancestor of HEAD, and the recomputed non-planning fingerprint exactly matches. Require all non-planning tracked, staged, and untracked paths clean, then run `npm run build` from that committed tree. Prove production ref `vltmrnjsubfzrgrtdqey` independently through the link file, project listing, and database/API host; reject TEST ref. Fetch and record `origin/main`, the live frontend deployment ID/URL, and current versions/deployment IDs for all four functions, using public metadata only.

    Require local/remote migration comparison to show exactly nine pending Phase 38 files `20260919000001` through `20260919000009`, no earlier or unexpected pending migration, and the reviewed Plan 17 checksum/diff for the guarded 00003 plus new 00009. In read-only SQL and the canary tool's inventory command, require exactly two unresolved legacy-only rows with the exact approved Plan 17 fingerprint/classification, including the one known cross-owner-only candidate, zero same-owner ambiguity, zero unsafe cross-owner UUID assignments, zero keyless share rows, and zero orphan/event_id hazards. Hold the two unresolved tokens only ephemerally in process, probe both, and require the same generic unavailable result class; record only their set fingerprint and aggregate result class. Probe one unique resolvable legacy token read-only and record only its redacted fingerprint and response class. Do not copy, repair, revoke, log, or expose any customer token or row.

    Only after every read-only gate passes, run the reviewed canary tool's production `provision` command with both exact ref and explicit production confirmation. Require exactly six newly created synthetic auth users and one isolated marked graph, record only run fingerprint/counts, and verify the manifest is mode `0600` outside the repository. Provisioning must complete immediately before Task 2; if Task 2 cannot start within ten minutes, clean and provision a new graph. After provision, immediately recheck target ref, source fingerprint, pending set, marker uniqueness, exact six-user count, and existing legacy invariant. Any failure must invoke targeted canary cleanup in a `finally` path, prove residue state, write STOP, and end without migration/function deployment. Per D-01 and D-07 through D-17, no customer data may substitute for any access/privacy/request probe.
  </action>
  <verify>
    <automated>e=.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PREPRODUCTION-VERIFICATION.md; c="$(sed -n 's/^Verified application source commit: //p' "$e")"; test "$(cat supabase/.temp/project-ref)" = "vltmrnjsubfzrgrtdqey" &amp;&amp; test "$(git branch --show-current)" = "v2.2-event-resolution" &amp;&amp; git merge-base --is-ancestor "$c" HEAD &amp;&amp; test "$(git ls-tree -r --full-tree HEAD | sed '/\t\.planning\//d' | git hash-object --stdin)" = "$(sed -n 's/^Verified application source fingerprint: //p' "$e")" &amp;&amp; rg -n '^Authorized Phase 38 migrations: 20260919000001\.\.20260919000009$|^Authorized unresolved legacy count: 2$|^Authorized unresolved legacy fingerprint: sha256:[0-9a-f]{64}$|^PRODUCTION-GATE: PASS$' "$e"</automated>
  </verify>
  <done>The exact production target, immutable source, nine-file pending set, unchanged two-row legacy baseline, and fresh six-user synthetic canary are proven, or cleanup has completed before rollout.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply exactly nine migrations and deploy exactly four Edge Functions</name>
  <files>.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md</files>
  <action>
    Begin only with a fresh verified canary from Task 1. Recheck branch, source fingerprint, production target, exact pending set, canary run ID/count, approved two-row unresolved fingerprint/classification, and zero ambiguity, unsafe assignment, and keyless counts immediately before mutation. Preview `supabase db push --linked` and require it to list only `20260919000001` through `20260919000009` in order. Apply that exact set. The guarded comment in modified 00003 must allow the missing historical `call_share_access_log` table, and later 00009 must create/reconcile it; do not manually create the table, skip/reorder a migration, repair migration history, or execute ad hoc customer-data SQL. Stop on the first migration failure.

    After all nine migration records are present, deploy only `share-call`, `mcp-server`, `public-recording`, and `recording-access`, one at a time, with `supabase functions deploy &lt;name&gt; --project-ref vltmrnjsubfzrgrtdqey --use-api` and each function's repository-configured JWT verification setting. Record sanitized timestamps, CLI result classes, versions, and deployment IDs. Do not install packages, push Git, checkout/merge `main`, call Vercel, or deploy any other function.

    Wrap every post-provision path so a migration or deployment STOP triggers the script's targeted canary cleanup before returning. If schema is partially advanced, keep additive tables/columns and recorded migration history, disable/avoid the affected new path, and prepare a forward corrective migration; never delete customer rows, drop new schema, or alter the two unresolved rows. If a function regresses, redeploy the recorded compatible function source/version when possible and record containment. PASS remains impossible until Task 3 completes.
  </action>
  <verify>
    <automated>supabase migration list --linked &amp;&amp; supabase functions list --project-ref vltmrnjsubfzrgrtdqey</automated>
  </verify>
  <done>Production history contains the nine reviewed migrations and only the four named functions are deployed, or the rollout is stopped, contained, and the synthetic graph is cleaned.</done>
</task>

<task type="auto">
  <name>Task 3: Prove live behavior, preserve unresolved legacy rows, clean the canary, and close the gate</name>
  <files>.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md</files>
  <action>
    Re-read remote migration history and require exactly `00001` through `00009`. Introspect access-policy columns/default trigger, bridge columns/check/FKs/indexes, restored `call_share_access_log` columns/nullability/FKs/indexes/RLS/policy/grants/comments, request/grant/audit/outbox/notification contracts, every Phase 38 RPC and execution grant, and all three copy-function definitions. Require zero orphan, duplicate-active, keyless, same-owner ambiguous, unsafe cross-owner UUID assignment, and event_id-loss counts. Recompute the legacy inventory: the same two unresolved rows, classification breakdown, and exact approved fingerprint must remain, both ephemeral-token probes must still return generic unavailable, and the resolvable legacy probe must retain its before-result class. Never write raw values or response content to evidence.

    Using only the marked synthetic graph, run the complete live matrix: create/resolve/revoke a UUID-native link; insert and read an anonymous access-log row with null accessor; prove authenticated/anon direct log forgery fails; owner/team/coach/admin and legacy-token access; private public-endpoint denial and allowlisted public response; verified-participant discovery; invitee-only/unverified denial; exact Zoom webinar/non_webinar and non-Zoom neutral-unknown aggregation; authoritative-webinar suppression; independent 49/50 participant boundary; request/retry/owner notification/email outbox; approve/grant/revoke; deny/cooldown; event_id preservation; and MCP `content[].text` markdown shape. Per D-01 through D-22, forbidden fields/content remain absent from denial/discovery responses and existing access never broadens.

    In a guaranteed finalizer after PASS or safe STOP, remove the canary's temporary share/access/request/grant/audit/outbox/notification/participant/identity/recording/workspace/event/org rows in dependency order, then delete exactly its six auth users. Delete the local manifest. Query every marked table plus Auth admin lookup and require zero rows/users for the run ID and exact production canary marker. Never call a broad cleanup routine. Recompute the legacy count/fingerprint after cleanup to prove customer legacy state is unchanged.

    Fetch `origin/main` and the production frontend deployment again; require exact equality with Task 1. Scan evidence for emails, full UUIDs/tokens, database URLs, keys, titles, transcripts, summaries, and payload bodies before commit. End with `PRODUCTION-SERVER-GATE: PASS` only when all migrations, function deployments, live probes, legacy checks, cleanup/zero residue, and release-boundary comparisons pass. Otherwise record STOP, the exact failed assertion, containment, function rollback or forward-fix state, and cleanup result. A cleanup failure is always STOP and must report only aggregate residue counts.
  </action>
  <verify>
    <automated>e=.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md; rg -n '^Production migrations: 20260919000001\.\.20260919000009$|^Unresolved legacy count after: 2$|^Unresolved legacy fingerprint after: sha256:[0-9a-f]{64}$|^Canary auth users after cleanup: 0$|^Canary graph rows after cleanup: 0$|^PRODUCTION-SERVER-GATE: PASS$' "$e" &amp;&amp; test "$(sed -n 's/^Origin main before: //p' "$e")" = "$(sed -n 's/^Origin main after: //p' "$e")" &amp;&amp; test "$(sed -n 's/^Production frontend before: //p' "$e")" = "$(sed -n 's/^Production frontend after: //p' "$e")"</automated>
  </verify>
  <done>All nine migrations and four functions are live and verified, both unresolved legacy rows remain fingerprint-identical and unavailable, the synthetic graph/users are gone, and the frontend release boundary is intact; otherwise a sanitized STOP records containment and zero-residue status.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: production recordings and identities, legacy/new share tokens, access policy and lifecycle rows, auth users, migration/function state, production frontend boundary.
- Boundaries: local CLI/script to production management APIs; pending SQL to Postgres; anonymous/authenticated probes to Edge Functions/RPCs; synthetic canary to customer dataset; runtime secrets to committed evidence.

| Threat ID | Category | Severity | Threat | Disposition | Mitigation and proof |
|---|---|---:|---|---|---|
| T-38-18-01 | Tampering | CRITICAL | Wrong project/source or unexpected migration is deployed | mitigate | Three-way production proof, immutable fingerprint, exact nine-file preview/history, immediate recheck |
| T-38-18-02 | Tampering | CRITICAL | Modified 00003 changes bridge behavior beyond the required absence guard | mitigate | Plan 17 semantic diff/test and fingerprint; exact ordered production apply |
| T-38-18-03 | Elevation of privilege | CRITICAL | RLS/RPC/function grants broaden content access | mitigate | Catalog introspection plus synthetic owner/admin/team/coach/participant/unverified/anonymous probes |
| T-38-18-04 | Information disclosure | CRITICAL | Customer data is used as a canary or evidence leaks token/PII/content | mitigate | Exact marked synthetic graph only; ephemeral secrets; redacted hashes/counts/classes; evidence scan |
| T-38-18-05 | Tampering | CRITICAL | Two unresolved legacy rows are arbitrarily mapped or behavior changes | mitigate | Pre/post count `2`, stable SHA-256 set fingerprint/classification, generic-unavailable probes, zero ambiguity, unsafe assignment, and keyless gates |
| T-38-18-06 | Denial of service | HIGH | Partial rollout or probe failure leaves synthetic residue | mitigate | Per-run manifest and cleanup finalizer on PASS/STOP; exact six-user and zero-row residue checks |
| T-38-18-07 | Repudiation | HIGH | Remote changes cannot be tied to reviewed code and commands | mitigate | Source commit/fingerprint, migration checksums/history, deployment IDs, timestamps, sanitized result classes |
| T-38-18-08 | Denial of service | HIGH | Function rollout regresses legacy share or MCP paths | mitigate | Before/after legacy probes, sequential deploy/live checks, recorded compatible function rollback |
| T-38-18-09 | Tampering | HIGH | Supabase rollout accidentally releases frontend code | mitigate | No Git/Vercel mutation; exact main SHA and frontend deployment before/after |
| T-38-18-SC | Tampering | LOW | Package change alters the reviewed deploy tree | mitigate | No install; clean non-planning tree and unchanged lockfile |
</threat_model>

<source_audit>

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Per-recording access, privacy-safe discovery/request flow, UUID bridge, existing sharing untouched | Tasks 1-3 plus prior Phase 38 plans | COVERED |
| REQ | ACCESS-01 through ACCESS-05, ACCESS-07 through ACCESS-09 | Task 3 live matrix | COVERED |
| REQ | ACCESS-06 preserve links, tokens, logs, and UUID bridge | Tasks 1-3 corrected legacy/table gates | COVERED |
| REQ | EVT-06 copies preserve event_id | Task 3 live/catalog probes | COVERED |
| RESEARCH | Additive production change, exact target/pending set, compatibility, fail-closed rollout | Tasks 1-3 | COVERED |
| CONTEXT | D-01 through D-06 existing/private/default/override behavior | Task 3 | COVERED |
| CONTEXT | D-07 through D-11 privacy, evidence, webinar, threshold behavior | Tasks 1 and 3 | COVERED |
| CONTEXT | D-12 through D-17 request lifecycle | Tasks 1 and 3 | COVERED |
| CONTEXT | D-18 through D-22 settings/per-recording UI | Plan 17 fingerprint-bound browser proof; frontend unchanged in Tasks 1-3 | COVERED |

</source_audit>

<verification>
Execute the gates in order without bypass: immutable source/target/pending set and legacy baseline; fresh six-user canary; exact nine migrations; exact four functions; live matrix; mandatory cleanup/zero residue; main/frontend equality; PASS or sanitized STOP.
</verification>

<success_criteria>
- Production history advances by exactly migrations `20260919000001` through `20260919000009` in order.
- Exactly four named Edge Functions deploy from the fingerprinted tree and pass all live compatibility, privacy, authorization, request, event, and MCP probes.
- The two unresolved legacy-only rows retain their approved stable fingerprint/classification and generic-unavailable behavior; ambiguity, unsafe cross-owner UUID assignment, and keyless counts stay zero.
- The controlled graph uses exactly six synthetic auth users and leaves zero database/Auth residue after PASS or STOP.
- Evidence contains no customer data or secret, and `origin/main` plus the production frontend deployment remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-18-SUMMARY.md` when done.
</output>
