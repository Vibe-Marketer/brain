---
phase: 38-access-policy-share-link-key-migration-request-flow
plan: "04"
type: execute
wave: 2
depends_on: ["38-01", "38-02", "38-03"]
files_modified:
  - supabase/migrations/20260919000001_phase38_access_policy_schema.sql
  - supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-03, ACCESS-04, ACCESS-05, ACCESS-09]
must_haves:
  truths:
    - "D-01 preserves private default/existing shares; D-02 constrains the six policy levels; D-03 snapshots the account default; D-04 permits per-recording overrides; D-05 keeps default changes future-only; D-06 restricts changes to the owner."
    - "Recording content remains default-deny and event membership alone never grants it. Existing owner/admin/team/coach paths remain additive."
    - "D-07 returns anonymous numbered copies; D-08 forbids owner/provider/title/transcript/summary disclosure; D-09 requires verified confirmed evidence; D-10 suppresses webinars and 50+ confirmed participants; D-11 permits large-event access only by invitation/share."
    - "D-12 persists owner notification/email, D-13 allows only the approved owner-review evidence, D-14 forbids a requester message, D-15 grants until revoked, D-16 logs every decision, and D-17 returns generic denial with a 30-day cooldown."
  artifacts:
    - path: "supabase/migrations/20260919000001_phase38_access_policy_schema.sql"
      provides: "Policy columns/default trigger, lifecycle tables, evidence normalization, constraints and indexes"
    - path: "supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql"
      provides: "RLS, authorization helpers, discovery/existence and lifecycle RPCs"
  key_links:
    - from: "recordings BEFORE INSERT trigger"
      to: "user_settings.default_recording_access_level"
      via: "owner default snapshot with private fallback"
      pattern: "default_recording_access_level"
    - from: "request_recording_access"
      to: "recording_access_requests|recording_access_audit_log|user_notifications|recording_access_email_outbox"
      via: "one locked transaction"
      pattern: "request_recording_access"
---

<objective>
Author the additive access-policy, privacy-safe discovery, and request lifecycle database layer.

Purpose: Put authorization and disclosure decisions inside PostgreSQL so stale or malicious clients cannot bypass the locked behavior.
Output: Two forward-only migrations with policy snapshotting, evidence normalization, RLS, hardened RPCs, audit, notifications, and lifecycle state.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-CONTEXT.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-RESEARCH.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PATTERNS.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-VALIDATION.md
@supabase/migrations/20260831000001_create_events_and_extend_participants.sql
@supabase/migrations/20260831020000_fix_events_participation_rls_and_updated_at_trigger.sql
@supabase/migrations/20260905140000_create_identities_and_link_tables.sql
@supabase/migrations/20260908130001_create_org_identity_self_serve_rpcs.sql
@supabase/migrations/20260131000004_create_notifications_table.sql
@supabase/CLAUDE.md
</context>

## Artifacts this phase produces

- Columns: `user_settings.default_recording_access_level`, `recordings.access_level`, `recordings.access_policy_origin`.
- Tables: `recording_access_requests`, `recording_access_grants`, `recording_access_audit_log`, `recording_access_email_outbox`.
- Trigger/functions: default snapshot trigger; participant evidence normalization; internal verified-identity/content predicates.
- RPCs: `get_recording_access_policy`, `set_default_recording_access_level`, `set_recording_access_level`, `reset_recording_access_level`, `get_event_existence_for_participant`, `list_discoverable_recording_copies`, `request_recording_access`, `get_recording_access_management`, `approve_recording_access_request`, `deny_recording_access_request`, and `revoke_recording_access_grant`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add constrained policy snapshots and lifecycle storage</name>
  <read_first>
    - supabase/migrations/20260919000001_phase38_access_policy_schema.sql (create; no prior file)
    - supabase/migrations/20260831000001_create_events_and_extend_participants.sql
    - supabase/migrations/20260309120000_call_participants.sql
    - supabase/migrations/20260407160000_backfill_zoom_recorded_by.sql
    - supabase/migrations/20260905140000_create_identities_and_link_tables.sql
    - supabase/migrations/20260131000004_create_notifications_table.sql
    - src/test/access-policy.integration.test.ts
    - src/test/migrations/phase38-access-migrations.test.ts
  </read_first>
  <files>supabase/migrations/20260919000001_phase38_access_policy_schema.sql</files>
  <behavior>
    - New recordings snapshot the owner's current default, or Private when no setting row exists; existing rows backfill to Private/default and never follow later setting changes.
    - Explicit per-recording selection marks `custom`; reset later snapshots the then-current default and marks `default`.
    - Transcript sources normalize to speaker/confirmed speech, recorded-by/host evidence normalizes to organizer evidence, and calendar-only rows remain invitees without confirmed attendance.
    - Lifecycle tables forbid duplicate pending requests and duplicate active grants while preserving resolved/revoked history.
  </behavior>
  <action>
    Create additive columns with a six-value check (`private`, `attendees`, `invitees`, `organization`, `link`, `public`) and origin check (`default`, `custom`). Backfill existing recordings deterministically to Private/default, then make the policy non-null. Add a `BEFORE INSERT` trigger that reads `NEW.owner_user_id`'s setting and overrides absent/caller-supplied default-state values, so connector, paste, MCP, direct SQL, and copy functions share one interception point. Add an idempotent call-participant normalization trigger/backfill based only on existing explicit evidence: `sources @> ARRAY['transcript']` plus speaker type confirms speech; `recorded_by`/host is organizer evidence; `calendar_invitees` alone is invitee/non-confirmed. Create request/grant/audit/outbox tables with UUID FKs, state checks, partial unique indexes, lookup indexes, minimal evidence snapshots, exact 30-day cooldown fields, and timestamps. Do not add a requester message/reason column per D-14/D-17.
  </action>
  <acceptance_criteria>
    - The migration contains no DROP/rename/destructive rewrite and is wrapped in a transaction.
    - All six policy values and both origin values are constrained in SQL.
    - The recording trigger covers every INSERT without requiring caller changes and a copied recording uses its destination owner setting.
    - One pending request per recording/requester and one active grant per recording/grantee are database-enforced.
    - Audit and outbox rows cannot cascade-delete history merely because a request resolves.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/test/migrations/phase38-access-migrations.test.ts</automated>
  </verify>
  <done>The additive storage layer makes policy/default/lifecycle states representable and guards forbidden duplicates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add hardened authorization, discovery, and lifecycle RPCs</name>
  <read_first>
    - supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql (create; no prior file)
    - supabase/migrations/20260919000001_phase38_access_policy_schema.sql
    - supabase/migrations/20260908130001_create_org_identity_self_serve_rpcs.sql
    - supabase/migrations/20260831020000_fix_events_participation_rls_and_updated_at_trigger.sql
    - supabase/migrations/20260310000000_org_admin_recordings_rls.sql
    - supabase/migrations/20260308000002_tighten_recordings_select_rls.sql
    - supabase/migrations/20260309120000_call_participants.sql
    - supabase/migrations/20260303000006_create_zoom_raw_calls.sql
    - src/test/access-policy.integration.test.ts
    - src/test/rls-regression.test.ts
  </read_first>
  <files>supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql</files>
  <behavior>
    - Only the owner can set/reset a recording policy (D-06), and changing the account default never updates recordings (D-05).
    - Attendees requires verified caller alias plus explicit confirmed speech/host/organizer evidence; Invitees accepts verified calendar invitation or stronger evidence; Organization accepts active organization membership; Link stays token-mediated; Public stays allowlisted-Edge-mediated.
    - Event existence/discovery requires verified confirmed evidence and returns only the declared safe shape. Discovery and request both suppress Zoom `meeting_type IN (5,6,9)` and confirmed participant count `>= 50`.
    - Request/approve/deny/revoke derive actors with `auth.uid()`, lock rows, validate state transitions, write audit/notifications atomically, and return stable codes under retries.
  </behavior>
  <action>
    Enable and FORCE RLS on new tables. Add internal schema-qualified SECURITY DEFINER helpers with `SET search_path=''`, revoked PUBLIC/anon/authenticated execution unless directly required, and no caller-supplied user identity. Preserve current recording SELECT policies and add only policy/grant paths. Add owner/requester read policies while denying direct lifecycle/audit/outbox writes. Replace the permissive `user_notifications` INSERT policy/grants so authenticated clients cannot forge notices while service-role and hardened definer functions continue. Implement minimal existence/discovery projections, using verified `identity_aliases` ownership and confirmed `call_participants` evidence; calendar/org alone must fail. Determine provider webinar status only from explicit stored provider evidence, with Zoom raw types 5/6/9 as the currently verified mapping, and enforce the `< 50` cutoff in both list and request functions. Owner review may return D-13 fields; requester denial status never returns a private reason. Approval creates an until-revoked grant, denial sets server-calculated `NOW()+INTERVAL '30 days'`, and every request/approve/deny/revoke appends audit.
  </action>
  <acceptance_criteria>
    - Every SECURITY DEFINER function has empty pinned search path, schema-qualified references, and explicit revoke/grant statements.
    - `request_recording_access` repeats eligibility, webinar, and count checks rather than trusting discovery output.
    - Discovery output contains only ordinal, opaque/request target, and request/cooldown state; no owner/provider/title/content/raw evidence fields.
    - Existing owner/admin/team/workspace/coach policies are not dropped or replaced.
    - Public/link do not create broad `anon` or authenticated raw-table SELECT policies.
    - Lifecycle actions are idempotent under duplicate requests and concurrent owner actions.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/test/migrations/phase38-access-migrations.test.ts</automated>
  </verify>
  <done>Database authorization satisfies D-01 through D-17 and high-severity disclosure paths fail closed.</done>
</task>

</tasks>

<threat_model>
## Assets

- Recording content, policy settings, verified identity aliases, participant evidence, request/grant state, notification recipients, audit history.

## Trust boundaries

- Authenticated JWT to SECURITY DEFINER RPC.
- Event participation to recording content authorization.
- Provider metadata/participant count to discovery eligibility.
- Lifecycle RPC to notification/audit/outbox storage.

| Threat ID | Category | Severity | Threat | Mitigation | Verification |
|---|---|---:|---|---|---|
| T-38-04-01 | Elevation of privilege | HIGH | Non-owner mutates policy or request decision | `auth.uid()` owner predicates, locked rows, direct-write denial | Owner/non-owner real-DB matrix |
| T-38-04-02 | Information disclosure | HIGH | Broad event helper leaks copy/content | Dedicated verified-confirmed helper; purpose-built projection; separate content helper | Payload allowlist and content-denial tests |
| T-38-04-03 | Information disclosure | HIGH | Webinar/50-person graph exposed | Zoom 5/6/9 and `confirmed_count < 50` checks in both RPCs | 49/50/webinar tests |
| T-38-04-04 | Tampering | HIGH | Audit/notification/outbox forged | Client DML denied; trusted transaction writers only | RLS direct-write tests |
| T-38-04-05 | Elevation of privilege | HIGH | Search-path/function grant exploit | Empty search path, schema qualification, explicit EXECUTE revocation | Catalog introspection in Plan 06 |
| T-38-04-06 | Denial of service | MEDIUM | Replay creates duplicate request/grant/mail | Partial unique constraints, row locks, idempotent stable codes/outbox key | Retry/concurrency integration tests |
| T-38-04-SC | Tampering | LOW | Package dependency compromises migration workflow | SQL only; no package changes | Lockfile diff gate |
</threat_model>

<verification>
Run migration source gates now. Do not apply any migration in this plan; the dedicated-test-project apply is the blocking Plan 06.
</verification>

<success_criteria>
- The schema is additive and legacy behavior remains represented.
- All policy/discovery/lifecycle decisions are server-side.
- All high-severity threats have concrete SQL mitigations and tests.
</success_criteria>

<output>
Create `.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-04-SUMMARY.md` when done.
</output>
