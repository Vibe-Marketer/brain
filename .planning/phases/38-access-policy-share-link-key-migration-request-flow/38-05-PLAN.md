---
phase: 38-access-policy-share-link-key-migration-request-flow
plan: "05"
type: execute
wave: 3
depends_on: ["38-01", "38-02", "38-03"]
files_modified:
  - supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql
  - supabase/migrations/20260919000004_phase38_copy_event_preservation.sql
autonomous: true
requirements: [ACCESS-07, ACCESS-08, EVT-06]
must_haves:
  truths:
    - "Every existing share row, token, recipient, status, timestamp, and access-log relationship survives the UUID bridge unchanged."
    - "New UUID-only recordings can own share links while `call_recording_id` remains available for legacy fallback throughout Phase 38."
    - "Team, coach, owner, admin, and token access definitions are not removed or superseded."
    - "All three latest cross-org copy/routing functions preserve exact event_id and allow the destination default trigger to set independent policy."
  artifacts:
    - path: "supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql"
      provides: "Nullable UUID FK, safe backfill, compatibility constraint/index, UUID-native shared-with-me RPC"
    - path: "supabase/migrations/20260919000004_phase38_copy_event_preservation.sql"
      provides: "Latest three copy/routing bodies with event_id preservation"
  key_links:
    - from: "call_share_links.recording_id"
      to: "recordings.id"
      via: "nullable UUID FK and owner-scoped deterministic backfill"
      pattern: "recording_id uuid"
    - from: "copy/routing INSERT"
      to: "recordings.event_id"
      via: "v_source.event_id"
      pattern: "event_id.*v_source.event_id"
---

<objective>
Author the non-destructive share-link UUID bridge and preserve event identity across every current cross-org copy path.

Purpose: Remove the UUID/BIGINT blocker without invalidating existing URLs or logs, and close EVT-06 using the actual latest function bodies.
Output: Two additive migrations for ACCESS-07/08 and EVT-06.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-RESEARCH.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PATTERNS.md
@supabase/migrations/20260108000001_create_single_call_share_tables.sql
@supabase/migrations/20260210235500_shared_links_and_vault_invite_rpc.sql
@supabase/migrations/20260610121000_rename_legacy_recording_id_to_fathom_provider_id.sql
@supabase/migrations/20260730160000_fix_cross_org_copy_dedup.sql
@src/test/migrations/phase38-access-migrations.test.ts
@supabase/CLAUDE.md
</context>

## Artifacts this phase produces

- `call_share_links.recording_id UUID REFERENCES recordings(id)` plus UUID and compatibility indexes/check.
- Owner-scoped deterministic UUID backfill and unresolved/ambiguous inventory helpers.
- `get_calls_shared_with_me_v3()` returning canonical UUID while a compatibility wrapper remains.
- Replaced latest bodies of `copy_recording_to_org`, `copy_recording_to_organization`, and `route_recording_cross_org` with exact `event_id` preservation.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bridge share links to recording UUIDs without row/token replacement</name>
  <read_first>
    - supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql (create; no prior file)
    - supabase/migrations/20260108000001_create_single_call_share_tables.sql
    - supabase/migrations/20260210235500_shared_links_and_vault_invite_rpc.sql
    - supabase/migrations/20260610121000_rename_legacy_recording_id_to_fathom_provider_id.sql
    - supabase/functions/share-call/index.ts
    - supabase/functions/share-call/__tests__/share-call.integration.test.ts
    - src/test/migrations/phase38-access-migrations.test.ts
  </read_first>
  <files>supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql</files>
  <behavior>
    - Existing rows backfill only when `(call_recording_id,user_id)` maps to exactly one `(recordings.fathom_provider_id,recordings.owner_user_id)` row.
    - Ambiguous/unmapped rows never choose an arbitrary UUID and remain usable through the legacy key.
    - New rows may set UUID with a null legacy key; every row must have at least one key.
    - UUID-native shared-with-me results return `recording_id UUID`; legacy-only results still resolve through fallback.
  </behavior>
  <action>
    Add nullable `recording_id` with FK/index, make `call_recording_id` nullable without dropping it or its legacy FK, and add a NOT VALID then validated at-least-one-key check after backfill. Backfill in place using owner-scoped unique matches; never update token, id, status, recipient, timestamps, or access logs. Add read-only helper queries/comments for unresolved and ambiguous counts. Create `get_calls_shared_with_me_v3()` as UUID-first with legacy fallback and preserve the existing v2 signature as a wrapper/compatibility surface until every caller migrates. Keep all existing RLS access paths.
  </action>
  <acceptance_criteria>
    - No INSERT/DELETE/re-tokenization of existing share rows appears in the migration.
    - `call_recording_id` remains present and nullable; `recording_id` is UUID FK to `recordings.id`.
    - Backfill includes owner equality and exact-one-match logic.
    - Shared-with-me v3 never returns a numeric canonical identifier for UUID-native rows.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/test/migrations/phase38-access-migrations.test.ts</automated>
  </verify>
  <done>The bridge is reversible at the application layer and preserves every existing share identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve event_id in all current copy/routing definitions</name>
  <read_first>
    - supabase/migrations/20260919000004_phase38_copy_event_preservation.sql (create; no prior file)
    - supabase/migrations/20260730160000_fix_cross_org_copy_dedup.sql
    - src/services/data-movement.service.ts
    - src/services/__tests__/data-movement.dedup.integration.test.ts
    - supabase/migrations/20260919000001_phase38_access_policy_schema.sql
  </read_first>
  <files>supabase/migrations/20260919000004_phase38_copy_event_preservation.sql</files>
  <behavior>
    - Each current function inserts `v_source.event_id` into the destination recording.
    - Null event stays null; dedup/retry returns the existing destination without corrupting its event association.
    - No policy columns are explicitly copied, so the Phase 38 INSERT trigger snapshots the destination owner's current default.
  </behavior>
  <action>
    Copy the exact latest definitions and grants for `copy_recording_to_org(UUID,UUID,UUID,BOOLEAN)`, `copy_recording_to_organization(UUID,UUID)`, and `route_recording_cross_org(UUID,UUID,UUID,BOOLEAN,UUID)` from `20260730160000_fix_cross_org_copy_dedup.sql`. Make the smallest body change: add `event_id` to every target column list and `v_source.event_id` to matching values. Preserve membership checks, dedup keys, workspace entry creation, statement timeouts, delete behavior, search path, signatures, and grants. Do not set `access_level` or `access_policy_origin` in these INSERTs.
  </action>
  <acceptance_criteria>
    - Static tests find all three exact signatures and `event_id`/`v_source.event_id` pairs.
    - A diff against the latest bodies shows no unrelated authorization, dedup, timeout, workspace, or delete changes.
    - Function grants and security-definer hardening remain explicit.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/test/migrations/phase38-access-migrations.test.ts</automated>
  </verify>
  <done>EVT-06 is implemented in every current copy/routing function without coupling access policies across copies.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: live share tokens/logs, canonical recording UUIDs, event associations, existing cross-org authorization behavior.
- Boundaries: legacy BIGINT to UUID mapping; source recording to destination copy; authenticated recipient to shared-with-me RPC.

| Threat ID | Category | Severity | Threat | Mitigation | Verification |
|---|---|---:|---|---|---|
| T-38-05-01 | Tampering | HIGH | Backfill maps a share row to the wrong recording | Owner-scoped exact-one-match backfill; ambiguity inventory; never arbitrary LIMIT 1 | Legacy/ambiguous fixture tests |
| T-38-05-02 | Denial of service | HIGH | Existing token or log breaks after migration | In-place nullable expansion; preserve IDs/tokens/legacy key/access logs | Before/after row identity assertions |
| T-38-05-03 | Information disclosure | HIGH | UUID bridge widens share/team/coach access | Preserve RLS and token checks; no new broad policy | Authorization continuity matrix |
| T-38-05-04 | Tampering | HIGH | Cross-org copy loses or changes event association | Latest bodies only; exact event_id copy; retry tests | Three-function real-DB tests |
| T-38-05-SC | Tampering | LOW | Dependency change affects migration | SQL only; no installs | Lockfile diff gate |
</threat_model>

<verification>
Run the migration source gates. Do not link or push either migration to any Supabase project in this plan; Plan 06 owns the dedicated-test apply.
</verification>

<success_criteria>
- Legacy and UUID-native share rows can coexist.
- Existing share tokens/logs remain stable.
- All current copy/routing functions preserve event_id and independent access policy.
</success_criteria>

<output>
Create `.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-05-SUMMARY.md` when done.
</output>
