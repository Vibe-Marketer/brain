---
phase: 38-access-policy-share-link-key-migration-request-flow
plan: "17"
type: execute
wave: 11
depends_on: ["38-16"]
gap_closure: true
files_modified:
  - supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql
  - supabase/migrations/20260919000009_phase38_restore_share_access_log.sql
  - src/test/migrations/phase38-access-migrations.test.ts
  - src/test/migrations/phase38-restore-share-access-log.integration.test.ts
  - supabase/functions/share-call/__tests__/share-call.integration.test.ts
  - scripts/phase38-production-canary.ts
  - scripts/__tests__/phase38-production-canary.test.ts
  - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-REMEDIATION-EVIDENCE.md
  - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PREPRODUCTION-VERIFICATION.md
  - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-VALIDATION.md
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-03, ACCESS-04, ACCESS-05, ACCESS-06, ACCESS-07, ACCESS-08, ACCESS-09, EVT-06]
must_haves:
  truths:
    - "The UUID bridge deliberately preserves the two known legacy-only unresolved production rows; rollout is blocked by any same-owner ambiguity, unsafe cross-owner UUID assignment, keyless row, or change in their redacted stable fingerprint."
    - "The production-pending UUID bridge no longer fails when historical drift left call_share_access_log absent; its only 00003 change is a guarded comment that is a semantic no-op on TEST."
    - "A forward-only ninth migration restores call_share_access_log after the guarded bridge, while remaining safe and idempotent where the table already exists."
    - "Anonymous share access can be logged with a null accessor, owners can read only logs for links they own, and browser clients cannot forge log rows."
    - "A guarded canary tool provisions and cleans one isolated TEST or production graph with exactly six synthetic auth users, never customer data."
    - "Migration 00009 is applied only to the dedicated TEST project in this plan, and a new committed-tree fingerprint receives a full Plan 15-equivalent PASS before production is reconsidered."
  artifacts:
    - path: "supabase/migrations/20260919000009_phase38_restore_share_access_log.sql"
      provides: "Forward-only idempotent repair for the missing share access audit table"
      contains: "CREATE TABLE IF NOT EXISTS public.call_share_access_log"
    - path: "supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql"
      provides: "Production-safe bridge ordering when the historical access-log table is absent"
      contains: "to_regclass('public.call_share_access_log')"
    - path: "scripts/phase38-production-canary.ts"
      provides: "Target-guarded six-user synthetic canary provision, inventory, verification, and cleanup commands"
    - path: ".planning/phases/38-access-policy-share-link-key-migration-request-flow/38-REMEDIATION-EVIDENCE.md"
      provides: "TEST apply, absence-drift, unresolved-baseline, canary-cycle, and residue evidence"
    - path: ".planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PREPRODUCTION-VERIFICATION.md"
      provides: "New nine-migration fingerprint-bound production authorization or exact STOP"
  key_links:
    - from: "src/test/migrations/phase38-restore-share-access-log.integration.test.ts"
      to: "supabase/migrations/20260919000009_phase38_restore_share_access_log.sql"
      via: "transactional absent-table replay plus idempotent existing-table replay"
      pattern: "phase38_restore_share_access_log"
    - from: "scripts/phase38-production-canary.ts"
      to: "production legacy bridge inventory"
      via: "aggregate guard and SHA-256 over sorted unresolved-row identity retained only in process"
      pattern: "unresolved.*fingerprint"
    - from: "38-PREPRODUCTION-VERIFICATION.md"
      to: "38-18-PLAN.md"
      via: "matching source fingerprint, nine-migration set, and approved unresolved baseline"
      pattern: "PRODUCTION-GATE: PASS"
---

<objective>
Close the two blockers exposed by the stopped production rollout without changing customer rows or weakening the legacy bridge.

Purpose: Guard the one production-pending 00003 statement that assumes the drifted table exists, repair the table through forward-only migration 00009, replace the incorrect zero-unresolved gate with a preservation invariant for the two known unavailable legacy rows, and create a safe synthetic canary lifecycle for TEST and production.
Output: Migration 00009, regression tests, a guarded six-user canary tool, TEST-only apply evidence, and a fresh full preproduction PASS or precise STOP.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/V2.2-COMPLETION-PLAN.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-CONTEXT.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-15-PLAN.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-15-SUMMARY.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-16-PLAN.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-16-STOP.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PRODUCTION-DEPLOYMENT-EVIDENCE.md
@supabase/migrations/20260108000001_create_single_call_share_tables.sql
@supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql
@src/test/migrations/phase38-access-migrations.test.ts
@src/test/phase38-fixtures.ts
@e2e/helpers/phase38-test-fixtures.ts
@supabase/functions/share-call/__tests__/share-call.integration.test.ts
@supabase/CLAUDE.md
@AGENTS.md
</context>

## Gap closure contract

- Make exactly one minimal guarded edit to TEST-applied but production-pending `20260919000003_phase38_share_link_uuid_bridge.sql`: wrap its `COMMENT ON TABLE public.call_share_access_log` in a `DO`/`to_regclass` existence check. Do not change bridge columns, backfill, constraints, indexes, inventory, RPCs, grants, or token behavior. Document this as a semantic no-op on TEST and invalidate the old source fingerprint.
- Repair the production absence drift through new forward-only migration `20260919000009_phase38_restore_share_access_log.sql`; production must still apply the complete pending sequence `00001` through `00009` in order.
- Preserve the two production legacy-only unresolved rows exactly as legacy rows. Their continued presence is expected. They must remain unavailable for content resolution and must not be assigned an arbitrary recording UUID.
- The safe bridge invariant is: unresolved count exactly `2` with the approved stable redacted fingerprint and classification breakdown (one source absent, one source present only under another owner); ambiguous owner-scoped matches `0`; unsafe cross-owner UUID assignments `0`; rows with neither key `0`.
- No customer row may be rewritten, deleted, used as a mutable canary, or copied into evidence. No raw token, user ID, email, provider key, database URL, service key, or response body may be logged or committed.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore the missing share access log and lock the corrected legacy invariant in tests</name>
  <files>supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql, supabase/migrations/20260919000009_phase38_restore_share_access_log.sql, src/test/migrations/phase38-access-migrations.test.ts, src/test/migrations/phase38-restore-share-access-log.integration.test.ts, supabase/functions/share-call/__tests__/share-call.integration.test.ts</files>
  <behavior>
    - "On a TEST transaction where call_share_access_log is absent, modified 00003 reaches completion because only the table comment is guarded; 00009 then recreates the table, UUID primary key, share-link FK, nullable auth-user accessor FK, timestamp/IP fields, required indexes, RLS, owner-only SELECT policy, service-role write access, and comments; rollback leaves TEST unchanged."
    - "On a schema where the table already exists, running 00009 twice succeeds without losing rows, changing link IDs, or making accessed_by_user_id required."
    - "An anonymous access log with accessed_by_user_id NULL can be inserted by service role; the link owner can read it, an unrelated authenticated user cannot, and authenticated/anon clients cannot insert it."
    - "A legacy-only unresolved token returns the same generic unavailable result before and after migration; an owner may list/revoke the link as legacy_unresolved without receiving an arbitrary recording_id."
    - "Ambiguous same-owner matches, unsafe cross-owner UUID assignments, and rows with neither canonical nor legacy key fail their respective bridge or constraint gates; a cross-owner-only candidate remains unresolved and unavailable."
  </behavior>
  <action>
    Write the RED tests first. In 00003, replace only the unconditional `COMMENT ON TABLE public.call_share_access_log` with a `DO` block that checks `to_regclass('public.call_share_access_log') IS NOT NULL` before executing that same comment. Preserve every other byte of 00003 behavior and document why this is a semantic no-op on the TEST schema where the table already exists. This is the smallest necessary exception to the usual no-edit rule because production has not applied 00003 and would otherwise fail before later 00009 can run. The changed source fingerprint invalidates the Plan 15 authorization and must be re-proven in Task 3.

    Then add `20260919000009_phase38_restore_share_access_log.sql`. Make 00009 safe for both historical shapes: `CREATE TABLE IF NOT EXISTS public.call_share_access_log`, reconcile the original columns without removing data, make `accessed_by_user_id` nullable for anonymous access, retain a foreign key to `auth.users`, retain the required `share_link_id` cascade FK, create the share-link/accessor/access-time indexes with `IF NOT EXISTS`, enable RLS, and install an owner-only SELECT policy through `public.call_share_links`. Revoke direct mutation from `PUBLIC`, `anon`, and `authenticated`; grant authenticated owner reads and service-role operations. Fully qualify policy objects and add table/column comments. Keep the migration forward-only and idempotent.

    Extend the static migration ledger to include both previously omitted `00008` and new `00009`, assert all nine exact filenames, require the 00003 `DO`/`to_regclass` guard, forbid any other bridge semantic change and destructive share-row/log changes, and require the repair DDL/RLS/grants/comments. In the new real-DB migration test, hard-reject any ref except TEST `swjzxiddcrtaqixsfaac`; use `SUPABASE_TEST_DB_URL` and one transaction to simulate the absent-table production shape, replay the modified 00003, execute 00009, inspect catalog behavior, replay 00009, then always roll back. Extend the share-call real-DB suite so unresolved, ambiguous, cross-owner, and no-key cases prove the corrected invariant. Per D-01 and ACCESS-06, unresolved rows stay unavailable; per D-03/D-04 existing valid share paths remain unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/test/migrations/phase38-access-migrations.test.ts src/test/migrations/phase38-restore-share-access-log.integration.test.ts supabase/functions/share-call/__tests__/share-call.integration.test.ts --maxWorkers=1 --reporter=verbose</automated>
  </verify>
  <done>Migration 00009 safely repairs the absent and existing table shapes, access logging supports anonymous access without client forgery, and every legacy resolution class has a fail-closed regression test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the exact six-user guarded canary lifecycle and redacted legacy inventory</name>
  <files>scripts/phase38-production-canary.ts, scripts/__tests__/phase38-production-canary.test.ts</files>
  <behavior>
    - "Provision creates exactly six auth users and one uniquely marked isolated graph: owner, organization admin, team/org-only actor, coach/unverified actor, verified confirmed participant/requester, and invitee-only actor."
    - "A TEST command refuses production credentials; a production-mutating command refuses to run without the exact production ref plus an explicit confirmation argument."
    - "A partially failed provision immediately cleans only IDs created for that run; cleanup is idempotent and proves zero graph rows and zero auth users remain."
    - "Inventory reports only aggregate counts and a stable SHA-256 fingerprint for sorted unresolved-row identity; it never prints raw IDs, provider keys, emails, tokens, URLs, service keys, or row payloads."
    - "The production legacy gate accepts exactly two unchanged unresolved rows, including one stable cross-owner-only candidate, and rejects any changed fingerprint/classification, ambiguity, unsafe UUID assignment, or keyless share row."
  </behavior>
  <action>
    Implement `scripts/phase38-production-canary.ts` as an npm/tsx-compatible command with explicit `inventory`, `provision`, `verify`, and `cleanup` actions. Require `--target test|production` and an exact project-ref assertion before constructing a service client. For production mutation require a second explicit `--confirm-production vltmrnjsubfzrgrtdqey`; TEST must require `swjzxiddcrtaqixsfaac`. Never infer a target from the resting Supabase link.

    Provision exactly six synthetic auth users with reserved non-routable `phase38.invalid` addresses and a cryptographically random run ID: owner, organization admin, team/org-only actor, coach/unverified actor, verified confirmed participant/requester, and invitee-only actor. Create a dedicated isolated business organization and team workspace. Give organization membership only to owner/admin/team; give baseline workspace membership only to owner. Keep coach, confirmed participant, and invitee outside the organization. Reuse the coach actor for the unverified-participant denial before creating its temporary direct share. Add marked UUID-only and legacy-compatible recordings, verified confirmed participation, invitee-only evidence, unverified speaker evidence, and pre-rollout-compatible share data. Every synthetic row must carry or join to the exact marker `source_metadata.integration_test = 'phase-38-production-canary'` plus the run ID. Store the cleanup manifest only at an explicit `/tmp` path with mode `0600`; never print its secret fields. On any partial failure, clean the recorded IDs in dependency order and delete exactly those six auth users through the admin API. Do not call broad fixture-cleanup RPCs in production or use domains targeted by existing broad test-user cleanup jobs.

    The inventory action must evaluate owner-scoped ambiguity, unsafe cross-owner UUID assignments, keyless rows, and unresolved legacy-only rows, including aggregate unresolved classification counts. Compute the stable unresolved fingerprint locally from sorted immutable row identity and record only `sha256:&lt;digest&gt;`, counts, and PASS/STOP. Tests must use injected fake clients to prove target guards, exact user count/roles, partial-failure cleanup, manifest permissions/shape, fingerprint stability/order independence, redaction, and zero-residue enforcement. Per D-07 through D-17, the graph must support privacy, request, approval, denial, revocation, and role probes without any customer substitution.
  </action>
  <verify>
    <automated>npx vitest run scripts/__tests__/phase38-production-canary.test.ts --reporter=verbose</automated>
  </verify>
  <done>The same reviewed tool can safely create, inspect, and completely remove the six-user synthetic graph on TEST or production while exposing only redacted evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Apply 00009 to TEST, prove canary cleanup, and issue a new nine-migration fingerprint-bound gate</name>
  <files>.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-REMEDIATION-EVIDENCE.md, .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PREPRODUCTION-VERIFICATION.md, .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-VALIDATION.md</files>
  <action>
    Commit Tasks 1 and 2 before computing evidence. Hard-stop unless branch is `v2.2-event-resolution`, every non-planning path is clean, and secure DB/API credentials independently resolve to TEST ref `swjzxiddcrtaqixsfaac`; reject production ref `vltmrnjsubfzrgrtdqey`. Verify TEST migration history is exactly `00001` through `00008`, apply only `20260919000009_phase38_restore_share_access_log.sql` to TEST through the database URL, then require history `00001` through `00009` in order and introspect the repaired table, nullable accessor, FKs, indexes, RLS, policy, grants, and comments.

    Run one TEST `provision -> verify -> cleanup -> verify-zero-residue` canary cycle and prove exactly six synthetic users were created and removed. Then rerun the complete Plan 15 gate from the committed tree: focused Phase 38/static tests (including both new suites), the full serial real-DB integration/RLS suite, `npm test`, type check, lint, committed-tree build, and serial Phase 38 Chromium/axe/privacy suite with zero skips and TEST-only Vite variables. Recheck D-01 through D-22, ACCESS-01 through ACCESS-09, and EVT-06. Record exact commands, exit codes, test counts, browser evidence paths, TEST target proof, and cleanup result.

    Finally run the canary tool's inventory action read-only against production. Require exactly two legacy-only unresolved rows with the stable classification breakdown (one source absent and one source present only under another owner), zero ambiguous rows, zero unsafe cross-owner UUID assignments, and zero rows with neither key; record only aggregate counts and the stable redacted fingerprint. Do not fetch content and do not mutate production. Replace the old eight-migration authorization with a new evidence block containing exact lines `Verified application source commit: &lt;sha&gt;`, `Verified application source fingerprint: &lt;fingerprint&gt;`, `Authorized Phase 38 migrations: 20260919000001..20260919000009`, `Authorized unresolved legacy count: 2`, and `Authorized unresolved legacy fingerprint: sha256:&lt;digest&gt;`. Append `PRODUCTION-GATE: PASS` only when every command, catalog assertion, canary cleanup, and production read-only invariant is green; otherwise append STOP with the exact blocker. Update the validation ledger only for evidence actually produced.
  </action>
  <verify>
    <automated>e=.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PREPRODUCTION-VERIFICATION.md; rg -n '^Verified application source commit: [0-9a-f]{40}$|^Verified application source fingerprint: [0-9a-f]{40}$|^Authorized Phase 38 migrations: 20260919000001\.\.20260919000009$|^Authorized unresolved legacy count: 2$|^Authorized unresolved legacy fingerprint: sha256:[0-9a-f]{64}$|^PRODUCTION-GATE: PASS$' "$e" &amp;&amp; test "$(git ls-tree -r --full-tree HEAD | sed '/\t\.planning\//d' | git hash-object --stdin)" = "$(sed -n 's/^Verified application source fingerprint: //p' "$e")"</automated>
  </verify>
  <done>TEST contains all nine migrations, the exact six-user canary leaves zero residue, the corrected production legacy baseline is redacted and stable, and the current non-planning tree has a full PASS or an explicit STOP.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: legacy share tokens/rows/logs, auth users, TEST and production project boundaries, service-role credentials, production authorization evidence.
- Boundaries: local script to Supabase Auth/Postgres; migration SQL to TEST; synthetic graph to existing customer data; redacted inventory to committed evidence.

| Threat ID | Category | Severity | Threat | Disposition | Mitigation and proof |
|---|---|---:|---|---|---|
| T-38-17-01 | Tampering | CRITICAL | The required 00003 production-order fix changes already-applied TEST history or bridge semantics | mitigate | Restrict diff to guarded COMMENT only, prove semantic no-op on TEST and absent-table success, invalidate/re-run fingerprint |
| T-38-17-02 | Elevation of privilege | CRITICAL | Restored log table permits forged inserts or cross-owner reads | mitigate | RLS owner SELECT, revoked browser mutation, service-role write, real-DB role tests |
| T-38-17-03 | Information disclosure | CRITICAL | Inventory or manifest leaks customer IDs, PII, tokens, or service secrets | mitigate | Aggregate/hash output only; mode-0600 `/tmp` manifest; redaction unit tests and evidence scan |
| T-38-17-04 | Tampering | CRITICAL | An unresolved legacy row is silently mapped to the wrong recording | mitigate | Preserve exactly two fingerprinted unresolved rows; zero ambiguity, unsafe assignment, and keyless gates; unavailable-response tests |
| T-38-17-05 | Denial of service | HIGH | Partial canary provisioning leaves auth/data residue | mitigate | Per-run manifest, failure cleanup, idempotent cleanup, exact six-user/zero-residue assertions |
| T-38-17-06 | Repudiation | HIGH | New PASS is reused after source drift | mitigate | Commit and full non-planning fingerprint plus exact migration and baseline lines |
| T-38-17-SC | Tampering | LOW | New package introduces unreviewed supply-chain code | mitigate | No package install or lockfile change; use existing Supabase, pg, tsx, and Vitest dependencies |
</threat_model>

<source_audit>

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Per-recording access, privacy-safe discovery/request flow, UUID bridge, existing sharing untouched | Tasks 1-3 plus Plans 01-16 | COVERED |
| REQ | ACCESS-01 through ACCESS-05, ACCESS-07 through ACCESS-09 | Task 3 full Plan 15-equivalent rerun | COVERED |
| REQ | ACCESS-06 preserve links, tokens, logs, and UUID bridge | Tasks 1-3 | COVERED |
| REQ | EVT-06 copy functions preserve event_id | Task 3 full gate | COVERED |
| RESEARCH | Additive schema, real TEST DB, target guard, legacy compatibility, fail-closed rollout | Tasks 1-3 | COVERED |
| CONTEXT | D-01 through D-06 existing access/default/override behavior | Tasks 1 and 3 | COVERED |
| CONTEXT | D-07 through D-11 privacy and participation rules | Tasks 2 and 3 | COVERED |
| CONTEXT | D-12 through D-17 request lifecycle | Tasks 2 and 3 | COVERED |
| CONTEXT | D-18 through D-22 settings and per-recording UI contract | Task 3 browser rerun | COVERED |

</source_audit>

<verification>
No production write is allowed in this plan. Migration 00009 is applied only to TEST. Production access is limited to target proof and aggregate/redacted read-only inventory. The final gate must bind the exact nine-migration non-planning tree and corrected two-row legacy invariant.
</verification>

<success_criteria>
- Modified 00003 differs only by the guarded table comment, succeeds with the table absent, and is proven a semantic no-op on TEST.
- Migration 00009 restores the absent audit table safely and is idempotent on the existing TEST shape.
- Static and real-DB tests prove anonymous nullable logging, owner-only reads, unavailable unresolved links, and zero ambiguity, unsafe assignment, and keyless cases.
- The canary tool creates exactly six synthetic auth users in an isolated marked graph and removes all rows/users on success or failure.
- TEST alone advances from eight to nine Phase 38 migrations and passes every Plan 15-equivalent gate.
- New preproduction evidence records a matching source fingerprint, exact nine-migration set, unresolved count `2`, stable redacted fingerprint, and PASS or exact STOP.
</success_criteria>

<output>
Create `.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-17-SUMMARY.md` when done.
</output>
