---
phase: 38-access-policy-share-link-key-migration-request-flow
plan: "13"
type: execute
wave: 7
depends_on: ["38-10", "38-11", "38-12"]
files_modified:
  - src/components/sharing/RecordingAccessPanel.tsx
  - src/components/sharing/__tests__/RecordingAccessPanel.test.tsx
  - src/components/call-detail/CallDetailHeader.tsx
autonomous: true
requirements: [ACCESS-01, ACCESS-05, ACCESS-06, ACCESS-08]
must_haves:
  truths:
    - "D-19 owner-only ACCESS appears immediately before SHARE and opens Popover on desktop/tablet or one Dialog tree on mobile."
    - "D-20 copy-only notice and existing team/coach/share helper remain visible in loading, error, and loaded states."
    - "D-21 Public confirmation, D-22 inherited/custom/reset states, pending requests, owner review, active grants, deny, and revoke match the UI contract."
    - "Existing Share dialog remains a separate unchanged control."
  artifacts:
    - path: "src/components/sharing/RecordingAccessPanel.tsx"
      provides: "Responsive recording policy/request/grant management surface"
    - path: "src/components/call-detail/CallDetailHeader.tsx"
      provides: "Owner-only ACCESS entry beside SHARE"
  key_links:
    - from: "CallDetailHeader ACCESS"
      to: "RecordingAccessPanel"
      via: "controlled open state and canonical UUID"
      pattern: "RecordingAccessPanel"
    - from: "RecordingAccessPanel"
      to: "useAccessPolicy|useRecordingAccess"
      via: "hooks only"
      pattern: "useRecordingAccess"
---

<objective>
Implement the compact owner-only Recording access management surface without altering the existing Share experience.

Purpose: Let owners manage one recording's policy, requests, and individual grants in the approved one-click UI.
Output: Responsive access panel and header entry point satisfying D-19 through D-22.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-UI-SPEC.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-11-SUMMARY.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-12-SUMMARY.md
@src/components/call-detail/CallDetailHeader.tsx
@src/components/notifications/NotificationBell.tsx
@src/hooks/useBreakpoint.ts
@src/components/sharing/__tests__/RecordingAccessPanel.test.tsx
@src/CLAUDE.md
</context>

## Artifacts this phase produces

- `RecordingAccessPanel` with shared picker, policy badge/reset, pending review, active grants/revoke, exact notice, loading/error/empty/conflict/live states, desktop Popover and mobile Dialog.
- Owner-only `ACCESS` button before `SHARE` in `CallDetailHeader`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the responsive access management panel</name>
  <read_first>
    - src/components/sharing/RecordingAccessPanel.tsx (create; no prior file)
    - src/components/access/AccessLevelPicker.tsx
    - src/hooks/useAccessPolicy.ts
    - src/hooks/useRecordingAccess.ts
    - src/hooks/useBreakpoint.ts
    - src/components/notifications/NotificationBell.tsx
    - src/components/ui/popover.tsx
    - src/components/ui/dialog.tsx
    - src/components/ui/alert-dialog.tsx
    - src/components/sharing/__tests__/RecordingAccessPanel.test.tsx
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-UI-SPEC.md
  </read_first>
  <files>src/components/sharing/RecordingAccessPanel.tsx, src/components/sharing/__tests__/RecordingAccessPanel.test.tsx</files>
  <behavior>
    - Heading/notice/helper always render; data regions skeleton or show exact load error/Retry without editable stale controls.
    - Policy shows “Using default: [level]” or “Custom”; explicit selection creates Custom; Reset snapshots current default; Public confirms.
    - Pending request review shows D-13 fields in order, no message field, one-click approve, confirm-to-deny; grants show revoke confirmation.
    - Concurrent/resolved/unavailable states and all success/failure copy match UI spec.
  </behavior>
  <action>
    Build one shared content tree rendered inside Popover for desktop/tablet and Dialog for mobile based on `useBreakpointFlags()`. Use ScrollArea, shared picker, badges, skeletons, separators, Remix icons, exact empty/error/toast/live-region copy, and row-scoped pending states. Render pending requests before grants. Implement Public, Deny, and Revoke AlertDialogs with exact variants/focus behavior. Do not put share-link management or direct service/Supabase calls in this panel. Convert the five existing `it.fails` acceptance contracts in `RecordingAccessPanel.test.tsx` to active tests as each behavior becomes implemented; the plan is not complete while those contracts remain expected failures.
  </action>
  <acceptance_criteria>
    - Exact D-20 sentence and team/coach/share helper are present in loading/error/loaded renders.
    - Requests/grants remain distinct from team/coach/org/public/share access.
    - Mobile uses one Dialog interactive tree and 44px minimum key actions; desktop panel is 400px/max viewport and 70vh ScrollArea.
    - All component acceptance tests pass.
    - `RecordingAccessPanel.test.tsx` contains no remaining `it.fails` contracts for Plan 13 behavior.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/components/sharing/__tests__/RecordingAccessPanel.test.tsx</automated>
  </verify>
  <done>The complete owner management workflow is compact, responsive, accessible, and copy-accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Add the owner-only ACCESS entry beside SHARE</name>
  <read_first>
    - src/components/call-detail/CallDetailHeader.tsx
    - src/components/sharing/RecordingAccessPanel.tsx
    - src/components/sharing/ShareCallDialog.tsx
    - src/contexts/AuthContext.tsx
    - src/types/index.ts
    - src/components/sharing/__tests__/RecordingAccessPanel.test.tsx
  </read_first>
  <files>src/components/call-detail/CallDetailHeader.tsx</files>
  <action>
    Determine ownership from the authenticated user and `call.owner_user_id`. For the owner only, render a hollow small ACCESS button with a Remix lock/shield icon immediately before SHARE and control `RecordingAccessPanel` with canonical `call.canonical_uuid`. Do not render a disabled/tooltip hint for non-owners. Preserve the separate Share dialog and the UUID fix from Plan 10. Expose controlled props needed by Plan 14 for deep-link focus without automatically opening yet.
  </action>
  <acceptance_criteria>
    - Owner sees ACCESS immediately before SHARE; non-owner sees no ACCESS element.
    - Button and panel use canonical UUID only.
    - Existing SHARE, MOVE/COPY, refresh, edit, and title behavior remains green.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/components/sharing/__tests__/RecordingAccessPanel.test.tsx &amp;&amp; npm run type-check</automated>
  </verify>
  <done>The access surface is reachable only by the recording owner and does not absorb or regress sharing.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: owner controls, requester verified PII/evidence, active grants, policy state.
- Boundaries: authenticated user/call model to owner-only control; owner RPC response to review UI; responsive surface to focus management.

| Threat ID | Category | Severity | Threat | Mitigation | Verification |
|---|---|---:|---|---|---|
| T-38-13-01 | Information disclosure | HIGH | Non-owner sees control or requester evidence | Owner-only render plus owner-authorized management RPC | Non-owner component test |
| T-38-13-02 | Tampering | HIGH | Accidental Public/deny/revoke action | Required confirmations and server owner checks | Dialog/action tests |
| T-38-13-03 | Repudiation | MEDIUM | UI implies policy controls other copies | Notice always visible in every state | Loading/error/loaded assertions |
| T-38-13-04 | Tampering | MEDIUM | Concurrent owner actions render stale result | Row lock/stable code, refetch and explicit conflict copy | Conflict test |
| T-38-13-SC | Tampering | LOW | Banned component/icon package | Existing shadcn and Remix only | Import/lockfile scan |
</threat_model>

<verification>
Run focused panel tests and type check; preserve Plan 10 sharing tests when resolving the shared header file.
</verification>

<success_criteria>
- D-19 through D-22 are fully implemented.
- Request/grant management is owner-only and role-safe.
- Existing sharing remains separate and unchanged.
</success_criteria>

<output>
Create `.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-13-SUMMARY.md` when done.
</output>
