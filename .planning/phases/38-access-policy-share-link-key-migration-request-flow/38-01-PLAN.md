---
phase: 38-access-policy-share-link-key-migration-request-flow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/integration-setup.ts
  - src/test/phase38-fixtures.ts
  - src/test/phase38-fixtures.integration.test.ts
  - src/test/fixtures/phase38-provider-event-kind.ts
  - supabase/functions/share-call/__tests__/share-call.integration.test.ts
autonomous: true
requirements: [ACCESS-01, ACCESS-02, ACCESS-03, ACCESS-04, ACCESS-05, ACCESS-07, ACCESS-08, ACCESS-09, EVT-06]
must_haves:
  truths:
    - "No Phase 38 integration client can be created without dedicated-test credentials, and the production ref vltmrnjsubfzrgrtdqey is rejected before any network or database operation."
    - "Reusable fixtures can create isolated users, organizations, events, UUID recordings, verified aliases, confirmed/invitee participants, legacy share rows, requests, grants, and audit rows without using customer data."
    - "Checked-in provider fixtures classify Zoom 5/6/9 as webinar, Zoom 1/2/3/4/7/8/99 as non_webinar, and every malformed, unknown, other-provider, and internal-source signal as unknown."
    - "The existing share-call suite defines the preserved old-token behavior and the new UUID-only behavior before the bridge is implemented."
  artifacts:
    - path: "src/test/integration-setup.ts"
      provides: "Central dedicated-test environment guard and real Supabase clients"
    - path: "src/test/phase38-fixtures.ts"
      provides: "Deterministic Phase 38 real-database fixture factory and cleanup"
    - path: "src/test/phase38-fixtures.integration.test.ts"
      provides: "Two-cycle real-database fixture creation/cleanup residue smoke test"
    - path: "src/test/fixtures/phase38-provider-event-kind.ts"
      provides: "Complete provider signal and event aggregation expectation matrix"
    - path: "supabase/functions/share-call/__tests__/share-call.integration.test.ts"
      provides: "Legacy-token and UUID-token compatibility acceptance matrix"
  key_links:
    - from: "src/test/phase38-fixtures.ts"
      to: "src/test/integration-setup.ts"
      via: "guarded service-role and signed-in test clients"
      pattern: "makeIntegrationClient|assertDedicatedTestProject"
    - from: "supabase/functions/share-call/__tests__/share-call.integration.test.ts"
      to: "call_share_links.recording_id"
      via: "real dedicated-test project fixtures"
      pattern: "recording_id|call_recording_id"
---

<objective>
Build the non-negotiable test-environment hard stop and shared real-database fixtures before any Phase 38 migration is authored.

Purpose: Prevent the destructive integration-test incident class and pin the compatibility behavior required by D-01, D-07 through D-11, and ACCESS-07/08 before schema work begins.
Output: A centralized production-ref rejection guard, reusable Phase 38 fixtures, and a RED share-link compatibility matrix.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/V2.2-COMPLETION-PLAN.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-CONTEXT.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-RESEARCH.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PATTERNS.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-VALIDATION.md
@AGENTS.md
@supabase/CLAUDE.md
</context>

## Artifacts this phase produces

- `src/test/integration-setup.ts`: `assertDedicatedTestProject()`, `integrationDbReachable`, guarded service-role and anon client factories.
- `src/test/phase38-fixtures.ts`: `createPhase38FixtureGraph()`, signed-in role clients, controlled legacy/UUID share fixtures, and `cleanupPhase38FixtureGraph()`.
- `src/test/phase38-fixtures.integration.test.ts`: real dedicated-test-project smoke coverage that runs two complete create/cleanup cycles and proves zero residue.
- `src/test/fixtures/phase38-provider-event-kind.ts`: Zoom documented mappings, malformed/future values, all other supported providers/internal sources, aggregation combinations, and 49/50 expectations.
- `supabase/functions/share-call/__tests__/share-call.integration.test.ts`: old-token preservation, UUID-only non-Fathom create/resolve/revoke, wrong-recipient, expired/revoked, and production-ref rejection cases.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce the dedicated-test environment boundary</name>
  <read_first>
    - src/test/integration-setup.ts
    - package.json
    - supabase/functions/share-call/__tests__/share-call.integration.test.ts
    - .env.test.example
    - .planning/V2.2-COMPLETION-PLAN.md
    - supabase/CLAUDE.md
  </read_first>
  <files>src/test/integration-setup.ts, supabase/functions/share-call/__tests__/share-call.integration.test.ts</files>
  <behavior>
    - Missing `VITE_SUPABASE_TEST_URL`, `VITE_SUPABASE_TEST_ANON_KEY`, or `SUPABASE_TEST_SERVICE_ROLE_KEY` disables or fails the integration suite before a client is created.
    - A URL whose hostname contains project ref `vltmrnjsubfzrgrtdqey` throws a stable production-target error before client construction.
    - Production-named URL/key fallbacks are absent from the share-call suite.
    - Project ref `swjzxiddcrtaqixsfaac` is accepted when the required test keys are present.
  </behavior>
  <action>
    Refactor the integration bootstrap around an exported `assertDedicatedTestProject(url)` that parses the hostname/project ref, rejects missing/invalid URLs, and hard-rejects `vltmrnjsubfzrgrtdqey`. Make every client factory call the guard before `createClient`. Remove `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`, and `SUPABASE_SERVICE_ROLE_KEY` fallbacks from the share-call integration test. Do not print key values. Preserve the serial real-database runner and explicit skip semantics for genuinely unavailable test credentials.
  </action>
  <acceptance_criteria>
    - `rg -n "VITE_SUPABASE_URL|VITE_SUPABASE_PUBLISHABLE_KEY|SUPABASE_SERVICE_ROLE_KEY" supabase/functions/share-call/__tests__/share-call.integration.test.ts` returns no production fallback use.
    - `rg -n "vltmrnjsubfzrgrtdqey" src/test/integration-setup.ts` finds the hard stop.
    - A targeted test passes for missing credentials, production ref rejection, and test ref acceptance without making a remote call.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration</automated>
  </verify>
  <done>All Phase 38 integration clients fail closed before touching production and share-call no longer falls back to production-named variables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create isolated Phase 38 fixture graphs</name>
  <read_first>
    - src/test/phase38-fixtures.ts (create; no prior file)
    - src/test/phase38-fixtures.integration.test.ts (create; no prior file)
    - src/test/fixtures/phase38-provider-event-kind.ts (create; no prior file)
    - src/test/integration-setup.ts
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-RESEARCH.md
    - src/config/source-registry.ts
    - supabase/functions/zoom-sync-meetings/index.ts
    - supabase/functions/zoom-webhook/index.ts
    - src/services/__tests__/data-movement.dedup.integration.test.ts
    - src/test/rls-regression.test.ts
    - supabase/migrations/20260905140000_create_identities_and_link_tables.sql
    - supabase/migrations/20260831000001_create_events_and_extend_participants.sql
    - supabase/migrations/20260108000001_create_single_call_share_tables.sql
  </read_first>
  <files>src/test/phase38-fixtures.ts, src/test/phase38-fixtures.integration.test.ts, src/test/fixtures/phase38-provider-event-kind.ts</files>
  <behavior>
    - Fixtures create owner, admin, team/workspace member, coach, verified confirmed participant, invitee-only, unrelated, grant recipient, and anonymous clients.
    - Provider fixtures cover Zoom values 1,2,3,4,5,6,7,8,9,99, null, a non-integer/malformed value, and an unknown future integer with exact tri-state expectations.
    - Fathom, Fireflies, Read.ai, Grain, Plaud, YouTube, file-upload, paste-transcript, and manual-mcp-import fixtures all expect unknown; Grain includes a free-form name containing “webinar” and still expects unknown.
    - Events can aggregate webinar+unknown, non_webinar+unknown, unknown-only, conflicting webinar/non_webinar, and all-known non_webinar copies, with 49 and 50 distinct confirmed identities.
    - Cleanup deletes dependent rows in FK-safe order, checks every deletion error, and removes test auth users last.
  </behavior>
  <action>
    Add a timestamp/UUID-prefixed fixture module that uses only the guarded service-role client for setup and real signed-in anon clients for RLS behavior. Model the full authorization matrix, verified `identity_aliases`, `call_participants.sources`, roles and confirmation evidence, UUID-only non-Fathom recordings, and legacy BIGINT share rows. Add checked-in typed provider cases for the exact Zoom mapping and every supported non-Zoom/internal source listed above; never infer from title, tags, platform, `live_enabled`, Grain free-form taxonomy, filenames, descriptions, or audience counts. Add event aggregation builders for every required combination and the independent 49/50 boundary. Create a real `.integration.test.ts` smoke suite that performs create → assert → cleanup → zero-residue twice using distinct prefixes and verifies the second cycle is not relying on first-cycle state. Never borrow or mutate customer rows; never mock Supabase. Make cleanup idempotent and independently guarded so one failed delete does not skip the rest.
  </action>
  <acceptance_criteria>
    - The fixture module exports typed setup and cleanup APIs and contains no `any`.
    - Every created table/auth resource has a cleanup path with checked errors.
    - `phase38-fixtures.integration.test.ts` executes two complete create/cleanup cycles against `swjzxiddcrtaqixsfaac`, queries every touched table/auth resource by fixture prefix after each cleanup, and asserts zero residue.
    - The provider fixture matrix contains every exact Zoom case, every supported non-Zoom provider, all three internal sources, all five aggregation signal combinations, and 49/50 counts.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration</automated>
  </verify>
  <done>Subsequent plans can exercise every required identity and boundary using isolated real-database fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin old-token and UUID-native share behavior in RED tests</name>
  <read_first>
    - supabase/functions/share-call/__tests__/share-call.integration.test.ts
    - src/test/phase38-fixtures.ts
    - supabase/functions/share-call/index.ts
    - supabase/functions/mcp-server/tools/write/create_share_link.ts
    - supabase/functions/mcp-server/tools/read/list_shared_calls.ts
    - src/hooks/useSharing.ts
    - src/types/sharing.ts
  </read_first>
  <files>supabase/functions/share-call/__tests__/share-call.integration.test.ts</files>
  <behavior>
    - A legacy-only row keeps the same row ID, token, status, recipient, and access-log relationship after the bridge.
    - A UUID-only non-Fathom recording can create, resolve, list, and revoke a share link.
    - Existing wrong-recipient, expired, revoked, anonymous, owner, and access-log outcomes remain unchanged.
  </behavior>
  <action>
    Extend the real-DB suite with pre-migration legacy fixtures and UUID-native expectations. Assert exact compatibility fields and `/s/<token>` semantics; do not regenerate or replace old tokens. Mark bridge-dependent assertions as the intentional RED contract until Plans 05-07 implement and deploy the bridge to the dedicated test project.
  </action>
  <acceptance_criteria>
    - The suite asserts token and row identity preservation, not just a 200 response.
    - The UUID-only fixture has `fathom_provider_id IS NULL` and proves no numeric coercion path is required.
    - Failures before implementation name missing `recording_id`/UUID behavior rather than environment or fixture errors.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration</automated>
  </verify>
  <done>The share-link bridge has a precise real-database compatibility contract before implementation.</done>
</task>

</tasks>

<threat_model>
## Assets

- Production Supabase data, service-role secrets, share tokens, and test user credentials.

## Trust boundaries

- Local environment variables to Supabase client construction.
- Service-role fixture setup to user-scoped RLS probes.
- Public share token to Edge Function response.

## Threats and mitigations

| Threat ID | Category | Severity | Threat | Mitigation | Verification |
|---|---|---:|---|---|---|
| T-38-01-01 | Tampering | HIGH | Integration tests mutate production | Hard-coded prod-ref rejection before client creation; test-only variables; no fallbacks | Targeted production-ref rejection test and source scan |
| T-38-01-02 | Information disclosure | HIGH | Secrets or customer rows enter fixtures/logs | Synthetic users and labeled rows only; never log keys/tokens; no donor/customer records | Fixture source review and cleanup inventory |
| T-38-01-03 | Repudiation | MEDIUM | Cleanup silently fails and leaves fixtures | Check every cleanup result and report table/resource name | Two-run idempotency smoke test |
| T-38-01-SC | Tampering | LOW | Supply-chain change alters test runtime | No package installs; lockfile must remain unchanged | `git diff --exit-code -- package.json package-lock.json` |
</threat_model>

<verification>
Run the targeted guard tests and fixture smoke path. Bridge-dependent cases may remain RED only for the named missing schema/function behavior; environment, auth, and cleanup must already be green.
</verification>

<success_criteria>
- Production is unreachable from Phase 38 integration helpers.
- Synthetic fixtures cover the complete authorization matrix and event-size boundaries.
- Existing and UUID-native share behavior is pinned before schema work.
</success_criteria>

<output>
Create `.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-01-SUMMARY.md` when done.
</output>
