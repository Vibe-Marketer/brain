---
phase: 38-access-policy-share-link-key-migration-request-flow
plan: "10"
type: execute
wave: 5
depends_on: ["38-06"]
files_modified:
  - src/types/sharing.ts
  - src/services/sharing.service.ts
  - src/hooks/useSharing.ts
  - src/hooks/__tests__/useSharing.test.ts
  - src/components/sharing/ShareCallDialog.tsx
  - src/components/call-detail/CallDetailHeader.tsx
  - src/components/transcripts/TranscriptsTab.tsx
autonomous: true
requirements: [ACCESS-07, ACCESS-08]
must_haves:
  truths:
    - "Frontend sharing uses canonical UUIDs, pure services, and TanStack hooks with no numeric recording coercion."
    - "Share dialog behavior and existing `/s/<token>` copy/revoke flows remain unchanged."
    - "Shared With Me consumes the UUID-native v3 RPC and yields canonical UUIDs while legacy-only rows still appear."
  artifacts:
    - path: "src/services/sharing.service.ts"
      provides: "Pure UUID-native share CRUD/public-token/shared-with-me operations"
    - path: "src/hooks/useSharing.ts"
      provides: "TanStack wrappers with UUID cache keys"
    - path: "src/types/sharing.ts"
      provides: "Bridge-aware nullable legacy and canonical share types"
  key_links:
    - from: "ShareCallDialog"
      to: "sharing.service.ts"
      via: "useSharing hook"
      pattern: "recording_id"
    - from: "TranscriptsTab Shared With Me"
      to: "get_calls_shared_with_me_v3"
      via: "useSharedWithMe hook/service"
      pattern: "canonical_uuid"
---

<objective>
Complete the frontend UUID share migration and extract all sharing data access behind the required service/hook boundary.

Purpose: Remove current `parseInt`/direct-Supabase sharing paths while preserving the existing user experience and shared-with-me results.
Output: Bridge-aware types, pure sharing service, refactored hooks/dialog/header, and UUID-native Shared With Me integration.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-06-SUMMARY.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PATTERNS.md
@src/hooks/useSharing.ts
@src/components/sharing/ShareCallDialog.tsx
@src/components/transcripts/TranscriptsTab.tsx
@src/lib/recording-ids.ts
@src/CLAUDE.md
</context>

## Artifacts this phase produces

- Bridge-aware `ShareLink` with canonical `recording_id` and nullable legacy `call_recording_id`.
- Pure `sharing.service.ts` functions for links, access logs, token responses, and shared-with-me v3.
- UUID-native `useSharing`, `useSharedCall`, `useAccessLog`, and `useSharedWithMe` hooks.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract sharing data access into a UUID-native service</name>
  <read_first>
    - src/types/sharing.ts
    - src/services/sharing.service.ts (create; no prior file)
    - src/hooks/useSharing.ts
    - src/lib/recording-ids.ts
    - src/types/supabase.ts
    - src/services/data-movement.service.ts
    - supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql
  </read_first>
  <files>src/types/sharing.ts, src/services/sharing.service.ts</files>
  <behavior>
    - New create input is `recording_id: string`; bridge response can carry `call_recording_id: number | null`.
    - Service lists/creates/revokes/loads logs by UUID or link ID and loads shared-with-me v3 canonical rows.
    - Shared token fetch preserves the existing discriminated response union.
  </behavior>
  <action>
    Update sharing types to canonical UUID identity while retaining nullable legacy fields for read compatibility. Move all Supabase/fetch/token generation/data transformation from `useSharing.ts` into pure async service functions. Prefer the authenticated `share-call` Edge API for mutations where it owns authorization, and use the v3 RPC for Shared With Me. Use `toRecordingUuid()` only at a real upstream legacy boundary; never `parseInt`, `Number`, or `String` as identity conversion.
  </action>
  <acceptance_criteria>
    - Service imports no React/TanStack/sonner.
    - `CreateShareLinkInput` requires canonical UUID.
    - Public token response union stays source-compatible for SharedCallView.
    - No recording identity numeric coercion exists in service/types.
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check &amp;&amp; ! rg -n "parseInt\(|Number\(" src/services/sharing.service.ts src/types/sharing.ts</automated>
  </verify>
  <done>All sharing I/O has one pure UUID-aware service boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor sharing hooks and dialog to canonical UUIDs</name>
  <read_first>
    - src/hooks/useSharing.ts
    - src/hooks/__tests__/useSharing.test.ts
    - src/services/sharing.service.ts
    - src/components/sharing/ShareCallDialog.tsx
    - src/components/call-detail/CallDetailHeader.tsx
    - src/lib/query-config.ts
    - src/lib/recording-ids.ts
  </read_first>
  <files>src/hooks/useSharing.ts, src/hooks/__tests__/useSharing.test.ts, src/components/sharing/ShareCallDialog.tsx, src/components/call-detail/CallDetailHeader.tsx</files>
  <behavior>
    - Hooks call the service only, key caches by UUID, preserve current loading/mutation API, and invalidate in `onSettled`.
    - Share dialog submits the canonical UUID and keeps token copy/revoke UX unchanged.
    - Header passes `call.canonical_uuid`; if absent it resolves with the approved recording-ID helper or disables sharing until canonical resolution succeeds.
  </behavior>
  <action>
    Replace hook-internal Supabase access with service calls and update unit mocks to the service boundary. Remove numeric normalization and dialog `parseInt`. Preserve existing toast copy, URL `/s/<token>`, recipient input, active/revoked display, and access-log behavior. Ensure `invalidateCallListCaches` runs on create/revoke `onSettled` where sharing changes visible access.
  </action>
  <acceptance_criteria>
    - `rg -n "\.from\(|\.rpc\(" src/hooks/useSharing.ts` returns no direct data access.
    - `rg -n "parseInt\(|Number\(" src/hooks/useSharing.ts src/components/sharing/ShareCallDialog.tsx src/components/call-detail/CallDetailHeader.tsx` returns no recording conversion.
    - Existing hook/dialog tests pass with UUID fixtures.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/hooks/__tests__/useSharing.test.ts &amp;&amp; npm run type-check</automated>
  </verify>
  <done>Existing sharing UI works through UUID-native service/hook code with no behavior regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Move Shared With Me to the UUID-native RPC</name>
  <read_first>
    - src/components/transcripts/TranscriptsTab.tsx
    - src/hooks/useSharing.ts
    - src/services/sharing.service.ts
    - src/types/sharing.ts
    - supabase/migrations/20260919000003_phase38_share_link_uuid_bridge.sql
    - e2e/sharing.spec.ts
  </read_first>
  <files>src/components/transcripts/TranscriptsTab.tsx</files>
  <behavior>
    - Shared With Me loads through `useSharedWithMe(enabled)` and never calls Supabase directly for share rows.
    - Rows expose canonical UUID as both detail-navigation identity and `canonical_uuid`, including UUID-only non-Fathom recordings.
    - Search, pagination, source label, and legacy recipient visibility remain unchanged.
  </behavior>
  <action>
    Replace the component-embedded v2 RPC branch with the service-backed hook data. Keep hooks unconditional with an `enabled` flag. Map canonical UUID rows into the existing Meeting representation without setting `canonical_uuid: null` or coercing to numbers. Preserve the current virtual workspace search/pagination behavior.
  </action>
  <acceptance_criteria>
    - `get_calls_shared_with_me_v2` no longer appears in TranscriptsTab.
    - A UUID-only shared row opens the correct call detail route.
    - Existing Shared With Me E2E and unit behavior remains green.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/hooks/__tests__/useSharing.test.ts &amp;&amp; npm run type-check</automated>
  </verify>
  <done>Shared With Me is canonical-UUID native and still includes compatible legacy shares.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: share tokens, recipient rules, recording identity, shared call list.
- Boundaries: UI ID to Edge/RPC; hook cache to service results; legacy list row to canonical navigation.

| Threat ID | Category | Severity | Threat | Mitigation | Verification |
|---|---|---:|---|---|---|
| T-38-10-01 | Tampering | HIGH | Numeric coercion targets wrong recording | UUID-only types/helpers; no parseInt/Number | Source scan and UUID tests |
| T-38-10-02 | Information disclosure | HIGH | Component bypasses authorized service/RPC | Service/hook separation; v3 RPC | Import/source assertions |
| T-38-10-03 | Denial of service | MEDIUM | Stale caches show revoked access | onSettled invalidation and live refetch | Hook tests |
| T-38-10-SC | Tampering | LOW | Dependency change alters browser behavior | No installs | Lockfile diff |
</threat_model>

<verification>
Run sharing hook tests, type check, and source scans. Full real-DB and browser verification occurs in Plan 15.
</verification>

<success_criteria>
- No frontend sharing path coerces recording IDs numerically.
- Service/hook/component boundaries match repo rules.
- Existing and UUID-only shares appear and open correctly.
</success_criteria>

<output>
Create `.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-10-SUMMARY.md` when done.
</output>
