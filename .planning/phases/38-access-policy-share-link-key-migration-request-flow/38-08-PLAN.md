---
phase: 38-access-policy-share-link-key-migration-request-flow
plan: "08"
type: execute
wave: 5
depends_on: ["38-06"]
files_modified:
  - supabase/functions/public-recording/index.ts
  - src/types/public-recording.ts
  - src/services/public-recording.service.ts
  - src/hooks/usePublicRecording.ts
  - src/pages/PublicRecordingView.tsx
  - src/App.tsx
  - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-PUBLIC-RECORDING-EVIDENCE.md
autonomous: true
requirements: [ACCESS-01, ACCESS-03]
must_haves:
  truths:
    - "D-02 Public is an affirmative owner policy that provides an actual anonymous read path without raw anon SELECT on recordings."
    - "Every non-Public policy, missing UUID, malformed UUID, and unavailable recording returns the same non-disclosing result."
    - "The public response includes only the explicitly documented content allowlist and no owner/provider/source/internal metadata."
  artifacts:
    - path: "supabase/functions/public-recording/index.ts"
      provides: "Anonymous allowlisted Public recording response"
    - path: "src/services/public-recording.service.ts"
      provides: "Pure typed Edge client"
    - path: "src/hooks/usePublicRecording.ts"
      provides: "TanStack Query wrapper"
    - path: "src/pages/PublicRecordingView.tsx"
      provides: "Public/unavailable rendering"
  key_links:
    - from: "/public/:recordingId"
      to: "public-recording Edge Function"
      via: "service and query hook"
      pattern: "public-recording"
    - from: "public-recording Edge Function"
      to: "recordings.access_level"
      via: "service-role lookup plus explicit allowlist"
      pattern: "access_level.*public"
---

<objective>
Implement the affirmative Public read path recommended by the research without exposing the raw recordings table.

Purpose: Make the Public access level functional and safe while keeping default/private behavior indistinguishable to anonymous callers.
Output: A public Edge endpoint, service/hook boundary, public view route, and dedicated-test proof.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-RESEARCH.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-UI-SPEC.md
@supabase/functions/public-recording/__tests__/public-recording.integration.test.ts
@src/pages/SharedCallView.tsx
@src/pages/__tests__/PublicRecordingView.test.tsx
@src/CLAUDE.md
@supabase/CLAUDE.md
</context>

## Artifacts this phase produces

- `public-recording` Edge Function with UUID validation, generic failures, and a fixed safe-content response.
- `PublicRecordingPayload`/status types, pure service, TanStack Query hook, and `/public/:recordingId` page/route.
- Dedicated-test deployment and live behavior evidence.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the allowlisted public-recording endpoint</name>
  <read_first>
    - supabase/functions/public-recording/index.ts (create; no prior file)
    - supabase/functions/public-recording/__tests__/public-recording.integration.test.ts
    - supabase/functions/share-call/index.ts
    - supabase/functions/_shared/cors.ts
    - supabase/functions/_shared/auth.ts
    - supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql
  </read_first>
  <files>supabase/functions/public-recording/index.ts</files>
  <behavior>
    - GET validates one UUID and returns content only when the row is explicitly `public`.
    - Success allowlist is exactly `recording_id`, `call_name`, `recording_start_time`, `duration`, and `full_transcript` (nullable where appropriate).
    - All non-Public/missing/malformed/unavailable cases return the same 404-style generic body; database/provider details never escape.
  </behavior>
  <action>
    Add a narrowly scoped anonymous Edge Function using shared CORS and a service-role database client. Validate input with existing Zod version, query only the allowlisted fields plus `access_level` for the predicate, and construct a new response object. Do not grant anon raw-table SELECT and do not return owner IDs/emails, provider/source fields, summary, internal metadata, media URLs, event IDs, org/workspace IDs, or participation data. Keep logs free of content/PII.
  </action>
  <acceptance_criteria>
    - Response construction is an explicit allowlist and tests reject extra keys.
    - Non-Public states are indistinguishable from not found.
    - No authentication/token is required for an explicitly Public recording.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration</automated>
  </verify>
  <done>Public is functional through one safe response boundary and raw recording rows stay protected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the public route through service and hook layers</name>
  <read_first>
    - src/types/public-recording.ts (create; no prior file)
    - src/services/public-recording.service.ts (create; no prior file)
    - src/hooks/usePublicRecording.ts (create; no prior file)
    - src/pages/PublicRecordingView.tsx (create; no prior file)
    - src/App.tsx
    - src/pages/SharedCallView.tsx
    - src/pages/__tests__/PublicRecordingView.test.tsx
    - src/lib/query-config.ts
    - src/CLAUDE.md
  </read_first>
  <files>src/types/public-recording.ts, src/services/public-recording.service.ts, src/hooks/usePublicRecording.ts, src/pages/PublicRecordingView.tsx, src/App.tsx</files>
  <behavior>
    - `/public/:recordingId` loads via service -> hook -> page, never component-direct Supabase/fetch.
    - Loading, public content, unavailable, and network error are distinct accessible states without existence leakage.
    - UUID is validated/encoded; no recording content is cached under a token or unrelated ID.
  </behavior>
  <action>
    Define a strict discriminated status type matching the Edge allowlist. Implement a pure async service and TanStack Query hook with a stable UUID key. Reuse the visual treatment of SharedCallView where appropriate, but do not display inviter/owner/provider/source fields. Register an unauthenticated `/public/:recordingId` route in `App.tsx`. Use frontend `@/` imports and no new packages.
  </action>
  <acceptance_criteria>
    - Components do not import Supabase or call the service directly.
    - Page tests pass for success, generic unavailable, malformed, and network cases.
    - Route is public and does not redirect through auth.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/pages/__tests__/PublicRecordingView.test.tsx &amp;&amp; npm run type-check</automated>
  </verify>
  <done>Anonymous users can open an explicitly Public recording through a typed, allowlisted path.</done>
</task>

<task type="auto" gate="blocking">
  <name>[BLOCKING] Task 3: Deploy public-recording to dedicated test and probe default deny</name>
  <read_first>
    - supabase/functions/public-recording/index.ts
    - supabase/functions/public-recording/__tests__/public-recording.integration.test.ts
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-PUBLIC-RECORDING-EVIDENCE.md (create; no prior file)
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-SCHEMA-EVIDENCE.md
    - supabase/CLAUDE.md
  </read_first>
  <files>.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-TEST-PUBLIC-RECORDING-EVIDENCE.md</files>
  <action>
    Link to and verify `swjzxiddcrtaqixsfaac`, deploy `public-recording` with `--use-api` and the correct anonymous JWT verification setting, then run the real integration suite and live HTTP probes for all six access levels. Record target, SHA, deployment output, status codes, response-key allowlist, and absence of raw anonymous table access. Relink to production afterward without deploying.
  </action>
  <acceptance_criteria>
    - Only Public returns content; the other five policies and missing IDs have the same generic response class/body.
    - Successful response keys match the exact five-field allowlist.
    - Production remains undeployed and final CLI ref is `vltmrnjsubfzrgrtdqey`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration</automated>
  </verify>
  <done>The actual dedicated-test endpoint proves Public works and every non-Public state fails closed.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: transcript content, recording existence, owner/provider/source metadata.
- Boundaries: anonymous UUID request to service-role Edge read; Edge response to public browser.

| Threat ID | Category | Severity | Threat | Mitigation | Verification |
|---|---|---:|---|---|---|
| T-38-08-01 | Information disclosure | CRITICAL | Anonymous caller retrieves non-Public content | Exact `access_level='public'` predicate and generic fail-closed response | Six-policy live matrix |
| T-38-08-02 | Information disclosure | HIGH | Public response leaks internal/PII fields | Explicit five-field projection and response object | Exact-key assertions |
| T-38-08-03 | Denial of service | MEDIUM | Malformed IDs trigger expensive/errors | Zod UUID validation before DB query | Malformed-input test |
| T-38-08-04 | Tampering | HIGH | Test function deploy reaches production | Test-ref double check and final relink | Deployment evidence |
| T-38-08-SC | Tampering | LOW | New UI dependency expands surface | No install; existing primitives only | Lockfile diff |
</threat_model>

<verification>
Unit/page tests, type check, dedicated-test deployment, and real anonymous response matrix must pass.
</verification>

<success_criteria>
- Public has a real anonymous access path.
- No raw-table anonymous permission is added.
- Non-Public existence/content remains undisclosed.
</success_criteria>

<output>
Create `.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-08-SUMMARY.md` when done.
</output>
