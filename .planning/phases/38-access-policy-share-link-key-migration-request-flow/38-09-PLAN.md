---
phase: 38-access-policy-share-link-key-migration-request-flow
plan: "09"
type: execute
wave: 4
depends_on: ["38-06"]
files_modified:
  - src/types/access-policy.ts
  - src/lib/query-config.ts
  - src/services/access-policy.service.ts
  - src/hooks/useAccessPolicy.ts
autonomous: true
requirements: [ACCESS-01, ACCESS-03, ACCESS-06]
must_haves:
  truths:
    - "D-01..D-06 account and per-recording policy operations use canonical UUIDs and typed owner-only RPCs."
    - "A default change is future-only; an explicit recording selection is Custom; reset snapshots the current default."
    - "Hooks own optimistic updates, rollback, toasts, and cache invalidation while services remain pure async TypeScript."
  artifacts:
    - path: "src/types/access-policy.ts"
      provides: "Six-level policy/default-origin contracts"
    - path: "src/services/access-policy.service.ts"
      provides: "Pure typed policy/default RPC client"
    - path: "src/hooks/useAccessPolicy.ts"
      provides: "TanStack queries and optimistic owner mutations"
  key_links:
    - from: "src/hooks/useAccessPolicy.ts"
      to: "src/services/access-policy.service.ts"
      via: "query/mutation functions"
      pattern: "accessPolicyService|setRecordingAccess"
    - from: "policy mutations"
      to: "call-list caches"
      via: "onSettled invalidateCallListCaches"
      pattern: "invalidateCallListCaches"
---

<objective>
Create the typed service and TanStack Query boundary for account defaults and per-recording policy management.

Purpose: Give the approved Settings and Access panel a UUID-native data layer that preserves D-03 through D-06 semantics.
Output: Access policy types, query keys, pure RPC service, and optimistic hooks.
</objective>

<execution_context>
@/Users/admin/.codex/gsd-core/workflows/execute-plan.md
@/Users/admin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-06-SUMMARY.md
@.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-PATTERNS.md
@src/services/data-movement.service.ts
@src/hooks/useNotifications.ts
@src/lib/query-config.ts
@src/CLAUDE.md
</context>

## Artifacts this phase produces

- `RecordingAccessLevel`, `AccessPolicyOrigin`, `AccountAccessDefault`, and `RecordingAccessPolicy` types.
- Query keys for account default, recording policy/management, event copies, and request/grant state.
- Pure service functions for get/set default and get/set/reset recording policy.
- `useAccountAccessDefault`, `useRecordingAccessPolicy`, and mutation hooks.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define policy contracts and stable query keys</name>
  <read_first>
    - src/types/access-policy.ts (create; no prior file)
    - src/types/supabase.ts
    - src/lib/query-config.ts
    - src/types/sharing.ts
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-UI-SPEC.md
  </read_first>
  <files>src/types/access-policy.ts, src/lib/query-config.ts</files>
  <behavior>
    - Access level is exactly `private | attendees | invitees | organization | link | public`.
    - Origin is exactly `default | custom`; labels support “Using default: X” and “Custom”.
    - Query keys are UUID-based, user/default scoped, and stable for policy, management, discovery, request, and grant invalidation.
  </behavior>
  <action>
    Define strict exported interfaces/discriminated results with no `any` and no legacy numeric recording identity. Extend the central query key factory with `accessPolicy.accountDefault`, `.recording(uuid)`, `.management(uuid)`, `.eventCopies(eventUuid)`, and request/grant keys needed by Plan 11. Do not embed display copy in database types.
  </action>
  <acceptance_criteria>
    - The six values match database checks and UI values exactly.
    - Every recording key accepts a UUID string, not number/string union.
    - Type check finds no unsafe casts introduced by the new contracts.
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>Frontend policy and cache interfaces are explicit, UUID-native, and shared by later slices.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the pure access-policy RPC service</name>
  <read_first>
    - src/services/access-policy.service.ts (create; no prior file)
    - src/types/access-policy.ts
    - src/types/supabase.ts
    - src/types/db-extensions.ts
    - src/services/data-movement.service.ts
    - supabase/migrations/20260919000002_phase38_access_policy_rls_rpcs.sql
  </read_first>
  <files>src/services/access-policy.service.ts</files>
  <behavior>
    - Get/set account default; get/set/reset a recording policy by UUID.
    - Expected stable RPC codes map to typed results; unexpected database errors throw contextual Error objects.
    - No owner ID is accepted from the client because RPCs derive `auth.uid()`.
  </behavior>
  <action>
    Add pure async functions over typed/untyped Supabase RPC as required by generated types. Validate result shapes with local type guards, return the canonical stored level/origin, and throw contextual errors without toasts/logging PII. Do not import React, TanStack Query, or component state. Never mutate existing recordings when setting the account default.
  </action>
  <acceptance_criteria>
    - Service exports have UUID string inputs and typed outputs.
    - No React/TanStack/sonner import exists in the service.
    - Calls target only the hardened Phase 38 RPCs, not direct table mutations.
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check &amp;&amp; ! rg -n "react|@tanstack|sonner|\.from\(['\"]recordings" src/services/access-policy.service.ts</automated>
  </verify>
  <done>Policy reads/mutations cross one pure, owner-safe RPC service boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add optimistic policy hooks with rollback and complete invalidation</name>
  <read_first>
    - src/hooks/useAccessPolicy.ts (create; no prior file)
    - src/services/access-policy.service.ts
    - src/hooks/useNotifications.ts
    - src/lib/query-config.ts
    - src/lib/logger.ts
    - .planning/phases/38-access-policy-share-link-key-migration-request-flow/38-UI-SPEC.md
  </read_first>
  <files>src/hooks/useAccessPolicy.ts</files>
  <behavior>
    - Non-Public choices save immediately; UI owns Public confirmation before calling mutations.
    - Default and recording policy update optimistically, restore previous cache on error, and surface exact UI-spec success/failure copy.
    - Recording set/reset invalidates policy/management and `invalidateCallListCaches` in `onSettled`.
  </behavior>
  <action>
    Implement queries and mutations using the service only. Follow the cancel/snapshot/update/rollback/invalidate lifecycle from `useNotifications`. Expose granular pending states so only the relevant picker is disabled. Keep confirmation logic outside hooks. Invalidate the account-default key after default changes and policy/management/call-list keys after per-recording changes.
  </action>
  <acceptance_criteria>
    - Hooks contain no direct Supabase table/RPC call.
    - Rollback restores both value and origin.
    - `onSettled` calls `invalidateCallListCaches` for set/reset recording mutations.
    - Exact success/failure strings match the UI spec.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/components/settings/__tests__/PrivacyAccessSettings.test.tsx src/components/sharing/__tests__/RecordingAccessPanel.test.tsx &amp;&amp; npm run type-check</automated>
  </verify>
  <done>Later UI plans can implement immediate optimistic behavior without bypassing the service boundary.</done>
</task>

</tasks>

<threat_model>
## Assets and trust boundaries

- Assets: owner policy/default, cache correctness, recording UUIDs.
- Boundaries: React hook to service; browser RPC arguments to owner-derived database authorization.

| Threat ID | Category | Severity | Threat | Mitigation | Verification |
|---|---|---:|---|---|---|
| T-38-09-01 | Spoofing | HIGH | Client supplies another owner ID | Service has no owner input; RPC uses auth.uid | Signature/source assertions |
| T-38-09-02 | Tampering | MEDIUM | Optimistic failure leaves false UI state | Snapshot/rollback and refetch in onSettled | Component hook tests |
| T-38-09-03 | Information disclosure | HIGH | Numeric/ambiguous ID targets wrong recording | UUID-only contracts and query keys | Type checks and source scan |
| T-38-09-SC | Tampering | LOW | New dependency bypasses standards | No installs; existing TanStack/Zod patterns | Lockfile diff |
</threat_model>

<verification>
Run targeted component contracts and type check. No database integration suite is mocked or replaced.
</verification>

<success_criteria>
- Service + hook separation is enforced.
- D-03 through D-06 transitions are represented exactly.
- Cache rollback and invalidation are complete.
</success_criteria>

<output>
Create `.planning/phases/38-access-policy-share-link-key-migration-request-flow/38-09-SUMMARY.md` when done.
</output>
