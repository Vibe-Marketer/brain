---
phase: "07"
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/pages/TranscriptsNew.tsx
  - src/services/__tests__/folders.integration.test.ts
autonomous: false
requirements:
  - "Review follow-up from full-codebase audit 2026-06-09"

must_haves:
  truths:
    - "TranscriptsNew.handleDragEnd folder-drop and legacy-folder-zone branches use isLegacyId/isRecordingUuid branching — parseInt is gone from those paths"
    - "DnD drag of a UUID recording to a folder sidebar drop zone calls assignWorkspaceEntryToFolder, not assignToFolder(numericIds)"
    - "folders.integration.test.ts contains a UUID round-trip test: assign UUID recording → visible in getRecordingIdsForFolderFilter → unassign → no longer visible"
    - "npm run type-check, npm test, and npm run build all pass"
  artifacts:
    - path: "src/pages/TranscriptsNew.tsx"
      provides: "UUID-aware DnD folder assignment"
      contains: "isLegacyId"
    - path: "src/services/__tests__/folders.integration.test.ts"
      provides: "UUID round-trip regression coverage"
      contains: "TEST_UUID_RECORDING"
  key_links:
    - from: "src/pages/TranscriptsNew.tsx"
      to: "src/services/folders.service.ts"
      via: "assignWorkspaceEntryToFolder(uuid, folderId, workspaceId)"
      pattern: "isLegacyId branch"
    - from: "src/services/__tests__/folders.integration.test.ts"
      to: "src/services/transcript-filters.service.ts"
      via: "getRecordingIdsForFolderFilter"
      pattern: "UUID round-trip"
---

<objective>
Fix DnD folder assignment in TranscriptsNew.tsx to handle UUID recording IDs, and add UUID round-trip regression tests to folders.integration.test.ts.

Purpose: The DnD path has the same `parseInt` bug as AssignFolderDialog — UUID drag IDs are silently dropped. This plan fixes that path and adds integration test coverage proving the full UUID assign → filter-visible → unassign → not-visible cycle works.

Output: Fixed DnD handler in TranscriptsNew.tsx; new UUID test cases in folders.integration.test.ts.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-recording-id-and-folder-assignment-correctness/07-RESEARCH.md
@.planning/phases/07-recording-id-and-folder-assignment-correctness/07-01-SUMMARY.md

<interfaces>
<!-- From src/lib/recording-ids.ts -->
export function isLegacyId(input: RecordingIdInput): boolean
export function isRecordingUuid(input: RecordingIdInput): boolean

<!-- New functions from Plan 07-01 (src/services/folders.service.ts) -->
export async function assignWorkspaceEntryToFolder(recordingUuid: string, folderId: string, workspaceId: string): Promise<void>
export async function removeWorkspaceEntryFromFolder(recordingUuid: string, folderId: string, workspaceId: string): Promise<void>

<!-- Fixed functions from Plan 07-01 (src/services/transcript-filters.service.ts) -->
export async function getRecordingIdsForFolderFilter(folderIds: string[]): Promise<string[]>
export async function getAssignedWorkspaceEntryFolderUuids(): Promise<Set<string>>

<!-- TranscriptsNew.tsx current DnD pattern (lines 244-274) — BROKEN -->
// Both folder-drop and legacy-folder-zone branches:
//   const raw = str.startsWith("recording-") ? str.replace("recording-", "") : str
//   const id = parseInt(raw, 10)
//   if (!isNaN(id)) acc.push(id)
// → calls assignToFolder(numericIds, folderId) — UUID IDs silently dropped

<!-- Integration test fixture shape (from folders.integration.test.ts lines 23-34) -->
const TEST_LEGACY_ID = 999000001
// Test creates a recording with legacy_recording_id=TEST_LEGACY_ID
// orgId, userId, workspaceId, folderId, recordingId resolved in beforeAll
// Uses: makeIntegrationClient() from @/test/integration-setup
// Uses: describe.skipIf(!integrationDbReachable)(...)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix TranscriptsNew handleDragEnd to handle UUID recording IDs in folder drops</name>
  <files>src/pages/TranscriptsNew.tsx</files>
  <read_first>
    - src/pages/TranscriptsNew.tsx lines 235-278 — the two folder-drop branches with parseInt
    - src/lib/recording-ids.ts — isLegacyId, isRecordingUuid (lines 44-56)
    - src/services/folders.service.ts — assignWorkspaceEntryToFolder added in Plan 07-01; also existing assignToFolder call pattern on line ~254
  </read_first>
  <behavior>
    - Dragging a UUID recording ('recording-uuid-abc-...') to a folder sidebar zone → assignWorkspaceEntryToFolder called with the UUID string, not dropped
    - Dragging a numeric recording ('recording-143800259') to a folder sidebar zone → existing numeric path preserved (assignToFolder with numericIds)
    - Mixed drag selection (some UUID, some numeric) → each handled by its own path
  </behavior>
  <action>
Read `src/pages/TranscriptsNew.tsx` lines 235-278 before editing.

Replace both `parseInt`-based folder-drop reduction blocks (the one at lines 247-253 for `folder-` prefix drops, and the identical one at lines 264-270 for `folder-zone` type drops) with a branching reducer.

The new reducer pattern for each block:

```
const numericIds: number[] = []
const uuidIds: string[] = []
dragHelpers.draggedItems.forEach((item) => {
  const str = String(item)
  const raw = str.startsWith('recording-') ? str.replace('recording-', '') : str
  if (isLegacyId(raw)) {
    numericIds.push(parseInt(raw, 10))
  } else if (isRecordingUuid(raw)) {
    uuidIds.push(raw)
  }
})
if (numericIds.length > 0) {
  assignToFolder(numericIds, folderId)
}
for (const uuid of uuidIds) {
  assignWorkspaceEntryToFolder(uuid, folderId, activeWorkspaceId ?? '').catch((err) => {
    console.error('[DnD] Failed to assign UUID recording to folder', err)
  })
}
```

Add imports at top of file (with existing imports):
- `import { isLegacyId, isRecordingUuid } from '@/lib/recording-ids'`
- `import { assignWorkspaceEntryToFolder } from '@/services/folders.service'`

Check what `activeWorkspaceId` is called in this component — it may be `activeWorkspaceId` from `useWorkspaceContext()` or a different name. Read the top of the file to confirm the variable name, then use the correct one.

Apply this same pattern to BOTH folder-drop branches (the `overId.startsWith('folder-')` branch and the `overData?.type === "folder-zone"` branch).
  </action>
  <verify>
    <automated>npm run type-check 2>&1 | grep "TranscriptsNew" || echo "no TS errors in TranscriptsNew"; grep -n "parseInt" /Users/admin/dev/brain/src/pages/TranscriptsNew.tsx | grep -v "^\s*//" || echo "no parseInt in folder-drop paths"</automated>
  </verify>
  <done>
    - `parseInt` is no longer used in the DnD folder-drop branches in TranscriptsNew.tsx
    - `isLegacyId` and `isRecordingUuid` are imported from `@/lib/recording-ids`
    - `assignWorkspaceEntryToFolder` is imported from `@/services/folders.service`
    - `npm run type-check` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Add UUID round-trip regression tests to folders.integration.test.ts</name>
  <files>src/services/__tests__/folders.integration.test.ts</files>
  <read_first>
    - src/services/__tests__/folders.integration.test.ts — read the full file; understand the beforeAll setup pattern, fixture shape, integrationDbReachable guard
    - src/services/transcript-filters.service.ts — getRecordingIdsForFolderFilter and getAssignedWorkspaceEntryFolderUuids signatures after Plan 07-01
    - src/services/folders.service.ts — assignWorkspaceEntryToFolder, removeWorkspaceEntryFromFolder added in Plan 07-01
    - src/test/integration-setup.ts — makeIntegrationClient, integrationDbReachable patterns
  </read_first>
  <action>
Read `src/services/__tests__/folders.integration.test.ts` in full before editing.

Add a second `describe` block (after the existing BUG-01 block) within the same `describe.skipIf(!integrationDbReachable)(...)` outer scope, titled `'P7-SC5: UUID recording folder assignment round-trip'`.

The new describe block needs its own setup:
1. In `beforeAll`: query for an existing canonical UUID recording in the test org that has `legacy_recording_id = null` (source_app != 'fathom'). If none exists, insert a synthetic one with `source_app = 'manual'` and `title = '[p7-integration-uuid-test] do-not-touch'` and record its `id` as `uuidRecordingId`. Also reuse `orgId`, `userId`, `workspaceId`, `folderId` from the outer scope (or re-derive them using the same donor pattern).

2. In `afterAll`: clean up by calling `removeWorkspaceEntryFromFolder(uuidRecordingId, folderId, workspaceId)` and deleting the synthetic recording if it was inserted.

Test cases (all using `it(...)` with `await`):

- `'assigns UUID recording to folder via workspace_entries'`: call `assignWorkspaceEntryToFolder(uuidRecordingId, folderId, workspaceId)` → query `workspace_entries` WHERE `recording_id = uuidRecordingId AND folder_id = folderId` → `expect(data).toHaveLength(1)`

- `'getRecordingIdsForFolderFilter includes UUID-assigned recording'`: after assignment, call `getRecordingIdsForFolderFilter([folderId])` → `expect(ids).toContain(uuidRecordingId)`

- `'getAssignedWorkspaceEntryFolderUuids includes UUID-assigned recording'`: call `getAssignedWorkspaceEntryFolderUuids()` → `expect(uuids.has(uuidRecordingId)).toBe(true)`

- `'unassigns UUID recording from folder'`: call `removeWorkspaceEntryFromFolder(uuidRecordingId, folderId, workspaceId)` → query `workspace_entries` WHERE same predicates → `expect(data?.folder_id).toBeNull()`

- `'getRecordingIdsForFolderFilter excludes unassigned UUID recording'`: after unassignment, call `getRecordingIdsForFolderFilter([folderId])` → `expect(ids).not.toContain(uuidRecordingId)`

Import `assignWorkspaceEntryToFolder` and `removeWorkspaceEntryFromFolder` from `@/services/folders.service`.
Import `getRecordingIdsForFolderFilter` and `getAssignedWorkspaceEntryFolderUuids` from `@/services/transcript-filters.service`.

The `makeIntegrationClient` is used to create the db client; the service functions use the singleton `supabase` client from `@/integrations/supabase/client`. The tests verify behavior at the service level, not the DB client level.

NOTE: These integration tests skip cleanly when `SUPABASE_TEST_SERVICE_ROLE_KEY` is absent (same guard as existing tests). Do not change the outer `describe.skipIf` pattern.
  </action>
  <verify>
    <automated>npm run type-check 2>&1 | grep "folders.integration" || echo "no TS errors in integration test"; grep -n "P7-SC5\|uuidRecordingId\|assignWorkspaceEntryToFolder" /Users/admin/dev/brain/src/services/__tests__/folders.integration.test.ts | head -20</automated>
  </verify>
  <done>
    - `folders.integration.test.ts` contains a `'P7-SC5: UUID recording folder assignment round-trip'` describe block with 5 test cases
    - Tests import `assignWorkspaceEntryToFolder`, `removeWorkspaceEntryFromFolder`, `getRecordingIdsForFolderFilter`, `getAssignedWorkspaceEntryFolderUuids`
    - `npm run type-check` exits 0
    - `npm test -- src/services/__tests__/folders.integration.test.ts` either passes or shows skip (not red failure) in an environment without the integration key
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All Phase 07 fixes are in place:
- Plan 07-01: assignWorkspaceEntryToFolder / removeWorkspaceEntryFromFolder added to folders.service.ts; getRecordingIdsForFolderFilter fixed to dual-source; getAssignedWorkspaceEntryFolderUuids added
- Plan 07-02: AssignFolderDialog uses toRecordingUuidBatch (no parseInt), success toast guarded by writtenCount; TranscriptsTab folderingCallId widened; useFolderAssignment hooks accept string | number
- Plan 07-03: TranscriptsNew DnD uses isLegacyId/isRecordingUuid branching; UUID regression tests added
  </what-built>
  <how-to-verify>
1. Run: `npm run type-check` — must exit 0 with zero errors
2. Run: `npm test` — full suite must pass (or same pre-existing failures as before Phase 07)
3. Run: `npm run build` — must succeed, no import errors
4. Browser walkthrough (app at http://localhost:3001 via `npm run dev`):
   a. Navigate to Transcripts tab
   b. Find a non-Fathom recording (Zoom, manual import, or MCP import — check source badge)
   c. Click the folder icon on that recording
   d. In the AssignFolderDialog: check that existing folder selections load correctly (should show pre-selected if already in a folder)
   e. Select a folder and click Save
   f. Verify: folder badge appears on the recording row (no blank badge)
   g. Verify: the folder filter in the filter bar shows the recording when that folder is selected
   h. Verify: no false success toast fires when no folder was actually assigned (e.g., save with no folders selected on a recording with no prior assignment)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DnD drag ID → service layer | Drag item IDs come from DOM data-attributes (drag data set by DraggableCallRow); validated via isLegacyId/isRecordingUuid before routing |
| Integration test → real Supabase | Tests use service-role key; test rows are scoped with recognizable prefixes and cleaned in afterAll |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | DnD drag ID injection | mitigate | isLegacyId/isRecordingUuid validates format before any DB call; RLS enforces org scope |
| T-07-06 | Information Disclosure | integration test fixtures | mitigate | test rows use recognizable titles ('[p7-integration-uuid-test] do-not-touch') and are cleaned in afterAll; service role key never committed |
| T-07-SC | Tampering | npm install | accept | No new packages installed in this plan |
</threat_model>

<verification>
Phase gate — all must be green before marking Phase 07 complete:
- `npm run type-check` — zero errors
- `npm test` — full suite passes (same baseline as pre-Phase-07)
- `npm run build` — exits 0
- Browser: assign a non-Fathom recording to a folder → folder badge appears → folder filter shows it
- Browser: named folder filter on a UUID-assigned recording returns correct results
- `grep -rn "parseInt" src/components/AssignFolderDialog.tsx src/pages/TranscriptsNew.tsx` — no parseInt on recording ID inputs
</verification>

<success_criteria>
- TranscriptsNew.tsx DnD folder-drop branches handle UUID recording IDs via assignWorkspaceEntryToFolder
- folders.integration.test.ts has 5 UUID round-trip tests covering assign, filter-visible, filter-by-assigned-set, unassign, filter-excludes
- `npm run type-check` exits 0
- `npm test` passes (or pre-existing failures only)
- `npm run build` succeeds
- Human browser walkthrough confirms folder assignment works for a non-Fathom canonical recording
</success_criteria>

<output>
Create `.planning/phases/07-recording-id-and-folder-assignment-correctness/07-03-SUMMARY.md` when done
</output>
