---
phase: "07"
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/components/AssignFolderDialog.tsx
  - src/components/transcripts/TranscriptsTab.tsx
  - src/hooks/useFolderAssignment.ts
autonomous: true
requirements:
  - "Review follow-up from full-codebase audit 2026-06-09"

must_haves:
  truths:
    - "AssignFolderDialog.loadExistingAssignments uses toRecordingUuidBatch, queries both workspace_entries and folder_assignments, merges results — no parseInt"
    - "AssignFolderDialog.handleSave uses toRecordingUuidBatch to split IDs; writes folder_assignments only for legacyId records; writes workspace_entries.folder_id for ALL records via assignWorkspaceEntryToFolder; success toast fires only when writtenCount > 0"
    - "TranscriptsTab.folderingCallId state is string | number | null; the onFolderCall cast is removed"
    - "TranscriptsTab unorganized filter uses getAssignedWorkspaceEntryFolderUuids() to exclude UUID-assigned recordings"
    - "useFolderAssignment hooks (useAssignToFolder, useRemoveFromFolder, useMoveToFolder) accept callRecordingId: string | number and branch on isLegacyId vs isRecordingUuid"
  artifacts:
    - path: "src/components/AssignFolderDialog.tsx"
      provides: "Correct dual-source folder load + UUID-aware save"
      contains: "toRecordingUuidBatch"
    - path: "src/components/transcripts/TranscriptsTab.tsx"
      provides: "Widened folderingCallId type, UUID-aware unorganized filter"
      contains: "getAssignedWorkspaceEntryFolderUuids"
    - path: "src/hooks/useFolderAssignment.ts"
      provides: "UUID-aware mutation hooks"
      contains: "isLegacyId"
  key_links:
    - from: "src/components/AssignFolderDialog.tsx"
      to: "src/lib/recording-ids.ts"
      via: "toRecordingUuidBatch(targetRecordingIds)"
      pattern: "toRecordingUuidBatch"
    - from: "src/hooks/useFolderAssignment.ts"
      to: "src/services/folders.service.ts"
      via: "assignWorkspaceEntryToFolder or assignCallToFolder branch"
      pattern: "isLegacyId"
---

<objective>
Fix the UI and hook layer: AssignFolderDialog gets dual-source load + UUID-aware save; TranscriptsTab gets widened folderingCallId type and correct unorganized filter; useFolderAssignment hooks accept string | number.

Purpose: With the service layer fixed in Plan 07-01, this plan wires the UI to use those correct paths. The `parseInt` calls in AssignFolderDialog are the source of silent data loss for non-Fathom recordings; the `as number` type lie in TranscriptsTab and the `number`-typed hooks are the TypeScript surface that hides the bug.

Output: Corrected AssignFolderDialog, TranscriptsTab state + filter, and useFolderAssignment hooks.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-recording-id-and-folder-assignment-correctness/07-RESEARCH.md
@.planning/phases/07-recording-id-and-folder-assignment-correctness/07-01-SUMMARY.md

<interfaces>
<!-- From src/lib/recording-ids.ts -->
export function isLegacyId(input: RecordingIdInput): boolean
export function isRecordingUuid(input: RecordingIdInput): boolean
export async function toRecordingUuidBatch(inputs: RecordingIdInput[], opts?): Promise<{ resolved: ResolvedRecording[]; uuids: string[]; legacyIds: number[] }>

<!-- New functions added in Plan 07-01 (src/services/folders.service.ts) -->
export async function assignWorkspaceEntryToFolder(recordingUuid: string, folderId: string, workspaceId: string): Promise<void>
export async function removeWorkspaceEntryFromFolder(recordingUuid: string, folderId: string, workspaceId: string): Promise<void>

<!-- New function added in Plan 07-01 (src/services/transcript-filters.service.ts) -->
export async function getAssignedWorkspaceEntryFolderUuids(): Promise<Set<string>>

<!-- Existing functions consumed by useFolderAssignment.ts -->
export async function assignCallToFolder(callRecordingId: number, folderId: string, userId: string, workspaceId?: string | null): Promise<void>
export async function removeCallFromFolder(callRecordingId: number, folderId: string, workspaceId?: string | null): Promise<void>
export async function moveCallToFolder(callRecordingId: number, fromFolderId: string, toFolderId: string, userId: string, workspaceId?: string | null): Promise<void>

<!-- TranscriptsTab current state (lines 155, 1258, 664-679) -->
// state: const [folderingCallId, setFolderingCallId] = useState<number | null>(null);
// cast: onFolderCall={(callId) => setFolderingCallId(callId as number)}
// usage: recordingId={String(folderingCallId)} (line ~1387 in AssignFolderDialog)
// unorganized: const assignedLegacyIds = await getAssignedFolderLegacyRecordingIds();
//              if (r.legacy_recording_id === null || !assignedLegacyIds.has(r.legacy_recording_id))

<!-- AssignFolderDialog props interface (current) -->
interface AssignFolderDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  recordingId?: string      // single-recording mode
  recordingIds?: string[]   // bulk mode
  onFoldersUpdated: () => void
  onCreateFolder?: () => void
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix AssignFolderDialog — dual-source load and UUID-aware save</name>
  <files>src/components/AssignFolderDialog.tsx</files>
  <read_first>
    - src/components/AssignFolderDialog.tsx — read the full file; fix loadExistingAssignments (line 65-101) and handleSave (line 239-306)
    - src/lib/recording-ids.ts — toRecordingUuidBatch signature (lines 123-186)
    - src/services/folders.service.ts — assignWorkspaceEntryToFolder added in Plan 07-01; also assignCallToFolder for the legacy path
  </read_first>
  <behavior>
    - loadExistingAssignments called with ['uuid-abc']: queries workspace_entries for that UUID, returns correct pre-selected folders — no parseInt
    - loadExistingAssignments called with ['143800259']: resolves via toRecordingUuidBatch, queries folder_assignments for legacy ID — existing behavior preserved
    - handleSave with UUID recording: writtenCount increments; assignWorkspaceEntryToFolder called; success toast shows; no NaN in any DB call
    - handleSave with Fathom recording (has legacyId): both folder_assignments upsert AND assignWorkspaceEntryToFolder called (dual-write)
    - handleSave with zero actual writes (empty targetRecordingIds): no toast fires
  </behavior>
  <action>
Read `src/components/AssignFolderDialog.tsx` in full before editing.

**Fix `loadExistingAssignments`** (replace lines 65-101):

Add import at top of file: `import { toRecordingUuidBatch } from '@/lib/recording-ids'`

Replace the `loadExistingAssignments` body:
1. Call `const { resolved, uuids: canonicalUuids, legacyIds } = await toRecordingUuidBatch(targetRecordingIds)` (import from `@/lib/recording-ids`)
2. Parallel fetch:
   - If `legacyIds.length > 0`: query `folder_assignments` WHERE `call_recording_id IN legacyIds` → `legacyData`
   - If `canonicalUuids.length > 0`: query `workspace_entries` SELECT `recording_id, folder_id` WHERE `recording_id IN canonicalUuids` AND `folder_id IS NOT NULL` → `uuidData`
3. Merge folder IDs: `[...legacyData.map(a => a.folder_id), ...uuidData.map(e => e.folder_id).filter(Boolean)]`
4. In bulk mode, compute intersection by counting how many of the `resolved.length` recordings appear in each folder (count across both sources by folder_id)
5. Set `selectedFolders` from merged set

**Fix `handleSave`** (replace lines 239-306):

1. Call `const { resolved, uuids: canonicalUuids, legacyIds } = await toRecordingUuidBatch(targetRecordingIds)`
2. `let writtenCount = 0`
3. Legacy path (Fathom dual-write): for recordings where `legacyId !== null`, run existing `folder_assignments` upsert/delete logic using the `legacyIds` array (unchanged from current code, just sourced from `toRecordingUuidBatch` result instead of `parseInt`)
4. UUID-canonical write: for ALL recordings (using `canonicalUuids`), for each folder in `legacySelected` (selected non-personal folders):
   - If folder should be assigned: call `assignWorkspaceEntryToFolder(uuid, folderId, activeWorkspaceId ?? folder.workspace_id)` — increment `writtenCount` for each successful write
   - If folder should be removed (was selected before, now deselected): call `removeWorkspaceEntryFromFolder(uuid, folderId, workspaceId)` — increment `writtenCount`
5. Show success toast only if `writtenCount > 0` OR `targetRecordingIds.length === 0` (no-op case shows "No changes made")
6. Add imports: `assignWorkspaceEntryToFolder, removeWorkspaceEntryFromFolder` from `@/services/folders.service`

The `resolveFolderWorkspaceId` call: when `activeWorkspaceId` is available from the context, pass it; otherwise call `resolveFolderWorkspaceId` (already exists in folders.service as a private helper — expose it or inline the folder.workspace_id lookup from `allFolders`).

NOTE: `activeWorkspaceId` is not currently in the AssignFolderDialog component. Use `allFolders[0]?.workspace_id` as a fallback workspace lookup when it is not available from context, OR query the folder's `workspace_id` from the already-loaded `allFolders` array before writing.
  </action>
  <verify>
    <automated>npm run type-check 2>&1 | grep "AssignFolderDialog" || echo "no TS errors in AssignFolderDialog"; grep -n "parseInt\|Number(" /Users/admin/dev/brain/src/components/AssignFolderDialog.tsx | grep -v "//\s*" | grep -v "count.*1 ? " || echo "no parseInt found"</automated>
  </verify>
  <done>
    - No `parseInt` or `Number(` calls remain in `AssignFolderDialog.tsx` on recording ID inputs
    - `toRecordingUuidBatch` is imported and called in both `loadExistingAssignments` and `handleSave`
    - `assignWorkspaceEntryToFolder` is imported from `@/services/folders.service`
    - `npm run type-check` exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen folderingCallId type in TranscriptsTab + fix unorganized filter + widen useFolderAssignment hooks</name>
  <files>
    src/components/transcripts/TranscriptsTab.tsx
    src/hooks/useFolderAssignment.ts
  </files>
  <read_first>
    - src/components/transcripts/TranscriptsTab.tsx — read lines 140-165 (state declarations), lines 650-690 (unorganized filter block), lines 1240-1400 (onFolderCall cast and AssignFolderDialog usage)
    - src/hooks/useFolderAssignment.ts — read the full file; all three hooks have callRecordingId: number
    - src/services/transcript-filters.service.ts — getAssignedWorkspaceEntryFolderUuids added in Plan 07-01
    - src/lib/recording-ids.ts — isLegacyId, isRecordingUuid signatures
  </read_first>
  <behavior>
    - TranscriptsTab.folderingCallId state holds a UUID string without truncation when a non-Fathom recording's folder button is clicked
    - Unorganized filter: a recording with recording.id='uuid-xyz' and legacy_recording_id=null that has workspace_entries.folder_id set is excluded from the "unorganized" list (i.e., it does NOT appear as unorganized)
    - useAssignToFolder.mutationFn: when callRecordingId is a UUID string, calls assignWorkspaceEntryToFolder(callRecordingId, folderId, workspaceId??''); when it is a number, calls existing assignCallToFolder path
    - useRemoveFromFolder.mutationFn: same branching — UUID string → removeWorkspaceEntryFromFolder; number → removeCallFromFolder
    - useMoveToFolder.mutationFn: UUID string → removeWorkspaceEntryFromFolder(fromFolderId) + assignWorkspaceEntryToFolder(toFolderId); number → existing moveCallToFolder
  </behavior>
  <action>
**In `src/components/transcripts/TranscriptsTab.tsx`:**

1. Line 155: Change `useState<number | null>(null)` → `useState<number | string | null>(null)`
2. Line 1258: Change `onFolderCall={(callId) => setFolderingCallId(callId as number)}` → `onFolderCall={(callId) => setFolderingCallId(callId)}`
3. Unorganized filter block (around line 663-679): Import `getAssignedWorkspaceEntryFolderUuids` from `@/services/transcript-filters.service`. Replace the body of the `if (includeUnorganized)` block:
   - Call `const assignedUuids = await getAssignedWorkspaceEntryFolderUuids()`
   - Then for `(allRecs || []).forEach((r) => { if (!assignedUuids.has(r.id)) { allowedRecordingIds.add(r.id) } })`
   - Remove the old `getAssignedFolderLegacyRecordingIds` import/call from this block (leave the function itself exported, just remove this call site)

**In `src/hooks/useFolderAssignment.ts`:**

1. Add imports: `isLegacyId, isRecordingUuid` from `@/lib/recording-ids`; `assignWorkspaceEntryToFolder, removeWorkspaceEntryFromFolder` from `@/services/folders.service`

2. `useAssignToFolder` mutation type: change `callRecordingId: number` → `callRecordingId: number | string`; add optional `workspaceId?: string | null`; inside `mutationFn`: `if (isLegacyId(callRecordingId)) { return assignCallToFolder(callRecordingId as number, folderId, user.id) } else { return assignWorkspaceEntryToFolder(callRecordingId as string, folderId, workspaceId ?? '') }`

3. `useRemoveFromFolder` mutation type: change `callRecordingId: number` → `callRecordingId: number | string`; inside `mutationFn`: `if (isLegacyId(callRecordingId)) { return removeCallFromFolder(callRecordingId as number, folderId, workspaceId) } else { return removeWorkspaceEntryFromFolder(callRecordingId as string, folderId, workspaceId ?? '') }`

4. `useMoveToFolder` mutation type: change `callRecordingId: number` → `callRecordingId: number | string`; add optional `workspaceId?: string | null`; inside `mutationFn`: `if (isLegacyId(callRecordingId)) { return moveCallToFolder(callRecordingId as number, fromFolderId, toFolderId, user.id, workspaceId) } else { await removeWorkspaceEntryFromFolder(callRecordingId as string, fromFolderId, workspaceId ?? ''); return assignWorkspaceEntryToFolder(callRecordingId as string, toFolderId, workspaceId ?? '') }`
  </action>
  <verify>
    <automated>npm run type-check 2>&1 | grep -E "TranscriptsTab|useFolderAssignment" || echo "no TS errors in these files"; grep -n "as number" /Users/admin/dev/brain/src/components/transcripts/TranscriptsTab.tsx | grep "folderingCallId" || echo "no folderingCallId as-number cast found"</automated>
  </verify>
  <done>
    - `folderingCallId` state type is `number | string | null` in TranscriptsTab
    - `onFolderCall` callback no longer uses `as number` cast
    - The unorganized filter calls `getAssignedWorkspaceEntryFolderUuids()` instead of `getAssignedFolderLegacyRecordingIds()`
    - All three hooks in `useFolderAssignment.ts` accept `callRecordingId: number | string`
    - `npm run type-check` exits 0
    - `npm test -- src/hooks` passes (or pre-existing failures only)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → service layer | Recording IDs from user selection pass through toRecordingUuidBatch before any DB write |
| hook → service layer | callRecordingId input is validated via isLegacyId/isRecordingUuid before routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Spoofing (trust) | AssignFolderDialog success toast | mitigate | writtenCount check: toast fires only when actual DB rows were written |
| T-07-04 | Tampering | useFolderAssignment callRecordingId | mitigate | isLegacyId/isRecordingUuid validation before DB branch; invalid input falls through without write |
| T-07-SC | Tampering | npm install | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:
- `npm run type-check` — zero TS errors
- `npm test` — all pre-existing passing tests remain green
- `grep -n "parseInt\|Number(" src/components/AssignFolderDialog.tsx` — no parseInt on recording IDs
- `grep -n "as number" src/components/transcripts/TranscriptsTab.tsx | grep foldering` — empty
- `grep -n "callRecordingId: number[^|]" src/hooks/useFolderAssignment.ts` — empty (all widened to `number | string`)
</verification>

<success_criteria>
- AssignFolderDialog uses `toRecordingUuidBatch` in both load and save paths — no `parseInt` on recording IDs
- AssignFolderDialog success toast is guarded by `writtenCount > 0`
- `folderingCallId` state in TranscriptsTab accepts UUID strings without type lie
- Unorganized filter checks `getAssignedWorkspaceEntryFolderUuids()` so UUID-assigned recordings are excluded
- All three `useFolderAssignment` hooks accept `string | number` and branch on `isLegacyId`
- `npm run type-check` exits 0
</success_criteria>

<output>
Create `.planning/phases/07-recording-id-and-folder-assignment-correctness/07-02-SUMMARY.md` when done
</output>
