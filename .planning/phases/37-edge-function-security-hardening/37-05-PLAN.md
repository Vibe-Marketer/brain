---
plan: 37-05
phase: 37
title: Deployed-vs-source orphan cleanup — auto-delete confirmed-dead
wave: 5
depends_on: [37-01]
files_modified:
  - .planning/security/2026-05-Q2-deployed-source-delta.md
autonomous: true
requirements: [SEC-05A, SEC-05B, SEC-05C]
---

# Plan 37-05: Edge Function Orphan Cleanup

## Objective

Reconcile the 76 deployed functions against the 38 source functions. Auto-delete confirmed-dead functions (zero callers in repo + zero recent activity); list any ambiguous ones for user decision.

## Tasks

<task id="37-05-T01">
<action>
Run `supabase functions list` and capture the raw output to `/tmp/supabase-functions-deployed.txt`. Then produce `.planning/security/2026-05-Q2-deployed-source-delta.md` with four sections:

1. **Snapshot Summary** — total deployed, total source, delta count.
2. **Section A — In Source AND Deployed (38 expected)** — table.
3. **Section B — Deployed-Only Orphans** — for each, perform a caller check:
   ```bash
   for orphan in <list>; do
     hits=$(git log --since="120 days" -p -- src/ supabase/functions/ | grep -c "$orphan")
     echo "$orphan: $hits hits"
   done
   ```
   Then classify each orphan as:
   - **Confirmed-dead (auto-delete)** — 0 hits in repo, name matches a removed feature (google-meet-*, automation-*, content-*, coach-*, team-*, extract-*, etc.).
   - **Ambiguous (needs decision)** — has hits OR name doesn't clearly map to a removed feature.

Use this initial classification (verify each via the caller check):

**Confirmed-dead candidates (FOUND-09 Google Meet removal + v1 dead-code):**
- google-meet-fetch-meetings
- google-meet-sync-meetings
- google-oauth-url
- google-oauth-callback
- google-oauth-refresh
- google-poll-sync
- automation-webhook
- automation-engine
- automation-email
- automation-sentiment
- automation-scheduler
- content-classifier
- content-insight-miner
- content-hook-generator
- content-builder
- extract-knowledge
- extract-action-items
- extract-profits
- coach-relationships
- coach-shares
- coach-notes
- send-coach-invite
- team-memberships
- team-direct-reports
- team-shares
- coach-relationships
- manager-notes
- check-client-health
- migrate-recordings
- bulk-apply-routing-rules
- save-webhook-secret
- get-config-status
- save-fathom-key
- test-fathom-connection
- test-env-vars
- test-secrets
- get-available-models
- sync-openrouter-models
- resync-all-calls
- delete-all-calls

**Likely-ambiguous (verify):**
- (none expected — all 76 deployed should map to either the 38 in source or the list above)

4. **Section C — Decisions Applied** — log every delete with timestamp.
</action>
<read_first>
- supabase/functions/ (full listing)
- .planning/REQUIREMENTS.md (CLAUDE.md HARD CONSTRAINT FOUND-09 removed Google Meet)
</read_first>
<acceptance_criteria>
- `.planning/security/2026-05-Q2-deployed-source-delta.md` exists.
- Lists every deployed function as in-source OR confirmed-dead OR ambiguous.
- Confirmed-dead list is justified (per-row evidence: 0 callers + feature-removed).
- Sum of (in-source + confirmed-dead + ambiguous) == total deployed.
</acceptance_criteria>
</task>

<task id="37-05-T02">
<action>
For every function classified "confirmed-dead" in T01, run:
```bash
supabase functions delete <name> --project-ref <ref> 2>&1 | tee -a /tmp/orphan-delete-log.txt
```

Note: `supabase functions delete` may require confirmation. Use `--no-verify-jwt` or interactive non-issue. If the CLI version supports a `--force` flag, use it. Otherwise, capture every prompt and respond `y` automatically (the deletion list is preapproved per CONTEXT.md decision).

If interactive prompts cannot be auto-confirmed in non-TTY mode, PAUSE this task and write the exact commands to `.planning/security/2026-05-Q2-orphan-delete-commands.sh` for the user to run.

After every delete (or dry-run command emit), append to the audit doc Section C: `| 2026-05-12 | <name> | DELETED | reason |`.
</action>
<read_first>
- .planning/security/2026-05-Q2-deployed-source-delta.md
</read_first>
<acceptance_criteria>
- Either every confirmed-dead function is deleted via `supabase functions delete` (verified by re-running `supabase functions list`), OR a runnable `orphan-delete-commands.sh` script lists them all and Section C explains the deferral.
- Audit doc Section C lists every action with timestamp.
- Re-running `supabase functions list` after the delete (if performed live) shows deployed count == 38 + any explicitly retained items.
</acceptance_criteria>
</task>

<task id="37-05-T03">
<action>
Update `.planning/security/2026-05-Q2-deployed-source-delta.md` with the final state:

- Deployed-before count
- Deployed-after count
- Source count
- Net deletion count
- List of preserved exceptions (deployed-only functions explicitly kept and why)
- A `SEC-05` SUCCESS line: `deployed_count == source_count` OR an explicit exception note.
</action>
<read_first>
- .planning/security/2026-05-Q2-deployed-source-delta.md
- output of post-delete `supabase functions list`
</read_first>
<acceptance_criteria>
- Doc has both before and after counts.
- A clear SEC-05 result line (PASS or PARTIAL with exceptions documented).
</acceptance_criteria>
</task>

## Verification

- [ ] Deployed-source delta doc exists with full classification.
- [ ] Confirmed-dead functions deleted from production OR commands stashed for user run.
- [ ] Final state documented.

## must_haves

1. SEC-05A: produce the snapshot doc.
2. SEC-05B: cross-reference every deployed-but-not-in-source function.
3. SEC-05C: delete every confirmed-dead orphan.
