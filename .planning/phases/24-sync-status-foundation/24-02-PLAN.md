---
phase: 24-sync-status-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260620120000_sync_jobs_durable_resource.sql
  - src/test/rls-regression.test.ts
autonomous: true
requirements: [IMP-03]
must_haves:
  truths:
    - "sync_jobs gains source_app, organization_id, workspace_id, source_id, mode, date_start, date_end, provider_cursor, last_heartbeat_at — all nullable/defaulted so in-flight jobs stay readable"
    - "sync_jobs has an org-scoped RLS policy alongside the existing user policy; another org's rows are invisible to a tenant JWT"
    - "sync_jobs is registered in CROSS_ORG_TABLES so the RLS regression CI gate fails loud on any future cross-org leak"
    - "recording_ids/synced_ids/failed_ids remain TEXT[] (untouched); sync_jobs Realtime membership is verified, not re-added"
  artifacts:
    - path: "supabase/migrations/20260620120000_sync_jobs_durable_resource.sql"
      provides: "additive sync_jobs columns + org-scoped RLS + Realtime membership verification + existing-row backfill"
      contains: "ADD COLUMN IF NOT EXISTS"
    - path: "src/test/rls-regression.test.ts"
      provides: "sync_jobs entry in CROSS_ORG_TABLES"
      contains: "sync_jobs"
  key_links:
    - from: "src/test/rls-regression.test.ts CROSS_ORG_TABLES"
      to: "sync_jobs RLS policy"
      via: "{ table: 'sync_jobs', filterColumn: 'organization_id' }"
      pattern: "sync_jobs.*organization_id"
---

<objective>
Extend `sync_jobs` into the durable, provider-agnostic resource every later v2.1 phase reads and writes — via an additive (`ADD COLUMN IF NOT EXISTS`) migration that leaves in-flight jobs readable — plus org-scoped RLS, `CROSS_ORG_TABLES` registration, and Realtime membership verification.

Purpose: Phases 27 (observable jobs) and 28 (resumable sync-all) read/write `source_app`, org/workspace scope, `mode`, date range, `provider_cursor`, and `last_heartbeat_at`. These columns must exist and be org-isolated before those phases can build. The migration must be non-destructive against prod (ref `vltmrnjsubfzrgrtdqey`).

Output: the additive migration SQL and the `CROSS_ORG_TABLES` extension. (The migration is APPLIED to prod and the TEST DB in Plan 04's BLOCKING push task — this plan only WRITES the SQL. The related `cancelSyncJob` `error`-column fix lives in Plan 01, which owns `sync-tab.service.ts`.)
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-sync-status-foundation/24-RESEARCH.md
@supabase/CLAUDE.md

<interfaces>
<!-- Extracted from codebase + live prod introspection (RESEARCH.md). -->

Missing sync_jobs columns to add (VERIFIED against live prod):
  source_app TEXT, organization_id UUID, workspace_id UUID, source_id UUID,
  mode TEXT DEFAULT 'selected', date_start TIMESTAMPTZ, date_end TIMESTAMPTZ,
  provider_cursor TEXT, last_heartbeat_at TIMESTAMPTZ.

ALREADY present (do NOT touch / do NOT re-add):
  - `error` TEXT (live column — NOT `error_message`), `skipped_count`
  - `recording_ids`, `synced_ids`, `failed_ids` are TEXT[] (_text) — leave as-is, never retype
  - sync_jobs IS already a member of the `supabase_realtime` publication with ALL columns
    (prattrs IS NULL → new columns auto-replicate). Verify membership, do NOT ALTER PUBLICATION ADD TABLE blindly (it would error).

Additive pattern reference: supabase/migrations/20251124010359_add_sync_jobs_progress_columns.sql
TEXT[] conversion reference: supabase/migrations/20260410180000_sync_jobs_text_arrays.sql

CROSS_ORG_TABLES (src/test/rls-regression.test.ts:40-76): a ReadonlyArray of { table, filterColumn }.
  The filterColumn union type already includes "organization_id". Append { table: "sync_jobs", filterColumn: "organization_id" }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write additive sync_jobs migration with org-scoped RLS + Realtime verification</name>
  <files>supabase/migrations/20260620120000_sync_jobs_durable_resource.sql</files>
  <read_first>
    - supabase/migrations/20251124010359_add_sync_jobs_progress_columns.sql (the additive ADD COLUMN IF NOT EXISTS pattern to copy)
    - supabase/migrations/00000000000000_consolidated_schema.sql (search for the base sync_jobs CREATE TABLE + its existing RLS policy ~line 173 and ~line 645 to mirror policy naming + the existing user_id policy)
    - supabase/migrations/20260410180000_sync_jobs_text_arrays.sql (confirm arrays are already TEXT[] — do not retype)
    - supabase/CLAUDE.md (migration conventions: additive only, prod ref guard, RLS regression gate)
    - .planning/phases/24-sync-status-foundation/24-RESEARCH.md "IMP-03" + "Realtime publication assertion" sections
  </read_first>
  <action>
    Create `supabase/migrations/20260620120000_sync_jobs_durable_resource.sql`. (1) `ALTER TABLE public.sync_jobs ADD COLUMN IF NOT EXISTS` each of: `source_app TEXT`, `organization_id UUID`, `workspace_id UUID`, `source_id UUID`, `mode TEXT DEFAULT 'selected'`, `date_start TIMESTAMPTZ`, `date_end TIMESTAMPTZ`, `provider_cursor TEXT`, `last_heartbeat_at TIMESTAMPTZ`. (2) Backfill existing rows non-destructively: `UPDATE public.sync_jobs SET source_app = COALESCE(source_app, 'fathom'), mode = COALESCE(mode, 'selected')` (every prior job was a Fathom selected-import per RESEARCH). Do NOT backfill organization_id blindly — leave NULL where unknown (additive, in-flight jobs stay readable). (3) Add an index on `(organization_id, status)` IF NOT EXISTS to support org-scoped polling in later phases. (4) Add an org-scoped RLS policy ALONGSIDE the existing user_id policy (do not drop the existing policy): a SELECT/ALL policy named e.g. `sync_jobs_org_isolation` using the project's standard org-membership check (mirror the org-scoped policy shape used by `recordings`/`workspaces` in the consolidated schema — use the same `organization_memberships` membership predicate). (5) Verify (do NOT re-add) Realtime membership with the DO-block guard from RESEARCH: only `ALTER PUBLICATION supabase_realtime ADD TABLE public.sync_jobs` inside `IF NOT EXISTS (SELECT 1 FROM pg_publication_tables WHERE pubname='supabase_realtime' AND tablename='sync_jobs')`. Do NOT touch `recording_ids`/`synced_ids`/`failed_ids`. Do NOT reference `error_message` anywhere.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/20260620120000_sync_jobs_durable_resource.sql ; grep -vE "^\s*--" supabase/migrations/20260620120000_sync_jobs_durable_resource.sql | grep -c "error_message"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/20260620120000_sync_jobs_durable_resource.sql` returns 9 (one per new column).
    - `grep -vE '^\s*--' supabase/migrations/20260620120000_sync_jobs_durable_resource.sql | grep -c "error_message"` returns 0 (never references the nonexistent column).
    - `grep -cE "DROP |ALTER COLUMN|RENAME" supabase/migrations/20260620120000_sync_jobs_durable_resource.sql` returns 0 (additive only — no destructive ops on existing columns).
    - File contains `sync_jobs_org_isolation` (or an org-scoped policy name) AND a `pg_publication_tables` membership guard.
  </acceptance_criteria>
  <done>The additive migration adds all 9 columns, backfills source_app/mode on existing rows, adds an org-scoped RLS policy alongside the existing one, verifies (not re-adds) Realtime membership, and never names error_message or mutates existing columns.</done>
</task>

<task type="auto">
  <name>Task 2: Register sync_jobs in CROSS_ORG_TABLES</name>
  <files>src/test/rls-regression.test.ts</files>
  <read_first>
    - src/test/rls-regression.test.ts:40-76 (CROSS_ORG_TABLES array — append shape; the filterColumn union already includes "organization_id")
  </read_first>
  <action>
    In `src/test/rls-regression.test.ts`, append `{ table: "sync_jobs", filterColumn: "organization_id" }` to the `CROSS_ORG_TABLES` array. No type change is needed (the `filterColumn` union already includes `"organization_id"`). Do NOT touch any other file (Plan 01 owns the `cancelSyncJob` `error`-column fix in `sync-tab.service.ts`).
  </action>
  <verify>
    <automated>grep -c "sync_jobs" src/test/rls-regression.test.ts ; npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '{ table: "sync_jobs", filterColumn: "organization_id" }' src/test/rls-regression.test.ts` returns 1.
    - `npx tsc -p tsconfig.app.json --noEmit` reports zero errors.
  </acceptance_criteria>
  <done>sync_jobs is registered in CROSS_ORG_TABLES and the build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant JWT → sync_jobs | A user from Org B must never read Org A's sync_jobs rows once org columns exist. |
| migration runner → prod DB | DDL crosses into the production project (ref vltmrnjsubfzrgrtdqey) — must be additive and prod-ref-guarded (push is Plan 04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Information Disclosure | sync_jobs new org columns | mitigate | Add an org-scoped RLS policy using the organization_memberships predicate ALONGSIDE the existing user policy; register sync_jobs in CROSS_ORG_TABLES so the CI gate fails loud on any cross-org leak. Verified by Plan 04's rls-regression run. |
| T-24-05 | Tampering (data integrity) | additive migration on a live shared table | mitigate | ADD COLUMN IF NOT EXISTS only; all new columns nullable/defaulted; never DROP/ALTER/RENAME existing columns or retype the TEXT[] arrays — enforced by the acceptance grep (0 DROP/ALTER COLUMN/RENAME). In-flight jobs stay readable across the two-repo (brain + autopilot) contract. |
</threat_model>

<verification>
- `grep -c "ADD COLUMN IF NOT EXISTS"` on the migration returns 9.
- No `error_message`, no `DROP`/`ALTER COLUMN`/`RENAME` in the migration.
- `sync_jobs` present in CROSS_ORG_TABLES.
- `npx tsc -p tsconfig.app.json --noEmit` green.
(Live RLS isolation + column existence proven in Plan 04 after `supabase db push` against the TEST DB.)
</verification>

<success_criteria>
- Additive sync_jobs migration written (9 new columns, org RLS, Realtime verified, backfill of source_app/mode).
- sync_jobs registered in CROSS_ORG_TABLES.
- Build green; migration is non-destructive.
</success_criteria>

<output>
Create `.planning/phases/24-sync-status-foundation/24-02-SUMMARY.md` when done.
</output>
