---
phase: 24-sync-status-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260620120500_backfill_null_source_call_id.sql
  - supabase/migrations/20260620121000_reconcile_orphan_fathom_calls.sql
autonomous: true
requirements: [IMP-02, IMP-04]
must_haves:
  truths:
    - "The org-scoped unique constraint recordings_source_dedup is NOT recreated (it already exists live) — IMP-02 work is the NULL-source_call_id backfill plus a regression test (test lands in Plan 04)"
    - "Fathom recordings with NULL source_call_id are backfilled from fathom_provider_id::text where derivable; genuinely-unkeyable rows are documented as a bounded known gap, not silently left"
    - "The 60 truly-orphan fathom_calls are REPORTED for manual review by the reconciliation (operator-locked decision: EXCLUDE from the canonical reader, do NOT fabricate recordings rows)"
    - "All migrations are additive/idempotent and re-runnable; the orphan report table has fathom_call_id as PRIMARY KEY so ON CONFLICT DO NOTHING has a real arbiter"
  artifacts:
    - path: "supabase/migrations/20260620120500_backfill_null_source_call_id.sql"
      provides: "idempotent backfill of NULL source_call_id from fathom_provider_id for fathom rows; documents the residual gap"
      contains: "source_call_id"
    - path: "supabase/migrations/20260620121000_reconcile_orphan_fathom_calls.sql"
      provides: "idempotent orphan-report with fathom_call_id PRIMARY KEY: identifies fathom_calls with no recordings match by either bridge path; reports count, does not fabricate rows"
      contains: "canonical_recording_id"
  key_links:
    - from: "recordings.source_call_id"
      to: "recordings_source_dedup unique constraint"
      via: "backfill closes the NULL-escapes-constraint gap"
      pattern: "source_call_id"
    - from: "fathom_calls"
      to: "recordings"
      via: "canonical_recording_id (UUID) and fathom_provider_id (BIGINT) bridge paths"
      pattern: "canonical_recording_id"
---

<objective>
Close the two data-state gaps the live-prod introspection found: (IMP-02) the 184 `recordings` rows with NULL `source_call_id` that silently escape the org-scoped unique constraint, and (IMP-04) the 60 truly-orphan `fathom_calls` rows that match `recordings` by neither bridge path.

Purpose: The org-scoped unique constraint `recordings_source_dedup (organization_id, source_app, source_call_id)` ALREADY EXISTS in prod (migration 20260303000004) with zero blocking duplicates — so IMP-02 is NOT "create the constraint". Postgres treats NULLs as distinct, so NULL-`source_call_id` rows can be double-imported; this plan backfills them where derivable. For IMP-04, the operator-locked decision is that the canonical reader EXCLUDES orphans (do NOT fabricate recordings rows); the reconciliation migration REPORTS the orphan count/list for manual review.

Output: two additive, idempotent, re-runnable data migrations. (They are APPLIED and proven by the real-DB test in Plan 04 — this plan only WRITES the SQL.)
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-sync-status-foundation/24-RESEARCH.md
@supabase/CLAUDE.md

<interfaces>
<!-- Extracted from live-prod introspection (RESEARCH.md) + recording-ids contract. -->

LIVE FACTS (VERIFIED, prod ref vltmrnjsubfzrgrtdqey):
  - Constraint `recordings_source_dedup UNIQUE (organization_id, source_app, source_call_id)` ALREADY EXISTS (migration 20260303000004). DO NOT recreate/drop. Zero duplicate rows currently block it.
  - 184 recordings rows have NULL source_call_id (182 source_app='fathom', 2 'fathom-paste').
  - recordings.fathom_provider_id is BIGINT (Fathom's numeric id). source_call_id is TEXT.
  - fathom_calls has 2,094 rows; 2,017 have a valid canonical_recording_id (UUID) bridge; 60 are TRULY ORPHAN (no match by fathom_calls.recording_id BIGINT -> recordings.fathom_provider_id AND no valid canonical_recording_id UUID -> recordings.id).

Two bridge paths (both must be checked to classify an orphan):
  1. fathom_calls.recording_id (BIGINT) -> recordings.fathom_provider_id (BIGINT)
  2. fathom_calls.canonical_recording_id (UUID) -> recordings.id (UUID)

OPERATOR-LOCKED DECISIONS:
  - IMP-04: canonical reader EXCLUDES the 60 orphans. The reconciliation migration REPORTS them (count + ids surfaced via RAISE NOTICE and/or a report table), it does NOT INSERT fabricated recordings rows.
  - IMP-02 NULL handling: backfill source_call_id = fathom_provider_id::text for fathom rows WHERE fathom_provider_id IS NOT NULL; rows with both NULL are a bounded, documented gap (no provider id to re-fetch, so they cannot be double-imported via the connector path).

Orphan-report idempotency contract:
  - The report table `fathom_calls_orphan_report` MUST declare `fathom_call_id` as PRIMARY KEY in its
    CREATE TABLE so that `INSERT ... ON CONFLICT (fathom_call_id) DO NOTHING` has a real conflict arbiter.
    A bare `ON CONFLICT DO NOTHING` with no PK/unique would error ("there is no unique or exclusion
    constraint matching the ON CONFLICT specification"). PK arbiter is the chosen idempotency path.

Cross-ID rule (src/lib/recording-ids.ts): never parseInt/Number a UUID. In SQL, cast explicitly (fathom_provider_id::text) — never compare a BIGINT column directly against a UUID column.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Idempotent NULL source_call_id backfill migration (IMP-02 gap)</name>
  <files>supabase/migrations/20260620120500_backfill_null_source_call_id.sql</files>
  <read_first>
    - supabase/migrations/20260303000004_add_source_call_id.sql (the source_call_id column + recordings_source_dedup constraint origin — confirm the constraint already exists; do NOT recreate it)
    - .planning/phases/24-sync-status-foundation/24-RESEARCH.md "Pitfall 1", "Pitfall 2", "IMP-02" sections
    - src/CLAUDE.md "Recording IDs (two parallel systems)" (fathom_provider_id is BIGINT, source_call_id is TEXT)
    - supabase/CLAUDE.md (migration conventions)
  </read_first>
  <action>
    Create `supabase/migrations/20260620120500_backfill_null_source_call_id.sql`. Open with a comment block stating the constraint `recordings_source_dedup` ALREADY EXISTS and must NOT be recreated. Run an idempotent UPDATE: set `source_call_id = fathom_provider_id::text` on `public.recordings` WHERE `source_call_id IS NULL` AND `source_app IN ('fathom','fathom-paste')` AND `fathom_provider_id IS NOT NULL`. Use the `::text` cast explicitly (never implicit TEXT/BIGINT coercion). After the UPDATE, emit a residual-gap report via a `DO $$ ... RAISE NOTICE` block counting rows that still have `source_call_id IS NULL`. Add a SQL comment documenting that rows with both source_call_id AND fathom_provider_id NULL are a known bounded gap (no provider id to re-fetch → cannot be double-imported via the connector path) and that a `UNIQUE NULLS NOT DISTINCT` upgrade is deferred as a destructive change out of additive-only scope. Do NOT add/drop/recreate any constraint. The WHERE clause must make a second run a no-op (idempotent).
  </action>
  <verify>
    <automated>grep -c "fathom_provider_id::text" supabase/migrations/20260620120500_backfill_null_source_call_id.sql ; grep -cE "ADD CONSTRAINT|DROP CONSTRAINT|CREATE UNIQUE" supabase/migrations/20260620120500_backfill_null_source_call_id.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fathom_provider_id::text" supabase/migrations/20260620120500_backfill_null_source_call_id.sql` returns >= 1 (explicit cast used).
    - `grep -cE "ADD CONSTRAINT|DROP CONSTRAINT|CREATE UNIQUE" supabase/migrations/20260620120500_backfill_null_source_call_id.sql` returns 0 (constraint never recreated/dropped).
    - File contains a `RAISE NOTICE` reporting the residual NULL count and a comment documenting the bounded gap.
    - The UPDATE is guarded by `source_call_id IS NULL` so re-running is a no-op.
  </acceptance_criteria>
  <done>NULL source_call_id is backfilled from fathom_provider_id::text where derivable, the residual gap is reported + documented, no constraint is recreated, and the migration is idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: Idempotent orphan-report reconciliation migration (IMP-04)</name>
  <files>supabase/migrations/20260620121000_reconcile_orphan_fathom_calls.sql</files>
  <read_first>
    - .planning/phases/24-sync-status-foundation/24-RESEARCH.md "IMP-04", "Pitfall 3", "Runtime State Inventory", "Open Questions Q3" sections (the operator-locked EXCLUDE-and-report decision)
    - supabase/migrations/20260131000007_create_recordings_tables.sql (recordings columns: id UUID, fathom_provider_id BIGINT)
    - src/CLAUDE.md "Recording IDs (two parallel systems)" (the two bridge paths)
    - supabase/CLAUDE.md (migration conventions)
  </read_first>
  <action>
    Create `supabase/migrations/20260620121000_reconcile_orphan_fathom_calls.sql`. Implement an idempotent ORPHAN REPORT — do NOT INSERT into recordings (operator-locked: exclude orphans, do not fabricate). Define "orphan" as a `fathom_calls` row that matches recordings by NEITHER bridge path: NOT EXISTS a recordings row where `recordings.fathom_provider_id = fathom_calls.recording_id` (both BIGINT) AND NOT EXISTS a recordings row where `recordings.id = fathom_calls.canonical_recording_id` (both UUID; guard the UUID comparison so a NULL canonical_recording_id is treated as no-match, never cast a BIGINT to UUID). Persist the report idempotently: `CREATE TABLE IF NOT EXISTS public.fathom_calls_orphan_report (fathom_call_id <type matching fathom_calls PK> PRIMARY KEY, recording_id_bigint BIGINT, detected_at TIMESTAMPTZ DEFAULT now())` — `fathom_call_id` MUST be the PRIMARY KEY so the next step has a conflict arbiter. Then `INSERT INTO public.fathom_calls_orphan_report (fathom_call_id, recording_id_bigint) SELECT ... ON CONFLICT (fathom_call_id) DO NOTHING` the current orphan set (name the arbiter column explicitly — `ON CONFLICT (fathom_call_id)`, NOT a bare `ON CONFLICT DO NOTHING`). Re-running yields identical content (already-reported orphans hit the PK conflict and are skipped). Emit a `DO $$ ... RAISE NOTICE` with the orphan COUNT for operator visibility. Add a SQL comment stating these rows are intentionally EXCLUDED from the canonical reader and surfaced here for manual review (per the locked decision), NOT backfilled. Keep all comparisons type-safe (never compare BIGINT directly to UUID).
  </action>
  <verify>
    <automated>grep -c "canonical_recording_id" supabase/migrations/20260620121000_reconcile_orphan_fathom_calls.sql ; grep -cE "INSERT INTO (public\.)?recordings" supabase/migrations/20260620121000_reconcile_orphan_fathom_calls.sql ; grep -c "RAISE NOTICE" supabase/migrations/20260620121000_reconcile_orphan_fathom_calls.sql ; grep -c "fathom_call_id" supabase/migrations/20260620121000_reconcile_orphan_fathom_calls.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "canonical_recording_id" supabase/migrations/20260620121000_reconcile_orphan_fathom_calls.sql` returns >= 1 (the UUID bridge path is checked).
    - `grep -cE "INSERT INTO (public\.)?recordings" supabase/migrations/20260620121000_reconcile_orphan_fathom_calls.sql` returns 0 (NEVER fabricates recordings rows — operator-locked exclude decision).
    - `grep -c "RAISE NOTICE" supabase/migrations/20260620121000_reconcile_orphan_fathom_calls.sql` returns >= 1 (reports the orphan count).
    - `grep -cE "fathom_call_id[[:space:]]+[A-Za-z].*PRIMARY KEY" supabase/migrations/20260620121000_reconcile_orphan_fathom_calls.sql` returns >= 1 (the report table declares fathom_call_id as PRIMARY KEY — the arbiter).
    - `grep -c "ON CONFLICT (fathom_call_id)" supabase/migrations/20260620121000_reconcile_orphan_fathom_calls.sql` returns >= 1 (named arbiter, not a bare ON CONFLICT DO NOTHING).
    - File uses `CREATE TABLE IF NOT EXISTS` for the report table and re-running produces identical report content (idempotent via the PK arbiter).
  </acceptance_criteria>
  <done>The reconciliation identifies orphans via BOTH bridge paths with type-safe comparisons, persists them to a report table whose fathom_call_id PRIMARY KEY arbitrates an explicit `ON CONFLICT (fathom_call_id) DO NOTHING`, reports the count to the operator, never fabricates recordings rows, and is idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → prod recordings/fathom_calls | Data migrations touch real customer recording rows on the production project (ref vltmrnjsubfzrgrtdqey); push is Plan 04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Tampering (data integrity) | NULL source_call_id backfill | mitigate | Backfill only where `fathom_provider_id IS NOT NULL`; explicit `::text` cast; idempotent WHERE guard. Never recreate/drop the existing constraint (grep gate = 0). |
| T-24-08 | Tampering (data resurrection) | orphan reconciliation | accept | Operator-locked decision EXCLUDES orphans and reports them rather than fabricating recordings rows — avoids resurrecting deliberately-deleted calls. Acceptance grep enforces 0 INSERT INTO recordings. |
| T-24-09 | Tampering (UUID/BIGINT confusion) | bridge-path comparison | mitigate | Type-safe comparisons only (BIGINT↔BIGINT, UUID↔UUID); never cast BIGINT to UUID. Prevents `invalid input syntax for type uuid` and silent under-counting (Pitfall 3). |
| T-24-14 | Tampering (idempotency failure) | orphan-report ON CONFLICT | mitigate | The report table declares `fathom_call_id` PRIMARY KEY and the INSERT uses an explicit `ON CONFLICT (fathom_call_id) DO NOTHING`, so re-running the migration never errors and never duplicates rows. A bare ON CONFLICT with no arbiter would throw — prevented by the PK + named-arbiter grep gates. |
</threat_model>

<verification>
- NULL backfill: explicit `::text` cast present, no constraint recreate/drop, idempotent, residual gap reported.
- Orphan report: both bridge paths checked, 0 INSERT INTO recordings, orphan count reported, fathom_call_id PRIMARY KEY present, explicit `ON CONFLICT (fathom_call_id)` arbiter, idempotent.
(Live counts — NULL set shrinks correctly, orphan count matches the live 60 — are proven in Plan 04's real-DB test after `supabase db push`.)
</verification>

<success_criteria>
- Two additive, idempotent data migrations written.
- IMP-02 NULL gap backfilled where derivable + documented; constraint untouched.
- IMP-04 orphans reported (not fabricated) per operator-locked decision, with a PK-arbitrated ON CONFLICT for safe re-runs.
</success_criteria>

<output>
Create `.planning/phases/24-sync-status-foundation/24-03-SUMMARY.md` when done.
</output>
</content>
