---
phase: 24-sync-status-foundation
plan: 04
type: execute
wave: 2
depends_on: ["24-01", "24-02", "24-03"]
files_modified:
  - src/test/migrations/phase24-sync-status-foundation.integration.test.ts
autonomous: false
requirements: [IMP-01, IMP-02, IMP-03, IMP-04]
user_setup:
  - service: supabase-test-project
    why: "IMP-04's reconciliation test is real-DB only; mocked tests are FORBIDDEN (mocked tests passed for this exact bug class in the Phase 30/BUG-01 incident). The test runs against a SEPARATE TEST project, never prod."
    env_vars:
      - name: VITE_SUPABASE_TEST_URL
        source: ".env.test — SEPARATE Supabase TEST project (must NOT equal prod ref vltmrnjsubfzrgrtdqey)"
      - name: SUPABASE_TEST_SERVICE_ROLE_KEY
        source: ".env.test — TEST project service role key"
      - name: VITE_SUPABASE_TEST_ANON_KEY
        source: ".env.test — TEST project anon key (for the RLS-as-tenant assertions)"
must_haves:
  truths:
    - "All Phase 24 migrations are pushed to prod (ref vltmrnjsubfzrgrtdqey) via supabase db push, and to the TEST project, so the live DB has the new sync_jobs columns and the backfilled/reported data"
    - "A Zoom (UUID), a Fathom (BIGINT-as-string), and a paste recording all report synced=true from getSyncStatusForExternalIds in ONE query against the real TEST DB"
    - "Two inserts of the same (organization_id, source_app, source_call_id) yield exactly one recordings row (constraint blocks the 2nd) — proven against the real TEST DB"
    - "The new sync_jobs columns exist, recording_ids stays TEXT[], the org-scoped RLS policy isolates a second org's rows, and sync_jobs is in supabase_realtime — proven against the real TEST DB"
    - "The 60-style orphan classification (no recordings match by either bridge path) is reported by the reconciliation, verified against the real TEST DB; the test can NEVER hit prod"
  artifacts:
    - path: "src/test/migrations/phase24-sync-status-foundation.integration.test.ts"
      provides: "real-DB integration coverage of IMP-01/02/03/04, TEST-DB-guarded, skips cleanly when .env.test unset"
      contains: "integrationDbReachable"
  key_links:
    - from: "phase24 integration test"
      to: "TEST Supabase project"
      via: "makeIntegrationClient() + describe.skipIf(!integrationDbReachable)"
      pattern: "describe.skipIf\\(!integrationDbReachable\\)"
---

<objective>
Apply every Phase 24 migration to the live databases and prove all four requirements against a REAL TEST database — never mocked, never prod.

Purpose: Build and type checks pass WITHOUT the migration being applied (types come from config, not the live DB), so the phase cannot be considered done until (a) `supabase db push` lands the additive migrations on prod, and (b) a real-DB integration test proves the canonical reader, the idempotency constraint, the extended sync_jobs schema + RLS, and the orphan reconciliation. Mocked tests are FORBIDDEN for IMP-04 — they passed for this exact UUID/BIGINT bug class in the Phase 30/BUG-01 incident.

Output: the `supabase db push` (BLOCKING, prod-ref-guarded), the migrations applied to the TEST project, the new integration test file, and a green RLS regression run.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-sync-status-foundation/24-RESEARCH.md
@supabase/CLAUDE.md
@.planning/phases/24-sync-status-foundation/24-01-SUMMARY.md
@.planning/phases/24-sync-status-foundation/24-02-SUMMARY.md
@.planning/phases/24-sync-status-foundation/24-03-SUMMARY.md

<interfaces>
<!-- Extracted from codebase. Use these directly. -->

Test harness (src/test/integration-setup.ts):
  export const integrationDbReachable: boolean  // true only when VITE_SUPABASE_TEST_URL + SUPABASE_TEST_SERVICE_ROLE_KEY are set AND not equal to prod
  export function makeIntegrationClient(): SupabaseClient  // service-role TEST client
  - Module THROWS at load if TEST URL/key equals prod URL/key (hard guard).

Test template to model (src/test/migrations/phase39-fathom-reconcile-cron.integration.test.ts):
  - describe.skipIf(!integrationDbReachable)(...)
  - donor pattern: find an existing row's user_id/organization_id and reuse it (avoids auth.users admin createUser)
  - randomUUID() for seed ids; afterAll cleanup wrapped in try/catch deleting only seeded rows

Test scripts (package.json):
  - npm run test           -> vitest run (UNIT only; integration excluded by config)
  - npm run test:integration -> VITEST_INTEGRATION_OK=true vitest run --reporter=verbose src/**/*.integration.test.ts ...

Reader under test (Plan 01): getSyncStatusForExternalIds(sourceApp, externalIds[], opts?) from src/services/sync-status.service.ts.
Migrations under test (Plans 02/03): sync_jobs durable-resource, NULL source_call_id backfill, orphan reconciliation report.
Constraint under test: recordings_source_dedup UNIQUE (organization_id, source_app, source_call_id) — ALREADY LIVE.

DB safety (CLAUDE.md): migrations read DATABASE_URL from `.env` (PROD) and MUST verify the URL contains prod ref `vltmrnjsubfzrgrtdqey` before connecting. The prod-ref-guarded runner is ~/dev/autopilot/src/gsd-runner.ts. The integration TEST must NEVER point at vltmrnjsubfzrgrtdqey.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Apply Phase 24 migrations to prod and TEST DB (supabase db push)</name>
  <what-built>
    Plans 01-03 wrote three migration files (sync_jobs durable-resource, NULL source_call_id backfill, orphan reconciliation report) plus the canonical reader. Build/type checks pass WITHOUT the migration applied because types come from config, not the live DB — so the schema must be pushed before any real-DB verification can run.
  </what-built>
  <read_first>
    - supabase/CLAUDE.md "Running integration tests safely" + migration/DDL safety (the prod ref guard, .env=PROD)
    - .env (confirm DATABASE_URL contains prod ref vltmrnjsubfzrgrtdqey BEFORE connecting)
    - supabase/migrations/20260620120000_sync_jobs_durable_resource.sql
    - supabase/migrations/20260620120500_backfill_null_source_call_id.sql
    - supabase/migrations/20260620121000_reconcile_orphan_fathom_calls.sql
  </read_first>
  <action>
    Autonomously execute the push BEFORE pausing. (1) Verify `.env` DATABASE_URL contains `vltmrnjsubfzrgrtdqey` (abort if not — wrong project). (2) With `SUPABASE_ACCESS_TOKEN` set, apply the three additive Phase 24 migrations to PROD via the project's prod-ref-guarded path (`supabase db push`, or the gsd-runner at ~/dev/autopilot/src/gsd-runner.ts which already enforces the prod-ref guard — follow supabase/CLAUDE.md for the canonical command). (3) Apply the SAME migrations to the TEST project referenced by `.env.test` (VITE_SUPABASE_TEST_URL) so the integration test in Task 2 has the new columns/constraint/report. (4) Confirm the new sync_jobs columns and the orphan report table exist on BOTH databases. Then PAUSE for human verification of the push output (this is a production schema change).
  </action>
  <how-to-verify>
    1. Confirm the push output shows the three Phase 24 migrations applied (no errors, no destructive ops).
    2. On prod: confirm `sync_jobs` has the 9 new columns and `fathom_calls_orphan_report` exists.
    3. On the TEST project: confirm the same.
    4. Confirm DATABASE_URL used for the prod push contained `vltmrnjsubfzrgrtdqey` (right project) and the TEST push did NOT.
  </how-to-verify>
  <acceptance_criteria>
    - `supabase db push` (or gsd-runner) output shows all three 20260620* migrations applied to prod with zero errors.
    - The same three migrations are applied to the TEST project.
    - Prod ref guard confirmed: prod push used vltmrnjsubfzrgrtdqey; TEST push used a different ref.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the migrations are confirmed applied to both prod and TEST, or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Real-DB integration test for IMP-01/02/03/04 + RLS regression</name>
  <files>src/test/migrations/phase24-sync-status-foundation.integration.test.ts</files>
  <read_first>
    - src/test/migrations/phase39-fathom-reconcile-cron.integration.test.ts (the template: skipIf, donor pattern, randomUUID seeds, afterAll try/catch cleanup)
    - src/test/integration-setup.ts (integrationDbReachable, makeIntegrationClient, the prod-equality hard guard)
    - src/test/rls-regression.test.ts (the CROSS_ORG_TABLES gate now including sync_jobs from Plan 02)
    - src/services/sync-status.service.ts (the reader signature from Plan 01)
    - .planning/phases/24-sync-status-foundation/24-RESEARCH.md "Phase Requirements → Test Map" + "Wave 0 Gaps"
  </read_first>
  <action>
    Create `src/test/migrations/phase24-sync-status-foundation.integration.test.ts` wrapped in `describe.skipIf(!integrationDbReachable)`. Use `makeIntegrationClient()` (service-role TEST client) and the donor pattern (reuse an existing organization_id/owner_user_id) with `randomUUID()` seeds and an `afterAll` try/catch that deletes ONLY seeded rows. Cover: (IMP-01) seed three recordings under one org — a Zoom row (UUID-style source_call_id), a Fathom row (BIGINT-as-string source_call_id), a paste row — then assert `getSyncStatusForExternalIds` resolves all three via ONE call (test against the recordings table directly via the service-role client mirroring the reader's query, since the reader uses the anon client; assert the TEXT IN match returns all three with no coercion). (IMP-02) insert the same (organization_id, source_app, source_call_id) twice and assert the second insert is rejected by `recordings_source_dedup` (expect a unique-violation error) → exactly one row persists. (IMP-03) assert the new sync_jobs columns exist (insert a row using source_app/organization_id/mode/provider_cursor/last_heartbeat_at and read it back), assert `recording_ids` accepts a TEXT[] value, and assert sync_jobs is in supabase_realtime (query pg_publication_tables). (IMP-04) seed a synthetic orphan fathom_calls row (recording_id BIGINT with NO matching recordings.fathom_provider_id AND canonical_recording_id NULL), run/assert the orphan report classifies it as orphan and does NOT create a recordings row for it. Add an explicit assertion that `process.env.VITE_SUPABASE_TEST_URL` does NOT contain `vltmrnjsubfzrgrtdqey` (the test must never run against prod). Do NOT add any mocked variant of these assertions.
  </action>
  <verify>
    <automated>npm run test:integration -- src/test/migrations/phase24-sync-status-foundation.integration.test.ts && npx vitest run src/test/rls-regression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:integration -- src/test/migrations/phase24-sync-status-foundation.integration.test.ts` runs against the real TEST DB (NOT skipped) and passes IMP-01/02/03/04 assertions. If it reports "skipped" the TEST DB is unconfigured → IMP-04 is NOT verified (fail the gate, configure .env.test).
    - `npx vitest run src/test/rls-regression.test.ts` passes with `sync_jobs` exercised in CROSS_ORG_TABLES (cross-org isolation green).
    - `grep -c "describe.skipIf(!integrationDbReachable)" src/test/migrations/phase24-sync-status-foundation.integration.test.ts` returns >= 1.
    - `grep -c "vltmrnjsubfzrgrtdqey" src/test/migrations/phase24-sync-status-foundation.integration.test.ts` returns >= 1 AND it is inside a guard that ASSERTS the TEST url does NOT equal/contain the prod ref.
    - No mocked/`vi.mock` substitute for the reconciliation assertion exists in the file (`grep -c "vi.mock" ...` returns 0).
  </acceptance_criteria>
  <done>A real-DB, TEST-project-guarded integration test proves all four requirements; the RLS regression (with sync_jobs) is green; the test can never hit prod and has no mocked reconciliation path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration test → Supabase project | The test MUST hit the TEST project only; a misconfig pointing at prod would mutate real customer rows (the 2026-05 incident). |
| migration push → prod DB | DDL crosses into the production project; must be additive + prod-ref-verified. |
| tenant JWT → sync_jobs (RLS) | Cross-org isolation is proven here via the rls-regression run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | Tampering (test mutates prod) | integration test target | mitigate | integration-setup.ts throws at load if TEST url/key equals prod; describe.skipIf(!integrationDbReachable); explicit in-test assertion that VITE_SUPABASE_TEST_URL does not contain vltmrnjsubfzrgrtdqey. |
| T-24-11 | Information Disclosure | sync_jobs cross-org RLS | mitigate | rls-regression run with sync_jobs in CROSS_ORG_TABLES proves Org B's JWT reads 0 of Org A's sync_jobs rows; CI gate fails loud on any leak. |
| T-24-12 | Tampering (false-green via mock) | IMP-04 reconciliation verification | mitigate | Mocked tests FORBIDDEN (Phase 30/BUG-01 precedent); acceptance grep enforces 0 `vi.mock`; the test must run un-skipped (real TEST DB) or the gate fails. |
| T-24-13 | Tampering (destructive migration on prod) | supabase db push | mitigate | Migrations are additive only (proven in Plans 02/03 grep gates); prod push verifies vltmrnjsubfzrgrtdqey before connecting; BLOCKING human verification of push output. |
</threat_model>

<verification>
- `supabase db push` applied all three Phase 24 migrations to prod + TEST (BLOCKING checkpoint approved).
- `npm run test:integration -- ...phase24-sync-status-foundation...` un-skipped and green (IMP-01/02/03/04).
- `npx vitest run src/test/rls-regression.test.ts` green with sync_jobs cross-org isolation.
- Test guarded against prod (skipIf + explicit prod-ref assertion); no mocked reconciliation.
</verification>

<success_criteria>
- Phase 24 migrations live on prod and TEST.
- Real-DB test proves Zoom+Fathom+paste all synced=true from one query, double-import yields one row, sync_jobs schema+RLS+Realtime correct, orphan classified+reported without fabrication.
- RLS regression green. Mocked reconciliation forbidden and absent.
</success_criteria>

<output>
Create `.planning/phases/24-sync-status-foundation/24-04-SUMMARY.md` when done.
</output>
