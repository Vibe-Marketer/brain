---
phase: 28-server-side-sync-all
plan: 04
type: execute
wave: 3
depends_on: ["28-02", "28-03"]
files_modified:
  - supabase/migrations/20260625120000_sync_all_resume_heartbeat.sql
  - src/components/connectors/registry/adapters/fathom.ts
  - src/components/connectors/registry/adapters/grain.ts
  - src/components/connectors/registry/adapters/zoom.ts
  - src/components/connectors/registry/adapters/read-ai.ts
  - src/components/connectors/registry/adapters/fireflies.ts
  - src/components/connectors/registry/adapters/plaud.ts
  - src/components/connectors/import/ImportSurface.tsx
autonomous: true
requirements: [SYNC-01, SYNC-02]
must_haves:
  truths:
    - "A pg_cron resume-heartbeat re-kicks 'processing' mode='all' jobs whose heartbeat is stale (>2min) but not yet reaper-failed (<5min), via pg_net to connector-sync-all with NO JWT (service-role resume path)"
    - "The 6 list-API adapters implement syncAll (create the sync-all job); YouTube/file-upload leave it undefined"
    - "The frontend 'Sync all' button is wired to create a sync-all job and shows progress via the Phase 27 useSyncJobs surface — builds green but is NOT pushed to origin"
    - "A measure-first task records real per-provider slice latency on TEST and tunes SLICE_ITEM_BUDGET"
  artifacts:
    - path: "supabase/migrations/20260625120000_sync_all_resume_heartbeat.sql"
      provides: "additive pg_cron resume-heartbeat for stalled sync-all jobs"
      contains: "sync-all-resume-heartbeat"
    - path: "src/components/connectors/import/ImportSurface.tsx"
      provides: "Sync all button wiring (build-green, not pushed)"
      contains: "syncAll"
  key_links:
    - from: "supabase/migrations/20260625120000_sync_all_resume_heartbeat.sql"
      to: "connector-sync-all edge fn"
      via: "net.http_post (no Authorization header → service-role resume branch)"
      pattern: "net.http_post"
    - from: "src/components/connectors/registry/adapters/*.ts"
      to: "ConnectorAdapter.syncAll"
      via: "syncAll impl creating the sync-all job"
      pattern: "syncAll"
---

<objective>
Close the resume safety net and the user-facing seam: an additive pg_cron resume-heartbeat (belt-and-suspenders with the LIVE Phase 27 reaper), the 6 adapter `syncAll` implementations, and the frontend "Sync all" button wiring — plus a measure-first task to tune the per-slice item budget from real TEST latency. All frontend builds green but is NOT pushed to origin (batched to milestone-end per CONTEXT).

Purpose: Self-chain is the fast loop; the resume cron re-advances any 'processing' sync-all job whose self-chain link dropped, before the 5-min reaper gives up. The adapters + button make the capability reachable from the UI. The measurement task replaces a guessed slice budget with data (RESEARCH A1).
Output: One additive migration (NOT pushed here — Plan 05 pushes), 6 adapter syncAll entries, the button wiring, and a recorded slice-budget measurement.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-server-side-sync-all/28-RESEARCH.md
@.planning/phases/28-server-side-sync-all/28-CONTEXT.md
@supabase/CLAUDE.md
@src/CLAUDE.md
@.planning/phases/28-server-side-sync-all/28-02-PLAN.md
@.planning/phases/28-server-side-sync-all/28-03-PLAN.md

<read_first>
- supabase/migrations/20251128100000_embedding_queue_system.sql lines 257-278 — the cron.schedule + net.http_post pattern (graceful undefined_function/others degradation; NO Authorization header in the POST). Copy this shape.
- supabase/migrations/20260623120000_sync_jobs_reaper.sql — the LIVE reaper (5-min stale fail threshold); the resume-heartbeat must fire EARLIER (2min) so it re-kicks before the reaper fails the job. Confirm it is purely additive (no ALTER/DROP) — STATE confirms 27-02/27-04 shipped it additively.
- src/components/connectors/registry/adapters/fathom.ts (existing searchAvailable/importSelected impl — the pattern syncAll mirrors; how it invokes its edge fn + returns a jobId-bearing result).
- src/components/connectors/registry/adapters/plaud.ts (already has searchAvailable + importSelected per spike — add syncAll; correcting the prior webhook-only classification).
- src/components/connectors/registry/types.ts (the syncAll? contract from Plan 01).
- src/components/connectors/import/ImportSurface.tsx (the Phase-26 sync-all affordance seam + Phase-25 select-all-matching descriptor client twin + Phase-27 useSyncJobs banner/chip already mounted — wire the button into the existing seam, do not invent a new surface).
- src/hooks/useSyncJobs.ts (Phase 27 progress hook the button's progress reads through — no new query).
</read_first>

<interfaces>
The resume-heartbeat cron (additive, model the embedding-worker-backup):
  schedule 'sync-all-resume-heartbeat' '* * * * *' →
    net.http_post(url=.../functions/v1/connector-sync-all, headers={Content-Type}, body={jobId})
    FROM sync_jobs WHERE status='processing' AND mode='all'
      AND provider_cursor IS NOT NULL
      AND last_heartbeat_at < NOW() - INTERVAL '2 minutes'
  NO Authorization header → connector-sync-all takes its service-role resume branch (Plan 02 Task 1).

ConnectorAdapter.syncAll (Plan 01 contract): creates a sync-all sync_jobs row via connector-sync-all start path, returns a jobId-bearing descriptor; progress observed via useSyncJobs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive resume-heartbeat cron migration + slice-latency measurement on TEST</name>
  <files>supabase/migrations/20260625120000_sync_all_resume_heartbeat.sql</files>
  <read_first>
  - supabase/migrations/20251128100000 lines 257-278 (cron.schedule + net.http_post, graceful degradation).
  - supabase/migrations/20260623120000 (reaper 5-min threshold — resume must fire earlier).
  </read_first>
  <action>
  Write supabase/migrations/20260625120000_sync_all_resume_heartbeat.sql — STRICTLY ADDITIVE (no ALTER/DROP): register a 'sync-all-resume-heartbeat' pg_cron job ('* * * * *') that net.http_post's to connector-sync-all with body {jobId} (NO Authorization header) for every sync_jobs row WHERE status='processing' AND mode='all' AND provider_cursor IS NOT NULL AND last_heartbeat_at < NOW() - INTERVAL '2 minutes'. Wrap in DO/EXCEPTION graceful degradation (undefined_function then notice; others then notice) exactly like embedding-worker-backup so a free-tier env without pg_cron does not error. Use the prod URL host with the prod ref (the cron lives in the DB; the migration is pushed prod-ref-guarded in Plan 05). Add a guard so re-running the migration does not double-register (unschedule-if-exists or a DO-block existence check), per additive-migration discipline. Do NOT touch the reaper or any existing cron.

  Then run a slice-latency MEASUREMENT against the TEST project (RESEARCH A1 — measure-first): invoke connector-sync-all for one slice per available provider on TEST (or exercise the listPage path directly), log elapsed wall-clock per slice, and record the numbers in the SUMMARY. Use the measured worst case to set or confirm SLICE_ITEM_BUDGET in connector-sync-all (Plan 02) so a slice stays comfortably under 150s — give Read.ai (10/page) and Zoom (300/page, 30-day windows) explicit attention as the outliers. If a provider has no TEST credentials, note it and fall back to the conservative ~20-25 default with the rationale recorded. This measurement does NOT push to prod.
  </action>
  <verify>
    <automated>rtk grep -c "sync-all-resume-heartbeat" supabase/migrations/20260625120000_sync_all_resume_heartbeat.sql; rtk grep -v '^ *--' supabase/migrations/20260625120000_sync_all_resume_heartbeat.sql | rtk grep -c "ALTER \|DROP "</automated>
  </verify>
  <acceptance_criteria>
  - Migration registers the resume-heartbeat cron with the 2-min stale predicate scoped to mode='all' + non-null provider_cursor, fires net.http_post with NO Authorization header, graceful-degradation wrapped, idempotent re-registration; ZERO destructive DDL (ALTER/DROP count is 0 on non-comment lines).
  - Measured (or rationalized-default) SLICE_ITEM_BUDGET recorded in the SUMMARY with per-provider latency notes; Read.ai + Zoom outliers addressed.
  - Migration NOT pushed to any DB in this plan (push is Plan 05).
  </acceptance_criteria>
  <done>Additive resume-heartbeat migration authored (un-pushed); slice budget measured/tuned on TEST and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: 6 adapter syncAll impls + frontend Sync-all button wiring (build green, not pushed)</name>
  <files>src/components/connectors/registry/adapters/fathom.ts, src/components/connectors/registry/adapters/grain.ts, src/components/connectors/registry/adapters/zoom.ts, src/components/connectors/registry/adapters/read-ai.ts, src/components/connectors/registry/adapters/fireflies.ts, src/components/connectors/registry/adapters/plaud.ts, src/components/connectors/import/ImportSurface.tsx</files>
  <read_first>
  - src/components/connectors/registry/adapters/fathom.ts (searchAvailable/importSelected as the syncAll pattern; how it calls its edge fn + returns jobId).
  - src/components/connectors/import/ImportSurface.tsx (Phase-26 sync-all seam; Phase-25 select-all-matching descriptor; Phase-27 useSyncJobs banner/chip mount points).
  - src/hooks/useSyncJobs.ts (progress surface — reuse, no new query).
  - src/CLAUDE.md (Remix Icons only; motion/react not framer-motion; @/ aliases; no id coercion).
  </read_first>
  <action>
  Implement ConnectorAdapter.syncAll on the 6 list-API adapters (fathom, grain, zoom, read-ai, fireflies, plaud) per SYNC-02. Each syncAll invokes the connector-sync-all start path (the JWT path from Plan 02) with { sourceId, source_app, dateStart, dateEnd } and returns the jobId-bearing descriptor (mirror the existing importSelected return shape). Leave syncAll UNDEFINED on the youtube and file-upload adapters (no list endpoint) so the surface shows "imports automatically".

  Wire the "Sync all from this provider" button into ImportSurface.tsx at the existing Phase-26 sync-all affordance seam: when the active provider's adapter has syncAll defined, render the button; on click it calls adapter.syncAll with the current date-range filter (the Phase-25 select-all-matching descriptor is the client twin — the SERVER enumerates, the button does not pass enumerated ids). Progress shows via the already-mounted Phase-27 useSyncJobs banner/chip — DO NOT add a new progress query. When syncAll is undefined (youtube/file-upload), show the "imports automatically" affordance instead of the button.

  CRITICAL boot-crash guard (phase-spanning knowledge): the registry/adapters churn risks zeroing OAUTH_CALLBACK_ROUTES then prod white screen. Build against the COMMITTED tree before considering done. This frontend is BUILD-GREEN ONLY — it is NOT pushed to origin (batched to milestone-end per CONTEXT/deferred).
  </action>
  <verify>
    <automated>rtk grep -c "syncAll" src/components/connectors/registry/adapters/fathom.ts src/components/connectors/registry/adapters/grain.ts src/components/connectors/registry/adapters/zoom.ts src/components/connectors/registry/adapters/read-ai.ts src/components/connectors/registry/adapters/fireflies.ts src/components/connectors/registry/adapters/plaud.ts; rtk grep -c "syncAll" src/components/connectors/import/ImportSurface.tsx; rtk proxy npm run build 2>&1 | rtk grep -c "error\|White screen\|OAUTH_CALLBACK_ROUTES" || true</automated>
  </verify>
  <acceptance_criteria>
  - All 6 list-API adapters export a syncAll impl (grep count 1 per file); youtube/file-upload do NOT (verified absent).
  - ImportSurface renders the Sync-all button when syncAll is defined, "imports automatically" otherwise; progress reuses useSyncJobs (no new query).
  - npm run build exit 0 against the COMMITTED tree; OAUTH_CALLBACK_ROUTES non-empty (boot-crash guard holds).
  - NO origin push (frontend batched to milestone-end).
  </acceptance_criteria>
  <done>6 adapters advertise syncAll; the button is wired into the existing seam; build is green on the committed tree; nothing pushed to origin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron/pg_net → connector-sync-all | No JWT; drives the service-role resume branch — must bind to job-row org/user |
| browser → connector-sync-all (via adapter.syncAll) | JWT-bearing start; org/workspace membership-validated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-11 | Elevation | resume cron advancing a job into the wrong org | mitigate | cron passes only jobId with no Authorization; connector-sync-all resume branch binds to the job row's stored org/user (Plan 02) |
| T-28-12 | DoS | resume cron + self-chain double-driving a job (duplicate slices) | accept | duplicate slices are idempotent — 23505 reclassified as skipped (Plan 02); cron only fires when heartbeat stale >2min so overlap is bounded |
| T-28-13 | Tampering | migration accidentally mutating reaper/existing cron | mitigate | strictly additive; only schedules a NEW cron; no ALTER/DROP; idempotent re-registration; pushed prod-ref-guarded in Plan 05 |
| T-28-14 | DoS / availability | adapter churn zeroes OAUTH_CALLBACK_ROUTES then prod white screen | mitigate | build against committed tree; OAUTH_CALLBACK_ROUTES non-empty assertion |
| T-28-SC | Tampering | npm/deno installs | accept | zero new packages (RESEARCH audit N/A) |
</threat_model>

<verification>
- Resume-heartbeat cron additive, 2-min mode='all' predicate, no-JWT net.http_post, idempotent, 0 destructive DDL.
- Slice budget measured/recorded; Read.ai + Zoom outliers handled.
- 6 adapters advertise syncAll; youtube/file-upload do not; ImportSurface button wired; build green on committed tree; no origin push.
</verification>

<success_criteria>
The resume safety net (cron) is authored additively and the user-facing sync-all (adapters + button) is wired and build-green, with the slice budget tuned from real TEST data — all staged for the Plan 05 prod push.
</success_criteria>

<output>
Create `.planning/phases/28-server-side-sync-all/28-04-SUMMARY.md` when done.
</output>
