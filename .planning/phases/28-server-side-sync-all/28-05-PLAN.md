---
phase: 28-server-side-sync-all
plan: 05
type: execute
wave: 4
depends_on: ["28-04"]
files_modified: []
autonomous: false
requirements: [SYNC-01, SYNC-02, SYNC-03]
user_setup: []
must_haves:
  truths:
    - "The real-DB concurrency/idempotency + resume integration tests (Plan 01 scaffolds) run GREEN against the TEST project with the pager + listPage deployed — not a guard skip"
    - "The resume-heartbeat migration is pushed TEST-then-PROD, prod-ref-guarded, additive-verified (0 destructive DDL)"
    - "connector-sync-all is deployed to PROD via --use-api (Docker-less)"
    - "A live end-to-end sync-all on TEST completes across multiple self-chaining slices, resumes after a killed slice, and produces no duplicates"
  artifacts: []
  key_links:
    - from: "sync-all-resume-heartbeat cron (PROD)"
      to: "connector-sync-all (PROD)"
      via: "net.http_post verified registered + active in cron.job"
      pattern: "sync-all-resume-heartbeat"
---

<objective>
[BLOCKING] Prove Phase 28 on a real database, then ship the backend to prod. Run the Plan 01 integration scaffolds GREEN (truthful, not guard-skipped) against the TEST project with the pager + listPage + resume cron in place; then push the resume-heartbeat migration TEST-then-PROD (prod-ref-guarded, additive-verified) and deploy connector-sync-all to PROD via --use-api.

Purpose: This is the highest-risk phase's go/no-go gate. SYNC-03's correctness (no duplicates under concurrency; 23505 to skipped) and SYNC-01's resumability are claims that MUST be proven on a real DB, not mocked — per BUG-01 the exact mocked-passes/real-fails bug class lives here. The backend pager + cron must reach prod to function; the frontend stays un-pushed (milestone-end).
Output: A truthful GREEN integration run, a prod-deployed pager + resume cron verified active, and a live multi-slice sync-all proof. No frontend origin push.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-server-side-sync-all/28-RESEARCH.md
@supabase/CLAUDE.md
@.planning/phases/28-server-side-sync-all/28-01-PLAN.md
@.planning/phases/28-server-side-sync-all/28-02-PLAN.md
@.planning/phases/28-server-side-sync-all/28-04-PLAN.md

<read_first>
- supabase/CLAUDE.md "Running integration tests safely" (triple guard; npm run test:integration; TEST-ref-only; cleanup contract) + "Deploying Edge Functions" (--use-api, Docker-less).
- .planning/STATE.md last-session 27-04 entry — the EXACT proven prod-push procedure to mirror: link TEST (ref swjzxiddcrtaqixsfaac, TEST DB password from .env.local) then db push; then prod-ref guard (assert DATABASE_URL contains vltmrnjsubfzrgrtdqey, booleans only) then link PROD then db push; then functions deploy --use-api.
- supabase/functions/connector-sync-all/__tests__/resume.integration.test.ts + idempotency.integration.test.ts (Plan 01 — the proofs to turn truthfully GREEN).
- CLAUDE.md env-file map: .env = PROD ref vltmrnjsubfzrgrtdqey; .env.local/.env.test = TEST. Migrations/DDL read .env; verify prod ref before connecting.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the real-DB integration proofs GREEN on TEST (truthful, not a guard skip)</name>
  <files></files>
  <read_first>
  - supabase/CLAUDE.md integration-test safety + cleanup contract.
  - The two Plan 01 integration test files.
  - STATE 27-04 entry — the "seed under a real auth.users id so the donor-guard doesn't short-circuit" technique used to prove the reaper LIVE.
  </read_first>
  <action>
  First, push the Plan 04 resume-heartbeat migration to the TEST project so the schema/cron exist there (link TEST ref swjzxiddcrtaqixsfaac, TEST DB password from .env.local, supabase db push --linked). Deploy connector-sync-all to TEST via --use-api so the pager is invokable. Confirm the 6 listPage providers + registry are present (they are code, deployed with the fn).

  Then run the integration suite (npm run test:integration, VITEST_INTEGRATION_OK=true, TEST env) for:
  - idempotency.integration.test.ts (SYNC-03): concurrent selective-import + sync-all of the SAME source_call_id → exactly ONE recordings row; the loser is reclassified skipped (in skipped_count), NOT in failed_ids; a slice retry after simulated crash produces no duplicate.
  - resume.integration.test.ts (SYNC-01): one slice advances provider_cursor + freshens last_heartbeat_at; a killed-mid-run job resumes from provider_cursor to a terminal status; failed_ids/skipped_count written.

  If the TEST project has no seed rows and the donor-guard would short-circuit, SEED under a real auth.users id (mirror the 27-04 technique) so the assertions actually execute — a truthful GREEN, never a guard skip claimed as a pass. Clean up every seeded row in afterAll (capture-before-mutate, try/catch, cleanup_test_fixture_users). Zero mocks.

  Finally, run a LIVE end-to-end multi-slice proof on TEST: start a sync-all over a range large enough to require 2+ slices for at least one provider with TEST credentials; observe self-chaining advance provider_cursor across slices to a terminal status; then kill a slice mid-run and confirm the resume path (self-chain or the 2-min cron) re-advances it to completion with no duplicates. Record slice count + final status + dedup count in the SUMMARY.
  </action>
  <verify>
    <human-check>
    Integration suite output shows resume + idempotency tests GREEN (assertions executed, not skipped). The live multi-slice proof shows provider_cursor advancing across >=2 slices to a terminal status, a killed slice resuming, and exactly one recordings row per call. Confirm the numbers in the SUMMARY.
    </human-check>
  </verify>
  <acceptance_criteria>
  - resume.integration.test.ts + idempotency.integration.test.ts pass GREEN against TEST with assertions actually executed (seeded if needed), not a describe.skipIf short-circuit.
  - Live TEST sync-all completes across >=2 self-chaining slices; a killed slice resumes from provider_cursor; concurrent import + sync-all yields exactly 1 recordings row per call; 23505 loser counted in skipped_count, absent from failed_ids.
  - All seeded TEST rows cleaned up (suite re-runnable, leaves DB unchanged).
  </acceptance_criteria>
  <done>SYNC-01 and SYNC-03 are proven on a real DB (truthful GREEN + live multi-slice/resume/no-dup proof); cleanup verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
  The resume-heartbeat migration (Plan 04) is verified additive and the pager + listPage + adapters are tested GREEN on TEST. This checkpoint pushes the migration to PROD and deploys connector-sync-all to PROD — the [BLOCKING] prod write the phase requires. PROD writes are destructive-adjacent; operator pre-approval per the 27-04 precedent.
  </what-built>
  <how-to-verify>
  Procedure (mirror the proven 27-04 prod-push exactly):
  1. Verify the Plan 04 migration is STRICTLY ADDITIVE: grep for ALTER/DROP on non-comment lines — must be 0 (only cron.schedule + net.http_post).
  2. TEST first: link TEST ref swjzxiddcrtaqixsfaac (TEST DB password from .env.local), `supabase db push --linked`; confirm 'sync-all-resume-heartbeat' appears in cron.job (active=true) on TEST.
  3. PROD-REF GUARD: assert DATABASE_URL from .env contains vltmrnjsubfzrgrtdqey (booleans only, no secret echo) BEFORE connecting.
  4. PROD push: link PROD ref vltmrnjsubfzrgrtdqey, `supabase db push`; confirm 'sync-all-resume-heartbeat' cron active on PROD via direct pg query against cron.job.
  5. Deploy `supabase functions deploy connector-sync-all --use-api` to PROD (Docker-less). Confirm the fn is live.
  6. Phase gate: `npm run build` exit 0 on the committed tree (boot-crash guard); full unit suite shows ZERO new failures vs the documented baseline.
  Expected: resume cron active on PROD+TEST; connector-sync-all deployed to PROD; build green; no new test failures; NO origin push (frontend batched to milestone-end).
  </how-to-verify>
  <resume-signal>Type "approved" after the prod push + deploy are verified (cron active on PROD, fn live), or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → PROD Supabase | Migration push + edge deploy against the prod ref — catastrophic if confused with TEST |
| test harness → TEST Supabase | Service-role bypasses RLS; TEST ref only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-15 | Tampering | migration pushed to wrong project (prod/test confusion) | mitigate | prod-ref guard asserts DATABASE_URL contains vltmrnjsubfzrgrtdqey before connect (booleans only); TEST uses .env.local creds; 27-04 proven procedure |
| T-28-16 | Tampering | integration test mutating prod | mitigate | triple-guarded TEST-only env; cleanup_test_fixture_users afterAll; mocks rejected |
| T-28-17 | Availability | bad edge deploy or zeroed registry breaks prod | mitigate | build-green committed-tree gate + OAUTH_CALLBACK_ROUTES assertion before deploy; --use-api verified live |
| T-28-SC | Tampering | npm/deno installs at deploy | accept | zero new packages (RESEARCH audit N/A) |
</threat_model>

<verification>
- Integration proofs GREEN on TEST (executed, not skipped) + live multi-slice/resume/no-dup proof.
- Migration additive (0 destructive DDL); pushed TEST-then-PROD prod-ref-guarded; resume cron active on both refs.
- connector-sync-all deployed to PROD via --use-api; build green; zero new test failures.
- No origin push.
</verification>

<success_criteria>
SYNC-01/02/03 proven on a real DB and the backend (pager + resume cron) is live on PROD, verified active — the highest-risk phase shipped safely, frontend staged for milestone-end.
</success_criteria>

<output>
Create `.planning/phases/28-server-side-sync-all/28-05-SUMMARY.md` when done.
</output>
