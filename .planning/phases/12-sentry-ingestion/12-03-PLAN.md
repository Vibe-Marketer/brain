---
phase: 12-sentry-ingestion
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - supabase/functions/sentry-webhook/__tests__/sentry-webhook.integration.test.ts
  - docs/operations/sentry-webhook-setup.md
autonomous: true
requirements: [SEN-01, SEN-02]
user_setup:
  - service: sentry
    why: "Live end-to-end proof (real Sentry alert → ticket) is blocked until Andrew completes the Sentry-side setup documented in docs/operations/sentry-webhook-setup.md — the ONE human prerequisite for this phase"
    env_vars:
      - name: SENTRY_WEBHOOK_SECRET
        source: "Internal integration Client Secret — supabase secrets set SENTRY_WEBHOOK_SECRET=<client-secret>"
    dashboard_config:
      - task: "Complete the internal integration + alert rule per docs/operations/sentry-webhook-setup.md"
        location: "sentry.io org ai-simple / project call-vault"
must_haves:
  truths:
    - "Two synthetic deliveries with the same issue_id yield exactly one ticket with occurrence_count=2 and an advanced last_seen_at (SEN-02 probe, ISA ISC-17)"
    - "A fatal-level synthetic delivery creates a ticket AND one user_notifications row per ADMIN; the second delivery adds no notification"
    - "A delivery with a bad signature leaves the database untouched"
    - "The deployed function at /functions/v1/sentry-webhook responds 401 to an unsigned POST (deploy proof)"
    - "Andrew has a complete, exact sentry.io click path + webhook URL + secret destination in docs/operations/sentry-webhook-setup.md"
  artifacts:
    - path: "supabase/functions/sentry-webhook/__tests__/sentry-webhook.integration.test.ts"
      provides: "Real-Supabase behavioral proof of dedup + notification semantics"
    - path: "docs/operations/sentry-webhook-setup.md"
      provides: "Human-prerequisite runbook: integration creation, alert rule, secret provisioning, smoke test"
  key_links:
    - from: "sentry-webhook.integration.test.ts"
      to: "public.ingest_sentry_ticket"
      via: "service-role rpc calls against the TEST project"
      pattern: "ingest_sentry_ticket"
---

<objective>
Prove SEN-01/SEN-02 behaviorally against a real database, deploy the function, and write the operator runbook for the single human prerequisite (Sentry-side configuration).

Purpose: Build/type gates cannot prove dedup or notification semantics — only real INSERT/ON CONFLICT execution can. The runbook converts "Sentry setup" from tribal knowledge into an exact click path Andrew can execute in five minutes.
Output: Integration test, deployed function, `docs/operations/sentry-webhook-setup.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-sentry-ingestion/12-CONTEXT.md
@.planning/phases/12-sentry-ingestion/12-RESEARCH.md
@.planning/phases/12-sentry-ingestion/12-PATTERNS.md
@supabase/CLAUDE.md
@src/test/rls-regression.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-Supabase integration test for dedup + notification semantics</name>
  <files>supabase/functions/sentry-webhook/__tests__/sentry-webhook.integration.test.ts</files>
  <read_first>
    - supabase/CLAUDE.md (RUNNING INTEGRATION TESTS SAFELY — test project only, VITEST_INTEGRATION_OK gate, cleanup contract, donor pattern)
    - src/test/rls-regression.test.ts (reference afterAll try/catch cleanup idiom; describe.skipIf guard pattern)
    - .planning/phases/12-sentry-ingestion/12-01-PLAN.md frontmatter must_haves (the RPC semantics under test)
  </read_first>
  <action>
    Write a vitest integration test (picked up by `npm run test:integration` glob `supabase/functions/**/__tests__/*.integration.test.ts`) that exercises `ingest_sentry_ticket` via the TEST project's service-role client (env: `VITE_SUPABASE_TEST_URL` + `SUPABASE_TEST_SERVICE_ROLE_KEY`; `describe.skipIf` cleanly skips when unset). NOTE: requires the test project to have migrations applied (`supabase db push` against the linked test ref per supabase/CLAUDE.md Option A) — assert the RPC exists first and skip with a clear message if the test project is behind.

    Cases (use a unique TEST-tagged fingerprint per run, e.g. `sentry:test-<random hex>` so reruns are idempotent):
    1. First call → returns created=true, occurrence_count=1; tickets row exists with source='sentry', type='bug', status='new', reporter_id NULL, severity as passed; ticket_events has a 'created' row with actor_id NULL.
    2. Second call, same fingerprint → created=false, occurrence_count=2; SELECT count(*) of tickets with that fingerprint = 1; last_seen_at strictly greater than the first call's value; severity/context unchanged from first call even if different args passed.
    3. Severity 'high' first call → for a temp ADMIN user (create temp auth user + user_roles ADMIN row inside the test, donor pattern allowed), exactly one user_notifications row with metadata.ticket_id = the ticket; second call adds zero new notifications.
    4. RLS visibility: a temp NON-admin authenticated user selecting tickets sees zero NULL-reporter Sentry rows; service-role sees them (proves T-12-04 disposition).

    Cleanup contract: delete created tickets (cascades messages/events), user_notifications rows, temp users via `cleanup_test_fixture_users` RPC; wrap each cleanup step in try/catch; suite idempotent across reruns.
  </action>
  <verify>
    <automated>npm run test:integration -- sentry-webhook (exit 0, or clean documented skip when .env.test unset)</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at the exact path and is matched by the `npm run test:integration` glob
    - Cases 1-4 implemented with exact assertions (occurrence_count values, single-row count, zero-notification on dedup, RLS zero-rows for non-admin)
    - Cleanup follows the supabase/CLAUDE.md contract (temp rows, try/catch afterAll, idempotent)
    - When `.env.test` is configured: suite green. When not: `describe.skipIf` reports a clean skip — SUMMARY must state which outcome occurred and, if skipped, flag the live-run as outstanding verification
  </acceptance_criteria>
  <done>SEN-02 (dedup) and the notification rule have executable, repeatable behavioral proof.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy function + write the Sentry setup runbook</name>
  <files>docs/operations/sentry-webhook-setup.md</files>
  <read_first>
    - supabase/CLAUDE.md (deploy with --use-api; Docker absent)
    - .planning/phases/12-sentry-ingestion/12-RESEARCH.md ("Manual Sentry Setup" section — the verified click path and citation notes)
    - docs/operations/mcp-runbook.md (existing operations-doc tone/structure to mirror)
  </read_first>
  <action>
    1. Deploy: `supabase functions deploy sentry-webhook --use-api`. Capture the deployed URL. Smoke: `curl -s -o /dev/null -w "%{http_code}" -X POST https://<project-ref>.supabase.co/functions/v1/sentry-webhook -d '{}'` MUST return 401 (signature gate live, gateway JWT exemption working — a 401 from OUR body shape, not the gateway's "Missing authorization header").

    2. Write `docs/operations/sentry-webhook-setup.md` — the human-prerequisite runbook. Content (from RESEARCH.md verified click path; keep the [ASSUMED] caveat on the Client Secret label):
       - **Prerequisite framing:** "Execution of live Sentry ingestion has ONE human prerequisite: this page."
       - **Step 1 — Create the integration:** sentry.io → org `ai-simple` → Settings → Developer Settings → Internal Integrations → New Internal Integration. Name: `CallVault Tickets`. Webhook URL: `https://<project-ref>.supabase.co/functions/v1/sentry-webhook` (paste the real project-ref URL captured at deploy). Enable **Alert Rule Action**. Save.
       - **Step 2 — Provision the secret:** copy the integration's **Client Secret** from the integration page → run `supabase secrets set SENTRY_WEBHOOK_SECRET=<client-secret>` (this is where the secret goes; never commit it). Note the fallback: if the signature path is ever unavailable, set `SENTRY_WEBHOOK_SECRET=qp:<random>` and append `?secret=<random>` to the webhook URL instead.
       - **Step 3 — Create the alert rule:** project `call-vault` → Alerts → Create Alert Rule (issue alert). Recommended conditions: "A new issue is created" OR "The issue changes state from resolved to unresolved" OR "The issue is seen more than 100 times in 1 hour" (the seen-more-than condition makes dedup increments visible in production — Pitfall 4 in RESEARCH.md). Action: **"Send a notification via CallVault Tickets"**. Save.
       - **Step 4 — Smoke test:** trigger a synthetic error (e.g. throw from the browser console on app.callvaultai.com), confirm a ticket appears in AdminTab → Tickets with source=sentry, and the function logs show a 200.
       - **Troubleshooting table:** 401 (secret mismatch / not set), gateway 401 "Missing authorization header" (config.toml verify_jwt block missing from deploy), no delivery (Alert Rule Action not enabled / rule action not selected), every error dedupes into one ticket (fingerprint bug — should never happen; file a ticket).
       Run `npm run lint:docs` (warning-only gate) before committing.

    3. Note in the SUMMARY: deferred idea recorded — `sentry-autofix.yml` integration intentionally out of scope (CONTEXT.md deferred list).
  </action>
  <verify>
    <automated>supabase functions deploy sentry-webhook --use-api (exit 0) && curl -s -o /dev/null -w "%{http_code}" -X POST https://$(supabase status 2>/dev/null | grep -o '[a-z]*\.supabase\.co' | head -1 || echo "<project-ref>.supabase.co")/functions/v1/sentry-webhook -d '{}' | grep -q 401 && test -f docs/operations/sentry-webhook-setup.md && npm run lint:docs</automated>
  </verify>
  <acceptance_criteria>
    - Deploy exits 0; unsigned POST to the live URL returns 401 with the function's own `{"error":"invalid signature"}`-style body (NOT the gateway's missing-JWT message)
    - `docs/operations/sentry-webhook-setup.md` contains the 4 steps verbatim-actionable: integration click path, `supabase secrets set SENTRY_WEBHOOK_SECRET=`, alert-rule action label "Send a notification via CallVault Tickets", and the smoke test
    - `npm run lint:docs` reports no new violations
    - Live end-to-end (real Sentry → ticket) explicitly listed in the SUMMARY as PENDING the human prerequisite — do not claim it works
  </acceptance_criteria>
  <done>Function live behind its signature gate; Andrew has an exact, complete setup runbook; untested surfaces honestly flagged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → deployed sentry-webhook | Live unauthenticated ingress once deployed |
| Test code → TEST Supabase project | Service-role against test project only — never prod (supabase/CLAUDE.md triple guard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Tampering | integration tests vs prod data | mitigate | `VITEST_INTEGRATION_OK` + `*_TEST_*`-only env reads + equality fatal guard (existing triple-layer guard; tests use TEST-tagged temp rows) |
| T-12-10 | Information disclosure | runbook contents | mitigate | Runbook never contains a real secret value — placeholder `<client-secret>` only |
| T-12-01 | Spoofing | deployed endpoint | mitigate | Deploy smoke proves 401-on-unsigned before the URL is handed to Sentry |
| T-12-SC | Tampering | package installs | accept | No new packages |
</threat_model>

<verification>
- `npm run test:integration -- sentry-webhook` green (or documented clean skip)
- Deploy smoke: unsigned POST → 401 from function code
- `docs/operations/sentry-webhook-setup.md` exists and passes `npm run lint:docs`
- SUMMARY honestly separates proven (synthetic POST dedup) from pending (live Sentry delivery — human prerequisite)
</verification>

<success_criteria>
- SEN-01 and SEN-02 have automated behavioral proof at the DB layer plus a deployed, signature-gated endpoint; the one human prerequisite is fully documented with the exact sentry.io click path, webhook URL, and secret destination.
</success_criteria>

<output>
Create `.planning/phases/12-sentry-ingestion/12-03-SUMMARY.md` when done
</output>
