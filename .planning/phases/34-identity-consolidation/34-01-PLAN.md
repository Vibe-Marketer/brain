---
phase: 34-identity-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34-identity-consolidation/reader-inventory.md
autonomous: false
requirements: [IDENT-01]

must_haves:
  truths:
    - "Every reader of speakers, contacts, and call_participants across src/ and supabase/functions/ is enumerated and classified (explicit-column select vs bare select()/SELECT *) in reader-inventory.md — the mandatory precondition for IDENT-01's 'existing readers unchanged'."
    - "Any reader that does a bare select() / SELECT * on the three tables (a column that WOULD surface a new nullable identity_id) is flagged REQUIRES-ATTENTION so Plan 02's noop test covers it."
    - "The one-way-door decisions (identities/identity_aliases schema shape, non-org-scoped RLS pattern, additive nullable identity_id, custom-OTP-not-Supabase-Auth, redacted evidence RPC, owner_user_id nullability, lazy-vs-eager identity creation, resolver edge-function-vs-trigger) are human-approved before any migration is authored."
  artifacts:
    - path: ".planning/phases/34-identity-consolidation/reader-inventory.md"
      provides: "Complete, classified inventory of every speakers/contacts/call_participants reader in the repo"
      min_lines: 40
      contains: "REQUIRES-ATTENTION"
  key_links:
    - from: "reader-inventory.md"
      to: "Plan 02 noop test + Plan 04 resolver match sites"
      via: "enumerated reader list drives what 'readers unchanged' must prove and where identity_id is consumed"
      pattern: "call_participants|speakers|contacts"
---

<objective>
De-risk the entire phase BEFORE touching schema. Two things happen here and nothing else: (1) a complete grep sweep enumerates every reader of `speakers`, `contacts`, and `call_participants` in the codebase and classifies each by how it selects columns — this is 34-RESEARCH.md's explicit "make this the plan's first task" recommendation (Common Pitfall 5 / Assumption A4), because research only confirmed a handful of readers while the codebase has dozens (mcp-server tools, callvault-api routes, useGlobalSearch, transcript-filters, connector-pipeline, event-resolver, useCallAnalytics, the CallDetail speaker pipeline). (2) A reversibility gate locks the one-way-door design decisions that Phases 35 (speaker resolution) and 39 (discovery/claim) build on.

This plan authors NO schema and NO code. It produces one inventory document and one recorded decision.

Purpose: The single largest risk in IDENT-01 is a migration that is correct against every reader research found but silently breaks one it did not find. A cheap grep sweep converts that unknown into a checked artifact. The reversibility gate ensures a human signs off on the identity spine's shape before it becomes load-bearing for downstream phases.
Output: `.planning/phases/34-identity-consolidation/reader-inventory.md`; a recorded option-a/option-b decision carried into Plan 02/03/04.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-identity-consolidation/34-RESEARCH.md
@.planning/phases/34-identity-consolidation/34-CONTEXT.md

Branch: `v2.2-event-resolution` (already cut and checked out — do NOT work on `main`; this milestone touches RLS on live prod with real customer data).

Key research findings this plan acts on (see 34-RESEARCH.md "Common Pitfalls 5", "Open Questions 1-3", "Assumptions Log A1-A4"):
- The reader inventory was explicitly NOT completed during research (context budget) and is flagged as the mandatory first task. A repo sweep this planning session already confirmed the real reader set is far larger than the three RPCs research named.
- `contacts` uses `org_id`; `call_participants` uses `organization_id` — a real naming inconsistency that will bite any cross-table SQL. Record both exact names in the inventory.
- `call_participants` carries BOTH `participant_type` (attendee/speaker/host) and `role` (organizer/invitee/attendee/speaker, from Phase 30) — do not conflate. Note this in the inventory for the resolver's benefit.
- Supabase Auth is one-email-per-account; the OTP mechanism is a custom hashed-OTP table + 2 edge functions reusing the existing Resend integration, NOT `auth.updateUser`/`verifyOtp` (CONTEXT.md Grey Area 1, corrected).

<interfaces>
<!-- Reader call sites already located this planning session (starting point — the sweep must confirm + extend, not assume complete). -->
Frontend (src/):
  - src/hooks/useGlobalSearch.ts        → .from('contacts'), .from('call_participants')
  - src/services/transcript-filters.service.ts → .from('call_participants')
  - src/hooks/useCallAnalytics.ts       → .from('call_speakers')  (legacy analytics join)
  - src/hooks/useCallDetailQueries.ts   → builds `callSpeakers` (Speaker[]) consumed by CallDetail tabs

Edge functions (supabase/functions/):
  - mcp-server/tools/write/set_speakers.ts, _ingest_helpers.ts → speakers, call_participants, call_speakers
  - mcp-server/tools/read/{list_contacts,get_contact,get_contact_calls,list_speakers,get_speaker_calls,get_recording_context}.ts
  - callvault-api/routes/{contacts,calls,speakers}.ts
  - zoom-sync-meetings/index.ts, _shared/connector-pipeline.ts, _shared/event-resolver.ts → call_participants

SQL RPC readers (supabase/migrations/*.sql): get_people_summary, get_recordings_for_person (20260309120000_call_participants.sql) — plus any others the migration sweep surfaces.

<!-- Exact column names to record verbatim (naming inconsistency is a documented footgun). -->
contacts.org_id            (NOT organization_id)
call_participants.organization_id
call_participants.participant_type   (attendee|speaker|host)  — original sourcing classification
call_participants.role               (organizer|invitee|attendee|speaker) — Phase 30 event-model classification
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate and classify every speakers/contacts/call_participants reader</name>
  <files>.planning/phases/34-identity-consolidation/reader-inventory.md</files>
  <read_first>
    - 34-RESEARCH.md ("Common Pitfalls 5", "Assumptions Log A1 + A4")
    - src/test/event-schema-noop.integration.test.ts (Phase 30's EVT-03 precedent — how "existing readers unchanged" was PROVEN there; the noop test relied on every read path using explicit column lists, never select('*'). This sweep produces the same guarantee's evidence for identity_id.)
  </read_first>
  <action>
    Run a full sweep for every read/write call site of the three tables plus the legacy `call_speakers` join, across BOTH `src/` and `supabase/functions/`, and the SQL RPCs in `supabase/migrations/`. Use grep with quoted include globs (zsh expands bare globs): grep -rn "from('speakers')" src supabase --include='*.ts'; repeat for 'contacts', 'call_participants', 'call_speakers'; and grep the migrations for the SQL functions that SELECT these tables (get_people_summary, get_recordings_for_person, get_call_speakers-style RPCs, global_search, and any others surfaced). For each call site, record in a markdown table: reader path | table | select style (EXPLICIT-COLUMNS vs BARE-select()/SELECT * — the latter would surface a new nullable identity_id into the returned row shape and its consumer's type) | risk (SAFE if explicit columns → an added nullable column is structurally invisible; REQUIRES-ATTENTION if bare select/*) | one-line note. Explicitly record the two naming facts: `contacts.org_id` vs `call_participants.organization_id`, and that `call_participants` has both `participant_type` AND `role` (do not conflate). Add a short "Match sites for the resolver" subsection listing where a verified email / provider participant id is stored per table (speakers.email, contacts.email, call_participants.email + any provider id column), which Plan 04's resolver will read. Do NOT modify any reader — this task only reads and documents.
  </action>
  <acceptance_criteria>
    - `reader-inventory.md` exists, is >= 40 lines, and contains a classified table covering speakers, contacts, call_participants, and call_speakers readers in BOTH src/ and supabase/functions/.
    - Every reader is tagged SAFE or REQUIRES-ATTENTION; the literal string "REQUIRES-ATTENTION" appears (even if only in the legend when zero bare-selects are found — state that explicitly).
    - The `contacts.org_id` vs `call_participants.organization_id` naming inconsistency and the participant_type-vs-role distinction are both recorded.
    - No source file (src/ or supabase/) is modified by this task.
    <automated>test -f .planning/phases/34-identity-consolidation/reader-inventory.md && grep -q "REQUIRES-ATTENTION" .planning/phases/34-identity-consolidation/reader-inventory.md && grep -q "org_id" .planning/phases/34-identity-consolidation/reader-inventory.md && grep -q "participant_type" .planning/phases/34-identity-consolidation/reader-inventory.md && echo INVENTORY_OK</automated>
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/34-identity-consolidation/reader-inventory.md && grep -qi "call_participants" .planning/phases/34-identity-consolidation/reader-inventory.md && echo OK</automated>
  </verify>
  <done>A complete, classified reader inventory exists; any bare-select reader on the three tables is flagged for Plan 02's noop test to cover.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Reversibility gate — lock the identity spine schema, RLS pattern, and OTP mechanism</name>
  <files>none — decision gate; gates the migrations authored in Plan 02 (identities/identity_aliases) and Plan 03 (identity_alias_verifications)</files>
  <read_first>
    - 34-RESEARCH.md ("Architecture Patterns" Pattern 1/2/3; "Assumptions Log A2/A3"; "Open Questions 1 + 2")
    - .planning/phases/34-identity-consolidation/reader-inventory.md (Task 1 output — a REQUIRES-ATTENTION reader could influence the go/no-go)
  </read_first>
  <action>Present the decision, context, and options below to the user and PAUSE — do not let Plan 02/03/04 begin until an option is selected. Record the choice (and any option-b knobs) in this plan's SUMMARY and carry it verbatim into Plan 02's DDL, Plan 03's OTP table + functions, and Plan 04's resolver. This is a reversibility gate: the identities/identity_aliases column set, the non-org-scoped RLS pattern, and the OTP design are one-way doors Phases 35 and 39 build on.</action>
  <decision>
    Lock the load-bearing, hard-to-reverse design decisions for the identity spine before any production DDL exists: the `identities` and `identity_aliases` column sets, the non-org-scoped participation/ownership RLS pattern, the additive nullable `identity_id` on all three person tables, the custom-OTP email-verification mechanism, and the redacted evidence RPC. Changing a shape after the fact requires a second prod migration; the RLS pattern specifically is the access model Phase 35 (speaker resolution) and Phase 39 (claim flow) build on.
  </decision>
  <context>
    This is additive DDL against a LIVE production database with real customer data, on the `v2.2-event-resolution` branch. The migrations themselves are additive and NULL-safe (low risk), but the SHAPES they lock are load-bearing for two downstream phases. Research is MEDIUM-HIGH confidence and recommends option A (the schema facts are HIGH/verified from live migration reads; the OTP mechanism is a correction of CONTEXT.md's original Supabase-Auth assumption). This gate exists so a human signs off on the door, not because the design is uncertain.
  </context>
  <options>
    <option id="option-a">
      <name>Approve the research-recommended design as-is (RECOMMENDED)</name>
      <pros>
        - identities: id UUID PK default gen_random_uuid(); owner_user_id UUID NULL REFERENCES auth.users(id) ON DELETE SET NULL (nullable per A2 — an identity is "claimed" only when a user verifies an email against it, which sets up Phase 39's DISCO-02 as "set owner_user_id" on a pre-existing row); display_name TEXT NULL; created_at/updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(). NO organization_id (non-org-scoped, mirroring events/EVT-04).
        - identities RLS: ENABLE + FORCE; SELECT via a user_can_view_identity(id, auth.uid()) SECURITY DEFINER helper = owner OR participation via call_participants.identity_id + is_organization_member(organization_id) OR contacts.identity_id + is_organization_member(org_id); owner-only UPDATE; service-role FOR ALL. NO org-admin bypass. Mirrors the audited events participation-RLS precedent (and its CR-01 fix), not the contacts/call_participants org-only pattern.
        - identity_aliases: id; identity_id FK ON DELETE CASCADE; alias_type TEXT CHECK IN ('email','provider_participant_id','display_name'); value TEXT (normalized lower(trim) for email); provider TEXT NULL; verified BOOLEAN NOT NULL DEFAULT false; verified_at TIMESTAMPTZ; confidence NUMERIC NULL; evidence TEXT NOT NULL; created_at. Partial UNIQUE INDEX on (alias_type, value) WHERE verified = true (weak/candidate signals never block each other; no two identities can claim the same verified email). SELECT = owner-only (via identities.owner_user_id) so the AccountTab list shows the user's own emails; service-role FOR ALL; non-owners read only via the redacted RPC.
        - identity_id: nullable UUID FK -> identities(id) ON DELETE SET NULL added to speakers, contacts, AND call_participants. All additive, all nullable, no existing reader touched.
        - Email verification (IDENT-03): identity_alias_verifications table (user_id, email, code_hash, expires_at, attempts, created_at; RLS enabled+forced, service-role-only / client-deny) + request-email-alias-verification + confirm-email-alias-verification edge functions, reusing the existing Resend HTTP integration and RECONCILE-style secrets. 6-digit CSPRNG code, SHA-256 hashed at rest, 10-min expiry, 5-attempt cap, per-user request rate limit. NEVER touches auth.users or the session.
        - get_identity_evidence(p_identity_id) SECURITY DEFINER RPC RETURNS (alias_type, confidence, evidence) — never `value` (raw email); EXECUTE granted to authenticated (safe for any viewer because it is redacted).
        - Identity creation is LAZY (Open Q2): a row is created on first email verification (or first deterministic provider-id resolution), consistent with the milestone's forward-only philosophy. No proactive backfill of existing users.
        - Resolver runs as a resolve-identities edge function (A3) mirroring resolve-events, forward-only, verified/provider-id matches only.
      </pros>
      <cons>Locks column names/types now; later changes are additive-only (cheap) but type/constraint/RLS changes need another prod migration. Lazy creation means no identity rows exist until the first verification.</cons>
    </option>
    <option id="option-b">
      <name>Approve the design but turn one or more reversible knobs</name>
      <pros>
        - Knob 1 (A2): make identities.owner_user_id NOT NULL — every identity is user-owned from creation; drops the "unclaimed person cluster" concept from v2.2 (simpler now, but Phase 39's claim flow would need a different shape).
        - Knob 2 (A3): resolve identities via an AFTER INSERT trigger on speakers/contacts/call_participants instead of an edge function — no cron/sweep to wire, but harder to feature-flag/shadow and runs inside the write transaction.
        - Knob 3 (Open Q2): proactively create identities for existing users on migration (eager backfill) instead of lazy — deviates from forward-only; a real false-link exposure if any non-email signal is used, so verified-email-only if chosen.
        - Knob 4: tune OTP params (expiry minutes, attempt cap, code length) from the 10-min / 5-attempt / 6-digit defaults.
      </pros>
      <cons>Knob 1 changes Phase 39's claim-flow shape; Knob 2 changes testability/isolation; Knob 3 is a forward-only-policy deviation with real exposure — justify against research before choosing.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - A human has selected option-a or option-b (with named knobs) and the choice is recorded in this plan's SUMMARY.
    - If option-b, each chosen knob is written into the SUMMARY exactly as approved so Plans 02/03/04 carry it verbatim.
  </acceptance_criteria>
  <verify>Human decision gate — no automated check. Plan 02 must not begin until an option is selected and recorded.</verify>
  <done>A human has selected an option (and any knobs); the choice is recorded in the SUMMARY and ready to carry into Plans 02, 03, and 04.</done>
  <resume-signal>Select: option-a, or option-b with the specific knob(s) named.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer -> committed artifact | The reader inventory is committed to the branch; the only egress is what lands in git. It documents schema/reader shape only — no rows, secrets, or PII. |
| human decision -> downstream migrations | The gate's recorded choice governs DDL against live prod in Plans 02/03; an unrecorded or ambiguous choice risks an unreviewed schema shape shipping. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-01 | Information Disclosure | reader-inventory.md | mitigate | The inventory records file paths, table names, and column-select styles only — no email addresses, no row data, no secrets. Do not paste sampled row values into it. |
| T-34-01-02 | Tampering / Elevation of Privilege | the locked design (deferred to Plan 02) | mitigate | This gate is precisely the mitigation: a human approves the identities RLS pattern and OTP design before either becomes live. The security-critical shapes (non-org-scoped participation RLS, redacted evidence RPC, hashed rate-limited OTP) are named in option-a so approval is informed, not blind. |
| T-34-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (34-RESEARCH.md "Package Legitimacy Audit: N/A — no new external packages"). No supply-chain surface. |
</threat_model>

## Artifacts this phase produces (Plan 01)

- **New file:** `.planning/phases/34-identity-consolidation/reader-inventory.md` (classified inventory of every speakers/contacts/call_participants/call_speakers reader)
- **Recorded decision:** option-a / option-b (+ knobs) in `34-01-SUMMARY.md`, governing Plans 02/03/04
- **No schema, no code** — this plan authors neither a migration nor any source change.

<verification>
- `reader-inventory.md` covers src/ AND supabase/functions/ readers, classified SAFE vs REQUIRES-ATTENTION.
- The org_id-vs-organization_id and participant_type-vs-role facts are recorded.
- A human option is selected and recorded in the SUMMARY before Plan 02 begins.
- No migration file and no source file were created or modified.
</verification>

<success_criteria>
IDENT-01's "existing readers unchanged" precondition is made checkable (not assumed): every reader is enumerated and risk-classified before any migration exists. The identity spine's one-way-door decisions are human-approved and recorded, ready to carry verbatim into the schema, OTP, and resolver plans.
</success_criteria>

<output>
Create `.planning/phases/34-identity-consolidation/34-01-SUMMARY.md` when done. Record the Task 2 decision (option-a or option-b + named knobs) explicitly — Plans 02, 03, and 04 read it.
</output>
