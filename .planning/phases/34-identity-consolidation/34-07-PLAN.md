---
phase: 34-identity-consolidation
plan: 07
type: execute
wave: 5
depends_on: ["34-02", "34-03", "34-04", "34-05", "34-06"]
files_modified:
  - src/types/supabase.ts
autonomous: false
requirements: [IDENT-01, IDENT-02, IDENT-03, IDENT-08]

must_haves:
  truths:
    - "Both identity migrations (20260905140000 identity spine, 20260905150000 OTP table) are applied to production (ref vltmrnjsubfzrgrtdqey), guarded, with the tables/RPC/index confirmed live by introspection."
    - "The three new edge functions (request-email-alias-verification, confirm-email-alias-verification, resolve-identities) are deployed to prod via --use-api."
    - "The phase ships SAFE: identity_id is NULL across all existing rows (forward-only, no backfill), identity_alias_verifications is service-role-only, get_identity_evidence EXECUTE is granted to authenticated but returns no raw email, and RLS isolation holds in prod."
    - "A real end-to-end add-email round-trip succeeds in production (real inbox → code → verified alias), the one path dev-browser cannot cover."
  artifacts:
    - path: "src/types/supabase.ts"
      provides: "Types regenerated from prod after the identity migrations apply"
      contains: "identity_aliases"
  key_links:
    - from: "supabase db push --linked (prod)"
      to: "identities + identity_aliases + identity_alias_verifications"
      via: "prod-ref-guarded apply of both migrations"
      pattern: "vltmrnjsubfzrgrtdqey"
    - from: "supabase functions deploy --use-api"
      to: "request/confirm-email-alias-verification + resolve-identities"
      via: "edge function deploy"
      pattern: "email-alias-verification"
---

<objective>
Ship Phase 34 to production, guarded and forward-only-safe — following the exact prod-apply discipline of Phases 31/32/33. Apply both identity migrations, deploy the three new edge functions, regenerate types from prod, then prove by introspection that the phase ships safely (no existing row auto-linked, OTP table client-deny, evidence RPC redacted, RLS isolation intact) and confirm the one thing dev-browser cannot: a real add-email round-trip through a real inbox.

Purpose: The milestone's rhythm is TEST-then-prod guarded apply per phase, with a human authorizing the prod step. Phase 34 additionally needs a real-inbox E2E because the OTP loop (email → code → confirm) cannot be automated — dev-browser can't read the verification email.
Output: Both migrations live in prod; three functions deployed; types re-synced; safety proven by introspection; a real add-email verified end to end.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-identity-consolidation/34-02-SUMMARY.md
@.planning/phases/34-identity-consolidation/34-03-SUMMARY.md
@.planning/phases/34-identity-consolidation/34-04-SUMMARY.md

Branch: `v2.2-event-resolution` (do NOT work on `main`). This plan is the only one that touches production.

<interfaces>
<!-- Apply discipline + gotchas from prior guarded applies (Phase 31/32/33). Follow exactly. -->
- Prod Supabase ref is `vltmrnjsubfzrgrtdqey`. Verify the linked project ref BEFORE and AFTER any prod push (prior phases verified 2-3x). Migrations read prod creds from `.env` DATABASE_URL, prod-ref guarded (root CLAUDE.md "Environment files" + supabase/CLAUDE.md).
- Migrations to apply (in order): supabase/migrations/20260905140000_create_identities_and_link_tables.sql (Plan 02), 20260905150000_create_identity_alias_verifications.sql (Plan 03).
- Edge deploys use `--use-api` (Docker is not running; plain deploy hangs): request-email-alias-verification, confirm-email-alias-verification, resolve-identities (the last with `--no-verify-jwt`, app-code secret gate).
- Secrets already set project-wide: RESEND_API_KEY, RESEND_DOMAIN_VERIFIED, RECONCILE_SECRET (no new secret this phase).
- Types regen gotcha (Phase 32 P04): strip any CLI update-nag banner lines that leak past stderr after the `} as const` terminator before committing src/types/supabase.ts.
- type-baseline.json holds accepted pre-existing errors (incl. Deno esm.sh type-only imports, TS2307 under Node tsc). If regen surfaces only that known class, --update-baseline; do not fix.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final TEST gate — full suite green with both migrations present</name>
  <files>(none in-repo — runs the suite against TEST)</files>
  <read_first>
    - supabase/migrations/README_APPLY_MIGRATION.md
    - .planning/phases/33-content-proof-matching-alibi-constraint/33-03-SUMMARY.md (the exact TEST-then-prod steps used last phase)
    - supabase/CLAUDE.md → "Running integration tests safely"
  </read_first>
  <action>
    Confirm both migrations (20260905140000, 20260905150000) are applied to the TEST project (they were applied during Plans 02/03; re-link TEST and introspect to confirm identities, identity_aliases, identity_alias_verifications, the three identity_id columns, the partial unique index, user_can_view_identity, and get_identity_evidence all exist). Run the phase's full relevant suite against TEST as the gate evidence: VITEST_INTEGRATION_OK=true npx vitest run src/test/identity-schema-noop.integration.test.ts src/test/identity-evidence-rpc.integration.test.ts src/test/rls-regression.test.ts supabase/functions/confirm-email-alias-verification/__tests__/confirm-email-alias.integration.test.ts supabase/functions/_shared/__tests__/otp.test.ts supabase/functions/_shared/__tests__/identity-resolver.test.ts — all green. Do NOT touch prod in this task. Re-link the CLI back to prod when done.
  </action>
  <verify>
    <automated>VITEST_INTEGRATION_OK=true npx vitest run src/test/identity-schema-noop.integration.test.ts src/test/identity-evidence-rpc.integration.test.ts src/test/rls-regression.test.ts supabase/functions/_shared/__tests__/otp.test.ts supabase/functions/_shared/__tests__/identity-resolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both migrations confirmed present on TEST by introspection.
    - The full identity test set is green on TEST (IDENT-01 noop, IDENT-08 evidence, cross-org isolation, OTP unit, confirm integration, resolver unit).
    - No prod database or edge function touched; CLI relinked to prod.
  </acceptance_criteria>
  <done>All Phase 34 tests green on TEST with both migrations live; prod untouched; ready for the approval gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve production apply + edge-function deploy</name>
  <files>(none — approval gate before the prod apply in Task 3)</files>
  <action>Present the TEST evidence below and STOP. Do not run Task 3 until the operator types "approved". This gate authorizes two DDL applies to production (real customer data) plus three edge-function deploys.</action>
  <what-built>
    The identity spine (identities + identity_aliases + nullable identity_id on speakers/contacts/call_participants), the redacted get_identity_evidence RPC, the OTP table + request/confirm-email-alias-verification functions, and the forward-only resolve-identities function — all proven on TEST (IDENT-01 readers unchanged, IDENT-02 never-link-on-name, IDENT-03 OTP happy-path + brute-force guard, IDENT-08 redacted evidence, cross-org isolation).
  </what-built>
  <how-to-verify>
    1. Review 34-02/03/04-SUMMARY.md and the Task-1 TEST introspection + green suite.
    2. Confirm you approve applying migrations 20260905140000 and 20260905150000 to PRODUCTION (ref vltmrnjsubfzrgrtdqey) and deploying request-email-alias-verification, confirm-email-alias-verification, and resolve-identities.
    3. This ships forward-only and SAFE: no existing row is auto-linked (identity_id stays NULL until a future resolver run/verification), the OTP table is service-role-only, and get_identity_evidence returns no raw email.
  </how-to-verify>
  <verify>
    <automated>MANUAL — human approval gate; Task 3 does not run until "approved".</automated>
  </verify>
  <resume-signal>Type "approved" to authorize the prod apply + the three function deploys, or describe concerns.</resume-signal>
  <done>Operator approved the prod apply + deploys (or supplied overrides).</done>
</task>

<task type="auto">
  <name>Task 3: Guarded prod apply + function deploys + type re-sync + safety proof</name>
  <files>src/types/supabase.ts</files>
  <read_first>
    - .planning/phases/33-content-proof-matching-alibi-constraint/33-03-SUMMARY.md (prod-ref-guard steps, --use-api deploy, types regen stdout gotcha)
    - root CLAUDE.md → "Environment files" (prod ref guard)
  </read_first>
  <action>
    After approval: link the prod project and verify the linked ref equals vltmrnjsubfzrgrtdqey BEFORE doing anything. Apply both migrations in order (supabase db push --linked applies 20260905140000 then 20260905150000). Deploy the functions: supabase functions deploy request-email-alias-verification --use-api; supabase functions deploy confirm-email-alias-verification --use-api; supabase functions deploy resolve-identities --use-api --no-verify-jwt. Regenerate types: supabase gen types typescript --linked > src/types/supabase.ts (strip any CLI banner garbage after `} as const`). Then confirm SAFETY by prod introspection and record each in the SUMMARY: (a) identities, identity_aliases, identity_alias_verifications exist; the three identity_id columns exist; identity_aliases_verified_unique exists; get_identity_evidence exists. (b) SELECT COUNT(*) FROM speakers WHERE identity_id IS NOT NULL, same for contacts and call_participants — expect 0 across all three (forward-only, nothing auto-linked yet). (c) identity_alias_verifications is service-role-only: confirm no authenticated/anon policy exists (pg_policies introspection). (d) get_identity_evidence has EXECUTE granted to authenticated but its definition selects no `value` column (confirm via pg_get_functiondef). (e) FORCE RLS is on for identities, identity_aliases, identity_alias_verifications. Verify the linked ref is STILL vltmrnjsubfzrgrtdqey AFTER the push. Finally run npx tsc -p tsconfig.app.json; if the only new errors are the known Deno esm.sh type-only class, --update-baseline, else fix.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.app.json 2>&1 | tail -5; grep -q "identity_aliases" src/types/supabase.ts && grep -q "get_identity_evidence" src/types/supabase.ts && echo TYPES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Prod ref confirmed vltmrnjsubfzrgrtdqey before AND after; both migrations live in prod; all three functions deployed (--use-api; resolve-identities --no-verify-jwt).
    - src/types/supabase.ts regenerated from prod, clean (no banner garbage), includes identities/identity_aliases/get_identity_evidence.
    - Safety proven in prod: identity_id IS NOT NULL count = 0 on all three tables; identity_alias_verifications has no authenticated/anon policy; get_identity_evidence grants EXECUTE to authenticated and selects no raw value; FORCE RLS on all three new tables.
    - tsc -p tsconfig.app.json clean vs baseline (baseline updated only for the known esm.sh class if needed).
  </acceptance_criteria>
  <done>Both migrations + three functions live in prod; types re-synced; forward-only safety and RLS posture proven by introspection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Real end-to-end add-email round-trip in production</name>
  <files>(none — live production verification of the one flow dev-browser cannot automate)</files>
  <action>Present the steps below and PAUSE for the operator to run the real add-email flow (a real inbox is required to read the code — this is the single manual verification the automation-first rule genuinely cannot replace). After the operator reports the outcome, confirm the resulting DB state by introspection and record it. Do not mark the phase done until the round-trip succeeds or a defect is filed.</action>
  <what-built>The deployed add-email flow: AccountTab "Verified Emails" section → request-email-alias-verification (Resend email) → confirm-email-alias-verification → verified identity_aliases row.</what-built>
  <how-to-verify>
    1. Log in to production (app.callvaultai.com), open Settings → Account → Verified Emails.
    2. Add a second email address you control; confirm a verification email arrives from CallVault (Resend) with a 6-digit code.
    3. Enter the code; confirm the UI shows success and the new email appears in the verified list.
    4. Report success (or the failure point). The agent then introspects prod: an identities row with owner_user_id = your user exists, an identity_aliases row (alias_type='email', verified=true) for the new address exists, and the pending identity_alias_verifications row was deleted. Also confirm a wrong code is rejected and 5 wrong attempts invalidate the pending row.
  </how-to-verify>
  <verify>
    <automated>MANUAL — real-inbox round-trip; the agent introspects prod to confirm the verified alias + deleted pending row after the operator reports success.</automated>
  </verify>
  <resume-signal>Report "verified" once the added email shows as verified (or describe where it failed).</resume-signal>
  <done>A real add-email round-trip verified in production, with the verified alias + owner_user_id confirmed by introspection and the brute-force guard spot-checked. IDENT-03 proven live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → production Postgres (vltmrnjsubfzrgrtdqey) | Applying DDL to a live system with real customer data; wrong-project apply is catastrophic (supabase/CLAUDE.md 2026-05 incident). |
| deployed edge functions → prod data | request/confirm write the OTP table + identities/identity_aliases; resolve-identities links identity_id — all service-role. |
| real inbox → confirm function | The only place the plaintext OTP exists; the human E2E exercises the real send/verify path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07-01 | Tampering | migration/deploy applied to the wrong project | mitigate | Prod-ref guard verifies vltmrnjsubfzrgrtdqey before AND after; both migrations proven on TEST first (Task 1); human checkpoint (Task 2) authorizes the prod step. |
| T-34-07-02 | Information Disclosure | phase ships and silently auto-links real people | mitigate | Forward-only: Task 3 proves identity_id IS NOT NULL count = 0 on all three tables at ship (nothing auto-linked); resolve-identities is invoke-gated and not wired to run automatically this phase. |
| T-34-07-03 | Information Disclosure | OTP table or evidence RPC over-exposed in prod | mitigate | Task 3 introspection proves identity_alias_verifications has no authenticated policy (client-deny) and get_identity_evidence selects no raw `value` despite EXECUTE-to-authenticated; FORCE RLS confirmed on all three new tables. |
| T-34-07-04 | Spoofing | the OTP brute-force guard fails to hold in prod | mitigate | Task 4 spot-checks that a wrong code is rejected and 5 wrong attempts invalidate the pending row against the live deployed function. |
| T-34-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (34-RESEARCH.md Package Legitimacy Audit: N/A). |
</threat_model>

## Artifacts this phase produces (Plan 07)

- **Production state:** migrations 20260905140000 + 20260905150000 applied; request-email-alias-verification, confirm-email-alias-verification, resolve-identities deployed
- **Modified:** `src/types/supabase.ts` (regenerated from prod, canonical sync)
- **Evidence:** introspection proof of forward-only safety (zero auto-linked rows), client-deny OTP table, redacted evidence RPC, FORCE RLS on all three new tables; a real add-email round-trip verified live
- **No new schema symbols** — this plan applies and deploys what Plans 02-06 built.

<verification>
- Both migrations live in prod (ref guarded before+after); three functions deployed (--use-api; resolver --no-verify-jwt).
- Forward-only safety proven: identity_id IS NOT NULL count = 0 on speakers/contacts/call_participants.
- OTP table client-deny + evidence RPC redacted + FORCE RLS on all three new tables, proven by introspection.
- Types re-synced from prod; tsc clean vs baseline.
- Real add-email round-trip verified in production (verified alias + owner_user_id + deleted pending row confirmed).
</verification>

<success_criteria>
Phase 34 is live in production and safe: the identity spine, OTP verification, resolver, evidence RPC, and both UI surfaces ship forward-only (nothing auto-linked at cutover), with RLS/redaction posture proven by introspection and IDENT-03 confirmed by a real end-to-end email round-trip. IDENT-01, IDENT-02, IDENT-03, and IDENT-08 are all delivered.
</success_criteria>

<output>
Create `.planning/phases/34-identity-consolidation/34-07-SUMMARY.md` when done. Record: prod-ref-guard confirmations (before+after), the zero-auto-linked counts, the OTP client-deny + evidence-RPC-redacted + FORCE-RLS introspection results, the real add-email round-trip outcome, and any User Setup Required (e.g. if resolve-identities is later wired to cron, note the existing event-resolution-sweep GUC issue from Phase 32 as a known pattern).
</output>
