---
phase: 06.2-callvault-rest-api
plan: 03
type: execute
wave: 1
depends_on:
  - 06.2-01
files_modified:
  - supabase/functions/obsidian-sync/**
  - src/services/obsidian-tokens.service.ts
  - src/hooks/useObsidianTokens.ts
  - src/components/settings/ObsidianConnectorSection.tsx
  - src/lib/export-utils.ts
  - docs/help/export-system.md
autonomous: true
requirements:
  - Phase-06.2-Context-D-27
  - Phase-06.2-Context-D-28
  - Phase-06.2-Context-D-29
must_haves:
  truths:
    - "The unused Obsidian-specific backend/token path is removed or explicitly deprecated in favor of API tokens and REST."
    - "Obsidian ZIP export remains available only as a client/export utility if it does not require Obsidian-specific token auth."
  artifacts:
    - path: "src/components/settings/ApiTokensSection.tsx"
      provides: "Obsidian setup guidance through API tokens"
    - path: "supabase/functions/obsidian-sync"
      provides: "removed or disabled legacy unused backend path"
---

<objective>
Remove or neutralize the rushed unused Obsidian-specific API/token architecture so Phase 06.2 does not ship two competing integration models.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-callvault-rest-api/06.2-CONTEXT.md
@.planning/phases/06.2-callvault-rest-api/06.2-RESEARCH.md
@.planning/phases/06.2-callvault-rest-api/06.2-UI-SPEC.md
@src/CLAUDE.md
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove or disable obsolete Obsidian token code</name>
  <files>src/services/obsidian-tokens.service.ts, src/hooks/useObsidianTokens.ts, src/components/settings/ObsidianConnectorSection.tsx</files>
  <read_first>
    - src/services/obsidian-tokens.service.ts
    - src/hooks/useObsidianTokens.ts
    - src/components/settings/ObsidianConnectorSection.tsx
    - src/components/settings/ApiTokensSection.tsx
    - src/components/settings/IntegrationsTab.tsx
  </read_first>
  <action>Delete obsolete Obsidian token service/hook/component files if no imports remain after Plan 01. If deleting is unsafe due to import chains, replace them with a thin deprecated wrapper that does not call `generate_obsidian_token`, does not query `token_source = 'obsidian'`, and directs callers to API-token services. Do not leave active UI paths that generate Obsidian-specific tokens.</action>
  <acceptance_criteria>
    - `rg -n "useObsidianTokens|generateObsidianToken|revokeObsidianToken|GeneratedObsidianToken" src` returns no active production imports.
    - No production service calls `generate_obsidian_token`.
    - No settings UI contains `Generate Obsidian Token`.
  </acceptance_criteria>
  <verify>
    <automated>! rg -n "useObsidianTokens|generateObsidianToken|revokeObsidianToken|generate_obsidian_token|Generate Obsidian Token" src</automated>
  </verify>
  <done>Frontend no longer contains an active Obsidian-specific token architecture.</done>
</task>

<task type="auto">
  <name>Task 2: Remove or deprecate obsidian-sync Edge Function</name>
  <files>supabase/functions/obsidian-sync/index.ts, supabase/functions/deno.json, docs/operations/api-runbook.md</files>
  <read_first>
    - supabase/functions/obsidian-sync/index.ts
    - supabase/functions/callvault-api/index.ts
    - supabase/functions/callvault-api/routes/calls.ts
    - supabase/functions/deno.json
    - docs/operations/mcp-runbook.md
  </read_first>
  <action>If `obsidian-sync` has no deployment or source-registry dependency that execution discovers, remove the function source and any docs that present it as the integration API. If removal would break deployment manifests, replace the handler with a 410 JSON response that says the Obsidian integration uses the CallVault REST API at `/v1`, and document deletion as a follow-up in the summary. The preferred result is deletion because no real Obsidian users exist yet.</action>
  <acceptance_criteria>
    - `supabase/functions/obsidian-sync/index.ts` is deleted or returns HTTP 410 JSON with a REST API migration message.
    - No active docs tell users to call `/obsidian-sync`.
    - No production route exposes Obsidian-specific token auth as the future path.
  </acceptance_criteria>
  <verify>
    <automated>! rg -n "/obsidian-sync|token_source\\s*=\\s*'obsidian'|Invalid or revoked Obsidian token" src docs cloudflare supabase/functions || rg -n "410|REST API|/v1" supabase/functions/obsidian-sync/index.ts</automated>
  </verify>
  <done>The unused Obsidian backend path is removed or explicitly marked non-canonical.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve useful Obsidian export behavior without separate auth</name>
  <files>src/lib/export-utils.ts, docs/help/export-system.md, src/components/settings/ApiTokensSection.tsx</files>
  <read_first>
    - src/lib/export-utils.ts
    - src/services/obsidian-tokens.service.ts
    - src/components/settings/ApiTokensSection.tsx
    - docs/help/export-system.md
  </read_first>
  <action>Keep existing Obsidian-compatible ZIP/markdown export behavior only if it remains a client-side export action backed by authenticated app session data or REST API data. Remove copy that implies a separate Obsidian token is required for export. In setup guidance, say Obsidian live sync uses a CallVault API token and the REST API.</action>
  <acceptance_criteria>
    - Obsidian ZIP export copy does not mention Obsidian-specific token generation.
    - `ApiTokensSection.tsx` setup guidance names CallVault API tokens for Obsidian/plugin usage.
    - `docs/help/export-system.md` remains accurate for ZIP export if touched.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "CallVault API token|REST API|Obsidian" src/components/settings docs/help/export-system.md && ! rg -n "Obsidian token|obsidian-sync" src/components/settings docs/help/export-system.md</automated>
  </verify>
  <done>Obsidian remains a useful export/integration story without a separate credential/API universe.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06.2-07 | Confusion/UX risk | integration settings | mitigate | Remove Obsidian-token copy so users learn one API-token model. |
| T-06.2-08 | Attack surface | unused Edge Function | mitigate | Delete or return 410 from the unused Obsidian-specific backend route. |
</threat_model>

<verification>
Run source assertions for obsolete Obsidian token strings, targeted tests if any touched export utilities have tests, and `npm run build`.
</verification>

<success_criteria>
- The rushed Obsidian-specific backend/token path is not active as a canonical integration model.
- Obsidian guidance points to API tokens and `/v1`.
- Build passes after cleanup.
</success_criteria>

<output>
Create `.planning/phases/06.2-callvault-rest-api/06.2-03-SUMMARY.md` when done.
</output>
