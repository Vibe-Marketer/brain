# Phase 06.2: CallVault REST API - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-09
**Phase:** 06.2-callvault-rest-api
**Areas discussed:** API architecture, token model, Obsidian relationship, cleanup posture

---

## API Architecture

| Option | Description | Selected |
|--------|-------------|----------|
| Raw Supabase REST | Expose or lean on Supabase generated `/rest/v1` database API | |
| Dedicated CallVault REST API | Build `api.callvaultai.com/v1/*` through the existing proxy and Edge Functions | x |
| MCP-based API | Represent integration access as MCP tools only | |

**User's choice:** Dedicated CallVault REST API.
**Notes:** User confirmed this is the best option and different from MCP. Research confirmed raw Supabase API keys are not the right customer-facing product API token model.

---

## Token Model

| Option | Description | Selected |
|--------|-------------|----------|
| Supabase API keys | Use Supabase anon/service/project keys for external developers | |
| MCP tokens | Let REST callers reuse MCP tokens | |
| API source tokens | Use CallVault personal API tokens labeled with `token_source = 'api'` | x |

**User's choice:** API source tokens, with explanation requested.
**Notes:** Existing table name `mcp_tokens` is misleading, but the table already has token ownership, scope, revocation, and `token_source` support. REST must reject MCP/Obsidian tokens.

---

## Obsidian Relationship

| Option | Description | Selected |
|--------|-------------|----------|
| Keep Obsidian API separate | Continue growing `/obsidian-sync` and Obsidian-specific token generation | |
| REST API as canonical integration surface | Move Obsidian toward `/v1` and use API tokens | x |
| Preserve both permanently | Maintain parallel Obsidian and REST contracts | |

**User's choice:** REST API as canonical integration surface.
**Notes:** User clarified the REST API is for future integrations and for Obsidian to use. Existing Obsidian path has not been used yet.

---

## Cleanup Posture

| Option | Description | Selected |
|--------|-------------|----------|
| Preserve rushed Obsidian work for compatibility | Avoid cleanup because code exists | |
| Remove/replace unused Obsidian-specific pieces | Treat rushed unused path as disposable scaffolding | x |
| Only add REST API beside it | Leave parallel architecture untouched | |

**User's choice:** Remove or replace what does not make sense.
**Notes:** User explicitly said the existing Obsidian work has not been used and now is the time to change/remove the rushed pieces.

---

## the agent's Discretion

- Exact internal REST Edge Function module layout.
- Cursor vs offset pagination after planning evaluates current query patterns.
- Exact API-token UI placement and whether to generalize token services or create a dedicated `api-tokens.service.ts`.
- Whether `GET /v1/calls/{id}` or transcript-specific routes are required in the first REST slice for Obsidian.

## Deferred Ideas

- Full developer SDKs and OpenAPI publishing.
- Broad write/admin REST API.
- Permanent support for a separate Obsidian backend API.

---

*Phase: 06.2-callvault-rest-api*
*Discussion log generated: 2026-06-09*
