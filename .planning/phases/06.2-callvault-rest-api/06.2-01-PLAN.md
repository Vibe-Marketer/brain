---
phase: 06.2-callvault-rest-api
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/*api_token*.sql
  - src/services/api-tokens.service.ts
  - src/hooks/useApiTokens.ts
  - src/components/settings/ApiTokensSection.tsx
  - src/components/settings/IntegrationsTab.tsx
  - src/types/supabase.ts
autonomous: true
requirements:
  - Phase-06.2-SC-5
  - Phase-06.2-SC-6
must_haves:
  truths:
    - "Users can generate and revoke CallVault API tokens that are stored with `token_source = 'api'`."
    - "Settings UI no longer teaches users to generate an Obsidian-specific token as the integration credential."
  artifacts:
    - path: "supabase/migrations/*api_token*.sql"
      provides: "API token generator, lookup index, and source-labeled token storage contract"
    - path: "src/services/api-tokens.service.ts"
      provides: "service-layer API token CRUD/generation functions"
    - path: "src/hooks/useApiTokens.ts"
      provides: "TanStack Query hooks for API token UI"
    - path: "src/components/settings/ApiTokensSection.tsx"
      provides: "Settings UI for API token generation, one-time reveal, list, and revoke"
  key_links:
    - from: "src/components/settings/ApiTokensSection.tsx"
      to: ".planning/phases/06.2-callvault-rest-api/06.2-UI-SPEC.md"
      via: "copy/layout/token reveal contract"
---

<objective>
Build the API-token foundation and replace the Obsidian-specific token UI with a general CallVault API token surface.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-callvault-rest-api/06.2-CONTEXT.md
@.planning/phases/06.2-callvault-rest-api/06.2-RESEARCH.md
@.planning/phases/06.2-callvault-rest-api/06.2-UI-SPEC.md
@.planning/phases/06.2-callvault-rest-api/06.2-PATTERNS.md
@supabase/CLAUDE.md
@src/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API-token SQL/RPC foundation</name>
  <files>supabase/migrations/*api_token*.sql, src/types/supabase.ts</files>
  <read_first>
    - supabase/migrations/20260608120000_obsidian_sync_token_label.sql
    - supabase/migrations/20260310160000_mcp_tokens.sql
    - supabase/migrations/20260601054000_fix_mcp_token_crypto_search_path.sql
    - src/types/supabase.ts
    - supabase/CLAUDE.md
  </read_first>
  <action>Add a new Supabase migration that creates an API-token lookup index on `mcp_tokens(token)` for rows where `token_source = 'api' AND revoked_at IS NULL`, and adds `generate_api_token(p_org_id UUID, p_name TEXT, p_scope TEXT DEFAULT 'organization', p_workspace_id UUID DEFAULT NULL)` returning `id`, `token`, `name`, `scope`, `org_id`, `workspace_id`, and `created_at`. Generated tokens must use a clear API prefix such as `cv_api_`, insert `token_source = 'api'`, preserve the existing `scope` values `organization` and `workspace`, validate workspace scope requires `p_workspace_id`, and avoid creating Obsidian-sourced tokens. Update generated Supabase types so `mcp_tokens.Row`, `Insert`, and `Update` include `token_source` and `token_label` if the type file is stale.</action>
  <acceptance_criteria>
    - A migration contains `generate_api_token`.
    - A migration contains an index whose predicate includes `token_source = 'api'` and `revoked_at IS NULL`.
    - The token prefix string `cv_api_` appears in the API-token generator.
    - `generate_api_token` inserts `token_source` with value `api`.
    - `src/types/supabase.ts` exposes `token_source` on `mcp_tokens.Row`, `Insert`, and `Update`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "generate_api_token|cv_api_|token_source.*api|mcp_tokens_api" supabase/migrations src/types/supabase.ts</automated>
  </verify>
  <done>Database/token types can create API tokens without using the Obsidian token RPC.</done>
</task>

<task type="auto">
  <name>Task 2: Add API token service and hooks</name>
  <files>src/services/api-tokens.service.ts, src/hooks/useApiTokens.ts</files>
  <read_first>
    - src/services/mcp-tokens.service.ts
    - src/services/obsidian-tokens.service.ts
    - src/hooks/useObsidianTokens.ts
    - src/CLAUDE.md
  </read_first>
  <action>Create `src/services/api-tokens.service.ts` with pure async functions `getApiTokens()`, `generateApiToken({ org_id, name, scope, workspace_id })`, and `revokeApiToken(id)`. These functions must query/update `mcp_tokens` with `token_source = 'api'`, call the `generate_api_token` RPC, and never call `generate_obsidian_token`. Create `src/hooks/useApiTokens.ts` with TanStack Query hooks for list/generate/revoke, invalidating a stable `['api-tokens']` query key after mutations.</action>
  <acceptance_criteria>
    - `src/services/api-tokens.service.ts` exists and contains `getApiTokens`, `generateApiToken`, and `revokeApiToken`.
    - `src/services/api-tokens.service.ts` filters list/revoke operations with `token_source` equal to `api`.
    - `src/hooks/useApiTokens.ts` exists and imports from `@/services/api-tokens.service`.
    - No new component imports Supabase directly for API-token data access.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "getApiTokens|generateApiToken|revokeApiToken|\\['api-tokens'\\]|token_source.*api" src/services/api-tokens.service.ts src/hooks/useApiTokens.ts && ! rg -n "generate_obsidian_token|useObsidianTokens" src/services/api-tokens.service.ts src/hooks/useApiTokens.ts</automated>
  </verify>
  <done>Frontend has service+hook separated API-token management.</done>
</task>

<task type="auto">
  <name>Task 3: Replace Obsidian token UI with API token UI</name>
  <files>src/components/settings/ApiTokensSection.tsx, src/components/settings/IntegrationsTab.tsx</files>
  <read_first>
    - src/components/settings/ObsidianConnectorSection.tsx
    - src/components/settings/IntegrationsTab.tsx
    - src/hooks/useApiTokens.ts
    - .planning/phases/06.2-callvault-rest-api/06.2-UI-SPEC.md
    - src/CLAUDE.md
  </read_first>
  <action>Create `ApiTokensSection.tsx` using existing shadcn and Remix Icon patterns. It must render an `API Tokens` or `Developer API` settings section, use `useApiTokens` hooks, provide a `Generate API token` flow, show the raw token exactly once with copy affordance, list active API tokens, and require confirmation before revoke. Update `IntegrationsTab.tsx` to render `ApiTokensSection` instead of `ObsidianConnectorSection`. Preserve the existing one-click Obsidian ZIP export only if it remains wired through client-side export utilities and does not depend on Obsidian token generation.</action>
  <acceptance_criteria>
    - `IntegrationsTab.tsx` imports `ApiTokensSection`.
    - `IntegrationsTab.tsx` no longer imports `ObsidianConnectorSection`.
    - User-facing copy includes `Generate API token`.
    - User-facing copy does not include `Generate Obsidian Token` or `New Obsidian token`.
    - `ApiTokensSection.tsx` uses Remix icons and does not import lucide.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "ApiTokensSection|Generate API token|Copy your API token now" src/components/settings && ! rg -n "Generate Obsidian Token|New Obsidian token|useObsidianTokens" src/components/settings src/hooks src/services && npm run build</automated>
  </verify>
  <done>Settings presents API tokens as the integration credential and no longer promotes Obsidian-specific tokens.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06.2-01 | Spoofing | API token generation | mitigate | Generate source-labeled API tokens server-side and show raw token only once. |
| T-06.2-02 | Information Disclosure | settings token UI | mitigate | List token metadata only; never re-expose stored token values after creation. |
| T-06.2-03 | Elevation of Privilege | workspace-scoped tokens | mitigate | Validate workspace scope inputs in SQL/RPC and preserve org/workspace columns. |
</threat_model>

<verification>
Run the targeted `rg` assertions, `npm run build`, and if Supabase credentials are available run `supabase db push` after the migration is reviewed.
</verification>

<success_criteria>
- API tokens can be generated with `token_source = 'api'`.
- Settings UI manages API tokens through service/hook separation.
- Obsidian-specific token creation is no longer the user-facing credential path.
</success_criteria>

<output>
Create `.planning/phases/06.2-callvault-rest-api/06.2-01-SUMMARY.md` when done.
</output>
