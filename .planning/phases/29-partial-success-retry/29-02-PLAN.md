---
phase: 29-partial-success-retry
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/components/import/SyncJobBanner.tsx
  - src/components/import/__tests__/SyncJobBanner.retry.test.tsx
autonomous: true
requirements: [FAIL-02]
must_haves:
  truths:
    - "A 'Retry failed (N)' action on a partial/failed banner re-attempts ONLY the failed_ids set — never the already-synced or skipped"
    - "Retry dispatches through the EXISTING single-call retry path (no new import path is built)"
    - "Retry is idempotent — a call that actually imported produces no duplicate (Phase 24 constraint); successful retries drop out of the failed set as counts update via useSyncJobs"
    - "Retry scopes to the caller's own job/org — a user cannot retry another org's failed ids"
  artifacts:
    - path: "src/components/import/SyncJobBanner.tsx"
      provides: "Retry failed (N) action on completed_with_errors + failed banners, wired to the existing retry path"
      contains: "failed_ids"
    - path: "src/components/import/__tests__/SyncJobBanner.retry.test.tsx"
      provides: "Tests proving retry fires only failed_ids through the existing path, count N matches failed_ids.length, and disabled/empty handling"
  key_links:
    - from: "src/components/import/SyncJobBanner.tsx"
      to: "useRetryFailedImport (existing) -> retryFailedImport -> getConnectorSyncFunctionName + { singleCallId }"
      via: "onRetry callback per failed id"
      pattern: "onRetry|useRetryFailedImport|singleCallId"
---

<objective>
Add the FAIL-02 "Retry failed" action to the SyncJobBanner: re-attempt ONLY the failure set (failed_ids = requested − synced) by dispatching each failed id through the EXISTING single-call retry path. Do NOT build a new import path, and do NOT retry the already-synced or skipped ids.

Purpose: The last piece of "trustworthy import" — John retries just the 12 failures with one click, idempotently (no dupes, no full replay). The retry machinery already exists in the service/hook layer (`retryFailedImport` -> `{ singleCallId }`, `useRetryFailedImport`); this plan WIRES it to the banner so failures are actionable where they are surfaced, instead of only in the unwired FailedImportsSection.
Output: Retry action on the banner + tests. Frontend-only; no backend change (the existing per-call path already accepts singleCallId).
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/CLAUDE.md

# The banner extended in Plan 01 (this plan adds the retry action to it)
@src/components/import/SyncJobBanner.tsx
# Wiring source where the import was triggered (where the banner mounts)
@src/components/import/ImportSurface.tsx

<interfaces>
The EXISTING retry path — REUSE, do not rebuild. Source files below.

src/services/import-sources.service.ts:
```typescript
// Idempotent single-call retry: invokes getConnectorSyncFunctionName(sourceApp)
// with { singleCallId: failedExternalId }. canRetryFailedImport gates providers
// that cannot retry (e.g. file-upload). Returns { success, error? }.
export async function retryFailedImport(
  sourceApp: string,
  failedExternalId: string,
): Promise<{ success: boolean; error?: string }>;
```

src/hooks/useImportSources.ts:
```typescript
// TanStack mutation wrapping retryFailedImport. onSuccess invalidates
// imports.failed / sources / counts + call-list caches and toasts.
export function useRetryFailedImport(): UseMutationResult<
  { success: boolean; error?: string },
  Error,
  { sourceApp: string; failedExternalId: string }
>;
```

src/lib/connector-sync-functions.ts:
```typescript
export function canRetryFailedImport(sourceApp: string): boolean; // false for providers w/o a sync fn (file-upload)
```

The SyncJob (src/hooks/useSyncJobs.ts) carries the failure set:
  failed_ids: string[] | null   — the EXACT retry set (genuine errors only; skipped/synced excluded by the pager already)
  source_app: string | null     — the provider to dispatch retry through
</interfaces>

<reference>
- The pager (Phase 28) already guarantees failed_ids holds ONLY genuine errors — 23505/duplicate losers are classified into skipped_count, never failed_ids. So "retry every id in failed_ids" IS "retry only the failures" by construction; no client-side subtraction of synced/skipped is needed.
- Idempotency: the Phase 24 org-scoped unique index (organization_id, source_app, source_call_id) means retrying a call that did import is a no-op/skip in the connector pipeline — never a duplicate row.
- Org/IDOR scoping: retryFailedImport dispatches through getConnectorSyncFunctionName(sourceApp), and the connector sync functions authenticate the caller + run validateRequestedWorkspaceId server-side (sync-meetings:543 IDOR gate). The retry therefore reuses the existing authenticated per-call path — a user cannot retry another org's ids because the server resolves the caller's own identity/org for the singleCallId import.
- FailedImportsSection.tsx already demonstrates the exact mutation call shape: retryMutation.mutate({ sourceApp, failedExternalId }). Mirror it.
- canRetryFailedImport(sourceApp) must gate the button (disable + explanatory label when a provider cannot retry, e.g. file-upload).
</reference>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — retry-only-failures tests</name>
  <read_first>
    - src/components/import/SyncJobBanner.tsx (post Plan 01 — the completed_with_errors + failed branches)
    - src/components/import/FailedImportsSection.tsx (the existing retryMutation.mutate({ sourceApp, failedExternalId }) call shape to mirror)
    - src/components/import/__tests__/SyncJobBanner.breakdown.test.tsx (Plan 01 fixture factory to reuse) and the Phase-27 banner test for render setup
    - src/hooks/useImportSources.ts (useRetryFailedImport signature)
  </read_first>
  <files>src/components/import/__tests__/SyncJobBanner.retry.test.tsx</files>
  <action>
    Write a RED-first test. Decide the banner's retry surface as a prop the parent supplies — add an optional onRetry callback to SyncJobBanner (signature: (sourceApp: string, failedIds: string[]) => void) so the banner stays presentational and the mutation lives in the parent (matches the existing onDismiss prop pattern; keeps the banner free of hooks/data access). The test passes a spy onRetry.

    Render a completed_with_errors job (reuse the Plan 01 fixture: progress_total 30, synced 18, failed_ids of 12 specific TEXT ids, skipped_count 5, source_app "fathom") with the onRetry spy. Assert: (1) a "Retry failed (12)" affordance is present and the count equals failed_ids.length; (2) clicking it calls onRetry exactly once with ("fathom", the EXACT 12 failed_ids array) — assert the array passed is the failed_ids only, NOT including synced or skipped (skipped has no ids on the row; assert length is 12 and the ids equal failed_ids); (3) for a job with empty/null failed_ids the retry affordance is absent or disabled; (4) when onRetry is not provided the banner renders without crashing and shows no retry button.

    These MUST fail (RED) — the banner has no retry affordance yet.
  </action>
  <acceptance_criteria>
    - Test file runs via vitest and FAILS (RED) — no retry affordance exists yet
    - Asserts the count N equals failed_ids.length and onRetry receives ONLY failed_ids (not synced/skipped)
    - Asserts disabled/absent on empty failed_ids and graceful render with no onRetry
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy npx vitest run src/components/import/__tests__/SyncJobBanner.retry.test.tsx</automated>
  </verify>
  <done>RED test committed asserting retry fires only failed_ids with the correct N and provider, plus disabled/absent edge cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — wire the Retry failed action to the existing path</name>
  <read_first>
    - src/components/import/SyncJobBanner.tsx (DismissibleBanner — add a retry button alongside the dismiss control)
    - src/components/import/ImportSurface.tsx lines ~377-406 (useRetryFailedImport import + where the banner is mapped at ~580) and the existing useSyncJobs/dismissJob wiring
    - src/components/import/FailedImportsSection.tsx (the canRetryFailedImport gate + RiRefreshLine spinner pattern)
  </read_first>
  <files>src/components/import/SyncJobBanner.tsx, src/components/import/ImportSurface.tsx</files>
  <action>
    In SyncJobBanner.tsx: add an optional onRetry?: (sourceApp: string, failedIds: string[]) => void prop. On the completed_with_errors branch (and the failed branch when failed_ids is non-empty) render a "Retry failed ({N})" button (Remix RiRefreshLine icon, hollow/bordered style consistent with FailedImportsSection's retry button) where N = job.failed_ids?.length ?? 0. Button onClick calls onRetry(job.source_app ?? "", job.failed_ids ?? []). Gate with canRetryFailedImport(job.source_app ?? "") — disable with an explanatory label when the provider cannot retry (mirror FailedImportsSection). Hide/disable the button when failed_ids is empty or onRetry is undefined. Pass failed_ids verbatim — opaque TEXT ids, no coercion. No setTimeout.

    In ImportSurface.tsx: import and instantiate useRetryFailedImport() (already imported into the file's domain via useImportSources). Define a handleRetryFailed callback that, given (sourceApp, failedIds), fires the existing per-call retry path for EACH id — loop failedIds and call retryMutation.mutate({ sourceApp, failedExternalId: id }) (the same shape FailedImportsSection uses). Pass handleRetryFailed as onRetry to the SyncJobBanner in the existing [...activeJobs, ...visibleTerminalJobs].map at ~line 580. After retries fire, counts/banner update through the existing useSyncJobs Realtime/poll path (the mutation's onSuccess already invalidates imports.failed/sources/counts) — successful retries leave the failed set with no extra wiring. Do NOT build a new import path, a new edge function, or a new sync_jobs row — reuse the singleCallId path only.
  </action>
  <acceptance_criteria>
    - Banner shows "Retry failed (N)" with N = failed_ids.length; clicking fires onRetry with (source_app, failed_ids) only
    - ImportSurface loops failed_ids through the EXISTING useRetryFailedImport / retryFailedImport / { singleCallId } path — grep confirms no new edge-function name and no new import path added
    - canRetryFailedImport gates the button; empty failed_ids disables/hides it
    - Plan 01 + retry tests GREEN; existing banner + ImportSurface tests GREEN; no setTimeout; tsc clean for touched files
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy npx vitest run src/components/import/__tests__/SyncJobBanner.retry.test.tsx src/components/import/__tests__/SyncJobBanner.breakdown.test.tsx</automated>
    <automated>npx tsc -p tsconfig.app.json --noEmit 2>&1 | rtk grep -E "SyncJobBanner|ImportSurface"</automated>
  </verify>
  <done>Retry failed (N) action wired to the existing singleCallId path; retries only failed_ids; idempotent (no new path, Phase 24 constraint); successful retries drop from the failed set via useSyncJobs; all tests GREEN; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| banner click -> retry mutation -> connector sync edge function | User-triggered retry of specific source_call_ids crosses to the server; the server authenticates the caller and gates workspace/org. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Elevation of privilege / IDOR | retry dispatch to another org's failed ids | mitigate | Retry reuses the EXISTING authenticated per-call path: retryFailedImport -> getConnectorSyncFunctionName -> connector sync fn, which authenticates the caller and runs validateRequestedWorkspaceId (sync-meetings:543). The server resolves the caller's own identity/org for the singleCallId import — a user cannot import a call into / retry against an org they do not own. No org/workspace id is taken from the banner. |
| T-29-04 | Tampering / duplication | retrying an already-imported call | mitigate | Idempotent by the Phase 24 org-scoped unique index (organization_id, source_app, source_call_id); the connector pipeline checkDuplicate path makes a retry of an actually-imported call a no-op/skip, never a duplicate row. |
| T-29-05 | Tampering | id coercion on failed_ids | mitigate | failed_ids passed verbatim as opaque TEXT through { singleCallId }; no parseInt/Number (dual-ID rule, src/CLAUDE.md). |
| T-29-SC | Tampering | npm installs | mitigate | Zero new packages — reuses @remixicon/react + existing hooks. No install task — legitimacy gate N/A. |
</threat_model>

<verification>
- vitest SyncJobBanner.retry.test.tsx + SyncJobBanner.breakdown.test.tsx GREEN
- Existing banner + ImportSurface tests still GREEN
- Retry dispatches through retryFailedImport / useRetryFailedImport / { singleCallId } — no new edge function name introduced (grep)
- setTimeout count in SyncJobBanner.tsx == 0
- tsc clean for SyncJobBanner.tsx + ImportSurface.tsx
</verification>

<success_criteria>
- FAIL-02 met: "Retry failed (N)" re-attempts ONLY failed_ids via the existing single-call retry path
- No new import path / edge function / sync_jobs row created
- Idempotent — no duplicate rows on retry (Phase 24 constraint)
- Successful retries drop from the failed set as counts update via useSyncJobs
- Retry scoped to the caller's own org via the existing authenticated path (no IDOR)
- Frontend-only; no backend or prod push (batched to milestone-end per the v2.1 deploy decision)
</success_criteria>

<output>
Create `.planning/phases/29-partial-success-retry/29-02-SUMMARY.md` when done
</output>
