---
phase: 29-partial-success-retry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/import/SyncJobBanner.tsx
  - src/components/import/__tests__/SyncJobBanner.breakdown.test.tsx
autonomous: true
requirements: [FAIL-01]
must_haves:
  truths:
    - "After a partial-success import, the banner where the import was triggered shows '{synced} of {requested} imported, {failed} failed' from the job's own counts"
    - "Skipped (already-synced duplicates) are shown as informational, never counted as failures"
    - "The breakdown persists with no timer — it leaves the DOM only via the existing user dismiss control"
  artifacts:
    - path: "src/components/import/SyncJobBanner.tsx"
      provides: "completed_with_errors banner rendering the precise {synced} of {requested} imported, {failed} failed breakdown with skipped distinguished"
      contains: "of"
    - path: "src/components/import/__tests__/SyncJobBanner.breakdown.test.tsx"
      provides: "RED-first tests asserting the breakdown copy, skipped-vs-failed distinction, and no-timer persistence"
  key_links:
    - from: "src/components/import/SyncJobBanner.tsx"
      to: "SyncJob (job.progress_total / synced_ids / failed_ids / skipped_count)"
      via: ".length on string[] arrays + numeric counts, no coercion"
      pattern: "progress_total|skipped_count|failed_ids"
---

<objective>
Make the SyncJobBanner render the precise partial-success breakdown FAIL-01 requires: "{synced} of {requested} imported, {failed} failed", reading the job's own counts, distinguishing failed (genuine, retryable errors) from skipped (already-synced duplicates — informational). This replaces the current vague "{synced} synced, {failed} failed" line.

Purpose: Closes John's exact complaint — after an import he saw no precise status. Phase 27 made failures visible + persistent; this tells him exactly "18 of 30 imported, 12 failed" where the import was triggered.
Output: An extended SyncJobBanner (display only — retry action is Plan 02) + RED-first tests.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/CLAUDE.md

# The component to extend (Phase 27 banner — display only here)
@src/components/import/SyncJobBanner.tsx

<interfaces>
The SyncJob shape the banner reads. Source: src/hooks/useSyncJobs.ts.
Counts the breakdown is built from — all already present on the row:
  progress_total: number        — total recordings REQUESTED in this job (= "requested")
  synced_ids: string[] | null   — successfully imported ids (= "synced")
  failed_ids: string[] | null   — genuine errors, retryable (= "failed")
  skipped_count: number | null  — Phase 28 23505 to skipped already-synced dupes (informational)
Ids are opaque TEXT — use .length only, never parseInt/Number (src/CLAUDE.md dual-ID rule).
</interfaces>

<reference>
Current banner behaviour (src/components/import/SyncJobBanner.tsx):
- completed_with_errors -> DismissibleBanner subtitle "{syncedCount} synced, {failedCount} failed" (syncedCount = synced_ids.length, failedCount = failed_ids.length).
- failed -> DismissibleBanner subtitle job.error.
- NO setTimeout anywhere — terminal banners leave the DOM only via onDismiss(job.id). This MUST stay true.
- DismissibleBanner subtitle accepts React.ReactNode, so richer breakdown markup is already supported.
</reference>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — breakdown + skipped-distinction tests</name>
  <read_first>
    - src/components/import/SyncJobBanner.tsx (current completed_with_errors / failed branches; DismissibleBanner subtitle is ReactNode)
    - src/components/import/__tests__/ for the existing banner test style (Phase 27 wrote SyncJobBanner tests — match their render/query patterns; use @testing-library/react)
    - src/hooks/useSyncJobs.ts (SyncJob fields the test fixtures must set)
  </read_first>
  <files>src/components/import/__tests__/SyncJobBanner.breakdown.test.tsx</files>
  <action>
    Write a RED-first test file for the FAIL-01 breakdown. Build a SyncJob fixture factory producing a completed_with_errors job with progress_total 30, synced_ids of length 18, failed_ids of length 12, skipped_count 0. Assert the rendered banner text contains the precise breakdown "18 of 30 imported, 12 failed" — match via container.textContent against tolerant regexes (such as one for 18 of 30 imported and one for 12 failed) so whitespace and element boundaries do not make it brittle; the Phase-27 banner tests already use the container.textContent approach.

    Add a second fixture with skipped_count 5 (progress_total 30, synced 13, failed 12, skipped 5) and assert the banner ALSO surfaces the skipped count as informational, separate from failed — assert the word "skipped" appears and that the failed count text is still "12 failed" (skipped is NOT folded into failed). Assert the skipped segment is omitted when skipped_count is 0/null (the zero-skipped fixture renders no "skipped" text).

    Add a third assertion that the component renders NO timer: read the SOURCE of SyncJobBanner.tsx via fs.readFileSync (the existing src/services/__tests__/import-sources.retry.test.ts uses readFileSync for exactly this kind of source assertion) and assert it contains zero setTimeout — persistence is load-bearing for FAIL-01.

    These MUST fail against the current banner (current copy is "{synced} synced, {failed} failed" with no "of"/"imported"/"skipped").
  </action>
  <acceptance_criteria>
    - New test file exists and runs via vitest
    - Tests FAIL (RED) against the current banner — failure is the of/imported/skipped copy not yet present
    - No mocks of the banner itself; fixtures set real SyncJob fields; counts derived from .length / numeric fields only
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy npx vitest run src/components/import/__tests__/SyncJobBanner.breakdown.test.tsx</automated>
  </verify>
  <done>Test file committed RED; asserts breakdown copy, skipped-vs-failed distinction (incl. omitted at 0), and zero setTimeout in the banner source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — render the precise breakdown + skipped distinction</name>
  <read_first>
    - src/components/import/SyncJobBanner.tsx (completed_with_errors and failed branches + DismissibleBanner)
    - The RED test from Task 1 (the exact copy/regex it expects)
  </read_first>
  <files>src/components/import/SyncJobBanner.tsx</files>
  <action>
    In the completed_with_errors branch, replace the subtitle "{syncedCount} synced, {failedCount} failed" with the FAIL-01 breakdown: "{syncedCount} of {requested} imported, {failedCount} failed", where requested is the total recordings requested. Wrap each number in a tabular-nums span (matches existing style; src/CLAUDE.md typography rule — numbers always tabular-nums). Compute requested as job.progress_total when truthy; if progress_total is 0/null (legacy rows that never set it) fall back to syncedCount + failedCount + skippedCount so the "of N" denominator stays truthful.

    Add a skipped segment: read skippedCount from job.skipped_count, defaulting to 0. When skippedCount is greater than 0, append an informational clause such as a muted "{skippedCount} already synced (skipped)" — NEVER counted into failed. When skippedCount is 0, render nothing for it. Skipped must read as informational, not an error.

    Keep counts derived from .length on the string[] arrays plus numeric progress_total/skipped_count only — no parseInt/Number coercion (dual-ID rule). Do NOT introduce any setTimeout/timer. Leave the failed-status branch's job.error subtitle as-is for this plan (Plan 02 adds the retry action to both branches). Do not change the active/processing or clean-completed branches.
  </action>
  <acceptance_criteria>
    - completed_with_errors banner renders "{synced} of {requested} imported, {failed} failed" (verifiable: the fixture 18/30/12 produces "18 of 30 imported, 12 failed")
    - skipped_count > 0 surfaces an informational skipped/already-synced clause distinct from the failed count; skipped_count 0/null renders no skipped text
    - Zero setTimeout in SyncJobBanner.tsx (grep gate)
    - Task 1 tests GREEN; existing Phase-27 SyncJobBanner tests still GREEN
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy npx vitest run src/components/import/__tests__/SyncJobBanner.breakdown.test.tsx</automated>
    <automated>npx tsc -p tsconfig.app.json --noEmit 2>&1 | rtk grep "SyncJobBanner"</automated>
  </verify>
  <done>completed_with_errors banner shows the precise breakdown; skipped surfaced as informational and distinct from failed; zero setTimeout; new + existing banner tests GREEN; tsc clean for the touched file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync_jobs row -> banner render | The banner only DISPLAYS counts from the user's own RLS-scoped job (useSyncJobs). No input crosses to the server in this plan — display only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Information disclosure | SyncJobBanner counts | accept | Counts come from the user's own RLS-scoped sync_jobs row (useSyncJobs filters user_id=eq + org); no cross-user data is reachable to display. No new query added. |
| T-29-02 | Tampering | id coercion | mitigate | Counts use .length on TEXT[] arrays + numeric progress_total/skipped_count only — no parseInt/Number (dual-ID rule, src/CLAUDE.md). |
| T-29-SC | Tampering | npm installs | mitigate | Zero new packages (uses existing @remixicon/react + tailwind tokens). No install task — legitimacy gate N/A. |
</threat_model>

<verification>
- vitest SyncJobBanner.breakdown.test.tsx GREEN
- Existing Phase-27 banner tests still GREEN
- setTimeout count in SyncJobBanner.tsx == 0
- tsc clean for SyncJobBanner.tsx
</verification>

<success_criteria>
- FAIL-01 display met: completed_with_errors banner renders "{synced} of {requested} imported, {failed} failed" from the job's own counts where the import was triggered
- Skipped distinguished from failed (informational, never retried-as-failure; omitted at 0)
- No timer — banner persists until user dismiss (Phase 27 invariant preserved)
- No backend/prod contact; no new package
</success_criteria>

<output>
Create `.planning/phases/29-partial-success-retry/29-01-SUMMARY.md` when done
</output>
