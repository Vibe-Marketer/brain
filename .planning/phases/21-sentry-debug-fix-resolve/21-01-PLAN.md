---
phase: 21-sentry-debug-fix-resolve
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260613230000_sentry_debounce_cycletime_cap.sql
  - supabase/functions/sentry-resolve/__tests__/sentry-cap-debounce.integration.test.ts
autonomous: false
requirements: [SEN-04, SEN-05]
user_setup:
  - service: supabase
    why: "Apply the new migration to the linked project so both repos can consume the new schema"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: ".env (already present) — used by supabase db push --linked"

must_haves:
  truths:
    - "A Sentry fingerprint that has recurred <3 times, or whose recurrences fall outside a 15-min window, is NOT claimable"
    - "A Sentry fingerprint that has recurred >=3 times within 15 min IS claimable"
    - "Per-fingerprint fix-attempt state (attempts, frozen) is durable and service-role-only"
    - "A resolved Sentry ticket records when the write-back succeeded and appears in a tracking surface that evaluates the resolve-ASAP target"
    - "The migration is applied to the linked Supabase project (schema push succeeds)"
  artifacts:
    - path: "supabase/migrations/20260613230000_sentry_debounce_cycletime_cap.sql"
      provides: "Debounce predicate/RPC, sentry_resolved_at column, resolve-ASAP cycle-time metrics RPC/view, sentry_fingerprint_cap table + cap RPCs"
      contains: "sentry_fingerprint_cap"
    - path: "supabase/functions/sentry-resolve/__tests__/sentry-cap-debounce.integration.test.ts"
      provides: "Integration coverage for the debounce predicate + cap state transitions"
      contains: "sentry_fingerprint_cap"
  key_links:
    - from: "supabase/migrations/20260613230000_sentry_debounce_cycletime_cap.sql"
      to: "public.tickets"
      via: "ADD COLUMN sentry_resolved_at (additive — never edits ingest_sentry_ticket)"
      pattern: "ADD COLUMN sentry_resolved_at"
    - from: "supabase/migrations/20260613230000_sentry_debounce_cycletime_cap.sql"
      to: "SEN-04 resolve-ASAP tracking"
      via: "metrics RPC/view computes error→ticket→fix→resolve cycle time and target status"
      pattern: "resolve_asap|cycle_time"
---

<objective>
Create the brain-owned schema that BOTH repos depend on for Phase 21: the debounce gate (SEN-04), the cycle-time column + resolve-ASAP tracking surface (SEN-04), and the per-fingerprint fix-cap state (SEN-05). This migration is the cross-repo coordination point per `docs/architecture/autopilot-brain-ownership.md` — brain owns schema; autopilot consumes it. It MUST be schema-pushed before any autopilot plan that reads the cap table, debounce predicate, or cycle-time tracking RPC/view (Wave 2/3).

Purpose: SEN/QA ship their damping in the same phase that wires the source (STATE Key Decision) — auto-ingestion without debounce floods the queue, and auto-resolve without a per-fingerprint cap self-feeds a regression loop (RESEARCH Pitfall 3). The guards are part of definition-of-done, not a follow-up.
Output: one additive migration + an integration test pinning the debounce predicate and cap transitions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-sentry-debug-fix-resolve/21-CONTEXT.md
@.planning/phases/21-sentry-debug-fix-resolve/21-RESEARCH.md
@.planning/phases/21-sentry-debug-fix-resolve/21-PATTERNS.md
@supabase/CLAUDE.md
@supabase/migrations/20260612130000_sentry_ticket_ingestion.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration — debounce predicate, cycle-time column, per-fingerprint cap table + RPCs</name>
  <files>supabase/migrations/20260613230000_sentry_debounce_cycletime_cap.sql</files>
  <behavior>
    - sentry_fingerprint_cap with frozen=false and fix_attempts<3 → fingerprint is claimable + still incrementable
    - 4th attempt recorded → frozen=true, frozen_at stamped (category-scoped, never a global flag)
    - Debounce predicate: a sentry ticket with occurrence_count>=3 AND last_seen_at within 15 min of created_at → fixable=true; <3 occurrences OR stale → fixable=false
    - tickets.sentry_resolved_at is NULL until a resolve write-back stamps it
    - A resolved sentry ticket appears in the metrics tracking surface with cycle_time_seconds and resolve_asap_met/resolve_asap_status evaluated against the target
  </behavior>
  <action>
Per D-02 (debounce) and D-03 (per-fingerprint cap) and Claude's-Discretion (schema for cycle-time + cap state). Follow the EXACT migration template in `supabase/CLAUDE.md` (header block, `====` section banners, COMMENT ON, RLS on every new table, REVOKE-from-public + GRANT-service_role on every new function) and the lineage idiom of the analog `supabase/migrations/20260612130000_sentry_ticket_ingestion.sql`. Filename uses timestamp `20260613230000` (after the ingestion migration).

ADDITIVE ONLY — do NOT edit `ingest_sentry_ticket` or the `tickets` partial-unique index (RESEARCH Anti-Pattern: changing v1.0 ingestion is out of scope; Deferred). Build, in one migration:

(1) `ALTER TABLE public.tickets ADD COLUMN sentry_resolved_at TIMESTAMPTZ;` for SEN-04 cycle time (set later by the resolve path; cycle time = sentry_resolved_at - created_at). COMMENT ON the column.

(1b) Add a service-role-readable cycle-time tracking surface for SEN-04 — either a SECURITY DEFINER RPC (preferred for target parameterization) named `sentry_resolve_cycle_time_metrics(p_target_minutes INTEGER DEFAULT 30)` or a view plus RPC wrapper. It MUST compute the full error→ticket→fix→resolve path for `source='sentry'` tickets: `error_at`/`ticket_created_at` from `tickets.created_at`, `fix_at` from the latest successful/merged `runner_runs` row for the ticket (use the actual runner_runs columns from the existing writer), `resolved_at` from `tickets.sentry_resolved_at`, `cycle_time_seconds = EXTRACT(EPOCH FROM sentry_resolved_at - tickets.created_at)`, and an explicit target evaluation (`resolve_asap_target_minutes`, `resolve_asap_met`, and/or `resolve_asap_status`). This must be TRACKING, not just a raw timestamp: a resolved Sentry ticket must show whether it met the resolve-ASAP target. REVOKE from PUBLIC/anon/authenticated; GRANT EXECUTE/SELECT to service_role only. If a view is used, keep user-facing access out of scope unless an Admin metric consumes it via service role.

(2) `CREATE TABLE public.sentry_fingerprint_cap (fingerprint TEXT PRIMARY KEY, fix_attempts INTEGER NOT NULL DEFAULT 0, frozen BOOLEAN NOT NULL DEFAULT FALSE, frozen_at TIMESTAMPTZ, last_attempt_at TIMESTAMPTZ)` per RESEARCH Code Examples / PATTERNS. `fingerprint` holds `sentry:<issue_id>` matching `tickets.fingerprint`. `ENABLE ROW LEVEL SECURITY` with NO policy (deny-all to anon/authenticated is correct — service-role-only, no user columns; PATTERNS RLS note). This table does NOT go in CROSS_ORG_TABLES.

(3) A SECURITY DEFINER RPC `record_fingerprint_fix_attempt(p_fingerprint TEXT, p_cap INTEGER DEFAULT 3)` that upserts the cap row, increments `fix_attempts`, stamps `last_attempt_at`, and when the post-increment `fix_attempts > p_cap` sets `frozen=true` + `frozen_at=NOW()`. RETURNS the new `fix_attempts` + `frozen`. This is the per-fingerprint cap mechanism that freezes the SINGLE category on the 4th regression (D-03, ≤3 then freeze). REVOKE from PUBLIC/anon/authenticated; GRANT EXECUTE to service_role.

(4) A debounce predicate the autopilot claimer can read. Express it as a SECURITY DEFINER RPC `sentry_ticket_fixable(p_ticket_id UUID, p_min_occurrences INTEGER DEFAULT 3, p_window_minutes INTEGER DEFAULT 15)` returning BOOLEAN: TRUE only when the ticket is source='sentry' AND occurrence_count >= p_min_occurrences AND last_seen_at <= created_at + (p_window_minutes || ' minutes')::interval (recurred enough, within the window — RESEARCH Pattern 3, D-02 ≥3 in 15 min). Non-sentry tickets return TRUE (debounce applies only to sentry). REVOKE/GRANT service_role as above. (RPC chosen over a generated column so the window is tunable without a schema change.)

Use snake_case columns (supabase/CLAUDE.md). Do NOT add a `user_id` to the cap table — it is system state, not user-owned.
  </action>
  <verify>
    <automated>set -euo pipefail; cd ~/dev/brain && grep -q "sentry_fingerprint_cap" supabase/migrations/20260613230000_sentry_debounce_cycletime_cap.sql && grep -q "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260613230000_sentry_debounce_cycletime_cap.sql && grep -q "GRANT EXECUTE ON FUNCTION public.record_fingerprint_fix_attempt" supabase/migrations/20260613230000_sentry_debounce_cycletime_cap.sql && grep -q "sentry_resolve_cycle_time_metrics\\|resolve_asap" supabase/migrations/20260613230000_sentry_debounce_cycletime_cap.sql && echo MIGRATION_OK</automated>
  </verify>
  <done>Migration file exists with the cap table (RLS enabled), the cycle-time column, the resolve-ASAP cycle-time tracking RPC/view, debounce/cap RPCs (record_fingerprint_fix_attempt, sentry_ticket_fixable), and service-role-only privileges. `ingest_sentry_ticket` is untouched (grep shows no edits to the ingestion function).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test — debounce predicate + cap transitions (uses TEST project)</name>
  <files>supabase/functions/sentry-resolve/__tests__/sentry-cap-debounce.integration.test.ts</files>
  <behavior>
    - sentry_ticket_fixable returns false for a fresh single-occurrence sentry ticket
    - sentry_ticket_fixable returns true once occurrence_count>=3 within the 15-min window
    - record_fingerprint_fix_attempt increments and, on the 4th call (cap 3), returns frozen=true
    - non-sentry ticket → sentry_ticket_fixable returns true (debounce does not apply)
    - a resolved Sentry ticket with a successful runner_run appears in the cycle-time tracking surface with cycle_time_seconds and target evaluation populated
  </behavior>
  <action>
Per SEN-04 (debounce + resolve-ASAP tracking) + SEN-05 (cap). Mirror the EXACT structure of `supabase/functions/sentry-webhook/__tests__/sentry-webhook.integration.test.ts`: vitest, service-role client against the TEST project, `describe.skipIf` guard on the `*_TEST_*` env vars, idempotent `afterAll` cleanup (capture-before-mutate; wrap each cleanup in try/catch). Follow `supabase/CLAUDE.md` "Running integration tests safely" — NEVER fall back to prod URLs; create TEST-tagged tickets inside the test and drop them in afterAll; sweep via `cleanup_test_fixture_users` if any user rows are seeded.

Seed sentry tickets via `ingest_sentry_ticket` (do not hand-INSERT — exercise the real ingestion path) so occurrence_count advances through real upserts. Assert the debounce/cap behaviors by calling the new RPCs. For the cycle-time tracking assertion, create or update the TEST ticket into the resolved shape (`status='resolved'`, `sentry_resolved_at` set) and seed the minimal successful/merged `runner_runs` row required by the metrics query, then assert the tracking surface returns that ticket with non-null `cycle_time_seconds` and explicit resolve-ASAP target evaluation. Use a unique TEST fingerprint per run (e.g. `sentry:test-<timestamp>`). Clean up the `sentry_fingerprint_cap`, runner_runs row, and seeded ticket in afterAll.
  </action>
  <verify>
    <automated>set -euo pipefail; cd ~/dev/brain && npm run test:integration -- sentry-cap-debounce</automated>
  </verify>
  <done>Integration test exists with safe TEST-env guards and pins debounce, resolve-ASAP tracking, and cap behaviors against the TEST project. Once TEST schema/env are present, the command must pass rather than skip. No prod fallback possible (guards intact).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Schema push the migration to the linked project</name>
  <what-built>The Phase 21 schema (cap table, cycle-time column, debounce + cap RPCs) applied to the linked Supabase project so the autopilot Wave 2/3 plans can consume it.</what-built>
  <how-to-verify>
1. Confirm `SUPABASE_ACCESS_TOKEN` is present: `set -euo pipefail; cd ~/dev/brain && grep -q SUPABASE_ACCESS_TOKEN .env && echo TOKEN_PRESENT`
2. Apply: `set -euo pipefail; cd ~/dev/brain && supabase db push --linked` — expect the new migration `20260613230000_sentry_debounce_cycletime_cap` in the applied list with no error.
3. Verify the table/RPCs landed: re-run `set -euo pipefail; cd ~/dev/brain && npm run test:integration -- sentry-cap-debounce` with TEST env set and confirm the cap/debounce/cycle-time integration test PASSES against a project that has the migration.
This is [BLOCKING] per the planner context: no autopilot plan that reads the cap table / debounce RPC / sentry_resolved_at / cycle-time tracking surface may run until this push succeeds. A skip is not a pass for this blocking gate after schema/env are present.
  </how-to-verify>
  <resume-signal>Type "pushed" once `supabase db push --linked` applied the migration cleanly, or describe the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/authenticated → cap table & RPCs | The cap table and both RPCs are system state; no user role may read or write them. |
| daemon (service-role) → schema | The daemon consumes the new RPCs/table with service-role auth (RLS bypassed by design). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Elevation of Privilege | sentry_fingerprint_cap table | mitigate | RLS enabled with NO policy → deny-all to anon/authenticated; only service-role (and SECURITY DEFINER RPCs) can touch it. |
| T-21-05 | Tampering | debounce predicate | mitigate | Predicate is a DB-side RPC reading occurrence_count/last_seen_at — no client SELECT-then-decide; the claimer cannot weaken the gate from app code. |
| T-21-SC | Tampering | supabase migration apply | mitigate | [BLOCKING] human-verified `supabase db push --linked`; additive-only (no edits to ingest_sentry_ticket); no new npm/pip/cargo packages installed. |
</threat_model>

<verification>
- `supabase db push --linked` applies the migration with no error (Task 3).
- `npm run test:integration -- sentry-cap-debounce` exercises debounce + cap + resolve-ASAP cycle-time tracking against the TEST project. It may skip only before schema/env are available; after the [BLOCKING] schema gate, a real pass is required.
- `grep` confirms RLS enabled + service-role-only grants on every new object; `ingest_sentry_ticket` unedited.
</verification>

<success_criteria>
- New migration exists, additive-only, RLS + service-role-only on all new objects.
- Debounce RPC enforces ≥3 occurrences within 15 min for sentry tickets (D-02); non-sentry returns true.
- Resolve-ASAP cycle-time tracking surface returns resolved sentry tickets with cycle time and target status.
- Cap RPC freezes the single fingerprint on the 4th attempt (≤3 then freeze, D-03).
- `sentry_resolved_at` column exists for cycle-time tracking.
- Migration applied to the linked project ([BLOCKING] push verified).
</success_criteria>

<output>
Create `.planning/phases/21-sentry-debug-fix-resolve/21-01-SUMMARY.md` when done.
</output>
