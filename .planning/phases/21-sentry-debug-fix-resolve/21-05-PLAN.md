---
phase: 21-sentry-debug-fix-resolve
plan: 05
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - autopilot/src/lib/claim.ts
  - autopilot/src/lib/claim.test.ts
autonomous: true
requirements: [SEN-04]

must_haves:
  truths:
    - "A Sentry candidate that has not met the debounce predicate (<3 occ / outside 15min) is dropped before pickNext"
    - "A Sentry candidate whose fingerprint is frozen in sentry_fingerprint_cap is excluded from selection (category-scoped, never global)"
    - "The locked claim ordering (urgent DESC → priority DESC → severity rank → created_at ASC) is unchanged"
    - "Severity→priority is satisfied (SEVERITY_RANK already participates; a redundant bump is added only if verified necessary, A5)"
  artifacts:
    - path: "autopilot/src/lib/claim.ts"
      provides: "selectNextTicket extended with a sentry debounce + frozen-fingerprint client-side filter, mirroring the excludeSources precedent"
      contains: "selectNextTicket"
    - path: "autopilot/src/lib/claim.test.ts"
      provides: "Unit coverage for debounce drop, frozen exclusion, ordering preservation, severity verification"
      contains: "debounce"
  key_links:
    - from: "autopilot/src/lib/claim.ts"
      to: "sentry_ticket_fixable / sentry_fingerprint_cap"
      via: "debounce predicate + frozen lookup feeding the client-side filter"
      pattern: "fixable|frozen"
---

<objective>
SEN-04 claim-side wiring: harden Sentry dedup with the DEBOUNCE gate (D-02 — a fingerprint must recur ≥3 times within 15 min before it is claimable) and EXCLUDE frozen fingerprints from selection (D-03 cap — category-scoped freeze, never global). Also satisfy "severity boosts priority": per RESEARCH A5, `compareTickets` ALREADY ranks by `SEVERITY_RANK` after `priority` — VERIFY that covers the requirement before adding a redundant priority bump.

Purpose: auto-ingestion without debounce floods the queue with transient spikes (RESEARCH Pitfall, STATE Key Decision: damping ships in the same phase). A frozen fingerprint must stop being claimed or it self-feeds the oscillation loop (Pitfall 3). This is the claimer's half of those guards; the cap state + debounce predicate live in the Plan 01 migration.
Output: `selectNextTicket` extended with a Sentry-only client-side filter (debounce + frozen), tests pinning the behavior and the unchanged ordering.

NOTE: work in `~/dev/autopilot` (separate repo; commits local-only). Requires Plan 01's migration schema-pushed (the debounce RPC + cap table) before this code can read them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-sentry-debug-fix-resolve/21-CONTEXT.md
@.planning/phases/21-sentry-debug-fix-resolve/21-RESEARCH.md
@.planning/phases/21-sentry-debug-fix-resolve/21-PATTERNS.md
@autopilot/src/lib/claim.ts
@autopilot/src/claimer.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Debounce + frozen-fingerprint exclusion in selectNextTicket</name>
  <files>autopilot/src/lib/claim.ts, autopilot/src/lib/claim.test.ts</files>
  <behavior>
    - A sentry candidate with occurrence data not meeting the debounce predicate is filtered out before pickNext
    - A sentry candidate whose fingerprint is frozen is filtered out before pickNext
    - Non-sentry candidates are never affected by debounce/frozen filtering
    - With debounce-met + unfrozen candidates, the head returned is identical to the pre-change ordering (urgent/priority/severity/created_at)
  </behavior>
  <action>
Per D-02 (debounce) + D-03 (frozen exclusion) + PATTERNS claim.ts section. Mirror the EXACT `excludeSources` client-side filter precedent in `selectNextTicket` (claim.ts ~lines 118-125): filter candidates BEFORE `pickNext`, leaving the locked `compareTickets` ordering untouched (the comment at the ordering block is the contract — do NOT invert the queue; SEVERITY_RANK exists because the enum declares `critical` first).

Extend `selectNextTicket` (and its `SelectOptions`) so that, for `source === "sentry"` candidates only, it drops any candidate that (a) has not met the debounce predicate or (b) is frozen. Two viable shapes — pick the one that keeps `selectNextTicket` testable with a stubbed `DbLike`:
  - Read the debounce/frozen state via the Plan 01 RPCs/table (e.g. call `sentry_ticket_fixable` per sentry candidate, and look up `sentry_fingerprint_cap.frozen`), OR
  - Widen the candidate `select` to include `occurrence_count, last_seen_at, fingerprint` and join/lookup the cap table, applying the debounce predicate client-side using the same ≥3-in-15min rule the migration encodes (keep the rule defined in ONE place — prefer calling the RPC so the window stays DB-tunable per Plan 01's design).
Default debounce ≥3 occurrences within 15 min (D-02); cap freeze is category-scoped (D-03) — a frozen fingerprint is excluded, but NON-frozen sentry candidates and ALL non-sentry candidates remain claimable (never a global freeze).

Write/extend claim.test.ts FIRST (RED): stub `DbLike` to return mixed candidates (sentry below debounce, sentry frozen, sentry fixable, non-sentry); assert the filtered-out ones never become the head, the fixable sentry + non-sentry are eligible, and the head under an all-eligible set matches the existing ordering.
  </action>
  <verify>
    <automated>set -euo pipefail; cd ~/dev/autopilot && bun test src/lib/claim.test.ts</automated>
  </verify>
  <done>selectNextTicket drops below-debounce and frozen sentry candidates before pickNext; non-sentry and fixable-sentry remain; locked ordering unchanged; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify severity→priority (A5) — bump only if necessary</name>
  <files>autopilot/src/lib/claim.test.ts</files>
  <behavior>
    - A higher-severity sentry ticket sorts ahead of a lower-severity one at equal urgent+priority (SEVERITY_RANK already does this)
    - If (and only if) priority and severity are proven independent such that severity does NOT boost ordering, a priority-bump mapping is added and tested
  </behavior>
  <action>
Per RESEARCH A5 + Code Examples "Severity → priority". `compareTickets` already orders by `SEVERITY_RANK[b.severity] - SEVERITY_RANK[a.severity]` AFTER `priority`. Add a focused test asserting that at equal `urgent` and `priority`, a `critical`/`high` sentry ticket sorts ahead of a `low` one — proving "severity boosts priority" is ALREADY satisfied by the existing rank. Only if this test reveals a gap (severity does not influence ordering as required) add a thin severity→priority mapping at ingestion-read/claim time and test it. Do NOT add a redundant bump if the existing rank covers it (anti-pattern: duplicate the same signal in two fields). Document the finding in the SUMMARY (covered by existing rank vs new bump needed).
  </action>
  <verify>
    <automated>set -euo pipefail; cd ~/dev/autopilot && bun test src/lib/claim.test.ts</automated>
  </verify>
  <done>A test proves severity ordering is satisfied by SEVERITY_RANK (or a justified priority-bump is added + tested); the SUMMARY records which path was taken. No redundant duplication of the severity signal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket pool (DB) → claimer selection | The claimer must not claim un-debounced or frozen sentry tickets; the gate decision is grounded in DB state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Tampering | debounce/frozen filter | mitigate | The debounce rule lives in the DB (Plan 01 RPC); the claimer reads it rather than reimplementing a weakenable copy. Frozen exclusion is category-scoped (single fingerprint), never a global stop. |
| T-21-SC | Tampering | new npm package | mitigate | No `bun add`; pure logic over existing DbLike client — assert `git diff package.json` empty. |
</threat_model>

<verification>
- `bun test src/lib/claim.test.ts` green (debounce drop, frozen exclusion, ordering preserved, severity proven).
- `git -C ~/dev/autopilot diff package.json` empty.
- Manual read confirms the locked `compareTickets` ordering is untouched.
</verification>

<success_criteria>
- Below-debounce and frozen sentry candidates are excluded from selection (D-02, D-03), never globally.
- Non-sentry selection and the locked ordering are unchanged.
- Severity→priority verified against the existing rank; redundant bump avoided unless proven necessary (A5).
</success_criteria>

<output>
Create `.planning/phases/21-sentry-debug-fix-resolve/21-05-SUMMARY.md` when done.
</output>
