---
phase: 21-sentry-debug-fix-resolve
plan: 06
type: execute
wave: 3
depends_on: [21-01, 21-02, 21-05]
files_modified:
  - autopilot/src/lib/sentry-resolve.ts
  - autopilot/src/lib/sentry-resolve.test.ts
  - autopilot/src/lib/approval.ts
  - autopilot/src/lib/approval.test.ts
autonomous: false
requirements: [SEN-05]

must_haves:
  truths:
    - "A Sentry ticket is resolved (write-back fires) ONLY when verifyDeploySha is true AND the 30-min quiet window has elapsed AND the fingerprint is not frozen"
    - "On the 4th regression for a fingerprint, the single fingerprint/category freezes (never global) and Andrew is paged via a solution-shaped tier-2 digest"
    - "A successful resolve stamps tickets.sentry_resolved_at (cycle-time close)"
    - "The daemon→sentry-resolve Edge-Function invoke seam is confirmed (no daemon precedent) before it ships"
    - "Resolve is never fired on merge alone — never resolve-on-merge"
  artifacts:
    - path: "autopilot/src/lib/sentry-resolve.ts"
      provides: "Daemon-side caller: verified-stable + quiet-window + cap gate → invoke sentry-resolve Edge Function → stamp sentry_resolved_at; oscillation freeze + page"
      exports: ["resolveSentryIfVerifiedStable"]
    - path: "autopilot/src/lib/sentry-resolve.test.ts"
      provides: "Unit coverage for the gate (no resolve unless verified+quiet+unfrozen), idempotency, cap freeze + page"
      contains: "verifyDeploySha"
  key_links:
    - from: "autopilot/src/lib/sentry-resolve.ts"
      to: "verifyDeploySha (approval.ts)"
      via: "import — reuse verbatim, never re-implement prod-SHA polling"
      pattern: "verifyDeploySha"
    - from: "autopilot/src/lib/sentry-resolve.ts"
      to: "sentry-resolve Edge Function"
      via: "supabase.functions.invoke OR direct fetch with service-role bearer (NO-ANALOG seam — checkpoint)"
      pattern: "functions.invoke|sentry-resolve"
    - from: "autopilot/src/lib/approval.ts"
      to: "resolveSentryIfVerifiedStable"
      via: "call from the executeApproval tail after status→resolved, for sentry-source tickets only"
      pattern: "resolveSentryIfVerifiedStable"
---

<objective>
SEN-05 daemon half: the outward-facing resolve write-back, fully gated. After `executeApproval` merges + verifies the deploy + sets the ticket `status: resolved` (approval.ts ~752-765), for Sentry-source tickets ONLY, gate the OUTWARD Sentry resolve on three conditions (D-03/D-04): (1) the fingerprint is not frozen in `sentry_fingerprint_cap`, (2) `verifyDeploySha` is true (deployed SHA == fix SHA), (3) the 30-min post-deploy quiet window has elapsed. Only then invoke the `sentry-resolve` Edge Function (Plan 02) and stamp `tickets.sentry_resolved_at` (cycle-time close). On the 4th regression for a fingerprint, FREEZE the single fingerprint/category (never global, via Plan 01's `record_fingerprint_fix_attempt`) and page Andrew with a solution-shaped tier-2 digest.

This plan contains the NO-ANALOG call seam: the autopilot daemon has NO existing Edge-Function-invoke call (it only talks to Postgres tables/RPCs today). The daemon→Edge-Function invocation is genuinely new wiring (RESEARCH A4, PATTERNS "No Analog Found") — it carries a blocking checkpoint.

Purpose: D-04 — resolving a real issue on the live `ai-simple` org is irreversible-ish; the write-back path must be gated, idempotent, and fire ONLY on verified-stable deploys. Never resolve-on-merge (manufactures false-regression storms). The cap is the anti-oscillation guard (Pitfall 3).
Output: `sentry-resolve.ts` daemon caller + the executeApproval tail hook + tests; the invoke-seam choice confirmed via checkpoint.

NOTE: work in `~/dev/autopilot` (separate repo; commits local-only). Requires Plan 01 schema-pushed (cap RPC + sentry_resolved_at) AND Plan 02's function deployed with secrets.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-sentry-debug-fix-resolve/21-CONTEXT.md
@.planning/phases/21-sentry-debug-fix-resolve/21-RESEARCH.md
@.planning/phases/21-sentry-debug-fix-resolve/21-PATTERNS.md
@.planning/design/escalation-tier2-solutions-not-problems.md
@autopilot/src/lib/approval.ts
@autopilot/src/lib/tier2.ts
@autopilot/src/lib/db.ts
@supabase/functions/sentry-resolve/index.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the daemon→Edge-Function invoke seam (NO precedent)</name>
  <decision>How does the daemon call the deployed sentry-resolve Edge Function?</decision>
  <context>
The autopilot daemon has NO existing Edge-Function-invoke call — it only talks to Postgres tables/RPCs (verified: grep of `~/dev/autopilot/src` for `functions.invoke`/`functions/v1` is empty; matches the ownership doc "share no code, integrate only through the DB"). The verified-stable gate / cap / paging all have analogs in approval.ts; the CALL itself is new wiring (RESEARCH A4). The Edge Function holds the Sentry secret (D-03); the daemon must never hold it. Closest precedent is `createServiceClient()` in db.ts.
  </context>
  <options>
    <option id="functions-invoke">
      <name>supabase.functions.invoke("sentry-resolve", { body: { issue_id } }) from a service-role client</name>
      <pros>Uses the supabase-js client the daemon already constructs (createServiceClient); auth header handled by the client; matches "integrate through Supabase" posture.</pros>
      <cons>Need to confirm the service-role client's functions.invoke carries an auth header the function's caller-authz check accepts.</cons>
    </option>
    <option id="direct-fetch">
      <name>Direct fetch to the function URL with the service-role bearer</name>
      <pros>Explicit control over headers; no reliance on functions.invoke auth behavior.</pros>
      <cons>Hardcodes the function URL; re-implements what functions.invoke does.</cons>
    </option>
  </options>
  <resume-signal>Select: functions-invoke or direct-fetch (and confirm A4 — the daemon reaches the deployed function with service-role auth the function accepts).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sentry-resolve.ts daemon caller — verified-stable + quiet-window + cap gate, idempotent, with cap freeze + page</name>
  <files>autopilot/src/lib/sentry-resolve.ts, autopilot/src/lib/sentry-resolve.test.ts</files>
  <behavior>
    - No resolve invoke when verifyDeploySha returns verified:false (never resolve-on-merge)
    - No resolve invoke when the quiet window (30 min since deploy) has not elapsed
    - No resolve invoke when the fingerprint is frozen
    - When verified + quiet + unfrozen: invokes sentry-resolve once; on success stamps tickets.sentry_resolved_at
    - record_fingerprint_fix_attempt increments per attempt; the 4th flips frozen=true → page via a solution-shaped tier-2 digest (category-scoped, never global)
    - An already-resolved (idempotent 200 from the function) is treated as success
  </behavior>
  <action>
Per D-03/D-04 + PATTERNS sentry-resolve.ts section + RESEARCH Don't Hand-Roll. Follow the `db.ts` DbLike injection idiom (service-role; log-don't-throw). Write the test FIRST (RED) with stubbed `verifyDeploySha`, a stubbed invoke fn, a fake clock, and a stubbed `DbLike`.

Export `resolveSentryIfVerifiedStable({ db, ticketId, fingerprint, mergedSha, deployedAt, now, verifyDeploySha?, invokeResolve?, quietWindowMs? })` (inject the seam fn + verifier + clock so the unit test never hits network/prod):
  1. CAP CHECK — read `sentry_fingerprint_cap.frozen` for the fingerprint; if frozen, do NOT resolve, return early (the cap already paged on the freeze).
  2. VERIFIED-STABLE — call `verifyDeploySha(mergedSha)` (import from ./approval — reuse VERBATIM, never re-implement prod-SHA polling). If `!verified`, do NOT resolve (anti-pattern: resolve-on-merge); return.
  3. QUIET WINDOW — require `now - deployedAt >= quietWindowMs` (default 30 min, D-03). If not elapsed, do NOT resolve (caller reschedules); return.
  4. RESOLVE — invoke the seam fn chosen in Task 1 with the bare issue_id (strip `sentry:` — the function also strips, defense in depth). Idempotent 200 = success.
  5. On success — stamp `tickets.sentry_resolved_at = now` (cycle-time close, Plan 01 column).
  6. On a 4xx/error from the function — DO NOT mark resolved; treat as a fix that did not hold. Use Plan 01's `record_fingerprint_fix_attempt(fingerprint)`; when it returns `frozen=true` (the 4th regression, ≤3 cap, D-03), FREEZE the single fingerprint/category (the RPC already set it) and page Andrew.

Cap-freeze paging: build a SOLUTION-SHAPED digest via `escalationDigest` + `validateTier2Digest` from `tier2.ts` (RESEARCH Don't Hand-Roll; the escalation design forbids raw error dumps — 1-2 sentences + 2-3 decisions). Page via the `user_notifications` insert shape from approval.ts::pageAdmin (NOTE: pageAdmin is NOT exported — replicate its insert shape: `user_id: ADMIN_USER_ID` (exported from approval.ts), `type: "health_alert"`, title/body from the digest, metadata source `autopilot-sentry-resolve`). Use `buildFixCategory` (exported, approval.ts) for the category key so the freeze matches the trust-ladder vocabulary `source:sentry:error:<class>`.

Never log the Sentry token (the daemon never holds it — the function does); never resolve on an unverified deploy.
  </action>
  <verify>
    <automated>cd ~/dev/autopilot && bun test src/lib/sentry-resolve.test.ts 2>&1 | grep -Eiq "pass|0 fail" && echo RESOLVE_CALLER_GREEN</automated>
  </verify>
  <done>resolveSentryIfVerifiedStable resolves ONLY on verified+quiet+unfrozen; idempotent; stamps sentry_resolved_at on success; 4th regression freezes the single fingerprint + pages a solution-shaped digest; verifyDeploySha reused verbatim; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the gate into the executeApproval tail (sentry source only)</name>
  <files>autopilot/src/lib/approval.ts, autopilot/src/lib/approval.test.ts</files>
  <behavior>
    - For a sentry-source approved ticket, after status→resolved, resolveSentryIfVerifiedStable is invoked with the merged SHA + fingerprint
    - For a non-sentry ticket, executeApproval behavior is byte-unchanged (no resolve call)
    - executeApproval still returns its existing outcome shape; the resolve hook is additive and never blocks the merge result
  </behavior>
  <action>
Per PATTERNS "The full gate sequence to mirror" (approval.ts:752-765). EXTEND the executeApproval tail: AFTER `db.from("tickets").update({ status: "resolved" })`, for the sentry source ONLY, call `resolveSentryIfVerifiedStable(...)` passing `db`, the ticket id, the fingerprint (`sentry:<issue_id>` from the ticket — fetch via the existing ticket lookup or thread it through), the `mergedSha`, and the deploy timestamp. Do NOT block or change the merge outcome — the resolve hook is additive (log-don't-throw). Determine sentry source from the approval/ticket (reuse `fetchTicketForCategory`/the existing ticket fetch already in approval.ts rather than adding a new query if one is available). Non-sentry approvals are byte-unchanged.

Extend approval.test.ts: assert that a sentry approval triggers the resolve hook (stub `resolveSentryIfVerifiedStable`) and a non-sentry approval does not, and that the executeApproval return shape is unchanged in both cases.
  </action>
  <verify>
    <automated>cd ~/dev/autopilot && bun test src/lib/approval.test.ts 2>&1 | grep -Eiq "pass|0 fail" && echo APPROVAL_WIRED_GREEN</automated>
  </verify>
  <done>executeApproval calls the resolve gate for sentry tickets only, after status→resolved; non-sentry path unchanged; return shape preserved; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon → sentry-resolve Edge Function | New, no-precedent invoke; must carry service-role auth the function's caller-authz accepts. |
| fix verdict → live Sentry resolve | The outward write must fire only on a genuinely verified-stable deploy; spoofed/early resolve is the core risk. |
| regression loop → live org | Repeated resolve/regress against the real org is self-inflicted DoS without the cap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Denial of Service | resolve oscillation loop | mitigate | Per-fingerprint cap ≤3 via record_fingerprint_fix_attempt; 4th regression freezes the single fingerprint (never global) + pages a tier-2 digest; verified-stable + quiet-window precondition + idempotency bound the PUTs. |
| T-21-08 | Spoofing | early/unverified resolve | mitigate | verifyDeploySha (reused verbatim, reads prod-baked SHA) gates the resolve; 30-min quiet window adds margin; resolve-on-merge is explicitly forbidden. NO-ANALOG invoke seam confirmed via blocking checkpoint (Task 1). |
| T-21-03 | Information Disclosure | Sentry token | mitigate | The daemon never holds the token (the Edge Function does, D-03); the daemon caller never logs it. |
| T-21-SC | Tampering | new npm package | mitigate | No `bun add`; reuse verifyDeploySha/tier2/db helpers — assert `git diff package.json` empty. |
</threat_model>

<verification>
- `bun test src/lib/sentry-resolve.test.ts` + `bun test src/lib/approval.test.ts` green.
- Invoke-seam choice recorded (Task 1 checkpoint) and A4 confirmed.
- `git -C ~/dev/autopilot diff package.json` empty.
- Manual: confirm no resolve path exists that bypasses verifyDeploySha + quiet window + cap.
</verification>

<success_criteria>
- Resolve fires ONLY on verified-stable + quiet-window-elapsed + unfrozen (D-03/D-04); never resolve-on-merge.
- 4th regression freezes the single fingerprint/category + pages a solution-shaped digest (not a raw dump).
- Successful resolve stamps sentry_resolved_at; verifyDeploySha + tier2 digest + paging reused, not re-implemented.
- The no-precedent daemon→Edge-Function invoke seam is confirmed before shipping.
</success_criteria>

<output>
Create `.planning/phases/21-sentry-debug-fix-resolve/21-06-SUMMARY.md` when done.
</output>
