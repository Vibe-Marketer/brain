---
phase: 21-sentry-debug-fix-resolve
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/functions/sentry-resolve/index.ts
  - supabase/functions/sentry-resolve/lib.ts
  - supabase/functions/sentry-resolve/__tests__/sentry-resolve.deno.test.ts
  - supabase/functions/sentry-resolve/__tests__/sentry-resolve.integration.test.ts
autonomous: false
requirements: [SEN-05]
user_setup:
  - service: sentry
    why: "The sentry-resolve function makes the outbound resolve PUT to the live ai-simple org; it needs the token + org as DEPLOYED function secrets (not just .env)"
    env_vars:
      - name: SENTRY_AUTH_TOKEN
        source: "Already in .env (scope event:write); must ALSO be set as a Supabase Edge Function secret"
      - name: SENTRY_ORG
        source: "Already in .env (slug 'ai-simple'); must ALSO be set as a Supabase Edge Function secret"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Must be available to the Edge Function env for constant-time daemon bearer comparison; set as a secret if Supabase does not provide it automatically"
    dashboard_config:
      - task: "Set function secrets: supabase secrets set SENTRY_AUTH_TOKEN=... SENTRY_ORG=... and SUPABASE_SERVICE_ROLE_KEY=... if needed"
        location: "Supabase project (CLI)"

must_haves:
  truths:
    - "sentry-resolve builds the correct endpoint PUT /api/0/organizations/{org}/issues/{issue_id}/ with body {\"status\":\"resolved\"}"
    - "issue_id is validated (numeric, bounded) and the sentry: prefix is stripped before URL interpolation"
    - "The caller is authorized by the daemon service-role key only; no-auth returns 401, normal user/admin JWT returns 403, service-role daemon returns 200"
    - "sentry-resolve is NOT configured with verify_jwt=false; gateway JWT verification remains enabled"
    - "The token is never logged or echoed; missing config fails closed with 503"
    - "Resolving an already-resolved issue is a no-op 200 (idempotent)"
  artifacts:
    - path: "supabase/functions/sentry-resolve/lib.ts"
      provides: "Pure resolve-payload + endpoint builder + zod input schema (testable without network)"
      exports: ["resolveInputSchema", "buildResolveUrl", "stripFingerprintPrefix", "RESOLVE_BODY"]
    - path: "supabase/functions/sentry-resolve/index.ts"
      provides: "Handler: service-role daemon authz → validate → outbound resolve PUT → idempotent result"
      contains: "Deno.serve"
    - path: "supabase/config.toml"
      provides: "Function config proving sentry-resolve does not opt out of JWT verification"
      contains: "functions.sentry-resolve"
    - path: "supabase/functions/sentry-resolve/__tests__/sentry-resolve.deno.test.ts"
      provides: "Unit coverage pinning the pure-logic contracts"
      contains: "buildResolveUrl"
  key_links:
    - from: "supabase/functions/sentry-resolve/index.ts"
      to: "https://sentry.io/api/0/organizations"
      via: "raw fetch PUT (no @sentry/* SDK)"
      pattern: "organizations.*issues"
    - from: "supabase/functions/sentry-resolve/index.ts"
      to: "supabase/functions/sentry-resolve/lib.ts"
      via: "import buildResolveUrl + resolveInputSchema"
      pattern: "from \"./lib"
---

<objective>
Build the NEW `sentry-resolve` Edge Function (SEN-05): the only new secret holder. It accepts a validated `issue_id` from the service-role daemon caller and makes ONE outbound resolve PUT to the live `ai-simple` Sentry org. Caller auth is pinned to ONE scheme: Supabase gateway JWT verification stays enabled, and the function accepts only the daemon's service-role key using constant-time comparison against the function env. Per D-04 the BUILD is safe to ship — it only ever fires when the daemon-side gate (Plan 06: verifyDeploySha + quiet window + cap) calls it. This plan ships the function + tests + secrets; Plan 06 ships the daemon caller and the real-condition gate.

Purpose: marking a real issue resolved on the live org is outward-facing/irreversible-ish (D-04), so the function must be gated (daemon-only service-role authz), idempotent, fail-closed on missing config, and path-injection-proof. Reuse the inbound `sentry-webhook` structure but flip the auth posture: webhook is Internet→us (HMAC, `verify_jwt=false`); this is daemon→us (gateway JWT verification enabled plus service-role-only authorization).
Output: index.ts + lib.ts + Deno unit test + mocked integration test + deployed function secrets.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-sentry-debug-fix-resolve/21-RESEARCH.md
@.planning/phases/21-sentry-debug-fix-resolve/21-PATTERNS.md
@supabase/CLAUDE.md
@supabase/functions/sentry-webhook/index.ts
@supabase/functions/sentry-webhook/lib.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure resolve logic (lib.ts) + Deno unit test (RED first)</name>
  <files>supabase/functions/sentry-resolve/lib.ts, supabase/functions/sentry-resolve/__tests__/sentry-resolve.deno.test.ts</files>
  <behavior>
    - resolveInputSchema rejects non-numeric issue_id, empty string, and >256 chars; accepts "12345" and "sentry:12345"
    - stripFingerprintPrefix("sentry:12345") === "12345"; stripFingerprintPrefix("12345") === "12345"
    - buildResolveUrl("ai-simple","sentry:12345") === "https://sentry.io/api/0/organizations/ai-simple/issues/12345/"
    - RESOLVE_BODY deep-equals { status: "resolved" }
  </behavior>
  <action>
Per RESEARCH Pattern 4 + PATTERNS lib.ts section. Mirror the EXACT pure-logic split of `supabase/functions/sentry-webhook/lib.ts` (testable functions, contracts pinned by tests). Write the Deno test FIRST (RED), then lib.ts (GREEN). Export:
- `resolveInputSchema` — `z.object({ issue_id: z.string().trim().min(1).max(256).regex(/^(sentry:)?\d+$/, "issue_id must be numeric") })`, using the EXACT pin `https://esm.sh/zod@3.23.8` (supabase/CLAUDE.md V5; webhook lib uses the same pin). This is the V5 path-injection guard (T-21-01).
- `stripFingerprintPrefix(issueId: string): string` → `issueId.replace(/^sentry:/, "")` (RESEARCH Pitfall 5).
- `buildResolveUrl(org: string, issueId: string): string` → the organization-issues endpoint with the stripped id.
- `RESOLVE_BODY = { status: "resolved" } as const`.
Deno test: `import { assertEquals, assertThrows } from "https://deno.land/std@0.224.0/assert/mod.ts";` mirroring `sentry-webhook.deno.test.ts`. Pin all four behaviors above. Run header comment: `// Run: deno test supabase/functions/sentry-resolve/__tests__/`.
  </action>
  <verify>
    <automated>set -euo pipefail; cd ~/dev/brain && deno test supabase/functions/sentry-resolve/__tests__/sentry-resolve.deno.test.ts</automated>
  </verify>
  <done>lib.ts exports the four contracts; Deno unit test passes pinning zod rejection, prefix strip, URL build, and resolve body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handler (index.ts) — caller authz, fail-closed config, idempotent resolve PUT</name>
  <files>supabase/functions/sentry-resolve/index.ts</files>
  <behavior>
    - OPTIONS → CORS preflight 200; non-POST → 405
    - No Authorization header → 401, no outbound PUT
    - Normal user/admin JWT that is not the service-role daemon key → 403, no outbound PUT
    - Service-role daemon key → 200 on mocked Sentry success
    - `supabase/config.toml` does not disable JWT verification for sentry-resolve (`verify_jwt=false` is forbidden)
    - Missing SENTRY_ORG or SENTRY_AUTH_TOKEN → 503 "resolve not configured" (value never echoed)
    - Valid call → outbound PUT to the built URL with Bearer token; already-resolved 200 treated as success; 4xx → error result; token never appears in any response/log
  </behavior>
  <action>
Per RESEARCH Pattern 4 + PATTERNS index.ts section + supabase/CLAUDE.md index.ts template. Copy the webhook's `json()` helper, OPTIONS preflight, method gate, and generic catch (never echo token/digest). KEY DIFFERENCE from the webhook (PATTERNS): this is the daemon→us boundary, so AUTHORIZE THE CALLER with the single pinned scheme from this revision: the daemon presents the service-role key, the Supabase gateway verifies it as a JWT, and the function then constant-time compares the bearer token to the function's `SUPABASE_SERVICE_ROLE_KEY` env value. No `authenticateRequest` fallback, no admin-user JWT allowance, and no OR condition. Missing Authorization returns 401. Any valid but non-service-role user/admin JWT returns 403. Service-role daemon auth proceeds. Do NOT copy the webhook's no-auth/`verify_jwt=false` posture (T-21-02); add or verify a `supabase/config.toml` stanza for `sentry-resolve` with `verify_jwt = true` (or omit a false stanza only if the deploy config proves the default is true), and add a grep test that fails if `[functions.sentry-resolve]` is paired with `verify_jwt = false`.

Config presence guard (Pitfall 4, T-21-03): read `SENTRY_ORG` + `SENTRY_AUTH_TOKEN` via `Deno.env.get`; if either is unset return `json({ error: "resolve not configured" }, 503)` — presence-check only, never log or echo the value.

Validate the body with `resolveInputSchema` (from ./lib); on failure return 400 with the zod message (not the raw token). Build the URL with `buildResolveUrl(org, issue_id)` and PUT `RESOLVE_BODY` with `Authorization: Bearer <token>`, `Content-Type: application/json`. Idempotency: an already-resolved issue returns 200 (no-op) — treat 2xx as success; 4xx as a real error (return a 502/500 generic body so the caller can page); 5xx as retryable (surface a distinct status the caller can retry on). Never `console.log` the token; on catch, `console.error("sentry-resolve error:", message)` only. Do NOT install any `@sentry/*` SDK — raw fetch only (RESEARCH Don't Hand-Roll).
  </action>
  <verify>
    <automated>set -euo pipefail; cd ~/dev/brain && grep -q "Deno.serve" supabase/functions/sentry-resolve/index.ts && grep -q "resolveInputSchema" supabase/functions/sentry-resolve/index.ts && grep -q "503" supabase/functions/sentry-resolve/index.ts && ! grep -qi "console.log.*token\|console.log.*SENTRY_AUTH" supabase/functions/sentry-resolve/index.ts && echo HANDLER_OK</automated>
  </verify>
  <done>Handler authorizes the caller, fails closed (503) on missing config, validates input, makes the idempotent resolve PUT via raw fetch, and never logs/echoes the token.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test with MOCKED Sentry endpoint (never the live org)</name>
  <files>supabase/functions/sentry-resolve/__tests__/sentry-resolve.integration.test.ts, supabase/config.toml</files>
  <behavior>
    - no-auth request returns 401 and does not call mocked Sentry
    - normal user/admin JWT request returns 403 and does not call mocked Sentry
    - service-role daemon request returns 200 on mocked Sentry success
    - With a mocked Sentry endpoint returning 200, a valid resolve call succeeds and PUTs the right URL/body
    - An already-resolved (mock 200 no-op) call is also success (idempotent)
    - A mock 403 surfaces as an error result (caller would page); the token never appears in the captured request log assertion
  </behavior>
  <action>
Per RESEARCH Sampling Rate (CRITICAL) + PATTERNS test section. Mirror the structure of `supabase/functions/sentry-webhook/__tests__/sentry-webhook.integration.test.ts`: vitest, `describe.skipIf` on `*_TEST_*` env only for real Supabase env needed to mint JWTs, idempotent `afterAll`. **MOCK the Sentry endpoint — NEVER fire a real resolve PUT against `ai-simple`** (stub global `fetch` to capture the outbound request and return canned 200/403). Assert the auth matrix explicitly: no Authorization → 401, normal user/admin JWT → 403, service-role daemon bearer → 200. Assert unauthorized cases do not invoke mocked Sentry. Assert the captured URL equals the org-issues endpoint, the body is `{"status":"resolved"}`, the Authorization header carries a Bearer token to Sentry, and that no assertion or log leaks either the Sentry token or service-role key. Follow supabase/CLAUDE.md integration-safety (no prod fallback; this test does not mutate prod rows because Sentry is mocked). Add a config assertion that `sentry-resolve` is never deployed with `verify_jwt=false`.
  </action>
  <verify>
    <automated>set -euo pipefail; cd ~/dev/brain && npm run test:integration -- sentry-resolve</automated>
  </verify>
  <done>Integration test exists, mocks Sentry (no live PUT), pins no-auth/user-JWT/service-role auth behavior, outbound URL/body/auth, and verifies idempotency + error surfacing without leaking tokens. Config proves `verify_jwt=false` is not used for sentry-resolve.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Deploy function + set function secrets (token + org)</name>
  <what-built>The sentry-resolve Edge Function deployed via --use-api, with SENTRY_AUTH_TOKEN + SENTRY_ORG set as DEPLOYED function secrets (Pitfall 4: .env ≠ function secret).</what-built>
  <how-to-verify>
1. Confirm config before deploy: `set -euo pipefail; cd ~/dev/brain && ! awk '/^\[functions\.sentry-resolve\]/{flag=1; next} /^\[functions\./{flag=0} flag && /verify_jwt *= *false/{bad=1} END{exit bad ? 1 : 0}' supabase/config.toml`
2. Deploy: `set -euo pipefail; cd ~/dev/brain && supabase functions deploy sentry-resolve --use-api` (Docker is not running; --use-api is mandatory per supabase/CLAUDE.md).
3. Set secrets: `supabase secrets set SENTRY_AUTH_TOKEN=<from .env> SENTRY_ORG=<from .env>` (do NOT print the token). Confirm `SUPABASE_SERVICE_ROLE_KEY` is available to the function env for the daemon comparison; if not inherited by Supabase, set it as a function secret without printing the value.
4. Confirm names present: `supabase secrets list` shows `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, and any explicitly set service-role-key secret name.
5. Confirm token scope is `event:write` (A3) — check in the Sentry org Auth Tokens UI; a read-only token will 403 on resolve.
The function is safe to deploy now (D-04): it cannot resolve anything until Plan 06's gate calls it on a verified-stable deploy.
  </how-to-verify>
  <resume-signal>Type "deployed" once the function is deployed and both secrets are listed (and token scope confirmed event:write), or describe the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon (service-role) → sentry-resolve | The only intended caller; must be authorized (not a public endpoint). |
| sentry-resolve → live Sentry org | Outbound write to the real ai-simple org; irreversible-ish (D-04). |
| function env → response/logs | The Sentry token must never cross into a response body or log line. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | issue_id → Sentry URL | mitigate | zod-validate issue_id to numeric (max 256), strip `sentry:` prefix before interpolation — prevents path injection into the Sentry URL. |
| T-21-02 | Elevation of Privilege | sentry-resolve handler | mitigate | Gateway JWT verification remains enabled; function constant-time compares bearer token to service-role env. No-auth → 401; user/admin JWT → 403; service-role daemon → allowed. |
| T-21-03 | Information Disclosure | SENTRY_AUTH_TOKEN | mitigate | Presence-check only (`!!Deno.env.get`); never log/echo; 503 fail-closed when unset; generic error bodies. |
| T-21-SC | Tampering | npm/deno installs | mitigate | No new packages — raw fetch only, zod/supabase-js already pinned via esm.sh. slopcheck N/A (zero installs). |
</threat_model>

<verification>
- Deno unit test green (pure logic contracts).
- Integration test green with MOCKED Sentry (no live PUT) once auth env is present; a skip is not a pass for deploy/auth gates.
- Handler grep: authorizes caller, 503 on missing config, no token logging.
- Function deployed `--use-api`; `supabase secrets list` shows required secret names; config does not disable JWT verification for sentry-resolve.
</verification>

<success_criteria>
- sentry-resolve builds the correct org-issues endpoint + resolve body, with issue_id validated and prefix stripped.
- Caller authorized by service-role daemon key only; no-auth rejected 401; normal user/admin JWT rejected 403; token never logged/echoed; 503 fail-closed.
- Idempotent (already-resolved = success); 4xx surfaces as error for the caller to page.
- Function deployed with secrets set (build safe per D-04; no resolve until Plan 06 gate).
</success_criteria>

<output>
Create `.planning/phases/21-sentry-debug-fix-resolve/21-02-SUMMARY.md` when done.
</output>
