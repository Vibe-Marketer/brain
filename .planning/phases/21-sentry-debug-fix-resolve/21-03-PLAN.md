---
phase: 21-sentry-debug-fix-resolve
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/sentry-autofix.yml
autonomous: true
requirements: [SEN-05]

must_haves:
  truths:
    - "A Sentry issue can no longer be worked as a GitHub PR AND a DB ticket simultaneously"
    - "The legacy Sentry→GitHub-issue→@claude workflow no longer fires on new issues"
    - "The workflow's history/intent is preserved (neutralized, not deleted)"
  artifacts:
    - path: ".github/workflows/sentry-autofix.yml"
      provides: "Neutralized legacy autofix workflow with a pointer to the Phase 21 DB-ticket path"
      contains: "if: false"
  key_links:
    - from: ".github/workflows/sentry-autofix.yml"
      to: "the DB-ticket → fix-loop → verified-resolve path"
      via: "disabled trigger + comment pointing to Phase 21"
      pattern: "if: false"
---

<objective>
Disable the legacy GitHub-issue Sentry autofix path (D-06) so Sentry errors flow ONLY through the new DB-ticket → fix-loop → verified-resolve pipeline. No double-handling: a single Sentry issue must never be worked as both a GitHub PR (legacy `sentry-autofix.yml`) and a DB ticket (the daemon loop).

Purpose: the daemon loop (Phases 17–21) functionally supersedes the parallel Sentry→GitHub→@claude mechanism. The workflow's own comment already anticipated "auto-resolved once deployed" — which is exactly what SEN-05 builds. Leaving both live would race two fixers on one issue.
Output: the workflow neutralized in place (history preserved), with a comment pointing to the Phase 21 path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-sentry-debug-fix-resolve/21-CONTEXT.md
@.planning/phases/21-sentry-debug-fix-resolve/21-PATTERNS.md
@.github/workflows/sentry-autofix.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Neutralize sentry-autofix.yml (D-06)</name>
  <files>.github/workflows/sentry-autofix.yml</files>
  <action>
Per D-06 (LOCKED — Andrew 2026-06-13) and PATTERNS "Disable cleanly". Neutralize the trigger rather than deleting the file (preserve history/intent for the migration record). Add `if: false` to the `claude-autofix` job's existing `if:` condition (or replace it with `if: false`) so the job never runs, AND add a comment block at the top of the file stating: this path is superseded by Phase 21 — Sentry errors now flow through the DB-ticket → autopilot fix-loop → SHA-verified `sentry-resolve` path; re-enabling would cause double-handling (one Sentry issue worked as both a GitHub PR and a DB ticket). Do NOT delete the steps — only neutralize the trigger so intent is recoverable. (PATTERNS offers `workflow_dispatch`-only as an alternative; use `if: false` — it is the most unambiguous full stop.)
  </action>
  <verify>
    <automated>set -euo pipefail; cd ~/dev/brain && grep -q 'if: false' .github/workflows/sentry-autofix.yml && grep -qi "Phase 21" .github/workflows/sentry-autofix.yml && echo WORKFLOW_DISABLED</automated>
  </verify>
  <done>The `claude-autofix` job carries `if: false` (never fires) and a top-of-file comment points to the Phase 21 DB-ticket path. Steps preserved (not deleted).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sentry → GitHub Actions | The legacy trigger that creates a parallel fixer; neutralized here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Tampering | sentry-autofix.yml | mitigate | `if: false` neutralizes the job so a Sentry issue cannot be double-handled (GitHub PR + DB ticket); intent preserved via comment, not deletion. |
| T-21-SC | Tampering | CI config change | accept | No package installs; pure workflow gate edit — no supply-chain surface. |
</threat_model>

<verification>
- `grep` confirms `if: false` present and a Phase 21 pointer comment exists.
- The workflow file is neutralized in place (steps not deleted).
</verification>

<success_criteria>
- The legacy Sentry→GitHub-issue autofix job never fires (`if: false`).
- A comment documents why and points to the Phase 21 DB-ticket path.
- No double-handling possible.
</success_criteria>

<output>
Create `.planning/phases/21-sentry-debug-fix-resolve/21-03-SUMMARY.md` when done.
</output>
