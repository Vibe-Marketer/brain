---
phase: 30-schema-reconciliation-event-model-foundation
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - src/test/event-schema-noop.integration.test.ts
  - src/test/rls-regression.test.ts
autonomous: true
requirements: [EVT-03, EVT-04, EVT-07, SAFE-05]

must_haves:
  truths:
    - "get_workspace_recordings, global_search, MCP search_calls, and MCP ask_call return byte-identical output while event_id is NULL across the board — proven by test"
    - "A participant (via call_participants.email) or owner (via recordings.owner_user_id) reads exactly their event; an unrelated org reads ZERO rows for it"
    - "events cross-org isolation is proven by a bespoke test block (not a CROSS_ORG_TABLES array entry); call_participants is confirmed already registered in CROSS_ORG_TABLES"
    - "No SELECT * on recordings/call_participants exists in the four EVT-03 read paths (verified), so the additive columns are structurally invisible to consumers"
  artifacts:
    - path: "src/test/event-schema-noop.integration.test.ts"
      provides: "EVT-03 byte-identical regression across the four read paths"
      min_lines: 60
    - path: "src/test/rls-regression.test.ts"
      provides: "Bespoke events participation/ownership isolation block (positive + negative)"
      contains: "events"
  key_links:
    - from: "bespoke events isolation test"
      to: "events RLS policy"
      via: "two signed-in JWT clients (owner/participant reads 1, foreign org reads 0)"
      pattern: "event_id"
    - from: "byte-identical test"
      to: "get_workspace_recordings / global_search / search_calls / ask_call"
      via: "call each with event_id NULL, assert output has no event_id/role/has_confirmed_speech keys"
      pattern: "get_workspace_recordings|global_search"
---

<objective>
Prove the two guarantees the migration must satisfy — and prove them by test, because both are literal "proven by test" requirements: (1) EVT-03, that the four read paths are byte-identical while `event_id` is NULL; (2) EVT-04 + SAFE-05, that `events` is readable only through participation or ownership and never cross-org. These are the expansion tests on top of Plan 02's tracer migration.

Both tests run against the TEST project (which received the migration in Plan 02). Prod apply is Plan 04. If the TEST project env is unavailable to the executor, the integration tests skip cleanly (`describe.skipIf`) — but SAFE-05/EVT-03 cannot be marked proven until they run green, so surface an unavailable TEST project rather than declaring done.

Purpose: A false merge or a mis-scoped RLS policy is a data-exposure incident. These tests are the gate that catches both before prod apply.
Output: One new integration test file; one new bespoke block inside the existing RLS regression suite.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-schema-reconciliation-event-model-foundation/30-RESEARCH.md
@.planning/phases/30-schema-reconciliation-event-model-foundation/30-VALIDATION.md
@supabase/migrations/20260831000001_create_events_and_extend_participants.sql

Read-path sources to snapshot (EVT-03):
@supabase/migrations/20260618160000_recordings_canonical_ai_title.sql
@supabase/functions/mcp-server/tools/read/search_calls.ts
@supabase/functions/mcp-server/tools/ai/ask_call.ts

<interfaces>
<!-- Existing RLS test harness anchors (src/test/rls-regression.test.ts) — extend, don't rebuild -->
line 40:  const CROSS_ORG_TABLES: ReadonlyArray<{ table; filterColumn: <closed 8-name union> }>
line 61:  { table: "call_participants", filterColumn: "recording_id" }   // ALREADY REGISTERED — SAFE-05 call_participants half is DONE
line 87:  const CLIENT_DENY_TABLES: ReadonlyArray<string>                 // precedent for a bespoke block with its own loop
line 806: for (const { table, filterColumn } of CROSS_ORG_TABLES) { ... assert data.length === 0 }
line 888: for (const table of CLIENT_DENY_TABLES) { ... }

<!-- Why events cannot join CROSS_ORG_TABLES: the filterColumn union is
     organization_id|org_id|user_id|recording_id|workspace_id|folder_id|reporter_id|ticket_id.
     events has NONE of these by design (EVT-04). It needs a hand-written block. -->

<!-- TEST project env (distinct from prod ref vltmrnjsubfzrgrtdqey) -->
VITE_SUPABASE_TEST_URL, SUPABASE_TEST_SERVICE_ROLE_KEY, VITE_SUPABASE_TEST_ANON_KEY
Run: npx vitest run src/test/rls-regression.test.ts --reporter=verbose
Integration glob: npm run test:integration  (matches src/**/*.integration.test.ts)
</interfaces>

Decision baked into this plan (research Open Question #1 / Assumption A2): "chat" in EVT-03 maps to the MCP `ask_call` tool. No dedicated chat edge function or frontend chat UI exists in this repo (79 edge functions, none named chat*; the `chat_message` AI-action-type in useAiGate.ts has zero call sites). Covering `ask_call` is strictly more coverage, not less, so this is low-risk. If a real chat surface is later found, extend the test to it — note this in the SUMMARY.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the EVT-03 byte-identical regression test (Wave 0 scaffold + implementation)</name>
  <files>src/test/event-schema-noop.integration.test.ts</files>
  <behavior>
    - get_workspace_recordings(workspace with recordings whose event_id IS NULL) returns rows whose JSON keys do NOT include event_id/role/has_confirmed_speech, and whose values match a captured baseline shape (18 named columns).
    - global_search over the same fixture returns the same jsonb_build_object shape as before the migration (no new keys).
    - MCP search_calls (workspace-scope via global_search; org-scope via workspace_entries join) returns unchanged shape.
    - MCP ask_call ("chat" per A2) selects only id/title/full_transcript through the workspace_entries membership check — output unchanged.
    - Every recording in the fixture has event_id IS NULL (the only state in Phase 30) — the test asserts the additive columns are structurally invisible.
  </behavior>
  <read_first>
    - src/test/rls-regression.test.ts (reuse its fixture style: service-role setup client + signed-in JWT clients, describe.skipIf for TEST-env availability — do NOT invent a new harness)
    - supabase/migrations/20260618160000_recordings_canonical_ai_title.sql (get_workspace_recordings' exact 18-column RETURNS TABLE list — the expected shape)
    - supabase/functions/mcp-server/tools/read/search_calls.ts and supabase/functions/mcp-server/tools/ai/ask_call.ts (the MCP paths' exact .select() column lists)
    - .planning/phases/30-schema-reconciliation-event-model-foundation/30-RESEARCH.md ("EVT-03 table", "Common Pitfalls 3")
  </read_first>
  <action>
    First, run the exhaustive check Pitfall 3 requires: grep across src/ and supabase/functions/ for select('*') / select("*") on files that also reference recordings or call_participants. If any of the four EVT-03 paths (or a shared helper they use) does a select('*'), note it — the byte-identical assertion must then also assert no added key leaks through. Then create `src/test/event-schema-noop.integration.test.ts` (name matches the test:integration glob) using the rls-regression harness's fixture style: guard the whole describe with skipIf on TEST-project env availability. Seed (service-role) one workspace with >=2 recordings whose event_id IS NULL, plus workspace_entries linking them, plus a couple of call_participants rows (role/has_confirmed_speech NULL). For each of the four paths, call it and assert the response shape/values match a baseline that does NOT contain event_id/role/has_confirmed_speech keys: get_workspace_recordings RPC (assert the 18-column shape, no event_id key), global_search RPC (assert jsonb shape unchanged), the search_calls tool's query path (workspace-scope delegates to global_search; org-scope uses the recordings!inner(id,title,recording_start_time,summary) join through workspace_entries), and ask_call's recordings select (id,title,full_transcript through the membership check). The assertions are on exact key sets and values — an added key is the failure mode Pitfall 3 describes. Do NOT modify any of the four read-path bodies (EVT-03 is proof-of-no-change, not a change). Run the file against the TEST project.
  </action>
  <acceptance_criteria>
    - `src/test/event-schema-noop.integration.test.ts` exists, is >= 60 lines, and asserts, for each of the four paths, that the returned JSON key set excludes event_id, role, and has_confirmed_speech.
    - The test seeds recordings with event_id IS NULL and passes (or skips cleanly with a logged reason if TEST env is unavailable).
    - No read-path source file (get_workspace_recordings migration, global_search migration, search_calls.ts, ask_call.ts) was modified.
    - The Pitfall-3 select('*') sweep result is recorded in the SUMMARY (found none, or the specific sites found).
    <automated>npx vitest run src/test/event-schema-noop.integration.test.ts --reporter=verbose</automated>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/test/event-schema-noop.integration.test.ts --reporter=verbose</automated>
  </verify>
  <done>EVT-03 is proven by test: the four read paths are byte-identical while event_id is NULL, and the additive columns are structurally invisible to every consumer.</done>
</task>

<task type="auto">
  <name>Task 2: Add the bespoke events cross-org isolation block to rls-regression.test.ts (EVT-04 + SAFE-05)</name>
  <files>src/test/rls-regression.test.ts</files>
  <read_first>
    - src/test/rls-regression.test.ts (full — the CROSS_ORG_TABLES loop at 806, the CLIENT_DENY_TABLES bespoke-block precedent at 87/888, and the two-JWT-client fixture setup; confirm call_participants is already at line 61)
    - supabase/migrations/20260831000001_create_events_and_extend_participants.sql (the events RLS policy this block exercises)
    - .planning/phases/30-schema-reconciliation-event-model-foundation/30-RESEARCH.md ("SAFE-05 — why events cannot just be added to CROSS_ORG_TABLES", "Common Pitfall 4")
  </read_first>
  <action>
    Add a NEW hand-written test block to `src/test/rls-regression.test.ts` for `events` — do NOT add events to the CROSS_ORG_TABLES array (its filterColumn union has no member that applies to events; EVT-04 forbids organization_id). Follow the CLIENT_DENY_TABLES precedent of a separate block with its own assertions, but with BOTH a positive and a negative path (a leak-only test would pass for the wrong reason if a mis-scoped policy denied everyone). Using the existing service-role setup + two signed-in JWT clients (clientA = Org A user who owns recordingA, clientB = unrelated Org B user): insert an events row (service-role); UPDATE recordings SET event_id = <event> WHERE id = recordingAId; ensure a call_participants row for recordingAId has email = userAEmail and event_id = <event>. Then assert clientB reads ZERO rows from events for that id (negative/isolation), AND clientA reads EXACTLY ONE row (positive/participation-or-ownership works). Also add an inline comment (or keep the existing one) noting that call_participants' SAFE-05 half is already covered by the CROSS_ORG_TABLES entry at line 61 (select('*') there already covers the new columns) — no array change needed. Do NOT weaken any existing assertion.
  </action>
  <acceptance_criteria>
    - `src/test/rls-regression.test.ts` contains a new `events` block that asserts BOTH clientB reads 0 rows AND clientA reads exactly 1 row.
    - `events` is NOT added to the CROSS_ORG_TABLES array (the block is bespoke).
    - The existing `call_participants` entry (line ~61) is untouched and still present; a comment records that it already satisfies SAFE-05's call_participants half.
    - The full RLS regression suite passes against the TEST project (or skips cleanly with a logged reason if TEST env is unavailable).
    <automated>npx vitest run src/test/rls-regression.test.ts --reporter=verbose</automated>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/test/rls-regression.test.ts --reporter=verbose</automated>
  </verify>
  <done>SAFE-05 (events + call_participants) and EVT-04 are proven by test: participation/ownership grants read, unrelated org is denied, and the positive path guards against a deny-everyone false pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test JWT (Org A) -> events | Must read exactly the event it participates in / owns a capture of |
| test JWT (Org B) -> events | Unrelated org — must read ZERO rows |
| additive columns -> existing consumers | New nullable columns must not surface in any read-path output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | Information Disclosure | events RLS test (false-pass risk) | mitigate | The bespoke block asserts BOTH the negative (Org B reads 0) AND the positive (Org A reads exactly 1). A leak-only test would pass for the wrong reason against a deny-everyone policy — the positive assertion closes that. HIGH severity (this is the actual proof of EVT-04). |
| T-30-03-02 | Elevation of Privilege | byte-identical proof (missed audience-widening) | mitigate | Run the exhaustive select('*') sweep (Pitfall 3) before finalizing; assert exact key sets so an added event_id/role/has_confirmed_speech key fails the test. Confirms the additive columns did not widen any read path's output. |
| T-30-03-03 | Repudiation | TEST project currency | mitigate | If the TEST project lags migrations or its env is unavailable, tests skip rather than falsely pass — surface it (research Open Question #2) so "proven by test" is not claimed on a skip. |
| T-30-03-SC | Tampering | package installs | accept | No package installs this phase. |
</threat_model>

## Artifacts this phase produces (Plan 03)

- **New test file:** `src/test/event-schema-noop.integration.test.ts` (EVT-03 byte-identical proof, four read paths)
- **Extended test file:** `src/test/rls-regression.test.ts` (new bespoke `events` participation/ownership isolation block)
- **No schema symbols, no RPCs, no migration** — this plan only adds tests.
- **Confirmed (not created):** `call_participants` is already registered in `CROSS_ORG_TABLES` (line ~61) — SAFE-05's call_participants half needs no new code.

<verification>
- `npx vitest run src/test/event-schema-noop.integration.test.ts` passes (or clean skip w/ logged reason).
- `npx vitest run src/test/rls-regression.test.ts` passes, including the new events block (positive + negative).
- events is proven isolated by a bespoke block, not a CROSS_ORG_TABLES entry.
- The four read paths' bodies are unmodified.
- Pitfall-3 select('*') sweep result recorded.
</verification>

<success_criteria>
EVT-03, EVT-04, and SAFE-05 are proven by test against the TEST project. Plan 04 may now gate the prod apply on this green evidence.
</success_criteria>

<output>
Create `.planning/phases/30-schema-reconciliation-event-model-foundation/30-03-SUMMARY.md` when done. Record the select('*') sweep result and whether the tests ran or skipped.
</output>
