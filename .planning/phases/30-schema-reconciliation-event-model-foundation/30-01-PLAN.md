---
phase: 30-schema-reconciliation-event-model-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/supabase.ts
  - package.json
  - supabase/SCHEMA_TRUTH.md
  - type-baseline.json
autonomous: true
requirements: [SAFE-07]

must_haves:
  truths:
    - "src/types/supabase.ts is regenerated from the live prod database and matches it (no F17 drift remains)"
    - "The banks->organizations migration gap (F16) is documented so nobody tries to replay supabase/migrations/ from an empty database"
    - "npm run type-check passes on the regenerated types (baseline updated deliberately if the corrected types shifted it)"
    - "npm run gen:types no longer silently no-ops"
  artifacts:
    - path: "src/types/supabase.ts"
      provides: "Regenerated Database type matching live prod schema"
      contains: "fathom_calls_orphan_report"
    - path: "supabase/SCHEMA_TRUTH.md"
      provides: "Permanent note: migration folder is a historical log, not a replay script; documents the banks->organizations gap"
      min_lines: 20
    - path: "package.json"
      provides: "Fixed gen:types script that actually regenerates types"
      contains: "supabase gen types typescript --linked"
  key_links:
    - from: "package.json gen:types script"
      to: "supabase gen types typescript --linked"
      via: "npm script"
      pattern: "gen types typescript --linked"
---

<objective>
Make the schema record truthful BEFORE any new migration is authored (SAFE-07). This is the hard precondition for the entire phase and milestone: `supabase/migrations/` cannot reconstruct the live schema (the banks->organizations rename has no migration file), so the committed `src/types/supabase.ts` — regenerated from the live database — is the only source of truth going forward.

This plan changes NO database schema. It regenerates types, documents the reconciliation, and fixes a broken tooling script.

Purpose: Any migration authored against a stale or fictional schema record builds on a fiction. F16/F17 must be resolved and documented first.
Output: Regenerated `src/types/supabase.ts`; a permanent `supabase/SCHEMA_TRUTH.md` note; a fixed `gen:types` script.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-schema-reconciliation-event-model-foundation/30-RESEARCH.md

Branch: `v2.2-event-resolution` (already cut and checked out — do NOT work on `main`).

Key research findings this plan implements (see 30-RESEARCH.md "Architecture Patterns -> F16/F17" and "Common Pitfalls 1 & 2"):
- The committed types file is stale by ~7 migrations (two tables, ~18 columns across recordings/call_participants/sync_jobs/tickets, 3 RPCs). All already-applied, in-sync migrations. Regenerating resolves F17 completely.
- F16 is deeper: `recordings` was created as `bank_id UUID NOT NULL REFERENCES banks(id)` (`20260131000007_create_recordings_tables.sql`) but the live table has `organization_id` and no `bank_id` — and NO migration performs that rename. `supabase db push` from empty would fail at the undocumented banks->organizations transition. This is currently a LATENT risk (no CI job attempts a fresh replay) — document it, do NOT fabricate a synthetic rename migration against prod.
- `npm run gen:types` (package.json line 26) reads `$SUPABASE_DB_URL`, but this repo's convention is `DATABASE_URL`. `test -n "$SUPABASE_DB_URL"` is false -> the `&&` short-circuits -> the file is silently NOT regenerated, no error. Use `supabase gen types typescript --linked` directly instead.

<interfaces>
<!-- gen:types script as it exists today (package.json line 26) — the bug to fix -->
Current (broken): test -n "$SUPABASE_DB_URL" && supabase gen types typescript --db-url "$SUPABASE_DB_URL" > src/types/supabase.ts
Fixed target: supabase gen types typescript --linked > src/types/supabase.ts

<!-- type-check baseline mechanics (scripts/type-check.mjs) -->
BASELINE_PATH = <repo-root>/type-baseline.json  (keys are file|TScode|message-hash; line numbers excluded)
Update command: npm run type-check:update-baseline
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate and commit src/types/supabase.ts from the live prod database</name>
  <files>src/types/supabase.ts, type-baseline.json</files>
  <read_first>
    - src/types/supabase.ts (the stale committed file being replaced)
    - scripts/type-check.mjs (understand the baseline diff mechanism before running type-check)
    - .planning/phases/30-schema-reconciliation-event-model-foundation/30-RESEARCH.md ("Common Pitfalls 1 & 2")
  </read_first>
  <action>
    Confirm the Supabase CLI is linked to prod ref `vltmrnjsubfzrgrtdqey` (the research session confirmed it is; re-verify with `supabase projects list` or `supabase status` if in doubt). Regenerate types with `supabase gen types typescript --linked > src/types/supabase.ts` — do NOT use `npm run gen:types` yet (it silently no-ops, per Pitfall 1). Expect the file to grow (~5912 -> ~6093 lines) and to gain: tables `fathom_calls_orphan_report` and `organization_invitation_workspaces`; columns `recordings.ai_generated_title` and `recordings.ai_title_generated_at`; ~9 tickets columns; ~9 sync_jobs columns; RPCs `get_decrypted_source_credential`, `get_org_call_participant_contacts`, `reap_stale_sync_jobs`. Then run `npm run type-check`. If new (legitimate, non-regressive) error keys appear PURELY because the corrected types added fields (Pitfall 2 — TypeScript structural inference on Pick/Omit/exhaustive switches), read exactly what shifted first, confirm none is a real regression in code this plan did not touch, then run `npm run type-check:update-baseline` deliberately. Do NOT blindly update the baseline. Do NOT touch any code outside `src/types/supabase.ts` and (if needed) `type-baseline.json`.
  </action>
  <acceptance_criteria>
    - `src/types/supabase.ts` contains `fathom_calls_orphan_report` and `organization_invitation_workspaces` table definitions (proof F17 drift is gone) and `recordings.ai_generated_title` (the spec's named F17 column).
    - `npm run type-check` exits 0.
    - No source file other than `src/types/supabase.ts` and (conditionally) `type-baseline.json` is modified.
    <automated>npm run type-check</automated>
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check && grep -q "fathom_calls_orphan_report" src/types/supabase.ts && echo REGEN_OK</automated>
  </verify>
  <done>Regenerated types compile, match live prod schema, and any baseline shift was reviewed and applied deliberately.</done>
</task>

<task type="auto">
  <name>Task 2: Write supabase/SCHEMA_TRUTH.md and fix the gen:types script</name>
  <files>supabase/SCHEMA_TRUTH.md, package.json</files>
  <read_first>
    - supabase/RLS_POLICY_VERIFICATION.md (sibling doc — match its tone/format as the local convention for a supabase/ engineering note)
    - package.json (the scripts block, lines 6-27; the gen:types line is 26)
    - .planning/phases/30-schema-reconciliation-event-model-foundation/30-RESEARCH.md ("Architecture Patterns -> F16", "State of the Art")
  </read_first>
  <action>
    Create `supabase/SCHEMA_TRUTH.md` recording, permanently and concretely: (1) the live database — introspected via `supabase gen types typescript --linked` — is the single source of truth; `supabase/migrations/` is a historical log, NOT a replayable script. (2) The specific gap: `recordings` was created with `bank_id REFERENCES banks(id)` in `20260131000007_create_recordings_tables.sql`, the live table has `organization_id` and no `bank_id`, and no migration performs the banks->organizations rename (contrast the sibling `vaults`->`workspaces` rename which IS captured in `20260301000001_rename_vaults_to_workspaces.sql`). A `supabase db push`/`db reset` from empty will fail at that transition. (3) The correct way to regenerate types going forward: `supabase gen types typescript --linked` (never trust `supabase/migrations/` as a replay source). (4) An explicit instruction NOT to author a synthetic "rename banks to organizations" migration against prod — live data is already correct; a replay-only migration is pure risk. Then fix `package.json`: change the `gen:types` script (line 26) from the `$SUPABASE_DB_URL`-gated form to `supabase gen types typescript --linked > src/types/supabase.ts` so the next engineer's `npm run gen:types` actually regenerates instead of silently no-opping.
  </action>
  <acceptance_criteria>
    - `supabase/SCHEMA_TRUTH.md` exists, is >= 20 lines, and names both `20260131000007_create_recordings_tables.sql` and the banks->organizations gap explicitly.
    - `package.json` gen:types script is `supabase gen types typescript --linked > src/types/supabase.ts` and no longer references `$SUPABASE_DB_URL`.
    - `npm run gen:types` now regenerates the file (a fresh run produces byte-identical output to Task 1's committed file, since prod is unchanged) rather than exiting 0 with no write.
    <automated>grep -q "banks" supabase/SCHEMA_TRUTH.md && grep -q "gen types typescript --linked" package.json && ! grep -q 'SUPABASE_DB_URL' package.json && echo DOC_AND_SCRIPT_OK</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -q "gen types typescript --linked" package.json && ! grep -q "SUPABASE_DB_URL" package.json && test -f supabase/SCHEMA_TRUTH.md && echo OK</automated>
  </verify>
  <done>The F16 gap is permanently documented and the gen:types script regenerates types correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer -> committed artifact | Regenerated types + doc are committed to the branch; the only egress is what lands in git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01-01 | Information Disclosure | `src/types/supabase.ts`, `supabase/SCHEMA_TRUTH.md` | mitigate | The regenerated file is schema shape only (table/column names, types) — it contains no rows, secrets, or connection strings. Do not paste `DATABASE_URL`, service-role keys, or the prod password into `SCHEMA_TRUTH.md` — reference the ref `vltmrnjsubfzrgrtdqey` by name only. `supabase gen types --linked` uses the CLI's own auth, so no secret is echoed to the shell or the file. |
| T-30-01-02 | Tampering | Regenerated schema record | accept | Low severity: types are generated deterministically from live prod; a tampered file is caught by `npm run type-check` and by re-running `supabase gen types --linked`. |
| T-30-01-SC | Tampering | package installs (npm/pip/cargo) | accept | No package installs this phase (30-RESEARCH.md "Package Legitimacy Audit: Not applicable"). No supply-chain surface. |
</threat_model>

## Artifacts this phase produces (Plan 01)

- **Modified file:** `src/types/supabase.ts` (regenerated from live prod; +2 tables, ~18 columns, 3 RPCs vs. committed)
- **New file:** `supabase/SCHEMA_TRUTH.md` (permanent F16 reconciliation note)
- **Modified file:** `package.json` (gen:types script fixed to `--linked`)
- **Conditionally modified:** `type-baseline.json` (only if the corrected types shifted the type-check baseline)
- **No schema symbols** — this plan authors no migration and creates no tables/columns/RPCs.

<verification>
- `npm run type-check` exits 0 after regeneration.
- `src/types/supabase.ts` matches live prod (spot-check the named F17 additions exist).
- `supabase/SCHEMA_TRUTH.md` documents the F16 banks->organizations gap and the "do not replay from empty" rule.
- `package.json` gen:types uses `--linked`; no `SUPABASE_DB_URL` reference remains.
- No new migration file was authored (SAFE-07 is documentation + tooling only).
</verification>

<success_criteria>
SAFE-07 is satisfied: the committed types are regenerated from and reconciled against the live database, F16 and F17 are resolved/documented, and this happened BEFORE any new migration exists. Plan 02 (the migration) may now proceed on a truthful foundation.
</success_criteria>

<output>
Create `.planning/phases/30-schema-reconciliation-event-model-foundation/30-01-SUMMARY.md` when done.
</output>
