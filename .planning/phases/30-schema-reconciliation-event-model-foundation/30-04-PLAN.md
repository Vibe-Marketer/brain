---
phase: 30-schema-reconciliation-event-model-foundation
plan: 04
type: execute
wave: 4
depends_on: ["30-03"]
files_modified:
  - src/types/supabase.ts
  - type-baseline.json
autonomous: false
requirements: [EVT-01, EVT-02, EVT-05, SAFE-07]

must_haves:
  truths:
    - "The additive migration is applied to production (ref vltmrnjsubfzrgrtdqey) only after the Plan 03 tests are green on TEST and a human approved the prod touch"
    - "The events table and recordings.event_id / call_participants.role|event_id|has_confirmed_speech exist in production"
    - "src/types/supabase.ts is regenerated from prod post-migration and now includes event_id, the events table, and the new call_participants columns"
    - "No current behavior changed in production (event_id is NULL across the board; the byte-identical test proved invisibility)"
  artifacts:
    - path: "src/types/supabase.ts"
      provides: "Types re-synced from prod including the new event-model schema"
      contains: "event_id"
  key_links:
    - from: "prod migration apply"
      to: ".env DATABASE_URL prod-ref guard"
      via: "guarded runner verifies vltmrnjsubfzrgrtdqey before connecting"
      pattern: "vltmrnjsubfzrgrtdqey"
---

<objective>
Land the proven migration in production and re-sync the committed types. This is deliberately isolated as its own small, gated plan because it is the one high-stakes action in the phase: additive DDL against a LIVE production database with real customer data, on the `v2.2-event-resolution` branch. It runs only after Plan 03's isolation and byte-identical tests are green on the TEST project, and only after a human approves the prod touch (branch discipline: v2.2 changes touch prod only once proven and Andrew is comfortable).

Purpose: The event-model foundation must exist in prod for Phases 31-39 to build on — but proven-then-applied, never applied-then-hoped.
Output: The migration applied to prod; `src/types/supabase.ts` regenerated from prod (now carrying the event schema).
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/CLAUDE.md
@supabase/migrations/20260831000001_create_events_and_extend_participants.sql
@.planning/phases/30-schema-reconciliation-event-model-foundation/30-03-SUMMARY.md

Guarded apply rule (root CLAUDE.md + supabase/CLAUDE.md, non-negotiable): anything applying a migration or DDL to production reads `DATABASE_URL` from `.env` (the PROD file — NOT `.env.local`/`.env.test`) and MUST verify the URL contains the prod ref `vltmrnjsubfzrgrtdqey` before connecting. The autopilot migration runner (`~/dev/autopilot/src/gsd-runner.ts`) implements exactly this guard. Confusing the env files is the 2026-05 prod-mutation incident class documented in supabase/CLAUDE.md.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve applying the additive migration to production</name>
  <files>none — approval gate; gates the prod apply performed in Task 2</files>
  <read_first>
    - .planning/phases/30-schema-reconciliation-event-model-foundation/30-03-SUMMARY.md (confirm Plan 03's tests ran GREEN, not skipped)
    - supabase/migrations/20260831000001_create_events_and_extend_participants.sql (the exact DDL being applied)
  </read_first>
  <action>Present the what-built and how-to-verify below, then PAUSE for explicit human approval before Task 2 touches prod. Do not proceed if Plan 03's tests were skipped rather than green.</action>
  <what-built>
    The additive, NULL-safe event-model migration (`20260831000001_create_events_and_extend_participants.sql`): the `events` table + participation/ownership RLS, nullable `recordings.event_id`, and `call_participants.event_id/role/has_confirmed_speech`. It is applied and passing on the TEST project. Plan 03 proved, by test on TEST: (a) events cross-org isolation — a participant/owner reads their event, an unrelated org reads zero; (b) byte-identical output from get_workspace_recordings, global_search, MCP search_calls, and MCP ask_call while event_id is NULL. Every column is nullable; nothing populates event_id in Phase 30, so production behavior does not change on apply.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/30-schema-reconciliation-event-model-foundation/30-03-SUMMARY.md` and confirm both tests ran GREEN on the TEST project (not skipped for missing env). If they were skipped, do NOT approve — the "proven by test" bar is not met.
    2. Skim the migration file: confirm it is additive only (CREATE TABLE IF NOT EXISTS / ADD COLUMN IF NOT EXISTS), has no DROP/RENAME, does not touch participant_type or its trigger, and adds no workspace_id.
    3. Confirm you want prod DDL applied now on the `v2.2-event-resolution` branch (this is the one milestone action that touches prod before merge).
  </how-to-verify>
  <acceptance_criteria>
    - Plan 03's SUMMARY confirms green (not skipped) TEST-project runs for both the byte-identical and the events-isolation tests.
    - A human has explicitly approved the prod apply.
  </acceptance_criteria>
  <verify>Human verification gate — no automated check. Task 2 must not run until approval is given.</verify>
  <done>A human confirmed the TEST-green evidence and approved applying the additive migration to production.</done>
  <resume-signal>Type "approved" to apply to prod, or describe what to hold on.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply to prod (guarded) and regenerate the committed types from prod</name>
  <files>src/types/supabase.ts, type-baseline.json</files>
  <read_first>
    - supabase/CLAUDE.md (the guarded apply process and the prod-ref requirement)
    - scripts/type-check.mjs (baseline diff mechanism — same Pitfall 2 handling as Plan 01)
    - supabase/SCHEMA_TRUTH.md (regenerate via --linked, never replay migrations)
  </read_first>
  <action>
    Apply `supabase/migrations/20260831000001_create_events_and_extend_participants.sql` to PRODUCTION via the repo's guarded path: verify `DATABASE_URL` in `.env` contains the prod ref `vltmrnjsubfzrgrtdqey` BEFORE connecting (use the autopilot gsd-runner guarded apply, or an equivalent that performs the same prod-ref check — do NOT apply blind). Confirm the apply succeeded and is idempotent-safe (the IF NOT EXISTS guards mean a re-run is harmless). Then regenerate the committed types from prod with `supabase gen types typescript --linked > src/types/supabase.ts` — the file must now gain the `events` table, `recordings.event_id`, and `call_participants.event_id/role/has_confirmed_speech`. Run `npm run type-check`; if the newly-added schema fields shift the baseline (Pitfall 2), read what shifted, confirm no real regression, then run `npm run type-check:update-baseline` deliberately. Do NOT change any application code — only `src/types/supabase.ts` and (conditionally) `type-baseline.json`.
  </action>
  <acceptance_criteria>
    - The prod-ref guard confirmed `vltmrnjsubfzrgrtdqey` before the apply (record the guard confirmation in the SUMMARY).
    - Post-apply prod introspection shows `events` exists and `recordings.event_id` exists (via the regenerated types).
    - `src/types/supabase.ts` now contains `event_id` and an `events` table Row type; `npm run type-check` exits 0.
    <automated>grep -q "event_id" src/types/supabase.ts && npm run type-check</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -q "event_id" src/types/supabase.ts && npm run type-check && echo PROD_TYPES_OK</automated>
  </verify>
  <done>The migration is live in prod (guarded), the committed types reflect the new schema, and type-check passes. The event-model foundation exists truthfully in production with zero behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor -> production DB | Applying DDL to live prod with real customer data — the single highest-stakes boundary in the phase |
| .env selection -> which project | Wrong env file = wrong database (the 2026-05 incident class) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-01 | Tampering | wrong-database apply | mitigate | Guarded apply reads `DATABASE_URL` from `.env` and verifies prod ref `vltmrnjsubfzrgrtdqey` before connecting (autopilot gsd-runner). NEVER `.env.local`/`.env.test`. HIGH severity — blocks apply if the guard does not confirm the prod ref. |
| T-30-04-02 | Elevation of Privilege / Information Disclosure | applying unproven RLS to prod | mitigate | Apply is gated by Task 1's human checkpoint, which itself requires Plan 03's isolation test to have run GREEN (not skipped) on TEST. An unproven or mis-scoped events RLS never reaches prod. HIGH severity. |
| T-30-04-03 | Denial of Service / behavior change | additive migration in prod | mitigate | All changes are additive + nullable; event_id is NULL across the board post-apply; Plan 03's byte-identical test proved the four read paths are invisible to the new columns. No table rewrite, no NOT NULL backfill, no lock-heavy operation. |
| T-30-04-04 | Repudiation | undocumented prod change | mitigate | The guard confirmation and apply result are recorded in the plan SUMMARY; the migration file is the durable record; types are re-committed from prod. |
| T-30-04-SC | Tampering | package installs | accept | No package installs this phase. |
</threat_model>

## Artifacts this phase produces (Plan 04)

- **Production schema change:** `events` table + `recordings.event_id` + `call_participants.event_id/role/has_confirmed_speech` become live in prod (ref `vltmrnjsubfzrgrtdqey`).
- **Modified file:** `src/types/supabase.ts` (regenerated from prod post-migration — now includes the event schema).
- **Conditionally modified:** `type-baseline.json` (if the new schema fields shifted the baseline).
- **No new migration file** (Plan 02 authored it; this plan applies it) and **no new RPCs**.

<verification>
- Prod-ref guard confirmed `vltmrnjsubfzrgrtdqey` before apply.
- Migration applied to prod cleanly; `events` and `recordings.event_id` exist in prod.
- `src/types/supabase.ts` regenerated from prod contains `event_id` and the events Row type.
- `npm run type-check` exits 0.
- No application code changed.
</verification>

<success_criteria>
The event layer exists in the schema — truthfully, in production — and changed no current behavior. EVT-01/EVT-02/EVT-05 are TRUE in prod; SAFE-07's types are re-synced post-migration; the whole phase goal is met. Phases 31-39 can build on this foundation.
</success_criteria>

<output>
Create `.planning/phases/30-schema-reconciliation-event-model-foundation/30-04-SUMMARY.md` when done. Record the prod-ref guard confirmation and the apply result.
</output>
