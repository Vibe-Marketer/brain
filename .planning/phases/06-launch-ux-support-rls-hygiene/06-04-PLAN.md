---
phase: 06-launch-ux-support-rls-hygiene
plan: 04
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/components/billing/PaywallDialog.tsx
  - src/components/billing/LockedFeatureButton.tsx
  - src/hooks/useRequirePaidPlan.ts
  - src/components/billing/__tests__/paywall-gate.test.tsx
  - src/components/settings/MCPTab.tsx
  - src/components/settings/BillingTab.tsx
autonomous: true
requirements:
  - ONB-03
must_haves:
  truths:
    - "D-22: gated Pro/Team capabilities show visible locked affordances and paywalls on action attempt."
    - "D-23/D-24: successful checkout returns to the same route or surface and makes the gated control usable after subscription refetch."
    - "D-25: do not add a separate post-upgrade success page unless existing Polar mechanics require it."
  artifacts:
    - path: "src/components/billing/PaywallDialog.tsx"
      provides: "inline upgrade dialog for gated actions"
    - path: "src/components/billing/LockedFeatureButton.tsx"
      provides: "reusable locked action wrapper"
---

<objective>
Turn paid-feature redirects into inline paywall gates that preserve route context through Polar checkout.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-launch-ux-support-rls-hygiene/06-CONTEXT.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-RESEARCH.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-UI-SPEC.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-PATTERNS.md
@src/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reusable paywall dialog and locked action wrapper</name>
  <files>src/components/billing/PaywallDialog.tsx, src/components/billing/LockedFeatureButton.tsx</files>
  <read_first>
    - src/components/billing/UpgradeButton.tsx
    - src/hooks/useRequirePaidPlan.ts
    - src/hooks/useSubscription.ts
    - src/components/ui/dialog.tsx
    - src/components/ui/button.tsx
    - .planning/phases/06-launch-ux-support-rls-hygiene/06-UI-SPEC.md
  </read_first>
  <action>Create `PaywallDialog` that renders title `Upgrade to keep going`, one feature-specific body sentence, `UpgradeButton` as the primary CTA, and `Not now` as the secondary CTA. Create `LockedFeatureButton` that renders an in-place disabled/locked-looking action using `RiLockLine`, opens `PaywallDialog` on click, and passes `successPath` as the current `window.location.pathname + window.location.search` plus a stable action marker when provided. Keep all subscription checks in hooks or props; do not query Supabase directly in these components.</action>
  <acceptance_criteria>
    - `PaywallDialog.tsx` contains `Upgrade to keep going` and `Not now`.
    - `LockedFeatureButton.tsx` imports `RiLockLine` from `@remixicon/react`.
    - `UpgradeButton` receives a `successPath` prop from the current route/action context.
    - No direct `supabase.from` calls are added to billing components.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Upgrade to keep going|Not now|RiLockLine|successPath" src/components/billing/PaywallDialog.tsx src/components/billing/LockedFeatureButton.tsx</automated>
  </verify>
  <done>Paid gates can be rendered inline without losing route context.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the gate to visible Pro/Team launch affordances</name>
  <files>src/components/settings/MCPTab.tsx, src/components/settings/BillingTab.tsx, src/hooks/useRequirePaidPlan.ts</files>
  <read_first>
    - src/components/settings/MCPTab.tsx
    - src/components/settings/BillingTab.tsx
    - src/hooks/useRequirePaidPlan.ts
    - src/components/billing/PlanCards.tsx
    - src/components/billing/UpgradeButton.tsx
    - src/lib/query-config.ts
  </read_first>
  <action>Replace redirect-only paid-feature handling on launch-visible Pro/Team controls with `LockedFeatureButton` or `PaywallDialog`. Prioritize MCP/external AI connection controls in `MCPTab` and Billing/plan upgrade affordances. Keep `useRequirePaidPlan()` available for callers, but add or expose enough state for inline gates to avoid forced navigation to `/settings?tab=billing` when the user attempts a gated action.</action>
  <acceptance_criteria>
    - At least one MCP Pro-gated action renders an inline locked/paywall affordance instead of only redirecting.
    - Existing Billing tab upgrade buttons still use `UpgradeButton`.
    - `useRequirePaidPlan` continues to return `isRequired`, `isLoading`, and `redirectUrl` for backward compatibility.
    - Subscription-related mutations or checkout returns invalidate/refetch subscription state through existing query patterns.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "LockedFeatureButton|PaywallDialog|useRequirePaidPlan|UpgradeButton" src/components/settings src/hooks/useRequirePaidPlan.ts && npm run build</automated>
  </verify>
  <done>Free users see useful locked affordances and can upgrade in context.</done>
</task>

<task type="auto">
  <name>Task 3: Test successPath preservation for paywall gates</name>
  <files>src/components/billing/__tests__/paywall-gate.test.tsx</files>
  <read_first>
    - src/components/billing/LockedFeatureButton.tsx
    - src/components/billing/PaywallDialog.tsx
    - src/components/billing/UpgradeButton.tsx
    - src/components/billing/__tests__
    - src/components/settings/__tests__/MCPTab.permissions.test.tsx
  </read_first>
  <action>Add focused component tests that mock `UpgradeButton`, render a locked feature from a test route with a query string, open the paywall, and assert the mocked UpgradeButton receives a `successPath` that starts with the current route and preserves the action marker. Keep tests local to billing if possible; do not add a full Polar checkout integration test.</action>
  <acceptance_criteria>
    - Test file asserts `UpgradeButton` receives a route-preserving `successPath`.
    - Test file asserts paywall title `Upgrade to keep going`.
    - Test file does not call real Polar or Supabase.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run src/components/billing/__tests__/paywall-gate.test.tsx</automated>
  </verify>
  <done>Checkout context preservation is guarded by tests.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-04-01 | Tampering | checkout successPath | mitigate | Use relative current-route successPath only; backend already rejects unsafe absolute URLs. |
| T-06-04-02 | Elevation of Privilege | paid feature UI | mitigate | UI affordance does not grant access; server/backend subscription checks remain authoritative. |
| T-06-04-03 | Repudiation | checkout return | accept | Polar/webhook remains the source of paid status; UI only refetches after return. |
</threat_model>

<verification>
Run billing paywall tests and `npm run build`.
</verification>

<success_criteria>
- Paid features are visible but gated.
- Action attempt opens a paywall dialog.
- Checkout success returns to the same surface.
</success_criteria>

<output>
Create `.planning/phases/06-launch-ux-support-rls-hygiene/06-04-SUMMARY.md` when done.
</output>
