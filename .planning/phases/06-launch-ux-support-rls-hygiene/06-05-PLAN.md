---
phase: 06-launch-ux-support-rls-hygiene
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/rls-regression.test.ts
  - src/services/personal-folders.service.ts
  - src/hooks/usePersonalFolders.ts
autonomous: true
requirements:
  - HRD-02
must_haves:
  truths:
    - "D-26: `CROSS_ORG_TABLES` covers mcp_tokens, personal_folders, personal_tags, personal_folder_recordings, personal_tag_recordings, call_notes, contact_folders, import_sources, and import_routing_rules."
    - "D-27: minimum fixtures, stubs, or setup fixes are allowed only to make RLS coverage meaningful."
    - "D-28/D-29: this is not a broad compliance pass; adjacent fixes must be required by the RLS gate or trivial launch blockers."
  artifacts:
    - path: "src/test/rls-regression.test.ts"
      provides: "real Supabase bidirectional cross-org table isolation coverage"
---

<objective>
Expand the real-Supabase RLS regression test so the nine currently missing user-facing tables are covered with meaningful fixtures.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-CONTEXT.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-RESEARCH.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-VALIDATION.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-PATTERNS.md
@src/CLAUDE.md
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the nine missing tables to `CROSS_ORG_TABLES` with correct filters</name>
  <files>src/test/rls-regression.test.ts</files>
  <read_first>
    - src/test/rls-regression.test.ts
    - supabase/migrations/20260310160000_mcp_tokens.sql
    - supabase/migrations/20260306000000_personal_organization_and_home.sql
    - supabase/migrations/20260507083233_call_notes.sql
    - supabase/migrations/20260401140000_contact_folders.sql
    - supabase/migrations/20260228000002_create_import_sources.sql
    - supabase/migrations/20260228000003_create_import_routing_rules.sql
    - supabase/migrations/20260301000001_rename_vaults_to_workspaces.sql
  </read_first>
  <action>Extend `CROSS_ORG_TABLES` in `src/test/rls-regression.test.ts` with `mcp_tokens`, `personal_folders`, `personal_tags`, `personal_folder_recordings`, `personal_tag_recordings`, `call_notes`, `contact_folders`, `import_sources`, and `import_routing_rules`. Add any needed filter column variants to the `filterColumn` union. Use the most direct org/workspace/recording/user-owned filter that proves cross-org isolation for each table. Verify final `import_routing_rules` column names from migrations/types before choosing the filter.</action>
  <acceptance_criteria>
    - `CROSS_ORG_TABLES` contains all nine missing table names.
    - Each new table has a filter column that exists in the current schema.
    - Query errors continue to fail the test as setup errors.
    - No Supabase mocking is introduced.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "mcp_tokens|personal_folders|personal_tags|personal_folder_recordings|personal_tag_recordings|call_notes|contact_folders|import_sources|import_routing_rules" src/test/rls-regression.test.ts</automated>
  </verify>
  <done>The RLS table list covers the Phase 6 gap.</done>
</task>

<task type="auto">
  <name>Task 2: Create real fixture rows for every newly covered table</name>
  <files>src/test/rls-regression.test.ts</files>
  <read_first>
    - src/test/rls-regression.test.ts
    - supabase/migrations/20260310160000_mcp_tokens.sql
    - supabase/migrations/20260306000000_personal_organization_and_home.sql
    - supabase/migrations/20260507083233_call_notes.sql
    - supabase/migrations/20260401140000_contact_folders.sql
    - supabase/migrations/20260228000002_create_import_sources.sql
    - supabase/migrations/20260228000003_create_import_routing_rules.sql
  </read_first>
  <action>Add fixture variables and service-role inserts for one Org A row and one Org B row in every newly covered table. Insert parent rows before junction rows: personal folder/tag before personal_folder_recordings/personal_tag_recordings; recording/workspace before call_notes; organization/workspace before mcp_tokens; organization/workspace/folder before import_routing_rules. Keep cleanup dependency order explicit for new rows if cascade cannot be relied on. Do not broaden the test into unrelated tables.</action>
  <acceptance_criteria>
    - Each newly covered table has at least one inserted Org A fixture and one inserted Org B fixture where schema permits.
    - Junction table fixtures reference the phase test recordings.
    - Cleanup handles new parent rows or relies on verified cascade from existing org/recording cleanup.
    - The test still creates real auth users and real anon JWT clients.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run src/test/rls-regression.test.ts</automated>
  </verify>
  <done>The RLS test cannot pass vacuously for the new table list.</done>
</task>

<task type="auto">
  <name>Task 3: Fix minimal personal-folder stubs only if they block meaningful RLS or launch behavior</name>
  <files>src/services/personal-folders.service.ts, src/hooks/usePersonalFolders.ts</files>
  <read_first>
    - src/services/personal-folders.service.ts
    - src/hooks/usePersonalFolders.ts
    - src/services/personal-tags.service.ts
    - src/test/rls-regression.test.ts
    - .planning/codebase/CONCERNS.md
  </read_first>
  <action>If the RLS fixtures or empty-state work expose broken personal-folder reads, replace stubbed empty returns in `personal-folders.service.ts` with minimal real Supabase reads matching the existing `personal-tags.service.ts` pattern. Keep this fix limited to list folders and folder assignments needed by current UI/tests. Do not implement the broader `PF-V2-01` personal folders feature.</action>
  <acceptance_criteria>
    - If edited, `personal-folders.service.ts` queries only `personal_folders` and `personal_folder_recordings` needed by existing hooks.
    - If edited, service remains React-free.
    - No new personal folder creation/editing UI is added.
    - If not edited, the plan summary records that stubs did not block RLS coverage.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run src/test/rls-regression.test.ts && npm run build</automated>
  </verify>
  <done>RLS coverage is meaningful without pulling in a deferred feature.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-05-01 | Information Disclosure | RLS policies | mitigate | Add bidirectional real-JWT reads for every missing user-facing table. |
| T-06-05-02 | Tampering | test fixtures | mitigate | Service-role creates scoped rows; anon clients are used only for isolation assertions. |
| T-06-05-03 | Denial of Service | integration cleanup | mitigate | Keep dependency-ordered cleanup and existing fixture RPC sweep. |
</threat_model>

<verification>
Run `npm run test -- --run src/test/rls-regression.test.ts`. If Supabase test env vars are absent, the test may skip cleanly; do not claim cross-org proof unless it actually runs against a reachable test DB.
</verification>

<success_criteria>
- All nine missing tables are in `CROSS_ORG_TABLES`.
- Each table has meaningful cross-org fixture coverage.
- Real JWT cross-org reads return zero rows both directions.
</success_criteria>

<output>
Create `.planning/phases/06-launch-ux-support-rls-hygiene/06-05-SUMMARY.md` when done.
</output>
