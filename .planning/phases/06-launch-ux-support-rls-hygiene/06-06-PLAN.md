---
phase: 06-launch-ux-support-rls-hygiene
plan: 06
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - supabase/functions/fetch-meetings/index.ts
  - supabase/functions/fathom-refresh/index.ts
  - src/components/connectors/registry/types.ts
  - src/components/connectors/registry/adapters/fathom.ts
  - src/hooks/useSyncTabOrchestration.ts
  - src/services/sync-tab.service.ts
  - src/components/transcripts/UnsyncedMeetingsSection.tsx
  - src/components/transcript-library/TranscriptTableRow.tsx
  - src/components/connectors/__tests__/ConnectorImportWizard.test.tsx
autonomous: true
requirements:
  - ONB-01
must_haves:
  truths:
    - "D-30/D-31: Fathom metadata resync is an optional provider-to-CallVault launch-polish add-on; CallVault-to-provider push stays deferred."
    - "D-32/D-33/D-34: remote-updated imported calls surface at call/import-list level with a distinct resync state, not disabled imported state or brand-new available state."
    - "D-35/D-36: resync updates provider-owned fields only and preserves local notes, tags, folder/workspace placement, and user metadata."
    - "D-37/D-38/D-39: implement Fathom-first title/name detection; transcript/duration trim refresh is secondary and may be deferred if not cheap."
  artifacts:
    - path: "supabase/functions/fathom-refresh/index.ts"
      provides: "single-call provider refresh that preserves local associations"
    - path: "supabase/functions/fetch-meetings/index.ts"
      provides: "Fathom remote-change detection for import/search rows"
---

<objective>
Expose a Fathom-first `Updated remotely` state for already-imported calls whose provider title changed, and let users apply that update without duplicating calls.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-launch-ux-support-rls-hygiene/06-CONTEXT.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-RESEARCH.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-UI-SPEC.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-PATTERNS.md
@.planning/phases/05-connector-reliability-per-workspace-binding-unified-sync-tab/05-CONTEXT.md
@src/CLAUDE.md
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Return Fathom remote-change metadata from meeting fetch</name>
  <files>supabase/functions/fetch-meetings/index.ts</files>
  <read_first>
    - supabase/functions/fetch-meetings/index.ts
    - supabase/functions/fathom-refresh/index.ts
    - supabase/functions/_shared/auth.ts
    - supabase/functions/_shared/fathom-client.ts
    - supabase/CLAUDE.md
  </read_first>
  <action>Extend `fetch-meetings` so active imported Fathom meetings include the canonical `recording_uuid`, local title, remote title, and a stable `sync_state` or equivalent value with at least `available`, `imported`, and `updated_remotely`. Start with title/name comparison only: if an active imported call's local `recordings.title` differs from the Fathom meeting `title`, return `updated_remotely`. Do not fetch transcripts solely for change detection in this task. Keep existing workspace_entries logic so removed-from-all-workspaces recordings remain re-importable.</action>
  <acceptance_criteria>
    - Response rows can distinguish available, imported, and updated-remotely Fathom calls.
    - Updated-remotely rows include the canonical recording UUID needed by `fathom-refresh`.
    - Title comparison does not mark removed-from-all-workspaces recordings as imported.
    - No transcript fetch is added solely for remote-change detection.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "sync_state|updated_remotely|recording_uuid|local.*title|remote.*title" supabase/functions/fetch-meetings/index.ts</automated>
  </verify>
  <done>Fathom search can identify imported rows that changed remotely.</done>
</task>

<task type="auto">
  <name>Task 2: Extend connector/client types and Fathom adapter for resync state</name>
  <files>src/components/connectors/registry/types.ts, src/components/connectors/registry/adapters/fathom.ts, src/hooks/useSyncTabOrchestration.ts, src/services/sync-tab.service.ts</files>
  <read_first>
    - src/components/connectors/registry/types.ts
    - src/components/connectors/registry/adapters/fathom.ts
    - src/hooks/useSyncTabOrchestration.ts
    - src/services/sync-tab.service.ts
    - src/components/connectors/connectorSearch.ts
    - src/hooks/useMeetingsSync.ts
  </read_first>
  <action>Add a typed sync-state field to `AvailableCall` and the meeting mapping used by the sync/import list. Map Fathom `updated_remotely` rows to a distinct client state with `recordingUuid`, `localTitle`, and `remoteTitle`. Update `useSyncTabOrchestration.fetchMeetings()` so it no longer drops every synced call; keep ordinary imported calls muted/hidden as appropriate, but keep remote-updated calls visible and selectable for the resync action. Add a pure service function that invokes `fathom-refresh` with `recording_id` UUID for selected remote-updated rows and invalidates call/import caches from the hook layer.</action>
  <acceptance_criteria>
    - `AvailableCall` or mapped meeting type has a non-boolean sync state.
    - Fathom adapter maps provider response fields for recording UUID and old/new title.
    - `useSyncTabOrchestration` keeps `updated_remotely` calls visible after fetch.
    - A service function invokes `fathom-refresh` with a UUID `recording_id`.
    - Cache invalidation remains in hooks, not services.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "updated_remotely|syncState|recordingUuid|fathom-refresh|recording_id" src/components/connectors src/hooks/useSyncTabOrchestration.ts src/services/sync-tab.service.ts</automated>
  </verify>
  <done>The frontend can carry provider update state from fetch to action.</done>
</task>

<task type="auto">
  <name>Task 3: Render `Updated remotely` rows and apply selected updates</name>
  <files>src/components/transcripts/UnsyncedMeetingsSection.tsx, src/components/transcript-library/TranscriptTableRow.tsx, src/components/connectors/__tests__/ConnectorImportWizard.test.tsx</files>
  <read_first>
    - src/components/transcripts/UnsyncedMeetingsSection.tsx
    - src/components/transcript-library/TranscriptTable.tsx
    - src/components/transcript-library/TranscriptTableRow.tsx
    - src/components/ui/badge.tsx
    - src/hooks/useSyncTabSelection.ts
    - src/hooks/useSyncTabOrchestration.ts
    - .planning/phases/06-launch-ux-support-rls-hygiene/06-UI-SPEC.md
  </read_first>
  <action>Render remote-updated Fathom calls with an `Updated remotely` outline/status badge and normal row contrast. Make ordinary imported rows non-primary and not selected by `Sync all`. When selected rows are remote-updated, show action copy `Apply updates`; if mixed import/update selection is not safely supported, split into separate `Sync selected` and `Apply updates` actions. Before applying a Fathom title update, show a confirmation that displays `Current title` and `Fathom title`, with primary CTA `Update from Fathom` and secondary CTA `Keep current title`.</action>
  <acceptance_criteria>
    - Remote-updated rows display exact badge copy `Updated remotely`.
    - CTA copy changes to `Apply updates` when selected rows are update-only.
    - Title confirmation displays `Current title`, `Fathom title`, `Update from Fathom`, and `Keep current title`.
    - `Sync all` does not include ordinary imported rows.
    - Tests or source assertions cover remote-updated row display and CTA copy.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Updated remotely|Apply updates|Current title|Fathom title|Update from Fathom|Keep current title" src/components src/hooks && npm run build</automated>
  </verify>
  <done>Users can apply provider title updates without confusing them with new imports.</done>
</task>

<task type="auto">
  <name>Task 4: Verify local association preservation on Fathom refresh</name>
  <files>supabase/functions/fathom-refresh/index.ts, src/services/sync-tab.service.ts</files>
  <read_first>
    - supabase/functions/fathom-refresh/index.ts
    - supabase/functions/_shared/connector-pipeline.ts
    - src/services/sync-tab.service.ts
    - src/lib/query-config.ts
    - src/lib/recording-ids.ts
  </read_first>
  <action>Audit `fathom-refresh` and only edit it if needed so resync updates provider-owned fields while preserving `recordings.id`, `organization_id`, `owner_user_id`, `created_at`, `workspace_entries`, `folder_assignments`, `call_tag_assignments`, `transcript_tag_assignments`, and `call_notes`. Add source assertions or targeted tests if an existing Edge Function test harness exists; otherwise record source-verified preservation in the summary with exact lines read.</action>
  <acceptance_criteria>
    - `fathom-refresh` update statement does not set organization, owner, workspace, folder, tag, or note fields.
    - Resync path uses canonical UUID recording IDs, not parsed legacy IDs from the UI.
    - Summary records whether transcript/duration resync was kept or deferred.
    - If transcript/duration detection is not implemented, summary explicitly says title-only detection shipped.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "organization_id|owner_user_id|workspace_entries|folder_assignments|call_tag_assignments|call_notes|title|full_transcript|duration" supabase/functions/fathom-refresh/index.ts</automated>
  </verify>
  <done>Resync does not destroy CallVault-owned organization data.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-06-01 | Tampering | Fathom refresh | mitigate | Refresh only provider-owned fields and preserve local associations. |
| T-06-06-02 | Information Disclosure | imported-call search | mitigate | Fetch state only for owner_user_id and authenticated user's source rows. |
| T-06-06-03 | Repudiation | title overwrite | mitigate | Show old/new title confirmation before applying provider title. |
| T-06-06-04 | Denial of Service | provider fetch | mitigate | Do not add transcript fetches for change detection; title-only detection first. |
</threat_model>

<verification>
Run source guards for `updated_remotely`, targeted connector/sync tests if added, and `npm run build`. Live Fathom title-change verification requires credentialed provider access; record if unavailable.
</verification>

<success_criteria>
- Imported Fathom calls with changed provider titles surface as `Updated remotely`.
- Users can apply updates intentionally.
- Local notes, tags, folders, workspace placement, org, owner, and UUID are preserved.
- Transcript/duration refresh is either safely implemented or explicitly deferred.
</success_criteria>

<output>
Create `.planning/phases/06-launch-ux-support-rls-hygiene/06-06-SUMMARY.md` when done.
</output>
