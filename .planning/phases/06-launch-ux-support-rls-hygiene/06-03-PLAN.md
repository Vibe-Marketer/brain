---
phase: 06-launch-ux-support-rls-hygiene
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/transcript-library/EmptyStates.tsx
  - src/components/transcripts/TranscriptsTab.tsx
  - src/components/panes/WorkspaceSidebarPane.tsx
  - src/components/settings/WorkspaceManagement.tsx
  - src/components/contacts/ContactsTable.tsx
  - src/components/import/ImportHistoryPanel.tsx
  - src/pages/__tests__/ImportPage.connector-routing.test.ts
  - src/components/panes/__tests__/ImportSourcePane.registry.test.ts
autonomous: true
requirements:
  - ONB-02
must_haves:
  truths:
    - "D-09: every zero-data surface touched by launch gets one direct primary CTA."
    - "D-10/D-11: empty calls/vault primary CTA is `Connect a source`; `Import transcript` stays secondary only where useful."
    - "D-12/D-14: onboarding and contacts empty states push users toward real imported calls before structure or manual data entry."
    - "D-13: workspace and folder management empty states may use create CTAs when the user is clearly managing structure."
  artifacts:
    - path: "src/components/transcript-library/EmptyStates.tsx"
      provides: "canonical no-calls CTA copy"
---

<objective>
Remove dead-end and stale-upload empty states so launch users always get one concrete next action.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-launch-ux-support-rls-hygiene/06-CONTEXT.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-RESEARCH.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-UI-SPEC.md
@.planning/phases/06-launch-ux-support-rls-hygiene/06-PATTERNS.md
@src/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix canonical no-calls empty state copy and actions</name>
  <files>src/components/transcript-library/EmptyStates.tsx</files>
  <read_first>
    - src/components/transcript-library/EmptyStates.tsx
    - src/components/ui/button.tsx
    - src/pages/ImportPage.tsx
    - .planning/phases/06-launch-ux-support-rls-hygiene/06-UI-SPEC.md
  </read_first>
  <action>Update the `no-transcripts` empty state to heading `No calls yet`, body `Connect a recorder to bring your calls into CallVault.`, and primary button label `Connect a source` that navigates to `/import`. Remove the phrase `upload a file directly` and any audio/video upload promise from this component. Add an optional secondary action for `Import transcript` only if the component already supports or can safely accept a secondary callback without changing existing call sites.</action>
  <acceptance_criteria>
    - `EmptyStates.tsx` contains `Connect a recorder to bring your calls into CallVault.`
    - `EmptyStates.tsx` contains `Connect a source`.
    - `EmptyStates.tsx` does not contain `upload a file directly`.
    - The primary no-transcripts action still navigates to `/import`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Connect a recorder to bring your calls into CallVault|Connect a source|upload a file directly" src/components/transcript-library/EmptyStates.tsx</automated>
  </verify>
  <done>The main empty calls state points to source connection, not upload.</done>
</task>

<task type="auto">
  <name>Task 2: Patch launch-critical empty states across calls, contacts, workspaces, folders, and import history</name>
  <files>src/components/transcripts/TranscriptsTab.tsx, src/components/panes/WorkspaceSidebarPane.tsx, src/components/settings/WorkspaceManagement.tsx, src/components/contacts/ContactsTable.tsx, src/components/import/ImportHistoryPanel.tsx</files>
  <read_first>
    - src/components/transcripts/TranscriptsTab.tsx
    - src/components/panes/WorkspaceSidebarPane.tsx
    - src/components/settings/WorkspaceManagement.tsx
    - src/components/contacts/ContactsTable.tsx
    - src/components/import/ImportHistoryPanel.tsx
    - src/components/transcript-library/EmptyStates.tsx
    - src/CLAUDE.md
  </read_first>
  <action>Audit these launch-visible empty states and make the smallest code edits needed so each has a concrete primary action. Calls and contacts should point to `/import` or `Connect a source`. Workspace/folder management surfaces may keep `Create workspace` or `Create folder` when the user is already managing structure. Import history should point to selecting or connecting an import source. Do not refactor table pagination, folder DnD, or contacts data fetching.</action>
  <acceptance_criteria>
    - Contacts empty state mentions importing or connecting calls, not manual-only contact creation.
    - Workspace/folder management empty states keep a create CTA where structure management is the current task.
    - Import history empty state contains a source/import CTA.
    - No edited empty-state copy references audio/video file upload.
    - No direct Supabase queries are added to components.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "No calls yet|Connect a source|Import transcript|Create workspace|Create folder|No contacts yet|audio or video|upload a file" src/components/transcripts src/components/panes src/components/settings src/components/contacts src/components/import</automated>
  </verify>
  <done>Launch-visible empty states no longer strand users.</done>
</task>

<task type="auto">
  <name>Task 3: Add registry tests preventing file-upload copy drift</name>
  <files>src/pages/__tests__/ImportPage.connector-routing.test.ts, src/components/panes/__tests__/ImportSourcePane.registry.test.ts</files>
  <read_first>
    - src/pages/__tests__/ImportPage.connector-routing.test.ts
    - src/components/panes/__tests__/ImportSourcePane.registry.test.ts
    - src/components/transcript-library/EmptyStates.tsx
    - src/components/import/ImportHistoryPanel.tsx
  </read_first>
  <action>Extend existing registry tests so they fail if launch entry surfaces reintroduce `FileUploadDropzone`, `audio or video files directly`, `upload a file directly`, or equivalent direct transcription upload copy. Keep the manual path label as `Import transcript`.</action>
  <acceptance_criteria>
    - A test asserts `FileUploadDropzone` is not referenced by visible import/onboarding surfaces.
    - A test asserts `audio or video files directly` or `upload a file directly` is absent from launch entry copy.
    - Tests still allow `Import transcript`.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run src/pages/__tests__/ImportPage.connector-routing.test.ts src/components/panes/__tests__/ImportSourcePane.registry.test.ts</automated>
  </verify>
  <done>Stale upload promises stay out of the launch UI.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-03-01 | Denial of Service | onboarding UX | mitigate | Replace blank/dead empty states with reachable CTAs. |
| T-06-03-02 | Tampering | file-upload scope | mitigate | Add registry tests to prevent hidden v2 upload scope from reappearing in launch UI. |
</threat_model>

<verification>
Run targeted registry tests, source-copy guards, and `npm run build`.
</verification>

<success_criteria>
- Main no-calls empty state sends users to connector setup.
- Contacts empty state points toward imported calls.
- Structure-management empty states keep appropriate create CTAs.
- File-upload/transcription promises are not reintroduced.
</success_criteria>

<output>
Create `.planning/phases/06-launch-ux-support-rls-hygiene/06-03-SUMMARY.md` when done.
</output>
