---
phase: 20-nightly-qa-fixable-flake-suppression
plan: 03
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - /Users/admin/dev/autopilot/autopilot.config.ts
  - /Users/admin/dev/autopilot/src/lib/claim.ts
  - /Users/admin/dev/autopilot/src/lib/claim.test.ts
  - /Users/admin/dev/autopilot/src/claimer.ts
  - /Users/admin/dev/autopilot/src/claimer.test.ts
  - /Users/admin/dev/autopilot/src/runner.ts
  - /Users/admin/dev/autopilot/src/runner.test.ts
autonomous: true
requirements: [QA-04, QA-03]
must_haves:
  truths:
    - "Autopilot can claim low/medium `source='nightly_qa'` tickets in the same loop per D-05."
    - "High/critical QA tickets or QA failures that cannot be fixed route to tier-2/review, never raw operator dumps per D-05 and D-07."
    - "QA consumes at most 50% of the existing daily run cap and cannot starve user or Sentry tickets per D-06."
    - "Concurrency remains exactly 1 and Phase 20 does not raise total volume."
  artifacts:
    - path: "/Users/admin/dev/autopilot/autopilot.config.ts"
      provides: "sourceBudgets.qaMaxShare default 0.5 without run-cap increase"
    - path: "/Users/admin/dev/autopilot/src/lib/claim.ts"
      provides: "source-aware candidate selection/filtering"
    - path: "/Users/admin/dev/autopilot/src/claimer.ts"
      provides: "per-source budget gate before claim"
    - path: "/Users/admin/dev/autopilot/src/runner.ts"
      provides: "QA tier-2 routing for unfixable/high-risk outcomes"
  key_links:
    - from: "claimer.ts"
      to: "claim.ts"
      via: "excludedSources or sourceBudget filter"
      pattern: "nightly_qa"
    - from: "runner.ts"
      to: "tier2.ts"
      via: "enqueueTier2Escalation"
      pattern: "tier2_escalation_routed"
---

<objective>
Make QA tickets first-class work in the existing Autopilot loop while enforcing severity and per-source budget safety.

Purpose: QA broadens the input stream, but it must not consume the whole daily cap or bypass tier-2 when the finding is high risk or unfixable.
Output: Source-aware claim filtering, QA budget enforcement, tier-2 QA routing, and Bun tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-nightly-qa-fixable-flake-suppression/20-CONTEXT.md
@.planning/phases/20-nightly-qa-fixable-flake-suppression/20-RESEARCH.md
@.planning/phases/20-nightly-qa-fixable-flake-suppression/20-PATTERNS.md
@.planning/design/escalation-tier2-solutions-not-problems.md
@docs/architecture/autopilot-brain-ownership.md
@/Users/admin/dev/autopilot/autopilot.config.ts
@/Users/admin/dev/autopilot/src/lib/claim.ts
@/Users/admin/dev/autopilot/src/lib/claim.test.ts
@/Users/admin/dev/autopilot/src/claimer.ts
@/Users/admin/dev/autopilot/src/claimer.test.ts
@/Users/admin/dev/autopilot/src/runner.ts
@/Users/admin/dev/autopilot/src/runner.test.ts
@/Users/admin/dev/autopilot/src/lib/tier2.ts
</context>

## Artifacts this phase produces

- `TicketCandidate.source` and source-aware selection.
- QA share budget defaulting to 50% of the current daily run cap.
- Tier-2 routing for high/critical or unfixable QA findings without operator raw-problem output.

<tasks>

<task type="auto" tdd="true">
  <name>Task 20-03-01: Add source-aware candidate selection</name>
  <read_first>
    @/Users/admin/dev/autopilot/src/lib/claim.ts
    @/Users/admin/dev/autopilot/src/lib/claim.test.ts
    @.planning/ROADMAP.md
  </read_first>
  <files>/Users/admin/dev/autopilot/src/lib/claim.ts, /Users/admin/dev/autopilot/src/lib/claim.test.ts</files>
  <behavior>
    - `TicketCandidate` includes `source`.
    - `selectNextTicket` selects `source` from `tickets`.
    - The selector can exclude `nightly_qa` while preserving ordering among remaining candidates.
  </behavior>
  <action>Extend `TicketCandidate` with `source: "manual" | "sentry" | "unknown" | "nightly_qa" | "internal" | string`. Update the Supabase select list to include `source`. Add an optional excluded-source parameter or source predicate to `selectNextTicket`, filtering before `pickNext`. Keep the atomic claim guard unchanged: `eq(id)` plus `eq(status,'new')`. Add tests proving a higher-priority QA ticket is skipped when QA is excluded and a lower-priority Sentry/manual ticket is returned; prove `status='new'` remains required and non-new review items cannot be claimed.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && bun test src/lib/claim.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source filtering happens before claiming, not after a QA ticket is already in_progress.
    - Existing urgent/priority/severity/created ordering is unchanged for non-excluded candidates.
    - No ticket status or enum rewrite is introduced.
  </acceptance_criteria>
  <done>Claim selection can reserve capacity by source without weakening the atomic claim boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 20-03-02: Enforce QA <= 50% of the existing run budget</name>
  <read_first>
    @/Users/admin/dev/autopilot/autopilot.config.ts
    @/Users/admin/dev/autopilot/src/claimer.ts
    @/Users/admin/dev/autopilot/src/claimer.test.ts
    @/Users/admin/dev/autopilot/src/runner.ts
  </read_first>
  <files>/Users/admin/dev/autopilot/autopilot.config.ts, /Users/admin/dev/autopilot/src/claimer.ts, /Users/admin/dev/autopilot/src/claimer.test.ts</files>
  <behavior>
    - Config exposes a QA share cap defaulting to 0.5.
    - If QA has reached its share, the cycle still claims non-QA tickets when total budget remains.
    - If total budget is exhausted, existing total-budget suppression still stops all new claims.
    - `config.concurrency` stays the literal value 1 and `maxRunsPerWindow.maxRuns` is not raised.
  </behavior>
  <action>Add `sourceBudgets` or `qaMaxShare` to `AutopilotConfig` with default QA share `0.5`. Implement a source-budget helper in `claimer.ts` that counts started runs in the current budget window by joining `runner_runs.ticket_id` to `tickets.source` when possible, or by reading source from the structured JSONL if runner ledger already includes it after this plan. When QA count is at or above `floor(maxRunsPerWindow.maxRuns * 0.5)`, call `selectNextTicket` with `nightly_qa` excluded; do not return `suppressed:budget` if non-QA work is available and total budget remains. Keep approval merges exempt as they are today. Add tests for total cap, QA cap with non-QA fallback, QA cap with only QA available, and no live volume raise per D-06.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && bun test src/claimer.test.ts src/lib/claim.test.ts</automated>
    <automated>cd /Users/admin/dev/autopilot && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - QA cannot exceed 50% of the existing daily cap.
    - User/Sentry/internal work remains claimable when QA share is exhausted and total budget remains.
    - No config change raises `maxRunsPerWindow.maxRuns`, `pollIntervalSec`, or `concurrency`.
  </acceptance_criteria>
  <done>Per-source QA budgeting is enforced inside the existing claim cycle without changing global volume or concurrency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 20-03-03: Route high-risk or failed QA work to tier-2</name>
  <read_first>
    @/Users/admin/dev/autopilot/src/runner.ts
    @/Users/admin/dev/autopilot/src/runner.test.ts
    @/Users/admin/dev/autopilot/src/lib/tier2.ts
    @.planning/design/escalation-tier2-solutions-not-problems.md
  </read_first>
  <files>/Users/admin/dev/autopilot/src/runner.ts, /Users/admin/dev/autopilot/src/runner.test.ts</files>
  <behavior>
    - QA tickets that reach runner failure/escalation use the tier-2 queue, not GitHub/operator handoff text.
    - QA high/critical source tickets are not silently auto-fixed.
    - Tier-2 event trail records source-aware category such as `source:nightly_qa:error:...`.
  </behavior>
  <action>Audit the runner escalation path for QA. Preserve the existing `enqueueTier2Escalation` behavior, but make QA-specific failures and any high/critical QA row route to tier-2 with a solution-shaped note per D-07. Do not surface raw crawler details to the operator. If Plan 20-02 already prevents high/critical ticket creation, keep a defensive runner test proving high/critical `source='nightly_qa'` cannot proceed to auto-merge and instead records `tier2_escalation_routed`. Keep customer comms silent for QA by preserving reporter_id NULL and source gating.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && bun test src/runner.test.ts src/lib/tier2.test.ts src/lib/approval.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - No raw QA problem dump or GitHub handoff is introduced for D-07.
    - High/critical QA is blocked from low-friction auto-fix even if a bad row exists.
    - Existing tier-2 diversity/config tests still pass.
  </acceptance_criteria>
  <done>QA source tickets use the same loop for low/medium work and tier-2 routing for high-risk or unfixable work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tickets table -> claimer | DB queue data controls what local code gets agent execution. |
| Claimer -> runner | Claimed QA tickets trigger local worktree and headless agent execution. |
| Runner -> tier-2 | Failed or high-risk QA tickets move to a second model/cadence lane. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-13 | Spoofing | source budget counts | mitigate | Count source from DB ticket rows, not caller text. |
| T-20-14 | Tampering | `claimTicket` | mitigate | Preserve conditional UPDATE `eq(id)` and `eq(status,'new')`. |
| T-20-15 | Repudiation | tier-2 routing | mitigate | Write `tier2_escalation_routed` events with category and route action. |
| T-20-16 | Information Disclosure | QA escalation notes | mitigate | Solution-shaped tier-2 notes only; no raw stack/crawler dumps to operator. |
| T-20-17 | Denial of Service | QA budget | mitigate | Exclude `nightly_qa` after 50% share while reserving capacity for other sources. |
| T-20-18 | Elevation of Privilege | high/critical QA | mitigate | Severity gate high/critical to tier-2/review; defensive runner tests. |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; use existing Bun tests and TypeScript. |
</threat_model>

<verification>
- `cd /Users/admin/dev/autopilot && bun test src/lib/claim.test.ts src/claimer.test.ts src/runner.test.ts src/lib/tier2.test.ts`
- `cd /Users/admin/dev/autopilot && bun run typecheck`
</verification>

<success_criteria>
Autopilot fixes low/medium QA tickets in the existing loop, routes high-risk or failed QA work to tier-2, reserves at least half the current daily run cap for non-QA sources, and keeps concurrency locked at 1.
</success_criteria>

<output>
Create `.planning/phases/20-nightly-qa-fixable-flake-suppression/20-03-SUMMARY.md` when done.
</output>
