---
phase: "06.3.2-fathom-provider-id-rename"
plan: "04"
type: execute
wave: 3
depends_on:
  - "06.3.2-02"
files_modified:
  - supabase/functions/_shared/connector-pipeline.ts
  - supabase/functions/auto-tag-calls/index.ts
  - supabase/functions/auto-tag-calls/__tests__/auto-tag-calls.integration.test.ts
  - supabase/functions/fathom-refresh/index.ts
  - supabase/functions/fathom-refresh/__tests__/fathom-refresh.test.ts
  - supabase/functions/fetch-meetings/index.ts
  - supabase/functions/share-call/index.ts
  - supabase/functions/share-call/__tests__/share-call.integration.test.ts
  - supabase/functions/split-recording/index.ts
  - supabase/functions/mcp-server/tools/read/get_contact_calls.ts
  - supabase/functions/mcp-server/tools/read/list_shared_calls.ts
  - supabase/functions/mcp-server/tools/write/create_share_link.ts
autonomous: true
requirements:
  - "none/TBD"
requirements_addressed:
  - "D-07"
  - "D-09"
must_haves:
  truths:
    - "D-07: Edge Functions and MCP tools use `fathom_provider_id` for the renamed `recordings` DB column."
    - "D-09: `source_call_id` remains unchanged, including Fathom refresh fallback behavior."
    - "MCP tool result shape remains `content[].text` markdown; this rename does not introduce structured JSON tool results."
    - "MCP wildcard CORS boundaries and `tools/list` category filtering are not widened while files are touched."
  artifacts:
    - path: "supabase/functions/fathom-refresh/index.ts"
      provides: "Fathom refresh reads the renamed provider-ID column while preserving source_call_id fallback"
    - path: "supabase/functions/_shared/connector-pipeline.ts"
      provides: "Connector ingestion writes `fathom_provider_id` for Fathom rows"
    - path: "supabase/functions/share-call/index.ts"
      provides: "Share link lookup bridges call_recording_id to recordings.fathom_provider_id"
    - path: "supabase/functions/mcp-server/tools/**/*.ts"
      provides: "MCP read/write tools bridge shared/contact calls through `fathom_provider_id`"
  key_links:
    - from: "shared call BIGINT IDs"
      to: "recordings.fathom_provider_id"
      via: "share-call and MCP shared/contact call lookups"
    - from: "Fathom connector external_id"
      to: "recordings.fathom_provider_id"
      via: "connector pipeline insert/upsert"
---

<objective>
Update Edge Function and MCP tool references to the renamed provider-ID column.

Purpose: Edge Functions are a separate runtime surface from the Vite app and include sharing, Fathom refresh, connector ingestion, auto-tagging, split-recording, and MCP tools. They need focused updates without changing auth/CORS/tool result contracts.

Output: Edge Function source and focused tests compile against `fathom_provider_id`, with `source_call_id` and MCP markdown result boundaries preserved.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@supabase/CLAUDE.md
@src/CLAUDE.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-CONTEXT.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-RESEARCH.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-PATTERNS.md
@.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-02-SUMMARY.md
@supabase/functions/fathom-refresh/index.ts
@supabase/functions/_shared/connector-pipeline.ts
@supabase/functions/share-call/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update connector, Fathom refresh, and utility Edge Functions</name>
  <files>supabase/functions/_shared/connector-pipeline.ts, supabase/functions/auto-tag-calls/index.ts, supabase/functions/fathom-refresh/index.ts, supabase/functions/fetch-meetings/index.ts, supabase/functions/split-recording/index.ts, supabase/functions/auto-tag-calls/__tests__/auto-tag-calls.integration.test.ts, supabase/functions/fathom-refresh/__tests__/fathom-refresh.test.ts</files>
  <read_first>
    - supabase/functions/_shared/connector-pipeline.ts
    - supabase/functions/fathom-refresh/index.ts
    - supabase/functions/fetch-meetings/index.ts
    - supabase/functions/auto-tag-calls/index.ts
    - supabase/CLAUDE.md
  </read_first>
  <action>Replace Edge Function `recordings` column selectors, filters, inserts, row interfaces, and tests from `legacy_recording_id` to `fathom_provider_id`. In `_shared/connector-pipeline.ts`, Fathom `external_id` should populate `fathom_provider_id`, not `legacy_recording_id`, while leaving `source_call_id` unchanged. In `fathom-refresh/index.ts`, rename the selected row field and local type from `legacy_recording_id` to `fathom_provider_id`; preserve the fallback that uses `source_call_id` when the numeric provider ID is null; keep existing error code semantics unless a user-facing string explicitly names the old DB column, in which case update it to say missing Fathom provider ID. In `fetch-meetings/index.ts`, local maps from Fathom numeric ID to UUID should read `fathom_provider_id`. In `auto-tag-calls/index.ts`, queries should select and filter `fathom_provider_id` but still use the numeric array to query Fathom-owned tables by their existing `recording_id` columns. In `split-recording/index.ts`, update equality filters against `recordings` to `fathom_provider_id`. Do not inline auth boilerplate, do not weaken production/test guards, and do not rename `source_call_id`.</action>
  <verify>
    <automated>npm test -- supabase/functions/fathom-refresh/__tests__/fathom-refresh.test.ts && ! rg -n "legacy_recording_id" supabase/functions/_shared supabase/functions/auto-tag-calls supabase/functions/fathom-refresh supabase/functions/fetch-meetings supabase/functions/split-recording --glob '!**/*.md'</automated>
  </verify>
  <acceptance_criteria>
    - Touched Edge Functions select/filter/insert `fathom_provider_id`.
    - `fathom-refresh` still references `source_call_id` for the documented fallback.
    - Auto-tagging still queries Fathom-owned tables by existing `recording_id` columns after collecting Fathom provider IDs.
    - No auth boilerplate or CORS behavior is changed.
    - Whole-repo `npm run type-check` is intentionally deferred to Plan 05 after both Plan 03 app adapters and Plan 04 Edge adapters complete, so Plan 04 can still run after Plan 02 and in parallel with Plan 03.
  </acceptance_criteria>
  <done>Connector and Fathom Edge Functions use the renamed provider-ID field.</done>
</task>

<task type="auto">
  <name>Task 2: Update share-call and MCP tool bridge lookups</name>
  <files>supabase/functions/share-call/index.ts, supabase/functions/share-call/__tests__/share-call.integration.test.ts, supabase/functions/mcp-server/tools/read/get_contact_calls.ts, supabase/functions/mcp-server/tools/read/list_shared_calls.ts, supabase/functions/mcp-server/tools/write/create_share_link.ts</files>
  <read_first>
    - supabase/functions/share-call/index.ts
    - supabase/functions/mcp-server/tools/read/get_contact_calls.ts
    - supabase/functions/mcp-server/tools/read/list_shared_calls.ts
    - supabase/functions/mcp-server/tools/write/create_share_link.ts
    - AGENTS.md
  </read_first>
  <action>Update share-link and MCP bridge lookups where `shared_call_links.call_recording_id`, `call_participants.recording_id`, or other BIGINT Fathom-owned IDs are matched back to canonical `recordings`. Select and filter `recordings.fathom_provider_id`, and map rows by `fathom_provider_id` instead of `legacy_recording_id`. In `create_share_link.ts`, fetch `fathom_provider_id`; if it is absent, keep the existing behavior of refusing to create a BIGINT-backed share link and update the message to refer to a missing Fathom provider ID rather than a missing legacy ID. Preserve MCP tool response shape as markdown in `content[].text`; do not return structured JSON. Do not modify MCP CORS, auth dispatch, `tools/list` category filtering, or Edge Function splitting. For `share-call` integration verification, first check for the dedicated test DB environment (`SUPABASE_TEST_URL`, `SUPABASE_TEST_ANON_KEY`, and `SUPABASE_TEST_SERVICE_ROLE_KEY`, or the repo's documented equivalent dedicated-test variables). If dedicated test DB variables are absent, record `SKIPPED` with the missing variable names and do not run against production. If they are present, run the integration test as blocking; failures must fail the task.</action>
  <verify>
    <automated>bash -lc 'missing=(); for v in SUPABASE_TEST_URL SUPABASE_TEST_ANON_KEY SUPABASE_TEST_SERVICE_ROLE_KEY; do [[ -n "${!v:-}" ]] || missing+=("$v"); done; if (( ${#missing[@]} )); then echo "SKIPPED integration tests: missing dedicated test DB vars ${missing[*]}"; else npm run test:integration -- supabase/functions/share-call/__tests__/share-call.integration.test.ts; fi' && ! rg -n "legacy_recording_id" supabase/functions/share-call supabase/functions/mcp-server/tools/read/get_contact_calls.ts supabase/functions/mcp-server/tools/read/list_shared_calls.ts supabase/functions/mcp-server/tools/write/create_share_link.ts --glob '!**/*.md'</automated>
  </verify>
  <acceptance_criteria>
    - `share-call` queries `recordings.fathom_provider_id`.
    - MCP `get_contact_calls`, `list_shared_calls`, and `create_share_link` use `fathom_provider_id`.
    - MCP tool results remain markdown `content[].text`.
    - Integration test command is run only when dedicated test DB env is configured; unavailable env is recorded as `SKIPPED` with missing vars, and configured-env failures are blocking.
    - Whole-repo `npm run type-check` is intentionally deferred to Plan 05 after both Plan 03 app adapters and Plan 04 Edge adapters complete, so Plan 04 can still run after Plan 02 and in parallel with Plan 03.
  </acceptance_criteria>
  <done>Sharing and MCP bridge tools use the renamed provider-ID field without widening auth/CORS/tool contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external webhook/API request -> Edge Function | Connector and share-call functions receive untrusted HTTP requests and must preserve existing auth checks. |
| MCP client -> MCP tool handlers | MCP tools expose call data through token/category-scoped access and markdown result contracts. |
| integration tests -> Supabase project | Real-DB integration tests must not mutate production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.3.2-11 | Elevation of Privilege | Edge Function auth | mitigate | Rename DB fields only; do not inline auth or change `authenticateRequest` usage. |
| T-06.3.2-12 | Information Disclosure | MCP read tools | mitigate | Preserve token/category filters and markdown `content[].text`; do not add session-cookie data to wildcard-CORS endpoints. |
| T-06.3.2-13 | Tampering | Fathom refresh and connector ingestion | mitigate | Preserve `source_call_id` fallback and provider-owned BIGINT table joins; only rename the canonical recordings column. |
| T-06.3.2-14 | Tampering | integration tests | mitigate | Use dedicated test project guards; never run integration tests against production env fallback. |
| T-06.3.2-SC | Tampering | package installs | accept | No package installation occurs in this plan. |
</threat_model>

<verification>
Run:
- `npm test -- supabase/functions/fathom-refresh/__tests__/fathom-refresh.test.ts`
- `bash -lc 'missing=(); for v in SUPABASE_TEST_URL SUPABASE_TEST_ANON_KEY SUPABASE_TEST_SERVICE_ROLE_KEY; do [[ -n "${!v:-}" ]] || missing+=("$v"); done; if (( ${#missing[@]} )); then echo "SKIPPED integration tests: missing dedicated test DB vars ${missing[*]}"; else npm run test:integration -- supabase/functions/share-call/__tests__/share-call.integration.test.ts supabase/functions/auto-tag-calls/__tests__/auto-tag-calls.integration.test.ts; fi'`
- `! rg -n "legacy_recording_id" supabase/functions --glob '!**/*.md'`

Whole-repo `npm run type-check` is a Plan 05 gate after Plan 03 and Plan 04 are both complete; do not run it as a Plan 04 task-level verifier while Plan 03 may still be adapting app/service/hook/component references.
</verification>

<success_criteria>
- Edge Function and MCP tool source contains no active `legacy_recording_id` references.
- Sharing, Fathom refresh, connector ingestion, and MCP bridge lookups use `fathom_provider_id`.
- `source_call_id`, auth, wildcard MCP CORS, and MCP markdown result shape are preserved.
</success_criteria>

<output>
Create `.planning/phases/06.3.2-fathom-provider-id-rename-rename-legacy-recording-id-across-/06.3.2-04-SUMMARY.md` when done.
</output>
