# Phase 06.3.2-05 Summary

## Status

Complete — production rollout VERIFIED (2026-06-11).

The migration and matching code were confirmed live in production by a prior verification pass. On 2026-06-11 the deferred production DB assertions and smoke checks were run and all passed (see "Production Rollout Gate" below).

## Scope Completed

- Updated active connector-pipeline documentation to use `fathomProviderId`, `fathom_provider_id_numeric`, and `fathom_provider_id` terminology instead of doc-only legacy numeric-ID names.
- Verified active code and docs outside immutable historical migrations no longer contain `legacy_recording_id`.
- Verified `isLegacyId()` remains present as a numeric-vs-UUID format helper.
- Ran final pre-production DB catalog assertions against the same disposable local DB target used for Plan 01 and Plan 02.
- Ran type-check, focused tests, integration-test env preflight, and production build.

## Verification

The Supabase CLI query path was attempted first:

```text
supabase db query --db-url "$SUPABASE_TEST_DB_URL" "SELECT 1 AS ok;"
failed to connect to postgres: tls error (server refused TLS connection)
```

Because this local disposable Postgres container does not accept TLS and this Supabase CLI invocation still attempted TLS even with `sslmode=disable`, the same SQL assertions were run through `docker exec ... psql` against `callvault-phase0632-db`.

DB catalog assertion:

```text
NOTICE: old_columns=0, new_columns=1, stale_views=0, stale_functions=0, stale_indexes=0, stale_constraints=0
```

Exact source/doc gates:

```text
! rg -n "legacy_recording_id" src supabase/functions docs --glob '!docs/archive/**' --glob '!src/types/supabase.ts'
! rg -n "\.legacyId\b|legacyId:" src supabase/functions --glob '!**/*.md'
rg -n "isLegacyId" src/lib/recording-ids.ts src/lib/__tests__/recording-ids.test.ts
```

Type-check:

```text
npm run type-check
> callvault@0.0.0 type-check
> tsc --noEmit
```

Focused tests:

```text
npm test -- src/lib/__tests__/recording-ids.test.ts src/services/__tests__/sync-tab.service.test.ts supabase/functions/fathom-refresh/__tests__/fathom-refresh.test.ts
✓ supabase/functions/fathom-refresh/__tests__/fathom-refresh.test.ts (12 tests)
✓ src/lib/__tests__/recording-ids.test.ts (18 tests)
✓ src/services/__tests__/sync-tab.service.test.ts (5 tests)
Test Files 3 passed
Tests 35 passed
```

Integration-test preflight:

```text
SKIPPED integration tests: missing dedicated test DB vars SUPABASE_TEST_URL SUPABASE_TEST_ANON_KEY SUPABASE_TEST_SERVICE_ROLE_KEY
```

Build:

```text
npm run build
vite v5.4.21 building for production...
✓ 4792 modules transformed.
✓ built in 7.73s
```

Build warnings were limited to existing Vite/CJS, Browserslist age, dynamic/static import chunking, and chunk-size warnings.

## Production Rollout Gate

VERIFIED COMPLETE — 2026-06-11.

DB migration and matching app/Edge code were already live in production (confirmed by prior agent). The deferred assertions and smokes were executed against production on 2026-06-11:

**Production DB assertions (read-only, via `supabase db query --linked`):**

- `fathom_provider_id` exists exactly once in `information_schema.columns` (count = 1).
- `legacy_recording_id` columns: 0. Old-name indexes (`pg_indexes` ILIKE legacy_recording_id): 0. Old-name constraints (by `conname` and `pg_get_constraintdef`): 0.

**Production smoke checks (authed API, test account from `.env`):**

- Call list: `GET /rest/v1/recordings?select=id,title,fathom_provider_id&limit=3` → 3 rows. PASS.
- Search (the app's actual path — PostgREST `or=(title.ilike...,summary.ilike...)`; the `global_search` RPC is dead code, see note): 3 results. PASS.
- Fathom refresh: `POST /functions/v1/fathom-refresh` on a Fathom-sourced recording → `{"success":true, "fathom_provider_id":10690550, ...}`. PASS.
- Share-link list: `GET /rest/v1/call_share_links` → 2 links. PASS. (Create skipped deliberately — `share-call` POST takes `recipient_email` and can send a real email; list path exercises the renamed schema.)
- MCP tools via public worker (`https://mcp.callvaultai.com/mcp`, existing org MCP token): `list_shared_calls` → valid result. `list_contacts` → real contact rows. Both PASS. (Direct-to-Supabase 403 confirmed the internal-secret gate works.)

**Note / deferred:** prod `global_search` RPC still references dropped column `cta.call_recording_id` (42703 at runtime). It is unused by the app (frontend searches via PostgREST ilike) — logged as dead code to clean up, not a rollout blocker.
