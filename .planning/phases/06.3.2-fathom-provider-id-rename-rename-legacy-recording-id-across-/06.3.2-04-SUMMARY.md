# Phase 06.3.2-04 Summary

## Status

Complete.

Plan 04 verified Edge Function and MCP tool references use `fathom_provider_id` for the renamed `recordings` DB column. No Edge/MCP source edits were needed during this plan.

## Scope Verified

- `supabase/functions/_shared/connector-pipeline.ts` writes Fathom external IDs to `fathom_provider_id` and preserves `source_call_id`.
- `supabase/functions/fathom-refresh/index.ts` selects `fathom_provider_id`, preserves the `source_call_id` fallback, and keeps the user-facing missing-provider-ID message.
- `supabase/functions/fetch-meetings/index.ts`, `auto-tag-calls`, `split-recording`, and `share-call` use `fathom_provider_id` for the `recordings` bridge column.
- MCP tools `get_contact_calls`, `list_shared_calls`, and `create_share_link` use `fathom_provider_id`.
- MCP tool responses remain markdown text through `mcpOk`/`mcpError`; no structured JSON result shape was introduced.
- No auth, CORS, Edge Function splitting, or `tools/list` filtering behavior was changed.

## Verification

```text
npm test -- supabase/functions/fathom-refresh/__tests__/fathom-refresh.test.ts
✓ supabase/functions/fathom-refresh/__tests__/fathom-refresh.test.ts (12 tests)
```

```text
SKIPPED integration tests: missing dedicated test DB vars SUPABASE_TEST_URL SUPABASE_TEST_ANON_KEY SUPABASE_TEST_SERVICE_ROLE_KEY
```

Exact stale-reference gate:

```text
! rg -n "legacy_recording_id" supabase/functions --glob '!**/*.md'
```

The gate exited zero.

## Notes

- Existing compatibility terms such as `FATHOM_NO_LEGACY_ID`, `legacyIds`, and `legacyErr` remain where they describe historical error-code or numeric-ID compatibility semantics rather than the renamed DB column.
- No production DB or production integration-test fallback was used.
