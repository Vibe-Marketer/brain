---
phase: 23-reporter-comms-in-app
plan: 05
type: execute
wave: 3
depends_on: [23-01]
files_modified:
  - src/components/notifications/NotificationBell.tsx
  - src/components/notifications/__tests__/NotificationBell.test.tsx
  - src/components/ui/sidebar-nav.tsx
autonomous: false
requirements: [RSP-01]
must_haves:
  truths:
    - "A notification bell mounts in the existing global utility area and opens a popover panel of user_notifications"
    - "An unread count badge shows only when unreadCount > 0, displaying 9+ above 9, with no layout shift"
    - "Clicking a row marks it read via the existing useNotifications mutation"
    - "View report renders ONLY when metadata passes the explicit in-app reporter guard (source==='in_app_user', kind in locked set, ticket_id is string)"
    - "No reporter-facing surface shows raw ticket_messages, runner evidence, paths, SHAs, or internal agent/Autopilot language"
    - "Remix Icons only (RiNotification3Line); no Lucide/FontAwesome; no new npm packages"
  artifacts:
    - path: "src/components/notifications/NotificationBell.tsx"
      provides: "In-app notification bell + popover panel surfacing user_notifications"
      min_lines: 40
    - path: "src/components/ui/sidebar-nav.tsx"
      provides: "Mount point next to SupportPopover"
      contains: "NotificationBell"
  key_links:
    - from: "src/components/notifications/NotificationBell.tsx"
      to: "useNotifications"
      via: "hook consumption for list + markAsRead"
      pattern: "useNotifications"
    - from: "src/components/ui/sidebar-nav.tsx"
      to: "NotificationBell"
      via: "mounted in bottom utility section"
      pattern: "<NotificationBell"
---

<objective>
Surface the in-app reporter comms (RSP-01 "reporter receives a status update") in the UI. RESEARCH found `useNotifications` exists but has NO mounted consumer — the outbox is invisible. This plan adds the notification bell + popover panel per 23-UI-SPEC, mounting it in the existing global utility area (the SidebarNav bottom section, where SupportPopover already lives — the shell has a nav rail + page headers, not a universal top bar, so this is the real closest analog per PATTERNS). The panel renders only customer-safe data and gates every ticket action behind the explicit in-app reporter metadata guard so RLS is never the sole authority on the client.

Purpose: The comms produced by plans 02/04 are worthless if the reporter can't see them. Closes the visible half of RSP-01 and the milestone loop.
Output: NotificationBell component + test + mount. Includes a human-verify checkpoint for the visual surface (UI hint: yes).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-reporter-comms-in-app/23-UI-SPEC.md
@.planning/phases/23-reporter-comms-in-app/23-PATTERNS.md
@src/CLAUDE.md

# Composition analogs — popover trigger, panel, mount point, hook:
@src/components/support/SupportPopover.tsx
@src/components/ui/popover.tsx
@src/hooks/useNotifications.ts
@src/lib/query-config.ts
# Plan 01 added in_app_user to the types the metadata guard checks:
@.planning/phases/23-reporter-comms-in-app/23-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build NotificationBell component (bell + unread badge + popover panel)</name>
  <files>src/components/notifications/NotificationBell.tsx, src/components/notifications/__tests__/NotificationBell.test.tsx</files>
  <action>Per 23-UI-SPEC, create NotificationBell as an icon-only button (32px desktop / 40px touch box, no layout shift) using `RiNotification3Line` from `@remixicon/react` (fallback `RiNotificationLine`), aria-label "Notifications", wrapped in the shadcn `Popover`/`PopoverTrigger`/`PopoverContent` primitives (copy composition from SupportPopover.tsx). Consume `useNotifications()` directly for list + markAsRead (do not re-query raw Supabase). Show an unread-count badge ONLY when unreadCount > 0, rendering "9+" above 9, with accessible text like "N unread updates"; the badge area must be fixed-size so count changes never move adjacent controls. Panel: width 320px desktop / calc(100vw - 16px) mobile, max-height 70vh internal scroll (ScrollArea), header "Updates" with optional "Mark all read" when unread exists, rows showing status marker + title (14px/600) + body (14px/400, 2-line clamp) + relative time (12px tabular-nums). Unread rows use bg-muted/40 + a vibe-orange dot; read rows bg-card. Clicking a row calls markAsRead(notification.id). Define a typed metadata guard `isReporterTicketMetadata(m)` returning true only when m.source==='in_app_user' AND m.kind in {received,in_progress,resolved,escalated} AND typeof m.ticket_id==='string'; render the "View report" text action (vibe-orange) ONLY when that guard passes — otherwise render the row as a generic notification with no ticket action (23-UI-SPEC reporter visibility gate; never infer safety from missing metadata). Use only semantic Tailwind tokens, the typography/spacing scale from 23-UI-SPEC, and Remix Icons. Do not toast each comm. Do not import or render TicketEvidence, runner logs, ticket_messages, or any admin block. For "View report", wire it to the existing reporter-safe ticket view if one is reachable; if not, the action may be omitted this phase (do NOT build a new admin-style dialog for reporters) — keep the guard and action shell so the panel itself is complete.</action>
  <verify>
    <automated>npm test -- src/components/notifications/__tests__/NotificationBell.test.tsx</automated>
  </verify>
  <done>NotificationBell renders bell + conditional unread badge (9+ cap, no shift) + popover panel of notifications; clicking a row marks read; View report gated by the in-app reporter metadata guard; Remix Icons only; component test green.</done>
</task>

<task type="auto">
  <name>Task 2: Mount NotificationBell in the global utility area + render/guard tests</name>
  <files>src/components/ui/sidebar-nav.tsx, src/components/notifications/__tests__/NotificationBell.test.tsx</files>
  <action>Per PATTERNS, mount `<NotificationBell />` in the SidebarNav bottom utility section right next to `<SupportPopover isCollapsed={isCollapsed} />` (sidebar-nav.tsx ~line 237-240), passing the same isCollapsed prop so it follows the rail's collapsed/expanded behavior and reaches both the desktop rail and the mobile nav overlay (both render SidebarNav). Use `PopoverContent side="right"` when mounted in the rail (per UI-SPEC). Keep no-layout-shift sizing in collapsed mode (fixed icon box, no text expansion). Extend the component test to cover: badge hidden when unreadCount===0; badge shows "9+" for 12 unread; a notification with metadata.source!=='in_app_user' renders no "View report" action; a notification with full valid in_app_user metadata renders the action. Render the test with a QueryClientProvider wrapper mocking useNotifications data (mirror existing component test setup conventions).</action>
  <verify>
    <automated>npm test -- src/components/notifications/__tests__/NotificationBell.test.tsx && grep -q "NotificationBell" src/components/ui/sidebar-nav.tsx && echo OK</automated>
  </verify>
  <done>NotificationBell is mounted next to SupportPopover in SidebarNav (reaches desktop rail + mobile nav); tests cover badge thresholds and the View-report metadata gate; all green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The in-app notification bell + popover panel in the global utility area, surfacing reporter comms from user_notifications, with unread badge and the source-gated "View report" action.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3001 (or verify on https://app.callvaultai.com if localhost OAuth blocks).
    2. Confirm the bell icon appears in the sidebar utility area next to the Support button, in both expanded and collapsed rail states, with no layout jump when the unread count changes.
    3. Open the panel: confirm "Updates" header, rows with title/body/relative time, unread rows tinted with a vibe-orange dot, and that clicking a row clears its unread state.
    4. Confirm copy is reassuring and contains NO internal terms (no "agent", "Autopilot", file paths, SHAs).
    5. Take a screenshot of the open panel for the record.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user_notifications row → reporter UI | Client renders comms; RLS scopes rows, but the UI must also fail closed on source metadata before showing ticket actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-15 | Information Disclosure | UI inferring reporter-safe ticket action from ambiguous metadata | mitigate | isReporterTicketMetadata guard requires source==='in_app_user' + valid kind + string ticket_id; otherwise no action (UI-SPEC reporter gate) |
| T-23-16 | Information Disclosure | reporter-facing surface leaking admin/internal content | mitigate | No ticket_messages/TicketEvidence/runner logs rendered; copy already filtered upstream (plans 02/03/04) |
| T-23-17 | Tampering | banned UI libs / new packages | mitigate | Remix Icons only; reuse installed shadcn primitives; zero new npm packages |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages; no install tasks |
</threat_model>

<verification>
- `npm test -- src/components/notifications/__tests__/NotificationBell.test.tsx` green.
- `grep` confirms NotificationBell mounted in sidebar-nav.tsx and RiNotification3Line usage (no Lucide).
- Human-verify checkpoint approved with screenshot.
</verification>

<success_criteria>
- Notification bell + panel mounted and visible in the global utility area, no layout shift.
- Unread badge thresholds correct; row click marks read; View report gated on in-app reporter metadata.
- Remix Icons only, zero new packages; visual checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/23-reporter-comms-in-app/23-05-SUMMARY.md` when done.
</output>
