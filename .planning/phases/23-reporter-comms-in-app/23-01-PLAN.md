---
phase: 23-reporter-comms-in-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260614120000_phase23_source_gate_in_app_user.sql
  - supabase/functions/send-support-ticket/index.ts
  - supabase/functions/send-support-ticket/__tests__/source-stamping.test.ts
  - src/types/supabase.ts
  - src/lib/ticket-display.ts
  - src/lib/__tests__/ticket-display.test.ts
autonomous: true
requirements: [RSP-01]
user_setup:
  - service: supabase
    why: "Apply the enum migration to the linked Supabase project so generated types and downstream waves see in_app_user. Claude runs the migration via Supabase CLI; if the CLI lacks a linked project or DB password, this becomes an auth checkpoint at execution."

must_haves:
  truths:
    - "ticket_source enum includes in_app_user (additive, append-only — existing values untouched)"
    - "send-support-ticket stamps new in-app tickets server-side with source='in_app_user' (D-00 gate substrate)"
    - "The browser request schema still does NOT accept a client-supplied source"
    - "Generated TypeScript ticket_source union and Constants array both include in_app_user"
    - "ticketSourceLabel returns a first-class label for in_app_user (no longer a legacy fallback)"
    - "No historical 'manual' rows are backfilled to in_app_user (D-00 fail-closed)"
  artifacts:
    - path: "supabase/migrations/20260614120000_phase23_source_gate_in_app_user.sql"
      provides: "Additive ALTER TYPE ticket_source ADD VALUE 'in_app_user'"
      contains: "ADD VALUE"
    - path: "supabase/functions/send-support-ticket/index.ts"
      provides: "Server-authoritative source stamp = in_app_user"
      contains: "in_app_user"
    - path: "src/types/supabase.ts"
      provides: "Regenerated ticket_source enum union + Constants"
      contains: "in_app_user"
    - path: "src/lib/ticket-display.ts"
      provides: "First-class in_app_user source label"
      contains: "in_app_user"
  key_links:
    - from: "supabase/functions/send-support-ticket/index.ts"
      to: "tickets.source"
      via: "insert boundary stamp"
      pattern: "source:\\s*['\"]in_app_user['\"]"
    - from: "src/types/supabase.ts"
      to: "ticket_source enum"
      via: "generated union + Constants array"
      pattern: "in_app_user"
---

<objective>
Establish the source-attribution gate substrate that every Phase 23 comms path depends on. Phase 18 added `unknown | nightly_qa | internal` to the `ticket_source` enum but never added `in_app_user`, and `send-support-ticket` still stamps person-reported tickets as `manual`. Per D-00, customer comms fire ONLY for `source='in_app_user'` and fail closed for everything else — so that value must exist in the DB enum, in generated types, and on newly intaken in-app tickets BEFORE any comms trigger or hook can reference it.

Purpose: Without `in_app_user`, D-00 cannot be enforced and Waves 2-3 would have nothing safe to gate on. This is the locked Wave 0 from RESEARCH (Pitfall 1: "the locked gate does not exist yet").
Output: Additive enum migration, server-side stamp change, regenerated types, first-class source label, and updated contract tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-reporter-comms-in-app/23-CONTEXT.md
@.planning/phases/23-reporter-comms-in-app/23-RESEARCH.md
@.planning/phases/23-reporter-comms-in-app/23-PATTERNS.md

# Analog migration to copy enum-extension shape from (additive, append-only):
@supabase/migrations/20260613180000_extend_ticket_source_enum.sql
@supabase/functions/send-support-ticket/index.ts
@supabase/functions/send-support-ticket/__tests__/source-stamping.test.ts
@src/lib/ticket-display.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add in_app_user enum value via additive migration + regenerate types</name>
  <files>supabase/migrations/20260614120000_phase23_source_gate_in_app_user.sql, src/types/supabase.ts</files>
  <action>Per D-00 and the append-only enum invariant (STATE Phase-Spanning Knowledge), create a standalone migration that runs `ALTER TYPE public.ticket_source ADD VALUE IF NOT EXISTS 'in_app_user';`. Mirror the exact shape of the analog `20260613180000_extend_ticket_source_enum.sql` (which added unknown/nightly_qa/internal). Do NOT reorder, rename, or rewrite existing enum values. Do NOT include any UPDATE/backfill of historical `manual` rows — D-00 requires fail-closed; ambiguous `manual` rows must stay customer-silent (RESEARCH Pitfall 2). Then regenerate generated types: run `npx supabase gen types typescript` against the linked project (or the documented project ref) and write to src/types/supabase.ts. If local typegen is unavailable, hand-edit src/types/supabase.ts to add `in_app_user` to BOTH the `Enums.public.ticket_source` union (line ~5752-5755) AND the `Constants.public.Enums.ticket_source` array (line ~5890-5895), matching the generated shape exactly. Verify the result compiles with `npm run build` (use tsconfig.app.json — root tsconfig is hollow per memory).</action>
  <verify>
    <automated>grep -q "ADD VALUE IF NOT EXISTS 'in_app_user'" supabase/migrations/20260614120000_phase23_source_gate_in_app_user.sql && grep -v '^#' src/types/supabase.ts | grep -c "in_app_user" | grep -qv '^0$' && echo OK</automated>
  </verify>
  <done>Migration file exists with additive ADD VALUE for in_app_user and no manual-row backfill; src/types/supabase.ts contains in_app_user in both the union and the Constants array; build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stamp in-app tickets as in_app_user server-side + update contract test</name>
  <files>supabase/functions/send-support-ticket/index.ts, supabase/functions/send-support-ticket/__tests__/source-stamping.test.ts, src/lib/ticket-display.ts, src/lib/__tests__/ticket-display.test.ts</files>
  <behavior>
    - source-stamping test: the insert boundary stamps `source: 'in_app_user'` (was 'manual').
    - source-stamping test: `source` is still ABSENT from supportTicketSchema (no client-supplied source — Spoofing mitigation T-23-01).
    - source-stamping test: negative assertions hold — source is never `payload.source` nor `rawBody.source`.
    - source-stamping test: auth-before-body-parse ordering assertion unchanged.
    - ticket-display test: ticketSourceLabel('in_app_user') returns the first-class label "Reported by a person".
  </behavior>
  <action>Per D-00, change ONLY the server-authoritative insert stamp in send-support-ticket/index.ts (the `.insert({ ... source: 'manual' ... })` boundary around line 186-196) from `'manual'` to `'in_app_user'`. Do NOT add `source` to `supportTicketSchema` — the browser must never supply it. Leave DB-first / Resend-email-side-effect ordering intact. Update the existing source-stamping test: rename the stamp assertion to expect `source: 'in_app_user'`, keep both negative assertions (against payload.source and rawBody.source), keep the schema-denial test and the auth-before-parse test. In ticket-display.ts, move `in_app_user` from `LEGACY_TICKET_SOURCE_LABELS` into the first-class `TICKET_SOURCE_LABELS` record (label "Reported by a person") now that the type includes it; add/extend a ticket-display test asserting ticketSourceLabel('in_app_user') returns that label. Do NOT touch describeTicketEvent (it says "Autopilot" and is admin-only — not for reporter copy).</action>
  <verify>
    <automated>npm test -- supabase/functions/send-support-ticket/__tests__/source-stamping.test.ts src/lib/__tests__/ticket-display.test.ts</automated>
  </verify>
  <done>send-support-ticket stamps in_app_user at the insert boundary with source kept out of the request schema; source-stamping and ticket-display tests pass; in_app_user is a first-class source label.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → send-support-ticket Edge Function | Untrusted client body crosses here; client must not be able to set ticket source |
| Edge Function → tickets.source | Server-authoritative write; the stamp is the authority for the entire D-00 gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Spoofing | send-support-ticket request schema | mitigate | Keep `source` absent from supportTicketSchema; server stamps `in_app_user`; test asserts schema has no source and stamp is not payload/rawBody-derived (D-00) |
| T-23-02 | Tampering | ticket_source enum migration | mitigate | Additive `ADD VALUE` only; never reorder/rewrite existing values (append-only invariant); no manual-row backfill (fail-closed) |
| T-23-03 | Information Disclosure | historical 'manual' rows | accept | D-00 fail-closed — ambiguous manual rows stay customer-silent; no backfill, so they never enter the in_app_user gate |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages this phase (locked roadmap constraint); no install tasks; slopcheck N/A |
</threat_model>

<verification>
- `npm test -- supabase/functions/send-support-ticket/__tests__/source-stamping.test.ts src/lib/__tests__/ticket-display.test.ts` green.
- `grep` confirms the additive enum migration and the in_app_user presence in generated types.
- `npm run build` (tsconfig.app.json) compiles with the new enum value.
</verification>

<success_criteria>
- ticket_source enum includes in_app_user (additive); existing values untouched; no manual backfill.
- send-support-ticket stamps in_app_user server-side; schema rejects client source.
- Generated types + first-class source label updated; all touched tests green.
</success_criteria>

<output>
Create `.planning/phases/23-reporter-comms-in-app/23-01-SUMMARY.md` when done.
</output>
