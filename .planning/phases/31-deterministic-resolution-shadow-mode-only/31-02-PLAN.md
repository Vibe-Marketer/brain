---
phase: 31-deterministic-resolution-shadow-mode-only
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql
  - supabase/migrations/20260901000004_event_resolution_sweep_cron.sql
  - src/test/event-match-apply-reverse.integration.test.ts
autonomous: true
requirements: [MATCH-10, SAFE-02]

must_haves:
  truths:
    - "apply_event_match_atomic sets recordings.event_id on BOTH recordings to one event and writes a merge_applied ledger row, all in one transaction"
    - "reverse_event_match_atomic sets both recordings' event_id back to NULL and writes a 'reversed' ledger row whose reverses_decision_id points at the applied decision, all in one transaction"
    - "Both RPCs are SECURITY DEFINER, SET search_path=public, and have EXECUTE revoked from PUBLIC/anon/authenticated (service-role only)"
    - "The apply/reverse pair is built and proven by a DIRECT test — it is NEVER called by the automatic shadow sweep (SAFE-02)"
    - "A pg_cron job 'event-resolution-sweep' calls resolve-events on a timer, mirroring fathom-daily-reconcile, degrading gracefully when pg_cron is unavailable"
  artifacts:
    - path: "supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql"
      provides: "The atomic reversible merge/reverse RPC pair (MATCH-10)"
      contains: "CREATE OR REPLACE FUNCTION public.apply_event_match_atomic"
    - path: "supabase/migrations/20260901000004_event_resolution_sweep_cron.sql"
      provides: "pg_cron schedule triggering the shadow sweep"
      contains: "event-resolution-sweep"
    - path: "src/test/event-match-apply-reverse.integration.test.ts"
      provides: "Direct round-trip proof of apply then reverse + ledger entries"
  key_links:
    - from: "apply_event_match_atomic"
      to: "recordings.event_id + event_match_decisions"
      via: "one SECURITY DEFINER transaction"
      pattern: "SECURITY DEFINER"
    - from: "reverse_event_match_atomic"
      to: "event_match_decisions.reverses_decision_id"
      via: "reversal row references the applied decision"
      pattern: "reverses_decision_id"
    - from: "cron event-resolution-sweep"
      to: "/functions/v1/resolve-events"
      via: "net.http_post with X-Reconcile-Secret"
      pattern: "resolve-events"
---

<objective>
Expand the tracer with the reversibility mechanism and the automation that will drive it. It authors the atomic apply/reverse RPC pair (MATCH-10) — built and proven, but deliberately NOT wired to the automatic sweep (SAFE-02) — and the `pg_cron` schedule that periodically invokes `resolve-events`. Both migrations land together in one TEST `db push` (they are the only Wave-2 plan that applies migrations, so no concurrent-`db push` race).

The apply/reverse pair exists so MATCH-10 is provably correct and ready for Phase 32, NOT so shadow mode can use it. The single most important review property here: no code path lets the sweep call `apply_event_match_atomic`.

Purpose: Prove that any merge is reversible in one atomic operation (the `split_recording_atomic` guarantee), and schedule the shadow sweep to run on its own without touching the hot ingest path.
Output: Two migrations applied to TEST, and a direct round-trip integration test proving apply→reverse restores `recordings.event_id` to NULL with both actions logged.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-deterministic-resolution-shadow-mode-only/31-RESEARCH.md
@.planning/phases/31-deterministic-resolution-shadow-mode-only/31-01-SUMMARY.md
@supabase/CLAUDE.md

Precedent files to READ and mirror verbatim (do NOT invent structure):
@supabase/migrations/20260309220000_split_recording_rpc.sql
@supabase/migrations/20260512000002_fathom_daily_reconcile_cron.sql

<interfaces>
<!-- split_recording_atomic — the exact MATCH-10 precedent (verified, 20260309220000):
  CREATE OR REPLACE FUNCTION public.split_recording_atomic(...params...) RETURNS UUID
    LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$ ... $$;
  Ownership validated BY PARAMETER (IF NOT EXISTS (SELECT 1 FROM recordings WHERE id=... AND owner_user_id=p_owner_user_id) THEN RAISE EXCEPTION);
  REVOKE EXECUTE ON FUNCTION public.split_recording_atomic FROM PUBLIC; FROM anon; FROM authenticated;
  service_role bypasses EXECUTE grants — edge-function calls still work. -->

<!-- Locked signatures from 31-01 Task 1 gate (carry verbatim):
  apply_event_match_atomic(p_recording_id_a UUID, p_recording_id_b UUID, p_event_id UUID, p_decided_by TEXT, p_signals JSONB, p_owner_user_id UUID) RETURNS UUID
  reverse_event_match_atomic(p_decision_id UUID, p_owner_user_id UUID) RETURNS void -->

<!-- events table (live, Phase 30): id UUID PK, canonical_start_time, canonical_end_time,
  resolution_confidence, created_at, updated_at. NO organization_id. -->
</interfaces>

Locked constraint (31-CONTEXT.md, 31-RESEARCH.md Pitfall 2): the automatic shadow sweep computes-and-records ONLY. apply/reverse is a separate, service-role-only, directly-callable capability — never invoked by resolve-events.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the apply/reverse RPC pair + the sweep cron, apply both to TEST</name>
  <files>supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql, supabase/migrations/20260901000004_event_resolution_sweep_cron.sql</files>
  <read_first>
    - supabase/migrations/20260309220000_split_recording_rpc.sql (mirror EXACTLY: SECURITY DEFINER, SET search_path=public, ownership-by-parameter RAISE EXCEPTION, the three REVOKE EXECUTE lines)
    - supabase/migrations/20260512000002_fathom_daily_reconcile_cron.sql (mirror EXACTLY: CREATE EXTENSION IF NOT EXISTS pg_net/pg_cron; unschedule-first idempotency; cron.schedule with net.http_post + X-Reconcile-Secret; EXCEPTION WHEN undefined_function graceful NOTICE; the operator-runbook comment block for the app.supabase_url / app.reconcile_secret GUCs)
    - supabase/CLAUDE.md (migration structure/banners)
    - .planning/phases/31-deterministic-resolution-shadow-mode-only/31-01-SUMMARY.md (the locked RPC signature + any option-b knobs)
    - .planning/phases/31-deterministic-resolution-shadow-mode-only/31-RESEARCH.md ("Pattern 1", "Pattern 4", "Common Pitfalls 2")
  </read_first>
  <action>
    Author `20260901000003_create_event_match_apply_reverse_rpcs.sql`. `apply_event_match_atomic(p_recording_id_a UUID, p_recording_id_b UUID, p_event_id UUID, p_decided_by TEXT, p_signals JSONB, p_owner_user_id UUID) RETURNS UUID`, LANGUAGE plpgsql, SECURITY DEFINER, SET search_path = public: validate ownership of BOTH recordings by parameter (RAISE EXCEPTION if either recording's owner_user_id <> p_owner_user_id, mirroring split_recording_atomic — never auth.uid()); in one transaction, if p_event_id IS NULL create an events row (canonical_start_time/canonical_end_time may be derived from the two recordings' recording_start_time/recording_end_time, resolution_confidence left NULL) and use its id, else use p_event_id; UPDATE recordings SET event_id = <event id> WHERE id IN (p_recording_id_a, p_recording_id_b); INSERT INTO event_match_decisions (recording_id_a, recording_id_b canonical-ordered, event_id, tier='deterministic', score=NULL, signals=p_signals, decision='merge_applied', decided_by=p_decided_by, applied=true); RETURN the event id. `reverse_event_match_atomic(p_decision_id UUID, p_owner_user_id UUID) RETURNS void`, same guards: load the referenced merge_applied decision, validate ownership of both its recordings by parameter, in one transaction UPDATE recordings SET event_id = NULL WHERE id IN (that decision's two recordings), and INSERT INTO event_match_decisions (same pair, tier, decision='reversed', decided_by='admin' or the caller's actor, applied=false, reverses_decision_id = p_decision_id). Add the three `REVOKE EXECUTE ... FROM PUBLIC; FROM anon; FROM authenticated;` lines for BOTH functions. Add COMMENTs noting these are service-role-only and MUST NOT be called by the automatic shadow sweep (SAFE-02).

    Author `20260901000004_event_resolution_sweep_cron.sql` by copying `20260512000002_fathom_daily_reconcile_cron.sql`'s structure exactly, changing: job name to 'event-resolution-sweep'; schedule to '*/15 * * * *' (conservative — shadow has no latency requirement, Assumption A2, tunable later); target URL to `.../functions/v1/resolve-events`; body to `'{"mode": "shadow"}'::jsonb`. Keep the CREATE EXTENSION IF NOT EXISTS lines, the unschedule-first idempotency, the EXCEPTION WHEN undefined_function graceful NOTICE, and the operator-runbook comment for the app.supabase_url / app.reconcile_secret GUCs (same deferred-manual pattern as the existing crons — STATE.md's resume-heartbeat GUC).

    Apply BOTH migrations to the TEST project only (never the prod ref vltmrnjsubfzrgrtdqey). Confirm `cron.schedule` registered (or NOTICE'd gracefully) and both functions exist with EXECUTE revoked from PUBLIC/anon/authenticated (`SELECT proname FROM pg_proc` / check `has_function_privilege`).
  </action>
  <acceptance_criteria>
    - Both functions are SECURITY DEFINER with SET search_path = public, validate ownership by parameter, and have EXECUTE revoked from PUBLIC, anon, and authenticated (three REVOKE lines each).
    - apply writes decision='merge_applied' applied=true and sets both recordings' event_id; reverse writes decision='reversed' with reverses_decision_id set and nulls both event_ids.
    - The cron migration names the job 'event-resolution-sweep', posts to /functions/v1/resolve-events with the X-Reconcile-Secret header and body {"mode":"shadow"}, and degrades gracefully via EXCEPTION WHEN undefined_function.
    - Both migrations applied cleanly to TEST; NOT to prod.
    <automated>grep -q "CREATE OR REPLACE FUNCTION public.apply_event_match_atomic" supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql && grep -q "CREATE OR REPLACE FUNCTION public.reverse_event_match_atomic" supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql && grep -q "REVOKE EXECUTE" supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql && grep -q "authenticated" supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql && grep -q "SET search_path = public" supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql && grep -q "event-resolution-sweep" supabase/migrations/20260901000004_event_resolution_sweep_cron.sql && grep -q "resolve-events" supabase/migrations/20260901000004_event_resolution_sweep_cron.sql && echo RPC_CRON_OK</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -q "apply_event_match_atomic" supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql && grep -q "reverse_event_match_atomic" supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql && grep -q "event-resolution-sweep" supabase/migrations/20260901000004_event_resolution_sweep_cron.sql && echo OK</automated>
  </verify>
  <done>The atomic apply/reverse pair and the sweep cron exist on TEST with the correct security posture. Task 2 proves the round-trip.</done>
</task>

<task type="auto">
  <name>Task 2: Prove apply→reverse round-trips atomically, called directly (never via the sweep)</name>
  <files>src/test/event-match-apply-reverse.integration.test.ts</files>
  <read_first>
    - src/test/event-schema-noop.integration.test.ts (fixture + cleanup pattern to mirror)
    - supabase/CLAUDE.md ("Running integration tests safely", cleanup contract)
    - supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql (the signatures under test, from Task 1)
  </read_first>
  <action>
    Write `src/test/event-match-apply-reverse.integration.test.ts` (real TEST DB, describe.skipIf(!integrationDbReachable), full afterAll cleanup). Seed one org, one owner user, two recordings owned by that user (event_id NULL). Using the service-role client, call `apply_event_match_atomic` with the two recording IDs, p_event_id=NULL, p_decided_by='admin', p_signals a small jsonb, p_owner_user_id=<owner>. Assert: it returns an event id; BOTH recordings now have that event_id; exactly one event_match_decisions row with decision='merge_applied', applied=true. Then call `reverse_event_match_atomic(p_decision_id=<that applied row id>, p_owner_user_id=<owner>)`. Assert: BOTH recordings' event_id is back to NULL; a new event_match_decisions row exists with decision='reversed' and reverses_decision_id = the applied row id. Add a negative assertion that calling apply with a p_owner_user_id that does NOT own the recordings raises an error (ownership-by-parameter guard). Add an explicit test-level assertion / comment documenting the SAFE-02 boundary: this RPC is exercised directly here and is NOT reachable from resolve-events (the sweep). Ensure the suite runs GREEN on TEST (not skipped).
  </action>
  <acceptance_criteria>
    - apply→reverse round-trips recordings.event_id from NULL → set → NULL, atomically, with a merge_applied row then a reversed row (reverses_decision_id linking them).
    - Calling apply with a non-owner p_owner_user_id raises (ownership guard proven).
    - The test runs GREEN on TEST (not skipped) and documents that the RPC is called directly, never by the sweep.
    <automated>npm run test:integration -- event-match-apply-reverse.integration.test.ts && echo APPLY_REVERSE_GREEN</automated>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- event-match-apply-reverse.integration.test.ts && echo OK</automated>
  </verify>
  <done>MATCH-10 is proven: every merge is reversible in one atomic operation with the reversal recorded in the same ledger — and the sweep never touches it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (PostgREST) -> apply/reverse RPC | A client must NOT be able to call the merge/reverse RPCs directly |
| automatic sweep -> apply RPC | SAFE-02 boundary: the sweep must NEVER call the apply half |
| cron / third party -> resolve-events | Shared-secret gate (reiterated for the cron wiring) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02-01 | Elevation of Privilege | apply/reverse RPCs | mitigate | SECURITY DEFINER + SET search_path=public + REVOKE EXECUTE FROM PUBLIC/anon/authenticated + ownership validated by parameter (never auth.uid()). Mirrors split_recording_atomic exactly. HIGH — blocks if any REVOKE is missing or search_path is unset. |
| T-31-02-02 | Information Disclosure / data corruption | sweep ↔ apply coupling | mitigate | The automatic sweep (resolve-events / runShadowSweep) never calls apply_event_match_atomic; the RPC is proven by a direct test only (Pitfall 2). Grep-absence enforced in 31-01 Task 3 acceptance; code review confirms no sweep path reaches apply. HIGH. |
| T-31-02-03 | Spoofing | cron -> resolve-events | mitigate | cron posts the X-Reconcile-Secret header (app.reconcile_secret GUC) matching RECONCILE_SECRET on the function; unauthenticated third-party invocations are 401'd. HIGH. |
| T-31-02-04 | Tampering | wrong-database apply | mitigate | Both migrations applied to the TEST project only this plan; prod apply is Plan 04's guarded, human-gated step (prod-ref vltmrnjsubfzrgrtdqey verified there). |
| T-31-02-SC | Tampering | package installs | accept | Zero new packages (31-RESEARCH.md Package Legitimacy Audit: N/A). |
</threat_model>

## Artifacts this phase produces (Plan 02)

- **New RPC functions:** `apply_event_match_atomic(p_recording_id_a, p_recording_id_b, p_event_id, p_decided_by, p_signals, p_owner_user_id) RETURNS UUID`; `reverse_event_match_atomic(p_decision_id, p_owner_user_id) RETURNS void` — both SECURITY DEFINER, service-role only
- **New pg_cron job:** `event-resolution-sweep` (every 15 min → `net.http_post` to `/functions/v1/resolve-events`, `{"mode":"shadow"}`)
- **New migration files:** `20260901000003_create_event_match_apply_reverse_rpcs.sql`, `20260901000004_event_resolution_sweep_cron.sql`
- **New test file:** `src/test/event-match-apply-reverse.integration.test.ts`
- **Deferred operator setup (Phase 32 prerequisite, non-blocking this phase):** GUCs `app.supabase_url` + `app.reconcile_secret` and the edge function's `RECONCILE_SECRET` — same manual dashboard pattern as the existing crons; until set, the cron simply does not fire (safe — flag off)

<verification>
- Both RPCs SECURITY DEFINER + search_path=public + 3× REVOKE EXECUTE each; ownership by parameter.
- apply sets both event_ids + merge_applied row; reverse nulls both + reversed row with reverses_decision_id.
- cron job 'event-resolution-sweep' targets resolve-events with the shared-secret header; graceful when pg_cron absent.
- Round-trip integration test GREEN on TEST; non-owner apply raises; RPC never called by the sweep.
- Both migrations on TEST only; NOT prod.
</verification>

<success_criteria>
MATCH-10 (atomic reversible merge, reversal recorded in the same ledger) is TRUE and proven on TEST, and SAFE-02's automation (the shadow sweep runs on a cron without ever applying a merge) is in place — with the apply/reverse capability built but firewalled from the automatic path.
</success_criteria>

<output>
Create `.planning/phases/31-deterministic-resolution-shadow-mode-only/31-02-SUMMARY.md` when done. Note the GUC/secret deferral for Plan 04.
</output>
