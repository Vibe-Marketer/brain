---
phase: 31-deterministic-resolution-shadow-mode-only
plan: 04
type: execute
wave: 3
depends_on: ["31-01", "31-02", "31-03"]
files_modified:
  - src/types/supabase.ts
  - type-baseline.json
autonomous: false
requirements: [MATCH-01, MATCH-09, MATCH-10, SAFE-01, SAFE-02]

user_setup:
  - service: supabase
    why: "Cron-triggered sweep authentication (non-blocking this phase — flag is off for every org, so the sweep does nothing until Phase 32 enables an org)"
    env_vars:
      - name: RECONCILE_SECRET
        source: "Set as an Edge Function secret: supabase secrets set RECONCILE_SECRET=<random-32-byte-hex>; must equal the app.reconcile_secret DB GUC"
    dashboard_config:
      - task: "Set app.supabase_url and app.reconcile_secret DB GUCs (ALTER DATABASE postgres SET ...; SELECT pg_reload_conf();) so the event-resolution-sweep cron can call resolve-events — same manual pattern as the existing crons"
        location: "Supabase Dashboard -> Settings -> Database -> Custom postgres settings (or SQL editor)"

must_haves:
  truths:
    - "All four Phase-31 migrations are applied to production (ref vltmrnjsubfzrgrtdqey) ONLY after Plans 01-03 tests are green on TEST and a human approved the prod touch"
    - "event_match_decisions, organization_feature_flags, and the apply/reverse RPCs + event-resolution-sweep cron exist in production"
    - "The resolve-events edge function is deployed to production"
    - "No production behavior changed: no org has an enabled flag, so the sweep writes zero rows; recordings.event_id is NULL across the board in prod"
    - "src/types/supabase.ts is regenerated from prod and now includes event_match_decisions + organization_feature_flags"
  artifacts:
    - path: "src/types/supabase.ts"
      provides: "Types re-synced from prod including the two new tables"
      contains: "event_match_decisions"
  key_links:
    - from: "prod migration apply"
      to: ".env DATABASE_URL prod-ref guard"
      via: "guarded runner verifies vltmrnjsubfzrgrtdqey before connecting"
      pattern: "vltmrnjsubfzrgrtdqey"
---

<objective>
Land the proven Phase-31 mechanism in production and re-sync the committed types. This is the one high-stakes action of the phase, isolated as its own gated plan and mirroring Phase 30 Plan 04 exactly: additive DDL + two SECURITY DEFINER RPCs + a cron + an edge function against the LIVE production database on the `v2.2-event-resolution` branch. It runs ONLY after Plans 01-03's tests are green on TEST and ONLY after a human approves the prod touch (branch discipline: v2.2 touches prod only once proven and Andrew is comfortable).

Everything shipped here is inert in prod: no organization has an `organization_feature_flags` row with `enabled=true`, so even if the cron fires, `resolve-events` finds zero flagged orgs and writes nothing — and by SAFE-02 the sweep never writes `recordings.event_id` regardless. Phase 32 flips the flag for a single org only after SAFE-06 precision is proven.

Purpose: The shadow-resolution mechanism must exist in prod for Phase 32 to enable and measure — proven-then-applied, never applied-then-hoped.
Output: Four migrations applied to prod (guarded), `resolve-events` deployed to prod, `src/types/supabase.ts` regenerated from prod.
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/CLAUDE.md
@supabase/migrations/20260901000001_create_organization_feature_flags.sql
@supabase/migrations/20260901000002_create_event_match_decisions.sql
@supabase/migrations/20260901000003_create_event_match_apply_reverse_rpcs.sql
@supabase/migrations/20260901000004_event_resolution_sweep_cron.sql
@.planning/phases/31-deterministic-resolution-shadow-mode-only/31-01-SUMMARY.md
@.planning/phases/31-deterministic-resolution-shadow-mode-only/31-02-SUMMARY.md
@.planning/phases/31-deterministic-resolution-shadow-mode-only/31-03-SUMMARY.md

Guarded apply rule (root CLAUDE.md + supabase/CLAUDE.md, non-negotiable): anything applying a migration or DDL to production reads `DATABASE_URL` from `.env` (the PROD file — NOT `.env.local`/`.env.test`) and MUST verify the URL contains the prod ref `vltmrnjsubfzrgrtdqey` before connecting. The autopilot migration runner (`~/dev/autopilot/src/gsd-runner.ts`) implements exactly this guard. Confusing the env files is the 2026-05 prod-mutation incident class documented in supabase/CLAUDE.md. Edge Function deploys use `supabase functions deploy resolve-events --use-api` (Docker is not running).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve applying the Phase-31 migrations + resolve-events to production</name>
  <files>none — approval gate; gates the prod apply/deploy performed in Task 2</files>
  <read_first>
    - .planning/phases/31-deterministic-resolution-shadow-mode-only/31-01-SUMMARY.md (confirm the shadow integration test ran GREEN, not skipped)
    - .planning/phases/31-deterministic-resolution-shadow-mode-only/31-02-SUMMARY.md (confirm the apply/reverse round-trip test ran GREEN)
    - .planning/phases/31-deterministic-resolution-shadow-mode-only/31-03-SUMMARY.md (confirm the client-deny isolation assertions ran GREEN)
    - supabase/migrations/20260901000001_create_organization_feature_flags.sql, 20260901000002_create_event_match_decisions.sql, 20260901000003_create_event_match_apply_reverse_rpcs.sql, 20260901000004_event_resolution_sweep_cron.sql (the exact DDL being applied)
  </read_first>
  <action>Present the what-built and how-to-verify below, then PAUSE for explicit human approval before Task 2 touches prod. Do not proceed if any of Plans 01-03's tests were skipped rather than green.</action>
  <what-built>
    Four additive migrations + one edge function, all passing on the TEST project: the per-org `organization_feature_flags` table (off by default), the append-only `event_match_decisions` ledger, the `apply_event_match_atomic`/`reverse_event_match_atomic` RPC pair (service-role only), the `event-resolution-sweep` pg_cron job, and the `resolve-events` edge function. Proven by test on TEST: (a) a shared Zoom identifier produces exactly one merge_proposed ledger row while recordings.event_id stays NULL (shadow noop, SAFE-02); (b) the flag gate blocks unflagged orgs (SAFE-01); (c) apply→reverse round-trips event_id atomically with both actions logged (MATCH-10); (d) both new tables are invisible to any authenticated JWT (CLIENT_DENY_TABLES). Nothing enables the flag; the mechanism is inert in prod on apply.
  </what-built>
  <how-to-verify>
    1. Open the three SUMMARYs and confirm the shadow, apply/reverse, and isolation tests each ran GREEN on the TEST project (not skipped for missing env). If any were skipped, do NOT approve — the "proven by test" bar is not met.
    2. Skim the four migrations: additive only (CREATE TABLE IF NOT EXISTS / CREATE OR REPLACE FUNCTION / cron.schedule), no DROP/RENAME, both RPCs have REVOKE EXECUTE from PUBLIC/anon/authenticated, both new tables have RLS enabled+forced with no client policy, and the flag defaults to enabled=false with no broad-enable path.
    3. Confirm you want prod DDL applied + resolve-events deployed now on the `v2.2-event-resolution` branch, understanding the mechanism is inert until Phase 32 enables a single org after precision is proven.
  </how-to-verify>
  <acceptance_criteria>
    - All three SUMMARYs confirm green (not skipped) TEST-project runs for the shadow, apply/reverse, and isolation tests.
    - A human has explicitly approved the prod apply + edge deploy.
  </acceptance_criteria>
  <verify>Human verification gate — no automated check. Task 2 must not run until approval is given.</verify>
  <done>A human confirmed the TEST-green evidence and approved applying the four migrations + deploying resolve-events to production.</done>
  <resume-signal>Type "approved" to apply to prod + deploy, or describe what to hold on.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply to prod (guarded), deploy resolve-events, regenerate types from prod</name>
  <files>src/types/supabase.ts, type-baseline.json</files>
  <read_first>
    - supabase/CLAUDE.md (the guarded apply process, the prod-ref requirement, and "Deploying Edge Functions" → always `--use-api`)
    - scripts/type-check.mjs (baseline diff mechanism — same handling as Phase 30 Plan 04)
    - supabase/SCHEMA_TRUTH.md (regenerate via --linked, never replay migrations)
  </read_first>
  <action>
    Apply the four Phase-31 migrations (20260901000001, 20260901000002, 20260901000003, 20260901000004) to PRODUCTION via the repo's guarded path: verify `DATABASE_URL` in `.env` contains the prod ref `vltmrnjsubfzrgrtdqey` BEFORE connecting (use the autopilot gsd-runner guarded apply or an equivalent that performs the same prod-ref check — do NOT apply blind). All migrations are IF NOT EXISTS / CREATE OR REPLACE so a re-run is harmless. Confirm the apply succeeded. Deploy the edge function with `supabase functions deploy resolve-events --use-api`. Then regenerate the committed types from prod: `supabase gen types typescript --linked > src/types/supabase.ts` — the file must now gain `event_match_decisions` and `organization_feature_flags` Row types. Run `npm run type-check`; if the new tables shift the baseline, read what shifted, confirm no real regression, then run `npm run type-check:update-baseline` deliberately. Do NOT change any application code — only `src/types/supabase.ts` and (conditionally) `type-baseline.json`. Do NOT insert any organization_feature_flags row (no org is enabled this phase). Record the prod-ref guard confirmation and the apply/deploy results in the SUMMARY. Note the RECONCILE_SECRET / GUC setup as a Phase-32 prerequisite (non-blocking now — flag off).
  </action>
  <acceptance_criteria>
    - The prod-ref guard confirmed `vltmrnjsubfzrgrtdqey` before the apply (recorded in the SUMMARY).
    - Post-apply prod introspection (via the regenerated types) shows `event_match_decisions` and `organization_feature_flags` exist; the apply/reverse RPCs and the cron job exist in prod.
    - `resolve-events` is deployed to prod.
    - `src/types/supabase.ts` contains `event_match_decisions` and `organization_feature_flags`; `npm run type-check` exits 0.
    - No organization_feature_flags row with enabled=true exists in prod (mechanism inert).
    <automated>grep -q "event_match_decisions" src/types/supabase.ts && grep -q "organization_feature_flags" src/types/supabase.ts && npm run type-check</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -q "event_match_decisions" src/types/supabase.ts && grep -q "organization_feature_flags" src/types/supabase.ts && npm run type-check && echo PROD_TYPES_OK</automated>
  </verify>
  <done>The four migrations are live in prod (guarded), resolve-events is deployed, the committed types reflect the two new tables, and type-check passes. The shadow-resolution mechanism exists in prod, inert (no org enabled), with zero behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor -> production DB | Applying DDL + SECURITY DEFINER RPCs to live prod with real customer data — the single highest-stakes boundary in the phase |
| .env selection -> which project | Wrong env file = wrong database (the 2026-05 incident class) |
| deployed resolve-events -> prod data | A live cron-triggered function against prod — must be inert (no enabled flag) and shadow-only (SAFE-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04-01 | Tampering | wrong-database apply | mitigate | Guarded apply reads `DATABASE_URL` from `.env` and verifies prod ref `vltmrnjsubfzrgrtdqey` before connecting (autopilot gsd-runner). NEVER `.env.local`/`.env.test`. HIGH — blocks apply if the guard does not confirm the prod ref. |
| T-31-04-02 | Elevation of Privilege / Information Disclosure | applying unproven RPCs/RLS to prod | mitigate | Gated by Task 1's human checkpoint, which requires Plans 01-03's tests GREEN (not skipped) on TEST — including the apply/reverse REVOKE posture and the client-deny isolation. Unproven code never reaches prod. HIGH. |
| T-31-04-03 | Denial of Service / behavior change | shipping a live sweep to prod | mitigate | All DDL additive; no org has enabled=true, so resolve-events finds zero flagged orgs and does nothing; SAFE-02 means the sweep never writes recordings.event_id even if it ran; no NOT NULL backfill or lock-heavy op. Proven inert by Plan 01's flag-gate + noop tests. HIGH. |
| T-31-04-04 | Spoofing | deployed resolve-events endpoint | mitigate | The function 401s any request without a matching X-Reconcile-Secret; until the GUC/secret are set the cron cannot even reach it (safe). Documented as a Phase-32 prerequisite. MEDIUM. |
| T-31-04-05 | Repudiation | undocumented prod change | mitigate | Guard confirmation + apply/deploy results recorded in the SUMMARY; the four migration files are the durable record; types re-committed from prod. |
| T-31-04-SC | Tampering | package installs | accept | No package installs this phase (31-RESEARCH.md Package Legitimacy Audit: N/A). |
</threat_model>

## Artifacts this phase produces (Plan 04)

- **Production schema change:** `organization_feature_flags` + `event_match_decisions` tables, `apply_event_match_atomic`/`reverse_event_match_atomic` RPCs, and the `event-resolution-sweep` pg_cron job become live in prod (ref `vltmrnjsubfzrgrtdqey`)
- **Production deploy:** `resolve-events` edge function deployed (`--use-api`)
- **Modified file:** `src/types/supabase.ts` (regenerated from prod — now includes both new tables)
- **Conditionally modified:** `type-baseline.json` (if the new tables shifted the baseline)
- **No new migration file** (Plans 01/02 authored all four; this plan applies them) and **no new RPCs** (Plan 02 authored them)
- **Deferred (Phase-32 prerequisite, non-blocking):** `RECONCILE_SECRET` secret + `app.supabase_url`/`app.reconcile_secret` GUCs

<verification>
- Prod-ref guard confirmed `vltmrnjsubfzrgrtdqey` before apply.
- Four migrations applied to prod cleanly; both tables + both RPCs + the cron job exist in prod.
- resolve-events deployed to prod.
- `src/types/supabase.ts` regenerated from prod contains both new table Row types; `npm run type-check` exits 0.
- No org has enabled=true; no application code changed.
</verification>

<success_criteria>
The deterministic shadow-resolution mechanism (MATCH-01, MATCH-09, MATCH-10, SAFE-01, SAFE-02) exists truthfully in production, inert and safe (flag off for every org, event_id NULL across the board), with zero behavior change — ready for Phase 32 to enable and measure precision. The whole phase goal is met.
</success_criteria>

<output>
Create `.planning/phases/31-deterministic-resolution-shadow-mode-only/31-04-SUMMARY.md` when done. Record the prod-ref guard confirmation, the apply + deploy results, and the RECONCILE_SECRET/GUC deferral.
</output>
