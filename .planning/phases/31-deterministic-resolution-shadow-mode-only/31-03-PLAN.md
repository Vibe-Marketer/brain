---
phase: 31-deterministic-resolution-shadow-mode-only
plan: 03
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - src/test/rls-regression.test.ts
autonomous: true
requirements: [MATCH-09, SAFE-01]

must_haves:
  truths:
    - "event_match_decisions is registered as a client-deny table: an authenticated JWT reads ZERO rows even when the service role has seeded a real row"
    - "organization_feature_flags is registered as a client-deny table: an authenticated JWT reads ZERO rows even when the service role has seeded a row"
    - "The registration lives in the CI-enforced rls-regression suite so a future permissive-policy mistake fails the build"
  artifacts:
    - path: "src/test/rls-regression.test.ts"
      provides: "CLIENT_DENY_TABLES registration + seeded cross-JWT isolation assertions for both new tables"
      contains: "event_match_decisions"
  key_links:
    - from: "rls-regression CLIENT_DENY_TABLES"
      to: "event_match_decisions + organization_feature_flags"
      via: "authenticated JWT reads zero rows despite a service-role-seeded row"
      pattern: "event_match_decisions"
---

<objective>
Wire the two new backend-control tables into the CI-enforced cross-org isolation safety net so their service-role-only posture is guaranteed forever, not just asserted once. `event_match_decisions` and `organization_feature_flags` have RLS enabled with no client policy (deny-by-default); this plan proves — from real authenticated JWTs against TEST — that a signed-in client reads ZERO rows from either even when the service role has seeded one. It also documents WHY these belong in `CLIENT_DENY_TABLES` and NOT `CROSS_ORG_TABLES` (both structurally, and — for the ledger — because it has two recording FKs, not one org pivot).

Purpose: A false or premature merge is a data-exposure incident; the ledger and the enable-flag must be invisible and unflippable from any client JWT. This is the CI teeth behind SAFE-01/SAFE-02.
Output: `src/test/rls-regression.test.ts` extended and green on TEST. No migration, no `db push` (runs parallel to Plan 02 with zero file overlap).
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/31-deterministic-resolution-shadow-mode-only/31-RESEARCH.md
@.planning/phases/31-deterministic-resolution-shadow-mode-only/31-01-SUMMARY.md
@supabase/CLAUDE.md

<interfaces>
<!-- rls-regression.test.ts (verified): CLIENT_DENY_TABLES at ~line 87 is currently
  const CLIENT_DENY_TABLES: ReadonlyArray<string> = [ "fathom_calls_orphan_report" ];
  Its comment (lines 81-86): tables with RLS ENABLED but NO permissive authenticated/anon
  policy — assert an authenticated JWT reads ZERO rows even when a row exists (service-role seeded).

  The bespoke `events` isolation block (~lines 1045-1130) is the precedent for a deny table
  that needs FK parents seeded first (it seeds recordings + an events row via service-role, then
  asserts a foreign-org JWT reads zero). event_match_decisions has the SAME structural issue:
  it needs TWO recordings seeded (recording_id_a < recording_id_b) before a ledger row can exist,
  so a generic single-column loop entry is not enough — mirror the bespoke events block. -->

<!-- Tables under test (from Plan 01, live on TEST):
  event_match_decisions (RLS enabled+forced, service-role only): needs 2 recordings to seed a row.
  organization_feature_flags (RLS enabled+forced, service-role only): needs an org to seed a row. -->
</interfaces>

Locked scoping note (31-RESEARCH.md Pitfall 3): do NOT add either table to the generic `CROSS_ORG_TABLES` loop — its `filterColumn` union supports a single FK column, and `event_match_decisions` has two recording FKs. Use `CLIENT_DENY_TABLES` + a bespoke seed/assert, mirroring the `events` block. SAFE-05 (the tracked requirement ID) is scoped to Phase 30 in the traceability table, but SAFE-05's literal text names `event_match_decisions` — Phase 30 could not register a table that did not exist yet, so this registration happens here.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register both new tables as client-deny and prove zero-row reads from an authenticated JWT</name>
  <files>src/test/rls-regression.test.ts</files>
  <read_first>
    - src/test/rls-regression.test.ts (the CLIENT_DENY_TABLES array ~line 87 and its comment; the existing loop that seeds/asserts deny tables; the bespoke `events` isolation block ~lines 1045-1130 — the template for a deny table needing FK parents; the afterAll cleanup)
    - supabase/CLAUDE.md ("RLS Regression Test (CI-Enforced)", "Running integration tests safely", cleanup contract)
    - .planning/phases/31-deterministic-resolution-shadow-mode-only/31-01-SUMMARY.md (confirm both tables shipped to TEST with no client policy)
  </read_first>
  <action>
    Add `"event_match_decisions"` and `"organization_feature_flags"` to the `CLIENT_DENY_TABLES` array with a short comment explaining each is service-role-only (backend-control tables: the decision ledger and the per-org enable flag). Then ensure the deny assertions actually exercise them: because both need FK parents before a row can exist, add a bespoke seed+assert block modeled on the existing `events` isolation block rather than relying on a generic no-seed loop. Using the service-role client in beforeAll (reuse the suite's existing Org A / User A / recording fixtures where possible): seed ONE organization_feature_flags row (organization_id = Org A, flag_key='event_resolution', enabled=true) and ONE event_match_decisions row (recording_id_a < recording_id_b using two Org-A recordings — create a second Org-A recording if the suite only has one; tier='deterministic', decision='merge_proposed', decided_by='auto', applied=false). Then, from an authenticated JWT (User A's signed-in anon client — the same clients the suite already builds), assert `.from('event_match_decisions').select('*')` returns zero rows and `.from('organization_feature_flags').select('*')` returns zero rows, even though the service-role client can see the seeded rows. Extend afterAll to delete the seeded ledger + flag rows (and any extra recording created) with try/catch per the cleanup contract, keeping the suite idempotent. Run the suite GREEN on TEST (not skipped).
  </action>
  <acceptance_criteria>
    - CLIENT_DENY_TABLES contains both "event_match_decisions" and "organization_feature_flags".
    - A service-role-seeded event_match_decisions row and organization_feature_flags row are BOTH invisible (zero rows) to an authenticated JWT; the service role sees them (proves the row genuinely exists, so the deny is real, not an empty-table false pass).
    - afterAll removes every seeded row and any extra fixture; re-running the suite leaves TEST unchanged.
    - The suite runs GREEN on TEST (not skipped).
    <automated>grep -q "event_match_decisions" src/test/rls-regression.test.ts && grep -q "organization_feature_flags" src/test/rls-regression.test.ts && npm run test:integration -- rls-regression.test.ts && echo ISOLATION_GREEN</automated>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- rls-regression.test.ts && echo OK</automated>
  </verify>
  <done>Both new tables are CI-guaranteed client-invisible: a signed-in JWT reads zero rows from the decision ledger and the enable-flag even when a real row exists. A future permissive-policy mistake fails the build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated JWT -> event_match_decisions | A logged-in client must read ZERO ledger rows (existence + content of proposed merges is backend-only) |
| authenticated JWT -> organization_feature_flags | A logged-in client must read ZERO flag rows (cannot discover or flip the enable state) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03-01 | Information Disclosure | event_match_decisions client visibility | mitigate | Registered in CLIENT_DENY_TABLES with a seeded-row assertion: an authenticated JWT reads zero rows while the service role reads the seeded row. CI-enforced (rls-regression job). HIGH — a leak or a mistaken permissive policy fails the build. |
| T-31-03-02 | Information Disclosure / Elevation of Privilege | organization_feature_flags client visibility | mitigate | Same CLIENT_DENY_TABLES registration + seeded assertion — a client cannot read (or, absent any write policy, flip) the per-org enable flag. HIGH. |
| T-31-03-03 | Tampering (test integrity) | empty-table false pass | mitigate | The service role first asserts the seeded row IS visible to it, so the zero-rows-from-JWT assertion cannot pass merely because the table is empty. MEDIUM. |
| T-31-03-SC | Tampering | package installs | accept | Zero new packages (31-RESEARCH.md Package Legitimacy Audit: N/A). |
</threat_model>

## Artifacts this phase produces (Plan 03)

- **Modified test file:** `src/test/rls-regression.test.ts` — `CLIENT_DENY_TABLES` gains `event_match_decisions` and `organization_feature_flags`, plus a bespoke service-role-seed + authenticated-JWT zero-row isolation block (modeled on the existing `events` block)
- **No new tables, RPCs, migrations, or edge functions** (pure CI-gate registration)

<verification>
- CLIENT_DENY_TABLES lists both new tables.
- Authenticated JWT reads zero rows from each while the service role sees the seeded rows.
- afterAll restores TEST; suite is idempotent and runs GREEN (not skipped).
</verification>

<success_criteria>
The decision ledger (MATCH-09) and the per-org enable flag (SAFE-01) are provably invisible to every client JWT under the CI-enforced isolation suite — closing the primary Information-Disclosure boundary for shadow mode.
</success_criteria>

<output>
Create `.planning/phases/31-deterministic-resolution-shadow-mode-only/31-03-SUMMARY.md` when done.
</output>
