---
phase: 31-deterministic-resolution-shadow-mode-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260901000001_create_organization_feature_flags.sql
  - supabase/migrations/20260901000002_create_event_match_decisions.sql
  - supabase/functions/_shared/event-resolver.ts
  - supabase/functions/_shared/event-resolver.test.ts
  - supabase/functions/resolve-events/index.ts
  - src/test/event-resolution-shadow.integration.test.ts
autonomous: false
requirements: [MATCH-01, MATCH-09, SAFE-01, SAFE-02]

must_haves:
  truths:
    - "Two recordings in the same org sharing an identical tier-1 signal (Zoom recording UUID in source_metadata->>'zoom_meeting_id') produce exactly ONE event_match_decisions row with tier='deterministic', score=NULL, decision='merge_proposed', applied=false, decided_by='auto'"
    - "extractTier1Signal returns the Zoom UUID for zoom, null for fathom/grain/plaud, and NEVER reads zoom_numeric_id"
    - "After a full shadow sweep on TEST, recordings.event_id is NULL for every recording touched (production data untouched — SAFE-02)"
    - "The sweep only processes recordings whose organization has an organization_feature_flags row (flag_key='event_resolution', enabled=true); an org with no such row (or enabled=false) yields zero ledger rows (SAFE-01)"
    - "event_match_decisions and organization_feature_flags have RLS ENABLED and FORCED with no authenticated/anon policy (service-role only, deny-by-default)"
  artifacts:
    - path: "supabase/migrations/20260901000002_create_event_match_decisions.sql"
      provides: "The append-only decision ledger (MATCH-09 field set)"
      contains: "CREATE TABLE IF NOT EXISTS event_match_decisions"
    - path: "supabase/migrations/20260901000001_create_organization_feature_flags.sql"
      provides: "Per-org feature flag table, off by default (SAFE-01)"
      contains: "CREATE TABLE IF NOT EXISTS organization_feature_flags"
    - path: "supabase/functions/_shared/event-resolver.ts"
      provides: "Pure tier-1 extraction + match + shadow-sweep logic"
      exports: ["extractTier1Signal", "findDeterministicMatches", "runShadowSweep"]
    - path: "supabase/functions/resolve-events/index.ts"
      provides: "Cron-triggered shadow sweep edge function (shared-secret gated)"
    - path: "src/test/event-resolution-shadow.integration.test.ts"
      provides: "SAFE-02 noop + SAFE-01 flag gate + MATCH-09 shape proof on TEST"
  key_links:
    - from: "resolve-events/index.ts"
      to: "organization_feature_flags"
      via: "SELECT organization_id WHERE flag_key='event_resolution' AND enabled=true"
      pattern: "event_resolution"
    - from: "runShadowSweep"
      to: "event_match_decisions"
      via: "INSERT merge_proposed, guarded by unique_violation"
      pattern: "event_match_decisions"
    - from: "runShadowSweep"
      to: "recordings.event_id"
      via: "NEVER written — shadow only (SAFE-02)"
      pattern: "event_id"
---

<objective>
This is the production-quality end-to-end TRACER for Phase 31: the thinnest complete slice that proves the shadow-resolution pipeline works before any breadth is added. It creates the two storage contracts (the per-org feature flag table and the append-only decision ledger), a pure tier-1 matcher, and a cron-triggered edge function that sweeps flagged orgs' unresolved recordings, and it proves — by integration test on the TEST project — that a shared Zoom identifier produces exactly one `merge_proposed` ledger row while `recordings.event_id` stays NULL across the board.

This plan opens with a reversibility gate (`checkpoint:decision`): the `event_match_decisions` schema, the `organization_feature_flags` shape, AND the `apply_event_match_atomic`/`reverse_event_match_atomic` signatures (built in Plan 02) are one-way doors Phase 32+ builds on. A human locks all three shapes before any migration is authored.

Purpose: Establish the deterministic-resolution mechanism with zero data-corruption risk — the ledger exists, the merge does not (SAFE-02 is literal, not a metaphor).
Output: Two migrations applied to TEST, the resolver module + unit tests, the `resolve-events` edge function, and the shadow integration test proving the noop + flag gate on TEST. Prod apply and the cron are deferred (Plans 02/04).
</objective>

<execution_context>
@/Users/admin/dev/brain/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/dev/brain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-deterministic-resolution-shadow-mode-only/31-RESEARCH.md
@.planning/phases/31-deterministic-resolution-shadow-mode-only/31-CONTEXT.md
@supabase/CLAUDE.md

Precedent files to READ and mirror (do NOT invent structure — copy these):
@supabase/migrations/20260831000001_create_events_and_extend_participants.sql
@supabase/migrations/20260612120000_create_admin_audit_log.sql
@supabase/migrations/20260730160000_fix_cross_org_copy_dedup.sql
@supabase/functions/_shared/connector-pipeline.ts
@src/test/event-schema-noop.integration.test.ts

<interfaces>
<!-- LIVE schema the resolver/sweep operate against (verified in 31-RESEARCH.md). -->

recordings (live): id UUID, organization_id UUID, owner_user_id UUID, source_app TEXT,
  source_metadata JSONB, event_id UUID NULL (Phase 30, always NULL today), created_at TIMESTAMPTZ

organizations (live, 8 cols): id, name, slug, type, cross_org_default, logo_url, created_at, updated_at
  -- NO settings/flags column exists; there is NO organization_settings table.

<!-- Per-provider source_metadata tier-1 survey (31-RESEARCH.md Pitfall 1 & 5, verified against connector code):
  zoom   -> source_metadata->>'zoom_meeting_id'  (= recording.uuid, per-occurrence stable) = ONLY solid tier-1 signal.
            NEVER use zoom_numeric_id (reusable PMI — false-merge footgun).
  fireflies -> source_metadata->>'fireflies_meeting_link' (POSSIBLY — verify it is a real join URL, Assumption A4)
  read-ai   -> read_ai_platform_id / platform_meeting_id / platform_id (UNVERIFIED — Open Question A1; sample before including)
  fathom, grain, plaud -> NO tier-1 signal -> extractTier1Signal returns null (normal, not an error). -->

<!-- runPipeline seam (connector-pipeline.ts:572-788): the sweep does NOT hook here.
  It operates on recordings rows runPipeline already created, keyed on event_id IS NULL. -->
</interfaces>

Locked constraints (31-CONTEXT.md): tier-1 = exact shared identifier, NO scoring; shadow mode never writes recordings.event_id; flag off by default per org; do NOT touch dedup-fingerprint.ts (Phase 32 owns it); do NOT add a call inside canonical-recording.ts or any of runPipeline's 14 callers.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Reversibility gate — lock the ledger, flag, and apply/reverse RPC shapes</name>
  <files>none — decision gate; gates the migrations authored in Task 2 and the RPC pair in Plan 02</files>
  <read_first>
    - .planning/phases/31-deterministic-resolution-shadow-mode-only/31-RESEARCH.md ("Recommended event_match_decisions schema"; "Standard Stack -> Alternatives Considered" for the flag-table rationale; "Pattern 1" for the RPC signature; "Common Pitfalls 3 & 4")
  </read_first>
  <action>Present the decision, context, and options below and PAUSE — do not author any migration until an option is selected. This is a reversibility gate: the `event_match_decisions` column set, the `organization_feature_flags` shape, and the `apply_event_match_atomic`/`reverse_event_match_atomic` signatures are one-way doors that Plan 02 and Phases 32-39 build on. Record the chosen option (and any knobs) in the SUMMARY and carry it verbatim into Task 2's DDL and Plan 02's RPC DDL. Mirror Phase 30 Plan 02's gate exactly.</action>
  <decision>
    Lock three interdependent shapes against a LIVE production schema before authoring migrations: (1) the `event_match_decisions` ledger columns (MATCH-09), (2) the `organization_feature_flags` table (SAFE-01), and (3) the `apply_event_match_atomic`/`reverse_event_match_atomic` signatures (MATCH-10, implemented in Plan 02). Changing any after apply requires a second prod migration; the ledger shape specifically is read by Phase 32's hardening and the eventual merge-review UI.
  </decision>
  <context>
    Additive DDL against prod with real customer data on the `v2.2-event-resolution` branch. 31-RESEARCH.md is HIGH confidence and recommends option A; this gate exists so a human signs off on the load-bearing shapes, not because the design is uncertain. Nothing in this phase enables the flag or applies a merge (shadow only).
  </context>
  <options>
    <option id="option-a">
      <name>Approve the research-recommended shapes as-is (RECOMMENDED)</name>
      <pros>
        - event_match_decisions: id UUID PK default gen_random_uuid(); recording_id_a UUID NOT NULL REFERENCES recordings(id) ON DELETE CASCADE; recording_id_b UUID NOT NULL REFERENCES recordings(id) ON DELETE CASCADE; event_id UUID REFERENCES events(id) ON DELETE SET NULL; tier TEXT NOT NULL CHECK (tier IN ('deterministic','content_proof','metadata')); score NUMERIC CHECK (score IS NULL OR (score>=0 AND score<=1)); signals JSONB NOT NULL DEFAULT '{}'; decision TEXT NOT NULL CHECK (decision IN ('merge_proposed','merge_applied','reversed','rejected')); decided_by TEXT NOT NULL CHECK (decided_by IN ('auto','user','admin')); applied BOOLEAN NOT NULL DEFAULT false; reverses_decision_id UUID REFERENCES event_match_decisions(id); created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(); CHECK (recording_id_a < recording_id_b); UNIQUE (recording_id_a, recording_id_b, tier). RLS ENABLED + FORCED, NO client policy.
        - organization_feature_flags: id UUID PK; organization_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE; flag_key TEXT NOT NULL; enabled BOOLEAN NOT NULL DEFAULT false; created_at/updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(); UNIQUE (organization_id, flag_key). A flag is OFF unless an explicit row with enabled=true exists — there is deliberately NO global/wildcard row and NO default-true path (SAFE-01 "no broad-enable"). RLS ENABLED + FORCED, NO client policy.
        - apply_event_match_atomic(p_recording_id_a UUID, p_recording_id_b UUID, p_event_id UUID, p_decided_by TEXT, p_signals JSONB, p_owner_user_id UUID) RETURNS UUID — SECURITY DEFINER, SET search_path=public, REVOKE EXECUTE FROM PUBLIC/anon/authenticated, ownership validated by parameter (never auth.uid()). reverse_event_match_atomic(p_decision_id UUID, p_owner_user_id UUID) RETURNS void — same guards.
      </pros>
      <cons>Locks column names/types now; later changes are additive-cheap but type/constraint changes need another prod migration.</cons>
    </option>
    <option id="option-b">
      <name>Approve with one or more reversible knobs</name>
      <pros>
        - Knob 1: add a signals GIN index now if Phase 32 will query signals heavily (cheap deferral otherwise).
        - Knob 2: widen decided_by to include 'system' if a non-auto/non-human actor is anticipated.
        - Knob 3: make organization_feature_flags carry a JSONB config column now (for per-flag parameters) instead of a bare boolean.
      </pros>
      <cons>Any deviation from A should be justified against research; each changes a downstream reader's assumptions.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - A human selected option-a or option-b (with named knobs); the choice is recorded in the SUMMARY.
    - If option-b, the knobs are written into Task 2's DDL (and flagged for Plan 02's RPC DDL) exactly as approved.
  </acceptance_criteria>
  <verify>Human decision gate — no automated check. Task 2 must not begin until an option is selected and recorded.</verify>
  <done>A human selected an option (and any knobs); recorded in the SUMMARY, ready to carry into Task 2 and Plan 02.</done>
  <resume-signal>Select: option-a, or option-b with the specific knob(s) named.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the two migrations + build the pure tier-1 resolver (TDD)</name>
  <files>supabase/migrations/20260901000001_create_organization_feature_flags.sql, supabase/migrations/20260901000002_create_event_match_decisions.sql, supabase/functions/_shared/event-resolver.ts, supabase/functions/_shared/event-resolver.test.ts</files>
  <read_first>
    - supabase/CLAUDE.md (migration filename convention, header block, ==== section banners; snake_case; "All tables MUST have RLS enabled")
    - supabase/migrations/20260612120000_create_admin_audit_log.sql (the append-only, service-role-only ledger precedent to mirror for event_match_decisions)
    - supabase/migrations/20260831000001_create_events_and_extend_participants.sql (ENABLE + FORCE RLS + service-role posture; events(id) FK target)
    - supabase/functions/_shared/__tests__/connector-pipeline.test.ts (proves plain TS `_shared/` modules are vitest-importable with no Deno runtime — mirror its import style)
    - vitest.config.ts (confirm the include glob picks up supabase/functions/_shared/event-resolver.test.ts; if not, place the test under _shared/__tests__/ and adjust the run command)
    - .planning/phases/31-deterministic-resolution-shadow-mode-only/31-RESEARCH.md ("Recommended event_match_decisions schema", "Pattern 3" idempotency, "Common Pitfalls 1 & 5", "checkDuplicate's fail-open convention — do not copy it")
  </read_first>
  <behavior>
    Unit tests (event-resolver.test.ts), written BEFORE the implementation:
    - extractTier1Signal('zoom', { zoom_meeting_id: 'abc-uuid', zoom_numeric_id: '99', zoom_share_url: '...' }) === 'zoom:abc-uuid'
    - extractTier1Signal('zoom', { zoom_numeric_id: '99' }) === null  (never fall back to the reusable PMI)
    - extractTier1Signal('fathom', {...}) === null; extractTier1Signal('grain', {...}) === null; extractTier1Signal('plaud', {...}) === null
    - extractTier1Signal('zoom', null) === null; extractTier1Signal('zoom', {}) === null  (fail closed, no throw)
    - extractTier1Signal(<anything malformed>) never throws — returns null
    - findDeterministicMatches over 3 recordings where A and B share a signal and C differs -> returns exactly one pair [{recording_id_a, recording_id_b}] with recording_id_a < recording_id_b (canonical order); zero pairs when no two share a signal
    - findDeterministicMatches never pairs two recordings from different organization_id values (same-org only this phase)
  </behavior>
  <action>
    First, run a read-only sampling query against the TEST project (or read-only prod) to resolve Assumptions A1/A4: `SELECT source_app, source_metadata FROM recordings WHERE source_app IN ('read-ai','fireflies') LIMIT 5`. Include a provider's field in the tier-1 map ONLY if the sampled value is a real per-meeting-distinct conference identifier; otherwise leave it returning null (fail-closed is safe for shadow mode). Record what you found in the SUMMARY. Zoom (`source_metadata->>'zoom_meeting_id'`) is always included; fathom/grain/plaud always return null.

    Write `supabase/functions/_shared/event-resolver.test.ts` FIRST (the behaviors above), run it (RED). Then implement `supabase/functions/_shared/event-resolver.ts` exporting: `extractTier1Signal(sourceApp: string, sourceMetadata: Record<string, unknown> | null): string | null` (returns a provider-prefixed value like `zoom:<uuid>` so signals never collide across providers; centralize the per-provider key map in ONE place per research "Don't Hand-Roll"); `findDeterministicMatches(candidates)` (pure — pairs recordings sharing a non-null signal within the same organization_id, canonical-ordered a<b); and `runShadowSweep(supabase, opts)` (implemented here as the shared logic the edge function calls — see Task 3 for its contract; the pure functions are what the unit test covers). extractTier1Signal MUST fail closed (return null) on any malformed/missing input and MUST NOT read `zoom_numeric_id` — this is the opposite of checkDuplicate's fail-open convention (a false merge is a data-exposure incident). Run the unit test (GREEN).

    Author both migrations per supabase/CLAUDE.md structure (header block + ==== banners), using the shapes approved in Task 1. `20260901000001_create_organization_feature_flags.sql`: CREATE TABLE IF NOT EXISTS organization_feature_flags with the option-a columns, UNIQUE (organization_id, flag_key), index on (flag_key, enabled) WHERE enabled, ENABLE + FORCE ROW LEVEL SECURITY, a "Service role full access" FOR ALL TO service_role policy, and NO authenticated/anon policy. `20260901000002_create_event_match_decisions.sql`: CREATE TABLE IF NOT EXISTS event_match_decisions with the option-a columns including the `recording_id_a < recording_id_b` CHECK and UNIQUE (recording_id_a, recording_id_b, tier), indexes on (recording_id_a), (recording_id_b), (event_id), ENABLE + FORCE ROW LEVEL SECURITY, service-role FOR ALL policy, NO authenticated/anon policy. COMMENT the tables and the SAFE-02 semantics (applied=false always for automatic rows). Apply BOTH migrations to the TEST project only (VITE_SUPABASE_TEST_URL / SUPABASE_TEST_SERVICE_ROLE_KEY — never the prod ref vltmrnjsubfzrgrtdqey; fetch TEST creds via `supabase projects api-keys` per Phase 30 precedent if unset). If TEST is behind on migrations, STOP and surface it (blocks Task 3's test). Introspect TEST to confirm both tables exist with the locked shape.
  </action>
  <acceptance_criteria>
    - event-resolver.test.ts covers every behavior above and passes; extractTier1Signal never throws and never reads zoom_numeric_id (assert both).
    - Both migration files exist with supabase/CLAUDE.md banners; both tables ENABLE + FORCE RLS with a service-role FOR ALL policy and NO authenticated/anon policy.
    - event_match_decisions has the `recording_id_a < recording_id_b` CHECK and UNIQUE (recording_id_a, recording_id_b, tier); organization_feature_flags has UNIQUE (organization_id, flag_key) and enabled DEFAULT false.
    - Both migrations applied cleanly to the TEST project; introspection confirms the shape. NOT applied to prod.
    <automated>npx vitest run supabase/functions/_shared/event-resolver.test.ts && grep -q "CREATE TABLE IF NOT EXISTS event_match_decisions" supabase/migrations/20260901000002_create_event_match_decisions.sql && grep -q "recording_id_a < recording_id_b" supabase/migrations/20260901000002_create_event_match_decisions.sql && grep -q "FORCE ROW LEVEL SECURITY" supabase/migrations/20260901000002_create_event_match_decisions.sql && grep -q "enabled BOOLEAN NOT NULL DEFAULT false" supabase/migrations/20260901000001_create_organization_feature_flags.sql && ! grep -q "zoom_numeric_id" supabase/functions/_shared/event-resolver.ts && echo TASK2_OK</automated>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run supabase/functions/_shared/event-resolver.test.ts && echo RESOLVER_UNIT_OK</automated>
  </verify>
  <done>Both tables exist on TEST with the locked shape; the pure resolver passes its unit suite; zoom_numeric_id is never read. Ready for the sweep wiring in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Build the resolve-events shadow sweep + prove the end-to-end noop on TEST</name>
  <files>supabase/functions/resolve-events/index.ts, src/test/event-resolution-shadow.integration.test.ts</files>
  <read_first>
    - supabase/CLAUDE.md ("index.ts Structure Template", "Input Validation Requirements" Zod, "Running integration tests safely", the mandatory afterAll cleanup contract)
    - supabase/functions/fathom-reconcile/index.ts (the X-Reconcile-Secret shared-secret gate pattern — resolve-events is cron-triggered, NOT user-JWT; do NOT use authenticateRequest)
    - src/test/event-schema-noop.integration.test.ts (the SAFE-02 noop-proof template: fixture org/user/workspace/recordings via service-role, event_id NULL assertions, cleanup contract — mirror it directly)
    - supabase/functions/_shared/event-resolver.ts (the runShadowSweep contract from Task 2)
  </read_first>
  <action>
    Build `supabase/functions/resolve-events/index.ts` as a THIN wrapper following supabase/CLAUDE.md's structure template but gated by a shared secret, not a user JWT: on OPTIONS return CORS; validate the request body with Zod (`{ mode: z.literal('shadow') }`); reject with 401 if the `X-Reconcile-Secret` header is absent or does not equal `Deno.env.get('RECONCILE_SECRET')` (mirror fathom-reconcile) BEFORE any work; create the service-role client; SELECT organization_id FROM organization_feature_flags WHERE flag_key='event_resolution' AND enabled=true; if none, return `{ success:true, processed:0 }`; otherwise call `runShadowSweep(supabase, { flaggedOrgIds })` from `_shared/event-resolver.ts` and return its summary. runShadowSweep MUST: select recordings WHERE event_id IS NULL AND organization_id = ANY(flaggedOrgIds) ORDER BY created_at LIMIT a batch size; for each, extractTier1Signal; for each non-null signal find other same-org recordings sharing that exact signal; INSERT INTO event_match_decisions (recording_id_a, recording_id_b canonical-ordered, tier='deterministic', score=NULL, signals=jsonb like {"matched_field":"zoom_meeting_id"}, decision='merge_proposed', decided_by='auto', applied=false) guarded by `EXCEPTION`/onConflict on the UNIQUE(recording_id_a, recording_id_b, tier) so a re-run no-ops (Pattern 3); and it MUST NEVER write recordings.event_id or the events table (SAFE-02) and MUST NEVER call apply_event_match_atomic. On any extraction/query error, fail CLOSED (skip that recording, write nothing, log, continue). Scope matching to same organization_id only this phase (cross-org is Phase 32/SAFE-04).

    Write `src/test/event-resolution-shadow.integration.test.ts` modeled on event-schema-noop.integration.test.ts (real TEST DB, describe.skipIf(!integrationDbReachable), full afterAll cleanup via cleanup_test_fixture_users). Fixtures: create org O_on with an organization_feature_flags row (flag_key='event_resolution', enabled=true) and org O_off with NO such row (or enabled=false); in O_on seed two recordings with source_app='zoom' and the SAME source_metadata->>'zoom_meeting_id' UUID, plus a third with a different UUID; in O_off seed two recordings sharing a UUID. Call `runShadowSweep(admin, { flaggedOrgIds: [O_on] })` (call the shared function directly with the service-role client — the cron→HTTP path is exercised in prod, the logic is proven here). Assert: (a) exactly ONE event_match_decisions row for the O_on matching pair, with tier='deterministic', score=NULL, decision='merge_proposed', applied=false, decided_by='auto', and both recording IDs present and canonically ordered (MATCH-09 shape); (b) ZERO rows for the O_on non-matching third recording; (c) ZERO rows for O_off's pair even though they share a UUID (SAFE-01 flag gate); (d) after the sweep, recordings.event_id IS NULL for ALL five recordings (SAFE-02 noop). Ensure the suite runs GREEN on TEST (not skipped) — configure TEST env per supabase/CLAUDE.md if needed.
  </action>
  <acceptance_criteria>
    - resolve-events/index.ts rejects a missing/wrong X-Reconcile-Secret with 401 before any DB work, Zod-validates the body, and never imports authenticateRequest.
    - runShadowSweep writes only to event_match_decisions (merge_proposed/applied=false), never to recordings.event_id or events, and never calls apply_event_match_atomic (grep-verifiable absence).
    - The integration test runs GREEN on TEST (not skipped) and asserts all four conditions (a)-(d) above; recordings.event_id is NULL for every fixture recording post-sweep.
    - Cleanup restores TEST to its prior state (idempotent re-run).
    <automated>npm run test:integration -- event-resolution-shadow.integration.test.ts && ! grep -q "apply_event_match_atomic" supabase/functions/resolve-events/index.ts supabase/functions/_shared/event-resolver.ts && ! grep -Eiq "[.](update|upsert)[(]" supabase/functions/_shared/event-resolver.ts && grep -Eq "X-Reconcile-Secret|RECONCILE_SECRET" supabase/functions/resolve-events/index.ts && echo SHADOW_OK</automated>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- event-resolution-shadow.integration.test.ts && echo SHADOW_INTEGRATION_GREEN</automated>
  </verify>
  <done>The shadow pipeline works end-to-end on TEST: a shared Zoom UUID produces exactly one merge_proposed ledger row, the flag gate blocks the unflagged org, and recordings.event_id stays NULL everywhere. The tracer is proven; Plans 02/03 expand it; Plan 04 lands it in prod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron / third party -> resolve-events | HTTP trigger; must be gated by a shared secret, not open CORS |
| authenticated JWT -> event_match_decisions / organization_feature_flags | A logged-in client must read ZERO rows from either (service-role only) |
| source_metadata JSONB -> matcher | Untrusted/heterogeneous per-provider input; extraction must fail closed |
| service_role sweep -> recordings.event_id | Trusted, but SAFE-02 forbids ANY write to event_id in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-01 | Information Disclosure | event_match_decisions + organization_feature_flags | mitigate | ENABLE + FORCE ROW LEVEL SECURITY on both, service-role FOR ALL policy, NO authenticated/anon policy (deny-by-default). CI-proven in Plan 03 (CLIENT_DENY_TABLES). HIGH — blocks if any client policy is added. |
| T-31-01-02 | Elevation of Privilege | organization_feature_flags default | mitigate | `enabled BOOLEAN NOT NULL DEFAULT false`; flag is off unless an explicit (org, 'event_resolution', true) row exists; NO global/wildcard row and NO default-true path (SAFE-01 "no broad-enable"). HIGH. |
| T-31-01-03 | Information Disclosure / data corruption | runShadowSweep | mitigate | The sweep NEVER writes recordings.event_id or events and NEVER calls apply_event_match_atomic (SAFE-02); proven by the noop integration test (event_id NULL for all fixtures) + grep-absence in acceptance. HIGH. |
| T-31-01-04 | Spoofing | resolve-events HTTP endpoint | mitigate | X-Reconcile-Secret header compared to RECONCILE_SECRET env before any work; 401 otherwise (mirrors fathom-reconcile). Not left open with only CORS. HIGH. |
| T-31-01-05 | Tampering (data integrity) | duplicate/racy ledger rows | mitigate | UNIQUE (recording_id_a, recording_id_b, tier) + `recording_id_a < recording_id_b` CHECK + unique_violation-tolerant insert (Pattern 3). MEDIUM. |
| T-31-01-06 | Tampering / false merge | tier-1 extraction | mitigate | extractTier1Signal fails closed (null) on malformed input and NEVER reads zoom_numeric_id (reusable PMI); provider-prefixed signal values prevent cross-provider collision. MEDIUM. |
| T-31-01-SC | Tampering | package installs | accept | Zero new packages this phase (31-RESEARCH.md Package Legitimacy Audit: N/A — all Postgres built-ins + existing Zod/Vitest). |
</threat_model>

## Artifacts this phase produces (Plan 01)

- **New table:** `organization_feature_flags` (columns: `id`, `organization_id`, `flag_key`, `enabled`, `created_at`, `updated_at`; UNIQUE(organization_id, flag_key))
- **New table:** `event_match_decisions` (columns: `id`, `recording_id_a`, `recording_id_b`, `event_id`, `tier`, `score`, `signals`, `decision`, `decided_by`, `applied`, `reverses_decision_id`, `created_at`; CHECK a<b; UNIQUE(a,b,tier))
- **New RLS policies:** `organization_feature_flags` + `event_match_decisions` "Service role full access" (FOR ALL); no client policy
- **New shared module:** `supabase/functions/_shared/event-resolver.ts` (exports `extractTier1Signal`, `findDeterministicMatches`, `runShadowSweep`)
- **New edge function:** `supabase/functions/resolve-events/index.ts` (cron-triggered shadow sweep, shared-secret gated)
- **New migration files:** `20260901000001_create_organization_feature_flags.sql`, `20260901000002_create_event_match_decisions.sql`
- **New test files:** `supabase/functions/_shared/event-resolver.test.ts` (unit), `src/test/event-resolution-shadow.integration.test.ts` (integration)
- **New env var needed before Phase 32 enables any org:** `RECONCILE_SECRET` (edge function secret) — non-blocking this phase (flag off; deferred to Plan 04 notes)

<verification>
- event-resolver.test.ts passes; extractTier1Signal never reads zoom_numeric_id, never throws.
- Both tables on TEST: ENABLE+FORCE RLS, service-role FOR ALL only, correct constraints; NOT on prod.
- resolve-events gated by X-Reconcile-Secret; sweep writes only merge_proposed rows, never event_id/events, never calls apply.
- Shadow integration test GREEN on TEST: one merge_proposed row for the flagged matching pair; zero for the unflagged org; recordings.event_id NULL for all fixtures.
</verification>

<success_criteria>
MATCH-01 (deterministic exact-match, no scoring), MATCH-09 (ledger written with the full field shape), SAFE-01 (per-org flag gate, off by default), and SAFE-02 (shadow noop — event_id untouched) are all TRUE on the TEST project, proven by test. The tracer is complete.
</success_criteria>

<output>
Create `.planning/phases/31-deterministic-resolution-shadow-mode-only/31-01-SUMMARY.md` when done. Record the Task 1 decision (option-a/option-b + knobs) and the A1/A4 sampling result (which providers made the tier-1 map).
</output>
