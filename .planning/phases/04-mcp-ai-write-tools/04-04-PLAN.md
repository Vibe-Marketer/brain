---
phase: 04-mcp-ai-write-tools
plan: 04
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - docs/operations/mcp-runbook.md
  - supabase/functions/mcp-server/__tests__/contract-surface.test.ts
  - supabase/functions/mcp-server/__tests__/category-gating.test.ts
  - supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts
  - supabase/functions/mcp-server/__tests__/ingest-transcript.integration.test.ts
  - supabase/functions/mcp-server/__tests__/set-speakers.idempotency.test.ts
  - .planning/phases/04-mcp-ai-write-tools/04-VALIDATION.md
autonomous: true
requirements:
  - MCP-04
must_haves:
  truths:
    - "MCP-04: All four tools work through the modular one-function MCP server and keep `content[].text` markdown responses."
    - "D-06/D-07: Verification proves primary ingest success is separated from non-critical enrichment warnings."
    - "D-09/D-12: Verification proves Manual MCP Import provenance is visible and client/original URL metadata is secondary."
    - "D-13/D-14/D-15/D-16/D-17: Verification proves append/merge/upsert defaults and no hidden destructive side effects."
  artifacts:
    - path: "docs/operations/mcp-runbook.md"
      provides: "Operator-facing smoke commands and response-contract notes for Phase 04 tools"
    - path: ".planning/phases/04-mcp-ai-write-tools/04-VALIDATION.md"
      provides: "Updated validation status/checklist after Wave 0 requirements are satisfied"
  key_links:
    - from: "Phase 04 tool modules"
      to: "docs/operations/mcp-runbook.md"
      via: "smoke-test examples and markdown response contract"
      pattern: "ingest_transcript|append_to_transcript|update_call_metadata|set_speakers|content[].text"
---

<objective>
Close Phase 04 planning with contract verification, runbook smoke instructions, build/type gates, and live-ready proof commands for the new MCP write tools.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mcp-ai-write-tools/04-CONTEXT.md
@.planning/phases/04-mcp-ai-write-tools/04-RESEARCH.md
@.planning/phases/04-mcp-ai-write-tools/04-PATTERNS.md
@.planning/phases/04-mcp-ai-write-tools/04-VALIDATION.md
@docs/operations/mcp-runbook.md
@supabase/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten final contract tests and validation status after all four tools exist</name>
  <files>supabase/functions/mcp-server/__tests__/contract-surface.test.ts, supabase/functions/mcp-server/__tests__/category-gating.test.ts, supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts, supabase/functions/mcp-server/__tests__/ingest-transcript.integration.test.ts, supabase/functions/mcp-server/__tests__/set-speakers.idempotency.test.ts, .planning/phases/04-mcp-ai-write-tools/04-VALIDATION.md</files>
  <read_first>
    - supabase/functions/mcp-server/tools/definitions.ts
    - supabase/functions/_shared/mcp-tool-categories.ts
    - src/lib/mcp-tool-categories.ts
    - supabase/functions/mcp-server/tools/registry.ts
    - supabase/functions/mcp-server/__tests__/contract-surface.test.ts
    - supabase/functions/mcp-server/__tests__/category-gating.test.ts
    - supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts
    - supabase/functions/mcp-server/__tests__/ingest-transcript.integration.test.ts
    - supabase/functions/mcp-server/__tests__/set-speakers.idempotency.test.ts
    - .planning/phases/04-mcp-ai-write-tools/04-VALIDATION.md
  </read_first>
  <action>After Plans 02 and 03 land, update the final contract tests so the full tool count, registry coverage, category map coverage, client-visible discovery output, and markdown response-shape assertions match the implemented Phase 04 surface. Mark Wave 0 file-exists/status rows in `04-VALIDATION.md` as satisfied only when the named files exist and the quick MCP write-tool command exits 0. Keep `create_organization` and `create_workspace` admin-only by proving they remain in the `admin` category and are not moved to `write` during this phase.</action>
  <acceptance_criteria>
    - Contract tests fail if any Phase 04 tool is missing from definitions, registry, canonical categories, frontend mirror, or descriptions.
    - Category tests prove read-only tokens cannot discover the new write tools.
    - Category tests prove `create_organization` and `create_workspace` remain admin-only.
    - `04-VALIDATION.md` status changes are backed by actual test files and command output, not checklist-only edits.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/contract-surface.test.ts supabase/functions/mcp-server/__tests__/category-gating.test.ts supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts supabase/functions/mcp-server/__tests__/ingest-transcript.integration.test.ts supabase/functions/mcp-server/__tests__/set-speakers.idempotency.test.ts</automated>
  </verify>
  <done>Phase 04 contract tests and validation artifacts agree with the implemented tool surface.</done>
</task>

<task type="auto">
  <name>Task 2: Document operator smoke commands and run build/Deno gates</name>
  <files>docs/operations/mcp-runbook.md, .planning/phases/04-mcp-ai-write-tools/04-VALIDATION.md</files>
  <read_first>
    - docs/operations/mcp-runbook.md
    - supabase/functions/mcp-server/index.ts
    - supabase/functions/mcp-server/protocol.ts
    - supabase/functions/mcp-server/tools/registry.ts
    - supabase/functions/mcp-server/tools/definitions.ts
    - .env
    - .env.local
    - supabase/CLAUDE.md
  </read_first>
  <action>Add a concise Phase 04 section to `docs/operations/mcp-runbook.md` with redacted curl examples for `tools/list`, `ingest_transcript`, `append_to_transcript`, `update_call_metadata`, and `set_speakers` against `https://api.callvaultai.com/mcp/w/{workspace_uuid}`. The examples must assert HTTP 200 for valid calls, markdown `result.content[0].text`, read-only invisibility/rejection, wrong-workspace 403, and the visible Manual MCP Import provenance. Run `npm run build` because `supabase/functions/mcp-server/index.ts` is in the touched surface for this phase, and run `deno check supabase/functions/mcp-server/index.ts` to catch Edge Function type/import regressions. Do not echo MCP tokens or secrets into docs or summaries.</action>
  <acceptance_criteria>
    - Runbook examples use `https://api.callvaultai.com/mcp/w/{workspace_uuid}` and do not expose raw Supabase function URLs.
    - Runbook states all Phase 04 tool responses must be markdown `content[].text`.
    - `npm run build` exits 0 in this session after Phase 04 code changes.
    - `deno check supabase/functions/mcp-server/index.ts` exits 0 in this session or the summary records exact remaining type errors.
    - Secrets are referenced as environment variable names only.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && deno check supabase/functions/mcp-server/index.ts</automated>
  </verify>
  <done>The operator runbook contains usable smoke commands and the frontend/Edge Function gates have current proof.</done>
</task>

<task type="auto">
  <name>Task 3: Run production-ready MCP smoke or record the exact credential blocker</name>
  <files>.planning/phases/04-mcp-ai-write-tools/04-04-SUMMARY.md</files>
  <read_first>
    - .env
    - .env.local
    - docs/operations/mcp-runbook.md
    - .planning/phases/04-mcp-ai-write-tools/04-VALIDATION.md
  </read_first>
  <action>If a valid workspace UUID and workspace-scoped MCP token are available in local env or recent secure operator context, run the runbook smoke commands against `https://api.callvaultai.com/mcp/w/{workspace_uuid}`: `tools/list`, `ingest_transcript` with a small synthetic transcript, `append_to_transcript`, `update_call_metadata`, `set_speakers`, read-only-token invisibility when a read-only token is available, and wrong-workspace 403 when a mismatch workspace is available. Verify the created recording is visible in the app or through an authenticated backend read without leaking secrets. If credentials are not available, record the exact missing env vars and do not claim live verification. This task writes proof only into the SUMMARY, not into tracked code.</action>
  <acceptance_criteria>
    - If credentials exist, summary includes endpoint, HTTP status, JSON-RPC result shape, recording id, target workspace, and Manual MCP Import evidence with tokens redacted.
    - If credentials do not exist, summary says live smoke was not run and names the missing non-secret prerequisites.
    - No MCP bearer token, Supabase service role key, or user password appears in the summary.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/contract-surface.test.ts supabase/functions/mcp-server/__tests__/category-gating.test.ts supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts && npm run build</automated>
  </verify>
  <done>Phase 04 has either credential-backed production smoke proof or an explicit live-verification blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local verification -> production claim | Build/test success is not the same as live MCP proof |
| Runbook examples -> operator secrets | Smoke commands must be usable without leaking credentials |
| Admin tools -> write tool expansion | Existing admin creation tools must not become visible to write-only clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-04-01 | Information Disclosure | docs/summaries | mitigate | Use env var placeholders and redact all bearer tokens. |
| T-04-04-02 | Elevation of Privilege | create_organization/create_workspace | mitigate | Final category tests keep these tools admin-only. |
| T-04-04-03 | Repudiation | verification claims | mitigate | Separate unit/build/Deno/live smoke proof in the summary. |
| T-04-04-04 | Compatibility | MCP response shape | mitigate | Contract tests and runbook require markdown `content[].text`. |
</threat_model>

<verification>
Run the final Phase 04 local gate:

`npm test -- --run supabase/functions/mcp-server/__tests__/contract-surface.test.ts supabase/functions/mcp-server/__tests__/category-gating.test.ts supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts supabase/functions/mcp-server/__tests__/ingest-transcript.integration.test.ts supabase/functions/mcp-server/__tests__/set-speakers.idempotency.test.ts`

Then:

`npm run build`

`deno check supabase/functions/mcp-server/index.ts`
</verification>

<success_criteria>
- All four Phase 04 tools are discoverable only to write-enabled clients and return markdown `content[].text`.
- Admin creation tools remain admin-only.
- Local tests, frontend build, and Deno check have current proof.
- Live smoke is either run against `api.callvaultai.com/mcp/w/{workspace_uuid}` or explicitly blocked by missing credentials.
</success_criteria>

<output>
Create `.planning/phases/04-mcp-ai-write-tools/04-04-SUMMARY.md` when done.
</output>
