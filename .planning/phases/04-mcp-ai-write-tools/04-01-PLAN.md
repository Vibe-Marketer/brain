---
phase: 04-mcp-ai-write-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/mcp-server/tools/definitions.ts
  - supabase/functions/_shared/mcp-tool-categories.ts
  - src/lib/mcp-tool-categories.ts
  - supabase/functions/mcp-server/__tests__/contract-surface.test.ts
  - supabase/functions/mcp-server/__tests__/category-gating.test.ts
  - supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts
  - supabase/functions/mcp-server/__tests__/ingest-transcript.integration.test.ts
  - supabase/functions/mcp-server/__tests__/set-speakers.idempotency.test.ts
autonomous: true
requirements:
  - MCP-04
must_haves:
  truths:
    - "D-01/D-02/D-03: Speaker matching is best-effort and response text reports matched, created, unresolved, and clarification prompts."
    - "D-04/D-05: Link-only/title-only ingest is allowed as low-context; CallVault does not crawl, websearch, Firecrawl, browser-enrich, or OpenGraph-fetch in Phase 04."
    - "D-06/D-07/D-08: Recording creation is the primary success condition; enrichment failures become markdown warnings; tag lowercase/name dedup has executable coverage."
    - "D-09/D-10/D-11/D-12: Manual MCP Import is the visible source identity and client/original URL/OpenGraph data stays in source_metadata."
    - "D-13/D-14/D-15/D-16/D-17: Follow-up tools append, merge, and upsert by default; destructive replacement requires explicit caller intent."
  artifacts:
    - path: "supabase/functions/mcp-server/tools/definitions.ts"
      provides: "MCP schemas for ingest_transcript, append_to_transcript, update_call_metadata, and set_speakers"
    - path: "supabase/functions/_shared/mcp-tool-categories.ts"
      provides: "Canonical write-category mapping and user-facing descriptions for the four new tools"
    - path: "src/lib/mcp-tool-categories.ts"
      provides: "Frontend mirror of the write-category mapping for token permission UI consistency"
    - path: "supabase/functions/mcp-server/__tests__/*.test.ts"
      provides: "Wave 0 executable contract for category visibility, scope enforcement, low-context ingest, tag dedup, speaker ambiguity, and markdown output"
  key_links:
    - from: "supabase/functions/mcp-server/tools/definitions.ts"
      to: "supabase/functions/_shared/mcp-tool-categories.ts"
      via: "one-to-one tool name coverage"
      pattern: "ingest_transcript|append_to_transcript|update_call_metadata|set_speakers"
    - from: "supabase/functions/_shared/mcp-tool-categories.ts"
      to: "src/lib/mcp-tool-categories.ts"
      via: "manual mirror required by file header"
      pattern: "TOOL_CATEGORIES|TOOL_DESCRIPTIONS"
    - from: "supabase/functions/mcp-server/__tests__/category-gating.test.ts"
      to: "supabase/functions/_shared/mcp-tool-categories.ts"
      via: "read-only invisibility and write-category rejection assertions"
      pattern: "enabled_categories|write"
---

<objective>
Create the Phase 04 Wave 0 contract: define the four new MCP write tools at the discovery/category boundary and add failing-then-passing tests that pin the required behavior before implementation.

Purpose: Prevent hidden tool-surface drift and make the later ingest/follow-up implementation prove MCP-04, D-01 through D-17, category gating, workspace scope, and markdown-only responses.
Output: tool definitions, category maps, frontend mirror updates, and Wave 0 tests.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-mcp-ai-write-tools/04-CONTEXT.md
@.planning/phases/04-mcp-ai-write-tools/04-RESEARCH.md
@.planning/phases/04-mcp-ai-write-tools/04-PATTERNS.md
@.planning/phases/04-mcp-ai-write-tools/04-VALIDATION.md
@supabase/CLAUDE.md
@src/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tool-surface schemas and category gates for the four Phase 04 tools</name>
  <files>supabase/functions/mcp-server/tools/definitions.ts, supabase/functions/_shared/mcp-tool-categories.ts, src/lib/mcp-tool-categories.ts, supabase/functions/mcp-server/__tests__/contract-surface.test.ts, supabase/functions/mcp-server/__tests__/category-gating.test.ts</files>
  <read_first>
    - supabase/functions/mcp-server/tools/definitions.ts
    - supabase/functions/_shared/mcp-tool-categories.ts
    - src/lib/mcp-tool-categories.ts
    - supabase/functions/mcp-server/__tests__/contract-surface.test.ts
    - supabase/functions/mcp-server/__tests__/category-gating.test.ts
    - .planning/phases/04-mcp-ai-write-tools/04-CONTEXT.md
    - .planning/phases/04-mcp-ai-write-tools/04-VALIDATION.md
  </read_first>
  <action>Add `ingest_transcript`, `append_to_transcript`, `update_call_metadata`, and `set_speakers` to the MCP tool definitions with object input schemas and `outputSchema.properties.text` only. Put all four names in the canonical and frontend-mirror `TOOL_CATEGORIES` maps as `write`, add one-line descriptions to both description maps, and update contract/category tests so total tool count and category-name coverage include the four new names. Schema descriptions must state: `workspace_id` is required for organization-scoped tokens, workspace-scoped tokens are bound to their endpoint workspace, `ingest_transcript` accepts already-transcribed text plus metadata/speakers/tags/notes/folder, and all responses are markdown `content[].text` per MCP-04.</action>
  <acceptance_criteria>
    - `definitions.ts` contains exactly the four new tool names and each new definition has `outputSchema.required: ['text']`.
    - Both `TOOL_CATEGORIES` maps classify all four new names as `write`.
    - Both `TOOL_DESCRIPTIONS` maps contain all four new names.
    - `contract-surface.test.ts` fails if any new definition lacks a category or text-only output schema.
    - `category-gating.test.ts` asserts read-only tokens cannot see or invoke the four new tools.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/contract-surface.test.ts supabase/functions/mcp-server/__tests__/category-gating.test.ts</automated>
  </verify>
  <done>The new tool names are visible only as write-category tools and the contract tests pin discovery/category parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Wave 0 behavioral tests for ingest and follow-up tool contracts</name>
  <files>supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts, supabase/functions/mcp-server/__tests__/ingest-transcript.integration.test.ts, supabase/functions/mcp-server/__tests__/set-speakers.idempotency.test.ts</files>
  <read_first>
    - supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts
    - supabase/functions/mcp-server/__tests__/workspace-scope.integration.test.ts
    - supabase/functions/mcp-server/tools/write/create_note.ts
    - supabase/functions/mcp-server/tools/write/tag_call.ts
    - supabase/functions/mcp-server/tools/write/_access.ts
    - supabase/functions/_shared/connector-pipeline.ts
    - .planning/phases/04-mcp-ai-write-tools/04-VALIDATION.md
  </read_first>
  <action>Extend `write-tools-boundary.test.ts` and create the missing Wave 0 files named in 04-VALIDATION.md. Cover workspace-scoped mismatch rejection, organization-scoped missing/unauthorized `workspace_id` rejection, low-context link/title ingest labeling per D-04, Manual MCP Import provenance per D-09/D-12, tag creation and lowercase-name dedup per D-08, speaker ambiguity reporting and clarification prompt text per D-01/D-03, canonical `call_participants` speaker upsert with `participant_type = 'speaker'`, `append_to_transcript` append-not-replace per D-14, `update_call_metadata` merge-not-wipe per D-15, and `set_speakers` idempotent repeated payload behavior per D-16. Use real-DB integration only where needed; keep unit boundaries deterministic for handler-level assertions.</action>
  <acceptance_criteria>
    - `ingest-transcript.integration.test.ts` covers successful ingest, low-context ingest, non-critical enrichment warning mode, Manual MCP Import metadata, and workspace target enforcement.
    - `set-speakers.idempotency.test.ts` proves repeated equivalent speaker payloads do not create duplicate `call_participants` speaker rows.
    - `write-tools-boundary.test.ts` contains explicit assertions for tag lowercase dedup, ambiguous speaker reporting, metadata merge, and transcript append behavior.
    - No integration test falls back to production Supabase env vars; it uses the guarded integration setup pattern from `supabase/CLAUDE.md`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts supabase/functions/mcp-server/__tests__/ingest-transcript.integration.test.ts supabase/functions/mcp-server/__tests__/set-speakers.idempotency.test.ts</automated>
  </verify>
  <done>Wave 0 tests describe the Phase 04 behavior tightly enough that implementation cannot silently drop MCP-04 or D-01 through D-17.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| MCP discovery -> token permissions | Tool definitions must not disclose write capabilities to read-only or AI-only tokens |
| Backend category map -> frontend mirror | Permission UI must not diverge from server enforcement |
| Test fixtures -> live database | Integration coverage must use the protected test-project pattern, not production env fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-01-01 | Information Disclosure | tools/list | mitigate | Add all four tools as `write` and extend read-only invisibility tests. |
| T-04-01-02 | Elevation of Privilege | category map drift | mitigate | Contract test requires definition/category/description parity for every tool. |
| T-04-01-03 | Tampering | behavioral tests | mitigate | Add Wave 0 tests before implementation so append/merge/upsert semantics are executable. |
| T-04-01-04 | Information Disclosure | integration env | mitigate | Follow guarded `*.integration.test.ts` setup; never use production Supabase fallback. |
</threat_model>

<verification>
Run the Phase 04 Wave 0 contract command:

`npm test -- --run supabase/functions/mcp-server/__tests__/contract-surface.test.ts supabase/functions/mcp-server/__tests__/category-gating.test.ts supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts supabase/functions/mcp-server/__tests__/ingest-transcript.integration.test.ts supabase/functions/mcp-server/__tests__/set-speakers.idempotency.test.ts`
</verification>

<success_criteria>
- MCP discovery/category gates know the four new write tools and hide them from read-only tokens.
- Behavioral tests cover low-context ingest, provenance, warning-mode enrichment, tag dedup, speaker ambiguity, append/merge/upsert defaults, and markdown result shape.
- Frontend and backend category maps remain synchronized.
</success_criteria>

<output>
Create `.planning/phases/04-mcp-ai-write-tools/04-01-SUMMARY.md` when done.
</output>
