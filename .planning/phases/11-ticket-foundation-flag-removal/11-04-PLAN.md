---
phase: 11-ticket-foundation-flag-removal
plan: 04
type: execute
wave: 4
depends_on: [11-03]
files_modified:
  - src/components/settings/NewTicketDialog.tsx
  - src/components/settings/AdminTab.tsx
  - src/services/tickets.service.ts
  - src/hooks/useTickets.ts
  - src/components/settings/__tests__/NewTicketDialog.test.tsx
autonomous: true
requirements: [TKT-03]
must_haves:
  truths:
    - "An admin clicks 'New Ticket' in the Tickets section header, picks bug or task, sets severity, writes a description, and submits"
    - "The created ticket has context auto-attached (URL, user agent, org/workspace ids, app version, commit) without the admin entering any of it"
    - "The new ticket appears in the tickets list immediately after submission"
  artifacts:
    - path: "src/components/settings/NewTicketDialog.tsx"
      provides: "admin ticket submission dialog (type, severity, description)"
      min_lines: 50
    - path: "src/services/tickets.service.ts"
      provides: "createTicket() invoking send-support-ticket with type/severity + auto context"
      exports: ["createTicket"]
  key_links:
    - from: "src/components/settings/NewTicketDialog.tsx"
      to: "send-support-ticket Edge Function"
      via: "createTicket service → supabase.functions.invoke"
      pattern: "functions\\.invoke\\('send-support-ticket'"
    - from: "submission success"
      to: "tickets list"
      via: "TanStack invalidateQueries(['tickets'])"
      pattern: "invalidateQueries"
---

<objective>
Add in-app admin ticket submission: a `New Ticket` CTA in the Tickets section header opening a dialog (type bug|task, severity, description) that submits through the same `send-support-ticket` intake with context auto-attached, then refreshes the list.

Purpose: TKT-03 — admins file bugs and tasks where they'll be tracked, with the same context capture the support form already proves out. Single intake path (Edge Function pivoted in 11-02) keeps reporter attribution and the email side-effect consistent.
Output: NewTicketDialog wired into AdminTab, createTicket service + mutation, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-ticket-foundation-flag-removal/11-UI-SPEC.md (Copywriting Contract — dialog copy locked)
@.planning/phases/11-ticket-foundation-flag-removal/11-PATTERNS.md (dialog shell + context-capture analogs)
@src/services/support-ticket.service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: createTicket service + NewTicketDialog wired to the Tickets section CTA</name>
  <files>src/services/tickets.service.ts, src/hooks/useTickets.ts, src/components/settings/NewTicketDialog.tsx, src/components/settings/AdminTab.tsx</files>
  <read_first>src/services/support-ticket.service.ts, src/components/support/SupportTicketDialog.tsx, src/components/settings/AdminTab.tsx, .planning/phases/11-ticket-foundation-flag-removal/11-UI-SPEC.md</read_first>
  <behavior>
    - createTicket({type, severity, message}) builds the payload exactly like submitSupportTicket (url from window.location.href, userAgent, userId, organizationId, workspaceId, appVersion via VITE_APP_VERSION, commit via VITE_COMMIT_SHA fallback VITE_VERCEL_GIT_COMMIT_SHA) plus type and severity, invokes supabase.functions.invoke('send-support-ticket') and throws on error
    - submitting the dialog with empty description is blocked client-side
  </behavior>
  <action>Add createTicket to src/services/tickets.service.ts reusing the payload-builder approach from support-ticket.service.ts (import/borrow its getAppVersion/getCommit helpers or extract them into the new function — do not duplicate divergent copies; if extracting shared helpers, keep submitSupportTicket behavior identical). Pass type and severity in the body — the Edge Function schema was extended for these in plan 11-02. Add a useCreateTicket mutation in src/hooks/useTickets.ts invalidating ['tickets'] on success with toast.success('Ticket created') / toast.error('Ticket could not be created') per UI-SPEC. Create NewTicketDialog.tsx on the SupportTicketDialog shell analog: DialogContent sm:max-w-lg, title 'New Ticket', type Select limited to bug|task per TKT-03 (the enum supports more; the admin form intentionally offers two), severity Select (critical|high|medium|low default medium), description Textarea (required, maxLength 5000), submit Button labeled 'Create ticket' disabled while pending or description empty, resetForm + close on success. In AdminTab's Tickets section header add the right-aligned 'New Ticket' Button (variant default, leading RiAddLine) opening the dialog — per UI-SPEC focal-point contract.</action>
  <verify>
    <automated>rtk grep -n "functions.invoke('send-support-ticket'" src/services/tickets.service.ts ; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - createTicket exported from tickets.service.ts; payload includes url, userAgent, appVersion, commit, type, severity
    - NewTicketDialog offers exactly bug and task types and the four severities defaulting to medium
    - 'New Ticket' button renders in the Tickets section header with RiAddLine
    - Successful submit closes the dialog and the list refetches (invalidateQueries(['tickets']))
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Admin can create a bug or task ticket in-app with context auto-attached.</done>
</task>

<task type="auto">
  <name>Task 2: NewTicketDialog tests + phase-level regression sweep</name>
  <files>src/components/settings/__tests__/NewTicketDialog.test.tsx</files>
  <read_first>src/components/settings/__tests__/ (existing test style), src/components/settings/NewTicketDialog.tsx</read_first>
  <action>Write NewTicketDialog.test.tsx covering: renders type/severity/description fields with locked copy; submit disabled when description empty; successful submit calls createTicket with type and severity and shows the success path; failure shows error toast and keeps the dialog open. Then run the phase-level sweep: npm test (full unit suite), npm run build, and the FLAG-01 acceptance grep (rg -n "useFeatureFlags|isFeatureEnabled" src/ → 0 hits) to confirm no regressions across all four plans.</action>
  <verify>
    <automated>npx vitest run src/components/settings/__tests__/NewTicketDialog.test.tsx && npm test 2>&1 | tail -5 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - NewTicketDialog tests pass
    - Full `npm test` exits 0; `npm run build` exits 0
    - `rg -n "useFeatureFlags|isFeatureEnabled" src/` returns 0 matches
  </acceptance_criteria>
  <done>TKT-03 covered by tests; phase-wide build/test/grep green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → send-support-ticket | dialog payload crosses into the Edge Function (same boundary as 11-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Spoofing | admin submission reporter identity | mitigate | identical to T-11-04 — reporter_id from JWT inside the Edge Function; the dialog cannot set it |
| T-11-14 | Tampering | type/severity outside allowed values | mitigate | zod enums in the Edge Function (11-02) reject invalid values server-side; UI Selects are convenience, not control |
| T-11-15 | Denial of Service | rapid admin submissions | accept | admin-only surface behind isAdmin gate; rate limiting is ISA ISC-14, scoped to Phase 12 intake hardening, not TKT-01..04 |
| T-11-SC | Tampering | package installs | accept | No package installs this plan — slopcheck gate not applicable |
</threat_model>

<verification>
- All NewTicketDialog tests + full `npm test` + `npm run build` exit 0
- FLAG-01 grep returns 0 hits (phase-wide regression check)
- Execution-time dev-browser check: submit a task ticket, see it appear in the list, open detail and see the 'created' event
</verification>

<success_criteria>
Phase success criterion 4 satisfied: an admin submits a ticket in-app (bug or task) with context auto-attached and it is immediately visible in the list (TKT-03).
</success_criteria>

<output>
Create `.planning/phases/11-ticket-foundation-flag-removal/11-04-SUMMARY.md` when done
</output>
