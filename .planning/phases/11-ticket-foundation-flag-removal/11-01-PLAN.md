---
phase: 11-ticket-foundation-flag-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useFeatureFlags.ts
  - src/components/Layout.tsx
  - src/components/ui/sidebar-nav.tsx
  - src/components/settings/AdminTab.tsx
  - src/components/__tests__/Layout.test.tsx
  - src/components/ui/__tests__/sidebar-nav.test.tsx
  - supabase/migrations/20260611000001_drop_feature_flags.sql
autonomous: true
requirements: [FLAG-01]
must_haves:
  truths:
    - "Import and Rules nav items are visible to every user with no flag lookup"
    - "DebugPanel renders unconditionally in Layout (all gated surfaces hard-enabled)"
    - "AdminTab has no Feature Flags section and no Switch toggles for flags"
    - "npm test and npm run build pass with zero references to useFeatureFlags or isFeatureEnabled in src/"
    - "feature_flags table is dropped from the live database after push"
  artifacts:
    - path: "supabase/migrations/20260611000001_drop_feature_flags.sql"
      provides: "DROP TABLE IF EXISTS public.feature_flags"
      contains: "DROP TABLE IF EXISTS public.feature_flags"
  key_links:
    - from: "src/components/ui/sidebar-nav.tsx"
      to: "import/rules nav items"
      via: "unconditional visibility (gate removed)"
      pattern: "item.id === 'import'"
---

<objective>
Remove the dead feature-flag system end to end: delete the `useFeatureFlags` hook, remove the gates in `Layout.tsx` and `sidebar-nav.tsx`, delete the Feature Flags section from AdminTab, hard-enable every currently-gated surface, and drop the `feature_flags` table via a new migration pushed to the live database.

Purpose: FLAG-01 — clears the AdminTab surface BEFORE the tickets view lands there (roadmap ordering constraint), and removes a nonfunctional system that confuses the admin surface. Implements the locked decision "Feature-flag removal: hard-enable ALL currently-gated surfaces."
Output: flag-free frontend, drop migration applied, green build + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-ticket-foundation-flag-removal/11-CONTEXT.md
@.planning/phases/11-ticket-foundation-flag-removal/11-RESEARCH.md (FLAG-01 Removal Inventory — exact line sites)
@.planning/phases/11-ticket-foundation-flag-removal/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove flag system from frontend and hard-enable gated surfaces</name>
  <files>src/hooks/useFeatureFlags.ts, src/components/Layout.tsx, src/components/ui/sidebar-nav.tsx, src/components/settings/AdminTab.tsx, src/components/__tests__/Layout.test.tsx, src/components/ui/__tests__/sidebar-nav.test.tsx</files>
  <read_first>src/components/Layout.tsx, src/components/ui/sidebar-nav.tsx, src/components/settings/AdminTab.tsx, src/components/__tests__/Layout.test.tsx, src/components/ui/__tests__/sidebar-nav.test.tsx, .planning/phases/11-ticket-foundation-flag-removal/11-RESEARCH.md</read_first>
  <action>Delete src/hooks/useFeatureFlags.ts entirely. In Layout.tsx: remove the useFeatureFlags import (line 24) and hook call (line 37); change line 173 from the isFeatureEnabled('debug_panel') conditional to render DebugPanel unconditionally — per locked decision "hard-enable ALL currently-gated surfaces" (do not invent a replacement gate). In sidebar-nav.tsx: remove the useFeatureFlags import (line 35) and hook call (line 124); in the visibility callback (lines 127-131) make the 'import' and 'rules' items always visible and remove isFeatureEnabled from the dependency array. In AdminTab.tsx: delete the FeatureFlag interface (lines 50-57), featureFlags/updatingFlagId state (lines 77-79), loadFeatureFlags + its useEffect (lines 162-174), handleToggleFlag (lines 206-224), the entire Feature Flags JSX section including its leading Separator (lines 329-374), and the Switch import if no longer used. Update Layout.test.tsx and sidebar-nav.test.tsx: remove useFeatureFlags mocks and any assertions that surfaces are hidden when flags are off; assert import/rules items render and DebugPanel renders. Keep all non-flag behavior in all four components unchanged.</action>
  <verify>
    <automated>rtk grep -n "useFeatureFlags\|isFeatureEnabled" src/ ; npx vitest run src/components/__tests__/Layout.test.tsx src/components/ui/__tests__/sidebar-nav.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "useFeatureFlags|isFeatureEnabled" src/` returns 0 matches
    - `src/hooks/useFeatureFlags.ts` does not exist
    - Layout.tsx renders `<DebugPanel />` with no flag conditional
    - sidebar-nav.tsx contains no `isFeatureEnabled` reference; import/rules items unconditionally visible
    - AdminTab.tsx contains no string "Feature Flags" and no `feature_flags` query
    - `npx vitest run` on both test files exits 0; `npm run build` exits 0
  </acceptance_criteria>
  <done>Flag system gone from src/; gated surfaces hard-enabled; targeted tests + build green.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Drop feature_flags table via migration and push schema</name>
  <files>supabase/migrations/20260611000001_drop_feature_flags.sql</files>
  <read_first>supabase/migrations/20260302000000_feature_flags.sql, supabase/migrations/20260310160001_enable_beta_imports.sql, supabase/CLAUDE.md</read_first>
  <action>Create migration supabase/migrations/20260611000001_drop_feature_flags.sql following the supabase/CLAUDE.md migration header convention (Migration/Purpose/Date comment block citing FLAG-01). Content: DROP TABLE IF EXISTS public.feature_flags; (policies and seed rows drop with the table). Do NOT edit the historical migrations 20260302000000_feature_flags.sql or 20260310160001_enable_beta_imports.sql. Then run the BLOCKING schema push: supabase db push (non-TTY: ensure SUPABASE_ACCESS_TOKEN is set). This task is mandatory — build and type checks pass without the push (types come from files, not the live DB), which is a false-positive verification state. Do not regenerate src/types/supabase.ts in this plan — plan 11-02 regenerates types once after the ticket-tables migration.</action>
  <verify>
    <automated>supabase db push 2>&1 | tail -5 ; echo "SELECT to_regclass('public.feature_flags');" | supabase db query 2>/dev/null || echo "verify via supabase dashboard SQL: SELECT to_regclass('public.feature_flags') returns NULL"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists, contains `DROP TABLE IF EXISTS public.feature_flags`
    - `supabase db push` exits 0 and reports the migration applied
    - `SELECT to_regclass('public.feature_flags')` returns NULL on the linked project
  </acceptance_criteria>
  <done>feature_flags dropped from the live database; migration committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → removed gates | previously flag-hidden surfaces (Import, Rules, DebugPanel) become visible to all authenticated users |
| migration → live DB | DDL executes against production schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | DebugPanel hard-enabled in Layout.tsx | accept | Locked user decision "hard-enable ALL currently-gated surfaces"; DebugPanel contains no secrets (client-side state only) and the flag was already globally enableable. Recorded for verify-phase review. |
| T-11-02 | Elevation of Privilege | Import/Rules surfaces visible to all roles | accept | These surfaces were already enabled for all roles via the `beta_imports` seed (20260310160001); removal changes nothing in effective access. Underlying data access remains RLS-enforced. |
| T-11-03 | Tampering | drop migration on live DB | mitigate | `DROP TABLE IF EXISTS` only; no data the app still reads (all readers deleted in Task 1 first); migration committed to git for audit |
| T-11-SC | Tampering | package installs | accept | No package installs in this plan — slopcheck gate not applicable |
</threat_model>

<verification>
- `rg -n "useFeatureFlags|isFeatureEnabled" src/` → 0 hits
- `npm test` → exit 0 (full unit suite, no dead-flag test references)
- `npm run build` → exit 0
- `supabase db push` applied; `to_regclass('public.feature_flags')` IS NULL
</verification>

<success_criteria>
Phase success criterion 1 satisfied: flag table, hook, and all gates removed; gated surfaces hard-enabled; build and tests green with no dead-flag references (FLAG-01).
</success_criteria>

<output>
Create `.planning/phases/11-ticket-foundation-flag-removal/11-01-SUMMARY.md` when done
</output>
