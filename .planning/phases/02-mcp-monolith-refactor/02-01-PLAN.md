---
phase: 02-mcp-monolith-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/mcp-server/__tests__/golden-replay.test.ts
  - supabase/functions/mcp-server/__tests__/fixtures/golden-replay.json
  - supabase/functions/mcp-server/__tests__/contract-surface.test.ts
  - supabase/functions/mcp-server/__tests__/cold-start-baseline.test.ts
  - docs/operations/mcp-runbook.md
autonomous: true
requirements:
  - MCP-05
must_haves:
  truths:
    - "D-01/D-04: The restored monolith is the parity baseline and fixture replay exists before extraction."
    - "D-05/D-06: The test suite audits 41 current tools, `content[].text` tool responses, protocol JSON methods, and category coverage."
    - "D-09/D-10: Cold-start and live-token verification commands are documented before implementation claims success."
  artifacts:
    - path: "supabase/functions/mcp-server/__tests__/golden-replay.test.ts"
      provides: "Golden-master replay harness for protocol methods and representative tool calls"
    - path: "supabase/functions/mcp-server/__tests__/contract-surface.test.ts"
      provides: "Static contract audit for 41 tool definitions, outputSchema, categories, and top-level gate order"
    - path: "docs/operations/mcp-runbook.md"
      provides: "Reproducible Phase 2 verification commands"
  key_links:
    - "Golden fixtures freeze current monolith outputs before later plans move code."
    - "Contract-surface tests fail if tool count/category/outputSchema drift during extraction."
    - "Runbook commands make live and cold-start proof reproducible instead of inferred."
---

<objective>
Freeze the current MCP monolith behavior before any extraction. This plan implements D-01, D-04, D-05, D-06, D-09, and D-10 by adding parity fixtures, contract-surface assertions, and reproducible verification commands.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mcp-monolith-refactor/02-CONTEXT.md
@.planning/phases/02-mcp-monolith-refactor/02-RESEARCH.md
@.planning/phases/02-mcp-monolith-refactor/02-PATTERNS.md
@supabase/functions/mcp-server/index.ts
@supabase/functions/_shared/mcp-tool-categories.ts
@supabase/functions/mcp-server/__tests__/category-gating.test.ts
@supabase/functions/mcp-server/__tests__/ai-tools-invariants.test.ts
@supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts
@docs/operations/mcp-runbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the golden replay fixture harness</name>
  <files>supabase/functions/mcp-server/__tests__/golden-replay.test.ts, supabase/functions/mcp-server/__tests__/fixtures/golden-replay.json</files>
  <read_first>
    - .planning/phases/02-mcp-monolith-refactor/02-PATTERNS.md
    - supabase/functions/mcp-server/index.ts
    - supabase/functions/mcp-server/__tests__/category-gating.test.ts
    - supabase/functions/mcp-server/__tests__/write-tools-boundary.test.ts
  </read_first>
  <action>Create a Vitest source-level golden replay harness per D-04 without importing Deno URL modules directly. The fixture file must contain JSON-RPC examples for `initialize`, `tools/list`, and representative `tools/call` requests from read (`list_calls` or `search_calls`), write (`create_note` or `tag_call`), admin (`create_folder`), and ai (`ask_call` or `extract_action_items`). For calls that cannot run against a real DB in Vitest, assert the exact current source case block exists, the response path returns `mcpOk`, and the fixture expected envelope records `content[0].type === "text"` for tool calls while protocol methods remain structured JSON. Do not use archived modular files or commit `83693443` as source.</action>
  <acceptance_criteria>
    - Fixture entries include `initialize`, `tools/list`, one read tool, one write tool, one admin tool, and one AI tool.
    - The test fails if a fixture tool case is absent from `index.ts`.
    - The test asserts tool-call fixtures expect `content[].text` and protocol fixtures expect structured JSON.
    - The test file contains no imports from stale prior-run modules under `.planning/forensics/stale-prior-run-2026-05-27`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/golden-replay.test.ts</automated>
  </verify>
  <done>Golden replay coverage exists before any behavior-moving refactor begins.</done>
</task>

<task type="auto">
  <name>Task 2: Add current MCP contract surface checks</name>
  <files>supabase/functions/mcp-server/__tests__/contract-surface.test.ts</files>
  <read_first>
    - supabase/functions/mcp-server/index.ts
    - supabase/functions/_shared/mcp-tool-categories.ts
    - docs/operations/mcp-runbook.md
  </read_first>
  <action>Add a source-anchor test that audits D-05 and D-06. Parse the current `TOOLS` definitions from `index.ts` and `TOOL_CATEGORIES` from `_shared/mcp-tool-categories.ts`; assert there are exactly 41 tools, every tool has an `outputSchema` rooted at `{ type: "object" }` with required `text`, every tool has a category, and every category entry maps to a defined tool. Also assert top-level source order remains auth before protocol methods, protocol methods before plan gating, plan gating before category gating, and category gating before dispatch.</action>
  <acceptance_criteria>
    - `contract-surface.test.ts` fails if tool count differs from 41 without an intentional fixture update.
    - The test fails if any tool lacks category coverage or object-root `outputSchema`.
    - The test fails if `content[].text` helper shape is removed from `mcpOk`.
    - The test fails if `tools/list` moves ahead of token validation.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/contract-surface.test.ts supabase/functions/mcp-server/__tests__/category-gating.test.ts</automated>
  </verify>
  <done>The current 41-tool MCP surface is pinned by tests before extraction.</done>
</task>

<task type="auto">
  <name>Task 3: Document live and cold-start verification commands</name>
  <files>supabase/functions/mcp-server/__tests__/cold-start-baseline.test.ts, docs/operations/mcp-runbook.md</files>
  <read_first>
    - .planning/phases/02-mcp-monolith-refactor/02-CONTEXT.md
    - .planning/phases/02-mcp-monolith-refactor/02-RESEARCH.md
    - docs/operations/mcp-runbook.md
  </read_first>
  <action>Add a small source/doc test that asserts the runbook contains a Phase 2 section with exact commands for: invalid bearer 401 plus `WWW-Authenticate`, valid token `initialize`, valid token `tools/list`, valid token representative read tool, baseline cold-start capture before deploy, and candidate cold-start capture after deploy. The runbook must say Phase 2 cannot be marked fully verified unless the deployed candidate shows measured read-path cold-start improvement or the limitation is explicitly recorded as not verified per D-09. Do not include real token values.</action>
  <acceptance_criteria>
    - Runbook includes commands using the environment variable `CALLVAULT_MCP_TOKEN`, not a secret value.
    - Runbook includes `https://api.callvaultai.com/mcp`.
    - Runbook states invalid bearer must return HTTP 401 and `WWW-Authenticate`.
    - Runbook states cold-start improvement must be measured and cannot be inferred from build output.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/cold-start-baseline.test.ts</automated>
  </verify>
  <done>Execution has concrete verification commands before any refactor work can claim parity or cold-start success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| MCP client -> JSON-RPC endpoint | Untrusted method names and bearer tokens enter the Edge Function |
| test fixtures -> refactor execution | Fixtures become the guard against behavior drift |
| docs -> operator verification | Runbook commands are used to prove live behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01-01 | Tampering | Golden fixtures | mitigate | Store representative protocol/tool cases and fail when case blocks or response shapes drift. |
| T-02-01-02 | Information Disclosure | `tools/list` category map | mitigate | Contract test requires all 41 tools to map to `TOOL_CATEGORIES` so later filtering/gating cannot silently miss tools. |
| T-02-01-03 | Spoofing | Bearer-token handling | mitigate | Runbook and tests require invalid bearer 401 plus `WWW-Authenticate` before protocol introspection. |
| T-02-01-04 | Repudiation | Cold-start claims | mitigate | Require measured baseline/candidate commands; build output alone is not accepted evidence. |
| T-02-01-05 | Elevation of Privilege | Refactor parity | mitigate | Freeze plan-gating and category-gating order before moving handlers. |
</threat_model>

<verification>
Run:
`npm test -- --run supabase/functions/mcp-server/__tests__/golden-replay.test.ts supabase/functions/mcp-server/__tests__/contract-surface.test.ts supabase/functions/mcp-server/__tests__/cold-start-baseline.test.ts supabase/functions/mcp-server/__tests__/category-gating.test.ts`
</verification>

<success_criteria>
- MCP-05 has a parity harness before code extraction.
- Current production reality is recorded as 41 tools.
- Tool-call markdown and protocol JSON shapes are pinned.
- Live-token and cold-start verification commands are documented without secrets.
</success_criteria>

<output>
Create `.planning/phases/02-mcp-monolith-refactor/02-01-SUMMARY.md` when done.
</output>
