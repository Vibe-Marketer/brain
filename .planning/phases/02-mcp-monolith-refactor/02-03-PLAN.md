---
phase: 02-mcp-monolith-refactor
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - supabase/functions/mcp-server/index.ts
  - supabase/functions/mcp-server/tools/registry.ts
  - supabase/functions/mcp-server/tools/read/search_calls.ts
  - supabase/functions/mcp-server/tools/read/list_calls.ts
  - supabase/functions/mcp-server/tools/read/get_transcript.ts
  - supabase/functions/mcp-server/tools/read/get_recording_context.ts
  - supabase/functions/mcp-server/tools/read/list_workspaces.ts
  - supabase/functions/mcp-server/__tests__/golden-replay.test.ts
  - supabase/functions/mcp-server/__tests__/contract-surface.test.ts
autonomous: true
requirements:
  - MCP-05
must_haves:
  truths:
    - "D-04/D-06: A registry dispatcher exists and preserves the first read-tool slice behavior."
    - "D-05: `tools/list` uses module definitions without changing protocol output or category coverage."
  artifacts:
    - path: "supabase/functions/mcp-server/tools/registry.ts"
      provides: "Handler map and tool definition aggregation"
    - path: "supabase/functions/mcp-server/tools/read/*.ts"
      provides: "First modular read-tool slice"
  key_links:
    - "`index.ts` dispatches through `getToolModule(toolName)` for extracted tools and falls back only for not-yet-extracted inline cases."
    - "`tools/list` reads registry definitions while contract tests compare the 41-tool surface."
---

<objective>
Introduce the registry/handler-map pattern and extract the highest-volume read tools as the pilot slice. This plan implements D-04, D-05, D-06, and the first concrete MCP-05 modular-dispatch step.
</objective>

<execution_context>
@/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mcp-monolith-refactor/02-CONTEXT.md
@.planning/phases/02-mcp-monolith-refactor/02-PATTERNS.md
@supabase/functions/mcp-server/index.ts
@supabase/functions/mcp-server/protocol.ts
@supabase/functions/mcp-server/auth.ts
@supabase/functions/mcp-server/gating.ts
@supabase/functions/mcp-server/tools/_types.ts
@supabase/functions/_shared/mcp-tool-categories.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add registry dispatch without deleting inline fallback</name>
  <files>supabase/functions/mcp-server/index.ts, supabase/functions/mcp-server/tools/registry.ts, supabase/functions/mcp-server/tools/_types.ts</files>
  <read_first>
    - supabase/functions/mcp-server/index.ts
    - supabase/functions/mcp-server/tools/_types.ts
    - supabase/functions/_shared/mcp-tool-categories.ts
    - supabase/functions/mcp-server/__tests__/contract-surface.test.ts
  </read_first>
  <action>Create a registry that exports all tool definitions and a `getToolModule(toolName)` lookup. Initially register extracted read modules from this plan and preserve the inline switch for not-yet-extracted cases. `tools/list` must return the aggregated 41 definitions in the same order as current `TOOLS`; for not-yet-extracted tools, keep their definitions available through a temporary legacy definition export copied from current `TOOLS`, not by changing client-visible schema. The handler context must include `id`, `params`, `supabase`, `mcpToken`, `corsHeaders`, and any helpers currently shared by extracted cases.</action>
  <acceptance_criteria>
    - `tools/list` still exposes 41 definitions.
    - Extracted tools dispatch through the registry.
    - Not-yet-extracted tools still dispatch through the inline switch and behave as before.
    - No extracted or legacy definition loses object-root `outputSchema`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/contract-surface.test.ts supabase/functions/mcp-server/__tests__/golden-replay.test.ts</automated>
  </verify>
  <done>The registry exists and can coexist with remaining inline cases during incremental extraction.</done>
</task>

<task type="auto">
  <name>Task 2: Extract core read tools into modules</name>
  <files>supabase/functions/mcp-server/index.ts, supabase/functions/mcp-server/tools/read/search_calls.ts, supabase/functions/mcp-server/tools/read/list_calls.ts, supabase/functions/mcp-server/tools/read/get_transcript.ts, supabase/functions/mcp-server/tools/read/get_recording_context.ts, supabase/functions/mcp-server/tools/read/list_workspaces.ts, supabase/functions/mcp-server/tools/registry.ts</files>
  <read_first>
    - supabase/functions/mcp-server/index.ts case blocks for `search_calls`, `list_calls`, `get_transcript`, `get_recording_context`, `list_workspaces`
    - supabase/functions/mcp-server/protocol.ts
    - supabase/functions/mcp-server/tools/_types.ts
    - supabase/functions/mcp-server/__tests__/golden-replay.test.ts
  </read_first>
  <action>Move the exact current case bodies for `search_calls`, `list_calls`, `get_transcript`, `get_recording_context`, and `list_workspaces` into one-tool-per-file modules exporting `ToolModule`. Preserve current SQL table names, scope handling for organization versus workspace tokens, markdown formatting, no-result messages, and `mcpOk`/`mcpError` semantics. Do not change the UUID/BIGINT handling or source metadata assumptions while moving code. Register these modules and remove their inline cases only after the registry path is green.</action>
  <acceptance_criteria>
    - The five extracted read modules each export `definition`, `category: "read"`, and `handler`.
    - Inline cases for those five tools are removed only after registry dispatch works.
    - Golden replay covers at least one extracted read tool.
    - `TOOL_CATEGORIES` still maps all five names to `read`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/golden-replay.test.ts supabase/functions/mcp-server/__tests__/contract-surface.test.ts supabase/functions/mcp-server/__tests__/category-gating.test.ts</automated>
  </verify>
  <done>Core read tools are modularized through the registry with no client-visible response change.</done>
</task>

<task type="auto">
  <name>Task 3: Run the first extraction build/type gate decision</name>
  <files>supabase/functions/mcp-server/index.ts, supabase/functions/mcp-server/__tests__/contract-surface.test.ts</files>
  <read_first>
    - .planning/phases/02-mcp-monolith-refactor/02-CONTEXT.md D-07
    - supabase/functions/mcp-server/index.ts
  </read_first>
  <action>Run the targeted tests and `npm run build`. Attempt `deno check supabase/functions/mcp-server/index.ts`; if it passes, keep it as the Phase 2 Edge type gate in later plans. If it still fails because of pre-existing Supabase join typing or esm.sh provider/Zod drift, record the exact failure summary in the plan summary and ensure `contract-surface.test.ts`, targeted MCP tests, and `npm run build` remain the replacement gate per D-07 until a later plan fixes Deno check.</action>
  <acceptance_criteria>
    - `npm run build` exits 0 after the first extracted handler slice.
    - The summary records whether `deno check supabase/functions/mcp-server/index.ts` passed or remains an explicitly documented limitation.
    - Targeted MCP tests pass after registry pilot extraction.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run supabase/functions/mcp-server/__tests__/golden-replay.test.ts supabase/functions/mcp-server/__tests__/contract-surface.test.ts supabase/functions/mcp-server/__tests__/category-gating.test.ts && npm run build</automated>
  </verify>
  <done>The first registry extraction has a verified build/test gate and an explicit Deno check status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| registry -> tool module | Handler lookup determines which code runs for a validated request |
| tool definition -> client | `tools/list` exposes tool schemas to external MCP clients |
| read module -> Supabase | Read handlers enforce organization/workspace scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-03-01 | Tampering | `tools/registry.ts` | mitigate | Contract test requires 41 definitions and category coverage after registry introduction. |
| T-02-03-02 | Information Disclosure | read tool modules | mitigate | Preserve existing workspace/org filters from exact case blocks. |
| T-02-03-03 | Denial of Service | dispatch fallback | mitigate | Keep inline fallback only during incremental extraction and remove extracted cases after tests pass. |
| T-02-03-04 | Elevation of Privilege | category association | mitigate | Each module category must match `TOOL_CATEGORIES`. |
</threat_model>

<verification>
Run:
`npm test -- --run supabase/functions/mcp-server/__tests__/golden-replay.test.ts supabase/functions/mcp-server/__tests__/contract-surface.test.ts supabase/functions/mcp-server/__tests__/category-gating.test.ts && npm run build`
</verification>

<success_criteria>
- Registry dispatch is live for five read tools.
- `tools/list` remains client-compatible and still exposes 41 tools.
- Build and targeted MCP contract tests are green.
</success_criteria>

<output>
Create `.planning/phases/02-mcp-monolith-refactor/02-03-SUMMARY.md` when done.
</output>
