---
phase: 22-recurrence-structural-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - src/services/admin-dashboard.service.ts
  - src/services/__tests__/admin-dashboard.recurrence.test.ts
  - src/hooks/useAdminDashboard.ts
  - src/lib/query-config.ts
  - src/lib/ticket-display.ts
  - src/lib/__tests__/ticket-display.recurrence.test.ts
autonomous: true
requirements:
  - REC-01
  - REC-02
user_setup: []
must_haves:
  truths:
    - "D-04: Admin-facing code can read per-class recurrence metrics through service + hook separation."
    - "D-02: service models preserve structural task links so UI can show the class-root fix target."
    - "No component needs to query Supabase directly for ticket class metrics."
  artifacts:
    - path: "src/services/admin-dashboard.service.ts"
      provides: "typed `getTicketClassMetrics()` mapper over `ticket_class_metrics()`"
    - path: "src/hooks/useAdminDashboard.ts"
      provides: "TanStack Query hook for recurrence metrics"
    - path: "src/lib/query-config.ts"
      provides: "stable admin recurrence metrics query key"
    - path: "src/lib/ticket-display.ts"
      provides: "plain-English recurrence class/status labels"
  key_links:
    - from: "src/hooks/useAdminDashboard.ts"
      to: "src/services/admin-dashboard.service.ts"
      via: "queryFn"
      pattern: "useTicketClassMetrics.*getTicketClassMetrics"
    - from: "src/services/admin-dashboard.service.ts"
      to: "ticket_class_metrics"
      via: "supabase.rpc"
      pattern: "rpc\\(\"ticket_class_metrics\""
---

<objective>
Expose Phase 22 recurrence metrics through the Brain service, hook, query-key, and display helper layers.

Purpose: AdminTab must show recurrence-rate observability without violating the locked service + hook separation or leaking raw class keys into UI copy.
Output: Typed service/hook contracts and helper labels ready for the AdminTab UI plan.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-recurrence-structural-fix/22-CONTEXT.md
@.planning/phases/22-recurrence-structural-fix/22-PATTERNS.md
@src/CLAUDE.md
@src/services/admin-dashboard.service.ts
@src/hooks/useAdminDashboard.ts
@src/lib/query-config.ts
@src/lib/ticket-display.ts
</context>

<artifacts_this_phase_produces>
- Brain service/hook API for `ticket_class_metrics()`.
- Plain-English recurrence class labels and status helpers for AdminTab.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add service mapper and unit coverage for ticket class metrics</name>
  <files>src/services/admin-dashboard.service.ts, src/services/__tests__/admin-dashboard.recurrence.test.ts</files>
  <read_first>
    @src/services/admin-dashboard.service.ts
    @src/services/__tests__/admin-dashboard.service.test.ts
    @.planning/phases/22-recurrence-structural-fix/22-PATTERNS.md
  </read_first>
  <behavior>
    - `getTicketClassMetrics()` calls `ticket_class_metrics()` through the Supabase client and throws a descriptive error on RPC failure.
    - Mapper converts snake_case RPC rows to camelCase numbers/strings without `any`.
    - Structural task id, class key, source, error class, rates, counts, and status survive mapping.
  </behavior>
  <action>Add `TicketClassMetric` and `getTicketClassMetrics()` beside the existing source/trust metric patterns. Use `Database["public"]["Functions"]["ticket_class_metrics"]["Returns"][number]` from generated types. Keep the service pure async TypeScript with no React imports. Add a focused unit test with mocked Supabase RPC behavior; do not mock integration tests.</action>
  <verify>
    <automated>npm test -- src/services/__tests__/admin-dashboard.recurrence.test.ts</automated>
  </verify>
  <done>Service tests prove success and error mapping for recurrence metrics.</done>
  <acceptance_criteria>The AdminTab can consume recurrence metrics only through a typed service contract.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add recurrence query key, hook, and display helpers</name>
  <files>src/hooks/useAdminDashboard.ts, src/lib/query-config.ts, src/lib/ticket-display.ts, src/lib/__tests__/ticket-display.recurrence.test.ts</files>
  <read_first>
    @src/hooks/useAdminDashboard.ts
    @src/lib/query-config.ts
    @src/lib/ticket-display.ts
    @src/CLAUDE.md
  </read_first>
  <behavior>
    - `queryKeys.admin.ticketClassMetrics()` returns a stable admin key.
    - `useTicketClassMetrics()` wraps `getTicketClassMetrics()` with the same stale/refetch cadence as trust metrics.
    - Display helpers render raw class/status values as plain-English labels, including recurring, structural fix open, and killed states.
  </behavior>
  <action>Add the admin query key and hook using the existing `useAutopilotTrustMetrics()` pattern. Add recurrence helper functions to `ticket-display.ts` only if labels are needed outside the dashboard component; keep raw `class_key` and raw JSON out of user-visible copy. Use Remix-compatible type imports only and keep frontend AI code absent.</action>
  <verify>
    <automated>npm test -- src/lib/__tests__/ticket-display.recurrence.test.ts src/services/__tests__/admin-dashboard.recurrence.test.ts</automated>
  </verify>
  <done>Hook/query/display helpers exist and tests cover label fallbacks and recurrence metric service mapping.</done>
  <acceptance_criteria>D-04 recurrence observability is available to UI through the established service + hook boundary, with no component-side Supabase queries.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database RPC -> frontend service | Admin operational metrics enter browser code. |
| raw class fields -> UI labels | Machine class keys and status values are transformed into operator text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Information Disclosure | `getTicketClassMetrics()` | mitigate | Use only the admin-guarded RPC; do not query `ticket_classes` directly or expose unbounded context JSON. |
| T-22-06 | Tampering | Type mapping | mitigate | Use generated Supabase function row types and explicit numeric conversion helpers rather than `any`. |
| T-22-07 | Repudiation | Structural task links | accept | This plan only reads IDs; authoritative audit events are created in the DB/Autopilot plans. |
| T-22-SC | Tampering | npm installs | mitigate | No packages are installed; verify `git diff -- package.json package-lock.json` is clean. |
</threat_model>

<verification>
Overall checks: `npm test -- src/services/__tests__/admin-dashboard.recurrence.test.ts src/lib/__tests__/ticket-display.recurrence.test.ts`; `npm run type-check`; `git diff -- package.json package-lock.json`.
</verification>

<success_criteria>
Admin recurrence data is reachable through service + hook separation and carries all fields needed to surface class recurrence, structural task status, and before/after rate observability.
</success_criteria>

<output>
Create `.planning/phases/22-recurrence-structural-fix/22-02-SUMMARY.md` when done.
</output>
