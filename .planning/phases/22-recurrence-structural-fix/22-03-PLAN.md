---
phase: 22-recurrence-structural-fix
plan: 03
type: execute
wave: 3
depends_on:
  - 22-02
files_modified:
  - src/pages/admin/DashboardSection.tsx
  - src/pages/admin/__tests__/DashboardSection.recurrence.test.tsx
autonomous: true
requirements:
  - REC-01
  - REC-02
user_setup: []
must_haves:
  truths:
    - "D-04: AdminTab shows per-class recurrence rate before/after structural fixes."
    - "D-02: AdminTab links a recurring class to its structural-fix task rather than only listing instances."
    - "D-03: The UI frames structural fixes as admin-reviewed recommendations, not autonomous push actions."
  artifacts:
    - path: "src/pages/admin/DashboardSection.tsx"
      provides: "compact Recurrence Classes dashboard section"
    - path: "src/pages/admin/__tests__/DashboardSection.recurrence.test.tsx"
      provides: "component coverage for loading, empty, error, recurring, structural-open, and killed states"
  key_links:
    - from: "src/pages/admin/DashboardSection.tsx"
      to: "useTicketClassMetrics"
      via: "hook import"
      pattern: "useTicketClassMetrics"
    - from: "src/pages/admin/DashboardSection.tsx"
      to: "TicketClassMetric.structuralTicketId"
      via: "linked task display"
      pattern: "structuralTicketId"
---

<objective>
Add the AdminTab recurrence observability surface for Phase 22.

Purpose: Operators need to see which classes are recurring, whether a structural fix is open, and whether the class rate falls after the fix lands.
Output: A compact dashboard section with automated component coverage, using existing AdminTab patterns and no new workflow surface.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-recurrence-structural-fix/22-CONTEXT.md
@.planning/phases/22-recurrence-structural-fix/22-PATTERNS.md
@src/CLAUDE.md
@src/pages/admin/DashboardSection.tsx
@src/hooks/useAdminDashboard.ts
@src/lib/ticket-display.ts
</context>

<artifacts_this_phase_produces>
- AdminTab recurrence card/list showing class counts, rates, structural task link, and killed status.
- Component tests proving the surface remains opt-in, compact, and non-autonomous.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add component tests for recurrence states</name>
  <files>src/pages/admin/__tests__/DashboardSection.recurrence.test.tsx</files>
  <read_first>
    @src/pages/admin/DashboardSection.tsx
    @src/pages/admin/__tests__/DashboardSection.test.tsx
    @.planning/phases/22-recurrence-structural-fix/22-PATTERNS.md
  </read_first>
  <behavior>
    - Loading state renders stable skeleton rows.
    - Empty state says there are no recurring classes yet.
    - Error state says recurrence metrics failed to load and will retry.
    - Recurring class rows show current rate, baseline/post-fix rate when present, occurrence count, status, and structural task link.
    - No button or label implies autonomous structural auto-push.
  </behavior>
  <action>Add focused component tests by mocking `useTicketClassMetrics()` and existing AdminDashboard hooks as needed. Keep test fixtures small and use accessible text queries. The tests should fail before the UI is implemented.</action>
  <verify>
    <automated>npm test -- src/pages/admin/__tests__/DashboardSection.recurrence.test.tsx</automated>
  </verify>
  <done>Component tests encode D-04 observability and D-03 no-autopush copy before implementation.</done>
  <acceptance_criteria>Every AdminTab recurrence state has an automated assertion before UI code changes.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render Recurrence Classes in AdminTab</name>
  <files>src/pages/admin/DashboardSection.tsx</files>
  <read_first>
    @src/pages/admin/DashboardSection.tsx
    @src/CLAUDE.md
    @.planning/phases/22-recurrence-structural-fix/22-PATTERNS.md
  </read_first>
  <action>Add a compact recurrence section near the existing operational trust/source metrics sections. Use `useTicketClassMetrics()`, `SectionHeading`, existing Card/Skeleton patterns, Tailwind semantic tokens, tabular numerals for rates/counts, and Remix Icons only. Surface source/error class in plain English, current recurrence rate, baseline/post-fix rate, occurrence count, status, and structural task link/action if present. Keep the action opt-in and admin-review framed; do not add a new route, new settings flow, raw JSON, raw `class_key` as primary text, or any auto-push affordance.</action>
  <verify>
    <automated>npm test -- src/pages/admin/__tests__/DashboardSection.recurrence.test.tsx</automated>
  </verify>
  <done>AdminTab renders recurrence classes with loading/error/empty states and no autonomous structural-fix push control.</done>
  <acceptance_criteria>D-04 is visible in AdminTab, and D-03 is preserved by copy/actions that route structural fixes to admin review only.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run focused UI verification</name>
  <files>src/pages/admin/DashboardSection.tsx, src/pages/admin/__tests__/DashboardSection.recurrence.test.tsx</files>
  <read_first>
    @src/CLAUDE.md
    @.planning/phases/22-recurrence-structural-fix/22-VALIDATION.md
  </read_first>
  <action>Run the recurrence component test plus type-check. If a dev server and authenticated admin session are available, capture an AdminTab screenshot; if not, record the unavailable browser proof explicitly in the summary without claiming visual verification. Keep text within existing card dimensions and avoid introducing a landing-page or decorative layout.</action>
  <verify>
    <automated>npm test -- src/pages/admin/__tests__/DashboardSection.recurrence.test.tsx && npm run type-check</automated>
  </verify>
  <done>Focused tests and type-check pass; screenshot proof is captured or explicitly marked unavailable.</done>
  <acceptance_criteria>UI verification is evidence-backed and any missing browser proof is labeled SKIPPED rather than implied.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin metrics hook -> React UI | Operational recurrence data is displayed in browser. |
| Admin operator -> structural task | UI may navigate to or review a structural task, but not execute it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08 | Information Disclosure | `DashboardSection.tsx` | mitigate | Render bounded metric fields only; avoid raw context JSON and raw class keys as primary display. |
| T-22-09 | Elevation of Privilege | structural task action | mitigate | Provide review/navigation only; no direct merge, approve, push, or auto-fix mutation is introduced. |
| T-22-10 | Denial of Service | AdminTab rendering | mitigate | Use existing compact list/skeleton patterns and bounded metrics; no client-side recomputation over all tickets. |
| T-22-SC | Tampering | npm installs | mitigate | No packages are installed; verify `git diff -- package.json package-lock.json` is clean. |
</threat_model>

<verification>
Overall checks: `npm test -- src/pages/admin/__tests__/DashboardSection.recurrence.test.tsx`; `npm run type-check`; optional AdminTab screenshot when authenticated browser automation is available.
</verification>

<success_criteria>
AdminTab exposes recurrence-rate observability with a structural-fix link and no autonomous fix-push action. Operators can see whether a class is still recurring, has an open structural task, or has been killed after a fix lands.
</success_criteria>

<output>
Create `.planning/phases/22-recurrence-structural-fix/22-03-SUMMARY.md` when done.
</output>
