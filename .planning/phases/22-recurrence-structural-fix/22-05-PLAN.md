---
phase: 22-recurrence-structural-fix
plan: 05
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-04
files_modified:
  - /Users/admin/dev/autopilot/src/tier2.ts
  - /Users/admin/dev/autopilot/src/claimer.ts
  - /Users/admin/dev/autopilot/src/lib/db.ts
  - /Users/admin/dev/autopilot/src/tier2.test.ts
  - /Users/admin/dev/autopilot/src/claimer.test.ts
autonomous: true
requirements:
  - REC-01
  - REC-02
user_setup: []
must_haves:
  truths:
    - "D-01: recurrence class rollup runs on the existing Autopilot cadence without increasing concurrency or volume."
    - "D-02: recurring classes crossing threshold are visible to the tier-2 cycle as class-root structural tasks."
    - "D-03: cadence wiring never dispatches structural fixes into the tier-1 autonomous bug lane."
    - "D-04: rollup cadence updates rates so before/after AdminTab metrics stay current."
  artifacts:
    - path: "/Users/admin/dev/autopilot/src/tier2.ts"
      provides: "existing tier-2 cadence invokes Brain's recurrence rollup before digesting structural tasks"
    - path: "/Users/admin/dev/autopilot/src/lib/db.ts"
      provides: "typed Supabase RPC wrapper for `rollup_ticket_classes()`"
    - path: "/Users/admin/dev/autopilot/src/claimer.ts"
      provides: "guard that excludes structural task tickets from tier-1 claim/fix candidates"
    - path: "/Users/admin/dev/autopilot/src/tier2.test.ts"
      provides: "cadence and rollup invocation coverage"
    - path: "/Users/admin/dev/autopilot/src/claimer.test.ts"
      provides: "tier-1 structural task exclusion coverage"
  key_links:
    - from: "/Users/admin/dev/autopilot/src/tier2.ts"
      to: "public.rollup_ticket_classes"
      via: "Supabase service-role RPC"
      pattern: "rollup_ticket_classes"
    - from: "/Users/admin/dev/autopilot/src/claimer.ts"
      to: "tickets.context.structural_fix"
      via: "claim candidate exclusion"
      pattern: "structural_fix"
---

<objective>
Connect Brain's recurrence detector to the existing Autopilot tier-2 cadence and keep structural tasks out of the tier-1 bug lane.

Purpose: The recurring-class detector must refresh operationally, but Phase 22 must not raise volume, add a scheduler, or let structural fixes be claimed as ordinary autonomous bug fixes.
Output: Autopilot cadence wiring, DB wrapper, and claim guard tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-recurrence-structural-fix/22-CONTEXT.md
@.planning/phases/22-recurrence-structural-fix/22-RESEARCH.md
@docs/architecture/autopilot-brain-ownership.md
@/Users/admin/dev/autopilot/src/tier2.ts
@/Users/admin/dev/autopilot/src/claimer.ts
@/Users/admin/dev/autopilot/src/lib/db.ts
</context>

<artifacts_this_phase_produces>
- Existing tier-2 cadence refreshes recurrence rollups.
- Tier-1 claimer excludes structural-fix task tickets from autonomous bug-fix runs.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Autopilot cadence and claim-exclusion tests</name>
  <files>/Users/admin/dev/autopilot/src/tier2.test.ts, /Users/admin/dev/autopilot/src/claimer.test.ts</files>
  <read_first>
    @/Users/admin/dev/autopilot/src/tier2.ts
    @/Users/admin/dev/autopilot/src/claimer.ts
    @/Users/admin/dev/autopilot/src/lib/db.ts
    @.planning/phases/22-recurrence-structural-fix/22-RESEARCH.md
  </read_first>
  <behavior>
    - Tier-2 cadence calls `rollup_ticket_classes()` once before building digests.
    - A rollup failure is logged/deferred and does not increase concurrency or crash unrelated tier-2 processing.
    - Tier-1 claim selection excludes tickets where `type='task'` and `context.structural_fix` is present.
    - Non-structural bug tickets remain claimable.
  </behavior>
  <action>Add failing tests using the existing Autopilot mock DB/test patterns. Do not require a live Supabase connection. Tests must encode that recurrence refresh belongs to tier-2 cadence, not a new scheduler or tier-1 claim path.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && npm test -- src/tier2.test.ts src/claimer.test.ts</automated>
  </verify>
  <done>Tests fail until rollup cadence and structural claim exclusion exist.</done>
  <acceptance_criteria>D-01/D-04 cadence and D-03 no-tier-1 structural routing are pinned by automated tests.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invoke rollup from tier-2 and exclude structural tasks from tier-1 claims</name>
  <files>/Users/admin/dev/autopilot/src/tier2.ts, /Users/admin/dev/autopilot/src/claimer.ts, /Users/admin/dev/autopilot/src/lib/db.ts</files>
  <read_first>
    @/Users/admin/dev/autopilot/src/tier2.ts
    @/Users/admin/dev/autopilot/src/claimer.ts
    @/Users/admin/dev/autopilot/src/lib/db.ts
    @docs/architecture/autopilot-brain-ownership.md
  </read_first>
  <action>Add a narrow DB wrapper for `rollup_ticket_classes()` using existing Supabase service-role access. Call it from the existing tier-2 cadence before structural digests are collected. On failure, log and continue/defer according to existing tier-2 error patterns without claiming or pushing anything. Update tier-1 claim filtering so structural task tickets are not passed to the fix runner, preserving normal bug/source claim behavior and concurrency 1. Do not add launchd jobs, queues, npm packages, or run-cap changes.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && npm test -- src/tier2.test.ts src/claimer.test.ts && npm run typecheck</automated>
  </verify>
  <done>Tier-2 cadence refreshes recurrence state, and tier-1 claim code skips structural-fix task tickets while leaving ordinary tickets claimable.</done>
  <acceptance_criteria>REC-01 detection refresh and REC-02 structural escalation wiring operate through existing Autopilot cadence and cannot become autonomous bug-lane pushes.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Autopilot service role -> Brain RPC | Daemon invokes database rollup with write authority. |
| ticket queue -> tier-1 claimer | Structural tasks share the `tickets` table with ordinary bug tickets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-14 | Denial of Service | tier-2 rollup invocation | mitigate | Invoke once per existing tier-2 cycle, fail soft, and do not create a new scheduler or increase volume. |
| T-22-15 | Elevation of Privilege | tier-1 claimer | mitigate | Exclude `context.structural_fix` task tickets from autonomous runner claim candidates. |
| T-22-16 | Repudiation | rollup/escalation audit | mitigate | Rely on DB-created ticket_events and tier-2 digest events; preserve logs for rollup invocation failures. |
| T-22-SC | Tampering | npm installs | mitigate | No packages are installed; verify `git diff -- package.json package-lock.json` is clean in `/Users/admin/dev/autopilot`. |
</threat_model>

<verification>
Overall checks: `cd /Users/admin/dev/autopilot && npm test -- src/tier2.test.ts src/claimer.test.ts && npm run typecheck`; `git diff -- package.json package-lock.json`.
</verification>

<success_criteria>
The existing tier-2 cadence keeps recurrence classes current, structural-fix tasks are available for admin digest review, and tier-1 autonomous bug fixing cannot claim or push structural fixes.
</success_criteria>

<output>
Create `.planning/phases/22-recurrence-structural-fix/22-05-SUMMARY.md` when done.
</output>
