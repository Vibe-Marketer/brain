---
phase: 22-recurrence-structural-fix
plan: 05
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-04
files_modified:
  - /Users/admin/dev/autopilot/src/tier2.ts
  - /Users/admin/dev/autopilot/src/claimer.ts
  - /Users/admin/dev/autopilot/src/lib/db.ts
  - /Users/admin/dev/autopilot/src/tier2.test.ts
  - /Users/admin/dev/autopilot/src/claimer.test.ts
  - /Users/admin/dev/autopilot/src/lib/tier2.ts
  - /Users/admin/dev/autopilot/src/lib/tier2.test.ts
autonomous: true
requirements:
  - REC-01
  - REC-02
user_setup: []
must_haves:
  truths:
    - "D-01: recurrence class rollup runs on the existing Autopilot cadence without increasing concurrency or volume."
    - "D-02: recurring classes crossing threshold are visible to the tier-2 cycle as class-root structural tasks."
    - "D-03: cadence wiring never dispatches structural fixes into the tier-1 autonomous bug lane."
    - "D-04: rollup cadence updates rates so before/after AdminTab metrics stay current."
    - "D-03: structural-fix collection emits `tier2_digest_queued` and never `tier2_auto_fix_queued`."
  artifacts:
    - path: "/Users/admin/dev/autopilot/src/tier2.ts"
      provides: "existing tier-2 cadence invokes Brain's recurrence rollup before digesting structural tasks"
    - path: "/Users/admin/dev/autopilot/src/lib/db.ts"
      provides: "typed Supabase RPC wrapper for `rollup_ticket_classes()`"
    - path: "/Users/admin/dev/autopilot/src/claimer.ts"
      provides: "guard that excludes structural task tickets from tier-1 claim/fix candidates"
    - path: "/Users/admin/dev/autopilot/src/tier2.test.ts"
      provides: "cadence and rollup invocation coverage"
    - path: "/Users/admin/dev/autopilot/src/claimer.test.ts"
      provides: "tier-1 structural task exclusion coverage"
    - path: "/Users/admin/dev/autopilot/src/lib/tier2.test.ts"
      provides: "tier-2 digest-entry event coverage proving structural classes queue for admin approval only"
  key_links:
    - from: "/Users/admin/dev/autopilot/src/tier2.ts"
      to: "public.rollup_ticket_classes"
      via: "Supabase service-role RPC"
      pattern: "rollup_ticket_classes"
    - from: "/Users/admin/dev/autopilot/src/claimer.ts"
      to: "tickets.context.structural_fix"
      via: "claim candidate exclusion"
      pattern: "structural_fix"
    - from: "/Users/admin/dev/autopilot/src/lib/tier2.ts"
      to: "ticket_events.event_type"
      via: "structural-fix digest collection"
      pattern: "tier2_digest_queued"
---

<objective>
Connect Brain's recurrence detector to the existing Autopilot tier-2 cadence and keep structural tasks out of the tier-1 bug lane.

Purpose: The recurring-class detector must refresh operationally, but Phase 22 must not raise volume, add a scheduler, or let structural fixes be claimed as ordinary autonomous bug fixes.
Output: Autopilot cadence wiring, DB wrapper, and claim guard tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-recurrence-structural-fix/22-CONTEXT.md
@.planning/phases/22-recurrence-structural-fix/22-RESEARCH.md
@docs/architecture/autopilot-brain-ownership.md
@/Users/admin/dev/autopilot/src/tier2.ts
@/Users/admin/dev/autopilot/src/claimer.ts
@/Users/admin/dev/autopilot/src/lib/db.ts
</context>

<artifacts_this_phase_produces>
- Existing tier-2 cadence refreshes recurrence rollups.
- Tier-1 claimer excludes structural-fix task tickets from autonomous bug-fix runs.
- Structural-fix task collection queues a tier-2/admin digest entry with a solution-shaped message and an audit event named `tier2_digest_queued`, never `tier2_auto_fix_queued`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Autopilot cadence and claim-exclusion tests</name>
  <files>/Users/admin/dev/autopilot/src/tier2.test.ts, /Users/admin/dev/autopilot/src/claimer.test.ts</files>
  <read_first>
    @/Users/admin/dev/autopilot/src/tier2.ts
    @/Users/admin/dev/autopilot/src/claimer.ts
    @/Users/admin/dev/autopilot/src/lib/db.ts
    @.planning/phases/22-recurrence-structural-fix/22-RESEARCH.md
  </read_first>
  <behavior>
    - Tier-2 cadence calls `rollup_ticket_classes()` once before building digests.
    - A rollup failure is logged/deferred and does not increase concurrency or crash unrelated tier-2 processing.
    - Tier-1 claim selection excludes tickets where `type='task'` and `context.structural_fix` is present.
    - Non-structural bug tickets remain claimable.
    - Structural-fix task collection emits `tier2_digest_queued`, creates or preserves a solution-shaped tier-2 message, and never emits `tier2_auto_fix_queued`.
  </behavior>
  <action>Add failing tests using the existing Autopilot mock DB/test patterns. Do not require a live Supabase connection. Tests must encode that recurrence refresh belongs to tier-2 cadence, not a new scheduler or tier-1 claim path. Add a digest-lane test proving a detected class/structural task produces a tier-2 digest entry with event type exactly `tier2_digest_queued`, not `tier2_auto_fix_queued`, and with message content that states what the class is, why it recurs, the proposed structural fix, and two or three admin options.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && npm test -- src/tier2.test.ts src/claimer.test.ts</automated>
  </verify>
  <done>Tests fail until rollup cadence, structural claim exclusion, and tier-2 digest queue emission exist.</done>
  <acceptance_criteria>D-01/D-04 cadence, D-02 structural class visibility, and D-03 no-tier-1/no-auto-fix structural routing are pinned by automated tests.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invoke rollup from tier-2 and exclude structural tasks from tier-1 claims</name>
  <files>/Users/admin/dev/autopilot/src/tier2.ts, /Users/admin/dev/autopilot/src/claimer.ts, /Users/admin/dev/autopilot/src/lib/db.ts</files>
  <read_first>
    @/Users/admin/dev/autopilot/src/tier2.ts
    @/Users/admin/dev/autopilot/src/claimer.ts
    @/Users/admin/dev/autopilot/src/lib/db.ts
    @docs/architecture/autopilot-brain-ownership.md
  </read_first>
  <action>Add a narrow DB wrapper for `rollup_ticket_classes()` using existing Supabase service-role access. Call it from the existing tier-2 cadence before structural digests are collected. On failure, log and continue/defer according to existing tier-2 error patterns without claiming or pushing anything. Update tier-1 claim filtering so structural task tickets are not passed to the fix runner, preserving normal bug/source claim behavior and concurrency 1. Keep structural-fix digest collection on the tier-2/admin-approval lane only; this task may call the Plan 04 digest builder but must not emit `tier2_auto_fix_queued`. Do not add launchd jobs, queues, npm packages, or run-cap changes.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && npm test -- src/tier2.test.ts src/claimer.test.ts && npm run typecheck</automated>
  </verify>
  <done>Tier-2 cadence refreshes recurrence state, and tier-1 claim code skips structural-fix task tickets while leaving ordinary tickets claimable.</done>
  <acceptance_criteria>REC-01 detection refresh and REC-02 structural escalation wiring operate through existing Autopilot cadence and cannot become autonomous bug-lane pushes.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Queue structural tasks into the tier-2 digest lane only</name>
  <files>/Users/admin/dev/autopilot/src/lib/tier2.ts, /Users/admin/dev/autopilot/src/lib/tier2.test.ts, /Users/admin/dev/autopilot/src/tier2.ts, /Users/admin/dev/autopilot/src/tier2.test.ts</files>
  <read_first>
    @/Users/admin/dev/autopilot/src/lib/tier2.ts
    @/Users/admin/dev/autopilot/src/tier2.ts
    @/Users/admin/dev/autopilot/src/lib/db.ts
    @.planning/phases/22-recurrence-structural-fix/22-04-PLAN.md
    @.planning/phases/22-recurrence-structural-fix/22-PATTERNS.md
  </read_first>
  <behavior>
    - When Plan 01-created structural tasks are collected by the tier-2 cadence, Autopilot writes a `ticket_events` row with `event_type='tier2_digest_queued'`.
    - The collected structural task gets a solution-shaped `ticket_messages` agent/admin digest body that states what the class is, why it recurs, the proposed structural fix, and two or three a/b/c options with one recommendation.
    - Tests assert no structural-fix path writes or routes through `tier2_auto_fix_queued`.
  </behavior>
  <action>Wire structural task collection so every recurring-class structural task created by Plan 01 is queued into the existing tier-2/admin-approval digest lane. If Plan 01 already writes the initial `tier2_digest_queued` event/message during task creation, this task must treat collection as idempotent and preserve the digest entry; otherwise, collection must create it before the digest is delivered. Use the Plan 04 solution-shaped formatter where available. Assert and implement the event name as exactly `tier2_digest_queued`; structural fixes are admin-approval only per D-03 and must never emit `tier2_auto_fix_queued`, never call the auto-fix runner, and never auto-push.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && npm test -- src/lib/tier2.test.ts src/tier2.test.ts src/claimer.test.ts && npm run typecheck</automated>
  </verify>
  <done>A detected recurring class produces a tier-2 digest entry and solution-shaped message, and automated tests prove no structural-fix path emits `tier2_auto_fix_queued`.</done>
  <acceptance_criteria>D-02 structural task visibility and D-03 admin-approval-only routing are both enforced at the actual tier-2 collection point.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Autopilot service role -> Brain RPC | Daemon invokes database rollup with write authority. |
| ticket queue -> tier-1 claimer | Structural tasks share the `tickets` table with ordinary bug tickets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-14 | Denial of Service | tier-2 rollup invocation | mitigate | Invoke once per existing tier-2 cycle, fail soft, and do not create a new scheduler or increase volume. |
| T-22-15 | Elevation of Privilege | tier-1 claimer | mitigate | Exclude `context.structural_fix` task tickets from autonomous runner claim candidates. |
| T-22-16 | Repudiation | rollup/escalation audit | mitigate | Rely on DB-created ticket_events and tier-2 digest events; preserve logs for rollup invocation failures. |
| T-22-17 | Elevation of Privilege | structural-fix digest collection | mitigate | Emit `tier2_digest_queued` with a solution-shaped message and assert no structural path emits `tier2_auto_fix_queued` or invokes auto-push. |
| T-22-SC | Tampering | npm installs | mitigate | No packages are installed; verify `git diff -- package.json package-lock.json` is clean in `/Users/admin/dev/autopilot`. |
</threat_model>

<verification>
Overall checks: `cd /Users/admin/dev/autopilot && npm test -- src/lib/tier2.test.ts src/tier2.test.ts src/claimer.test.ts && npm run typecheck`; `git diff -- package.json package-lock.json`.
</verification>

<success_criteria>
The existing tier-2 cadence keeps recurrence classes current, structural-fix tasks are available for admin digest review through `tier2_digest_queued`, and tier-1 autonomous bug fixing cannot claim, auto-queue, or push structural fixes.
</success_criteria>

<output>
Create `.planning/phases/22-recurrence-structural-fix/22-05-SUMMARY.md` when done.
</output>
