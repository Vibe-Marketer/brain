---
phase: 22-recurrence-structural-fix
plan: 04
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - /Users/admin/dev/autopilot/src/lib/tier2.ts
  - /Users/admin/dev/autopilot/src/lib/approval.ts
  - /Users/admin/dev/autopilot/src/lib/tier2.test.ts
  - /Users/admin/dev/autopilot/src/lib/approval.test.ts
autonomous: true
requirements:
  - REC-02
user_setup: []
must_haves:
  truths:
    - "D-03: structural fixes always route to tier-2/admin approval and never to autonomous auto-push."
    - "D-02: the tier-2 digest targets the recurring class root and not a single ticket instance."
    - "Operator-facing output is solution-shaped: what the class is, why it recurs, one proposed fix, and 2-3 options with exactly one recommendation."
  artifacts:
    - path: "/Users/admin/dev/autopilot/src/lib/tier2.ts"
      provides: "structural-fix digest builder and manual/digest route override"
    - path: "/Users/admin/dev/autopilot/src/lib/approval.ts"
      provides: "approval guard ensuring structural tasks cannot enter auto approval"
    - path: "/Users/admin/dev/autopilot/src/lib/tier2.test.ts"
      provides: "digest shape and route override coverage"
    - path: "/Users/admin/dev/autopilot/src/lib/approval.test.ts"
      provides: "manual-only structural task approval routing coverage"
  key_links:
    - from: "/Users/admin/dev/autopilot/src/lib/tier2.ts"
      to: "ticket_classes structural task context"
      via: "Supabase ticket context"
      pattern: "structural_fix"
    - from: "/Users/admin/dev/autopilot/src/lib/approval.ts"
      to: "autopilot trust ladder"
      via: "manual override before auto approval"
      pattern: "auto.*structural"
---

<objective>
Wire Autopilot's tier-2 lane so recurring classes become structural-fix recommendations, never autonomous pushes.

Purpose: REC-02 is only safe if the daemon treats class-root fixes as higher-blast-radius work requiring tier-2/admin approval, regardless of per-category trust rung.
Output: Autopilot tests and implementation for structural digest formatting and manual-only routing.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-recurrence-structural-fix/22-CONTEXT.md
@.planning/phases/22-recurrence-structural-fix/22-RESEARCH.md
@.planning/design/escalation-tier2-solutions-not-problems.md
@docs/architecture/autopilot-brain-ownership.md
@/Users/admin/dev/autopilot/src/lib/tier2.ts
@/Users/admin/dev/autopilot/src/lib/approval.ts
@/Users/admin/dev/autopilot/src/lib/trust.ts
</context>

<artifacts_this_phase_produces>
- Autopilot structural-fix tier-2 digest builder.
- Manual/admin-only routing guard for structural-fix tasks even when trust rung is auto.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add structural digest tests and manual-route assertions</name>
  <files>/Users/admin/dev/autopilot/src/lib/tier2.test.ts, /Users/admin/dev/autopilot/src/lib/approval.test.ts</files>
  <read_first>
    @/Users/admin/dev/autopilot/src/lib/tier2.ts
    @/Users/admin/dev/autopilot/src/lib/approval.ts
    @/Users/admin/dev/autopilot/src/lib/trust.ts
    @.planning/design/escalation-tier2-solutions-not-problems.md
  </read_first>
  <behavior>
    - Structural digest summary is one or two sentences and passes `validateTier2Digest()`.
    - Structural digest contains 2-3 decisions and exactly one recommended option.
    - Structural route returns digest/manual route even when a matching category trust rung is auto.
    - Approval code refuses to auto-approve tickets carrying `context.structural_fix`.
  </behavior>
  <action>Add tests before implementation in the Autopilot repo. Fixture tickets should use `type='task'`, `source='internal'`, `status='escalated'`, and `context.structural_fix` fields produced by Plan 01. Tests must not require new packages or a live Supabase connection.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && npm test -- src/lib/tier2.test.ts src/lib/approval.test.ts</automated>
  </verify>
  <done>Autopilot tests fail until structural digest/manual-route logic exists.</done>
  <acceptance_criteria>D-03 is pinned by tests that fail if a structural fix can use the auto-fix path.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement structural-fix tier-2 digest and auto-approval override</name>
  <files>/Users/admin/dev/autopilot/src/lib/tier2.ts, /Users/admin/dev/autopilot/src/lib/approval.ts</files>
  <read_first>
    @/Users/admin/dev/autopilot/src/lib/tier2.ts
    @/Users/admin/dev/autopilot/src/lib/approval.ts
    @.planning/phases/22-recurrence-structural-fix/22-RESEARCH.md
  </read_first>
  <action>Add a structural-fix digest path that reads `context.structural_fix`, names the recurring class, explains why it recurs using source/error/fingerprint class context, proposes the class-root fix, and emits two or three admin decisions with exactly one recommendation. Force this path to tier-2 digest/manual route regardless of trust rung or category eligibility. Add an approval guard so structural task tickets are not auto-approved by the autonomy ladder. Do not push, merge, invoke the fix agent, or create a separate queue.</action>
  <verify>
    <automated>cd /Users/admin/dev/autopilot && npm test -- src/lib/tier2.test.ts src/lib/approval.test.ts && npm run typecheck</automated>
  </verify>
  <done>Structural-fix tickets render solution-shaped tier-2 digests and cannot enter autonomous auto-push.</done>
  <acceptance_criteria>D-02 class-root targeting and D-03 admin-only structural-fix routing are enforced in Autopilot code and tests.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase task context -> Autopilot digest | DB-stored class data becomes operator-facing recommendation text. |
| trust ladder -> approval route | Per-category auto trust could otherwise influence structural tasks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-11 | Elevation of Privilege | `approval.ts` auto approval | mitigate | Explicitly detect `context.structural_fix` and force manual/admin route before trust-rung auto approval. |
| T-22-12 | Information Disclosure | `tier2.ts` digest text | mitigate | Summarize class/source/error/rates only; do not include raw stack traces, paths, secrets, or full ticket context. |
| T-22-13 | Tampering | `context.structural_fix` input | mitigate | Treat DB context as untrusted; validate object shape and fall back to safe labels if fields are missing or malformed. |
| T-22-SC | Tampering | npm installs | mitigate | No packages are installed; verify `git diff -- package.json package-lock.json` is clean in `/Users/admin/dev/autopilot`. |
</threat_model>

<verification>
Overall checks: `cd /Users/admin/dev/autopilot && npm test -- src/lib/tier2.test.ts src/lib/approval.test.ts && npm run typecheck`; `git diff -- package.json package-lock.json`.
</verification>

<success_criteria>
REC-02 is satisfied in Autopilot when a recurring-class structural task produces a validated tier-2 digest and is blocked from autonomous auto-push regardless of trust level.
</success_criteria>

<output>
Create `.planning/phases/22-recurrence-structural-fix/22-04-SUMMARY.md` when done.
</output>
