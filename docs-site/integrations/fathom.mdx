---
title: "Fathom Integration"
description: "Connect one or more Fathom accounts to automatically sync your meeting recordings and transcripts to CallVault."
icon: "plug"
---

Fathom is an AI meeting notetaker that records and transcribes your calls. When you connect Fathom to CallVault, your recordings sync automatically — no manual uploads needed.

You can connect **multiple Fathom accounts** to a single CallVault account — useful if you record calls under different email addresses or work across multiple teams.

## Connecting Fathom

<Steps>
  <Step title="Open the Import page">
    In CallVault, go to **Import** and select **Fathom** from the source list.
  </Step>
  <Step title="Click Connect Fathom">
    You'll be redirected to Fathom to authorize the connection.
  </Step>
  <Step title="Sign in to Fathom and authorize">
    Log in with your Fathom credentials and grant CallVault permission to access your recordings.
  </Step>
  <Step title="Return to CallVault">
    After authorization, you're redirected back to CallVault. Your Fathom account appears in the Import page, ready to browse and import calls.
  </Step>
  <Step title="Select calls to import">
    Pick a date range, browse your Fathom recordings, and select the calls you want to bring into CallVault. Choose a destination workspace before importing.
  </Step>
</Steps>

## Connecting multiple accounts

If you use more than one Fathom account, you can link them all to CallVault:

1. On the **Import → Fathom** page, click **Add Account** in the top-right corner
2. Complete the OAuth flow for the additional Fathom account
3. Once connected, use the **account tabs** at the top of the Fathom import panel to switch between accounts

Each account maintains its own connection and credentials. You can import calls from any connected account into any workspace.

## What syncs automatically

Once connected, the following data flows from Fathom into CallVault for each meeting:

- **Recording** — audio/video of the meeting
- **Transcript** — Fathom's transcript is imported (CallVault may re-transcribe for consistency)
- **Meeting title** — used as the call name in CallVault
- **Date and duration**
- **Participants** — attendee names and emails when available

## Where synced calls land

New calls from Fathom land in your **My Calls** workspace by default. Use [Routing Rules](/automation/routing-rules) to automatically send calls to specific workspaces or folders based on meeting title, participants, or duration.

## Managing your connections

To view or disconnect a Fathom account:

1. Go to **Import → Fathom**
2. If you have multiple accounts, select the one you want to manage using the account tabs
3. Click **Disconnect** (or **Disconnect this account** when multiple accounts are connected)

Disconnecting stops future syncs for that account but does not delete calls that have already been imported. Your other connected Fathom accounts are not affected.

## Troubleshooting

<AccordionGroup>
  <Accordion title="My calls aren't showing up after connecting">
    The initial import can take a few minutes for large libraries. Check **Settings → Integrations → Fathom** for the sync status. If the import shows as complete but calls are missing, try disconnecting and reconnecting.
  </Accordion>
  <Accordion title="New calls from Fathom aren't syncing">
    Make sure the Fathom integration is still connected — check **Settings → Integrations**. If your Fathom password changed recently, you may need to reconnect.
  </Accordion>
  <Accordion title="A call appears twice">
    This can happen if a call was imported via Fathom and also uploaded manually. Delete the duplicate call from the call's **⋯** menu.
  </Accordion>
  <Accordion title="I want calls to go to a specific workspace, not My Calls">
    Set up a [Routing Rule](/automation/routing-rules) with a condition matching your meeting titles and an action to assign the target workspace and folder.
  </Accordion>
  <Accordion title="I connected a second Fathom account but don't see the account tabs">
    Account tabs appear when two or more Fathom accounts are connected. Make sure both accounts completed the OAuth flow successfully. If an account shows as disconnected, click **Add Account** and re-authorize it.
  </Accordion>
</AccordionGroup>
