# Phase 14: Foundation

**Type:** New repo scaffold from scratch

**Goal:** Create the callvault v2 repo, get auth working, render the AppShell with spring physics, wire all non-AI routes to placeholder content, and deploy to Vercel. No real data — routes show placeholders.

---

## Plan 01 — Scaffold Repo + Color System

### Initialize the Repository

- [ ] Create new repo at `/Users/Naegele/dev/callvault/` (GitHub: `Vibe-Marketer/callvault`, private)
- [ ] Use pnpm as package manager (brain repo uses npm; callvault uses pnpm)
- [ ] Install exact versions of the locked stack:
  - Vite 7 with `@vitejs/plugin-react-swc`
  - React 19 + TypeScript 5.9
  - TanStack Router v1 (file-based routing)
  - TailwindCSS v4 (CSS-first config — `@theme {}` in globals.css, NO tailwind.config.js)
  - motion v12 (import from `"motion/react"`, NOT `framer-motion`)
  - Zustand v5 (double-invocation pattern: `create<T>()(` for TypeScript)
  - TanStack Query v5
  - @supabase/supabase-js v2
  - @remixicon/react (Remix Icons ONLY — no Lucide, FontAwesome)
  - zod, date-fns, driver.js (onboarding tour library)
- [ ] Add `routeTree.gen.ts` to `.gitignore` (auto-generated by TanStack Router plugin on `pnpm dev`)

### Port the Color System

- [ ] Create `src/globals.css` with full color system ported from v1
- [ ] Use `hsl()` wrapping for all CSS variables — required for TailwindCSS v4 + shadcn/ui (v3 used raw HSL numbers, v4 requires `hsl()`)
- [ ] Configure shadcn/ui (New York style, TanStack Router adapter)

### Known Pitfalls to Handle

- [ ] shadcn init requires a pre-existing `globals.css` with `@import "tailwindcss"` AND root `tsconfig.json` path alias — create these first
- [ ] React 19: `import * as React` is not needed with JSX transform — use named imports only
- [ ] TailwindCSS v4: `@apply` only works with Tailwind utilities, not custom classes — inline utilities instead

---

## Plan 02 — Auth Foundation

### Connect to Existing Supabase Project

- [ ] Point at the existing Supabase project (`vltmrnjsubfzrgrtdqey` — same as v1)
- [ ] Generate fresh TypeScript types via `supabase gen types typescript`
- [ ] Create `src/lib/supabase.ts` with Supabase client

### Build Auth Flow

- [ ] Create `src/hooks/useAuth.tsx` with `signInWithOAuth` for Google OAuth
- [ ] Create `src/routes/_authenticated.tsx` as a pathless layout route with `beforeLoad` guard — all authenticated routes nest under this
- [ ] Use `createRootRouteWithContext` for passing auth context to route `beforeLoad` handlers
- [ ] Add `http://localhost:3000/oauth/callback` to Supabase redirect URL allowlist

### Known Pitfall

- [ ] Google OAuth may fail with `redirect_uri_mismatch` if Supabase uses custom auth domain (`auth.callvaultai.com`). Need to add `https://auth.callvaultai.com/auth/v1/callback` to Google Console authorized redirect URIs.

---

## Plan 03 — AppShell (4-Pane Layout)

### Build the Shell

- [ ] Create `src/components/app-shell/AppShell.tsx` implementing the 4-pane layout
- [ ] Define the spring constant: `const SPRING = { type: 'spring', stiffness: 260, damping: 28 } as const`
- [ ] Add `initial={false}` on ALL Motion elements to suppress entry animation on page load
- [ ] Pane 4 behavior: slides in while Pane 3 shrinks to make room — all panes on the SAME z-index plane, NO drawer overlays

### Define AppShell Interface

- [ ] `AppShellProps`: children, secondaryPane (optional), showDetailPane (boolean)
- [ ] `PanelData` as discriminated union with `type` field for type-safe narrowing

### State Management

- [ ] Create `src/stores/preferencesStore.ts` (Zustand v5):
  - `isSidebarExpanded` — persists to localStorage
  - `isSecondaryOpen` — session-only (no persistence)
- [ ] Mobile behavior: route-based stack navigation (not overlay/drawer)
- [ ] Tablet auto-collapse: `useEffect` watching `isTablet` calls `setSidebarExpanded(false)`

### Layout Constants

- [ ] Sidebar: 220px expanded / 72px collapsed
- [ ] Secondary pane: 280px
- [ ] Main pane: flex-1
- [ ] Detail pane: 360/320px

---

## Plan 04 — Route Wiring + Query Config

### Wire All Routes

Create 9 authenticated routes under `_authenticated/` with placeholder content:

- [ ] `/` — Home / All Calls
- [ ] `/calls/$callId` — Call Detail
- [ ] `/workspaces` — Workspace List
- [ ] `/workspaces/$workspaceId` — Workspace Detail
- [ ] `/import` — Import Calls
- [ ] `/sharing` — Shared With Me
- [ ] `/settings` — Settings (redirects to `/settings/account` via `beforeLoad`)
- [ ] `/settings/$category` — Settings Category
- [ ] `/join/workspace/$token` — Join Workspace (invite acceptance)

Create public routes:

- [ ] `/s/$token` — Public Share View

Create v1 redirect blockers:

- [ ] `/bank`, `/vault`, `/hub` — redirect to new paths

### Import Hub Placeholder

- [ ] Show Fathom, Zoom, YouTube, and File Upload cards
- [ ] NO Google Meet card — FOUND-09 hard constraint: removed from v2 entirely

### Query Config

- [ ] Create `src/lib/query-config.ts` with `queryKeys` factory covering 7 domains: calls, workspaces, organizations, folders, imports, sharing, user
- [ ] Add FOUND-06 hard boundary comment: "TanStack Query for server data, Zustand for UI state only"

---

## Plan 05 — Deploy to Vercel

### Production Deployment

- [ ] Push to GitHub repo `Vibe-Marketer/callvault`
- [ ] Create `.github/workflows/ci.yml` for CI pipeline
- [ ] Create `vercel.json` with `"framework": "vite"` + catch-all rewrite (needed for SPA client-side routing — without this, direct navigation to routes returns Vercel 404)
- [ ] Deploy to `https://callvault.vercel.app`
- [ ] Verify auth works end-to-end on production URL
